# Codex 실기 감사 보고서 교차검증 (Claude Deployer, 2026-08-09 16:0x)

대상: [CODEX_SIM2REAL_REAL_TEST_AUDIT_20260809.md](CODEX_SIM2REAL_REAL_TEST_AUDIT_20260809.md)
§19 체크리스트 12항목. 재계산 코드는 순수 파이썬 독립 구현
(scratchpad `codex_audit_recompute.py`, 문서 §1.3 정의 그대로: 정책률
`1/mean(tick_dt[1:])`, 백분위 linear interpolation, effort `[45,30,30,45,20,20]`,
chain = L(HP+K+AP) − R).

## 한 줄 결론

**A등급 수치는 사실상 전부 재현된다.** 12항목 중 11개 `재현`, 사소한 정의 차이 2건,
**해석 과장 0건** — 오히려 문서가 자기 결론에 필요 이상으로 보수적인 곳이 있다.
그리고 이 감사가 **내(Deployer) 문서의 오류 하나를 잡았다**(§6). 고쳤다.

## 항목별 판정

| § | 항목 | 판정 | 비고 |
|---|---|---|---|
| 1 | manifest SHA-256 9개 | **재현** | 전부 일치 |
| 2 | R1–R8·R10 지표 재계산 | **재현** | R1~R8·R10 **전 지표**(rows/span/율/tilt/ω/dq/p2p/토크비/action RMS/phase end) 문서값과 0.5 % 이내 일치. 표가 길어 생략 — 스크립트 출력이 전거 |
| 3 | R7/R8 stale 18/50·22/50 | **재현** | exact 36차원 비교로 동일. R7 최장 hold 179 ms 재현. R8 218 ms 는 **"fresh행→다음 fresh행" 정의**로 재현(내 "stale 구간 지속" 정의로는 196 ms — 같은 현상, 정의 차이) |
| 4 | R10 freshness | **재현** | unique 50/50 · non-incr 0 · step median 10 · callback 495.9 Hz · age 8.443/16.146/27.357 ms 전부 일치 |
| 5 | 역사 CSV 분절 | **재현** | rows 3961/543/1826, tilt 통계 전부 일치. span 14.490 은 내 계산 14.440(경계행 포함 차이, 0.05 s) — 숫자 불일치라기보다 정의 차이 |
| 6 | 역사 `t_s` = monotonic | **재현 — 그리고 내 문서가 틀렸었다** | `00ed5b8` 의 `_log_timing` 이 `now = time.monotonic()` 을 쓴다. `realdata/README.md:23` 의 *"t_s 는 벽시계가 아니다"* 는 **오류** → 정정 커밋함. 카운터 시계는 정책 스케줄링·gait_process 쪽 이야기다. **t_s 기반 율 계산(38.4 Hz 등)은 유효** |
| 7 | 해시 대조 | **재현+α** | ⭐ 저장소 `deploy_goal_pose.py` SHA-256 이 로봇 설치본 `d883ed...e64b` 와 **정확히 일치** — 문서의 "uncommitted dirty tree" 우려는 해소됐다(아래 '스윕 사고' 참조) |
| 8 | E0 게인 | **재현** | RL Hip/Knee 100/2 · Ankle 50/1, prepare [350,350,180,350,250,250]/[7.5,7.5,3,5.5,5,5] 일치 |
| 9 | V7 DR ±5 % | **재현** | `Goal_Pose_V7.yaml:495-502` `[0.95,1.05]`. ±15 % 기각 동의 |
| 10 | joint_zero 구현 | **재현** | `_ZERO_ON` 가중치 [1.0,0.5,1.0,1.0,0.7,0.7] · max 10° · init 0.1 · step 0.05 · iid 1.0 확인. `target_fall_rate` grep 0건(미사용 확인). `_zero_level` 은 env 에만 있고 runner 저장 경로에 없음 |
| 11 | probe L−R + 반론 | **재현** | 3회 L−R 평균·SD 전부 일치(Hip_Roll −2.167±0.006, chain −2.307±0.257), fresh t0 chain −2.287 일치. 반론 시도는 아래 |
| 12 | old vs fresh 교락 나열 | **재현** | 빠뜨린 교락 없음. 하나 보탬: fresh 빌드에는 **내 §8-69 표본 게이트**도 들어 있다(문서도 "joint sample gate" 로 나열함 — 정확) |

`--hold-prepare` no-op 주장(§4)도 확인: `:433` 에서 대입만 되고 **아무 데서도 안 읽는다.**

## §19-11 의 요청대로 — probe = encoder zero 에 대한 반론 시도

문서의 자기 반론(하중/기구/유한강성 교락, common-mode 불가시)은 전부 타당하고,
내가 보탤 수 있는 **독립 반론**은 하나다: probe 는 **접지 상태**에서 쟀고 사용자가
"오른발이 앞" 이라고 관찰했다 — 발 위치 비대칭 자체가 하중 경로를 좌우 다르게 만들어
같은 chain 지문을 낼 수 있다. **무부하(로봇을 뉘거나 든 상태) PREPARE probe 한 번이면
이 교락이 갈린다** — 하중 성분이면 사라지고 encoder zero 면 남는다. 문서 §18.4 의
multi-pose 계획에 이 한 칸을 먼저 넣는 것을 제안한다(read-only, 1분).

한편 문서 §15.3 의 Kp-compliance 정량(잔차 0.15/0.19 N·m)은 **강한 논증**이고,
이것이 "PREP 지문 ≠ 곧바로 zero" 를 이미 절반 증명한다. R7(내 영점 실측 레인)도
같은 이유로 **잰 값을 그대로 YAML offset 으로 옮기면 안 된다** — §18.4 동의.

## 교차검증이 찾은 것 둘 (문서 밖)

### ① 스윕 사고 — 그런데 이번엔 결과가 좋았다. 기록만 한다

커밋 `cbb3d0c`("crosslink: action_scale ...")는 내가 `git add -u` 로 만들었고, 그때
**Codex 의 미커밋 작업트리 수정 전부**(freshness fix +215줄, run_e0.sh, 테스트 3파일,
CLAUDE.md +17줄)가 한 줄짜리 메시지 밑에 쓸려 들어갔다. 이 저장소에서 병렬 세션이
서로의 수정을 쓸어 담은 **세 번째** 사고다(앞선 둘: `544642a`, `b93a2d6`).
- 결과적으로는 **로봇 설치본과 바이트 일치하는 코드가 git 에 보존**됐다(§7).
- 그러나 커밋 메시지가 내용을 속인다. 히스토리는 안 고친다(푸시됨) — 이 문서가 정오표다.
- 규칙 재확인: **`git add` 는 명시 경로로만.** `-u`/`-A` 금지.

### ② 병합 검증

로봇 빌드(= 현재 저장소 HEAD)는 Codex freshness fix **위에** 내 §8-69 게이트가
공존한다. 서버에서 세 테스트 전부 통과 확인: `test_recovery_reentry` 18/18 ·
`test_dof_gate` 9/9(+Codex 추가분) · `test_reentry_race` 0실패.

## 해석 층위에서 동의/이견

- **동의**: R7/R8 무효 판정, H0 "hands-off 인용 금지", NP 제외, §18.1 tau 24.2 단일
  레버 A/B 와 사전 판정 기준, §18.3 0.5 s 부터 시작, 낙상 수만 보지 않는 보행 판정.
- **동의(중요)**: "I3b > NH > NP" 는 rate 교락(40.7/45.4/49.8 Hz)이 있는 screen 이다 —
  문서 스스로 명시했고, 이 순위로 NH/NP 를 **영구 기각하면 안 된다.** 특히 NP 가
  가장 목표율에 가까웠는데 가장 동적이었다는 관찰은 armature co-adaptation 쪽을
  가리키지만 single-run 이다.
- **이견 하나(경미)**: §17 "정책/gait clock 느림 — 확정, 대부분 수정됨" 에서 gait clock
  쪽 근거로 S2(단일 seed MuJoCo smoke, D등급)를 쓰는데, MJC Tester 의 별도 결과에서는
  **시계만 다른 두 칸이 22 대 0 낙상**(armature 벤더 고정)으로 더 강한 근거가 이미 있다
  (`MJC_TEST_REQUEST_20260809.md` §결과). 같은 결론이므로 판정은 안 바뀐다 — 인용을
  그쪽으로 바꾸면 등급이 D→A 로 오른다.
- **연결**: §18.3 보행 판정 축에 MJC 가 확립한 **발끼리 접촉 계수**(geom 접촉, 간격은
  대리지표)를 넣기를 권함 — 낙상 0 셀과 낙상 셀을 0 대 21~84 로 가른 유일하게 깨끗한
  축이었다. 실기 로그에는 접촉 센서가 없으니 명령-FK(`tools/cmd_foot_sep.py`)로 대신한다.
