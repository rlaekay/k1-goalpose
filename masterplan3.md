# K1 Goal-Pose — Masterplan 3

> 2026-07-28 전면 재작성. 하네스 수정 후 재평가한 **실측**과, 그로부터 나온 G 배치 4종.
> 원자료: `K1_walk/select_results/*/report.json` (E0·E1은 재평가, E2·V7은 구데이터).
> 앞선 판단은 [masterplan2.md](masterplan2.md)에 있고, 그중 틀린 것은 §7에 정정했다.

---

## 0. 네 줄 요약

1. **E0가 게이트를 사실상 통과했다.** 자기 과제로 채점하니 **2.7 cm / 5.0 cm p90 / 2.5° /
   엄격 성공률 89% / 낙상 2회.** armB(3.9 cm, 52.8%, 37회)를 **더 어려운 지각노이즈 조건에서**
   전면적으로 이겼다. 이 프로젝트가 만든 최고 정책이다.
2. **E1의 재평가는 버려야 한다 — 원인은 내 코드 변경이다.** lookahead floor를 넣으면서
   path 모드의 과제 정의가 바뀌었고, 같은 정책이 path 24.8→**165.9 cm**, 낙상 5→**346회**가
   됐다. 정책이 실패한 게 아니라 **배운 적 없는 과제로 채점**된 것이다.
3. **E2·V7은 재평가가 끝나지 않았다.** 리포트가 08:29/09:11의 구데이터다.
   → **path 모드와 강건성 비용은 유효한 측정치가 하나도 없다.** 처음부터 다시 물어야 한다.
4. 그래서 G 배치 4종은 **전부 E0@6200에서 출발**한다. 네 번째는 SmoothTurn
   (arXiv:2603.12842)을 **관측 폭 54를 유지한 채** 이식했다.

---

## 1. E0 — 무엇을 건지는가

`config: .../E0_armB_armsdown/config.yaml`, `ckpt: model_6200.pth`, 게이트 채점 4633구간.

| 게이트 | 기준 | E0 | armB (참고) |
|---|---|---|---|
| 위치 median | ≤ 5 cm | **2.7 cm** ✅ | 3.9 cm |
| 위치 p90 | ≤ 10 cm | **5.0 cm** ✅ | 6.7 cm |
| heading median | ≤ 10° | **2.5°** ✅ | 7.3° |
| 낙상 | 0 | **2** ❌ | 37 |
| 엄격 성공률 | — | **89%** | 52.8% |

**카테고리별 편차가 거의 없다** — stand 2.9 / straight 2.8 / lateral 2.7 / turn 2.7 /
combined 2.6 cm. armA~D는 항상 combined가 무너졌는데(armD 31.7 cm) E0는 균일하다.

**E0에 들어간 것 = 건질 것 (전부 검증됨)**
| 요소 | 근거 |
|---|---|
| `goal_reached` | armB의 단일 레버, 유지 |
| **팔 내린 URDF** | 재학습 견딤. yaw 관성 −61%가 turn 2.7 cm에 기여한 것으로 보임 |
| **대칭 손실 (처음 실제 활성)** | 카테고리 균일성이 방증 |
| 미니배치 PPO (RunnerV3) | 〃 |
| 지각노이즈 (bias 0.05 / jitter 0.04 / hold 2-3) | 이 조건에서도 2.7 cm |

**속도**: p99 1.20 m/s, 구간최고 p90 1.37 m/s. path 모드 없이도 armB 시절(접근속도 p95
0.35 m/s)보다 훨씬 빠르다. **팔 관성 감소가 속도에도 기여했을 가능성이 있으나 미분리.**

---

## 2. E1 — 왜 버리는가 (내 잘못)

`f038e36`에서 lookahead floor를 넣으며 path 모드의 정의가 바뀌었다. 이전엔 목표가
로봇 위로 흘러내렸고(gap median 0.41 m), 지금은 **0.5~3.0 m 앞에 강제로 유지**된다.

**같은 체크포인트, 코드만 다름:**

| 지표 | 1차 (구코드) | 재평가 (신코드) |
|---|---|---|
| path 위치오차 | 24.8 cm | **165.9 cm** |
| straight | 41.8 cm | 53.9 cm |
| 낙상 | 5 | **346** |

추종비도 1.12 → 0.62 → 0.50 → 0.40 → 0.27 → 0.24 → **0.19**로 단조 붕괴한다.
**정책이 나쁜 게 아니라 과제가 바뀐 것이다.** E1의 어떤 숫자도 path 모드의 증거로 쓸 수 없다.

> **교훈 (구조적)**: 학습이 끝난 체크포인트를 **환경 코드가 바뀐 뒤에 재평가하면 안 된다.**
> config 변경은 `config.yaml`에 남아 추적되지만 **코드 변경은 아무 데도 안 남는다.**
> → §9-3에 재발 방지 항목으로 넣었다.

---

## 3. 유효한 측정이 없는 것들

| 질문 | 상태 | 이유 |
|---|---|---|
| path 모드가 속도를 올리나 | ❌ **미측정** | E1 무효(§2). 1차 데이터는 오염된 과제 |
| path가 waypoint를 파괴하나 | ⚠️ **1차 데이터만** | 6.3 vs 37.9 cm는 오염된 평가에서 나온 값. 방향은 신뢰하되 크기는 아님 |
| dwell이 그 충돌을 없애나 | ❌ 미측정 | 코드만 존재 |
| 강건성이 게이트에 물리는 비용 | ❌ **미측정** | E2 재평가 미완 |
| 통합 후보의 실력 | ❌ 미측정 | V7 재평가 미완 |
| 지속 가능 최고속도 | ❌ 미측정 | 그리드 필요 |

**즉 v7 배치에서 확정된 건 §1(E0) 하나뿐이다.** 나머지는 G 배치가 처음부터 답한다.

---

## 4. F 배치는 왜 그렇게 설계했었나 (요청하신 회고)

masterplan2 §8의 F 배치는 **v7 결과가 나오기 전에** 짠 것이고, 의도는 이랬다:

| arm | 이전 대비 수정 | 왜 |
|---|---|---|
| **F1_timed** | `final_window_s: 2.0` | Rudin 등(arXiv:2209.12827)의 timed reward. v3에도 있었지만 **한 번도 켠 적이 없어** 효과가 미측정. 마감시한은 속도를 명령하지 않고 속도 요구를 만든다 |
| **F2_grid** | lookahead floor + (속도×곡률) 그리드 + dwell | 1차 데이터의 **추종비 비단조**(0.92→0.45→0.77)가 근거. 명령속도와 곡률이 독립 샘플링돼 한 칸에 섞인 것이고, Margolis(RSS 2022)가 Box 대신 Grid를 쓴 이유와 같다 |
| **F3_stress** | flicker 0.004 → 0.01 | E2가 \|ω\| 최저(2.46)였지만 **속도도 최저**(>1.0 m/s 3.6%)라 "필터링을 배웠다"와 "덜 움직인다"가 분리 안 됨 |
| **F4** | 비워둠 | v7 피드백 수렴용 |

**G 배치로 개명·재설계한 이유**: F는 E0@12000에서 출발하도록 돼 있었는데, 재평가 결과
**E0의 best는 6200**이고 12000은 이미 퇴화한 지점이었다. 그리고 §2·§3 때문에 F2가 전제로
삼던 "E1이 path의 대조군" 이라는 구조가 무너졌다. 그래서 베이스와 질문을 다시 잡았다.

---

## 5. G 배치 — 4종 (A6000 2장)

**전부 `E0@model_6200`에서 warm start.** 재평가로 확정된 최고 정책이다.

| arm | GPU | = E0 + | 답하는 질문 | 성공 판정 |
|---|---|---|---|---|
| **G1_speed** | 0 | path(floor·dwell·0.35) + (속도×곡률) 그리드 | path가 속도를 올리면서 **2.7 cm를 지키나** | waypoint ≤ 5 cm **그리고** 구간최고 p90 ↑ |
| **G2_robust** | 0 | 외란 2종 + flicker 0.01 | 강건성이 게이트에 무엇을 물리나 | waypoint 열화 ≤ 2 cm, stress \|ω\| p90 < 3 |
| **G3_full** | 1 | G1 + G2 + 보호항 | 통합 후보 | 위 둘을 동시에 |
| **G4_smoothturn** | 1 | **순차 목표 + lookahead 관측** | 연속 turn을 감속 없이 하나 | 목표 banking 성공률, 완주 시간 |

> **G1이 세 수정(floor·grid·dwell)을 한꺼번에 켜는 건 의도적이다.** 셋은 서로의 전제다 —
> floor 없이는 `path_lag`가 정의되지 않고, 그리드는 `path_lag`로 승급하며, dwell 없이는
> waypoint 게이트가 무의미해진다. **E0가 "셋 다 없는" 대조군**을 이미 제공한다.

---

## 6. G4 — SmoothTurn을 clean하게 붙이는 법

논문: **"SmoothTurn: Learning to Turn Smoothly for Agile Navigation with Quadrupedal
Robots"** (arXiv:2603.12842, Unitree Go2, 1280 env / 6000 iter / RTX 3060 2.5h).

### 6-1. 진단이 우리 것보다 날카롭다

우리는 "목표가 도달 불가라서 뒤에 머무는 습관을 배운다"로 봤고 dwell(목표를 멈춤)로 풀었다.
논문은 같은 현상을 반대편에서 본다:

> 문제는 **목표가 움직인다는 것**이 아니라, 보상이 **한 목표에 머무는 것을 지불**하기 때문에
> 정책이 모든 목표마다 감속한다는 것이다.

### 6-2. 이식한 세 가지

**(1) 순차 목표 보상** (논문 eq. 4)
```
r_seq = (k_t + ρ(e_t)) / N          k_t = 지금까지 도달한 목표 수
ρ(e)  = 1 / (1 + (e/σ)²)
e     = d_xy + λ_θ · ρ(d_xy) · d_θ
```
`k_t`는 **단조 증가만** 하므로 늦게 도달하는 게 이득일 수 없고, 목표 전환 시
`ρ→1`이 되면서 `k_t`가 정확히 그만큼 증가해 **불연속이 사라진다.** 이것이 목표를
멈추지 않고도 stop-and-go를 없애는 방식이다.

`e`의 거리 게이트가 핵심이다 — 멀면 `ρ(d_xy)≈0`이라 heading이 거의 무료고, 가까워질수록
연속적으로 조여진다. **멀리서 heading을 벌하면 도달하지도 않은 목표를 향해 도는데,
그게 정확히 armD의 실패 모드**였다.

**(2) 이중 도달 조건** (eq. 2·3)
| 조건 | 위치 | heading | 속도 |
|---|---|---|---|
| direct-switch | 0.10 m | π/36 | **요구 없음** → 속도를 유지한 채 목표를 적립 |
| stop-switch | 0.5 m | π/3 | v<0.1, ω<0.1 → 근처에 멈춘 경우를 구제 |

하나만 쓰면 각각 실패한다: tight만 쓰면 만족 못 하는 목표 옆에서 영원히 정체하고,
loose만 쓰면 모든 목표에서 제동한다(= 없애려는 그 행동).

**(3) lookahead 관측** — 다음 2개 목표를 관측에 넣어 **도착 전에 턴을 준비**하게 한다.
논문 ablation에서 lookahead 없으면(n=1) 성능이 크게 떨어진다.

### 6-3. 관측 폭 문제와 해법 ← 이게 "clean"의 핵심

논문은 관측을 47 → 47+3n으로 늘린다. 우리가 그러면 **54 → 60이 되어 warm start가 전멸**한다
(heading sin/cos를 미루는 것과 같은 이유).

**피할 수 있다.** `commands`는 10칸인데 **앞 4칸만 정보를 담는다.** 슬롯 4~9
(`foot_yaw_L/R`, `body_pitch/roll_target`, `feet_offset_x/y_target`)는 milestone 2 이후
모든 config에서 `[0,0]`으로 못박혀 있다. **그게 정확히 6채널 = 2목표 × (Δx, Δy, Δθ)** 다.

→ **그 슬롯을 재사용한다.** 목표 채널 자체가 원래 ParameterWalk의 속도명령 슬롯을
빼앗아 만든 것과 똑같은 수법이다. **관측 54 유지, E0 가중치 그대로 로드.**

함께 처리한 것:
- 해당 슬롯의 `normalization`을 goal 스케일(0.5 / 1/π)로 재조준
- **`foot_yaw_L/R` 추종 페널티를 0으로** — 그 슬롯은 더 이상 발 목표가 아니므로,
  안 끄면 정책이 자기가 공급받은 lookahead 값 때문에 벌을 받는다

### 6-4. 자동 커리큘럼
`c ∈ [0,1]`: `Δθ ∈ [-cπ, cπ]`, `ℓ ∈ [1.5-1.5c, 2+2.5c]`.
도달률 >80%면 올리고 <20%면 내린다. `c=0`이면 직선 짧은 홉, `c=1`이면 ±π 턴 × 0~4.5 m.

---

## 7. masterplan2 대비 정정

| masterplan2에서 쓴 것 | 실측 후 |
|---|---|
| "E0는 팔 URDF가 warm start를 견디는지 보는 arm" | ✅ 견뎠고, **그 이상이었다** — 2.7 cm / 89%로 armB를 압도 |
| "path mode가 속도를 올린다 — 확인됨" | ❌ **철회.** 근거였던 E1이 무효(§2). 재측정 필요 |
| "waypoint 50%로는 정지 능력이 안 지켜진다 (37.9 cm)" | ⚠️ **방향만 유지.** 오염된 평가에서 나온 크기라 신뢰 불가 |
| "dwell이 충돌을 없앤다" | ⚠️ 코드만 존재, 미검증 |
| "F 배치는 E0@12000에서" | ❌ **6200이 best.** 12000은 퇴화 지점 |
| "낙상은 path 학습 arm이 오히려 최저" | ❌ **철회.** E1의 낙상 5회는 오염된 조건의 값 |

---

## 8. 실행

```bash
git pull

# 1) 신규 기계장치 4종 스모크 (G1/G4는 한 번도 실행된 적 없는 코드를 쓴다)
python tools/make_v7_arms.py --only G1_speed
python tools/make_v7_arms.py --only G4_smoothturn
python tools/smoke_v7.py --config sweeps/G1_speed.yaml --sim_device cuda:0 --rl_device cuda:0
python tools/smoke_v7.py --config sweeps/G4_smoothturn.yaml --task K1/Goal_Pose_V8 \
    --checkpoint logs/K1/K1/Goal_Pose_V7/2026-07-26-19-36-15_E0_armB_armsdown/nn/model_6200.pth \
    --sim_device cuda:0 --rl_device cuda:0

# 2) 전부 PASS면 4종 실행
SESSION=g CKPT=logs/K1/K1/Goal_Pose_V7/2026-07-26-19-36-15_E0_armB_armsdown/nn/model_6200.pth \
  bash tools/run_g_suite.sh
```

스모크가 G4에서 검사하는 것: 순차 env 비율, **목표가 실제로 적립되는지**(`seq_idx`가
안 오르면 전체가 단일목표 과제로 퇴화한다), **슬롯 4~9의 lookahead가 0이 아닌지**,
`r_seq`가 유한하고 [0,1] 범위인지.

---

## 9. 미해결 / 재발 방지

| # | 항목 | 조치 |
|---|---|---|
| 1 | **E2·V7 재평가 미완** | `ARMS="E2_robust V7_full" bash tools/reeval_v7.sh` — 학습 없음. 다만 §2의 이유로 **path를 쓰는 V7은 재평가해도 무효**다. E2(path OFF)만 유효 |
| 2 | 낙상 게이트 (E0 2회) | G 배치 전 arm에서 감시. 외란을 켜는 G2/G3는 일시적 악화가 정상 |
| 3 | **코드 변경 후 재평가 금지** | §2가 그것 때문에 날아갔다. `report.json`에 **env 코드의 git SHA**를 남겨 학습 시점과 평가 시점이 다르면 경고하도록 할 것 ← **미구현, 다음 작업 1순위** |
| 4 | 지속 가능 최고속도 | G1의 명령 vs 실제 표. 그리드가 곡률 교란을 분리한다 |
| 5 | SmoothTurn 관측 포화 | 슬롯 6칸을 다 썼다. lookahead를 3개로 늘리려면 관측을 늘려야 하고 warm start가 깨진다 — 현재 설계의 한계로 명시 |

---

## 10. 한 줄

> E0가 2.7 cm / 89%로 게이트에 도달했고, path·강건성·통합은 **측정 자체가 무효**여서
> 다시 물어야 한다. G 배치는 E0@6200 위에서 그 셋을 각각 격리해 묻고,
> 네 번째로 SmoothTurn을 **관측 폭을 늘리지 않고** 얹는다.
