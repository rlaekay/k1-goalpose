# K1 Goal-Pose — Masterplan 3 (v7 배치 결과와 결론)

> 작성 2026-07-27. [masterplan2.md](masterplan2.md)의 v7 설계에 대한 **실측 결과와 그에 따른 수정**.
> 원자료: `K1_walk/v7/*/report.md` (4 arm × 256 env × 120 s) + stress jitter 4건.

---

## 0. 세 줄 요약

1. **평가가 잘못 돌았다.** 4개 arm 전부 자기 config가 아니라 베이스 config로 평가됐다.
   path를 안 배운 arm이 path 46%인 과제로 채점됐고, **그들 낙상의 97%가 그 구간에서 났다.**
2. **path 학습이 waypoint 정확도를 파괴한다.** waypoint만 보면 6.3 cm(E0) vs 37.9 cm(E1).
   원인은 "절대 도달할 수 없는 목표"를 계속 쫓게 한 것 — 정책이 **뒤에 머무는 습관**을 배웠다.
3. **속도 목표는 사실상 달성됐다.** E1 구간최고속도 median **1.20 m/s**, p90 **1.80 m/s**,
   1.0 m/s 초과 시간 **22.5%**. armB 시절 접근속도 p95가 0.35 m/s였다.

---

## 1. ❌ 측정 결함: arm별 config가 무시됐다

전 arm의 리포트 헤더가 동일하다 — `config: envs/K1/Goal_Pose_V7.yaml`.

`train_and_eval.sh`가 `select_best_checkpoint.py`를 `--config` 없이 호출했고,
그쪽 기본값이 `envs/<task>.yaml`이라 **arm별 sweeps/*.yaml이 전혀 안 쓰였다.**
베이스 config는 path 50% + 지각노이즈 풀스택이다.

| arm | 학습 시 path | 평가 시 path 비중 | 낙상 | 그중 path 구간 |
|---|---|---|---|---|
| E0_armB_armsdown | **OFF** | 46% | 93 | **90 (97%)** |
| E1_path | ON | 49% | 5 | 1 |
| E2_robust | **OFF** | 44% | 125 | **123 (98%)** |
| V7_full | ON | 49% | 2 | 2 |

**E0와 E2의 낙상은 거의 전부 한 번도 학습한 적 없는 과제에서 났다.** 정책의 결함이 아니다.

### 무엇이 유효하고 무엇이 무효인가

- ✅ **arm 간 비교는 유효하다.** 4개가 완전히 동일한 과제로 채점됐다. 다만 의미가
  "각자의 과제를 얼마나 잘하나"가 아니라 **"통합 v7 과제로 일반화되나"**로 바뀐다.
- ❌ **armA~D와의 비교는 무효다.** 그쪽은 `Goal_Pose.yaml`(path 없음, 지각노이즈 [0,0])로
  평가됐다. **"E0 6.3 cm vs armB 3.9 cm"를 성능 퇴보로 읽으면 안 된다** — 과제가 다르다.
- ❌ **단일 arm의 게이트 수치를 절대값으로 읽으면 안 된다.**

### 수정 (적용됨)

1. `select_best_checkpoint.py`가 **run 디렉토리의 `config.yaml`을 기본값**으로 쓴다.
   `--config`를 주면 여러 run을 공통 과제로 채점하는 기존 용도도 그대로 남는다.
2. **path 구간을 게이트 채점에서 제외**한다(아래 §2).

---

## 2. ❌ path 구간의 위치오차는 정의상 무의미하다

path 모드에서 목표는 **의도적으로 lookahead만큼 앞에** 있고 계속 움직인다. 그래서
"구간 종료 시 목표까지 거리"는 추종오차가 아니라 **lookahead 거리를 재는 값**이고,
0에 수렴할 수가 없다. `도달후이탈`이 E0 100% / V7 96% / E1 87%인 것도 같은 이유다 —
carrot이 5 cm 반경을 스쳐 지나가면 구조적으로 "도달했다가 이탈"로 기록된다.

이 구간이 표본의 44~49%였으므로 **모든 헤드라인 수치가 오염됐다.**

| arm | 보고된 median | **waypoint만 (재집계)** |
|---|---|---|
| E0 | 13.2 cm | **6.3 cm** |
| E1 | 36.0 cm | **37.9 cm** |
| E2 | 34.5 cm | **15.2 cm** |
| V7 | 41.9 cm | **38.7 cm** |

**수정**: 게이트(위치 median/p90, heading, 성공률, CI)를 **waypoint 구간에서만** 계산한다.
path 추종은 `path_lag`와 명령-실제 속도표라는 자기 지표로 따로 채점한다.
리포트 헤더에 `게이트 채점 N개, path M개 제외`를 명시한다.

---

## 3. 🔴 핵심 발견: path 학습이 waypoint 정확도를 파괴한다

카테고리별 위치오차 median [cm]:

| | stand | turn | straight | lateral | combined |
|---|---|---|---|---|---|
| **E0** (path 미학습) | 4.8 | **6.7** | **6.4** | **6.2** | **6.6** |
| **E1** (path 학습) | 5.0 | 41.0 | 41.8 | 41.4 | 41.7 |
| E2 (path 미학습, 외란) | 2.7 | 8.6 | 15.8 | 15.0 | 23.0 |
| **V7** (path 학습) | 5.0 | 38.3 | 42.0 | 39.7 | 45.3 |

**path를 학습한 두 arm만 waypoint에서 ~40 cm로 무너졌고, `stand`는 멀쩡하다(5.0 cm).**
stand는 이동이 없어 "뒤에 머무는 습관"이 발현되지 않는 카테고리다. 이 대비가 원인을 특정한다.

### 원인

path 모드의 목표는 **한 번도 도달 가능한 적이 없다.** 정책이 배우는 최적 행동은
"목표 뒤 일정 거리를 유지하며 따라간다"이고, 그 습관이 waypoint 모드로 새어나간다.
E1의 증거: 미도달이 0.5~1.0 m 구간에서 **72%**인데 2.0 m 이상에서는 **5%**로 역전된다
(먼 구간은 대부분 path라 carrot이 알아서 지나가므로 "도달"로 잡힌다).

waypoint와 path를 **같은 보상으로 50:50 섞으면 정책은 둘 다 못 하는 절충안**으로 간다.

### 수정 (적용됨) — dwell

path 모드에 **carrot이 멈추는 구간**을 넣는다:
```yaml
dwell: {enabled: true, interval_s: [4,10], duration_s: [1.5,3.0]}
```
- 멈추는 동안 `path_speed → 0`
- **동시에 최소거리 floor도 해제**한다(안 하면 여전히 도달 불가라 의미가 없다)
- 그러면 path 모드가 **waypoint와 같은 "도착해서 정지" 상태로 끝난다**
- `goal_reached`가 양쪽에서 똑같이 발화한다 → **두 과제가 서로 당기지 않는다**

즉 path를 waypoint의 **상위집합**으로 만든다. 실제 A* lookahead도 목적지에 닿으면
멈추므로 현실과도 더 맞다.

추가로 path 비중을 **0.5 → 0.35**로 낮춘다. dwell이 충돌을 없앤다는 게 검증되기 전까지는
게이트로 채점되는 과제에 표본을 더 준다.

---

## 4. ✅ 속도: 목표에 도달했다

| | median | p99 | 구간최고 median | 구간최고 p90 | 순간최대 | >1.0 m/s 시간 |
|---|---|---|---|---|---|---|
| E0 | 0.33 | 1.49 | 1.04 | 1.50 | 2.56 | 14.4% |
| **E1** | 0.38 | **1.73** | **1.20** | **1.80** | 2.27 | **22.5%** |
| E2 | 0.19 | 1.28 | 0.60 | 1.21 | 2.79 | 3.6% |
| V7 | 0.32 | 1.57 | 1.05 | 1.64 | 2.12 | 16.9% |

**armB 시절 접근속도 p95가 0.35 m/s였다.** path 모드가 속도 요구를 만든다는 가설은
**검증됐다.** 목표 1.3~1.5 m/s는 E1의 구간최고 p90(1.80 m/s) 안에 들어온다.

낙상이 E1 5회 / V7 2회로 오히려 최저라는 점이 중요하다 — **속도가 낙상을 대가로 얻어진
것이 아니다.**

### 지속 가능 상한: 아직 확정 못 한다

추종비가 **비단조**다. E1: 0.6–0.8 구간 0.92 → 0.8–1.0 구간 0.45 → 1.0–1.2 구간 0.77.
명령속도만으로 비닝하면 이렇게 뒤죽박죽이 된다.

**이유가 곧 Margolis의 발견이다**: 명령속도와 **곡률이 독립적으로 샘플링**되므로
같은 1.0 m/s라도 급커브면 불가능하고 완만하면 쉽다. 두 경우가 한 칸에 섞인다.
→ **F2의 (속도 × 곡률) 그리드가 정확히 이 문제를 푼다.** 이번 데이터가 그리드 설계의
사후 정당화가 됐다.

---

## 5. ✅ stress jitter: 4개 전부 생존

| arm | 낙상 | 직립 유지 | \|ω\| p90 | 판정 |
|---|---|---|---|---|
| E0 | 0 | 100.0% | 2.76 | ✅ |
| E1 | 0 | 99.9% | **3.12** | ❌ 경련 |
| **E2** (flicker 학습) | 0 | 100.0% | **2.46** | ✅ 최저 |
| V7 | 0 | 100.0% | 2.48 | ✅ |

**낙상 0회 / 직립 ~100%** — 50 Hz로 ±3 m 튀는 목표에서도 안 넘어진다. 강건성 자체는 확보됐다.

**flicker 학습 효과는 있다**: E2(flicker O) 2.46 vs E0(flicker X) 2.76. 다만 E2는 속도도
가장 느려서(>1.0 m/s 3.6%) 순수 효과인지 단순히 덜 움직여서인지 **분리되지 않는다.**
F3가 이 질문에 답한다.

E1이 유일하게 실패한 건 path 학습으로 **더 민첩해진 대가**로 보인다.

---

## 6. 체크포인트 선택이 말해주는 것

| arm | best | 최종 |
|---|---|---|
| E0 | **6800** | 12000 |
| E2 | **10100** | 12000 |
| V7 | **7500** | 12000 |
| E1 | **12000** | 12000 |

**E1만 끝까지 개선됐다.** 나머지 셋은 중간에 정점을 찍고 나빠졌다.
단 이 판정도 오염된 평가 과제(§1) 위에서 이뤄진 것이므로, 수정된 하네스로 재선택해야
신뢰할 수 있다.

---

## 7. masterplan2 계획 대비 정정

| masterplan2에서 쓴 것 | 실측 후 |
|---|---|
| "path mode가 속도를 올린다" | ✅ **확인** (0.35 → 1.20 m/s 구간최고 median) |
| "leash가 gradient를 지킨다" | ✅ 확인 (E1 낙상 5회, 미도달 정상) |
| "waypoint 50% 유지로 armB의 정지 능력을 보존한다" | ❌ **틀렸다.** 50%로는 안 지켜진다 — 37.9 cm. dwell + 0.35로 수정 |
| "E3(무커리큘럼)는 위험한 도박" | ⚠️ 여전히 유효. 다만 이번 배치에서 검증 못 함 |
| "MaxSpeed를 별도 실험 없이 얻는다" | ⚠️ 부분 성공. 추종비 비단조 때문에 **그리드가 있어야** 확정된다 |
| "낙상 게이트가 걱정" | ✅ path 학습 arm은 2~5회로 오히려 최저 |

---

## 8. F 배치에 반영한 것

| 수정 | 내용 |
|---|---|
| **eval config** | run 자기 `config.yaml`을 기본으로 (§1) |
| **게이트 범위** | path 구간 제외, `path_lag`로 별도 채점 (§2) |
| **dwell** | carrot이 멈추고 floor 해제 → path가 waypoint의 상위집합 (§3) |
| **path 비중** | 0.5 → 0.35 |
| F2 | (속도 × 곡률) 그리드 — §4의 비단조 추종비가 근거 |
| F3 | flicker 0.01 — §5의 미분리 문제를 푼다 |

### F 배치 착수 전 확인

**warm start 대상**: F 배치는 `E0@model_12000`에서 잇도록 돼 있는데, **E0의 best는
6800이다.** best를 쓸지 최종을 쓸지는 §6의 재선택 후 결정하는 게 맞다. 다만 §1 때문에
그 선택 자체가 오염돼 있으므로, **당장은 E0의 12000을 쓰되 F 배치 결과에서 다시 본다.**

---

## 9. 미해결

| # | 항목 | 근거 |
|---|---|---|
| 1 | **v7 4개 재평가** — 수정된 하네스로 | §1·§2 때문에 현재 수치 전부 재산출 필요. 학습 없이 eval만 다시 돌리면 된다 |
| 2 | dwell이 실제로 충돌을 없애는지 | §3의 핵심 가설. F2에서 검증 |
| 3 | 지속 가능 최고속도 확정 | §4. F2의 그리드 필요 |
| 4 | flicker 효과 분리 | §5. E2가 느려서 교란됨. F3 |
| 5 | 영상이 하나도 안 나왔다 | 4개 run 모두 mp4 없음. `train_and_eval.sh`에 경고는 추가됐으나 원인 미규명 |
| 6 | `report.json` 미수집 | 이번엔 `report.md`만 복사됐다. 원자료 재집계가 불가능해 표에서 손으로 옮겨야 했다 |
