<!--Be concise. Read only the files you need. Stop searching once you have enough information. Make the smallest correct change. Reuse existing code whenever possible. Do not refactor unrelated code. Avoid unnecessary explanations. Output only the modified code or diff.-->

> **[답변]** 접수. 이 문서는 그 규칙의 예외다 — **일지이자 계획서라 보존이 목적**이다.
> 채팅 응답과 커밋 메시지는 짧게 유지하고, 근거·수치·결정은 전부 여기에만 남긴다.
> 코드는 최소 변경, 기존 코드 재사용, 무관한 리팩터 금지 — R0 구현에 그대로 적용했다(§9).


**이걸 읽는 순간 아래 11개 주석에 먼저 답하고 진행해. 답변은 바로 아래에 달도록 하자**
<!--학습 한 회차를 최대한 빠르게 돌리자. 카드 한 장에 두배치를 돌렸을 때 너무 느려지면 하나씩만 하는게 좋은듯-->

> **[답변]** **동의, 그리고 이미 그렇게 고쳐 놨다.** 카드당 2 프로세스는 처리량이 2배가 아니라 **약 1.5배**(시분할)라, 회차 지연시간은 오히려 1.33배 늘어난다. 회차를 자주 도는 게 목적이면 **카드당 1개가 맞다.** `GPU_OF`에 `I0a→cuda:0`, `I0b→cuda:1`로 박아 뒀다.
> **남는 병렬화 여지**: GPU가 1 프로세스로는 포화되지 않으므로 `num_envs`를 4096 → 8192로 올려 **같은 시간에 2배 표본**을 얻는 쪽이 낫다. 이건 프로세스를 늘리는 게 아니라 배치를 키우는 것이라 시분할 손실이 없다. R0에서 VRAM 여유(H 실측 8 GB/49 GB)를 보고 확정한다.

<!--robust계열에서 속도가 느린 것에 대한 추측: 학습 과정에서부터 goal에 너무 큰 지터를 줘서(어떻게 학습했는지 확인해봐 내 기억으론 2미터짜리 원에서 마구잡이로 섞으라고 됐었던 것 같은데) 그냥 해당 범위 내에 있는 goal에 대해서 모두 솎아내기를 한 결과로서 느려진거 아니야? jitter의 범위를 50cm짜리 원 가우시안?(보통 프로젝션 에러가 이렇게 나니까 공 프로젝션 에 따른 goal도 이정도 범위 안에서 jitter를 함.)을 반영해야 하는 것 아닌가? 그리고 test eval할 때는 커다란 골 jitter를 줘서 넘어지는지 확인하고 넘어지는 것은 다른 방법으로 achieve해야지.-->

> **[답변]** **추측이 맞다. 다만 범인은 지터가 아니라 편향이다.** 실제 설정을 확인했다:
> ```yaml
> goal_pos.range:      [0, 0.04]   # 스텝별 지터 σ=4 cm
> goal_pos_bias.range: [0, 0.05]   # 구간 고정 편향 σ=5 cm  ← 이게 문제
> goal_bt_flicker:     radius 1.0 m, prob 0.004/step   # 기억하신 "2 m 원"에 가까운 것
> ```
> 즉 기억하신 큰 지터는 **`goal_bt_flicker`(반경 1 m 점프)**이고, 상시 지터는 4 cm로 오히려 작다.
> **속도 붕괴의 실제 메커니즘**: 스텝 지터(σ=4 cm)는 시간 평균으로 씻긴다. 그런데 **구간 고정 편향(σ=5 cm)은 씻기지 않는다** — 한 구간 내내 같은 방향으로 틀려 있고, 보상은 참값을 읽으므로 정책은 자기가 틀렸다는 걸 알 방법이 없다. 이 조건에서 기대 보상을 최대화하는 해는 **천천히 가서 헛발질 횟수를 줄이는 것**이다. E2·G2에서 두 번 재현됐다(L4).
> **반영**: 학습에는 **reprojection uncertainty만** 넣는다 — 30–50 cm 가우시안 지터(씻김), **편향은 0**. 큰 flicker와 편향은 **평가 전용**으로 뺀다.

<!--애초에 goal jitter에 대한 대처를 학습에서 어떻게 하고있는지 잘 모르겠음.-->

> **[답변]** **구조는 이렇다** (`goal_pose.py::_update_perceived_goal`):
> | | 정책이 보는 것 | 보상이 읽는 것 |
> |---|---|---|
> | 목표 위치/heading | **노이즈 섞인 값**(지터+편향+2–3스텝 지연+flicker) | **참값** |
> >
> 즉 "관측만 흔들고 채점은 참값" 구조라, 의도는 *필터링을 배워라*였다.
> **문제는 정책이 필터링에 쓸 재료가 없다는 것이다.** 관측 54차원에 **과거 목표 이력도, 불확실성 추정치도 없다.** 매 스텝 한 장의 흔들린 목표만 주고 "걸러내라"고 요구한다. 정책이 할 수 있는 유일한 필터링은 **자기 행동을 둔감하게 만드는 것**이고, 그게 곧 감속이다.
> **그래서 노이즈를 키우면 반드시 느려진다.** 이건 정책의 실수가 아니라 구조가 강제한 최적해다. 해결은 노이즈 크기 조절이 아니라 (a) 씻기는 노이즈만 쓰거나, (b) 관측에 이력을 주는 것인데 (b)는 54차원을 깨므로 I 배치에서는 (a)만 한다.

<!--path를 학습한다는거를 개념을 삭제할까? path는 eval시에만 하고, 학습은 within 2m goal chasing만 하는건 어때? 좀 분리를 해야 낫지 않을까?-->

> **[답변]** **동의한다. 학습에서 뺀다.** 근거는 L5에 이미 있다 — path 학습은 세 대가를 청구한다: waypoint 정확도, 전방 이동 prior(옆·뒤 감속), 낙상(H에서 89.6%가 path 구간).
> **대가**: 지시 7-B(움직이는 목표 추종)를 학습에서 배우지 못한다. 이건 아래 "a와 b를 동시에" 질문과 직결되므로 거기서 답한다.
> **결정**: 학습 = **2 m 이내 goal chasing만**. path·8자·spiral은 **평가 전용**.

<!--네 보고서를 보니까 판정에 대한 정리가 잘 안돼있는 것 같은데 학습 계획 시에 판정까지 맞춤형으로 계획해-->

> **[답변]** **맞다. 판정을 사후에 만들었다.** H가 그 결과다 — 4 arm을 12000까지 돌린 뒤에야 "model_0이 이겼다"를 알았다.
> **고친 방식**: R0부터 모든 arm은 **학습 전에 판정표를 확정**하고, 그 판정을 **코드가 자동으로 내린다**(사람이 리포트를 읽고 판단하지 않는다). §8에 R0 판정표가 있고, 아래 "실시간 eval" 답변의 `watch_eval`이 그걸 자동 집행한다.

<!--eval//train 등 코드가 너무 많아. 사용하던 파일들은 모두 archive하고 버전 하나로 리셋해서 진행. -->

> **[답변]** **진행 중.** 하위 에이전트가 `htwk-gym/archive/`로 이동시키고 있다. 유지 목록은 `tonight.sh` / `make_v7_arms.py` / `smoke_v7.py` / `train_and_eval.sh` / `select_best_checkpoint.py` / `eval_goal_pose.py` / `train_v7.py` / `monitor.*` / `progress.py` / `check_names.py` / `envs/K1/goal_pose{,_v3,_v7}.py` / `utils/*` / `deploy/*` 뿐이다. 이동 후 `py_compile` + `check_names` + `bash -n`으로 회귀 검사하고 `archive/README.md`에 무엇이 왜 폐기됐는지 한 줄씩 남긴다.

<!--내가 headless server를 사용하니까 학습 진행도, 학습 끝, 평가중, 평가끝 등의 현황과 reward graph, iter, checkpoint등을 실시간으로 확인할 수 없어. 사후에도 마찬가지고. 계속 활용할 수 있는 webpage를 만들어서 서버에서 실시간으로 해당 정보들을 송신할 수 있도록 해. 실시간 수신을 마쳐도 계속 확인할 수 있도록 버전별로 또 분류를 하고. 이건 앞으로 하는 모든 학습에 동일하게 적용. 만약에 내가 언급한거 외에도 네가 중요하게 생각하는 부분이 있다면 추가해넣어. consensus가 있을거아니야<-이게 안되면 터미널 창에라도 볼 수 있는 코드를 만들어. 아마 local network를 사용하면 되려나..? 모르겠네.-->

> **[답변]** **만들었고, 지적하신 대로 반쪽이었다.** 아래 항목에서 이어서 답한다.

<!--7번 3축: A(정확·빠름)와 B(움직이는 목표)는 한 정책이 동시에 한 적이 없고, C(불안정 시 안정화 우선)는 이 프로젝트에 존재한 적 없는 개념입니다. §5-C에 게이팅 보상안을 뒀지만 I 1차에는 넣지 않았습니다 — I2(단일지지)가 먼저 서야 의미가 있습니다.<-이거까지 까먹지 않고 할 수 있도록 하려면 어떻게 해야해? 네 메모리에 집어넣으면 돼? i2결과를 보고 하라고? 네가 방법을 찾아봐. 나는 까먹고 싶지 않아.-->

> **[답변]** **메모리에 넣는 걸로는 부족하다 — 잊는 게 아니라 조건이 안 맞아 미뤄지는 것이다.** 세 겹으로 건다:
> 1. **`ibatch.md` §7 결정 트리에 노드로 존재**한다 — 문서를 보면 항상 보인다
> 2. **판정 코드에 미구현 게이트로 등록**한다: `C_stabilize_first` 게이트를 `NOT_IMPLEMENTED`로 넣어 두면 **모든 리포트에 매번 "미구현" 줄이 찍힌다.** 통과할 수 없으니 조용히 사라지지 않는다
> 3. **내 영구 메모리**에 저장한다 — 세션이 끊겨도 유지된다
> ②가 핵심이다. 문서와 메모리는 안 볼 수 있지만 **리포트는 매번 본다.**

<!--a와 b를 동시에 달성하지 못할거면 둘을 따로 학습해도 의미가 없는게, 나는 어차피 하나의 모션만 사용할건데? 그리고 난 둘의 목표가 동시에 필요해. c-->

> **[답변]** **맞는 지적이고, 제 §5-B 설계가 틀렸다.** "I1은 속도, I3은 움직이는 목표"처럼 축을 나눠 학습하면 **합쳐지지 않는다** — 하나의 정책만 쓰실 거니까.
> **고친 설계**: 축을 나누는 것은 **레버 검증 단계에만** 쓰고, 각 라운드의 승자는 **누적**된다. R1 승자 위에 R2를 얹고, R2 승자 위에 R3를 얹는다. 마지막 정책 하나가 A·B·C를 전부 갖는다.
> 그리고 **B(움직이는 목표)를 학습에서 뺐으므로**(위 path 답변) A와 B가 충돌할 일 자체가 줄어든다. 남은 질문은 "path 없이 학습한 정책이 평가의 움직이는 목표를 쫓을 수 있나"이고, 이건 **R0/R1 시점에 평가로 바로 측정된다** — 못 쫓으면 그때 학습에 최소한만 되돌린다.

<!--그니까 나의 수정의도의 whole point is that i dont want to hard code the reward or conditions. i want to drive the learning to the direction that i want from the highest level modifications. if i were to modify the condition or reward specified for a purpose, then there must be a method to achieve the same goal without doing so<-sth like "to make it standing firmly, if i give robustness to the disturbance, i wont have to reward it for standing upright. standing upright is for human. being robust to disturbance does the same thing with covering more fundamental thing". if these methods are findable, please do them.-->

> **[답변]** **이게 가장 중요한 지적이고, 제 §5 설계 전체가 이 원칙을 위반하고 있었다.**
> 제가 넣으려던 것들: `base_height_target` 튜닝, 발 하중분배 보상, heel 접지 보상, 순항 pitch 페널티 — **전부 "사람이 보기에 좋은 자세"를 직접 보상하는 하드코딩**이다. 지적대로, 목적이 있다면 그 목적을 **더 근본적인 조건**으로 유도할 방법이 있어야 한다.
> **원칙을 다시 적으면**: *결과(자세)를 보상하지 말고, 그 결과를 필연으로 만드는 조건(과제·물리·외란)을 만든다.*
> >
> | 하드코딩(하지 말 것) | 상위 조건으로 대체 | 왜 더 근본적인가 |
> |---|---|---|
> | 직립 자세 보상 | **외란 강건성** | 밀려도 안 넘어지려면 **결과적으로** 직립이 유리하다. 직립은 인간의 미감이고, 강건성은 물리적 요구다 |
> | `base_height_target` 값 튜닝 | **다양한 지면·마찰·외란에서 생존** | 최적 높이는 정책이 스스로 찾는다. 우리가 0.58을 고르면 그 조건에만 맞는다 |
> | 발 하중분배 보상 | **실제 발 크기 + 단일지지 과제** | 지지면이 진짜로 좁으면 하중을 고르게 쓰는 것 외에 답이 없다 |
> | heel 접지 보상 | **고속에서 외란** | 오버스트라이드하면 밀렸을 때 못 버틴다. 보상이 아니라 실패가 가르친다 |
> | 순항 pitch 페널티 | **케이던스–속도 커플링** | 보폭이 실현 가능해지면 몸을 던질 이유가 사라진다 |
> >
> **R0에 즉시 반영되는 것**: `feet_edge_pos` 교정은 **하드코딩이 아니라 사실 교정**이므로 유지한다(sim이 거짓말을 하고 있었다). 나머지 보상 항 추가는 **전부 보류**하고, 대신 **외란·지면·발 크기라는 조건**을 정직하게 만드는 쪽으로 라운드를 다시 짠다(§10).
> **단 정직하게**: 이 원칙이 항상 통한다는 보장은 없다. 조건만으로 안 되는 항목이 나오면 **그때 하드코딩을 쓰되, 왜 조건으로 안 됐는지를 먼저 기록**한다.


<!--fine tuning으로 단시간 안에 목표한 바를 학습할 수 있어? eval을 그냥 실시간으로 checkpoint가 나오면 돌려서 중단시키는 방법은 별론가?-->

> **[답변]** **두 질문 다 "그렇다"에 가깝다.**
> **(a) fine-tuning으로 되나** — H 데이터가 답을 준다: 손상이 **iteration 100 안에** 1.4–1.8배로 나타났다. 즉 **200 iteration이면 방향 판정에 충분하다.** 반대로 "좋아지는" 신호도 같은 시간 척도에서 보이는지는 미검증이라, R0가 그것도 같이 잰다(I0a가 E0를 유지하는지).
> **(b) checkpoint 나오는 즉시 eval 돌려 중단** — **별로가 아니라 이게 맞다.** 지금은 12000까지 다 돌린 뒤 32개 후보를 뽑아 평가한다. 그래서 H는 **4 GPU-일을 쓰고 "전부 나빠짐"** 하나를 얻었다.
> 구현: `tools/watch_eval.py` — `nn/model_*.pth`를 감시해 새 checkpoint가 생기면 **짧은 평가(20 s × 256 env)**를 돌리고, 사전 정의된 중단 규칙에 걸리면 run 디렉터리에 `STOP` 파일을 쓴다. `runner_v3.py`가 이미 `STOP` 파일을 읽어 **checkpoint를 저장하고 정상 종료**한다(기존 기능, 재사용).
> 이러면 나쁜 arm은 **몇 분 만에** 죽고 GPU가 다음 라운드로 넘어간다.

# I 배치 — 탐색 결과와 설계 근거

> 2026-08-03. **코드 수정 전 단계.** E/G/H 전 배치와 최상위 문서 20종, 실기 배포 코드,
> 외부 선례를 탐색한 결과를 정리한다. 사용자 검토 후 지시에 따라 구현한다.
>
> 규칙: **한 사실은 한 곳에만 적는다.** §5의 수정안 각 항목은 §1–§4의 근거 번호를
> 직접 참조하며, 근거 없는 수정은 넣지 않는다.

---

## 0. 왜 I 배치인가 — local minimum의 정체

사용자 진단: "현재 학습 방향이 local minima에 빠져버렸어. 이런식으로는 미시적인
수정밖에 하지 못해." **데이터가 이를 지지한다.**

| 배치 | 최고 정책 | 위치 median | 낙상 | strict | 무엇을 얻었나 |
|---|---|---:|---:|---:|---|
| **E** | **E0@6200** | **2.72 cm** | **2** | **89.3%** | 게이트 3/4 통과 |
| G | G1@10700 | 5.52 cm | 38 | 34.2% | >1 m/s 체류 3.0→17.7% |
| H | (없음) | 6.90 cm* | 24.5‰ | — | **없음 — 4 arm 전부 model_0 우승** |

\* H의 6.90 cm는 학습 결과가 아니라 **G1 warm start를 새 평가 프로토콜로 잰 값**이다.

**3세대 연속 후퇴했고, 각 세대는 직전 세대에서 warm start했다.** 즉 매번 "조금 더 나쁜
지점"에서 다시 출발해 미시 조정을 반복했다. 이것이 local minimum의 실체다.

> **탈출 방법은 하나뿐이다: 가중치를 E0로 되돌리고, 무엇을 바꿀지 근거로 다시 고른다.**
> (사용자 지시 2와 일치)

---

## 1. E/G/H에서 확정된 교훈 — 반복 금지 목록

각 항목은 **한 번 이상 실제로 대가를 치른 것**만 넣는다.

### L1. 변인통제 위반 — 5회 반복, 이 프로젝트 최대 손실원

| 사건 | 동시 변경 레버 | 결과 |
|---|---:|---|
| armD | 12 | 24.3 cm 붕괴, 원인 특정에 별도 실험 필요 |
| E2_robust | 7 (`_OFF_ROBUST` 통째) | 속도 붕괴. "필터링 vs 그냥 느림" 분리 실패 |
| G2_robust | 7 (동일 묶음) | **같은 실패 재현** |
| H0 | 10 | 4 arm 공통 붕괴, 원인 특정 불가 |
| H1 | 2 (mirror + joint DR) | mirror 효과 판정 불가 |

> **규칙**: I 배치의 모든 arm은 **직전 대조군 대비 정확히 1개**만 다르다.
> 예외는 사전에 문서에 근거를 적고 "bundle 시험"이라 명시한 경우뿐이다.

### L2. 측정 도구가 결과보다 먼저 검증돼야 한다 — 손실 1위

| 사건 | 증상 | 여파 |
|---|---|---|
| eval config 누락 | path 미학습 arm을 path 46% 과제로 채점 | E 배치 원본 리포트 4종 전부 무효 |
| 속도 지표 리셋 텔레포트 | `peak_speed` 최대 203 m/s | 전 구간 15.7~17.6% 오염. **이 배치가 올리려던 그 지표** |
| `path_lag = max(gap−lookahead, 0)` | 단측(one-sided) | 캐럿 **추월**(floor 붕괴)이 `lag=0` = 완벽으로 기록 |
| grid 승급 기준 `keepup_gap 2.0 m` | leash가 `path_lag ≤ 1.44 m`를 보장 | **구조적으로 실패 불가** → 커리큘럼이 1분 만에 만렙 |
| \|ω\| 지표 | yaw만 측정 | 상체 흔들림(롤·피치) 판정 근거가 애초에 없었음 |
| 외력 submit | decimation 10 중 1회 | **실전달 충격량이 명목의 1/10** (≈0.08 g) |
| `v7_extras` | 마지막 256-env snapshot | 대칭 분석의 통계적 무게 없음 |
| `time_out_buf` 갱신 시점 | 낙상과 goal-resample 동시 시 timeout이 낙상을 가림 | PPO bootstrap 오염 |

> **패턴**: 8건 중 6건이 **"판정에 쓴 상태가 판정 대상이 아니었다"**이다.
> 캐럿(위상 vs 거리), grid(leash가 자기 판정량을 상한), path_lag(단측),
> \|ω\|(축 하나), 외력(적분 구간), diag_reset(리셋 전후).

### L3. 코드 drift는 아무 데도 안 남는다
E1 재평가가 lookahead floor 도입 후 옛 체크포인트를 새 의미로 채점해 무효화됐다
(path 24.8 → 165.9 cm, 낙상 5 → 346). → `ENV_CODE_SHA` 기록으로 방어됨. **유지.**

### L4. 강건성 레버는 항상 "덜 움직이기"로 도망친다 — 2회 재현
E2와 G2가 같은 묶음에서 같은 결과를 냈다: 낙상은 줄지만(G2는 0회) 속도가 붕괴한다
(G2의 >1 m/s 체류 **0.50%**). 원인은 **씻기지 않는 구간별 목표 편향**(`goal_pos_bias`
σ=5 cm)이다 — 스텝 지터는 시간 평균으로 씻기지만 구간 고정 편향은 씻기지 않고,
보상은 참값을 읽으므로 정책은 자기가 틀렸다는 걸 알 수 없다. 기대값 최대화 해는
**천천히 가서 헛발질을 줄이는 것**이다.

> **규칙**: 강건성 항을 켤 때는 **속도 유지 게이트를 반드시 동반**한다.
> 그리고 `bias`(씻기지 않음)와 `jitter`(씻김)를 같은 레버로 묶지 않는다.

### L5. path 학습은 세 가지 대가를 동시에 청구한다
1. **waypoint 정확도** — dwell 도입 전 52.1 cm, 도입 후 5.5 cm. **G1의 dwell이 최대 성과.**
2. **전방 이동 prior** — 옆·뒤 목표에서 감속. path 노출과 단조:
   E0(path 0%) lateral이 straight보다 **+35% 빠름** → E1(46%) −4% → V7 −22%.
3. **낙상** — H에서 낙상의 **89.6%가 path 구간**.

### L6. 성공한 하네스 장치 — 그대로 승계
`ENV_CODE_SHA` 기록 / eval 기본 config = run 자기 config / path 구간 게이트 제외 /
영상 실패 시 exit code+stderr / `check_names.py`+`check_imports` / 5단계 smoke +
atomic marker / **공통 held-out 시험지 + effective test-config SHA 불일치 시 비교 거부** /
`model_0`과 초기 checkpoint를 selection 후보에 강제 포함 /
`(falls)/(completed+falls)×1000` (survivor bias 제거).

---

## 2. 사용자 강조 요구사항 — 우선순위

전 문서 20종에서 원문을 추출해 **반복 횟수**로 정렬했다.
(원문 위치: `masterplan2.md` 주석 17개, `gbatch.md` 주석 9개,
`MASTERPLAN_feedback.md` 불릿 50개, `masterplan2.md` 영상감상 13줄 — **그 외 문서에는
사용자 원문이 0줄이다.** 최신 문서일수록 사용자 목소리가 사라졌다.)

| 순위 | 요구 | 반복 | 시도 배치 | 현재 상태 |
|---:|---|---:|---|---|
| **P1** | 위치 ≤5/10 cm · heading ≤10° · **낙상 0** | **22+** | E·G·H | 위치·heading은 E0만 통과. **낙상은 전 배치 미통과** | <!--중요-->
| **P2** | 몸통속도 **1.3–1.5 m/s** | 9 | E1→G1→H | 미달. 원인 §3-2 | <!--중요-->
| **P3** | 외란을 **충격량 계산해서** 반영 | 9 | E2→G2/G3→H→M1 | 설정 40–150 N, **실전달 1/10** |
| **P4** | 좌우 대칭 (`right hip yaw`가 중심으로 치우침) | 10 | E0→G→H1/H2 | 지표가 3번 틀림. H1은 **역효과**(bias 2.9→9.4 cm) |<!--이건 그냥 빼자. robustness를 학습을 한 뒤에도 대칭이 안 맞으면 하는거로 밀어둬->
| **P5** | **팔 내리기** (팔뚝 90°, 손을 엉덩이 뒤로) | 8 | E0→G3→H | E0에서 확보 → **H가 asset 교체로 되돌림**(§3-1) |<!--k1_locomition.urdf의 기본자세로 하는거로 진행 ㄱㄱ-->
| **P6** | goal이 마구잡이로 변해도 robust | 7 | E2→G2→H | 낙상 0이나 "필터링 vs 느림" 미분리 (L4) |<!--큰 지터(2m)짜리는 학습과정에 넣을게 아니라 테스트만 하자. 학습에 넣을건 reprojection uncertainty만. 30cm에서 50cm정도-->
| **P7** | 8자·spiral·star 등 **연속 lookahead 목표** | 5 | E1→G1→H | G1 dwell로 수리. 낙상 89.6%가 path |<!--path도 big jitter와 마찬가지로 테스트만 하는건 어때? 학습 시에 집어넣지 말고-->
| **P8** | 로거/eval 병행 강제 | 7 | G·H | **가장 잘 이행됨** (L6) |<!--중요-->

**[질문] P1·P2·P8 <!--중요-->**
**[답변]**
> 접수. **I 배치의 채택 게이트는 P1(낙상 0)·P2(1.3–1.5 m/s)** 둘로 고정하고, 나머지는
> 비열세 조건으로만 쓴다. P8(로거/eval 병행)은 이미 하네스에 박혀 있다(L6) — 유지.

**[질문] P4 <!--이건 그냥 빼자. robustness를 학습을 한 뒤에도 대칭이 안 맞으면 하는거로 밀어둬-->**
**[답변]**
> **동의. 근거도 있다.** H1이 mirror를 강화했더니 오히려 mirror error +87.5%,
> 좌우 touchdown bias 2.9 → 9.4 cm로 **역효과**였다. 그리고 §3-1이 맞다면 그 실험 자체가
> asset 오염 위에서 돌았다. → **I 배치에서 제외.** robustness 학습 후에도 남으면 그때 본다.

**[질문] P5 <!--k1_locomition.urdf의 기본자세로 하는거로 진행 ㄱㄱ-->**
**[답변]**
> `k1/K1_locomotion.urdf`의 기본자세 = shoulder roll ∓1.35 rad, 팔꿈치 0
> = **`hbatch-codex.urdf`와 정확히 같은 각도**다. 즉 **H가 쓴 그 asset**이고,
> 양팔 yaw I_zz가 G1/E0 대비 **+32.3%**다.
>
> | | 각도 | I_zz | H에서 쓴 적 |
> |---|---|---|---|
> | `armsdown` (E0/G1이 학습) | roll ∓1.5708, elbow 2.08/∓1.58 | 0.07624 | 아니오 |
> | `k1_locomotion` 기본 = `hbatch-codex` | roll ∓1.35, elbow 0 | **0.10090** | **예 (4 arm 전부)** |
> | `hbatch`(신규) | roll ∓1.72, elbow 1.40/∓0.218 | 0.06984 | 아니오 |
>
> **지시하신 값이 제 유력 용의자와 같습니다.** 다만 §3-1은 **유력이지 확정이 아니고**,
> A 스크린이 지금 그걸 시험 중이다. → **A 스크린 결과를 보고 정한다.**
> A2(codex asset)만 무너지면 지시를 재고 요청드리고, 4셀 다 무너지면 asset은 무죄이므로
> 지시대로 `k1_locomotion` 기본자세로 간다.

**[질문] P6 <!--큰 지터(2m)짜리는 학습과정에 넣을게 아니라 테스트만 하자. 학습에 넣을건 reprojection uncertainty만. 30cm에서 50cm정도-->**
**[답변]**
> **동의하고, 이건 현재 설정보다 오히려 크게 키우는 지시다.** 현재 학습 노이즈는
> `goal_pos` σ=4 cm / `goal_pos_bias` σ=5 cm로 30–50 cm의 **1/10 수준**이다.
> - 학습: `goal_pos.range` σ를 0.30–0.50 m 상당으로 (reprojection uncertainty 모델)
> - 학습에서 제거: `goal_bt_flicker`(1 m 급 점프) → **평가 전용**
> - L4 규칙 적용: **`bias`(안 씻김)와 `jitter`(씻김)를 분리**하고 속도 유지 게이트 동반

**[질문] P7 <!--path도 big jitter와 마찬가지로 테스트만 하는건 어때? 학습 시에 집어넣지 말고-->**
**[답변]**
> **가장 큰 결정이고, 대체로 동의하지만 대가를 명시한다.**
> - 얻는 것: L5의 세 대가(waypoint 정확도·전방 prior·낙상 89.6%)가 **전부 사라진다**
> - 잃는 것: **지시 7-B(움직이는 목표 추종)를 학습에서 배우지 못한다.** path는 B축을
>   학습시키던 유일한 장치였다
> - **절충안**: path 자체는 빼되 **목표가 천천히 이동하는 waypoint**(예: 0.2–0.5 m/s로
>   드리프트하는 goal)를 낮은 비중으로 남긴다. 캐럿·leash·dwell·grid 기계 전부 불필요하고,
>   B축의 최소 형태만 유지된다
> → **I 1차는 path OFF로 간다.** 위 절충안은 I2 이후 별도 arm.

### 사용자 지시 7 — 병렬 만족해야 하는 3축

| 축 | 내용 | 현재 최고 | 대응 |
|---|---|---|---|
| **A** | 목표까지 **정확하고 빠르게** | 정확 E0(2.72 cm) / 빠름 G1(1.50 m/s p90) — **한 정책이 둘 다 한 적 없다** | §5 I1 |
| **B** | **움직이는 목표**에 대해 A 수행 | G1이 유일하게 근접(waypoint 5.5 cm + path) | §5 I2 |
| **C** | A·B를 하면서 외란/외력/팔걸림/충돌에 robust, **불안정 감지 시 목표추종보다 안정화 우선** | 미구현 | §5 I3 |

> **C의 "안정화 우선"은 이 프로젝트에 존재한 적 없는 개념이다.** 지금 구조는 보상 합이
> 항상 목표추종과 자세를 **동시에** 요구할 뿐, 상태에 따라 **우선순위를 바꾸는** 장치가
> 없다. §5 I3이 이를 다룬다.<!--중요. 나중에 안정화하는거 학습하고, walk-stablize-walk의 전환을 빨리 하는 것도 학습을 하자.-->

> **[답변]** 접수. **I 1차 범위 밖.** 단 전환 속도를 나중에 측정하려면 지금 **전환 지표를 먼저
> 심어야** 한다 — 불안정 진입/복귀 시각, 복귀까지 걸린 시간, 복귀 중 목표거리 증가량.
> 계측만 I 1차에 넣는다(비용 0).

---

## 3. 근본 원인 — 확정된 것

### 3-1. H 공통 붕괴: 팔 asset 교체 (유력, 미확정)

4 arm이 iteration 0부터 단조 열화해 **같은 값(≈0.40 m)으로 수렴**했고 `model_0`이
4/4 우승했다. 처치가 무관하면 공통부가 원인이다.

전 정점 7050개 메시 감사:

| asset | Trunk 관통 | ego 반폭 | 양팔 yaw I_zz | G1 대비 |
|---|---|---|---|---|
| `armsdown` (G1이 학습) | 25.3 mm | 0.160 | 0.07624 | — |
| `hbatch-codex` (H 4종 전부) | 0 | 0.251 | 0.10090 | **+32.3%** |
| `hbatch` (신규) | 0 | 0.155 | 0.06984 | −8.4% |

**그 교체는 물리적 이득이 0이다.** 팔 관절이 전부 `type="fixed"`이고
`asset.collapse_fixed_joints: true`라 팔이 Trunk와 **한 강체로 병합**된다 —
PhysX는 `armsdown`의 관통을 계산조차 하지 않는다. 사용자가 본 겹침은 **렌더링 전용**이다.
(`gbatch.md` §7-5④의 444,180 N은 팔꿈치가 `revolute`인 **armswing**이었다.)

→ **렌더링 수정 대가로 요 관성 +32.3%를 warm start 정책에 지불시켰다.**
현재 A 스크린(`hbatch.md` §4)이 이를 시험 중이다.
<!--롤백을 하라는 소리인가?-->

> **[답변]** **네. 두 가지 롤백이고 서로 다릅니다.**
> 1. **가중치 롤백**: H/G 체크포인트 버리고 `E0@model_6200`에서 재시작 — 지시 2, 확정
> 2. **asset 롤백**: `hbatch-codex.urdf` → `armsdown.urdf` — **7-3 때문에 보류**,
>    A 스크린 결과가 정함

### 3-2. 속도 미달: 케이던스가 보폭을 불가능하게 만든다

`gait_frequency: [1.8, 2.4]` Hz가 **명령속도와 독립 균일 샘플**이고(`goal_pose.py:446`),
1 사이클 = 2보(`goal_pose.py:1044-1045`) → 보속 = 2f = 3.6~4.8 보/초.

| 명령속도 | 필요 보폭 @1.8 Hz | @2.4 Hz | 실현가능(다리 0.52 × 0.5~0.6) |
|---|---|---|---|
| 1.0 m/s | 0.278 m | 0.208 m | 0.26–0.31 ✓ |
| **1.5 m/s** | **0.417 m** | 0.313 m | ✗ |
| 1.8 m/s | 0.500 m | 0.375 m | ✗ |

→ 정책의 유일한 대응은 **상체를 던져 CoM을 지지면 밖으로 내보내는 것**(제어된 낙하).
사용자가 관찰한 "고속에서 상체가 기울어짐"은 **학습된 선택이 아니라 강제된 결과**다.
G1의 실제 평균속도가 명령 0.4~1.9 m/s 전 구간에서 **0.32~0.49 m/s로 평평한** 것도 같은 원인.

> 이 처방(속도–케이던스 커플링)은 `gbatch_results.md` §3에서 "H 전부 채택"으로 선언됐으나
> **H0–H3 어느 config에도 반영되지 않았다.** 미이행 항목.

<!--나는 goal pose condition을 학습하고 있는데 속도샘플링이 무슨 뜻이지? 암튼 적합한 gait freq와 vel의 조합을 찾은다는 뜻이지?-->

> **[답변]** **맞게 이해하셨습니다 — 적합한 (gait_freq, 속도) 조합을 찾자는 뜻입니다.**
> 정확히는: `gait_frequency`가 **명령이 아니라 관측 슬롯 3번**으로 정책에 들어가고,
> 현재 `[1.8, 2.4]`에서 **매 구간 균일 난수**로 뽑힙니다. goal pose 과제에서 "속도"는
> 직접 명령되지 않고 **목표거리 ÷ 남은 시간**으로 암묵적으로 결정됩니다.
> 문제는 그 둘이 **독립**이라는 것 — 먼 목표(높은 암묵속도)에 낮은 케이던스가 뽑히면
> 물리적으로 불가능한 보폭이 요구됩니다. → 케이던스를 **암묵 요구속도의 함수**로 뽑는다.

### 3-3. 정확도: 공 내부에 기울기가 없다

`goal_reach_radius 0.1`, `goal_reached` 이진, `constellation_weight 0.2 × scale 3.5`:
- 10 cm 문턱 통과 = **+1.0/step**
- 오차 10→5 cm = **+0.005/step**
- **비율 200배**

E0가 같은 보상으로 2.72 cm를 냈으므로 이것이 **절대적 한계는 아니다.** 다만 공 안에서
어디에 앉을지는 다른 압력이 정하며, path 학습이 그 압력을 5.5 cm 쪽으로 민다.
→ **필수가 아닌 강건화 항목.**
<!--공이 무슨 뜻이지? goal? -->

> **[답변]** **goal 주위 반경 10 cm 구(sphere)**입니다 — `goal_reach_radius: 0.1`.
> 그 안에 들어가면 `goal_reached`가 +1/step을 주는데, **안에서는 더 가까워져도 보상이
> 거의 안 늘어납니다**(200배 차이). 그래서 "공 안에 겨우 들어가서 멈추기"가 최적해가 됩니다.

### 3-4. 옆·뒤 목표 감속: path 전방 prior (§L5-2, 반증 완료)

constellation 결합 때문이라는 가설은 **E0가 반증한다** — 같은 보상, path 미학습,
시작거리 0.6–0.9 m 통제 시 lateral peak **0.91 m/s** vs straight **0.71 m/s**.
E0에서는 lateral이 **더 빠르다.**

---

## 4. 실기 standing 불안정 (사용자 지시 6)

### 4-1. "body height 리워드로 고쳤다"는 주장 — **기각**

| 검증 | 결과 |
|---|---|
| 이미 있는가 | **있다.** `base_height: -20`, `base_height_target: 0.52`, `_reward_base_height = (h − 0.52)²`. Booster T1 레퍼런스와 같은 가중치 |
| 수치적으로 작동하는가 | E0 기본 자세의 몸통 높이가 이미 **0.5144 m** → 명목에서 **−0.0007/step**, 2 cm 침하에서 −0.008. `survival` +0.25, `constellation` 최대 +3.5 대비 무의미. `only_positive_rewards: true`가 아예 잘라낼 수도 있음 |
| 관측 가능한가 | **아니다.** base height는 54차원 관측에 **없다.** 보상은 특권 정보(ground truth)를 읽으므로 **개루프 자세 prior**만 설치할 수 있다 |
| 단일 지지에서 유효한가 | **아니다.** 한 발을 들면 다리 기구학이 몸통 높이를 더 이상 결정하지 않아, **정확히 실패하는 지점에서 신호가 없다** |
| 부작용 | 높이를 올리면 무릎이 펴져 capture point 한계가 **줄어든다** (`v_max = w√(g/l)`: 0.173 → 0.163 m/s) |

> **판정: 이미 최대 가중치로 들어 있고, 이 실패 양상에서는 수치적으로 불활성이다.**
> 친구분이 옳은 유일한 경우는 **로봇이 넘어지기 전에 눈에 띄게 주저앉을 때**다.
> → 확인 질문: "넘어지기 직전에 키가 낮아지던가?" 아니라면 다른 원인이다.
<!--일단 주저앉아. 그건 확실해. 확인질문에 대한 답: yes. 그리고 임의로 0.58로 올리니까 잘 서던데? 최적값을 찾는거를 목표로 세워-->

> **[답변]** **철회합니다. 친구분 방향이 맞습니다.** 제 기각의 유일한 반증 조건이 "넘어지기 전에
> 주저앉는가"였고 **yes**라고 하셨습니다. 그리고 코드에 이미 모순이 있습니다:
>
> ```yaml
> init_state.pos:      [0, 0, 0.58]     # 스폰 높이
> base_height_target:  0.52             # 보상이 끌고 가는 높이
> ```
> **스폰은 0.58인데 보상은 0.52로 끌어내립니다.** 6 cm 차이이고, 0.58에서 잘 섰다는
> 관찰과 정확히 일치합니다. → **`base_height_target`을 탐색 대상으로 승격**하고
> 0.52 / 0.55 / 0.58 세 값을 단일 레버로 비교한다. (§4-1 표는 "이미 있으니 무의미"가
> 아니라 **"값이 틀렸다"**로 정정.)

### 4-2. 진짜 원인 후보 ① — **배포 래퍼가 gait clock을 게이팅하지 않는다** (코드 확정)

학습:
```python
# goal_pose.py:490 — stand 목표는 클럭을 얼려 feet_swing 유인을 제거한다
self.gait_frequency[env_ids[stand]] = 0.0
```

ParameterWalk 배포 (`deploy/utils/policy.py:42-59`) — **게이팅 있음**:
```python
if np.linalg.norm(self.smoothed_commands) < 1e-5:
    self.gait_frequency = 0.0
self.obs[9] = np.cos(2*np.pi*self.gait_process) * (self.gait_frequency > 1.0e-8)
```

E0 배포 (`deploy/utils/policy_goal_pose.py:116-130`) — **게이팅 없음**:
```python
self.gait_process = np.fmod(time_now * self.gait_frequency, 1.0)   # 무조건
self.commands[3]  = self.gait_frequency                             # 무조건
self.obs[16] = np.cos(2.0*np.pi*self.gait_process)                  # 무조건
self.obs[17] = np.sin(2.0*np.pi*self.gait_process)
```
같은 파일 30줄에 **그 사실이 주석으로 적혀 있다**:
`NOTE: E0 does NOT gate the gait clock / commands by gait_frequency the way ...`

> **결론: 실기에서 E0는 stand regime에 진입할 수 없다.** 목표가 0이어도 2 Hz 클럭이
> 계속 돌아 제자리걸음한다. 관찰된 증상("서 있는 자세가 불안정, 한 발 들면 넘어짐")과 일치한다.
> **이것은 학습 문제가 아니라 배포 코드 문제이며, 리워드를 만지기 전에 고쳐야 한다.**
<!--배포에 관한거는 추후에 수정할테니까 해당 파일로 코멘트 위치를 옮겨.-->

> **[답변]** 접수. §4-2를 `htwk-gym/deploy/utils/policy_goal_pose.py` 상단 주석으로 옮기고
> ibatch.md에는 한 줄 포인터만 남긴다. **학습 항목이 아니므로 I 배치 게이트에서 제외.**
### 4-3. 진짜 원인 후보 ② — 단일 지지 자체가 학습 분포에 없다

`stand` 카테고리는 클럭을 0으로 만든다 → 정지 학습이 **양발 지지 정적 자세**로만 이뤄진다.
단일 지지는 **보행 중에만** 등장하고 그때는 전진 모멘텀이 안정성을 대신 준다.
**"제자리에서 한 발을 드는" 상태는 학습된 적이 없다.**
<!--그러면 이건 어떻게 해야해?-->

> **[답변]** `stand` 카테고리에서 클럭을 0으로 만드는 대신(`goal_pose.py:490`), **stand의 일부를
> 저주파 체중이동으로** 바꾼다. 즉 목표는 그대로 "제자리"인데 gait_frequency를
> 0.5–0.8 Hz로 주면 **제자리에서 좌우 단일지지**가 학습 분포에 들어온다.
> 비용: `stand` 비중 0.1 중 절반만 쓰면 전체의 5%. 관측·구조 변경 없음.

### 4-4. 물리적 여유가 없다 (K1 수치)

| 항목 | 값 |
|---|---|
| 무게 / CoM 높이 | 183.6 N / 발목 위 0.40 m |
| 발 반폭 (실제 충돌 박스 0.16×0.07) | **0.035 m** |
| 발산 시상수 √(l/g) | **0.202 s** |
| 2 Hz 단일지지 구간 | 0.25 s = **1.24 e-folds** → 이지 1 cm 오차가 접지 시 3.5 cm = **발 밖** |
| 최대 횡방향 복원 모멘트 | **6.4 N·m — 발 크기 제한**(발목 롤 한계 15 N·m가 아님) |
| `m·g·l` vs 발목 Kp | **74 N·m/rad > 50** → 발목은 **수동적으로 불안정**, 균형은 전부 능동 피드백 |
<!--이거에 대해서는 내가 어찌할 수 있지? 피드백에 반영해야해?-->

> **[답변]** **대부분 하드웨어 상수라 학습으로 못 바꿉니다.** 다만 셋은 반영 가능합니다:
> 1. **발 크기** — `feet_edge_pos`가 실제보다 크게 잡혀 있다(7-14). 고치면 sim이 실제만큼
>    어려워진다 → **이게 가장 큰 항목**
> 2. **발목 Kp 50 < m·g·l 74** — 실기에서 Kp를 올릴 수 있다면(7-16) sim/실기 동시 상향
> 3. 나머지(무게·CoM·발산 시상수)는 **설계 상수** → 학습이 그 안에서 살아남게 하는 수밖에 없다

### 4-5. 그 외 확인된 sim2real 격차

| 항목 | 현재 | 참조 |
|---|---|---|
| `asset.feet_edge_pos` | **T1의 발**(K1 박스보다 길이 +39%, 폭 +43%) — sim 접촉 판정을 좌우 | — |<!--T1이 갑자기 왜 나와 병신새끼야. 이거 k1에 맞게 수정이 안돼있어? crosscheck.-->
| 배포단 target EMA 0.8/0.2 | 학습에 없음. τ≈9 ms → 25 Hz에서 이득 0.58, 위상 54° 지연 | — |
| latency DR | 0–18 ms, **지터 없음** | HuB/ASAP 20–60 ms | <!--이건 무조건 넣어야지.-->
| PD 랜덤화 | **±5%** | 타 humanoid 배포 ±25% | <!--로봇에 넣을때 모터 pd를 내가 조정할 수 있던데? 100 50 혹은 80 30 이렇게. 이건 뭐야? 학습 내부로 집어넣을 수 없는건가?-->
| `joint_encoder_bias` | 코드에 있으나 **E0에서 OFF** | — |<!--이거 무조건 default로 해야하는게 k1의 joint zero가 꽤나 자주 틀어저. 그래서 posture가 비정상이되고 zero cali를 해야하는데 이거 랜덤화로 해결할 수 있는지를 절대 까먹으면 안돼. 최선을 다해서 최고의 결과를 낼 수 있는 문제해결 방법을 알아내봐-->
| IMU 장착 회전 DR | **없음** | 1° ≈ 7 mm ≈ **발 반폭의 20%** | <!--특히 이것도 randomization이 필요한 것이 개체차가 이미 존재하며, imu값은 우리가 안 만져. cali도 못해. 틀어지든 말든 믿고 가야하는 항목인데 joint encoder와 같이 묶어서 강건화 계획을 짜봐.-->
| 발 접촉력 기반 보상 | **35개 보상 중 0개.** `contact_force`는 텐서 셋업(`goal_pose.py:209-223`)에만 등장 | CoP/ZMP·하중분배·발목전략 전부 부재 |<!--접촉력을 real에서 측정할 수 있는 방법이 없는데.. 이건 어떻게 하겠다는거야?-->

**[질문] feet_edge_pos <!--T1이 갑자기 왜 나와 병신새끼야. 이거 k1에 맞게 수정이 안돼있어? crosscheck.-->**
**[답변]**
> | | x 앞 | x 뒤 | 길이 | y 반폭 | 폭 |
> |---|---|---|---|---|---|
> | `feet_edge_pos` (설정) | +0.1215 | −0.1015 | **0.223 m** | ±0.050 | **0.100 m** |
> | 실제 충돌 박스 (URDF) | +0.094 | −0.066 | **0.160 m** | ±0.035 | **0.070 m** |
> | 차이 | +29 mm | −36 mm | **+39%** | +15 mm | **+43%** |
>
> `left_foot_link` collision = `box 0.16 0.07 0.032 @ (0.014, 0, −0.008)`.
> **sim의 발 모서리 판정이 실제 발보다 사방으로 1.5~3.6 cm 크다.** §4-4의 발 반폭 0.035 m가
> 설정상 0.05 m로 들어가 있으니, **sim은 실제보다 43% 넓은 지지면에서 균형을 배웠다.**
> → `feet_edge_pos`를 실제 박스 모서리로 교정. **I 배치 필수 항목으로 승격.**

**[질문] latency DR <!--이건 무조건 넣어야지.-->**
**[답변]**
> 동의. 현재 0–18 ms, 지터 없음 → **20–60 ms + 스텝별 지터**로. 관측·구조 변경 없음.

**[질문] PD 랜덤화 <!--로봇에 넣을때 모터 pd를 내가 조정할 수 있던데? 100 50 혹은 80 30 이렇게. 이건 뭐야? 학습 내부로 집어넣을 수 없는건가?-->**
**[답변]**
> **넣을 수 있고, 넣어야 합니다.** 현재 sim은 `Hip 100 / Knee 100 / Ankle 50`,
> damping `2/2/1`이고 랜덤화는 **±5%** 뿐이다.
> - 실기에서 조정 가능하다면 **배포할 값을 먼저 정하고 sim을 거기에 맞춘다** — 지금은
>   sim 값이 무엇을 겨냥한 건지 문서에 없다
> - ±5%는 타 humanoid 배포(±25%) 대비 **5배 좁다**. armD가 PD 2배로 붕괴한 전례가 있으니
>   **범위를 넓히되 단일 레버로** 검증
> - 발목 Kp 50은 `m·g·l = 74 N·m/rad`보다 **작아서 수동 불안정**(§4-4).
>   실기에서 올릴 수 있다면 이게 standing에 직접 듣는 항목이다

**[질문] joint_encoder_bias <!--이거 무조건 default로 해야하는게 k1의 joint zero가 꽤나 자주 틀어저... 랜덤화로 해결할 수 있는지를 절대 까먹으면 안돼-->**
**[답변]**
> **부분적으로 됩니다. 그리고 이건 우선순위 상위입니다.**
> - `joint_encoder_bias`는 **코드에 이미 있는데 E0에서 꺼져 있다** → 켠다
> - 다만 두 개를 분리해야 한다:
>   | 항목 | 의미 | 대응 |
>   |---|---|---|
>   | `joint_encoder_bias` | 정책이 **보는** q가 틀림 | 관측에만 더함. 정책이 "내 센서는 틀릴 수 있다"를 배움 |
>   | `joint_target_offset` | PD **목표**가 틀림 | 실제 토크가 틀린 자세를 향함 |
>   실기의 zero 틀어짐은 **둘 다** 일으킨다 → 같은 크기로 함께 켠다
> - **랜덤화가 못 고치는 것**: bias가 관측에 없으므로 정책은 **어느 방향으로 틀렸는지 모른다.**
>   기대할 수 있는 건 "어떤 offset에서도 안 넘어지는 보수적 자세"이지 **offset 보정이 아니다**
> - **더 나은 해법(권고)**: 랜덤화 + **기립 시 자동 zero 추정**. 양발 접지·정지 상태에서
>   기구학으로 몸통 자세를 풀면 관절 zero 오차가 관측 가능해진다. 이건 학습이 아니라
>   배포단 루틴이며, 랜덤화보다 근본적이다
> → **I 배치: 랜덤화 ON(단일 레버). 배포단 zero 추정은 별도 항목으로 §6에 등재.**

**[질문] IMU 장착 회전 DR <!--imu값은 우리가 안 만져. cali도 못해. joint encoder와 같이 묶어서 강건화 계획을 짜봐.-->**
**[답변]**
> 동의. 둘은 **같은 종류의 결함**이다 — *정책이 볼 수 없는, 에피소드 내내 고정된 관측 편향*.
> → **하나의 레버 `calibration_dr`로 묶는다**: IMU 장착 회전(roll/pitch ±1–2°, yaw ±3°)
> + joint encoder bias + joint target offset. 셋 다 에피소드 상수, 셋 다 관측에 없음.
> 묶는 근거는 L1(변인통제)의 예외 조건 — **같은 물리적 원인(캘리브레이션)이고 실기에서
> 분리 측정이 불가능**하므로 bundle로 시험하고 문서에 명시한다.

**[질문] 발 접촉력 보상 <!--접촉력을 real에서 측정할 수 있는 방법이 없는데.. 이건 어떻게 하겠다는거야?-->**
**[답변]**
> **정책 관측에 넣자는 게 아닙니다. 보상에만 씁니다.**
> - 보상은 시뮬레이터의 **특권 정보(privileged)**를 읽어도 된다. `goal_dist`도 이미 그렇다
> - 정책 관측 54차원은 **그대로**다 → warm start 보존
> - 학습이 끝나면 그 보상은 사라지고, 남는 것은 "양발 하중을 고르게 쓰는 자세"라는 **행동**
> - 실기에서 측정할 필요가 없다
> → 이 방식(reward-only privileged term)은 asymmetric actor-critic의 표준 용법이다.

---

## 5. 수정안 — 근거 1:1 매핑

**아직 구현하지 않았다.** 각 항목은 근거 번호를 달고 있고, 근거 없는 항목은 없다.

### 5-0. 공통 전제

| # | 조치 | 근거 |
|---|---|---|
| **C1** | **가중치를 `E0@model_6200`으로 롤백** | §0 (3세대 연속 후퇴), 사용자 지시 2 |
| **C2** | asset은 A 스크린 결과가 정한다. 확정 전에는 **`armsdown`(G1/E0가 학습한 것)** 사용 | §3-1 |
| **C3** | arm당 대조군 대비 **정확히 1레버** | L1 |
| **C4** | L2의 계측 수정 8건이 전부 반영된 하네스에서만 학습 | L2 |
<!--만약 카드 당 두개씩 돌리느라 9시간 12시간 걸릴 것 같으면 카드 당 하나씩 4시간으로 해서 빠르게 여러번 돌리자. 계획을 모두 리스트업을 해. decision tree마냥. 비고도 크게크게 작성해서 언제 다시보든 계획을 이어갈 수 있도록 하고.-->

> **[답변]** **동의. 그리고 지금 데이터가 그 선택을 지지한다.** H의 열화는 iteration 100에 이미
> 1.4–1.8배였다 — **긴 학습이 아니라 짧은 학습 여러 번이 맞다.**
> 결정 트리는 §7에 작성.

### 5-A. 실기 standing — 학습 전에 고칠 것 (사용자 지시 6)

| # | 조치 | 근거 | 성격 |
|---|---|---|---|
| **S1** | `policy_goal_pose.py`에 ParameterWalk와 **같은 gait 게이팅** 추가 | §4-2 | **배포 코드 수정. 재학습 불필요** |
| **S2** | 진단 먼저: 낙상 특성화 → 클럭 회전 확인 → 오프라인 관측 재생 → 발목 추종/포화/대역폭 → IMU 프레임 → 발 형상 감사 | §4-4, §4-5 | 측정 |
| **S3** | `asset.feet_edge_pos`를 K1 실측 발로 교정 | §4-5 | asset |
| **S4** | 배포단 EMA(0.8/0.2)를 학습 액션 필터에 반영하거나 배포에서 제거 | §4-5 | 정합 |
| ~~S5~~ | ~~base height 리워드 추가~~ | §4-1 **기각** | — |

### 5-B. I 배치 arm 후보 (사용자 지시 7의 3축)

| arm | = E0 + | 축 | 근거 | 반증 조건 |
|---|---|---|---|---|
| **I0_control** | (없음, 하네스만 신규) | — | C1·C4 | E0 재현 실패 시 하네스가 원인 |
| **I1_cadence** | **속도–케이던스 커플링** `f = clip(v_cmd/(2·L_max), 1.6, 3.2)`, `L_max≈0.28` | **A(빠름)** | §3-2, P2 | 속도 미상승 또는 정확도 열화 |
| **I2_stand** | **단일지지 학습**: `stand` 카테고리에서 클럭을 0으로 두지 않고 **저주파 체중이동**을 명령 + 발 하중분배 보상 | **C(안정)**, P1(낙상 0) | §4-3, §4-5 | 단일지지 생존시간 미개선 |
| **I3_path** | path 재도입 (dwell 포함), **dose를 축으로** 0.15 | **B(움직이는 목표)** | L5, §3-4 | lateral 감속이 dose와 무관 |

**I2가 P1(낙상 0)의 핵심이다** — 전 배치가 낙상 게이트를 통과한 적이 없고,
발 접촉력을 쓰는 보상이 35개 중 0개다(§4-5).

### 5-C. "불안정 감지 시 안정화 우선" (사용자 지시 7-C)

현재 구조에 **없는 개념**이다. 두 방식이 가능하다:

| 방식 | 형태 | 장점 | 위험 |
|---|---|---|---|
| **게이팅 보상** | 불안정 지표 `u`(예: `‖projected_gravity_xy‖`, CoP 여유, \|ω_xy\|)로 목표추종 항을 **감쇠**: `constellation × (1−σ(u))` | 관측 변경 없음, 54차원 유지 | 게이트가 항상 켜지면 E2형 저속 붕괴(L4) |
| 계층 정책 | 안정화 정책과 추종 정책을 분리 | 명시적 | 관측/구조 변경 → **warm start 파괴** |

→ **게이팅 보상만 검토한다.** 단 L4에 따라 **속도 유지 게이트를 반드시 동반**하고,
`u`의 임계값은 §4-4의 물리량(발 반폭 0.035 m, 발산 시상수 0.202 s)에서 역산한다.
**단독 arm(I4)으로 격리하며, I 배치 1차에는 넣지 않는다** — I2가 먼저 서야 의미가 있다.
<!--좋아. 단계적으로 하되, 계획은 미리 해야지. 근데 이걸 발의 값을 계산해서 사용하나?-->

> **[답변]** **네. 그리고 지금 그 값이 틀려 있습니다(7-14).** heel 위치 `x = −0.1015 m`는
> `feet_edge_pos`의 뒤쪽 모서리에서 온 값인데, 실제 박스 뒤끝은 **−0.066 m**다.
> heel 관련 보상을 만들면 **3.6 cm 틀린 지점**을 heel이라 부르게 된다.
> → 7-14를 먼저 고치고, 그 다음에 heel 관련 항목을 논한다. **순서가 중요하다.**
>
> ---

---

## 6. 미해결 / 사용자 판단 필요

| # | 항목 | 왜 내가 정하지 않았나 |
|---|---|---|
| Q1 | **친구분 확인 질문**: 넘어지기 직전에 로봇이 눈에 띄게 **주저앉는가?** | 예이면 §4-1의 기각이 뒤집힌다 |
| Q2 | A 스크린(진행 중) 결과 전에 I를 시작할지 | asset이 확정되면 C2가 정해진다 |
| Q3 | 낙상 0(P1)과 속도 1.3–1.5 m/s(P2)를 **동시에** 요구할지, 단계로 나눌지 | 지금까지 한 정책이 둘 다 한 적이 없다 |
| Q4 | `stand_posture −1`, `stop_ang_speed_threshold 0.3`(H 공통 변경 D) | A 스크린이 아직 분리하지 않는 축 |
| Q5 | 실기 `.pt` export 단계 | 문서상 사용자 본인 작업으로 표시돼 있다 |

**탐색 원본**: `standing_balance_research.md`(선례·인용·8단계 진단 체크리스트),
`user_requirements.md`(원문 인용 전수·반복 횟수·배치별 시도 이력) — 둘 다 스크래치패드.

---

## 7. 실행 계획 — 결정 트리 (카드당 1 프로세스, 라운드당 ~4시간)

> 근거: H 열화가 iteration 100에 이미 1.4–1.8배(§0). **긴 학습 1회 < 짧은 학습 여러 회.**
> 카드당 2 프로세스는 처리량이 1.5배뿐이라, 카드당 1개로 **지연시간을 반으로** 줄이는 쪽이
> 라운드를 자주 도는 데 유리하다. 라운드마다 **2 arm 동시**, 라운드 간 판단.

**공통 전제 (전 라운드)**: 가중치 `E0@model_6200` (7-7). 하네스는 L2의 계측 수정 8건 반영.
arm당 대조군 대비 **1레버** (L1). path OFF (7-5). 대칭 항목 제외 (7-2).

```
R0 ─ 재현 확인 ─────────────────────────────────────────  2 arm, ~2h
 │   I0a = E0 config + 신규 하네스 (레버 0개)
 │   I0b = I0a + feet_edge_pos 실측 교정 (7-14)
 │
 ├─ I0a가 E0(2.72cm/낙상2)를 재현 못함 → ■ STOP. 하네스가 원인. R0 재설계
 └─ 재현함 ─────────────────────────────────────────────
     │  ※ I0b가 나빠져도 정상. sim이 실제만큼 어려워진 것. 이후 전부 I0b 기준.
     ▼
R1 ─ standing (P1: 낙상 0) ────────────────────────────  2 arm, ~4h
 │   I1a = I0b + base_height_target 0.55        (7-10)
 │   I1b = I0b + base_height_target 0.58        (7-10)
 │   판정: 단일지지 생존시간 · 낙상률 · 위치 비열세
 │
 ├─ 둘 다 무개선 → 높이는 원인 아님. R2로 (7-12 단일지지 학습이 본진)
 └─ 개선 → 이긴 값 채택 ────────────────────────────────
     ▼
R2 ─ 단일지지 학습 ────────────────────────────────────  2 arm, ~4h
 │   I2a = R1승자 + stand 절반을 0.5–0.8Hz 체중이동으로  (7-12)
 │   I2b = R1승자 + 발 하중분배 보상(reward-only privileged) (7-19)
 │   판정: 제자리 단일지지 생존시간, 낙상 0 접근도
 │
 └─ ▼ (둘 다 좋으면 다음 라운드에서 합침 — bundle임을 명시)
R3 ─ 캘리브레이션 강건성 ───────────────────────────────  2 arm, ~4h
 │   I3a = R2승자 + calibration_dr (IMU회전 + encoder bias + target offset) (7-18)
 │   I3b = R2승자 + latency DR 20–60ms + 지터        (7-15)
 │   판정: held-out offset/지연에서의 열화폭. clean 비열세 필수 (L4)
 │
 └─ ▼
R4 ─ 속도 (P2: 1.3–1.5 m/s) ───────────────────────────  2 arm, ~4h
 │   I4a = R3승자 + 케이던스–속도 커플링             (3-2, 7-8)
 │   I4b = R3승자 + PD 범위 확대 ±25% (+ 발목 Kp 상향) (7-16)
 │   판정: 구간최고속도 p90, 추종비 단조성, 낙상 비열세
 │
 └─ ▼
R5 ─ goal 불확실성 (P6) ───────────────────────────────  2 arm, ~4h
     I5a = R4승자 + reprojection noise 30–50cm (bias만)  (7-4)
     I5b = R4승자 + reprojection noise 30–50cm (jitter만) (7-4)
     ※ L4: 속도 유지 게이트 동반. 큰 지터·path는 평가 전용.
```

### 비고 (다시 볼 때 필요한 것)

| 항목 | 값 / 위치 |
|---|---|
| 가중치 출발점 | `logs/K1/K1/Goal_Pose_V7/2026-07-26-19-36-15_E0_armB_armsdown/nn/model_6200.pth` |
| E0 기준 수치 | 위치 2.72/5.01 cm · heading 2.52° · strict 89.3% · 낙상 2 / 4633구간 |
| 채택 게이트 | P1(낙상 0) · P2(1.3–1.5 m/s). 나머지는 **비열세**만 |
| 라운드당 iteration | **200** (H 열화가 100에 이미 확정적) |
| 중단 규칙 | iteration 100에서 위치 median이 출발 대비 1.3배 초과 → 즉시 STOP |
| asset | **A 스크린 결과 대기** (7-3). 결과 전에는 `armsdown` |
| 평가 | 전 arm 동일 held-out 시험지. config SHA 불일치 시 비교 거부 (L6) |
| 제외 항목 | 좌우대칭(7-2) · path 학습(7-5) · 안정화 우선(7-6) · 배포 게이팅(7-11) |
| 계측만 추가 | 전환 지표(7-6), 단일지지 생존시간, 발 하중분배 |

### 라운드별 예상 비용

| | arm | iteration | 카드 | 벽시계 |
|---|---:|---:|---:|---|
| R0 | 2 | 200 | 2 (각 1개) | ~2h (재현 확인만) |
| R1–R5 | 각 2 | 200 | 2 (각 1개) | 각 ~4h |
| **합계** | **12** | — | — | **~22h** |

H 한 배치(4 arm × 12000 iter)가 약 4 GPU-일이었다. **같은 예산으로 6라운드를 돈다.**

---

## 8. R0 구현 (2026-08-03)

### 8-1. 무엇을 만들었나 — 신규 파이프라인 없음

기존 `make_v7_arms.py` + `tonight.sh`를 그대로 쓴다. **새 도구를 만들지 않았다** —
L2(측정 도구가 결과보다 먼저 검증돼야 한다)에 따라, 검증된 하네스를 두고 새 하네스를
만드는 것이 이 프로젝트가 반복한 실수다.

`tools/make_v7_arms.py` 변경 (4곳, 전부 추가):

```python
FOOT_EDGE_REAL = [[ 0.094,  0.035, -0.024], [ 0.094, -0.035, -0.024],
                  [-0.066,  0.035, -0.024], [-0.066, -0.035, -0.024]]

I_ARMS = {
    "I0a_repro": dict(**_OFF_PATH, **_OFF_ROBUST, **_OFF_PROTECT),
    "I0b_foot":  dict(**_OFF_PATH, **_OFF_ROBUST, **_OFF_PROTECT,
                      **{"asset.feet_edge_pos": FOOT_EDGE_REAL}),
}
ARMS_ON_E0 = {"I0a_repro", "I0b_foot", ...}       # -> ARMSDOWN_CKPT = E0@6200
ALL_ARMS   = dict(**ARMS, **F_ARMS, **I_ARMS, **V8_ARMS)
GPU_OF     = {..., "I0a_repro": "cuda:0", "I0b_foot": "cuda:1"}   # 카드당 1개
```

`FOOT_EDGE_REAL` 유도 — `left_foot_link` collision `box 0.16 0.07 0.032 @ (0.014, 0, −0.008)`:

| 축 | 계산 | 결과 |
|---|---|---|
| x | 0.014 ± 0.080 | **−0.066 … +0.094** (기존 −0.1015 … +0.1215) |
| y | 0 ± 0.035 | **±0.035** (기존 ±0.050) |
| z (바닥) | −0.008 − 0.016 | **−0.024** (기존 −0.030) |

### 8-2. 실행

```bash
cd ~/RoboCup/k1-goalpose && git pull && cd htwk-gym && conda activate k1goalpose && ARMS="I0a_repro I0b_foot" ITERS=200 SESSION=i bash tools/tonight.sh
```

`tonight.sh`가 이미 하는 것: `check_names` → arm별 config 생성 → **arm별 smoke 게이트**
→ 통과 arm만 tmux 실행 → 120 s 기동 확인 → 실패 arm 마지막 출력 덤프.
`CKPT` 기본값이 이미 `E0@model_6200`이라 롤백(§7-7)이 자동으로 적용된다.

### 8-3. 판정 — 사전 고정

| 결과 | 해석 | 다음 |
|---|---|---|
| **I0a가 E0(2.72 cm / 낙상 2)를 재현** | 하네스 건전 | R1 진행 |
| I0a가 재현 실패 | **하네스가 원인.** L2의 8건 수정 중 무언가가 과제를 바꿨다 | ■ STOP. R0 재설계 |
| I0b < I0a (나빠짐) | **정상이자 목적.** sim이 실제만큼 어려워졌다 | I0b를 이후 전 라운드 기준선으로 |
| I0b ≈ I0a | 발 크기가 성능을 좌우하지 않았다 | 그래도 I0b 채택(실물 정합) |
| 둘 다 200 iteration 내 위치 median 1.3배 초과 | 중단 규칙 발동 | ■ STOP |

**I0b가 나빠지는 것을 실패로 세지 않는다.** 이 arm의 목적은 성능이 아니라
**기준선을 실물에 맞추는 것**이다. 이걸 명시해 두지 않으면 다음에 볼 때
"I0b 실패"로 오독하고 되돌릴 위험이 있다.

### 8-4. 이 라운드에서 의도적으로 하지 않은 것

| 항목 | 이유 |
|---|---|
| `base_height_target` 변경 | R1의 단일 레버 (§4-1 답변) |
| 팔 asset 변경 | A 스크린 결과 대기 (P5 답변) |
| 단일지지·하중분배·캘리브레이션 DR·케이던스 | R2–R4 |
| path·큰 지터 | 학습 제외 확정 (P6·P7 답변) |
| 좌우대칭 | 제외 확정 (P4 답변) |

### 8-5. R0 확정 (2026-08-03 재설계) — 4 arm, 2 카드, ~26분

카드는 **2장**이다(`Device count 2`). 200 iteration이면 arm 하나가 ~13분이라
카드당 2개로 돌려도 총 **~26분**이고, "카드당 하나" 규칙이 지키려던 것은
처리량이 아니라 **회차 지연시간**이므로 이 길이에서는 2개씩이 맞다.

| arm | I0a 대비 유일한 차이 | 카드 | 묻는 것 |
|---|---|---|---|
| **I0a_repro** | (대조군) | 0 | 하네스가 E0를 재현하나 |
| **I0b_foot** | `feet_edge_pos` 실측 교정 | 0 | 진짜 발 크기에서 얼마나 어려워지나 |
| **I0c_h055** | `base_height_target` 0.52 → 0.55 | 1 | 어느 높이가 맞나 |
| **I0d_h058** | `base_height_target` 0.52 → 0.58 | 1 | 〃 (실기에서 잘 섰다는 값) |

**전부 `I0a` 대비 단일 레버**다(L1 준수). 같은 카드에 붙인 쌍이 곧 핵심 비교쌍이라
열·클럭 조건이 동일하다.

#### `base_height_target`은 하드코딩인가 — 아니다

사용자 원칙("결과를 보상하지 말고 조건을 만들어라")에 걸리는지 검토했다. **안 걸린다.**
이 항은 **이미 scale −20으로 존재**하고, 문제는 항의 존재가 아니라 **상수가 틀렸다**는 것이다:

```yaml
init_state.pos:      [0, 0, 0.58]   # 스폰 높이
rewards.base_height_target: 0.52    # 보상이 끌어내리는 높이
```

**6 cm 차이로 config가 자기모순**이다. 실기에서 넘어지기 전에 주저앉고 0.58에서 잘 선다는
관찰이 이와 일치한다. 즉 **새 shaping 항을 추가하는 것이 아니라 틀린 상수를 재는 것**이고,
`feet_edge_pos` 교정과 같은 부류다.

#### 실시간 판정 — `tools/watch_eval.py`

checkpoint가 나올 때마다 20 s × 256 env로 채점하고, **학습 전에 고정한 판정**을 넘기면
`STOP` 파일을 쓴다. `runner_v3.py`가 이미 `STOP`을 읽어 checkpoint 저장 후 정상 종료한다.

| 규칙 | 값 | 근거 |
|---|---|---|
| 기준 위치 median | **0.0272 m** | E0@6200 실측 |
| 기준 낙상 | **2** | 〃 |
| 중단: 위치 | `> 1.3 × 기준` | H가 iteration 100에 1.4~1.8배였다 |
| 중단: 낙상 | `> max(3×기준, 기준+5)` | 낙상은 P1의 유일한 미통과 게이트 |
| 평가 실패 시 | **절대 중단 안 함** | 조용한 실패로 정상 run을 죽이지 않는다 |

#### 판정표 — 실행 전 확정

| 관측 | 결론 | 다음 |
|---|---|---|
| I0a가 2.72 cm / 낙상 2 재현 | 하네스 건전 | R1 진행 |
| I0a가 STOP에 걸림 | **하네스가 원인**(L2 수정 8건 중 무언가가 과제를 바꿈) | ■ 중단, R0 재설계 |
| I0b < I0a | **정상이자 목적** — sim이 실물만큼 어려워짐 | 이후 전 라운드 기준선 |
| I0c/I0d 중 하나가 I0a 우위 | 상수가 틀렸던 것 확정 | 그 값 채택 |
| I0c·I0d 둘 다 무개선 | 높이는 원인이 아님 | standing은 조건(외란)으로만 유도 |


---

## 8-6. R1 1차 — 중단 규칙이 과도해 3/4를 죽였다 (2026-08-03 21:0x)

| arm | 결과 |
|---|---|
| **I1b_force** | 200/200 완주. **model_175에서 낙상 0** — 위치 2.60 / p90 4.72 / hd 2.1°. **네 게이트 동시 통과 최초** |
| I1d_both | iter 125에서 STOP. 위치 **2.26**(최고) / p90 4.36 / 낙상 8 / strict 94.7% |
| I1a_base (대조) | iter ~109에서 STOP |
| I1c_cadence | iter ~95에서 STOP |

**셋을 죽인 것은 내 중단 규칙이다.** `STOP: falls 8 > 3.0x reference 2`.

두 가지가 겹쳤다:

1. **기준이 틀렸다.** `tonight.sh` 기본값 `REF_FALLS=2`(E0)가 쓰였다. 발 교정 후의 정직한
   기준은 **I0c의 4**다. → 기본값을 `0.0271 / 4`로 교정.
2. **과도구간을 죽였다.** warm start 정책이 새 조건(외란·케이던스·새 발)에 재적응하는
   동안 일시적으로 나빠진다. **`I1b`가 그 증거다: 낙상 3(iter 125) → 2(150) → 0(175).**
   `I1a`/`I1c`/`I1d`는 전부 **iter 50의 낙상 8~10 한 번**으로 잘렸다.
   I1b도 초기에 걸렸으면 낙상 0에 도달하지 못했다.

**규칙 수정 3건:**

| 항목 | 이전 | 이후 | 이유 |
|---|---|---|---|
| 판정 대상 | 낙상 **개수** | 낙상 **비율**(`fall_rate_per_attempt`) | 스크리닝이 arm마다 731~743구간을 채점한다. 개수 비교는 분모가 다른 것을 비교한다 |
| 발동 시점 | 첫 checkpoint부터 | **iter ≥ 100** | 재적응 딥을 보호 |
| 발동 조건 | 1회 위반 | **2회 연속** | 20 s 표본의 단발 요동으로 죽이지 않는다 |

검증: 이 규칙에서 `I1a@50`(낙상 10/730)은 **통과**하고, 진짜 붕괴(위치 12 cm)는 **잡는다**.

> **교훈**: 조기중단은 GPU를 아끼지만, **기준이 틀리면 아끼는 게 아니라 답을 버린다.**
> H는 중단이 없어서 4 GPU-일을 태웠고, R1은 중단이 과해서 대조군을 잃었다.
> 규칙은 "학습 전에 고정"하는 것만으로는 부족하고 **무엇에 대한 기준인지**가 맞아야 한다.

---

## 8-7. R1 판정 — 그리고 낙상 지표가 재현되지 않는다는 발견 (2026-08-03 22:3x)

### 2×2 완성 (20 s 스크리닝, checkpoint 125~200 평균)

| arm | 외란 | 케이던스 | pos med | p90 | 낙상 | strict |
|---|---|---|---:|---:|---:|---:|
| I1a_base | ✗ | ✗ | 3.01 | 5.54 | **9.75** | 84.7 |
| **I1b_force** | ✓ | ✗ | 2.64 | 4.82 | **1.50** | 91.2 |
| I1c_cadence | ✗ | ✓ | 2.47 | 4.55 | 6.25 | 92.4 |
| I1d_both | ✓ | ✓ | **2.36** | **4.47** | 5.50 | 92.5 |

- **외란 → 낙상**: 9.75 → 1.50 (−85%). **자세 보상 없이 낙상만 잡았다.**
- **케이던스 → 정확도**: 3.01 → 2.47 cm (−18%), p90 5.54 → 4.55.
- **상호작용이 나쁘다**: 케이던스를 켜면 외란의 낙상 이득이 −85% → −12%로 무너진다.

### I1b@175 전체 프로토콜 (120 s, 4632 구간)

| 게이트 | 기준 | I1b@175 | E0@6200 |
|---|---|---|---|
| 위치 median | ≤5 cm | **2.6** ✅ | 2.72 ✅ |
| 위치 p90 | ≤10 cm | **4.7** ✅ | 5.01 ✅ |
| heading median | ≤10° | **2.0** ✅ | 2.52 ✅ |
| 낙상 | 0 | **4** ❌ | 2 ❌ |
| strict 성공률 | — | **92.4%** | 89.29% |

정확도 3축 전부 E0를 넘었다. 낙상만 2 → 4로 늘었는데 — **그 비교가 성립하지 않는다.**

### ⛔ 낙상 수는 단일 run으로 재현되지 않는다

`I1c_cadence/model_95`를 **동일 프로토콜로 두 번** 평가한 결과가 남아 있다:

| run | seed | envs | duration | env SHA | 위치 median | **낙상** |
|---|---|---|---|---|---|---|
| `select_.../` | 0 | 256 | 120 s | `72a87cbc` | 2.64 cm | **3** |
| `select_.../winner_video/` | 0 | 256 | 120 s | `72a87cbc` | 2.66 cm | **18** |

**같은 checkpoint·같은 seed·같은 config.** 차이는 영상 녹화 여부뿐이다.
위치는 0.8% 안에서 일치하는데 **낙상은 6배**다. 공통 Poisson율로는 두 값이 함께
나올 확률이 사실상 0이므로 표본 노이즈가 아니라 **시뮬레이션 비결정성**이다
(GPU 파이프라인 + 렌더링 부하가 부동소수 경로를 바꾼다).

**여파:**
- E0의 낙상 2와 I1b의 4는 **구분 불가**다
- 위 2×2의 낙상 열(9.75 / 1.50 / 6.25 / 5.50)은 **제시한 것보다 훨씬 약하다**.
  다만 checkpoint 4개 평균이라 단일 run보다는 낫고, 9.75 vs 1.50의 6.5배 차이는
  관측된 재현 변동(6배)과 같은 크기라 **주효과의 방향만 신뢰하고 크기는 신뢰하지 않는다**
- 낙상 0 게이트는 **단일 run으로 판정할 수 없다**

**조치**: `tools/accept_eval.py` 신설 — 같은 checkpoint를 N seed로 돌리고 각 지표를
**중앙값과 범위**로 보고한다. 상대폭이 50%를 넘으면 "단일 run으로 판정 불가"를 찍는다.
위치·heading은 지루하게 안정적으로 나올 것이고, **그 대비가 이 도구의 요점이다.**

### 이 라운드에서 답하지 못한 것

- **외란 강건성은 여전히 미측정.** I1b 리포트가 명시한다: `외력 event 0회 —
  이 report는 충돌 강건성 근거로 사용할 수 없다`. 외란으로 **학습**한 정책을
  외란 **없이** 채점했다. E2·G2 때와 같은 공백이다
- **속도도 미측정.** 이 과제는 필요속도 p90이 **0.31 m/s**이고 시간 내 도달 불가가
  **0.0%**다. 속도를 요구하지 않는 과제에서 속도를 잴 수 없다(§3-2와 같은 구조)

---

## 8-8. R1 최종 — 2×2 완성 (2026-08-03 23:xx)

전 arm 200 iter 완주, checkpoint 7개씩. **워밍업 이후(it≥100) 5개 평균:**

| arm | 외란 | 케이던스 | pos cm | p90 cm | **낙상** | strict% |
|---|---|---|---:|---:|---:|---:|
| I1a_base | ✗ | ✗ | 3.01 | 5.54 | **9.60** | 84.6 |
| **I1b_force** | ✓ | ✗ | 2.71 | 4.97 | **2.20** | 89.8 |
| I1c_cadence | ✗ | ✓ | 2.52 | 4.66 | 7.20 | 91.7 |
| I1d_both | ✓ | ✓ | **2.44** | **4.51** | 5.80 | **92.5** |

### 주효과와 상호작용

**낙상 (arm의 유일한 미통과 게이트):**

| | 케이던스 OFF | 케이던스 ON |
|---|---|---|
| **외란 효과** | 9.60 → 2.20 (**−7.40**) | 7.20 → 5.80 (−1.40) |
| **케이던스 효과** | 9.60 → 7.20 (−2.40) | 2.20 → **5.80 (+3.60 악화)** |

**외란 단독이 낙상을 77% 줄인다.** 그런데 **케이던스를 얹으면 그 이득의 81%가 사라진다.**
케이던스 자체는 외란 없을 때 낙상을 약간 줄이지만(−2.40), 외란과 함께면 **거꾸로 늘린다**.
같은 자원(gait 타이밍)을 두 레버가 반대 방향으로 쓴다.

**위치**: 케이던스가 −16%(외란 OFF) / −10%(외란 ON). 두 레버 모두 정확도는 올리고
부분가산적이다.

### 낙상 차이가 노이즈가 아닌 근거

단일 run의 낙상은 재현되지 않는다(§8-7). 그래서 **5 checkpoint를 누적**한다:

| arm | 누적 낙상 / 약 3650 시도 | Poisson 오차 |
|---|---|---|
| I1a_base | **48** | ±6.9 |
| I1c_cadence | 36 | ±6.0 |
| I1d_both | 29 | ±5.4 |
| **I1b_force** | **11** | ±3.3 |

**48 ± 6.9 와 11 ± 3.3은 겹치지 않는다.** 단일 관측이 아니라 누적이므로 이 차이는 실재한다.

**추세는 더 강한 증거다:**

| arm | it 100 → 200 |
|---|---|
| I1a_base | 9 → 8 → 14 → 7 → 10 (평평·noisy) |
| **I1b_force** | **5 → 3 → 2 → 0 → 1 (단조 감소)** |
| I1c_cadence | 11 → 8 → 8 → 5 → 4 (감소) |
| I1d_both | 7 → 8 → 3 → 6 → 5 (평평·noisy) |

**I1b만 0으로 단조 수렴한다.** 단일 지점의 0이 아니라 궤적이다.

### 판정

- **외란은 채택.** 자세 보상 없이 낙상을 77% 줄였고 누적·추세 양쪽에서 확인된다.
  **사용자의 "결과가 아니라 조건을 만들라" 원칙이 데이터로 검증된 첫 사례다.**
- **케이던스는 보류.** 정확도는 확실히 올리지만(−16%) **외란과 함께 쓰면 낙상을 되돌린다.**
  낙상이 유일한 미통과 게이트이므로 정확도와 바꿀 수 없다.
- **I1d(둘 다)는 채택하지 않는다.** 위치·strict가 최고지만 낙상이 I1b의 2.6배다.

### 아직 남은 것

이 전부가 **외란 OFF 평가**다. "외란으로 학습했더니 clean에서 안 넘어진다"까지가
증거이고, **"외력을 맞으면 버티는가"는 여전히 미측정**이다. `force_ab.py`가 진행 중이며
그것이 R1의 원래 질문에 답한다.

---

## 8-9. 외력 ON 시험 — 확정 (2026-08-04)

공통 held-out 시험지: 간격 4–8 s, 충돌 50–120 N, 지지 4–10 N.
**세 arm 모두 force event 약 5,100회** — 외력이 실제로 전달됐고 감사가 그것을 확인한다.
seed 0/1/2, 각 256 env × 120 s.

| arm | 위치 median cm | **낙상** | 낙상/1000 | force events |
|---|---|---:|---:|---:|
| I1a_base (대조) | 3.03 [2.97, 3.08] | **17 [14, 20]** | 3.67 | ~5,100 |
| **I1b_force** | **2.57 [2.51, 2.60]** | **7 [5, 10]** | **1.51** | ~5,100 |

**범위가 겹치지 않는다.** 낙상이 재현되지 않는다는 것을 알고(§8-7) seed 3개로 설계한 것이
여기서 값을 했다. 누적으로 보면 더 분명하다:

| | 누적 낙상 / 약 13,900 시도 | Poisson 구간 |
|---|---|---|
| I1a_base | **51** | 43.9 ~ 58.1 |
| **I1b_force** | **22** | 17.3 ~ 26.7 |

**2.32배, 낙상 57% 감소.** 구간이 겹치지 않는다.

### 판정 — 세 번 묻고 세 번 못 답한 질문에 답이 나왔다

> **외란으로 학습하면 실제 외력에 강해지는가, 아니면 그냥 느려질 뿐인가?**

**강해진다. 그리고 아무것도 지불하지 않았다.**

| | I1a_base | I1b_force |
|---|---|---|
| 외력 하 낙상 | 17 | **7** (−57%) |
| 외력 하 위치 | 3.03 cm | **2.57 cm** (−15%) |

**위치가 오히려 더 좋다.** E2·G2에서 두 번 재현됐던 L4("강건성 레버는 항상 덜 움직이기로
도망친다")가 **이번에는 나타나지 않았다.** 차이는 §8-8의 저용량 설계다 — 접촉 렌치만
켜고 goal jitter·bias·flicker·staleness는 전부 껐다. **L4는 강건성의 법칙이 아니라
7-레버 묶음의 증상이었다.**

> **사용자 원칙("결과를 보상하지 말고 그 결과를 필연으로 만드는 조건을 만들라")이
> 대조군을 두고, 홀드아웃 시험지에서, 재현 가능한 형태로 검증됐다.**
> 자세 보상은 한 줄도 넣지 않았다.

### ⚠️ 하지 말아야 할 비교

clean(20 s 스크리닝)과 force(120 s)는 **지속시간이 달라 낙상률을 직접 비교할 수 없다**:

| | I1a | I1b |
|---|---|---|
| clean 20 s | 13.0 /1000 | 3.0 /1000 |
| force 120 s | 3.66 /1000 | 1.51 /1000 |

force 쪽이 오히려 낮게 나온다. 외력이 쉬워서가 아니라 **20초 창은 리셋 직후 과도구간
비중이 커서 낙상률을 과대평가**하기 때문이다. 초안에서 "I1b가 맞으면서 낸 낙상이 I1a가
clean에서 낸 것보다 적다"고 쓸 뻔했는데 **그 비교는 성립하지 않는다.**

같은 120 s 안에서 비교 가능한 유일한 쌍:

| | 낙상/1000 |
|---|---|
| I1b clean 120 s | 0.86 |
| I1b force 120 s | 1.51 (**+75%**) |

**I1a의 clean 120 s가 없다.** `--skip-clean`으로 돌려서다. 이것 하나(3 seed, ~42분)면
(arm × force) 2×2가 닫히고 **"외란 학습의 clean 비용"**까지 정량화된다.

---

## 8-10. R 트리 개정 (2026-08-03)

§7의 원안(R0 재현 → R1 높이 → R2 단일지지 → R3 캘리브 → R4 속도 → R5 goal)에서
실제 진행이 갈라졌다. 사후 합리화가 아니라 **왜 갈라졌는지**를 기록한다.

| 라운드 | 원안 | 실제 |
|---|---|---|
| R0 | 재현 확인 | ✅ + 발 교정 + 높이 0.55까지 흡수 (R1 내용 선취) |
| R1 | standing(높이) | 2×2로 대체 → **외란 채택**(낙상 −77%), 케이던스 보류 |

**원래 R2(단일지지 전용 학습)는 폐기한다.** 그 목적이 "낙상을 줄인다"였는데
**외란 하나로 77%가 줄었다.** 전용 arm의 근거가 사라졌다.

### 트리에 없던 자리에 들어가는 발견 둘

| 발견 | 성격 |
|---|---|
| `base_com ±0.1 m` 과설정 | **결함 교정** — 가설이 아니다 |
| `feet_swing`이 1 cm까지만 보상 | **조건화로 해결** — 지형 요철 |

`base_com` 정량 (URDF 실측):

| | 값 |
|---|---|
| Trunk 질량 | 6.392 kg (전체 18.714의 34%) |
| Trunk 충돌박스 반폭 | x ±0.060 m, y ±0.090 m |
| **현재 랜덤화** | **x·y·z 각각 ±0.100 m** |
| 발 x 범위 | −0.066 ~ +0.094 m |

→ **CoM이 몸통 밖으로 나가고**(x 1.7배), **지지면 밖으로도 나간다**(발 반길이의 1.1배).
정적으로 균형이 불가능한 로봇을 만들어 놓고 안 넘어지길 요구해 왔다.
실제 CoM 불확실성은 1–3 cm다. **`feet_edge_pos`와 같은 부류의 결함**이다.

> **이건 강건성 거래가 아니라 허구의 제거다.** L4("넓은 DR → 보수적")를 근거로
> "좁히면 약해진다"고 반박할 수 없다 — 없는 조건에 대비하느라 보수적이었던 것이라면
> 좁히는 것이 곧 성능 회복이다. 다만 **실측이 없으므로 레버로 검증한다.**

### 개정 트리

```
R1 완료 ─ 외란 채택
   │
   ├─ force_ab (진행 중) ── 외란이 실제 외력에 강한가          [R1 검증]
   ▼
R2  I2a_dr       DR을 현실에 맞춤 (CoM ±0.025 / 질량 ±8% / PD ±15%)   [결함 교정]
    I2b_terrain  지형 요철(trimesh) → 발 클리어런스                    [조건화]
   ▼
R3  캘리브레이션 DR (IMU 회전 + encoder bias + target offset)
    지연 DR 20–60 ms + 지터                                    [잔진동 ②]
   ▼
R4  속도 — 케이던스 재시도 (R2에서 DR을 고친 뒤)
   ▼
R5  goal 불확실성 (reprojection 30–50 cm)
```

**폐기**: 원래 R2(단일지지 전용) — 외란이 이미 −77%를 냈다
**보류**: 케이던스 — 외란과 충돌(§8-8). R2 이후 R4에서 재시도

### 왜 DR 교정이 속도보다 앞서나

케이던스가 외란과 충돌한 이유 중 하나가 **"정책이 ±10 cm CoM 변동까지 감당하느라
보수적"**일 수 있다. DR을 현실화하면 그 충돌 자체가 사라질 가능성이 있고, 그러면
케이던스를 다시 켤 수 있다. **R2는 R4의 전제조건이다** — 지금 케이던스를 재시도하면
같은 결과가 나온다.

**R2는 force_ab를 기다리지 않아도 된다.** 두 arm 모두 외란 레버를 R1 승자 그대로
켜둔 채 config만 바꾸므로 독립이다.

---

## 8-11. Bird's-eye — 목표 대비 현재 위치와 다음 수 (2026-08-04)

### 어디까지 왔나

| 항목 | 기준 | 현재 (I1b_force@175, 120 s) | |
|---|---|---|---|
| P1 위치 median | ≤5 cm | **2.6 cm** | ✅ |
| P1 위치 p90 | ≤10 cm | **4.7 cm** | ✅ |
| P1 heading | ≤10° | **2.0°** | ✅ |
| **P1 낙상** | **0** | **4 (clean) / 7 (외력)** | ❌ |
| P2 몸통속도 | 1.3~1.5 m/s | 구간최고 p90 1.29, **>1 m/s 체류 3.1%** | ❌ |
| 7-A 정확+빠름 동시 | — | 정확 ✅ / 빠름 **미측정** | ❌ |
| 7-B 움직이는 목표 | — | 학습에서 제외 | ❌ |
| 7-C 불안정 시 안정화 우선 | — | 미구현 | ❌ |

**정확도 3축은 E0를 넘었고 외력 강건성도 확보했다.** 남은 것은 낙상 0과, **한 번도 측정된 적
없는 속도**다.

### ⛔ 지금 과제는 P2를 요구할 수 없다 — 기하학적으로

```
goal_dx [-2, 2],  goal_dy [-1.5, 1.5],  resampling_time_s [4, 8]
가장 먼 목표 = √(2² + 1.5²) = 2.50 m,  최단 구간 = 4 s
→ 이 과제가 요구할 수 있는 최대 속도 = 0.62 m/s
```

P2는 **1.3~1.5 m/s**다. 실측 필요속도는 median 0.11 / **p90 0.31 m/s**이고
**시간 내 도달 불가 0.0%**다. 정책이 빨리 갈 이유가 과제에 **존재한 적이 없다.**

`masterplan2`가 E 배치에서 지적한 결함(*"요구속도 최댓값 0.574 m/s"*)이 그대로 돌아왔다.
그때의 해법이 path였고, 우리는 그 path를 학습에서 뺐다(§P7). **속도 요구를 만들던
유일한 장치를 제거한 것이다.**

### P1과 P2는 충돌하지 않는다 — 초안의 오판을 정정한다

`goal_reached`는 `dist<0.1 AND |v|<0.1`에서 `+1/step`을 준다. **일찍 도착할수록 공 안에서
더 오래 받는다.** 즉 보상 구조는 이미 속도를 장려한다. 문제는 **거리가 짧아 gradient가
약한 것**이다:

| 거리 | 구간 | 0.5 m/s | 1.4 m/s | 속도의 한계이득 |
|---|---|---|---|---|
| 1.0 m | 6 s | 정지 4.0 s | 5.3 s | +65 step |
| 2.5 m | 6 s | 1.0 s | 4.2 s | +160 step |
| **4.0 m** | 6 s | **도달 실패** | 3.1 s | **0 → 155 step** |

거리가 멀어지면 속도의 보상이 급격히 커진다. **보상을 고칠 필요가 없다. 과제 분포만
고치면 된다** — 사용자 원칙 그대로다.

### 속도 요구를 만드는 세 가지 안 (수치 비교)

이동 구간의 요구속도 분포(30,000 표본 시뮬):

| 안 | median | **p90** | p99 | max |
|---|---:|---:|---:|---:|
| 현재 (dx±2, T 4–8 s) | 0.05 | 0.23 | 0.43 | **0.62** |
| 거리 2배 (dx±4, T 4–8 s) | 0.10 | 0.45 | 0.84 | 1.24 |
| 마감시간 = 거리/목표속도 + 1.5 s | 0.19 | 0.55 | 0.79 | 0.96 |
| **sprint 카테고리 15%** (거리 2.5–4 m, 목표 1.2–1.6 m/s) | 0.06 | **1.34** | 1.57 | — |
| sprint 20% | 0.07 | **1.40** | 1.58 | — |

**sprint 카테고리 15~20%만이 p90을 1.3~1.5에 올린다.** 그리고 나머지 80%는 현재 과제
그대로이므로 **P1의 분포가 거의 안 변한다** — path가 청구했던 세 대가(§L5)를 피한다.

> 이건 path의 재도입이 아니다. 캐럿·leash·dwell·grid 기계가 전혀 없고,
> **"먼 목표 + 짧은 마감"**이라는 과제 조건 하나뿐이다.

### 개정된 다음 수

```
지금 (병렬, 코드 변경 0)
 ├─ I1a clean 120s × 3 seed  ── (arm × 외력) 2×2를 닫는다        [~42분]
 └─ R2  I2a_dr / I2b_terrain ── DR 과설정 교정 + 지형 요철        [~25분]
        ▼
R3  캘리브레이션 DR(IMU 회전 + encoder/target offset) + 지연 20–60 ms   [잔진동 ②]
        ▼
R4  속도 — sprint 카테고리 15%  ◀── 재설계됨. 케이던스는 여기서 재시도
        ▼
R5  goal 불확실성 (reprojection 30–50 cm)
```

**R4가 바뀌었다.** 원안은 "케이던스 재시도"였는데, **케이던스는 속도를 낼 수단이지
속도를 요구하는 장치가 아니다.** 요구가 없으면 수단을 고쳐도 측정할 수 없다.
sprint 카테고리로 요구를 먼저 만들고, 그 위에서 케이던스를 켜야 §8-8의 충돌 여부를
비로소 판정할 수 있다.

### 낙상 0 — 남은 P1 게이트

| | clean 120 s 낙상 | 낙상/1000 |
|---|---|---|
| E0@6200 | 2 | 0.43 |
| I1b@175 | 4 | 0.86 |

**둘은 구분되지 않는다**(§8-7: 같은 checkpoint가 3과 18을 냈다). 즉 **clean 낙상은 E0
이후 개선된 적이 없다.** 개선된 것은 정확도와 **외력 하 강건성**이다.

낙상 0을 주장하려면 `accept_eval.py`로 **다중 seed에서 최악 seed도 0**이어야 한다.
지금 데이터로는 그 주장을 할 수 없고, 할 수 있는 주장은 *"외력 하에서 대조군의 41%"*다.

> **2026-08-04 폐기.** 이 절의 "낙상 0" 게이트는 §8-12에서 철회했다. 아래 §8-12가
> 최신이며, 이 절은 그 판단이 어디서 왔는지를 남기기 위해 지운다.

---

## 8-12. 낙상 0 게이트 폐기 + R2 판정 (2026-08-04 01:4x)

### 사용자 질문: "낙상 0에 집착하는 이유가 내 지시 때문인가?"

**아니다.** 사용자 요구는 축 C — *"불안정할 때 목표추종보다 안정화를 우선"* — 이고,
"낙상 0"이라는 숫자는 내가 `accept_eval.py:111`에 써넣은 것이다. 요구를 지표로 번역할
때 내가 **잘못 번역했다**: 평지·무외란 sim의 낙상 수는 축 C가 말하는 것을 재지 않는다.

### 왜 나쁜 게이트인가 — 세 가지, 전부 자체 측정으로 확인됨

**(1) 검정력이 없다.** 전체 프로토콜 4,632구간 기준 95% Poisson 구간:

| 관측 | 구간(회) | 비율 /1000 |
|---|---|---|
| 2 (E0@6200) | [0.24, 7.2] | [0.05, 1.56] |
| 4 (I1b@175) | [1.09, 10.2] | [0.24, 2.21] |

겹친다. 즉 **E0와 I1b는 clean 낙상으로 구분 불가**다 — 이미 §8-7에 쓴 것이다. 그렇다면
**같은 이유로 "0"도 판정 불가**다. 0회를 관측해도 상한은 3.69회, 곧 "≤0.80/1000"까지만
말할 수 있다. "≤0.10/1000"을 주장하려면 무낙상 36,900구간 = **전체 프로토콜 8회분**,
arm당 약 2 GPU-시간이 필요하고, 그렇게 얻는 것은 *평평한 sim 바닥*에 대한 숫자다.

**(2) 위 계산조차 낙관적이다.** §8-7에서 같은 checkpoint가 3회와 18회를 냈다. Poisson이면
[0.62, 8.8] vs [10.7, 28.5]로 **분리**되어야 하는데 같은 정책이다. 즉 낙상 수는
**Poisson보다 과분산(overdispersed)**이다. → **Poisson 구간을 신뢰구간으로 쓰면 안 된다.
seed를 여러 개 돌려 경험적 범위를 재는 것만이 유효하다.**

**(3) 실기와 연결이 약하다.** 무외란 평지에서 0.43/1000이냐 0.86/1000이냐는 경기장에서
K1이 넘어지느냐를 결정하지 않는다. 결정하는 것은 밀렸을 때·발이 걸렸을 때·지연이
튀었을 때 회복하느냐다.

### 대체 지표 — 외력 하 낙상 (이미 작동한 적이 있다)

§8-9의 held-out 외력 시험, 3 seed **경험적 범위**:

| arm | 낙상 (seed 범위) | 누적 |
|---|---|---|
| I1a_base | **17 [14, 20]** | 51 ± 7.1 |
| I1b_force | **7 [5, 10]** | 22 ± 4.7 |

**범위가 겹치지 않는다.** clean이 4,632구간을 쓰고도 못 가른 차이를, 같은 시간에 갈랐다.
사건 발생률이 10~20배라 검정력이 그만큼 높고, `force_ab.py`는 구조상 다중 seed를 돈다.

**개정된 지표 체계:**

| | 폐기 | 채택 |
|---|---|---|
| clean 평가 | 정확도 + 낙상 게이트 | **정확도 전용.** 낙상은 기록만, 순위 판단 금지 |
| 강건성 | clean 낙상 0 | **held-out 외력 낙상**, ≥3 seed 경험적 범위 |
| 게이트 | 낙상 0 | 정확도 ≤ 기준×1.1 **AND** 외력 낙상 범위가 I1b보다 위로 벗어나지 않을 것 |

새 지표가 사용자의 축 C에 더 가깝다. 폐기한 게이트는 축 C를 clean 낙상수로 오역한 것이었다.

### R2 데이터 (2026-08-04 01:03 시작, 200 iter, warm start model_6200)

`_I2_BASE = merge(_I1_BASE, _I1_FORCE)` — 두 arm 모두 R1 승자(발 + h055 + 외란) 위에 얹었다.

- **I2a_dr**: `base_com ±0.025 m`, `base_mass ×[0.92,1.08]`, `dof_stiffness/damping ×[0.85,1.15]`
  (상위팀 레시피 "DR 10%"와 같은 자릿수)
- **I2b_terrain**: `terrain.type: trimesh`

**20초 스크리닝, checkpoint 125/150/175/200 (~740구간 × 4 = ~2,960구간):**

| arm | pos med 평균 cm | strict % 평균 | 낙상 합 | 체크포인트별 낙상 |
|---|---:|---:|---:|---|
| **I2a_dr** | 2.72 | **91.5** | **4** | 1, 0, 2, 1 |
| I1b_force (R1 승자) | 2.64 | 91.2 | **6** | 3, 2, 0, 1 |
| I1d_both | **2.36** | 92.5 | 22 | 8, 3, 6, 5 |
| I1c_cadence | 2.47 | **92.4** | 25 | 8, 8, 5, 4 |
| **I2b_terrain** | 2.72 | **85.0** | 32 | 5, 9, 9, 9 |
| I1a_base (대조군) | 3.01 | 84.7 | 39 | 8, 14, 7, 10 |

### 판정

**I2a_dr — 채택.** 정확도 2.72 vs 2.64 (차이 없음), strict 91.5 vs 91.2 (동률),
낙상 4 vs 6 (동률). **sim2real 마진을 아무 대가 없이 얻었다.** DR은 공짜다.

**I2b_terrain — ⛔ 판정 철회 (2026-08-04 02:1x). 각자 자기 시험지로 채점했다.**

`watch_eval`과 `select_best_checkpoint`는 **각 arm의 자기 config**로 평가한다
(`watch_eval.py:164` = `<run>/config.yaml`). 그래서 **I2b_terrain은 trimesh 지형 위에서,
I1b·I2a는 평지 위에서** 채점됐다. strict 91.2 → 85.0은 *정책이 나빠진 것*일 수도,
*시험이 어려워진 것*일 수도 있고 **이 데이터로는 구분되지 않는다.**

`force_ab.py`를 만든 이유가 정확히 이 오류였는데(각 정책을 자기 외란 설정으로 채점),
지형에서 같은 실수를 반복했다. 외란만 공통 프로파일로 묶고 **바닥은 안 묶었다.**

오히려 주목할 것: **위치 median이 2.72로 동일하다.** ±10 cm 자갈밭에서 평지와 같은
정확도가 나왔다면 그건 실패가 아니라 놀라운 결과다. "지형은 거래다"는 근거 없는 서술이었다.

**수정:** `eval_goal_pose.py --terrain plane`을 추가했다. `--force_profile heldout`과 같은
원리 — 바닥은 시험의 일부지 정책의 일부가 아니다. 리포트에 `eval_terrain`으로 찍힌다.
I2b_terrain은 평지 재평가 전까지 **미판정**이다.

**부수 관찰: 외란 학습 = 낮은 낙상이 clean에서도 보인다.** 외란을 넣은 두 arm(I1b, I2a)이
4~6, 안 넣은 넷이 22~39다. 단 이 비교는 **checkpoint 4개를 묶은 것**이라 정책 4개에
대한 서술이지 checkpoint 하나에 대한 주장이 아니며, (2)의 과분산 때문에 확정하려면
`force_ab.py`로 seed를 돌려야 한다.

### `round_summary.py` 결함 2건째 — 분모 혼합

R2 요약표가 I2a/I2b는 **20초 스크리닝(~740구간)**, I1b는 **120초 전체(4,632구간)**
리포트를 한 표에 놓았다(선택이 아직 안 끝나 전체 리포트가 없었다). "낙상 1 vs 4"를 보고
I2a가 낫다고 판단했는데 **비율로는 1.35 vs 0.86/1000으로 반대**였다.

`monitor`의 stress 리포트 혼입, 직전의 force 리포트 혼입에 이어 **"리포트 종류/노출량을
구분하지 않고 최근 것을 집는다"가 세 번째**다. 이번엔 `구간` 열과 `/1000` 열을 추가하고,
행 간 노출량이 1.5배 이상 차이 나면 경고를 찍게 했다. `--ref-falls`는 인자만 남기고
무시한다 — 낙상 판정은 `force_ab.py`로 옮겼다.

### 8-12b. I3_rough — 지형 재시도 (2026-08-04 구현, 미실행)

I2b가 지형을 켜면서 **base config의 숫자를 읽지 않고 그대로 상속**했다:

```yaml
terrain_proportions: [0.0, 0.0, 0.5, 0.5]   # [평지, 경사, random, discrete]
random_height: 0.1        # ±10 cm
horizontal_scale: 0.1     # 10 cm 격자  ->  국소 경사 ~45°
discrete_height: 0.02
```

다리 0.52 m, 발 두께 2.4 cm인 로봇에게 **자갈밭**이다. RoboCup 인조잔디는 ±1~2 cm이고,
discrete/경사는 *등반*이라는 다른 기술이며 이 과제는 그걸 요구한 적 없다.

`I3_rough` = `_I2_BASE + _I2_DR`(R2에서 채택) + 거칠기만:

```yaml
terrain.type: trimesh
terrain.terrain_proportions: [0.0, 0.0, 1.0, 0.0]   # random만
terrain.random_height: 0.02                          # ±2 cm
terrain.discrete_height: 0.0
terrain.slope: 0.0
```

I2a_dr과의 diff는 **terrain 5줄뿐**임을 생성해서 확인했다(obs 54 / act 12 유지 →
warm start 호환).

**왜 하드코딩이 아닌가:** 2 cm는 보상 목표가 아니라 바닥의 성질이다. `feet_contact`는
"모서리 하나라도 지면 1 cm 이내"이므로 평지에서는 **1.1 cm만 들면 `feet_swing` 만점**이고
아무도 더 요구하지 않는다. ±2 cm 바닥은 1.1 cm를 걸리게 만들 뿐, 높이를 지정하지 않는다.
정책이 필요한 높이를 정한다.

**왜 임계값 1 cm → 3 cm 상향은 안 되는가:** `feet_contact`가 두 보상에 동시에 쓰인다 —
`feet_swing`(+3.0, 스윙 중 비접촉 보상)과 `feet_slip`(-0.1, **접촉 중** 속도² 벌점).
3 cm로 올리면 스윙 발(대지속도 ~2.5 m/s, v²≈6 → 0.6/step)이 벌점을 맞아 **천천히 들라는
압력**이 생기고 `feet_slip`의 원래 목적이 희석된다. 하나 고치려고 다른 하나를 망가뜨린다.
그리고 3 cm는 사용자가 금지한 하드코딩 그 자체다.

**기각 조건(실행 전 확정):** **같은 바닥에서**(양쪽 `--terrain plane`) I2a_dr 대비
strict 하락 **2%p 초과면 기각.** I2b가 (다른 바닥에서) 6.2%p였으니, 파라미터를 5배 줄여
1/3 이하로 못 내리면 방향 자체가 틀린 것이다.

**잔진동은 이 arm이 풀지 않는다.** 원인은 sim에 지연·관측잡음이 없어 정책이 마음껏 떨 수
있는 것이고, 답은 지연 DR 20–60 ms + 지터다. `Goal_Pose_V7.yaml`에 지연 관련 키가
**하나도 없으므로** 그건 env 코드 작업이다 — R3에서 별도로 잡는다.

### R2 다음 수 — accept_eval이 아니라 force_ab

폐기한 게이트대로면 `accept_eval.py --seeds 0 1 2 3 4`(70분)로 clean 낙상을 재야 했다.
새 지표대로면 **I2a_dr vs I1b_force를 held-out 외력에서 비교**한다. 같은 시간에
판정 가능한 답이 나오는 쪽이다.

---

## 9. 실험 일지

| 날짜 | 라운드 | arm | iter | 결과 | 판정 | 다음 |
|---|---|---|---:|---|---|---|
| 2026-08-03 17:58 | R0 v1 | I0a_repro / I0b_foot | 200 | **중단** — 설계 결함 | 판정이 사후, 감시 없음, 카드당 1개 | v2로 재설계 |
| 2026-08-04 | R1 검증 | force_ab (외력 ON, 3 seed) | — | **I1a 17 [14,20] vs I1b 7 [5,10]**, 위치도 I1b 우세 | **외란 강건성 확정, 대가 없음** | I1a clean 120s로 2×2 닫기 |
| 2026-08-03 23:0x | R1 판정 | 2×2 완성 | 200 | **외란 채택**(낙상 −77%), 케이던스 보류(외란과 충돌) | 누적·추세로 확인 | force A/B로 강건성 직접 측정 |
| 2026-08-03 21:38 | R1 2차 | I1a/I1c/I1d 재실행 | 200 | 2×2 완성. I1b 전체 프로토콜 3/4 게이트 | 낙상 지표 재현 불가 발견 | 다중 seed 수용평가 |
| 2026-08-03 20:39 | R1 1차 | I1a/I1b/I1c/I1d | 200 | I1b만 완주(낙상 0 달성), 3개는 중단규칙에 잘림 | 규칙 과도 | 규칙 수정 후 3개 재실행 |
| 2026-08-03 18:50 | R0 v2 | I0a / I0b / I0c_h055 / I0d_h058 | 200 | **완료·전 arm 채점됨** | 하네스 건전, 높이 답 나옴 | R1 착수 |

### R0 v2 학습 완료 (2026-08-03 19:03)

4 arm 전부 200/200 완주. 실측 **3.59~3.62 s/iter**(카드당 2 프로세스) → **12분**.
사전 추정 2.0 s/iter는 틀렸다 — 2 프로세스 시분할을 과소평가했다. 다음 라운드 추정에 반영한다.

~~**문제: 네 arm 모두 `평가 0건`.**~~ → **오표시였다.** `watch_eval`은 정상 작동했고,
대시보드의 "평가 N건" 카운터가 `report.json` 스캔만 세느라 `watch_eval.jsonl`을 무시했다.
`round_summary.py`가 실제 값을 꺼내 확인했다(아래). 카운터는 수정했다.

(아래 진단 후보 3개는 결과적으로 전부 오진이었다. 기록만 남긴다.)

**당시 세웠던 가설:** `watch_eval`은 붙었다고 로그에 찍혔는데(4/4) 결과가 없다.
checkpoint가 arm당 2개뿐(`model_0`, `model_200`)이고 `--skip-first 1`이라 **채점 대상이
`model_200` 하나**인데, 그것마저 리포트가 없다. 가능성 셋:

1. 학습이 끝나며 GPU를 놓기 전에 평가가 시작돼 OOM/디바이스 충돌
2. `eval_goal_pose.py` 인자 불일치로 즉시 종료(→ `watch_eval/*/eval_failed.log`에 남음)
3. `watch_eval`이 run 디렉터리 해석에 실패(→ `logs/watch_eval/<arm>.log`에 남음)

**진단은 추측이 아니라 파일로 한다** → `tools/round_summary.py` 신설. 라운드 하나를
한 화면으로 요약하고, 결과가 없는 arm은 **왜 없는지를 디스크에서 찾아 같이 출력**한다.

**v1(18:03) 결과는 유효하고, R0의 첫 질문에는 이미 답이 나왔다:**

| | pos med | p90 | falls | strict |
|---|---|---|---|---|
| E0@6200 기준 | 2.72 | 5.01 | 2 | 89.3% |
| **I0a_repro** | **2.71** | 5.03 | **1** | **89.5%** |
| **I0b_foot** | 2.84 | 4.98 | 4 | 89.9% |

→ **하네스는 E0를 재현한다**(§8-3 판정표의 "R1 진행" 조건 충족).
→ **실제 발 크기의 대가는 +0.13 cm / +3 낙상**으로 예상보다 훨씬 싸다. `I0b_foot`을
이후 전 라운드의 기준선으로 쓰는 데 무리가 없다.
### R0 v2 판정 (2026-08-03 19:08, `round_summary.py`)

`model_200`, 20 s × 256 env 스크리닝 평가:

| arm | pos med | p90 | hd ° | **falls** | strict % |
|---|---:|---:|---:|---:|---:|
| E0@6200 기준 | 2.72 | 5.01 | 2.5 | 2 | 89.3 |
| I0a_repro (대조) | 2.65 | 4.93 | 1.8 | **15** ⛔STOP | 90.5 |
| I0b_foot | 2.89 | 4.93 | 2.3 | **4** | 90.6 |
| **I0c_h055** | **2.71** | **4.81** | 2.2 | **4** | **91.7** |
| I0d_h058 | **2.63** | 4.98 | 1.9 | 7 | 90.2 |

**① 하네스는 건전하다.** 위치가 넷 다 2.63~2.89 cm로 E0(2.72)와 같은 자리다.
아카이브·계측 수정 8건이 과제를 바꾸지 않았다. → §8-3 판정표의 "R1 진행" 조건 충족.

**② 발 크기 — 서술을 정정한다 (2026-08-03 팩트체크).**

앞서 "`feet_roll`/`feet_pitch`/`feet_slip` 보상이 오판한다"고 썼는데 **틀렸다.**
`feet_edge_pos`는 그 보상들이 아니라 **접촉 판정 `self.feet_contact` 자체**를 만든다
(`goal_pose.py:685-698`: 네 모서리 중 하나라도 지면 1 cm 이내면 접촉).
그리고 **"낙상이 줄었으니 옳다"는 정당화도 철회한다** — `feet_contact`은 `feet_swing`
보상에 들어가는 *신호*이고, 신호가 늦게 울리면 같은 걸음에도 보상을 더 받는다.
낙상 감소는 교정의 정당성 근거가 될 수 없다.

**실제 근거는 기하학이고, 세 갈래로 독립 확인된다:**

| | x 범위 | 폭 | 길이 | 바닥 z |
|---|---|---|---|---|
| config `feet_edge_pos` | [−0.1015, +0.1215] | ±0.050 | **0.223** | −0.0300 |
| collision box (PhysX가 미는 것) | [−0.066, +0.094] | ±0.035 | 0.160 | −0.0240 |
| visual mesh (K1 실제 발) | [−0.0685, +0.0965] | ±0.0392 | **0.165** | −0.0269 |

1. `envs/T1/{Base_Walk,Parameter_Walk,Kicking}.yaml`에 **바이트 단위로 동일한 값**이 있다
   → T1에서 포크하며 안 고친 것이 확인된다
2. K1 실제 발 메시 대비 **길이 +35.2%, 폭 +27.6%**
3. 바닥이 실제 밑창보다 **3.1 mm 아래**

**진짜 결함은 신호와 물리의 불일치다.** sim에 발 정의가 셋 있고, PhysX가 미는 것은
collision box인데 `feet_contact` 신호는 그보다 **39% 긴 상자**로 판정한다.
즉 물리적으로 아직 공중인 발을 "접지"라고 보고한다. 어느 쪽이 진짜냐 이전에
**내부적으로 모순**이다.

**채택 근거 (수정)**: I0b/I1에 넣은 값은 collision box 기준이다. box 자체도 실제
메시보다 폭 −10.7%, 바닥 +2.9 mm 작으므로 **실기 발보다 좁게 학습하는 셈**이다.
이는 "정확해서"가 아니라 **내부 일관적이고 전이에 보수적이라서** 채택한다 —
실기 발이 sim보다 넓으면 실제 로봇이 sim보다 안정적이고, 그 방향이 안전하다.

**미해결로 남긴 것**: collision box를 메시에 맞춰 넓히면 셋이 모두 정합하지만
물리가 바뀌어 warm start에 영향을 준다. **R2 후보로 등재**한다.

**③ 높이는 0.55가 낫다.** `I0c_h055`가 p90 4.81(최저)·strict 91.7%(최고)·낙상 4로
종합 1위다. `I0d_h058`은 median이 가장 낮지만(2.63) 낙상 7로 밀린다.
0.52(대조 I0a)는 낙상 15로 최악 — **사용자의 실기 관찰("0.58로 올리니 잘 섰다")이
방향은 맞았고, 최적값은 그 사이인 0.55다.**

**④ 대조군이 STOP에 걸린 것은 규칙이 작동한다는 증거다.**
`falls 15 > 3.0x reference 2` — 학습 전에 고정한 규칙이 자동으로 집행됐다.
H가 4 GPU-일을 태우고 알아낸 것을 이번엔 12분 만에 잡았다.

**⑤ 유보 조건 — 완화됨.** 서버 리포트 전문을 받아 확인하니 20 s 평가가 arm당
**731~743 구간**을 채점했다. 낙상률(시도당)은 I0a **0.0200** vs I0b/I0c **0.0054**,
I0d 0.0093. 734 시도 기준 15 vs 4는 Poisson 신뢰구간 밖이라 **노이즈가 아니다.**
다만 낙상 게이트는 0이므로 4도 미통과다. 전체 프로토콜 확인은 R1의 `train_and_eval`이
학습 후 자동으로 수행한다.

**⑥ 조기중단이 실제로는 작동할 기회가 없었다.** `save_interval: 100` + 200 iter =
checkpoint 2개(`model_0`, `model_200`), `--skip-first 1`이라 **채점 대상이 1개**였다.
STOP이 마지막에 걸린 것은 규칙이 맞았다는 뜻이지만 시간을 아끼지는 못했다.
→ R1은 `runner.save_interval: 25`로 **8개 지점**을 채점한다.

---

## 10. R1 — 2×2 요인설계 (2026-08-03 확정)

R0 승자 둘(**발 교정** + **높이 0.55**)은 **한 번도 같이 돌아본 적이 없다.**
`I1a_base`가 그 조합이자 R1의 대조군이다.

| arm | I1a 대비 | 카드 | 묻는 것 |
|---|---|---|---|
| **I1a_base** | (대조) 발 교정 + h055 | 0 | 두 승자를 합치면 유지되나 |
| **I1b_force** | + 외란(저용량, 접촉 렌치만) | 0 | **강건성이 직립을 대신 만드나** (하드코딩 금지 원칙) |
| **I1c_cadence** | + 케이던스–속도 커플링 | 1 | 강제된 상체 기울기가 사라지나 |
| **I1d_both** | + 외란 + 케이던스 | 1 | **상호작용** (유일하게 단일레버 아님, 명시) |

**왜 요인설계인가**: standing 문제와 속도 문제가 **상호작용할 것으로 의심**된다 —
케이던스 수정이 강제 기울기를 없애고, 외란이 직립을 *필요*하게 만든다.
따로 검증하면 합쳤을 때를 알 수 없다(사용자 지적: "따로 학습하면 의미 없다").
`I1d`는 그 교란을 **의도적으로** 넣은 셀이고 문서에 그렇게 적는다.

**외란은 저용량만.** E2·G2가 robust 묶음을 한꺼번에 켜서 두 번 다 저속 붕괴했다(L4).
`I1b`는 **접촉 렌치 2종만** 켠다 — goal jitter·bias·flicker·staleness는 전부 OFF.
외란 substep 재제출 수정(`goal_pose.py:587`)이 live에 살아 있어 명목 충격량이
온전히 전달된다.

**케이던스 커플링** (`_couple_cadence_to_speed`, `commands.cadence_coupling`):
```
f = clamp( 요구속도 / (2 × 0.28 m), 1.6, 3.2 )   요구속도 = goal_dist / 남은 시간
```
보상이 아니라 **명령 분포 수정**이다. 자세를 지킨 대가를 지불하지 않고,
과제가 요구하는 보폭을 다리가 낼 수 있는 값으로 만든다. 관측 54차원 불변
(gait_frequency는 이미 command 슬롯 3). `stand` 목표의 얼린 클럭은 건드리지 않는다 —
덮으면 제자리 서기가 다시 비최적이 된다.

### R1 판정표 — 실행 전 확정

| 관측 | 결론 |
|---|---|
| I1a가 I0b/I0c 수준(pos ≤2.9 cm, falls ≤4) 유지 | 두 승자가 합쳐진다 → 기준선 확정 |
| I1a가 열화 | 두 수정이 **간섭**한다 → 하나만 채택 |
| I1b 낙상 < I1a | **강건성이 직립을 만든다** — 원칙 검증 성공 |
| I1b 속도 붕괴(>1 m/s 체류가 I1a의 50% 미만) | L4 재발 → 용량을 더 낮춘다 |
| I1c 낙상·정확도 비열세 + 고속 구간 확대 | 케이던스가 병목이었음 확정 |
| I1d < max(I1b, I1c) | 두 레버가 충돌 → 순차 적용으로 전환 |

중단 규칙은 R0와 동일(위치 1.3×, 낙상 3×), 다만 **기준이 I0c의 2.71 cm / 4회**로 바뀐다.
`save_interval 25`라 8개 지점에서 판정하므로 이번엔 조기중단이 실제로 작동한다.

### R1 착수 전에 잡힌 결함 2건

| # | 결함 | 어디서 잡혔나 | 고침 |
|---|---|---|---|
| 1 | `dict(**_OFF_ROBUST, **_I1_FORCE)`가 `disturbance.enabled` 중복으로 TypeError | **GPU 앞 28분** (재평가 대기·모니터·이름검사 다 통과 후) | `merge()`로 뒤 레이어 우선. `DRYRUN`이 arm마다 `--gpu-of`를 돌려 모듈 import와 dict 구성을 강제 → 1초에 잡힘 |
| 2 | 외란 첫 이벤트를 **전체 간격에서** 뽑아 첫 8 s 동안 전 env가 무외란 | smoke `disturbance fires: 0/300` | 첫 이벤트를 **간격 내 균일 위상**(`randint(1, hi/dt)`)에서 뽑음 |

**②는 스모크 창 문제가 아니라 학습 분포 결함이다.** `interval_s [8,14]`에서
`randint(400,700)`으로 초기화하면 **어떤 env도 8초 전에는 외란을 받지 못한다**.
에피소드가 30 s이므로 **매 에피소드의 첫 27%가 전 env 동시에 외란 없는 구간**이었다.
E2·G2가 외란을 켜고도 "강건해졌다"는 증거를 못 준 이유의 일부일 수 있다 —
초반 구간이 통째로 clean이면 정책은 그 구간에 맞춰 최적화된다.
`_reroll_paths`의 dwell 스태거와 **같은 결함, 같은 수정**이다.

### R0 v1 → v2 재설계 경위 (2026-08-03)

v1을 띄운 직후 사용자가 네 가지를 지적했고 전부 타당했다. 기록해 둔다 — 같은 실수를 다시 하지 않기 위해서다.

| # | 지적 | 사실 확인 | 조치 |
|---|---|---|---|
| 1 | `tonight.sh` 로그가 "G 배치 4종"이라고 찍힘 | 사실. 헤더 문자열이 낡음(꼬리는 이미 고쳐져 있었음) | 헤더 수정 |
| 2 | 웹이 아무것도 안 뜸 | 사실. collector가 안 떠 있었음 | `tonight.sh`가 자동 기동하도록 연결 |
| 3 | 터미널이 옛 run까지 전부 출력 | 사실. "실시간 현황"이 목적인데 이력을 뿌림 | TUI 기본을 **진행 중인 run만**으로. 이력은 웹, `--all`로 복원 |
| 4 | 판정 정리가 안 돼 있음 | 사실. H는 12000까지 돌린 뒤에야 결과를 앎 | `tools/watch_eval.py` — checkpoint마다 채점, 사전 확정 규칙으로 자동 중단 |

추가로 사용자가 제기한 **하드코딩 금지 원칙**("결과를 보상하지 말고 그 결과를 필연으로
만드는 조건을 만들어라")에 따라 §5의 보상 추가안 4건(발 하중분배·heel 접지·순항 pitch
페널티·높이 튜닝)을 재검토했다. 앞의 셋은 **폐기**하고 조건(외란·실제 발 크기·케이던스
커플링)으로 대체한다. 높이만은 **새 항 추가가 아니라 기존 상수 교정**이므로 남긴다(§8-5).

그리고 코드 정리(지시 3)를 수행하다 **live 스택 전체가 죽을 뻔한 결함**을 발견했다:
`envs/K1/goal_pose_hbatch.py`를 아카이브했는데 `envs/__init__.py`가 계속 import하고
있었고, `eval_goal_pose.py`/`smoke_v7.py`/`utils/runner.py`가 전부 `from envs import *`를
한다. `check_names.py`는 이걸 **구조적으로 못 잡는다** — 로컬에서 안 풀리는 모듈을
stdlib로 간주하고 건너뛴다. 별도 검사로 local import 53개가 전부 실재함을 확인했다.

### 서버 접속 메모 (매번 찾지 않도록)

**전부 절대경로다.** 상대경로로 적었다가 `cd` 누락으로 두 번 실패했다
(`fatal: not a git repository`, `cd: htwk-gym: No such file`). 어디서 실행하든 동작해야 한다.

**서버에서 실행:**

```bash
# 중단 — 세션명은 SESSION= 로 준 값. GPU가 실제로 비었는지 확인까지 한 줄에.
tmux kill-session -t i 2>/dev/null; pkill -f "tools/watch_eval.py"; pkill -f train_v7.py; sleep 2; nvidia-smi --query-compute-apps=pid,used_memory --format=csv
```

```bash
# 실행
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose && git pull && cd htwk-gym && ARMS="I0a_repro I0b_foot I0c_h055 I0d_h058" ITERS=200 SESSION=i bash tools/tonight.sh
```

```bash
# 터미널 모니터
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose/htwk-gym && python tools/monitor.py --tui          # 진행 중인 run만
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose/htwk-gym && python tools/monitor.py --tui --all    # 끝난 것까지
```

**맥에서 (서버 아님):**

서버 IP `165.246.193.194`는 공인 주소라 터널 없이 바로 열린다:

```
http://165.246.193.194:8420/
```

포트가 막혀 있으면 그때만 터널 — **이 명령은 맥 터미널에서 실행한다.**
서버에서 실행하면 자기 자신에게 접속하는 꼴이고, 서버 conda의 OpenSSL 충돌로
`OpenSSL version mismatch`가 난다.

```bash
ssh -L 8420:localhost:8420 user@165.246.193.194 -p 6666
#   -> 맥 브라우저에서 http://localhost:8420/
```

> 기록 규칙: 라운드가 끝나면 이 표에 **한 줄**만 추가하고, 상세는 §7 결정 트리의
> 해당 노드에 반영한다. 판정은 §8-3처럼 **실행 전에 고정**한 기준으로만 내린다.
