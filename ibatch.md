<!--Be concise. Read only the files you need. Stop searching once you have enough information. Make the smallest correct change. Reuse existing code whenever possible. Do not refactor unrelated code. Avoid unnecessary explanations. Output only the modified code or diff.-->

> **[답변]** 접수. 이 문서는 그 규칙의 예외다 — **일지이자 계획서라 보존이 목적**이다.
> 채팅 응답과 커밋 메시지는 짧게 유지하고, 근거·수치·결정은 전부 여기에만 남긴다.
> 코드는 최소 변경, 기존 코드 재사용, 무관한 리팩터 금지 — R0 구현에 그대로 적용했다(§9).


**이걸 읽는 순간 아래 11개 주석에 먼저 답하고 진행해. 답변은 바로 아래에 달도록 하자**
<!--학습 한 회차를 최대한 빠르게 돌리자. 카드 한 장에 두배치를 돌렸을 때 너무 느려지면 하나씩만 하는게 좋은듯-->

> **[답변]** **동의, 그리고 이미 그렇게 고쳐 놨다.** 카드당 2 프로세스는 처리량이 2배가 아니라 **약 1.5배**(시분할)라, 회차 지연시간은 오히려 1.33배 늘어난다. 회차를 자주 도는 게 목적이면 **카드당 1개가 맞다.** `GPU_OF`에 `I0a→cuda:0`, `I0b→cuda:1`로 박아 뒀다.
> **남는 병렬화 여지**: GPU가 1 프로세스로는 포화되지 않으므로 `num_envs`를 4096 → 8192로 올려 **같은 시간에 2배 표본**을 얻는 쪽이 낫다. 이건 프로세스를 늘리는 게 아니라 배치를 키우는 것이라 시분할 손실이 없다. R0에서 VRAM 여유(H 실측 8 GB/49 GB)를 보고 확정한다.

<!--robust계열에서 속도가 느린 것에 대한 추측: 학습 과정에서부터 goal에 너무 큰 지터를 줘서(어떻게 학습했는지 확인해봐 내 기억으론 2미터짜리 원에서 마구잡이로 섞으라고 됐었던 것 같은데) 그냥 해당 범위 내에 있는 goal에 대해서 모두 솎아내기를 한 결과로서 느려진거 아니야? jitter의 범위를 50cm짜리 원 가우시안?(보통 프로젝션 에러가 이렇게 나니까 공 프로젝션 에 따른 goal도 이정도 범위 안에서 jitter를 함.)을 반영해야 하는 것 아닌가? 그리고 test eval할 때는 커다란 골 jitter를 줘서 넘어지는지 확인하고 넘어지는 것은 다른 방법으로 achieve해야지.-->

> **[답변]** **추측이 맞다. 다만 범인은 지터가 아니라 편향이다.** 실제 설정을 확인했다:
> ```yaml
> goal_pos.range:      [0, 0.04]   # 스텝별 지터 σ=4 cm
> goal_pos_bias.range: [0, 0.05]   # 구간 고정 편향 σ=5 cm  ← 이게 문제
> goal_bt_flicker:     radius 1.0 m, prob 0.004/step   # 기억하신 "2 m 원"에 가까운 것
> ```
> 즉 기억하신 큰 지터는 **`goal_bt_flicker`(반경 1 m 점프)**이고, 상시 지터는 4 cm로 오히려 작다.
> **속도 붕괴의 실제 메커니즘**: 스텝 지터(σ=4 cm)는 시간 평균으로 씻긴다. 그런데 **구간 고정 편향(σ=5 cm)은 씻기지 않는다** — 한 구간 내내 같은 방향으로 틀려 있고, 보상은 참값을 읽으므로 정책은 자기가 틀렸다는 걸 알 방법이 없다. 이 조건에서 기대 보상을 최대화하는 해는 **천천히 가서 헛발질 횟수를 줄이는 것**이다. E2·G2에서 두 번 재현됐다(L4).
> **반영**: 학습에는 **reprojection uncertainty만** 넣는다 — 30–50 cm 가우시안 지터(씻김), **편향은 0**. 큰 flicker와 편향은 **평가 전용**으로 뺀다.

<!--애초에 goal jitter에 대한 대처를 학습에서 어떻게 하고있는지 잘 모르겠음.-->

> **[답변]** **구조는 이렇다** (`goal_pose.py::_update_perceived_goal`):
> | | 정책이 보는 것 | 보상이 읽는 것 |
> |---|---|---|
> | 목표 위치/heading | **노이즈 섞인 값**(지터+편향+2–3스텝 지연+flicker) | **참값** |
> >
> 즉 "관측만 흔들고 채점은 참값" 구조라, 의도는 *필터링을 배워라*였다.
> **문제는 정책이 필터링에 쓸 재료가 없다는 것이다.** 관측 54차원에 **과거 목표 이력도, 불확실성 추정치도 없다.** 매 스텝 한 장의 흔들린 목표만 주고 "걸러내라"고 요구한다. 정책이 할 수 있는 유일한 필터링은 **자기 행동을 둔감하게 만드는 것**이고, 그게 곧 감속이다.
> **그래서 노이즈를 키우면 반드시 느려진다.** 이건 정책의 실수가 아니라 구조가 강제한 최적해다. 해결은 노이즈 크기 조절이 아니라 (a) 씻기는 노이즈만 쓰거나, (b) 관측에 이력을 주는 것인데 (b)는 54차원을 깨므로 I 배치에서는 (a)만 한다.

<!--path를 학습한다는거를 개념을 삭제할까? path는 eval시에만 하고, 학습은 within 2m goal chasing만 하는건 어때? 좀 분리를 해야 낫지 않을까?-->

> **[답변]** **동의한다. 학습에서 뺀다.** 근거는 L5에 이미 있다 — path 학습은 세 대가를 청구한다: waypoint 정확도, 전방 이동 prior(옆·뒤 감속), 낙상(H에서 89.6%가 path 구간).
> **대가**: 지시 7-B(움직이는 목표 추종)를 학습에서 배우지 못한다. 이건 아래 "a와 b를 동시에" 질문과 직결되므로 거기서 답한다.
> **결정**: 학습 = **2 m 이내 goal chasing만**. path·8자·spiral은 **평가 전용**.

<!--네 보고서를 보니까 판정에 대한 정리가 잘 안돼있는 것 같은데 학습 계획 시에 판정까지 맞춤형으로 계획해-->

> **[답변]** **맞다. 판정을 사후에 만들었다.** H가 그 결과다 — 4 arm을 12000까지 돌린 뒤에야 "model_0이 이겼다"를 알았다.
> **고친 방식**: R0부터 모든 arm은 **학습 전에 판정표를 확정**하고, 그 판정을 **코드가 자동으로 내린다**(사람이 리포트를 읽고 판단하지 않는다). §8에 R0 판정표가 있고, 아래 "실시간 eval" 답변의 `watch_eval`이 그걸 자동 집행한다.

<!--eval//train 등 코드가 너무 많아. 사용하던 파일들은 모두 archive하고 버전 하나로 리셋해서 진행. -->

> **[답변]** **진행 중.** 하위 에이전트가 `htwk-gym/archive/`로 이동시키고 있다. 유지 목록은 `tonight.sh` / `make_v7_arms.py` / `smoke_v7.py` / `train_and_eval.sh` / `select_best_checkpoint.py` / `eval_goal_pose.py` / `train_v7.py` / `monitor.*` / `progress.py` / `check_names.py` / `envs/K1/goal_pose{,_v3,_v7}.py` / `utils/*` / `deploy/*` 뿐이다. 이동 후 `py_compile` + `check_names` + `bash -n`으로 회귀 검사하고 `archive/README.md`에 무엇이 왜 폐기됐는지 한 줄씩 남긴다.

<!--내가 headless server를 사용하니까 학습 진행도, 학습 끝, 평가중, 평가끝 등의 현황과 reward graph, iter, checkpoint등을 실시간으로 확인할 수 없어. 사후에도 마찬가지고. 계속 활용할 수 있는 webpage를 만들어서 서버에서 실시간으로 해당 정보들을 송신할 수 있도록 해. 실시간 수신을 마쳐도 계속 확인할 수 있도록 버전별로 또 분류를 하고. 이건 앞으로 하는 모든 학습에 동일하게 적용. 만약에 내가 언급한거 외에도 네가 중요하게 생각하는 부분이 있다면 추가해넣어. consensus가 있을거아니야<-이게 안되면 터미널 창에라도 볼 수 있는 코드를 만들어. 아마 local network를 사용하면 되려나..? 모르겠네.-->

> **[답변]** **만들었고, 지적하신 대로 반쪽이었다.** 아래 항목에서 이어서 답한다.

<!--7번 3축: A(정확·빠름)와 B(움직이는 목표)는 한 정책이 동시에 한 적이 없고, C(불안정 시 안정화 우선)는 이 프로젝트에 존재한 적 없는 개념입니다. §5-C에 게이팅 보상안을 뒀지만 I 1차에는 넣지 않았습니다 — I2(단일지지)가 먼저 서야 의미가 있습니다.<-이거까지 까먹지 않고 할 수 있도록 하려면 어떻게 해야해? 네 메모리에 집어넣으면 돼? i2결과를 보고 하라고? 네가 방법을 찾아봐. 나는 까먹고 싶지 않아.-->

> **[답변]** **메모리에 넣는 걸로는 부족하다 — 잊는 게 아니라 조건이 안 맞아 미뤄지는 것이다.** 세 겹으로 건다:
> 1. **`ibatch.md` §7 결정 트리에 노드로 존재**한다 — 문서를 보면 항상 보인다
> 2. **판정 코드에 미구현 게이트로 등록**한다: `C_stabilize_first` 게이트를 `NOT_IMPLEMENTED`로 넣어 두면 **모든 리포트에 매번 "미구현" 줄이 찍힌다.** 통과할 수 없으니 조용히 사라지지 않는다
> 3. **내 영구 메모리**에 저장한다 — 세션이 끊겨도 유지된다
> ②가 핵심이다. 문서와 메모리는 안 볼 수 있지만 **리포트는 매번 본다.**

<!--a와 b를 동시에 달성하지 못할거면 둘을 따로 학습해도 의미가 없는게, 나는 어차피 하나의 모션만 사용할건데? 그리고 난 둘의 목표가 동시에 필요해. c-->

> **[답변]** **맞는 지적이고, 제 §5-B 설계가 틀렸다.** "I1은 속도, I3은 움직이는 목표"처럼 축을 나눠 학습하면 **합쳐지지 않는다** — 하나의 정책만 쓰실 거니까.
> **고친 설계**: 축을 나누는 것은 **레버 검증 단계에만** 쓰고, 각 라운드의 승자는 **누적**된다. R1 승자 위에 R2를 얹고, R2 승자 위에 R3를 얹는다. 마지막 정책 하나가 A·B·C를 전부 갖는다.
> 그리고 **B(움직이는 목표)를 학습에서 뺐으므로**(위 path 답변) A와 B가 충돌할 일 자체가 줄어든다. 남은 질문은 "path 없이 학습한 정책이 평가의 움직이는 목표를 쫓을 수 있나"이고, 이건 **R0/R1 시점에 평가로 바로 측정된다** — 못 쫓으면 그때 학습에 최소한만 되돌린다.

<!--그니까 나의 수정의도의 whole point is that i dont want to hard code the reward or conditions. i want to drive the learning to the direction that i want from the highest level modifications. if i were to modify the condition or reward specified for a purpose, then there must be a method to achieve the same goal without doing so<-sth like "to make it standing firmly, if i give robustness to the disturbance, i wont have to reward it for standing upright. standing upright is for human. being robust to disturbance does the same thing with covering more fundamental thing". if these methods are findable, please do them.-->

> **[답변]** **이게 가장 중요한 지적이고, 제 §5 설계 전체가 이 원칙을 위반하고 있었다.**
> 제가 넣으려던 것들: `base_height_target` 튜닝, 발 하중분배 보상, heel 접지 보상, 순항 pitch 페널티 — **전부 "사람이 보기에 좋은 자세"를 직접 보상하는 하드코딩**이다. 지적대로, 목적이 있다면 그 목적을 **더 근본적인 조건**으로 유도할 방법이 있어야 한다.
> **원칙을 다시 적으면**: *결과(자세)를 보상하지 말고, 그 결과를 필연으로 만드는 조건(과제·물리·외란)을 만든다.*
> >
> | 하드코딩(하지 말 것) | 상위 조건으로 대체 | 왜 더 근본적인가 |
> |---|---|---|
> | 직립 자세 보상 | **외란 강건성** | 밀려도 안 넘어지려면 **결과적으로** 직립이 유리하다. 직립은 인간의 미감이고, 강건성은 물리적 요구다 |
> | `base_height_target` 값 튜닝 | **다양한 지면·마찰·외란에서 생존** | 최적 높이는 정책이 스스로 찾는다. 우리가 0.58을 고르면 그 조건에만 맞는다 |
> | 발 하중분배 보상 | **실제 발 크기 + 단일지지 과제** | 지지면이 진짜로 좁으면 하중을 고르게 쓰는 것 외에 답이 없다 |
> | heel 접지 보상 | **고속에서 외란** | 오버스트라이드하면 밀렸을 때 못 버틴다. 보상이 아니라 실패가 가르친다 |
> | 순항 pitch 페널티 | **케이던스–속도 커플링** | 보폭이 실현 가능해지면 몸을 던질 이유가 사라진다 |
> >
> **R0에 즉시 반영되는 것**: `feet_edge_pos` 교정은 **하드코딩이 아니라 사실 교정**이므로 유지한다(sim이 거짓말을 하고 있었다). 나머지 보상 항 추가는 **전부 보류**하고, 대신 **외란·지면·발 크기라는 조건**을 정직하게 만드는 쪽으로 라운드를 다시 짠다(§10).
> **단 정직하게**: 이 원칙이 항상 통한다는 보장은 없다. 조건만으로 안 되는 항목이 나오면 **그때 하드코딩을 쓰되, 왜 조건으로 안 됐는지를 먼저 기록**한다.


<!--fine tuning으로 단시간 안에 목표한 바를 학습할 수 있어? eval을 그냥 실시간으로 checkpoint가 나오면 돌려서 중단시키는 방법은 별론가?-->

> **[답변]** **두 질문 다 "그렇다"에 가깝다.**
> **(a) fine-tuning으로 되나** — H 데이터가 답을 준다: 손상이 **iteration 100 안에** 1.4–1.8배로 나타났다. 즉 **200 iteration이면 방향 판정에 충분하다.** 반대로 "좋아지는" 신호도 같은 시간 척도에서 보이는지는 미검증이라, R0가 그것도 같이 잰다(I0a가 E0를 유지하는지).
> **(b) checkpoint 나오는 즉시 eval 돌려 중단** — **별로가 아니라 이게 맞다.** 지금은 12000까지 다 돌린 뒤 32개 후보를 뽑아 평가한다. 그래서 H는 **4 GPU-일을 쓰고 "전부 나빠짐"** 하나를 얻었다.
> 구현: `tools/watch_eval.py` — `nn/model_*.pth`를 감시해 새 checkpoint가 생기면 **짧은 평가(20 s × 256 env)**를 돌리고, 사전 정의된 중단 규칙에 걸리면 run 디렉터리에 `STOP` 파일을 쓴다. `runner_v3.py`가 이미 `STOP` 파일을 읽어 **checkpoint를 저장하고 정상 종료**한다(기존 기능, 재사용).
> 이러면 나쁜 arm은 **몇 분 만에** 죽고 GPU가 다음 라운드로 넘어간다.

# I 배치 — 탐색 결과와 설계 근거

> 2026-08-03. **코드 수정 전 단계.** E/G/H 전 배치와 최상위 문서 20종, 실기 배포 코드,
> 외부 선례를 탐색한 결과를 정리한다. 사용자 검토 후 지시에 따라 구현한다.
>
> 규칙: **한 사실은 한 곳에만 적는다.** §5의 수정안 각 항목은 §1–§4의 근거 번호를
> 직접 참조하며, 근거 없는 수정은 넣지 않는다.

---

## 0. 왜 I 배치인가 — local minimum의 정체

사용자 진단: "현재 학습 방향이 local minima에 빠져버렸어. 이런식으로는 미시적인
수정밖에 하지 못해." **데이터가 이를 지지한다.**

| 배치 | 최고 정책 | 위치 median | 낙상 | strict | 무엇을 얻었나 |
|---|---|---:|---:|---:|---|
| **E** | **E0@6200** | **2.72 cm** | **2** | **89.3%** | 게이트 3/4 통과 |
| G | G1@10700 | 5.52 cm | 38 | 34.2% | >1 m/s 체류 3.0→17.7% |
| H | (없음) | 6.90 cm* | 24.5‰ | — | **없음 — 4 arm 전부 model_0 우승** |

\* H의 6.90 cm는 학습 결과가 아니라 **G1 warm start를 새 평가 프로토콜로 잰 값**이다.

**3세대 연속 후퇴했고, 각 세대는 직전 세대에서 warm start했다.** 즉 매번 "조금 더 나쁜
지점"에서 다시 출발해 미시 조정을 반복했다. 이것이 local minimum의 실체다.

> **탈출 방법은 하나뿐이다: 가중치를 E0로 되돌리고, 무엇을 바꿀지 근거로 다시 고른다.**
> (사용자 지시 2와 일치)

---

## 1. E/G/H에서 확정된 교훈 — 반복 금지 목록

각 항목은 **한 번 이상 실제로 대가를 치른 것**만 넣는다.

### L1. 변인통제 위반 — 5회 반복, 이 프로젝트 최대 손실원

| 사건 | 동시 변경 레버 | 결과 |
|---|---:|---|
| armD | 12 | 24.3 cm 붕괴, 원인 특정에 별도 실험 필요 |
| E2_robust | 7 (`_OFF_ROBUST` 통째) | 속도 붕괴. "필터링 vs 그냥 느림" 분리 실패 |
| G2_robust | 7 (동일 묶음) | **같은 실패 재현** |
| H0 | 10 | 4 arm 공통 붕괴, 원인 특정 불가 |
| H1 | 2 (mirror + joint DR) | mirror 효과 판정 불가 |

> **규칙**: I 배치의 모든 arm은 **직전 대조군 대비 정확히 1개**만 다르다.
> 예외는 사전에 문서에 근거를 적고 "bundle 시험"이라 명시한 경우뿐이다.

### L2. 측정 도구가 결과보다 먼저 검증돼야 한다 — 손실 1위

| 사건 | 증상 | 여파 |
|---|---|---|
| eval config 누락 | path 미학습 arm을 path 46% 과제로 채점 | E 배치 원본 리포트 4종 전부 무효 |
| 속도 지표 리셋 텔레포트 | `peak_speed` 최대 203 m/s | 전 구간 15.7~17.6% 오염. **이 배치가 올리려던 그 지표** |
| `path_lag = max(gap−lookahead, 0)` | 단측(one-sided) | 캐럿 **추월**(floor 붕괴)이 `lag=0` = 완벽으로 기록 |
| grid 승급 기준 `keepup_gap 2.0 m` | leash가 `path_lag ≤ 1.44 m`를 보장 | **구조적으로 실패 불가** → 커리큘럼이 1분 만에 만렙 |
| \|ω\| 지표 | yaw만 측정 | 상체 흔들림(롤·피치) 판정 근거가 애초에 없었음 |
| 외력 submit | decimation 10 중 1회 | **실전달 충격량이 명목의 1/10** (≈0.08 g) |
| `v7_extras` | 마지막 256-env snapshot | 대칭 분석의 통계적 무게 없음 |
| `time_out_buf` 갱신 시점 | 낙상과 goal-resample 동시 시 timeout이 낙상을 가림 | PPO bootstrap 오염 |

> **패턴**: 8건 중 6건이 **"판정에 쓴 상태가 판정 대상이 아니었다"**이다.
> 캐럿(위상 vs 거리), grid(leash가 자기 판정량을 상한), path_lag(단측),
> \|ω\|(축 하나), 외력(적분 구간), diag_reset(리셋 전후).

### L3. 코드 drift는 아무 데도 안 남는다
E1 재평가가 lookahead floor 도입 후 옛 체크포인트를 새 의미로 채점해 무효화됐다
(path 24.8 → 165.9 cm, 낙상 5 → 346). → `ENV_CODE_SHA` 기록으로 방어됨. **유지.**

### L4. 강건성 레버는 항상 "덜 움직이기"로 도망친다 — 2회 재현
E2와 G2가 같은 묶음에서 같은 결과를 냈다: 낙상은 줄지만(G2는 0회) 속도가 붕괴한다
(G2의 >1 m/s 체류 **0.50%**). 원인은 **씻기지 않는 구간별 목표 편향**(`goal_pos_bias`
σ=5 cm)이다 — 스텝 지터는 시간 평균으로 씻기지만 구간 고정 편향은 씻기지 않고,
보상은 참값을 읽으므로 정책은 자기가 틀렸다는 걸 알 수 없다. 기대값 최대화 해는
**천천히 가서 헛발질을 줄이는 것**이다.

> **규칙**: 강건성 항을 켤 때는 **속도 유지 게이트를 반드시 동반**한다.
> 그리고 `bias`(씻기지 않음)와 `jitter`(씻김)를 같은 레버로 묶지 않는다.

### L5. path 학습은 세 가지 대가를 동시에 청구한다
1. **waypoint 정확도** — dwell 도입 전 52.1 cm, 도입 후 5.5 cm. **G1의 dwell이 최대 성과.**
2. **전방 이동 prior** — 옆·뒤 목표에서 감속. path 노출과 단조:
   E0(path 0%) lateral이 straight보다 **+35% 빠름** → E1(46%) −4% → V7 −22%.
3. **낙상** — H에서 낙상의 **89.6%가 path 구간**.

### L6. 성공한 하네스 장치 — 그대로 승계
`ENV_CODE_SHA` 기록 / eval 기본 config = run 자기 config / path 구간 게이트 제외 /
영상 실패 시 exit code+stderr / `check_names.py`+`check_imports` / 5단계 smoke +
atomic marker / **공통 held-out 시험지 + effective test-config SHA 불일치 시 비교 거부** /
`model_0`과 초기 checkpoint를 selection 후보에 강제 포함 /
`(falls)/(completed+falls)×1000` (survivor bias 제거).

---

## 2. 사용자 강조 요구사항 — 우선순위

전 문서 20종에서 원문을 추출해 **반복 횟수**로 정렬했다.
(원문 위치: `masterplan2.md` 주석 17개, `gbatch.md` 주석 9개,
`MASTERPLAN_feedback.md` 불릿 50개, `masterplan2.md` 영상감상 13줄 — **그 외 문서에는
사용자 원문이 0줄이다.** 최신 문서일수록 사용자 목소리가 사라졌다.)

| 순위 | 요구 | 반복 | 시도 배치 | 현재 상태 |
|---:|---|---:|---|---|
| **P1** | 위치 ≤5/10 cm · heading ≤10° · **낙상 0** | **22+** | E·G·H | 위치·heading은 E0만 통과. **낙상은 전 배치 미통과** | <!--중요-->
| **P2** | 몸통속도 **1.3–1.5 m/s** | 9 | E1→G1→H | 미달. 원인 §3-2 | <!--중요-->
| **P3** | 외란을 **충격량 계산해서** 반영 | 9 | E2→G2/G3→H→M1 | 설정 40–150 N, **실전달 1/10** |
| **P4** | 좌우 대칭 (`right hip yaw`가 중심으로 치우침) | 10 | E0→G→H1/H2 | 지표가 3번 틀림. H1은 **역효과**(bias 2.9→9.4 cm) |<!--이건 그냥 빼자. robustness를 학습을 한 뒤에도 대칭이 안 맞으면 하는거로 밀어둬->
| **P5** | **팔 내리기** (팔뚝 90°, 손을 엉덩이 뒤로) | 8 | E0→G3→H | E0에서 확보 → **H가 asset 교체로 되돌림**(§3-1) |<!--k1_locomition.urdf의 기본자세로 하는거로 진행 ㄱㄱ-->
| **P6** | goal이 마구잡이로 변해도 robust | 7 | E2→G2→H | 낙상 0이나 "필터링 vs 느림" 미분리 (L4) |<!--큰 지터(2m)짜리는 학습과정에 넣을게 아니라 테스트만 하자. 학습에 넣을건 reprojection uncertainty만. 30cm에서 50cm정도-->
| **P7** | 8자·spiral·star 등 **연속 lookahead 목표** | 5 | E1→G1→H | G1 dwell로 수리. 낙상 89.6%가 path |<!--path도 big jitter와 마찬가지로 테스트만 하는건 어때? 학습 시에 집어넣지 말고-->
| **P8** | 로거/eval 병행 강제 | 7 | G·H | **가장 잘 이행됨** (L6) |<!--중요-->

**[질문] P1·P2·P8 <!--중요-->**
**[답변]**
> 접수. **I 배치의 채택 게이트는 P1(낙상 0)·P2(1.3–1.5 m/s)** 둘로 고정하고, 나머지는
> 비열세 조건으로만 쓴다. P8(로거/eval 병행)은 이미 하네스에 박혀 있다(L6) — 유지.

**[질문] P4 <!--이건 그냥 빼자. robustness를 학습을 한 뒤에도 대칭이 안 맞으면 하는거로 밀어둬-->**
**[답변]**
> **동의. 근거도 있다.** H1이 mirror를 강화했더니 오히려 mirror error +87.5%,
> 좌우 touchdown bias 2.9 → 9.4 cm로 **역효과**였다. 그리고 §3-1이 맞다면 그 실험 자체가
> asset 오염 위에서 돌았다. → **I 배치에서 제외.** robustness 학습 후에도 남으면 그때 본다.

**[질문] P5 <!--k1_locomition.urdf의 기본자세로 하는거로 진행 ㄱㄱ-->**
**[답변]**
> `k1/K1_locomotion.urdf`의 기본자세 = shoulder roll ∓1.35 rad, 팔꿈치 0
> = **`hbatch-codex.urdf`와 정확히 같은 각도**다. 즉 **H가 쓴 그 asset**이고,
> 양팔 yaw I_zz가 G1/E0 대비 **+32.3%**다.
>
> | | 각도 | I_zz | H에서 쓴 적 |
> |---|---|---|---|
> | `armsdown` (E0/G1이 학습) | roll ∓1.5708, elbow 2.08/∓1.58 | 0.07624 | 아니오 |
> | `k1_locomotion` 기본 = `hbatch-codex` | roll ∓1.35, elbow 0 | **0.10090** | **예 (4 arm 전부)** |
> | `hbatch`(신규) | roll ∓1.72, elbow 1.40/∓0.218 | 0.06984 | 아니오 |
>
> **지시하신 값이 제 유력 용의자와 같습니다.** 다만 §3-1은 **유력이지 확정이 아니고**,
> A 스크린이 지금 그걸 시험 중이다. → **A 스크린 결과를 보고 정한다.**
> A2(codex asset)만 무너지면 지시를 재고 요청드리고, 4셀 다 무너지면 asset은 무죄이므로
> 지시대로 `k1_locomotion` 기본자세로 간다.

**[질문] P6 <!--큰 지터(2m)짜리는 학습과정에 넣을게 아니라 테스트만 하자. 학습에 넣을건 reprojection uncertainty만. 30cm에서 50cm정도-->**
**[답변]**
> **동의하고, 이건 현재 설정보다 오히려 크게 키우는 지시다.** 현재 학습 노이즈는
> `goal_pos` σ=4 cm / `goal_pos_bias` σ=5 cm로 30–50 cm의 **1/10 수준**이다.
> - 학습: `goal_pos.range` σ를 0.30–0.50 m 상당으로 (reprojection uncertainty 모델)
> - 학습에서 제거: `goal_bt_flicker`(1 m 급 점프) → **평가 전용**
> - L4 규칙 적용: **`bias`(안 씻김)와 `jitter`(씻김)를 분리**하고 속도 유지 게이트 동반

**[질문] P7 <!--path도 big jitter와 마찬가지로 테스트만 하는건 어때? 학습 시에 집어넣지 말고-->**
**[답변]**
> **가장 큰 결정이고, 대체로 동의하지만 대가를 명시한다.**
> - 얻는 것: L5의 세 대가(waypoint 정확도·전방 prior·낙상 89.6%)가 **전부 사라진다**
> - 잃는 것: **지시 7-B(움직이는 목표 추종)를 학습에서 배우지 못한다.** path는 B축을
>   학습시키던 유일한 장치였다
> - **절충안**: path 자체는 빼되 **목표가 천천히 이동하는 waypoint**(예: 0.2–0.5 m/s로
>   드리프트하는 goal)를 낮은 비중으로 남긴다. 캐럿·leash·dwell·grid 기계 전부 불필요하고,
>   B축의 최소 형태만 유지된다
> → **I 1차는 path OFF로 간다.** 위 절충안은 I2 이후 별도 arm.

### 사용자 지시 7 — 병렬 만족해야 하는 3축

| 축 | 내용 | 현재 최고 | 대응 |
|---|---|---|---|
| **A** | 목표까지 **정확하고 빠르게** | 정확 E0(2.72 cm) / 빠름 G1(1.50 m/s p90) — **한 정책이 둘 다 한 적 없다** | §5 I1 |
| **B** | **움직이는 목표**에 대해 A 수행 | G1이 유일하게 근접(waypoint 5.5 cm + path) | §5 I2 |
| **C** | A·B를 하면서 외란/외력/팔걸림/충돌에 robust, **불안정 감지 시 목표추종보다 안정화 우선** | 미구현 | §5 I3 |

> **C의 "안정화 우선"은 이 프로젝트에 존재한 적 없는 개념이다.** 지금 구조는 보상 합이
> 항상 목표추종과 자세를 **동시에** 요구할 뿐, 상태에 따라 **우선순위를 바꾸는** 장치가
> 없다. §5 I3이 이를 다룬다.<!--중요. 나중에 안정화하는거 학습하고, walk-stablize-walk의 전환을 빨리 하는 것도 학습을 하자.-->

> **[답변]** 접수. **I 1차 범위 밖.** 단 전환 속도를 나중에 측정하려면 지금 **전환 지표를 먼저
> 심어야** 한다 — 불안정 진입/복귀 시각, 복귀까지 걸린 시간, 복귀 중 목표거리 증가량.
> 계측만 I 1차에 넣는다(비용 0).

---

## 3. 근본 원인 — 확정된 것

### 3-1. H 공통 붕괴: 팔 asset 교체 (유력, 미확정)

4 arm이 iteration 0부터 단조 열화해 **같은 값(≈0.40 m)으로 수렴**했고 `model_0`이
4/4 우승했다. 처치가 무관하면 공통부가 원인이다.

전 정점 7050개 메시 감사:

| asset | Trunk 관통 | ego 반폭 | 양팔 yaw I_zz | G1 대비 |
|---|---|---|---|---|
| `armsdown` (G1이 학습) | 25.3 mm | 0.160 | 0.07624 | — |
| `hbatch-codex` (H 4종 전부) | 0 | 0.251 | 0.10090 | **+32.3%** |
| `hbatch` (신규) | 0 | 0.155 | 0.06984 | −8.4% |

**그 교체는 물리적 이득이 0이다.** 팔 관절이 전부 `type="fixed"`이고
`asset.collapse_fixed_joints: true`라 팔이 Trunk와 **한 강체로 병합**된다 —
PhysX는 `armsdown`의 관통을 계산조차 하지 않는다. 사용자가 본 겹침은 **렌더링 전용**이다.
(`gbatch.md` §7-5④의 444,180 N은 팔꿈치가 `revolute`인 **armswing**이었다.)

→ **렌더링 수정 대가로 요 관성 +32.3%를 warm start 정책에 지불시켰다.**
현재 A 스크린(`hbatch.md` §4)이 이를 시험 중이다.
<!--롤백을 하라는 소리인가?-->

> **[답변]** **네. 두 가지 롤백이고 서로 다릅니다.**
> 1. **가중치 롤백**: H/G 체크포인트 버리고 `E0@model_6200`에서 재시작 — 지시 2, 확정
> 2. **asset 롤백**: `hbatch-codex.urdf` → `armsdown.urdf` — **7-3 때문에 보류**,
>    A 스크린 결과가 정함

### 3-2. 속도 미달: 케이던스가 보폭을 불가능하게 만든다

`gait_frequency: [1.8, 2.4]` Hz가 **명령속도와 독립 균일 샘플**이고(`goal_pose.py:446`),
1 사이클 = 2보(`goal_pose.py:1044-1045`) → 보속 = 2f = 3.6~4.8 보/초.

| 명령속도 | 필요 보폭 @1.8 Hz | @2.4 Hz | 실현가능(다리 0.52 × 0.5~0.6) |
|---|---|---|---|
| 1.0 m/s | 0.278 m | 0.208 m | 0.26–0.31 ✓ |
| **1.5 m/s** | **0.417 m** | 0.313 m | ✗ |
| 1.8 m/s | 0.500 m | 0.375 m | ✗ |

→ 정책의 유일한 대응은 **상체를 던져 CoM을 지지면 밖으로 내보내는 것**(제어된 낙하).
사용자가 관찰한 "고속에서 상체가 기울어짐"은 **학습된 선택이 아니라 강제된 결과**다.
G1의 실제 평균속도가 명령 0.4~1.9 m/s 전 구간에서 **0.32~0.49 m/s로 평평한** 것도 같은 원인.

> 이 처방(속도–케이던스 커플링)은 `gbatch_results.md` §3에서 "H 전부 채택"으로 선언됐으나
> **H0–H3 어느 config에도 반영되지 않았다.** 미이행 항목.

<!--나는 goal pose condition을 학습하고 있는데 속도샘플링이 무슨 뜻이지? 암튼 적합한 gait freq와 vel의 조합을 찾은다는 뜻이지?-->

> **[답변]** **맞게 이해하셨습니다 — 적합한 (gait_freq, 속도) 조합을 찾자는 뜻입니다.**
> 정확히는: `gait_frequency`가 **명령이 아니라 관측 슬롯 3번**으로 정책에 들어가고,
> 현재 `[1.8, 2.4]`에서 **매 구간 균일 난수**로 뽑힙니다. goal pose 과제에서 "속도"는
> 직접 명령되지 않고 **목표거리 ÷ 남은 시간**으로 암묵적으로 결정됩니다.
> 문제는 그 둘이 **독립**이라는 것 — 먼 목표(높은 암묵속도)에 낮은 케이던스가 뽑히면
> 물리적으로 불가능한 보폭이 요구됩니다. → 케이던스를 **암묵 요구속도의 함수**로 뽑는다.

### 3-3. 정확도: 공 내부에 기울기가 없다

`goal_reach_radius 0.1`, `goal_reached` 이진, `constellation_weight 0.2 × scale 3.5`:
- 10 cm 문턱 통과 = **+1.0/step**
- 오차 10→5 cm = **+0.005/step**
- **비율 200배**

E0가 같은 보상으로 2.72 cm를 냈으므로 이것이 **절대적 한계는 아니다.** 다만 공 안에서
어디에 앉을지는 다른 압력이 정하며, path 학습이 그 압력을 5.5 cm 쪽으로 민다.
→ **필수가 아닌 강건화 항목.**
<!--공이 무슨 뜻이지? goal? -->

> **[답변]** **goal 주위 반경 10 cm 구(sphere)**입니다 — `goal_reach_radius: 0.1`.
> 그 안에 들어가면 `goal_reached`가 +1/step을 주는데, **안에서는 더 가까워져도 보상이
> 거의 안 늘어납니다**(200배 차이). 그래서 "공 안에 겨우 들어가서 멈추기"가 최적해가 됩니다.

### 3-4. 옆·뒤 목표 감속: path 전방 prior (§L5-2, 반증 완료)

constellation 결합 때문이라는 가설은 **E0가 반증한다** — 같은 보상, path 미학습,
시작거리 0.6–0.9 m 통제 시 lateral peak **0.91 m/s** vs straight **0.71 m/s**.
E0에서는 lateral이 **더 빠르다.**

---

## 4. 실기 standing 불안정 (사용자 지시 6)

### 4-1. "body height 리워드로 고쳤다"는 주장 — **기각**

| 검증 | 결과 |
|---|---|
| 이미 있는가 | **있다.** `base_height: -20`, `base_height_target: 0.52`, `_reward_base_height = (h − 0.52)²`. Booster T1 레퍼런스와 같은 가중치 |
| 수치적으로 작동하는가 | E0 기본 자세의 몸통 높이가 이미 **0.5144 m** → 명목에서 **−0.0007/step**, 2 cm 침하에서 −0.008. `survival` +0.25, `constellation` 최대 +3.5 대비 무의미. `only_positive_rewards: true`가 아예 잘라낼 수도 있음 |
| 관측 가능한가 | **아니다.** base height는 54차원 관측에 **없다.** 보상은 특권 정보(ground truth)를 읽으므로 **개루프 자세 prior**만 설치할 수 있다 |
| 단일 지지에서 유효한가 | **아니다.** 한 발을 들면 다리 기구학이 몸통 높이를 더 이상 결정하지 않아, **정확히 실패하는 지점에서 신호가 없다** |
| 부작용 | 높이를 올리면 무릎이 펴져 capture point 한계가 **줄어든다** (`v_max = w√(g/l)`: 0.173 → 0.163 m/s) |

> **판정: 이미 최대 가중치로 들어 있고, 이 실패 양상에서는 수치적으로 불활성이다.**
> 친구분이 옳은 유일한 경우는 **로봇이 넘어지기 전에 눈에 띄게 주저앉을 때**다.
> → 확인 질문: "넘어지기 직전에 키가 낮아지던가?" 아니라면 다른 원인이다.
<!--일단 주저앉아. 그건 확실해. 확인질문에 대한 답: yes. 그리고 임의로 0.58로 올리니까 잘 서던데? 최적값을 찾는거를 목표로 세워-->

> **[답변]** **철회합니다. 친구분 방향이 맞습니다.** 제 기각의 유일한 반증 조건이 "넘어지기 전에
> 주저앉는가"였고 **yes**라고 하셨습니다. 그리고 코드에 이미 모순이 있습니다:
>
> ```yaml
> init_state.pos:      [0, 0, 0.58]     # 스폰 높이
> base_height_target:  0.52             # 보상이 끌고 가는 높이
> ```
> **스폰은 0.58인데 보상은 0.52로 끌어내립니다.** 6 cm 차이이고, 0.58에서 잘 섰다는
> 관찰과 정확히 일치합니다. → **`base_height_target`을 탐색 대상으로 승격**하고
> 0.52 / 0.55 / 0.58 세 값을 단일 레버로 비교한다. (§4-1 표는 "이미 있으니 무의미"가
> 아니라 **"값이 틀렸다"**로 정정.)

### 4-2. 진짜 원인 후보 ① — **배포 래퍼가 gait clock을 게이팅하지 않는다** (코드 확정)

학습:
```python
# goal_pose.py:490 — stand 목표는 클럭을 얼려 feet_swing 유인을 제거한다
self.gait_frequency[env_ids[stand]] = 0.0
```

ParameterWalk 배포 (`deploy/utils/policy.py:42-59`) — **게이팅 있음**:
```python
if np.linalg.norm(self.smoothed_commands) < 1e-5:
    self.gait_frequency = 0.0
self.obs[9] = np.cos(2*np.pi*self.gait_process) * (self.gait_frequency > 1.0e-8)
```

E0 배포 (`deploy/utils/policy_goal_pose.py:116-130`) — **게이팅 없음**:
```python
self.gait_process = np.fmod(time_now * self.gait_frequency, 1.0)   # 무조건
self.commands[3]  = self.gait_frequency                             # 무조건
self.obs[16] = np.cos(2.0*np.pi*self.gait_process)                  # 무조건
self.obs[17] = np.sin(2.0*np.pi*self.gait_process)
```
같은 파일 30줄에 **그 사실이 주석으로 적혀 있다**:
`NOTE: E0 does NOT gate the gait clock / commands by gait_frequency the way ...`

> **결론: 실기에서 E0는 stand regime에 진입할 수 없다.** 목표가 0이어도 2 Hz 클럭이
> 계속 돌아 제자리걸음한다. 관찰된 증상("서 있는 자세가 불안정, 한 발 들면 넘어짐")과 일치한다.
> **이것은 학습 문제가 아니라 배포 코드 문제이며, 리워드를 만지기 전에 고쳐야 한다.**
<!--배포에 관한거는 추후에 수정할테니까 해당 파일로 코멘트 위치를 옮겨.-->

> **[답변]** 접수. §4-2를 `htwk-gym/deploy/utils/policy_goal_pose.py` 상단 주석으로 옮기고
> ibatch.md에는 한 줄 포인터만 남긴다. **학습 항목이 아니므로 I 배치 게이트에서 제외.**
### 4-3. 진짜 원인 후보 ② — 단일 지지 자체가 학습 분포에 없다

`stand` 카테고리는 클럭을 0으로 만든다 → 정지 학습이 **양발 지지 정적 자세**로만 이뤄진다.
단일 지지는 **보행 중에만** 등장하고 그때는 전진 모멘텀이 안정성을 대신 준다.
**"제자리에서 한 발을 드는" 상태는 학습된 적이 없다.**
<!--그러면 이건 어떻게 해야해?-->

> **[답변]** `stand` 카테고리에서 클럭을 0으로 만드는 대신(`goal_pose.py:490`), **stand의 일부를
> 저주파 체중이동으로** 바꾼다. 즉 목표는 그대로 "제자리"인데 gait_frequency를
> 0.5–0.8 Hz로 주면 **제자리에서 좌우 단일지지**가 학습 분포에 들어온다.
> 비용: `stand` 비중 0.1 중 절반만 쓰면 전체의 5%. 관측·구조 변경 없음.

### 4-4. 물리적 여유가 없다 (K1 수치)

| 항목 | 값 |
|---|---|
| 무게 / CoM 높이 | 183.6 N / 발목 위 0.40 m |
| 발 반폭 (실제 충돌 박스 0.16×0.07) | **0.035 m** |
| 발산 시상수 √(l/g) | **0.202 s** |
| 2 Hz 단일지지 구간 | 0.25 s = **1.24 e-folds** → 이지 1 cm 오차가 접지 시 3.5 cm = **발 밖** |
| 최대 횡방향 복원 모멘트 | **6.4 N·m — 발 크기 제한**(발목 롤 한계 15 N·m가 아님) |
| `m·g·l` vs 발목 Kp | **74 N·m/rad > 50** → 발목은 **수동적으로 불안정**, 균형은 전부 능동 피드백 |
<!--이거에 대해서는 내가 어찌할 수 있지? 피드백에 반영해야해?-->

> **[답변]** **대부분 하드웨어 상수라 학습으로 못 바꿉니다.** 다만 셋은 반영 가능합니다:
> 1. **발 크기** — `feet_edge_pos`가 실제보다 크게 잡혀 있다(7-14). 고치면 sim이 실제만큼
>    어려워진다 → **이게 가장 큰 항목**
> 2. **발목 Kp 50 < m·g·l 74** — 실기에서 Kp를 올릴 수 있다면(7-16) sim/실기 동시 상향
> 3. 나머지(무게·CoM·발산 시상수)는 **설계 상수** → 학습이 그 안에서 살아남게 하는 수밖에 없다

### 4-5. 그 외 확인된 sim2real 격차

| 항목 | 현재 | 참조 |
|---|---|---|
| `asset.feet_edge_pos` | **T1의 발**(K1 박스보다 길이 +39%, 폭 +43%) — sim 접촉 판정을 좌우 | — |<!--T1이 갑자기 왜 나와 병신새끼야. 이거 k1에 맞게 수정이 안돼있어? crosscheck.-->
| 배포단 target EMA 0.8/0.2 | 학습에 없음. τ≈9 ms → 25 Hz에서 이득 0.58, 위상 54° 지연 | — |
| latency DR | 0–18 ms, **지터 없음** | HuB/ASAP 20–60 ms | <!--이건 무조건 넣어야지.-->
| PD 랜덤화 | **±5%** | 타 humanoid 배포 ±25% | <!--로봇에 넣을때 모터 pd를 내가 조정할 수 있던데? 100 50 혹은 80 30 이렇게. 이건 뭐야? 학습 내부로 집어넣을 수 없는건가?-->
| `joint_encoder_bias` | 코드에 있으나 **E0에서 OFF** | — |<!--이거 무조건 default로 해야하는게 k1의 joint zero가 꽤나 자주 틀어저. 그래서 posture가 비정상이되고 zero cali를 해야하는데 이거 랜덤화로 해결할 수 있는지를 절대 까먹으면 안돼. 최선을 다해서 최고의 결과를 낼 수 있는 문제해결 방법을 알아내봐-->
| IMU 장착 회전 DR | **없음** | 1° ≈ 7 mm ≈ **발 반폭의 20%** | <!--특히 이것도 randomization이 필요한 것이 개체차가 이미 존재하며, imu값은 우리가 안 만져. cali도 못해. 틀어지든 말든 믿고 가야하는 항목인데 joint encoder와 같이 묶어서 강건화 계획을 짜봐.-->
| 발 접촉력 기반 보상 | **35개 보상 중 0개.** `contact_force`는 텐서 셋업(`goal_pose.py:209-223`)에만 등장 | CoP/ZMP·하중분배·발목전략 전부 부재 |<!--접촉력을 real에서 측정할 수 있는 방법이 없는데.. 이건 어떻게 하겠다는거야?-->

**[질문] feet_edge_pos <!--T1이 갑자기 왜 나와 병신새끼야. 이거 k1에 맞게 수정이 안돼있어? crosscheck.-->**
**[답변]**
> | | x 앞 | x 뒤 | 길이 | y 반폭 | 폭 |
> |---|---|---|---|---|---|
> | `feet_edge_pos` (설정) | +0.1215 | −0.1015 | **0.223 m** | ±0.050 | **0.100 m** |
> | 실제 충돌 박스 (URDF) | +0.094 | −0.066 | **0.160 m** | ±0.035 | **0.070 m** |
> | 차이 | +29 mm | −36 mm | **+39%** | +15 mm | **+43%** |
>
> `left_foot_link` collision = `box 0.16 0.07 0.032 @ (0.014, 0, −0.008)`.
> **sim의 발 모서리 판정이 실제 발보다 사방으로 1.5~3.6 cm 크다.** §4-4의 발 반폭 0.035 m가
> 설정상 0.05 m로 들어가 있으니, **sim은 실제보다 43% 넓은 지지면에서 균형을 배웠다.**
> → `feet_edge_pos`를 실제 박스 모서리로 교정. **I 배치 필수 항목으로 승격.**

**[질문] latency DR <!--이건 무조건 넣어야지.-->**
**[답변]**
> 동의. 현재 0–18 ms, 지터 없음 → **20–60 ms + 스텝별 지터**로. 관측·구조 변경 없음.

**[질문] PD 랜덤화 <!--로봇에 넣을때 모터 pd를 내가 조정할 수 있던데? 100 50 혹은 80 30 이렇게. 이건 뭐야? 학습 내부로 집어넣을 수 없는건가?-->**
**[답변]**
> **넣을 수 있고, 넣어야 합니다.** 현재 sim은 `Hip 100 / Knee 100 / Ankle 50`,
> damping `2/2/1`이고 랜덤화는 **±5%** 뿐이다.
> - 실기에서 조정 가능하다면 **배포할 값을 먼저 정하고 sim을 거기에 맞춘다** — 지금은
>   sim 값이 무엇을 겨냥한 건지 문서에 없다
> - ±5%는 타 humanoid 배포(±25%) 대비 **5배 좁다**. armD가 PD 2배로 붕괴한 전례가 있으니
>   **범위를 넓히되 단일 레버로** 검증
> - 발목 Kp 50은 `m·g·l = 74 N·m/rad`보다 **작아서 수동 불안정**(§4-4).
>   실기에서 올릴 수 있다면 이게 standing에 직접 듣는 항목이다

**[질문] joint_encoder_bias <!--이거 무조건 default로 해야하는게 k1의 joint zero가 꽤나 자주 틀어저... 랜덤화로 해결할 수 있는지를 절대 까먹으면 안돼-->**
**[답변]**
> **부분적으로 됩니다. 그리고 이건 우선순위 상위입니다.**
> - `joint_encoder_bias`는 **코드에 이미 있는데 E0에서 꺼져 있다** → 켠다
> - 다만 두 개를 분리해야 한다:
>   | 항목 | 의미 | 대응 |
>   |---|---|---|
>   | `joint_encoder_bias` | 정책이 **보는** q가 틀림 | 관측에만 더함. 정책이 "내 센서는 틀릴 수 있다"를 배움 |
>   | `joint_target_offset` | PD **목표**가 틀림 | 실제 토크가 틀린 자세를 향함 |
>   실기의 zero 틀어짐은 **둘 다** 일으킨다 → 같은 크기로 함께 켠다
> - **랜덤화가 못 고치는 것**: bias가 관측에 없으므로 정책은 **어느 방향으로 틀렸는지 모른다.**
>   기대할 수 있는 건 "어떤 offset에서도 안 넘어지는 보수적 자세"이지 **offset 보정이 아니다**
> - **더 나은 해법(권고)**: 랜덤화 + **기립 시 자동 zero 추정**. 양발 접지·정지 상태에서
>   기구학으로 몸통 자세를 풀면 관절 zero 오차가 관측 가능해진다. 이건 학습이 아니라
>   배포단 루틴이며, 랜덤화보다 근본적이다
> → **I 배치: 랜덤화 ON(단일 레버). 배포단 zero 추정은 별도 항목으로 §6에 등재.**

**[질문] IMU 장착 회전 DR <!--imu값은 우리가 안 만져. cali도 못해. joint encoder와 같이 묶어서 강건화 계획을 짜봐.-->**
**[답변]**
> 동의. 둘은 **같은 종류의 결함**이다 — *정책이 볼 수 없는, 에피소드 내내 고정된 관측 편향*.
> → **하나의 레버 `calibration_dr`로 묶는다**: IMU 장착 회전(roll/pitch ±1–2°, yaw ±3°)
> + joint encoder bias + joint target offset. 셋 다 에피소드 상수, 셋 다 관측에 없음.
> 묶는 근거는 L1(변인통제)의 예외 조건 — **같은 물리적 원인(캘리브레이션)이고 실기에서
> 분리 측정이 불가능**하므로 bundle로 시험하고 문서에 명시한다.

**[질문] 발 접촉력 보상 <!--접촉력을 real에서 측정할 수 있는 방법이 없는데.. 이건 어떻게 하겠다는거야?-->**
**[답변]**
> **정책 관측에 넣자는 게 아닙니다. 보상에만 씁니다.**
> - 보상은 시뮬레이터의 **특권 정보(privileged)**를 읽어도 된다. `goal_dist`도 이미 그렇다
> - 정책 관측 54차원은 **그대로**다 → warm start 보존
> - 학습이 끝나면 그 보상은 사라지고, 남는 것은 "양발 하중을 고르게 쓰는 자세"라는 **행동**
> - 실기에서 측정할 필요가 없다
> → 이 방식(reward-only privileged term)은 asymmetric actor-critic의 표준 용법이다.

---

## 5. 수정안 — 근거 1:1 매핑

**아직 구현하지 않았다.** 각 항목은 근거 번호를 달고 있고, 근거 없는 항목은 없다.

### 5-0. 공통 전제

| # | 조치 | 근거 |
|---|---|---|
| **C1** | **가중치를 `E0@model_6200`으로 롤백** | §0 (3세대 연속 후퇴), 사용자 지시 2 |
| **C2** | asset은 A 스크린 결과가 정한다. 확정 전에는 **`armsdown`(G1/E0가 학습한 것)** 사용 | §3-1 |
| **C3** | arm당 대조군 대비 **정확히 1레버** | L1 |
| **C4** | L2의 계측 수정 8건이 전부 반영된 하네스에서만 학습 | L2 |
<!--만약 카드 당 두개씩 돌리느라 9시간 12시간 걸릴 것 같으면 카드 당 하나씩 4시간으로 해서 빠르게 여러번 돌리자. 계획을 모두 리스트업을 해. decision tree마냥. 비고도 크게크게 작성해서 언제 다시보든 계획을 이어갈 수 있도록 하고.-->

> **[답변]** **동의. 그리고 지금 데이터가 그 선택을 지지한다.** H의 열화는 iteration 100에 이미
> 1.4–1.8배였다 — **긴 학습이 아니라 짧은 학습 여러 번이 맞다.**
> 결정 트리는 §7에 작성.

### 5-A. 실기 standing — 학습 전에 고칠 것 (사용자 지시 6)

| # | 조치 | 근거 | 성격 |
|---|---|---|---|
| **S1** | `policy_goal_pose.py`에 ParameterWalk와 **같은 gait 게이팅** 추가 | §4-2 | **배포 코드 수정. 재학습 불필요** |
| **S2** | 진단 먼저: 낙상 특성화 → 클럭 회전 확인 → 오프라인 관측 재생 → 발목 추종/포화/대역폭 → IMU 프레임 → 발 형상 감사 | §4-4, §4-5 | 측정 |
| **S3** | `asset.feet_edge_pos`를 K1 실측 발로 교정 | §4-5 | asset |
| **S4** | 배포단 EMA(0.8/0.2)를 학습 액션 필터에 반영하거나 배포에서 제거 | §4-5 | 정합 |
| ~~S5~~ | ~~base height 리워드 추가~~ | §4-1 **기각** | — |

### 5-B. I 배치 arm 후보 (사용자 지시 7의 3축)

| arm | = E0 + | 축 | 근거 | 반증 조건 |
|---|---|---|---|---|
| **I0_control** | (없음, 하네스만 신규) | — | C1·C4 | E0 재현 실패 시 하네스가 원인 |
| **I1_cadence** | **속도–케이던스 커플링** `f = clip(v_cmd/(2·L_max), 1.6, 3.2)`, `L_max≈0.28` | **A(빠름)** | §3-2, P2 | 속도 미상승 또는 정확도 열화 |
| **I2_stand** | **단일지지 학습**: `stand` 카테고리에서 클럭을 0으로 두지 않고 **저주파 체중이동**을 명령 + 발 하중분배 보상 | **C(안정)**, P1(낙상 0) | §4-3, §4-5 | 단일지지 생존시간 미개선 |
| **I3_path** | path 재도입 (dwell 포함), **dose를 축으로** 0.15 | **B(움직이는 목표)** | L5, §3-4 | lateral 감속이 dose와 무관 |

**I2가 P1(낙상 0)의 핵심이다** — 전 배치가 낙상 게이트를 통과한 적이 없고,
발 접촉력을 쓰는 보상이 35개 중 0개다(§4-5).

### 5-C. "불안정 감지 시 안정화 우선" (사용자 지시 7-C)

현재 구조에 **없는 개념**이다. 두 방식이 가능하다:

| 방식 | 형태 | 장점 | 위험 |
|---|---|---|---|
| **게이팅 보상** | 불안정 지표 `u`(예: `‖projected_gravity_xy‖`, CoP 여유, \|ω_xy\|)로 목표추종 항을 **감쇠**: `constellation × (1−σ(u))` | 관측 변경 없음, 54차원 유지 | 게이트가 항상 켜지면 E2형 저속 붕괴(L4) |
| 계층 정책 | 안정화 정책과 추종 정책을 분리 | 명시적 | 관측/구조 변경 → **warm start 파괴** |

→ **게이팅 보상만 검토한다.** 단 L4에 따라 **속도 유지 게이트를 반드시 동반**하고,
`u`의 임계값은 §4-4의 물리량(발 반폭 0.035 m, 발산 시상수 0.202 s)에서 역산한다.
**단독 arm(I4)으로 격리하며, I 배치 1차에는 넣지 않는다** — I2가 먼저 서야 의미가 있다.
<!--좋아. 단계적으로 하되, 계획은 미리 해야지. 근데 이걸 발의 값을 계산해서 사용하나?-->

> **[답변]** **네. 그리고 지금 그 값이 틀려 있습니다(7-14).** heel 위치 `x = −0.1015 m`는
> `feet_edge_pos`의 뒤쪽 모서리에서 온 값인데, 실제 박스 뒤끝은 **−0.066 m**다.
> heel 관련 보상을 만들면 **3.6 cm 틀린 지점**을 heel이라 부르게 된다.
> → 7-14를 먼저 고치고, 그 다음에 heel 관련 항목을 논한다. **순서가 중요하다.**
>
> ---

---

## 6. 미해결 / 사용자 판단 필요

| # | 항목 | 왜 내가 정하지 않았나 |
|---|---|---|
| Q1 | **친구분 확인 질문**: 넘어지기 직전에 로봇이 눈에 띄게 **주저앉는가?** | 예이면 §4-1의 기각이 뒤집힌다 |
| Q2 | A 스크린(진행 중) 결과 전에 I를 시작할지 | asset이 확정되면 C2가 정해진다 |
| Q3 | 낙상 0(P1)과 속도 1.3–1.5 m/s(P2)를 **동시에** 요구할지, 단계로 나눌지 | 지금까지 한 정책이 둘 다 한 적이 없다 |
| Q4 | `stand_posture −1`, `stop_ang_speed_threshold 0.3`(H 공통 변경 D) | A 스크린이 아직 분리하지 않는 축 |
| Q5 | 실기 `.pt` export 단계 | 문서상 사용자 본인 작업으로 표시돼 있다 |

**탐색 원본**: `standing_balance_research.md`(선례·인용·8단계 진단 체크리스트),
`user_requirements.md`(원문 인용 전수·반복 횟수·배치별 시도 이력) — 둘 다 스크래치패드.

---

## 7. 실행 계획 — 결정 트리 (카드당 1 프로세스, 라운드당 ~4시간)

> 근거: H 열화가 iteration 100에 이미 1.4–1.8배(§0). **긴 학습 1회 < 짧은 학습 여러 회.**
> 카드당 2 프로세스는 처리량이 1.5배뿐이라, 카드당 1개로 **지연시간을 반으로** 줄이는 쪽이
> 라운드를 자주 도는 데 유리하다. 라운드마다 **2 arm 동시**, 라운드 간 판단.

**공통 전제 (전 라운드)**: 가중치 `E0@model_6200` (7-7). 하네스는 L2의 계측 수정 8건 반영.
arm당 대조군 대비 **1레버** (L1). path OFF (7-5). 대칭 항목 제외 (7-2).

```
R0 ─ 재현 확인 ─────────────────────────────────────────  2 arm, ~2h
 │   I0a = E0 config + 신규 하네스 (레버 0개)
 │   I0b = I0a + feet_edge_pos 실측 교정 (7-14)
 │
 ├─ I0a가 E0(2.72cm/낙상2)를 재현 못함 → ■ STOP. 하네스가 원인. R0 재설계
 └─ 재현함 ─────────────────────────────────────────────
     │  ※ I0b가 나빠져도 정상. sim이 실제만큼 어려워진 것. 이후 전부 I0b 기준.
     ▼
R1 ─ standing (P1: 낙상 0) ────────────────────────────  2 arm, ~4h
 │   I1a = I0b + base_height_target 0.55        (7-10)
 │   I1b = I0b + base_height_target 0.58        (7-10)
 │   판정: 단일지지 생존시간 · 낙상률 · 위치 비열세
 │
 ├─ 둘 다 무개선 → 높이는 원인 아님. R2로 (7-12 단일지지 학습이 본진)
 └─ 개선 → 이긴 값 채택 ────────────────────────────────
     ▼
R2 ─ 단일지지 학습 ────────────────────────────────────  2 arm, ~4h
 │   I2a = R1승자 + stand 절반을 0.5–0.8Hz 체중이동으로  (7-12)
 │   I2b = R1승자 + 발 하중분배 보상(reward-only privileged) (7-19)
 │   판정: 제자리 단일지지 생존시간, 낙상 0 접근도
 │
 └─ ▼ (둘 다 좋으면 다음 라운드에서 합침 — bundle임을 명시)
R3 ─ 캘리브레이션 강건성 ───────────────────────────────  2 arm, ~4h
 │   I3a = R2승자 + calibration_dr (IMU회전 + encoder bias + target offset) (7-18)
 │   I3b = R2승자 + latency DR 20–60ms + 지터        (7-15)
 │   판정: held-out offset/지연에서의 열화폭. clean 비열세 필수 (L4)
 │
 └─ ▼
R4 ─ 속도 (P2: 1.3–1.5 m/s) ───────────────────────────  2 arm, ~4h
 │   I4a = R3승자 + 케이던스–속도 커플링             (3-2, 7-8)
 │   I4b = R3승자 + PD 범위 확대 ±25% (+ 발목 Kp 상향) (7-16)
 │   판정: 구간최고속도 p90, 추종비 단조성, 낙상 비열세
 │
 └─ ▼
R5 ─ goal 불확실성 (P6) ───────────────────────────────  2 arm, ~4h
     I5a = R4승자 + reprojection noise 30–50cm (bias만)  (7-4)
     I5b = R4승자 + reprojection noise 30–50cm (jitter만) (7-4)
     ※ L4: 속도 유지 게이트 동반. 큰 지터·path는 평가 전용.
```

### 비고 (다시 볼 때 필요한 것)

| 항목 | 값 / 위치 |
|---|---|
| 가중치 출발점 | `logs/K1/K1/Goal_Pose_V7/2026-07-26-19-36-15_E0_armB_armsdown/nn/model_6200.pth` |
| E0 기준 수치 | 위치 2.72/5.01 cm · heading 2.52° · strict 89.3% · 낙상 2 / 4633구간 |
| 채택 게이트 | P1(낙상 0) · P2(1.3–1.5 m/s). 나머지는 **비열세**만 |
| 라운드당 iteration | **200** (H 열화가 100에 이미 확정적) |
| 중단 규칙 | iteration 100에서 위치 median이 출발 대비 1.3배 초과 → 즉시 STOP |
| asset | **A 스크린 결과 대기** (7-3). 결과 전에는 `armsdown` |
| 평가 | 전 arm 동일 held-out 시험지. config SHA 불일치 시 비교 거부 (L6) |
| 제외 항목 | 좌우대칭(7-2) · path 학습(7-5) · 안정화 우선(7-6) · 배포 게이팅(7-11) |
| 계측만 추가 | 전환 지표(7-6), 단일지지 생존시간, 발 하중분배 |

### 라운드별 예상 비용

| | arm | iteration | 카드 | 벽시계 |
|---|---:|---:|---:|---|
| R0 | 2 | 200 | 2 (각 1개) | ~2h (재현 확인만) |
| R1–R5 | 각 2 | 200 | 2 (각 1개) | 각 ~4h |
| **합계** | **12** | — | — | **~22h** |

H 한 배치(4 arm × 12000 iter)가 약 4 GPU-일이었다. **같은 예산으로 6라운드를 돈다.**

---

## 8. R0 구현 (2026-08-03)

### 8-1. 무엇을 만들었나 — 신규 파이프라인 없음

기존 `make_v7_arms.py` + `tonight.sh`를 그대로 쓴다. **새 도구를 만들지 않았다** —
L2(측정 도구가 결과보다 먼저 검증돼야 한다)에 따라, 검증된 하네스를 두고 새 하네스를
만드는 것이 이 프로젝트가 반복한 실수다.

`tools/make_v7_arms.py` 변경 (4곳, 전부 추가):

```python
FOOT_EDGE_REAL = [[ 0.094,  0.035, -0.024], [ 0.094, -0.035, -0.024],
                  [-0.066,  0.035, -0.024], [-0.066, -0.035, -0.024]]

I_ARMS = {
    "I0a_repro": dict(**_OFF_PATH, **_OFF_ROBUST, **_OFF_PROTECT),
    "I0b_foot":  dict(**_OFF_PATH, **_OFF_ROBUST, **_OFF_PROTECT,
                      **{"asset.feet_edge_pos": FOOT_EDGE_REAL}),
}
ARMS_ON_E0 = {"I0a_repro", "I0b_foot", ...}       # -> ARMSDOWN_CKPT = E0@6200
ALL_ARMS   = dict(**ARMS, **F_ARMS, **I_ARMS, **V8_ARMS)
GPU_OF     = {..., "I0a_repro": "cuda:0", "I0b_foot": "cuda:1"}   # 카드당 1개
```

`FOOT_EDGE_REAL` 유도 — `left_foot_link` collision `box 0.16 0.07 0.032 @ (0.014, 0, −0.008)`:

| 축 | 계산 | 결과 |
|---|---|---|
| x | 0.014 ± 0.080 | **−0.066 … +0.094** (기존 −0.1015 … +0.1215) |
| y | 0 ± 0.035 | **±0.035** (기존 ±0.050) |
| z (바닥) | −0.008 − 0.016 | **−0.024** (기존 −0.030) |

### 8-2. 실행

```bash
cd ~/RoboCup/k1-goalpose && git pull && cd htwk-gym && conda activate k1goalpose && ARMS="I0a_repro I0b_foot" ITERS=200 SESSION=i bash tools/tonight.sh
```

`tonight.sh`가 이미 하는 것: `check_names` → arm별 config 생성 → **arm별 smoke 게이트**
→ 통과 arm만 tmux 실행 → 120 s 기동 확인 → 실패 arm 마지막 출력 덤프.
`CKPT` 기본값이 이미 `E0@model_6200`이라 롤백(§7-7)이 자동으로 적용된다.

### 8-3. 판정 — 사전 고정

| 결과 | 해석 | 다음 |
|---|---|---|
| **I0a가 E0(2.72 cm / 낙상 2)를 재현** | 하네스 건전 | R1 진행 |
| I0a가 재현 실패 | **하네스가 원인.** L2의 8건 수정 중 무언가가 과제를 바꿨다 | ■ STOP. R0 재설계 |
| I0b < I0a (나빠짐) | **정상이자 목적.** sim이 실제만큼 어려워졌다 | I0b를 이후 전 라운드 기준선으로 |
| I0b ≈ I0a | 발 크기가 성능을 좌우하지 않았다 | 그래도 I0b 채택(실물 정합) |
| 둘 다 200 iteration 내 위치 median 1.3배 초과 | 중단 규칙 발동 | ■ STOP |

**I0b가 나빠지는 것을 실패로 세지 않는다.** 이 arm의 목적은 성능이 아니라
**기준선을 실물에 맞추는 것**이다. 이걸 명시해 두지 않으면 다음에 볼 때
"I0b 실패"로 오독하고 되돌릴 위험이 있다.

### 8-4. 이 라운드에서 의도적으로 하지 않은 것

| 항목 | 이유 |
|---|---|
| `base_height_target` 변경 | R1의 단일 레버 (§4-1 답변) |
| 팔 asset 변경 | A 스크린 결과 대기 (P5 답변) |
| 단일지지·하중분배·캘리브레이션 DR·케이던스 | R2–R4 |
| path·큰 지터 | 학습 제외 확정 (P6·P7 답변) |
| 좌우대칭 | 제외 확정 (P4 답변) |

### 8-5. R0 확정 (2026-08-03 재설계) — 4 arm, 2 카드, ~26분

카드는 **2장**이다(`Device count 2`). 200 iteration이면 arm 하나가 ~13분이라
카드당 2개로 돌려도 총 **~26분**이고, "카드당 하나" 규칙이 지키려던 것은
처리량이 아니라 **회차 지연시간**이므로 이 길이에서는 2개씩이 맞다.

| arm | I0a 대비 유일한 차이 | 카드 | 묻는 것 |
|---|---|---|---|
| **I0a_repro** | (대조군) | 0 | 하네스가 E0를 재현하나 |
| **I0b_foot** | `feet_edge_pos` 실측 교정 | 0 | 진짜 발 크기에서 얼마나 어려워지나 |
| **I0c_h055** | `base_height_target` 0.52 → 0.55 | 1 | 어느 높이가 맞나 |
| **I0d_h058** | `base_height_target` 0.52 → 0.58 | 1 | 〃 (실기에서 잘 섰다는 값) |

**전부 `I0a` 대비 단일 레버**다(L1 준수). 같은 카드에 붙인 쌍이 곧 핵심 비교쌍이라
열·클럭 조건이 동일하다.

#### `base_height_target`은 하드코딩인가 — 아니다

사용자 원칙("결과를 보상하지 말고 조건을 만들어라")에 걸리는지 검토했다. **안 걸린다.**
이 항은 **이미 scale −20으로 존재**하고, 문제는 항의 존재가 아니라 **상수가 틀렸다**는 것이다:

```yaml
init_state.pos:      [0, 0, 0.58]   # 스폰 높이
rewards.base_height_target: 0.52    # 보상이 끌어내리는 높이
```

**6 cm 차이로 config가 자기모순**이다. 실기에서 넘어지기 전에 주저앉고 0.58에서 잘 선다는
관찰이 이와 일치한다. 즉 **새 shaping 항을 추가하는 것이 아니라 틀린 상수를 재는 것**이고,
`feet_edge_pos` 교정과 같은 부류다.

#### 실시간 판정 — `tools/watch_eval.py`

checkpoint가 나올 때마다 20 s × 256 env로 채점하고, **학습 전에 고정한 판정**을 넘기면
`STOP` 파일을 쓴다. `runner_v3.py`가 이미 `STOP`을 읽어 checkpoint 저장 후 정상 종료한다.

| 규칙 | 값 | 근거 |
|---|---|---|
| 기준 위치 median | **0.0272 m** | E0@6200 실측 |
| 기준 낙상 | **2** | 〃 |
| 중단: 위치 | `> 1.3 × 기준` | H가 iteration 100에 1.4~1.8배였다 |
| 중단: 낙상 | `> max(3×기준, 기준+5)` | 낙상은 P1의 유일한 미통과 게이트 |
| 평가 실패 시 | **절대 중단 안 함** | 조용한 실패로 정상 run을 죽이지 않는다 |

#### 판정표 — 실행 전 확정

| 관측 | 결론 | 다음 |
|---|---|---|
| I0a가 2.72 cm / 낙상 2 재현 | 하네스 건전 | R1 진행 |
| I0a가 STOP에 걸림 | **하네스가 원인**(L2 수정 8건 중 무언가가 과제를 바꿈) | ■ 중단, R0 재설계 |
| I0b < I0a | **정상이자 목적** — sim이 실물만큼 어려워짐 | 이후 전 라운드 기준선 |
| I0c/I0d 중 하나가 I0a 우위 | 상수가 틀렸던 것 확정 | 그 값 채택 |
| I0c·I0d 둘 다 무개선 | 높이는 원인이 아님 | standing은 조건(외란)으로만 유도 |


---

## 8-6. R1 1차 — 중단 규칙이 과도해 3/4를 죽였다 (2026-08-03 21:0x)

| arm | 결과 |
|---|---|
| **I1b_force** | 200/200 완주. **model_175에서 낙상 0** — 위치 2.60 / p90 4.72 / hd 2.1°. **네 게이트 동시 통과 최초** |
| I1d_both | iter 125에서 STOP. 위치 **2.26**(최고) / p90 4.36 / 낙상 8 / strict 94.7% |
| I1a_base (대조) | iter ~109에서 STOP |
| I1c_cadence | iter ~95에서 STOP |

**셋을 죽인 것은 내 중단 규칙이다.** `STOP: falls 8 > 3.0x reference 2`.

두 가지가 겹쳤다:

1. **기준이 틀렸다.** `tonight.sh` 기본값 `REF_FALLS=2`(E0)가 쓰였다. 발 교정 후의 정직한
   기준은 **I0c의 4**다. → 기본값을 `0.0271 / 4`로 교정.
2. **과도구간을 죽였다.** warm start 정책이 새 조건(외란·케이던스·새 발)에 재적응하는
   동안 일시적으로 나빠진다. **`I1b`가 그 증거다: 낙상 3(iter 125) → 2(150) → 0(175).**
   `I1a`/`I1c`/`I1d`는 전부 **iter 50의 낙상 8~10 한 번**으로 잘렸다.
   I1b도 초기에 걸렸으면 낙상 0에 도달하지 못했다.

**규칙 수정 3건:**

| 항목 | 이전 | 이후 | 이유 |
|---|---|---|---|
| 판정 대상 | 낙상 **개수** | 낙상 **비율**(`fall_rate_per_attempt`) | 스크리닝이 arm마다 731~743구간을 채점한다. 개수 비교는 분모가 다른 것을 비교한다 |
| 발동 시점 | 첫 checkpoint부터 | **iter ≥ 100** | 재적응 딥을 보호 |
| 발동 조건 | 1회 위반 | **2회 연속** | 20 s 표본의 단발 요동으로 죽이지 않는다 |

검증: 이 규칙에서 `I1a@50`(낙상 10/730)은 **통과**하고, 진짜 붕괴(위치 12 cm)는 **잡는다**.

> **교훈**: 조기중단은 GPU를 아끼지만, **기준이 틀리면 아끼는 게 아니라 답을 버린다.**
> H는 중단이 없어서 4 GPU-일을 태웠고, R1은 중단이 과해서 대조군을 잃었다.
> 규칙은 "학습 전에 고정"하는 것만으로는 부족하고 **무엇에 대한 기준인지**가 맞아야 한다.

---

## 8-7. R1 판정 — 그리고 낙상 지표가 재현되지 않는다는 발견 (2026-08-03 22:3x)

### 2×2 완성 (20 s 스크리닝, checkpoint 125~200 평균)

| arm | 외란 | 케이던스 | pos med | p90 | 낙상 | strict |
|---|---|---|---:|---:|---:|---:|
| I1a_base | ✗ | ✗ | 3.01 | 5.54 | **9.75** | 84.7 |
| **I1b_force** | ✓ | ✗ | 2.64 | 4.82 | **1.50** | 91.2 |
| I1c_cadence | ✗ | ✓ | 2.47 | 4.55 | 6.25 | 92.4 |
| I1d_both | ✓ | ✓ | **2.36** | **4.47** | 5.50 | 92.5 |

- **외란 → 낙상**: 9.75 → 1.50 (−85%). **자세 보상 없이 낙상만 잡았다.**
- **케이던스 → 정확도**: 3.01 → 2.47 cm (−18%), p90 5.54 → 4.55.
- **상호작용이 나쁘다**: 케이던스를 켜면 외란의 낙상 이득이 −85% → −12%로 무너진다.

### I1b@175 전체 프로토콜 (120 s, 4632 구간)

| 게이트 | 기준 | I1b@175 | E0@6200 |
|---|---|---|---|
| 위치 median | ≤5 cm | **2.6** ✅ | 2.72 ✅ |
| 위치 p90 | ≤10 cm | **4.7** ✅ | 5.01 ✅ |
| heading median | ≤10° | **2.0** ✅ | 2.52 ✅ |
| 낙상 | 0 | **4** ❌ | 2 ❌ |
| strict 성공률 | — | **92.4%** | 89.29% |

정확도 3축 전부 E0를 넘었다. 낙상만 2 → 4로 늘었는데 — **그 비교가 성립하지 않는다.**

### ⛔ 낙상 수는 단일 run으로 재현되지 않는다

`I1c_cadence/model_95`를 **동일 프로토콜로 두 번** 평가한 결과가 남아 있다:

| run | seed | envs | duration | env SHA | 위치 median | **낙상** |
|---|---|---|---|---|---|---|
| `select_.../` | 0 | 256 | 120 s | `72a87cbc` | 2.64 cm | **3** |
| `select_.../winner_video/` | 0 | 256 | 120 s | `72a87cbc` | 2.66 cm | **18** |

**같은 checkpoint·같은 seed·같은 config.** 차이는 영상 녹화 여부뿐이다.
위치는 0.8% 안에서 일치하는데 **낙상은 6배**다. 공통 Poisson율로는 두 값이 함께
나올 확률이 사실상 0이므로 표본 노이즈가 아니라 **시뮬레이션 비결정성**이다
(GPU 파이프라인 + 렌더링 부하가 부동소수 경로를 바꾼다).

**여파:**
- E0의 낙상 2와 I1b의 4는 **구분 불가**다
- 위 2×2의 낙상 열(9.75 / 1.50 / 6.25 / 5.50)은 **제시한 것보다 훨씬 약하다**.
  다만 checkpoint 4개 평균이라 단일 run보다는 낫고, 9.75 vs 1.50의 6.5배 차이는
  관측된 재현 변동(6배)과 같은 크기라 **주효과의 방향만 신뢰하고 크기는 신뢰하지 않는다**
- 낙상 0 게이트는 **단일 run으로 판정할 수 없다**

**조치**: `tools/accept_eval.py` 신설 — 같은 checkpoint를 N seed로 돌리고 각 지표를
**중앙값과 범위**로 보고한다. 상대폭이 50%를 넘으면 "단일 run으로 판정 불가"를 찍는다.
위치·heading은 지루하게 안정적으로 나올 것이고, **그 대비가 이 도구의 요점이다.**

### 이 라운드에서 답하지 못한 것

- **외란 강건성은 여전히 미측정.** I1b 리포트가 명시한다: `외력 event 0회 —
  이 report는 충돌 강건성 근거로 사용할 수 없다`. 외란으로 **학습**한 정책을
  외란 **없이** 채점했다. E2·G2 때와 같은 공백이다
- **속도도 미측정.** 이 과제는 필요속도 p90이 **0.31 m/s**이고 시간 내 도달 불가가
  **0.0%**다. 속도를 요구하지 않는 과제에서 속도를 잴 수 없다(§3-2와 같은 구조)

---

## 8-8. R1 최종 — 2×2 완성 (2026-08-03 23:xx)

전 arm 200 iter 완주, checkpoint 7개씩. **워밍업 이후(it≥100) 5개 평균:**

| arm | 외란 | 케이던스 | pos cm | p90 cm | **낙상** | strict% |
|---|---|---|---:|---:|---:|---:|
| I1a_base | ✗ | ✗ | 3.01 | 5.54 | **9.60** | 84.6 |
| **I1b_force** | ✓ | ✗ | 2.71 | 4.97 | **2.20** | 89.8 |
| I1c_cadence | ✗ | ✓ | 2.52 | 4.66 | 7.20 | 91.7 |
| I1d_both | ✓ | ✓ | **2.44** | **4.51** | 5.80 | **92.5** |

### 주효과와 상호작용

**낙상 (arm의 유일한 미통과 게이트):**

| | 케이던스 OFF | 케이던스 ON |
|---|---|---|
| **외란 효과** | 9.60 → 2.20 (**−7.40**) | 7.20 → 5.80 (−1.40) |
| **케이던스 효과** | 9.60 → 7.20 (−2.40) | 2.20 → **5.80 (+3.60 악화)** |

**외란 단독이 낙상을 77% 줄인다.** 그런데 **케이던스를 얹으면 그 이득의 81%가 사라진다.**
케이던스 자체는 외란 없을 때 낙상을 약간 줄이지만(−2.40), 외란과 함께면 **거꾸로 늘린다**.
같은 자원(gait 타이밍)을 두 레버가 반대 방향으로 쓴다.

**위치**: 케이던스가 −16%(외란 OFF) / −10%(외란 ON). 두 레버 모두 정확도는 올리고
부분가산적이다.

### 낙상 차이가 노이즈가 아닌 근거

단일 run의 낙상은 재현되지 않는다(§8-7). 그래서 **5 checkpoint를 누적**한다:

| arm | 누적 낙상 / 약 3650 시도 | Poisson 오차 |
|---|---|---|
| I1a_base | **48** | ±6.9 |
| I1c_cadence | 36 | ±6.0 |
| I1d_both | 29 | ±5.4 |
| **I1b_force** | **11** | ±3.3 |

**48 ± 6.9 와 11 ± 3.3은 겹치지 않는다.** 단일 관측이 아니라 누적이므로 이 차이는 실재한다.

**추세는 더 강한 증거다:**

| arm | it 100 → 200 |
|---|---|
| I1a_base | 9 → 8 → 14 → 7 → 10 (평평·noisy) |
| **I1b_force** | **5 → 3 → 2 → 0 → 1 (단조 감소)** |
| I1c_cadence | 11 → 8 → 8 → 5 → 4 (감소) |
| I1d_both | 7 → 8 → 3 → 6 → 5 (평평·noisy) |

**I1b만 0으로 단조 수렴한다.** 단일 지점의 0이 아니라 궤적이다.

### 판정

- **외란은 채택.** 자세 보상 없이 낙상을 77% 줄였고 누적·추세 양쪽에서 확인된다.
  **사용자의 "결과가 아니라 조건을 만들라" 원칙이 데이터로 검증된 첫 사례다.**
- **케이던스는 보류.** 정확도는 확실히 올리지만(−16%) **외란과 함께 쓰면 낙상을 되돌린다.**
  낙상이 유일한 미통과 게이트이므로 정확도와 바꿀 수 없다.
- **I1d(둘 다)는 채택하지 않는다.** 위치·strict가 최고지만 낙상이 I1b의 2.6배다.

### 아직 남은 것

이 전부가 **외란 OFF 평가**다. "외란으로 학습했더니 clean에서 안 넘어진다"까지가
증거이고, **"외력을 맞으면 버티는가"는 여전히 미측정**이다. `force_ab.py`가 진행 중이며
그것이 R1의 원래 질문에 답한다.

---

## 8-9. 외력 ON 시험 — 확정 (2026-08-04)

공통 held-out 시험지: 간격 4–8 s, 충돌 50–120 N, 지지 4–10 N.
**세 arm 모두 force event 약 5,100회** — 외력이 실제로 전달됐고 감사가 그것을 확인한다.
seed 0/1/2, 각 256 env × 120 s.

| arm | 위치 median cm | **낙상** | 낙상/1000 | force events |
|---|---|---:|---:|---:|
| I1a_base (대조) | 3.03 [2.97, 3.08] | **17 [14, 20]** | 3.67 | ~5,100 |
| **I1b_force** | **2.57 [2.51, 2.60]** | **7 [5, 10]** | **1.51** | ~5,100 |

**범위가 겹치지 않는다.** 낙상이 재현되지 않는다는 것을 알고(§8-7) seed 3개로 설계한 것이
여기서 값을 했다. 누적으로 보면 더 분명하다:

| | 누적 낙상 / 약 13,900 시도 | Poisson 구간 |
|---|---|---|
| I1a_base | **51** | 43.9 ~ 58.1 |
| **I1b_force** | **22** | 17.3 ~ 26.7 |

**2.32배, 낙상 57% 감소.** 구간이 겹치지 않는다.

### 판정 — 세 번 묻고 세 번 못 답한 질문에 답이 나왔다

> **외란으로 학습하면 실제 외력에 강해지는가, 아니면 그냥 느려질 뿐인가?**

**강해진다. 그리고 아무것도 지불하지 않았다.**

| | I1a_base | I1b_force |
|---|---|---|
| 외력 하 낙상 | 17 | **7** (−57%) |
| 외력 하 위치 | 3.03 cm | **2.57 cm** (−15%) |

**위치가 오히려 더 좋다.** E2·G2에서 두 번 재현됐던 L4("강건성 레버는 항상 덜 움직이기로
도망친다")가 **이번에는 나타나지 않았다.** 차이는 §8-8의 저용량 설계다 — 접촉 렌치만
켜고 goal jitter·bias·flicker·staleness는 전부 껐다. **L4는 강건성의 법칙이 아니라
7-레버 묶음의 증상이었다.**

> **사용자 원칙("결과를 보상하지 말고 그 결과를 필연으로 만드는 조건을 만들라")이
> 대조군을 두고, 홀드아웃 시험지에서, 재현 가능한 형태로 검증됐다.**
> 자세 보상은 한 줄도 넣지 않았다.

### ⚠️ 하지 말아야 할 비교

clean(20 s 스크리닝)과 force(120 s)는 **지속시간이 달라 낙상률을 직접 비교할 수 없다**:

| | I1a | I1b |
|---|---|---|
| clean 20 s | 13.0 /1000 | 3.0 /1000 |
| force 120 s | 3.66 /1000 | 1.51 /1000 |

force 쪽이 오히려 낮게 나온다. 외력이 쉬워서가 아니라 **20초 창은 리셋 직후 과도구간
비중이 커서 낙상률을 과대평가**하기 때문이다. 초안에서 "I1b가 맞으면서 낸 낙상이 I1a가
clean에서 낸 것보다 적다"고 쓸 뻔했는데 **그 비교는 성립하지 않는다.**

같은 120 s 안에서 비교 가능한 유일한 쌍:

| | 낙상/1000 |
|---|---|
| I1b clean 120 s | 0.86 |
| I1b force 120 s | 1.51 (**+75%**) |

**I1a의 clean 120 s가 없다.** `--skip-clean`으로 돌려서다. 이것 하나(3 seed, ~42분)면
(arm × force) 2×2가 닫히고 **"외란 학습의 clean 비용"**까지 정량화된다.

---

## 8-10. R 트리 개정 (2026-08-03)

§7의 원안(R0 재현 → R1 높이 → R2 단일지지 → R3 캘리브 → R4 속도 → R5 goal)에서
실제 진행이 갈라졌다. 사후 합리화가 아니라 **왜 갈라졌는지**를 기록한다.

| 라운드 | 원안 | 실제 |
|---|---|---|
| R0 | 재현 확인 | ✅ + 발 교정 + 높이 0.55까지 흡수 (R1 내용 선취) |
| R1 | standing(높이) | 2×2로 대체 → **외란 채택**(낙상 −77%), 케이던스 보류 |

**원래 R2(단일지지 전용 학습)는 폐기한다.** 그 목적이 "낙상을 줄인다"였는데
**외란 하나로 77%가 줄었다.** 전용 arm의 근거가 사라졌다.

### 트리에 없던 자리에 들어가는 발견 둘

| 발견 | 성격 |
|---|---|
| `base_com ±0.1 m` 과설정 | **결함 교정** — 가설이 아니다 |
| `feet_swing`이 1 cm까지만 보상 | **조건화로 해결** — 지형 요철 |

`base_com` 정량 (URDF 실측):

| | 값 |
|---|---|
| Trunk 질량 | 6.392 kg (전체 18.714의 34%) |
| Trunk 충돌박스 반폭 | x ±0.060 m, y ±0.090 m |
| **현재 랜덤화** | **x·y·z 각각 ±0.100 m** |
| 발 x 범위 | −0.066 ~ +0.094 m |

→ **CoM이 몸통 밖으로 나가고**(x 1.7배), **지지면 밖으로도 나간다**(발 반길이의 1.1배).
정적으로 균형이 불가능한 로봇을 만들어 놓고 안 넘어지길 요구해 왔다.
실제 CoM 불확실성은 1–3 cm다. **`feet_edge_pos`와 같은 부류의 결함**이다.

> **이건 강건성 거래가 아니라 허구의 제거다.** L4("넓은 DR → 보수적")를 근거로
> "좁히면 약해진다"고 반박할 수 없다 — 없는 조건에 대비하느라 보수적이었던 것이라면
> 좁히는 것이 곧 성능 회복이다. 다만 **실측이 없으므로 레버로 검증한다.**

### 개정 트리

```
R1 완료 ─ 외란 채택
   │
   ├─ force_ab (진행 중) ── 외란이 실제 외력에 강한가          [R1 검증]
   ▼
R2  I2a_dr       DR을 현실에 맞춤 (CoM ±0.025 / 질량 ±8% / PD ±15%)   [결함 교정]
    I2b_terrain  지형 요철(trimesh) → 발 클리어런스                    [조건화]
   ▼
R3  캘리브레이션 DR (IMU 회전 + encoder bias + target offset)
    지연 DR 20–60 ms + 지터                                    [잔진동 ②]
   ▼
R4  속도 — 케이던스 재시도 (R2에서 DR을 고친 뒤)
   ▼
R5  goal 불확실성 (reprojection 30–50 cm)
```

**폐기**: 원래 R2(단일지지 전용) — 외란이 이미 −77%를 냈다
**보류**: 케이던스 — 외란과 충돌(§8-8). R2 이후 R4에서 재시도

### 왜 DR 교정이 속도보다 앞서나

케이던스가 외란과 충돌한 이유 중 하나가 **"정책이 ±10 cm CoM 변동까지 감당하느라
보수적"**일 수 있다. DR을 현실화하면 그 충돌 자체가 사라질 가능성이 있고, 그러면
케이던스를 다시 켤 수 있다. **R2는 R4의 전제조건이다** — 지금 케이던스를 재시도하면
같은 결과가 나온다.

**R2는 force_ab를 기다리지 않아도 된다.** 두 arm 모두 외란 레버를 R1 승자 그대로
켜둔 채 config만 바꾸므로 독립이다.

---

## 8-11. Bird's-eye — 목표 대비 현재 위치와 다음 수 (2026-08-04)

### 어디까지 왔나

| 항목 | 기준 | 현재 (I1b_force@175, 120 s) | |
|---|---|---|---|
| P1 위치 median | ≤5 cm | **2.6 cm** | ✅ |
| P1 위치 p90 | ≤10 cm | **4.7 cm** | ✅ |
| P1 heading | ≤10° | **2.0°** | ✅ |
| **P1 낙상** | **0** | **4 (clean) / 7 (외력)** | ❌ |
| P2 몸통속도 | 1.3~1.5 m/s | 구간최고 p90 1.29, **>1 m/s 체류 3.1%** | ❌ |
| 7-A 정확+빠름 동시 | — | 정확 ✅ / 빠름 **미측정** | ❌ |
| 7-B 움직이는 목표 | — | 학습에서 제외 | ❌ |
| 7-C 불안정 시 안정화 우선 | — | 미구현 | ❌ |

**정확도 3축은 E0를 넘었고 외력 강건성도 확보했다.** 남은 것은 낙상 0과, **한 번도 측정된 적
없는 속도**다.

### ⛔ 지금 과제는 P2를 요구할 수 없다 — 기하학적으로

```
goal_dx [-2, 2],  goal_dy [-1.5, 1.5],  resampling_time_s [4, 8]
가장 먼 목표 = √(2² + 1.5²) = 2.50 m,  최단 구간 = 4 s
→ 이 과제가 요구할 수 있는 최대 속도 = 0.62 m/s
```

P2는 **1.3~1.5 m/s**다. 실측 필요속도는 median 0.11 / **p90 0.31 m/s**이고
**시간 내 도달 불가 0.0%**다. 정책이 빨리 갈 이유가 과제에 **존재한 적이 없다.**

`masterplan2`가 E 배치에서 지적한 결함(*"요구속도 최댓값 0.574 m/s"*)이 그대로 돌아왔다.
그때의 해법이 path였고, 우리는 그 path를 학습에서 뺐다(§P7). **속도 요구를 만들던
유일한 장치를 제거한 것이다.**

### P1과 P2는 충돌하지 않는다 — 초안의 오판을 정정한다

`goal_reached`는 `dist<0.1 AND |v|<0.1`에서 `+1/step`을 준다. **일찍 도착할수록 공 안에서
더 오래 받는다.** 즉 보상 구조는 이미 속도를 장려한다. 문제는 **거리가 짧아 gradient가
약한 것**이다:

| 거리 | 구간 | 0.5 m/s | 1.4 m/s | 속도의 한계이득 |
|---|---|---|---|---|
| 1.0 m | 6 s | 정지 4.0 s | 5.3 s | +65 step |
| 2.5 m | 6 s | 1.0 s | 4.2 s | +160 step |
| **4.0 m** | 6 s | **도달 실패** | 3.1 s | **0 → 155 step** |

거리가 멀어지면 속도의 보상이 급격히 커진다. **보상을 고칠 필요가 없다. 과제 분포만
고치면 된다** — 사용자 원칙 그대로다.

### 속도 요구를 만드는 세 가지 안 (수치 비교)

이동 구간의 요구속도 분포(30,000 표본 시뮬):

| 안 | median | **p90** | p99 | max |
|---|---:|---:|---:|---:|
| 현재 (dx±2, T 4–8 s) | 0.05 | 0.23 | 0.43 | **0.62** |
| 거리 2배 (dx±4, T 4–8 s) | 0.10 | 0.45 | 0.84 | 1.24 |
| 마감시간 = 거리/목표속도 + 1.5 s | 0.19 | 0.55 | 0.79 | 0.96 |
| **sprint 카테고리 15%** (거리 2.5–4 m, 목표 1.2–1.6 m/s) | 0.06 | **1.34** | 1.57 | — |
| sprint 20% | 0.07 | **1.40** | 1.58 | — |

**sprint 카테고리 15~20%만이 p90을 1.3~1.5에 올린다.** 그리고 나머지 80%는 현재 과제
그대로이므로 **P1의 분포가 거의 안 변한다** — path가 청구했던 세 대가(§L5)를 피한다.

> 이건 path의 재도입이 아니다. 캐럿·leash·dwell·grid 기계가 전혀 없고,
> **"먼 목표 + 짧은 마감"**이라는 과제 조건 하나뿐이다.

### 개정된 다음 수

```
지금 (병렬, 코드 변경 0)
 ├─ I1a clean 120s × 3 seed  ── (arm × 외력) 2×2를 닫는다        [~42분]
 └─ R2  I2a_dr / I2b_terrain ── DR 과설정 교정 + 지형 요철        [~25분]
        ▼
R3  캘리브레이션 DR(IMU 회전 + encoder/target offset) + 지연 20–60 ms   [잔진동 ②]
        ▼
R4  속도 — sprint 카테고리 15%  ◀── 재설계됨. 케이던스는 여기서 재시도
        ▼
R5  goal 불확실성 (reprojection 30–50 cm)
```

**R4가 바뀌었다.** 원안은 "케이던스 재시도"였는데, **케이던스는 속도를 낼 수단이지
속도를 요구하는 장치가 아니다.** 요구가 없으면 수단을 고쳐도 측정할 수 없다.
sprint 카테고리로 요구를 먼저 만들고, 그 위에서 케이던스를 켜야 §8-8의 충돌 여부를
비로소 판정할 수 있다.

### 낙상 0 — 남은 P1 게이트

| | clean 120 s 낙상 | 낙상/1000 |
|---|---|---|
| E0@6200 | 2 | 0.43 |
| I1b@175 | 4 | 0.86 |

**둘은 구분되지 않는다**(§8-7: 같은 checkpoint가 3과 18을 냈다). 즉 **clean 낙상은 E0
이후 개선된 적이 없다.** 개선된 것은 정확도와 **외력 하 강건성**이다.

낙상 0을 주장하려면 `accept_eval.py`로 **다중 seed에서 최악 seed도 0**이어야 한다.
지금 데이터로는 그 주장을 할 수 없고, 할 수 있는 주장은 *"외력 하에서 대조군의 41%"*다.

> **2026-08-04 폐기.** 이 절의 "낙상 0" 게이트는 §8-12에서 철회했다. 아래 §8-12가
> 최신이며, 이 절은 그 판단이 어디서 왔는지를 남기기 위해 지운다.

---

## 8-12. 낙상 0 게이트 폐기 + R2 판정 (2026-08-04 01:4x)

### 사용자 질문: "낙상 0에 집착하는 이유가 내 지시 때문인가?"

**아니다.** 사용자 요구는 축 C — *"불안정할 때 목표추종보다 안정화를 우선"* — 이고,
"낙상 0"이라는 숫자는 내가 `accept_eval.py:111`에 써넣은 것이다. 요구를 지표로 번역할
때 내가 **잘못 번역했다**: 평지·무외란 sim의 낙상 수는 축 C가 말하는 것을 재지 않는다.

### 왜 나쁜 게이트인가 — 세 가지, 전부 자체 측정으로 확인됨

**(1) 검정력이 없다.** 전체 프로토콜 4,632구간 기준 95% Poisson 구간:

| 관측 | 구간(회) | 비율 /1000 |
|---|---|---|
| 2 (E0@6200) | [0.24, 7.2] | [0.05, 1.56] |
| 4 (I1b@175) | [1.09, 10.2] | [0.24, 2.21] |

겹친다. 즉 **E0와 I1b는 clean 낙상으로 구분 불가**다 — 이미 §8-7에 쓴 것이다. 그렇다면
**같은 이유로 "0"도 판정 불가**다. 0회를 관측해도 상한은 3.69회, 곧 "≤0.80/1000"까지만
말할 수 있다. "≤0.10/1000"을 주장하려면 무낙상 36,900구간 = **전체 프로토콜 8회분**,
arm당 약 2 GPU-시간이 필요하고, 그렇게 얻는 것은 *평평한 sim 바닥*에 대한 숫자다.

**(2) 위 계산조차 낙관적이다.** §8-7에서 같은 checkpoint가 3회와 18회를 냈다. Poisson이면
[0.62, 8.8] vs [10.7, 28.5]로 **분리**되어야 하는데 같은 정책이다. 즉 낙상 수는
**Poisson보다 과분산(overdispersed)**이다. → **Poisson 구간을 신뢰구간으로 쓰면 안 된다.
seed를 여러 개 돌려 경험적 범위를 재는 것만이 유효하다.**

**(3) 실기와 연결이 약하다.** 무외란 평지에서 0.43/1000이냐 0.86/1000이냐는 경기장에서
K1이 넘어지느냐를 결정하지 않는다. 결정하는 것은 밀렸을 때·발이 걸렸을 때·지연이
튀었을 때 회복하느냐다.

### 대체 지표 — 외력 하 낙상 (이미 작동한 적이 있다)

§8-9의 held-out 외력 시험, 3 seed **경험적 범위**:

| arm | 낙상 (seed 범위) | 누적 |
|---|---|---|
| I1a_base | **17 [14, 20]** | 51 ± 7.1 |
| I1b_force | **7 [5, 10]** | 22 ± 4.7 |

**범위가 겹치지 않는다.** clean이 4,632구간을 쓰고도 못 가른 차이를, 같은 시간에 갈랐다.
사건 발생률이 10~20배라 검정력이 그만큼 높고, `force_ab.py`는 구조상 다중 seed를 돈다.

**개정된 지표 체계:**

| | 폐기 | 채택 |
|---|---|---|
| clean 평가 | 정확도 + 낙상 게이트 | **정확도 전용.** 낙상은 기록만, 순위 판단 금지 |
| 강건성 | clean 낙상 0 | **held-out 외력 낙상**, ≥3 seed 경험적 범위 |
| 게이트 | 낙상 0 | 정확도 ≤ 기준×1.1 **AND** 외력 낙상 범위가 I1b보다 위로 벗어나지 않을 것 |

새 지표가 사용자의 축 C에 더 가깝다. 폐기한 게이트는 축 C를 clean 낙상수로 오역한 것이었다.

### R2 데이터 (2026-08-04 01:03 시작, 200 iter, warm start model_6200)

`_I2_BASE = merge(_I1_BASE, _I1_FORCE)` — 두 arm 모두 R1 승자(발 + h055 + 외란) 위에 얹었다.

- **I2a_dr**: `base_com ±0.025 m`, `base_mass ×[0.92,1.08]`, `dof_stiffness/damping ×[0.85,1.15]`
  (상위팀 레시피 "DR 10%"와 같은 자릿수)
- **I2b_terrain**: `terrain.type: trimesh`

**20초 스크리닝, checkpoint 125/150/175/200 (~740구간 × 4 = ~2,960구간):**

| arm | pos med 평균 cm | strict % 평균 | 낙상 합 | 체크포인트별 낙상 |
|---|---:|---:|---:|---|
| **I2a_dr** | 2.72 | **91.5** | **4** | 1, 0, 2, 1 |
| I1b_force (R1 승자) | 2.64 | 91.2 | **6** | 3, 2, 0, 1 |
| I1d_both | **2.36** | 92.5 | 22 | 8, 3, 6, 5 |
| I1c_cadence | 2.47 | **92.4** | 25 | 8, 8, 5, 4 |
| **I2b_terrain** | 2.72 | **85.0** | 32 | 5, 9, 9, 9 |
| I1a_base (대조군) | 3.01 | 84.7 | 39 | 8, 14, 7, 10 |

### 판정

**I2a_dr — 채택.** 정확도 2.72 vs 2.64 (차이 없음), strict 91.5 vs 91.2 (동률),
낙상 4 vs 6 (동률). **sim2real 마진을 아무 대가 없이 얻었다.** DR은 공짜다.

**I2b_terrain — ⛔ 판정 철회 (2026-08-04 02:1x). 각자 자기 시험지로 채점했다.**

`watch_eval`과 `select_best_checkpoint`는 **각 arm의 자기 config**로 평가한다
(`watch_eval.py:164` = `<run>/config.yaml`). 그래서 **I2b_terrain은 trimesh 지형 위에서,
I1b·I2a는 평지 위에서** 채점됐다. strict 91.2 → 85.0은 *정책이 나빠진 것*일 수도,
*시험이 어려워진 것*일 수도 있고 **이 데이터로는 구분되지 않는다.**

`force_ab.py`를 만든 이유가 정확히 이 오류였는데(각 정책을 자기 외란 설정으로 채점),
지형에서 같은 실수를 반복했다. 외란만 공통 프로파일로 묶고 **바닥은 안 묶었다.**

오히려 주목할 것: **위치 median이 2.72로 동일하다.** ±10 cm 자갈밭에서 평지와 같은
정확도가 나왔다면 그건 실패가 아니라 놀라운 결과다. "지형은 거래다"는 근거 없는 서술이었다.

**수정:** `eval_goal_pose.py --terrain plane`을 추가했다. `--force_profile heldout`과 같은
원리 — 바닥은 시험의 일부지 정책의 일부가 아니다. 리포트에 `eval_terrain`으로 찍힌다.
I2b_terrain은 평지 재평가 전까지 **미판정**이다.

**부수 관찰: 외란 학습 = 낮은 낙상이 clean에서도 보인다.** 외란을 넣은 두 arm(I1b, I2a)이
4~6, 안 넣은 넷이 22~39다. 단 이 비교는 **checkpoint 4개를 묶은 것**이라 정책 4개에
대한 서술이지 checkpoint 하나에 대한 주장이 아니며, (2)의 과분산 때문에 확정하려면
`force_ab.py`로 seed를 돌려야 한다.

### `round_summary.py` 결함 2건째 — 분모 혼합

R2 요약표가 I2a/I2b는 **20초 스크리닝(~740구간)**, I1b는 **120초 전체(4,632구간)**
리포트를 한 표에 놓았다(선택이 아직 안 끝나 전체 리포트가 없었다). "낙상 1 vs 4"를 보고
I2a가 낫다고 판단했는데 **비율로는 1.35 vs 0.86/1000으로 반대**였다.

`monitor`의 stress 리포트 혼입, 직전의 force 리포트 혼입에 이어 **"리포트 종류/노출량을
구분하지 않고 최근 것을 집는다"가 세 번째**다. 이번엔 `구간` 열과 `/1000` 열을 추가하고,
행 간 노출량이 1.5배 이상 차이 나면 경고를 찍게 했다. `--ref-falls`는 인자만 남기고
무시한다 — 낙상 판정은 `force_ab.py`로 옮겼다.

### 8-12a. ⛔ 폐기한 낙상 게이트가 선택기 안에서 살아 있었다 (2026-08-04 02:2x)

I2a_dr의 선택기가 **`model_25`**를 뽑았다. warm start에서 25 스텝짜리다. selection.md는
*"학습 후반에 과제 성능이 나빠졌다"*고 경고했다. **그 경고가 틀렸다.**

`rank_key = (not gates_pass, worst_ratio, mean_ratio)` — lexicographic이고,
`gates_pass`는 `results["all_gates_pass"]`를 읽는데 거기에 **`max_falls: 0`이 들어 있다.**

단계 2 (60 s, 2,314~2,316구간) 실측을 그대로 재현했다:

| iter | pos med | p90 | heading | 낙상 | worst비 | 게이트 | **과제오차** |
|---:|---:|---:|---:|---:|---:|---|---:|
| 25 | 2.2 | 3.9 | 3.7° | 0 | **0.44** | ✅ | 6.8 cm |
| 50 | 2.9 | 5.2 | 3.2° | 0 | 0.58 | ✅ | 6.3 cm |
| **150** | 2.6 | 4.6 | **1.8°** | **1** | 0.52 | ❌ | **4.1 cm** |
| 200 | 2.6 | 4.6 | 2.6° | 1 | 0.52 | ❌ | 5.2 cm |
| 175 | 2.7 | 4.7 | 2.1° | 1 | 0.54 | ❌ | 4.6 cm |

재현한 worst비(0.44/0.58/0.52/0.52/0.54)가 **인쇄값(0.44/0.58/0.52/0.53/0.54)과 일치**하고,
재현한 순서 [25, 50, 150, 200, 175]가 **selection.md 인쇄 순서와 정확히 같다.** 기전 확정.

**두 결함이 겹쳤다.**

**(1) 낙상 1회가 상위 3개를 강등시켰다.** 2,314구간의 1회 = 0.43/1000, 95% Poisson 구간
[0.01, 2.4]/1000 — **0을 포함한다.** §8-12에서 "판정 불가"로 폐기한 바로 그 양이 선택을
결정했고, 그 결과 모든 지표가 나쁜 `model_50`(2.9 cm, 3.2°)이 `model_150`(2.6 cm, 1.8°)보다
위로 올라갔다.

**(2) worst_ratio는 사실상 위치 median 단독 순위다.** 한계가 위치 5 cm / heading 10°인데
후보 전원의 최악 게이트가 pos_median이었다(0.44~0.58 vs heading 0.18~0.37). 그래서 heading은
순위에 **거의 참여하지 못했다.**

**게이트와 보상이 환율에서 3.5배 어긋나 있다.** constellation은 `d² + 2r²(1−cosθ)`,
소각 근사로 `d² + r²θ²`, r = 1.0 m이므로 **1° ≡ 1.75 cm**다. 게이트는 10° ↔ 5 cm, 곧
**1° ≡ 0.5 cm**로 본다. 학습이 최소화한 것은 보상 쪽이다.

**그래서 학습은 나빠지지 않았다.** heading 3.7° → 1.8°는 위치 0.4 cm를 주고 산 것이고,
학습 목적함수로는 `model_150`이 `model_25`보다 **2.8배 좋다.**

**수정:** `select_best_checkpoint.py`에서 (a) 낙상을 순위에서 제외(보고는 유지),
(b) 1순위 키를 **과제오차 `sqrt(d² + r²θ²)`**로 교체. 실측을 다시 태우면
[150, 175, 200, 50, 25] → **`model_150` 선택**. 지금 force_ab가 돌고 있는 그 체크포인트다.

> **적용 범위:** 이건 앞으로의 모든 라운드가 뽑는 체크포인트를 바꾼다. E/G/H/I0/I1의
> 기존 선택은 옛 기준으로 뽑힌 것이므로, 비교에 쓸 때 이 차이를 감안할 것.

### 8-12b. I3_rough — 지형 재시도 (2026-08-04 구현, 미실행)

I2b가 지형을 켜면서 **base config의 숫자를 읽지 않고 그대로 상속**했다:

```yaml
terrain_proportions: [0.0, 0.0, 0.5, 0.5]   # [평지, 경사, random, discrete]
random_height: 0.1        # ±10 cm
horizontal_scale: 0.1     # 10 cm 격자  ->  국소 경사 ~45°
discrete_height: 0.02
```

다리 0.52 m, 발 두께 2.4 cm인 로봇에게 **자갈밭**이다. RoboCup 인조잔디는 ±1~2 cm이고,
discrete/경사는 *등반*이라는 다른 기술이며 이 과제는 그걸 요구한 적 없다.

`I3_rough` = `_I2_BASE + _I2_DR`(R2에서 채택) + 거칠기만:

```yaml
terrain.type: trimesh
terrain.terrain_proportions: [0.0, 0.0, 1.0, 0.0]   # random만
terrain.random_height: 0.02                          # ±2 cm
terrain.discrete_height: 0.0
terrain.slope: 0.0
```

I2a_dr과의 diff는 **terrain 5줄뿐**임을 생성해서 확인했다(obs 54 / act 12 유지 →
warm start 호환).

**왜 하드코딩이 아닌가:** 2 cm는 보상 목표가 아니라 바닥의 성질이다. `feet_contact`는
"모서리 하나라도 지면 1 cm 이내"이므로 평지에서는 **1.1 cm만 들면 `feet_swing` 만점**이고
아무도 더 요구하지 않는다. ±2 cm 바닥은 1.1 cm를 걸리게 만들 뿐, 높이를 지정하지 않는다.
정책이 필요한 높이를 정한다.

**왜 임계값 1 cm → 3 cm 상향은 안 되는가:** `feet_contact`가 두 보상에 동시에 쓰인다 —
`feet_swing`(+3.0, 스윙 중 비접촉 보상)과 `feet_slip`(-0.1, **접촉 중** 속도² 벌점).
3 cm로 올리면 스윙 발(대지속도 ~2.5 m/s, v²≈6 → 0.6/step)이 벌점을 맞아 **천천히 들라는
압력**이 생기고 `feet_slip`의 원래 목적이 희석된다. 하나 고치려고 다른 하나를 망가뜨린다.
그리고 3 cm는 사용자가 금지한 하드코딩 그 자체다.

**기각 조건(실행 전 확정):** **같은 바닥에서**(양쪽 `--terrain plane`) I2a_dr 대비
strict 하락 **2%p 초과면 기각.** I2b가 (다른 바닥에서) 6.2%p였으니, 파라미터를 5배 줄여
1/3 이하로 못 내리면 방향 자체가 틀린 것이다.

**잔진동은 이 arm이 풀지 않는다.** 원인은 sim에 지연·관측잡음이 없어 정책이 마음껏 떨 수
있는 것이고, 답은 지연 DR 20–60 ms + 지터다. `Goal_Pose_V7.yaml`에 지연 관련 키가
**하나도 없으므로** 그건 env 코드 작업이다 — R3에서 별도로 잡는다.

### R2 다음 수 — accept_eval이 아니라 force_ab

폐기한 게이트대로면 `accept_eval.py --seeds 0 1 2 3 4`(70분)로 clean 낙상을 재야 했다.
새 지표대로면 **I2a_dr vs I1b_force를 held-out 외력에서 비교**한다. 같은 시간에
판정 가능한 답이 나오는 쪽이다.

---

## 9. 실험 일지

| 날짜 | 라운드 | arm | iter | 결과 | 판정 | 다음 |
|---|---|---|---:|---|---|---|
| 2026-08-03 17:58 | R0 v1 | I0a_repro / I0b_foot | 200 | **중단** — 설계 결함 | 판정이 사후, 감시 없음, 카드당 1개 | v2로 재설계 |
| 2026-08-04 | R1 검증 | force_ab (외력 ON, 3 seed) | — | **I1a 17 [14,20] vs I1b 7 [5,10]**, 위치도 I1b 우세 | **외란 강건성 확정, 대가 없음** | I1a clean 120s로 2×2 닫기 |
| 2026-08-03 23:0x | R1 판정 | 2×2 완성 | 200 | **외란 채택**(낙상 −77%), 케이던스 보류(외란과 충돌) | 누적·추세로 확인 | force A/B로 강건성 직접 측정 |
| 2026-08-03 21:38 | R1 2차 | I1a/I1c/I1d 재실행 | 200 | 2×2 완성. I1b 전체 프로토콜 3/4 게이트 | 낙상 지표 재현 불가 발견 | 다중 seed 수용평가 |
| 2026-08-03 20:39 | R1 1차 | I1a/I1b/I1c/I1d | 200 | I1b만 완주(낙상 0 달성), 3개는 중단규칙에 잘림 | 규칙 과도 | 규칙 수정 후 3개 재실행 |
| 2026-08-03 18:50 | R0 v2 | I0a / I0b / I0c_h055 / I0d_h058 | 200 | **완료·전 arm 채점됨** | 하네스 건전, 높이 답 나옴 | R1 착수 |

### R0 v2 학습 완료 (2026-08-03 19:03)

4 arm 전부 200/200 완주. 실측 **3.59~3.62 s/iter**(카드당 2 프로세스) → **12분**.
사전 추정 2.0 s/iter는 틀렸다 — 2 프로세스 시분할을 과소평가했다. 다음 라운드 추정에 반영한다.

~~**문제: 네 arm 모두 `평가 0건`.**~~ → **오표시였다.** `watch_eval`은 정상 작동했고,
대시보드의 "평가 N건" 카운터가 `report.json` 스캔만 세느라 `watch_eval.jsonl`을 무시했다.
`round_summary.py`가 실제 값을 꺼내 확인했다(아래). 카운터는 수정했다.

(아래 진단 후보 3개는 결과적으로 전부 오진이었다. 기록만 남긴다.)

**당시 세웠던 가설:** `watch_eval`은 붙었다고 로그에 찍혔는데(4/4) 결과가 없다.
checkpoint가 arm당 2개뿐(`model_0`, `model_200`)이고 `--skip-first 1`이라 **채점 대상이
`model_200` 하나**인데, 그것마저 리포트가 없다. 가능성 셋:

1. 학습이 끝나며 GPU를 놓기 전에 평가가 시작돼 OOM/디바이스 충돌
2. `eval_goal_pose.py` 인자 불일치로 즉시 종료(→ `watch_eval/*/eval_failed.log`에 남음)
3. `watch_eval`이 run 디렉터리 해석에 실패(→ `logs/watch_eval/<arm>.log`에 남음)

**진단은 추측이 아니라 파일로 한다** → `tools/round_summary.py` 신설. 라운드 하나를
한 화면으로 요약하고, 결과가 없는 arm은 **왜 없는지를 디스크에서 찾아 같이 출력**한다.

**v1(18:03) 결과는 유효하고, R0의 첫 질문에는 이미 답이 나왔다:**

| | pos med | p90 | falls | strict |
|---|---|---|---|---|
| E0@6200 기준 | 2.72 | 5.01 | 2 | 89.3% |
| **I0a_repro** | **2.71** | 5.03 | **1** | **89.5%** |
| **I0b_foot** | 2.84 | 4.98 | 4 | 89.9% |

→ **하네스는 E0를 재현한다**(§8-3 판정표의 "R1 진행" 조건 충족).
→ **실제 발 크기의 대가는 +0.13 cm / +3 낙상**으로 예상보다 훨씬 싸다. `I0b_foot`을
이후 전 라운드의 기준선으로 쓰는 데 무리가 없다.
### R0 v2 판정 (2026-08-03 19:08, `round_summary.py`)

`model_200`, 20 s × 256 env 스크리닝 평가:

| arm | pos med | p90 | hd ° | **falls** | strict % |
|---|---:|---:|---:|---:|---:|
| E0@6200 기준 | 2.72 | 5.01 | 2.5 | 2 | 89.3 |
| I0a_repro (대조) | 2.65 | 4.93 | 1.8 | **15** ⛔STOP | 90.5 |
| I0b_foot | 2.89 | 4.93 | 2.3 | **4** | 90.6 |
| **I0c_h055** | **2.71** | **4.81** | 2.2 | **4** | **91.7** |
| I0d_h058 | **2.63** | 4.98 | 1.9 | 7 | 90.2 |

**① 하네스는 건전하다.** 위치가 넷 다 2.63~2.89 cm로 E0(2.72)와 같은 자리다.
아카이브·계측 수정 8건이 과제를 바꾸지 않았다. → §8-3 판정표의 "R1 진행" 조건 충족.

**② 발 크기 — 서술을 정정한다 (2026-08-03 팩트체크).**

앞서 "`feet_roll`/`feet_pitch`/`feet_slip` 보상이 오판한다"고 썼는데 **틀렸다.**
`feet_edge_pos`는 그 보상들이 아니라 **접촉 판정 `self.feet_contact` 자체**를 만든다
(`goal_pose.py:685-698`: 네 모서리 중 하나라도 지면 1 cm 이내면 접촉).
그리고 **"낙상이 줄었으니 옳다"는 정당화도 철회한다** — `feet_contact`은 `feet_swing`
보상에 들어가는 *신호*이고, 신호가 늦게 울리면 같은 걸음에도 보상을 더 받는다.
낙상 감소는 교정의 정당성 근거가 될 수 없다.

**실제 근거는 기하학이고, 세 갈래로 독립 확인된다:**

| | x 범위 | 폭 | 길이 | 바닥 z |
|---|---|---|---|---|
| config `feet_edge_pos` | [−0.1015, +0.1215] | ±0.050 | **0.223** | −0.0300 |
| collision box (PhysX가 미는 것) | [−0.066, +0.094] | ±0.035 | 0.160 | −0.0240 |
| visual mesh (K1 실제 발) | [−0.0685, +0.0965] | ±0.0392 | **0.165** | −0.0269 |

1. `envs/T1/{Base_Walk,Parameter_Walk,Kicking}.yaml`에 **바이트 단위로 동일한 값**이 있다
   → T1에서 포크하며 안 고친 것이 확인된다
2. K1 실제 발 메시 대비 **길이 +35.2%, 폭 +27.6%**
3. 바닥이 실제 밑창보다 **3.1 mm 아래**

**진짜 결함은 신호와 물리의 불일치다.** sim에 발 정의가 셋 있고, PhysX가 미는 것은
collision box인데 `feet_contact` 신호는 그보다 **39% 긴 상자**로 판정한다.
즉 물리적으로 아직 공중인 발을 "접지"라고 보고한다. 어느 쪽이 진짜냐 이전에
**내부적으로 모순**이다.

**채택 근거 (수정)**: I0b/I1에 넣은 값은 collision box 기준이다. box 자체도 실제
메시보다 폭 −10.7%, 바닥 +2.9 mm 작으므로 **실기 발보다 좁게 학습하는 셈**이다.
이는 "정확해서"가 아니라 **내부 일관적이고 전이에 보수적이라서** 채택한다 —
실기 발이 sim보다 넓으면 실제 로봇이 sim보다 안정적이고, 그 방향이 안전하다.

**미해결로 남긴 것**: collision box를 메시에 맞춰 넓히면 셋이 모두 정합하지만
물리가 바뀌어 warm start에 영향을 준다. **R2 후보로 등재**한다.

**③ 높이는 0.55가 낫다.** `I0c_h055`가 p90 4.81(최저)·strict 91.7%(최고)·낙상 4로
종합 1위다. `I0d_h058`은 median이 가장 낮지만(2.63) 낙상 7로 밀린다.
0.52(대조 I0a)는 낙상 15로 최악 — **사용자의 실기 관찰("0.58로 올리니 잘 섰다")이
방향은 맞았고, 최적값은 그 사이인 0.55다.**

**④ 대조군이 STOP에 걸린 것은 규칙이 작동한다는 증거다.**
`falls 15 > 3.0x reference 2` — 학습 전에 고정한 규칙이 자동으로 집행됐다.
H가 4 GPU-일을 태우고 알아낸 것을 이번엔 12분 만에 잡았다.

**⑤ 유보 조건 — 완화됨.** 서버 리포트 전문을 받아 확인하니 20 s 평가가 arm당
**731~743 구간**을 채점했다. 낙상률(시도당)은 I0a **0.0200** vs I0b/I0c **0.0054**,
I0d 0.0093. 734 시도 기준 15 vs 4는 Poisson 신뢰구간 밖이라 **노이즈가 아니다.**
다만 낙상 게이트는 0이므로 4도 미통과다. 전체 프로토콜 확인은 R1의 `train_and_eval`이
학습 후 자동으로 수행한다.

**⑥ 조기중단이 실제로는 작동할 기회가 없었다.** `save_interval: 100` + 200 iter =
checkpoint 2개(`model_0`, `model_200`), `--skip-first 1`이라 **채점 대상이 1개**였다.
STOP이 마지막에 걸린 것은 규칙이 맞았다는 뜻이지만 시간을 아끼지는 못했다.
→ R1은 `runner.save_interval: 25`로 **8개 지점**을 채점한다.

---

## 10. R1 — 2×2 요인설계 (2026-08-03 확정)

R0 승자 둘(**발 교정** + **높이 0.55**)은 **한 번도 같이 돌아본 적이 없다.**
`I1a_base`가 그 조합이자 R1의 대조군이다.

| arm | I1a 대비 | 카드 | 묻는 것 |
|---|---|---|---|
| **I1a_base** | (대조) 발 교정 + h055 | 0 | 두 승자를 합치면 유지되나 |
| **I1b_force** | + 외란(저용량, 접촉 렌치만) | 0 | **강건성이 직립을 대신 만드나** (하드코딩 금지 원칙) |
| **I1c_cadence** | + 케이던스–속도 커플링 | 1 | 강제된 상체 기울기가 사라지나 |
| **I1d_both** | + 외란 + 케이던스 | 1 | **상호작용** (유일하게 단일레버 아님, 명시) |

**왜 요인설계인가**: standing 문제와 속도 문제가 **상호작용할 것으로 의심**된다 —
케이던스 수정이 강제 기울기를 없애고, 외란이 직립을 *필요*하게 만든다.
따로 검증하면 합쳤을 때를 알 수 없다(사용자 지적: "따로 학습하면 의미 없다").
`I1d`는 그 교란을 **의도적으로** 넣은 셀이고 문서에 그렇게 적는다.

**외란은 저용량만.** E2·G2가 robust 묶음을 한꺼번에 켜서 두 번 다 저속 붕괴했다(L4).
`I1b`는 **접촉 렌치 2종만** 켠다 — goal jitter·bias·flicker·staleness는 전부 OFF.
외란 substep 재제출 수정(`goal_pose.py:587`)이 live에 살아 있어 명목 충격량이
온전히 전달된다.

**케이던스 커플링** (`_couple_cadence_to_speed`, `commands.cadence_coupling`):
```
f = clamp( 요구속도 / (2 × 0.28 m), 1.6, 3.2 )   요구속도 = goal_dist / 남은 시간
```
보상이 아니라 **명령 분포 수정**이다. 자세를 지킨 대가를 지불하지 않고,
과제가 요구하는 보폭을 다리가 낼 수 있는 값으로 만든다. 관측 54차원 불변
(gait_frequency는 이미 command 슬롯 3). `stand` 목표의 얼린 클럭은 건드리지 않는다 —
덮으면 제자리 서기가 다시 비최적이 된다.

### R1 판정표 — 실행 전 확정

| 관측 | 결론 |
|---|---|
| I1a가 I0b/I0c 수준(pos ≤2.9 cm, falls ≤4) 유지 | 두 승자가 합쳐진다 → 기준선 확정 |
| I1a가 열화 | 두 수정이 **간섭**한다 → 하나만 채택 |
| I1b 낙상 < I1a | **강건성이 직립을 만든다** — 원칙 검증 성공 |
| I1b 속도 붕괴(>1 m/s 체류가 I1a의 50% 미만) | L4 재발 → 용량을 더 낮춘다 |
| I1c 낙상·정확도 비열세 + 고속 구간 확대 | 케이던스가 병목이었음 확정 |
| I1d < max(I1b, I1c) | 두 레버가 충돌 → 순차 적용으로 전환 |

중단 규칙은 R0와 동일(위치 1.3×, 낙상 3×), 다만 **기준이 I0c의 2.71 cm / 4회**로 바뀐다.
`save_interval 25`라 8개 지점에서 판정하므로 이번엔 조기중단이 실제로 작동한다.

### R1 착수 전에 잡힌 결함 2건

| # | 결함 | 어디서 잡혔나 | 고침 |
|---|---|---|---|
| 1 | `dict(**_OFF_ROBUST, **_I1_FORCE)`가 `disturbance.enabled` 중복으로 TypeError | **GPU 앞 28분** (재평가 대기·모니터·이름검사 다 통과 후) | `merge()`로 뒤 레이어 우선. `DRYRUN`이 arm마다 `--gpu-of`를 돌려 모듈 import와 dict 구성을 강제 → 1초에 잡힘 |
| 2 | 외란 첫 이벤트를 **전체 간격에서** 뽑아 첫 8 s 동안 전 env가 무외란 | smoke `disturbance fires: 0/300` | 첫 이벤트를 **간격 내 균일 위상**(`randint(1, hi/dt)`)에서 뽑음 |

**②는 스모크 창 문제가 아니라 학습 분포 결함이다.** `interval_s [8,14]`에서
`randint(400,700)`으로 초기화하면 **어떤 env도 8초 전에는 외란을 받지 못한다**.
에피소드가 30 s이므로 **매 에피소드의 첫 27%가 전 env 동시에 외란 없는 구간**이었다.
E2·G2가 외란을 켜고도 "강건해졌다"는 증거를 못 준 이유의 일부일 수 있다 —
초반 구간이 통째로 clean이면 정책은 그 구간에 맞춰 최적화된다.
`_reroll_paths`의 dwell 스태거와 **같은 결함, 같은 수정**이다.

### R0 v1 → v2 재설계 경위 (2026-08-03)

v1을 띄운 직후 사용자가 네 가지를 지적했고 전부 타당했다. 기록해 둔다 — 같은 실수를 다시 하지 않기 위해서다.

| # | 지적 | 사실 확인 | 조치 |
|---|---|---|---|
| 1 | `tonight.sh` 로그가 "G 배치 4종"이라고 찍힘 | 사실. 헤더 문자열이 낡음(꼬리는 이미 고쳐져 있었음) | 헤더 수정 |
| 2 | 웹이 아무것도 안 뜸 | 사실. collector가 안 떠 있었음 | `tonight.sh`가 자동 기동하도록 연결 |
| 3 | 터미널이 옛 run까지 전부 출력 | 사실. "실시간 현황"이 목적인데 이력을 뿌림 | TUI 기본을 **진행 중인 run만**으로. 이력은 웹, `--all`로 복원 |
| 4 | 판정 정리가 안 돼 있음 | 사실. H는 12000까지 돌린 뒤에야 결과를 앎 | `tools/watch_eval.py` — checkpoint마다 채점, 사전 확정 규칙으로 자동 중단 |

추가로 사용자가 제기한 **하드코딩 금지 원칙**("결과를 보상하지 말고 그 결과를 필연으로
만드는 조건을 만들어라")에 따라 §5의 보상 추가안 4건(발 하중분배·heel 접지·순항 pitch
페널티·높이 튜닝)을 재검토했다. 앞의 셋은 **폐기**하고 조건(외란·실제 발 크기·케이던스
커플링)으로 대체한다. 높이만은 **새 항 추가가 아니라 기존 상수 교정**이므로 남긴다(§8-5).

그리고 코드 정리(지시 3)를 수행하다 **live 스택 전체가 죽을 뻔한 결함**을 발견했다:
`envs/K1/goal_pose_hbatch.py`를 아카이브했는데 `envs/__init__.py`가 계속 import하고
있었고, `eval_goal_pose.py`/`smoke_v7.py`/`utils/runner.py`가 전부 `from envs import *`를
한다. `check_names.py`는 이걸 **구조적으로 못 잡는다** — 로컬에서 안 풀리는 모듈을
stdlib로 간주하고 건너뛴다. 별도 검사로 local import 53개가 전부 실재함을 확인했다.

### 서버 접속 메모 (매번 찾지 않도록)

**전부 절대경로다.** 상대경로로 적었다가 `cd` 누락으로 두 번 실패했다
(`fatal: not a git repository`, `cd: htwk-gym: No such file`). 어디서 실행하든 동작해야 한다.

**서버에서 실행:**

```bash
# 중단 — 세션명은 SESSION= 로 준 값. GPU가 실제로 비었는지 확인까지 한 줄에.
tmux kill-session -t i 2>/dev/null; pkill -f "tools/watch_eval.py"; pkill -f train_v7.py; sleep 2; nvidia-smi --query-compute-apps=pid,used_memory --format=csv
```

```bash
# 실행
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose && git pull && cd htwk-gym && ARMS="I0a_repro I0b_foot I0c_h055 I0d_h058" ITERS=200 SESSION=i bash tools/tonight.sh
```

```bash
# 터미널 모니터
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose/htwk-gym && python tools/monitor.py --tui          # 진행 중인 run만
cd /mnt/DATA/workspace/ws_eungkyu/k1-goalpose/htwk-gym && python tools/monitor.py --tui --all    # 끝난 것까지
```

**맥에서 (서버 아님):**

서버 IP `165.246.193.194`는 공인 주소라 터널 없이 바로 열린다:

```
http://165.246.193.194:8420/
```

포트가 막혀 있으면 그때만 터널 — **이 명령은 맥 터미널에서 실행한다.**
서버에서 실행하면 자기 자신에게 접속하는 꼴이고, 서버 conda의 OpenSSL 충돌로
`OpenSSL version mismatch`가 난다.

```bash
ssh -L 8420:localhost:8420 user@165.246.193.194 -p 6666
#   -> 맥 브라우저에서 http://localhost:8420/
```

> 기록 규칙: 라운드가 끝나면 이 표에 **한 줄**만 추가하고, 상세는 §7 결정 트리의
> 해당 노드에 반영한다. 판정은 §8-3처럼 **실행 전에 고정**한 기준으로만 내린다.

---

## 8-13. 라운드 2 판정 — force_ab + I2b 평지 재평가 (2026-08-04 02:4x)

### (1) force_ab — 강건성 주장이 처음으로 근거를 가졌다

`logs/force_ab/2026-08-04-02-01-38`, 120 s × 256 envs × seed 0/1/2, held-out 외력.

| arm | pos cm | heading° | **과제오차 cm** | 낙상 (seed별) | 구간 | **events** | 외력중낙상 |
|---|---:|---:|---:|---|---:|---:|---:|
| I1b_force | 2.57 [2.51, 2.60] | 2.06 | 4.41 [4.29, 4.52] | 7, 5, 10 | 13,874 | 5,083 | 0, 1, 1 |
| **I2a_dr** | 2.61 [2.53, 2.62] | **1.86** | **4.12 [4.11, 4.21]** | **3, 0, 2** | 13,849 | 5,085 | 1, 0, 0 |

**events 5,051~5,103.** I1b의 원래 보고서는 `force events: 0`이라 스스로 "증거 아님"을
선언했다(§8-9). **그 구멍이 닫혔다.** 외력이 실제로 걸린 상태의 측정이다.

**I2a_dr이 낙상 4.4배 적다. 1.59 vs 0.36 /1000, 단측 p = 0.00077.**
seed 범위도 겹치지 않는다([5, 10] vs [0, 3]). 과제오차 범위도 겹치지 않는다.

> **§8-12a에서 낙상 게이트를 폐기한 것과 모순되지 않는다.** 폐기한 판정은
> **2,314구간에서 1 vs 0 (p = 0.50)**이었고, 여기는 **13,874구간에서 22 vs 5
> (p = 0.0008)**다. 노출 6배, 사건 27배. 원칙은 *"낙상은 무의미"*가 아니라
> *"그 노출에서는 무의미"*였고, 이번 결과가 그 경계를 실측으로 보여준다.

**판정: I2a_dr 채택 확정.** §8-10의 *"DR은 공짜다"*는 **틀렸다 — 공짜가 아니라 이득이다.**
20초 스크리닝에서 낙상 4 vs 6을 "동률"로 읽었던 것은 검정력이 없어서였다.

**⚠️ 낙상 27건 중 외력 중 발생은 3건뿐이다.** 외력은 낙상의 주된 원인이 아니다.
남은 낙상은 다른 데서 온다 — R3 이후 별도로 국소화할 것.

**⚠️ `--skip-clean`으로 돌아 clean 셀이 없다.** "외력이 얼마를 가져갔는가"는 **여전히
미측정**이고, §8-11이 예고한 (arm × 외력) 2×2는 **안 닫혔다.**

### (2) I2b_terrain 평지 재평가 — 철회는 옳았고, 판정은 아직 못 한다

`plane`, 외력 없음, 4,647구간: pos **2.16 cm** / heading **3.05°** / strict **93.5%** / 낙상 5.

**trimesh 85.0% → plane 93.5%. 8.5%p는 정책이 아니라 시험지였다** — §8-12의 철회 확정.

그러나 **채택 판정은 아직 불가능하다. 세 가지가 걸린다.**

1. **프로토콜 불일치.** 비교 대상 91.5%는 20초 스크리닝 4개 체크포인트 평균(~740구간씩),
   이건 4,647구간 단일 런이다. 지형은 맞췄지만 노출과 집계가 다르다.
2. **목적함수로는 오히려 나쁘다.** 위치는 좋고(2.16 vs 2.61) heading이 나쁘다
   (3.05° vs 1.86°) → **과제오차 5.75 vs 4.12 cm, I2b가 40% 나쁘다.**
3. **체크포인트를 옛 선택기가 뽑았다.** §8-12a의 위치 편향 그대로다. 새 기준으로
   재선택하면 다른 체크포인트가 나올 수 있다.

> **strict가 I2b를 좋게 본 이유도 같은 결함이다.** strict는 게이트와 같은 5 cm / 10°를
> 쓴다. heading 1.86° → 3.05°는 **10° 한계에서 거의 보이지 않는다.** strict는 heading에
> 둔감한 지표이고, 그래서 과제오차와 반대 방향을 가리켰다.

### (3) 남은 일 — 이 라운드를 닫는 데 필요한 것

| # | 할 일 | 비용 | 무엇이 닫히나 |
|---|---|---:|---|
| 1 | I2b를 **새 선택기**로 재선택 | ~30분 | (2)의 3번. I2b 판정 가능해짐 |
| 2 | `force_ab --skip-clean` **없이** clean 3 seed | ~42분 | 2×2 완성, 외력 비용 확정 |
| 3 | 위 둘로 I2b 최종 판정 | — | R2 종료 |

### (4) 계획 수정

- **P1 "낙상 0"을 개수에서 비율+CI로 바꾼다.** 개수 목표는 노출을 안 적으면 무의미하다
  (§8-12a가 그 대가를 치렀다). 현재 I2a_dr은 **0.36/1000 [0.12, 0.84]**. "0"을 95%로
  주장하려면 **무낙상 8,300구간 이상**이 필요하다(rule of three).
- **판정 지표를 과제오차로 통일한다.** strict와 위치 median은 heading에 둔감해 이번에
  두 번(선택기, I2b) 반대 방향을 가리켰다.

---

## 8-14. P2 "미달"은 상당 부분 비닝 아티팩트였다 (2026-08-04 03:2x)

`tools/speed_ceiling.py`로 **18개 셀 전부**(I1a_base / I1b_force / I2a_dr, clean+force,
seed 0/1/2, + i2b_flat) 거리별 peak을 읽었다. **만장일치다.**

### 거리 2.0–2.5 m 칸 (clean 기준)

| arm | peak med | peak p90 | **≥1.3 m/s 도달률** |
|---|---:|---:|---:|
| **I2a_dr** | **1.45, 1.46** | 1.67, 1.70 | **84.9%, 88.6%** |
| I1b_force | 1.36, 1.39, 1.37 | 1.61, 1.57, 1.57 | 64.4%, 67.3%, 66.0% |
| I1a_base (force) | 1.32–1.35 | 1.54–1.58 | 56.5–63.6% |
| I2b_terrain (평지) | 1.25 | 1.40 | **28.9%** |

**I2a_dr은 가장 긴 칸에서 peak median 1.43~1.46 m/s다 — P2 밴드(1.3–1.5) 안이다.**
force 셀 3개, clean 셀 2개가 모두 같은 값을 준다.

### "구간최고 p90 1.29 / >1 m/s 체류 3.1%"가 나온 이유

**구간의 44%가 0.5 m 미만이다.** 거기서는 로봇이 거의 움직이지 않는다(peak med 0.10).
그 2,000여 구간을 2 m 구간과 **한 통에 넣고 뽑은 p90**이 1.29였다. 능력의 측정이 아니라
**혼합분포의 통계**다 — §8-12a(선택기)·§8-9(round_summary force 혼입)와 **같은 부류의
결함**이고, 이번 배치에서 세 번째다.

### ⚠️ 별건: 구간 거리 분포가 설정과 다르다

`goal_dx ±2 / goal_dy ±1.5` 균등이면 나와야 할 분포와 실측이 크게 다르다:

| 거리 | 기대 | 실측 |
|---|---:|---:|
| 0.0–0.5 m | 6.5% | **44.0%** |
| 1.5–2.0 m | 30.9% | 14.2% |
| 2.0–2.5 m | 10.4% | **2.6%** |

**median 거리가 기대 1.38 m인데 실측은 약 0.6 m다.** 구간 수(4,600 ≈ 30,720 env-s / 6.7 s)는
resampling 4–8 s와 맞으므로 구간 정의 문제는 아니다. **목표 샘플링을 확인해야 한다** —
이게 사실이면 과제는 기하학이 시사한 것보다 **더** 속도를 안 시킨다.

### 판정

1. **천장은 과제 것이다.** 18/18 셀에서 2.0–2.5 m 칸까지 peak이 계속 오른다.
   정책이 속도에 막혀 있다는 증거는 **없다.**
2. **DR이 속도도 올렸다.** ≥1.3 도달률 82.5–88.6%(I2a_dr) vs 62.6–69.5%(I1b_force),
   범위가 겹치지 않는다. **I2a_dr의 세 번째 이득**(정확도 동률, 낙상 4.4배, 속도).
3. **I2b_terrain 세 번째 감점.** 1.25 / 28.9%로 확실히 느리다.
4. **외력은 속도를 못 깎는다.** clean 1.36/64.4% vs force 1.37/66.4%. 5,000회를 맞고도 같다.

### 남은 진짜 질문 — peak인가 순항인가

**P2 문구는 "몸통속도 1.3–1.5 m/s"뿐이고 peak/순항 구분이 없다**(§200, §654).
그리고 `segments.csv`에는 **구간 내 체류 시간이 없다** — peak과 mean(정지 포함)뿐이다.
2.25 m를 1.45로 가면 이동시간이 1.6초이고, 그 안에 순항 구간이 있는지는 **이 데이터로 모른다.**

- **peak으로 읽으면 I2a_dr은 P2를 이미 충족한다.**
- **순항으로 읽으면 아직 측정 수단이 없다.** `time_above_1p0_s` / `time_above_1p3_s`를
  구간별로 기록해야 한다(작은 변경, 다음 eval부터 채워짐).

**R4 sprint는 여전히 필요하지만 이유가 바뀐다.** "속도를 만들려고"가 아니라
**"2.5 m 너머의 천장을 보려고, 그리고 순항을 측정 가능하게 만들려고"**다.

---

## 8-15. 목표 샘플링 확인 — 버그가 아니라 설계다 (2026-08-04 03:3x)

**§8-14에서 "구간 거리 분포가 설정과 다르다"고 한 것은 내가 틀렸다.** 균등 상자
샘플링을 가정하고 `goal_categories`를 빼먹은 채 비교했다. 반영해서 다시 계산하면
**다섯 칸 전부 맞는다.**

| 거리 | 기대(카테고리 반영) | 실측 |
|---|---:|---:|
| 0.0–0.5 m | 43.6% | **44.0%** |
| 0.5–1.0 m | 17.6% | 17.2% |
| 1.0–1.5 m | 21.5% | 22.1% |
| 1.5–2.0 m | 14.2% | **14.2%** |
| 2.0–2.5 m | 3.1% | 2.6% |

요구속도도 median 0.12 / p90 0.30 / max 0.62가 나와, 기록된 **0.11 / 0.31**과 맞는다.
**기록이 옳았고 §8-14에서 내가 쓴 해석용 분포가 틀린 모델이었다.**

### 왜 이렇게 짧은가 — 세 겹이다

`goal_categories`(stand .1 / straight .2 / lateral .2 / turn .2 / combined .3), 그리고
`goal_pose.py:446-447`:

```python
dx_local = where(stand | cat==2 | cat==3, 0, dx_local)   # lateral, turn
dy_local = where(stand | cat==1 | cat==3, 0, dy_local)   # straight, turn
```

1. **stand(0.1) + turn(0.2) = 30%가 dx=dy=0.** 설계상 무이동이다.
2. **straight(0.2)·lateral(0.2)은 한 축으로 눌린다** — 상자 대각선을 못 쓴다.
3. **전 상자를 쓰는 건 combined(0.3)뿐이다.** 그래서 median 거리가 1.38 m가 아니라
   **0.70 m**다.

**결과: 2.0–2.5 m 구간은 전체의 3.1%다.** §8-14에서 I2a_dr이 peak median 1.45 m/s를
낸 그 칸이 **과제의 3%**다. 속도가 행사되는 유일한 영역에 과제가 3%를 준다.

### 그리고 속도 요구 장치는 꺼져 있다

`make_v7_arms.py:70` — 모든 I 배치 arm이 `_OFF_PATH`를 상속한다:

```python
_OFF_PATH = {"commands.goal_mode_mixture": {"waypoint": 1.0, "path": 0.0}}
```

V7 base는 `waypoint 0.65 / path 0.35`이고, 그 config의 주석이 path를 이렇게 설명한다 --
*"path = a lookahead point that ADVANCES along a curve at a commanded speed, **which is
what creates a speed demand**"*. §P7의 기록대로 **의도적으로 뺐고**, 그 판단 자체는
§L5의 세 대가를 감안하면 유지할 만하다. 다만 **지금 P2를 못 만드는 직접 원인**이다.

### R4 sprint — 생각보다 작은 변경이다

`cat` 추첨(`goal_pose.py:444`)과 마감 추첨(`:511`)이 **같은 resample 경로 안에 있다.**
그래서 카테고리별 거리 범위와 카테고리별 마감을 둘 다 넣을 수 있다.

**둘 다 필요하다.** 요구속도 1.3을 만들려면 `d/T ≥ 1.3`인데, 현재 마감 4–8 s에서는
d가 5–10 m여야 한다 — 상자(최대 2.5 m) 밖이다. 거리 2.5–4 m에 **마감 약 d/1.4 ≈ 2.5–3 s**를
같이 줘야 한다. **거리만 늘리는 안은 작동하지 않는다.**

---

## 8-16. 실행 전 전수조사 — 그리고 I2b는 애초에 지형 시험이 아니었다 (2026-08-04 04:0x)

두 번 헛돌린 뒤(§8-13 `--config` 오류로 35.6분) 이번 세션에 바꾼 코드와 드릴 명령을
전부 코드로 확인했다. **12항목 전부 통과**, 그리고 **더 큰 결함 하나를 찾았다.**

### (1) I2b 재선택 실수의 전모 — path 하나가 아니라 24개 키였다

`sweeps/I2b_terrain.yaml`을 직접 생성해 `envs/K1/Goal_Pose_V7.yaml`과 비교했다.
`--config envs/K1/Goal_Pose_V7.yaml`은 지형만 바꾼 게 아니라 **24개 키**를 바꿨다:

| 바뀐 것 | arm(학습된 것) | 내가 준 V7 base |
|---|---|---|
| **`asset.feet_edge_pos`** | 교정된 발(I0b) | **옛 발** |
| `commands.goal_mode_mixture` | waypoint 1.0 | path 0.35 |
| `noise.goal_*` (5개) | 전부 0 | 관측 노이즈 ON |
| `randomization.disturbance.*` (5개) | 저용량 | 고용량 |
| `rewards.base_height_target` | 0.55 (I0c) | 0.52 |
| `rewards.scales.*` (5개) | OFF | ON |

**교정된 발 기하까지 되돌아갔다.** 6.2 cm는 지형이나 path 때문이 아니라 **완전히 다른
실험**이었다. `eval_goal_pose.py:648`에 지형만 덮는 `--terrain`이 이미 있었는데
안 찾아보고 config를 통째로 바꾸는 길을 골랐다. 선택기에 그 플래그를 추가했다(6e0cb1d).

### (2) ⛔ 더 중요한 것 — I2b_terrain은 깨끗한 지형 시험이 아니다

I2a_dr과 I2b_terrain을 직접 비교하면 **지형만 다른 게 아니다**:

| 키 | I2a_dr | I2b_terrain |
|---|---|---|
| `randomization.base_com.range` | **[-0.025, 0.025]** | **[-0.1, 0.1]** |
| `randomization.base_mass.range` | [0.92, 1.08] | [0.8, 1.2] |
| `dof_stiffness/damping.range` | [0.85, 1.15] | [0.95, 1.05] |
| `terrain.type` | plane | trimesh |

**I2b는 DR 교정을 안 싣고 있다.** `_I2_BASE + trimesh`이고, `_I2_BASE`는 §8-10이
*"CoM이 지지면 밖으로 나간다 … 정적으로 균형이 불가능한 로봇"*이라고 판정한 그
`base_com ±0.1`을 그대로 쓴다.

**그래서 I2b가 느리고(1.25 m/s / 28.9%) 부정확한 것은 지형 탓인지 미교정 DR 탓인지
분리되지 않는다.** 두 arm 모두 `_I2_BASE`에서 뻗은 단일 레버라 *"어느 레버가 나은가"*는
유효한 질문이지만, *"지형이 대가를 치르게 하는가"*는 **이 설계로 답할 수 없다.**

### (3) 그 질문은 이미 I3_rough의 것이다

```
I3_rough vs I2a_dr 차이:  terrain.type / proportions / random_height / slope / discrete
                          → 지형 키 5개뿐. 나머지 전부 동일.
_I3_BASE = merge(_I2_BASE, _I2_DR)   → DR 교정을 싣는다.
```

**I3_rough는 I2a_dr과 지형만 다르다.** 게다가 지형이 현실적이다 —
**±2 cm turf**(`random_height 0.02`, slope 0, random only) vs I2b의 **±10 cm rubble +
경사 0.1 + discrete 50%**.

> **판정: I2b 평지 재선택을 하지 않는다.** 36분을 써도 지형 질문에 답하지 못하고,
> 그 질문은 I3_rough가 깨끗하게 답한다. **I2b_terrain은 여기서 종료한다.**

### (4) 전수조사 결과

| # | 항목 | 확인 방법 | 결과 |
|---|---|---|---|
| 1 | segments.csv 헤더 ↔ zip 정렬 | 26 대 26 원소별 대조 | ✅ category 인덱스 3 유지 |
| 2 | 새 seg 키 초기화·적재 | `seg = {k: [] for k in keys}` | ✅ peak_speed와 같은 블록·같은 `ids` |
| 3 | 구간 경계 리셋 | 트래커 4개 | ✅ |
| 4 | `speed_valid` dtype | `long >= int` → bool | ✅ `bool & bool` 정상 |
| 5 | `env.dt` = 스텝 증분 | `rollout(int(duration_s/env.dt))` | ✅ |
| 6 | 선택기 `--terrain`이 env 생성 전 | 424/426 < 480 | ✅ |
| 7 | `Terrain`이 type만으로 분기 | `utils/terrain.py:13-20` | ✅ trimesh 잔재 없음 |
| 8 | `EVAL_TERRAIN` → report.json | `summarize():2088` | ✅ |
| 9 | `results["gates"]["falls"]` 존재 | `:2146` | ✅ `legacy_pass`가 의존 |
| 10 | `constellation_radius` = 1.0 | I2a_dr.yaml | ✅ 과제오차 1°≡1.75 cm 근거 확인 |
| 11 | 순항 출력 경로 | 합성 데이터 실행 | ✅ |
| 12 | 옛 eval 하위호환 | 컬럼 없는 CSV 실행 | ✅ "미기록" 명시 |

---

## 8-17. 보상 포화 — 진단은 정정하되 처방은 맞다 (2026-08-04 04:2x)

### 켜진 항 확인 (끊겼던 질문)

| 항 | scale | 상태 |
|---|---:|---|
| **constellation** | **+3.5** | **ON** (weight 0.2, radius 1.0) |
| goal_reached | +1 | ON (반경 0.1 m + 정지 0.1 m/s) |
| goal_position / goal_heading / heading_near_goal / goal_progress / goal_stop | **0** | **전부 OFF** |

**그래서 `goal_position_sigma: 1.0` / `goal_heading_sigma: 0.4`는 죽은 설정이다** — 소비자가
전부 scale 0이다. constellation은 σ가 둘이 아니라 **하나**이고, `weight 0.2`가 `d_con`에
통째로 걸린다. 1/e 지점은 `d_con = 5`, heading 0일 때 **위치 2.24 m**.

> 부수 발견: heading 단독으로는 1/e에 도달할 수 없다. `2r²(1−cosθ)`의 최댓값이 4(θ=180°)라
> 최저 보상이 `exp(−0.8) = 0.449`다. **헤딩이 180° 틀려도 보상의 45%가 남는다.**

### 기전 정정 — 포화가 기울기를 죽인 게 아니다

2.6 cm / 1.9°에서 **exp항 = 0.9996**. 감쇠 기여가 0.04%다. 기울기가 작은 진짜 이유는
`d_con`이 d의 **2차식**이라 `∂r/∂d ∝ d`이기 때문이다. 지금은 지수의 **가장 선형적인
구간**에 있고, 커널이 사실상 `3.5(1 − 0.2·d_con)`으로 동작한다.

### 그 정정이 오히려 처방을 뒷받침한다

`exp ≈ 1` 구간이라 **w를 올리면 기울기가 거의 1:1로 는다.**

| w | 보상 | ∂r/∂d | 배수 |
|---:|---:|---:|---:|
| 0.2 (현재) | 3.4988 | 0.036 /m | 1.0 |
| 1.0 | 3.4938 | 0.182 /m | 5.0 |
| **5.0** | **3.4691** | **0.902 /m** | **24.8** |

**포화 대가 0.85%로 기울기 25배.** 커널을 좁히자는 방향은 수치적으로 맞다.

### 실무 결론은 제안보다 강하다

`goal_reach_radius = 0.1 m`인데 현재 오차가 2.6 cm — **이미 공 안쪽이다.**

- 2.6 → 2.0 cm 개선 시 **goal_reached 증가 0**, constellation 증가 **0.000193/step**
- **10 cm 아래로는 정확도를 올릴 이유가 두 항 어디에도 없다.**

다만 죽은 것은 아니다: iter25→150의 보상차가 **0.0021/step**이었고 정책은 그걸 학습했다
(heading 3.7° → 1.8°). **아주 얇지만 살아 있다.**

### 지금은 손대지 않는다

위치 2.6 cm 대 게이트 5 cm(2배 마진), heading 1.86° 대 10°(5배 마진). **정확도는 이 배치의
병목이 아니다** — 남은 건 P1 낙상과 P2 속도다. 잘 도는 정책의 보상 지형을 흔들 이유가 없다.

### ⚠️ 그러나 R4에서는 진짜로 물린다

기울기는 **원거리에서도 약하다**:

| 거리 | exp항 | ∂r/∂d |
|---:|---:|---:|
| 1.0 m | 0.819 | **1.15 /m** (최대) |
| 2.5 m | 0.287 | 1.00 /m |
| **4.0 m** | 0.041 | **0.23 /m** (1 m의 1/5) |

**sprint 카테고리는 거리 2.5–4 m다 — 정확히 기울기가 다시 죽는 구간이다.**

그리고 `goal_progress`가 꺼져 있다. 그 항의 docstring이 존재 이유를 이렇게 적어놨다:
*"dense gradient even far from the goal, where exp(-dist^2/sigma) is nearly flat"*.
**원거리 평탄화를 막으려고 만든 항이 꺼져 있고, R4는 정확히 그 원거리로 간다.**

> **R4 설계 항목 추가: sprint 도입 시 `goal_progress`를 켤지 함께 결정한다.**
> 안 켜면 sprint 구간 초반 2–4 m의 학습 신호가 현재 근거리의 1/5다.

---

## 8-18. P2 판정 — 못 하는 게 아니라 시킬 이유가 없다 (2026-08-04 04:5x)

`tools/p2_verdict.py`, cruise_i2a 3 seed, 2.0 m 이상 정지출발 구간 402개 전부
1.0 m/s 도달.

| | seed0 | seed1 | seed2 |
|---|---:|---:|---:|
| 기동: 0.5 m/s까지 / 1.0까지 | 0.40 / 0.70 s | 0.40 / 0.70 s | 0.40 / 0.72 s |
| **가속도**(사구간 제외) | **1.67** | **1.67** | **1.61** m/s² |
| 이론최대 순항(대칭 제동) | 0.87 s | 0.88 s | 0.86 s |
| **관측 순항** | **0.40 s** | **0.40 s** | **0.34 s** |
| **활용률** | 53% | 59% | 56% |

> 사구간 제외 추정이 중요했다. `1.0/t10`으로 잡으면 1.43 m/s²가 나오고 이론최대가
> 작아져 "구간이 짧아서"로 결론이 기운다. 실제 가속도는 **1.67 m/s²**다.

**제동을 빠르게 가정할수록 활용률은 더 떨어진다**(1.5배 → 40~48%, 2배 → 37~43%).
결론이 가정에 얹혀 있지 않다.

### 왜 절반을 남기나 — 보상에 차이가 없어서다

`goal_reached`(+1/step) 관점에서 "빠르게(1.45) vs 느리게(1.00)"의 차이:

| 설계 | 빠르게 | 느리게 | 차이 |
|---|---:|---:|---|
| **현재** d=2.25 m, 마감 6 s | 159 step | 138 step | **16%** |
| 거리만 d=3.5 m, 마감 6 s | 116 step | 75 step | 54% |
| **거리+마감** d=3.5 m, 마감 4 s | 16 step | 0 step | **전부 아니면 전무** |

**현재 과제는 2.25 m를 6초 안에 가면 된다 — 요구 0.375 m/s다.** 이미 3.2초를 공 안에서
보내고 있어서, 속도를 더 내도 체류가 16% 늘 뿐이다. §8-17에서 본 constellation 기여
(2.6→2.0 cm에 0.000193/step)와 합치면 **속도를 올릴 이유가 보상 구조에 사실상 없다.**

### 판정

1. **P2를 순항으로 읽으면 미달이다.** 1.3 m/s를 median 0.40 s 유지, 최댓값 1.16 s.
2. **원인은 무능이 아니다.** 가속도 1.67 m/s², peak 1.45 m/s, 여유 0.87 s가 있는데
   0.40 s만 쓴다.
3. **⛔ 따라서 sprint 카테고리만으로는 P2가 안 닫힌다.** 2.25 m에서 여유의 절반을
   남기는 정책은 4 m에서도 남길 수 있다. **거리는 여유를 늘릴 뿐 요구를 만들지 않는다.**
4. **레버는 거리가 아니라 마감이다.** 거리만 늘리면 차이가 54%지만, 마감을 `d/1.4 + 1.5 s`로
   조이면 도달 자체가 걸려 **전부 아니면 전무**가 된다.

> **R4 설계 확정 항목**
> - sprint 카테고리에 **카테고리별 마감**을 반드시 함께 넣는다(`goal_pose.py:444`의 `cat`
>   추첨과 `:511`의 마감 추첨이 같은 경로에 있어 구현은 작다)
> - `goal_progress`(현재 scale 0) 재활성 여부를 함께 결정한다 — §8-17의 원거리 기울기
> - 거리만 늘리는 안은 **기각**

### 이 판정이 못 넘는 선

가속도 1.67 m/s²는 **능력이 아니라 행동**이다. 요구속도 최대가 0.62 m/s인 과제에서 빨리
가속할 이유가 없다. sprint 아래에서는 가속도 자체가 올라갈 수 있고 이론최대도 같이 오른다.
**"sprint를 넣으면 1.4 m/s를 순항한다"는 보장이 아니라, "지금 안 하는 이유가 무능이
아니다"까지가 이 데이터의 사정거리다.**

---

## 8-19. ⛔ §8-18의 판정을 철회한다 — 지표가 속도가 아니었다 (2026-08-04 05:0x)

§8-18은 활용률 56%를 근거로 *"거리 여유가 있는데 안 쓴다 → 정책/보상 쪽 한계"*라고
판정했다. **틀렸다.**

`t_max`는 "1.3 m/s **이상으로 보낸 시간**"의 상한인데, **그 시간은 정확히 1.3으로 갈 때
최대가 된다.** 더 빨리 가면 같은 거리를 빨리 지나가므로 오히려 줄어든다:

| 순항속도 | 1.3초과 시간 | **도착시간** |
|---:|---:|---:|
| 1.30 m/s | (경계) | 2.91 s |
| 1.45 m/s | 0.86 s | **2.82 s** |
| 1.94 m/s | 0.76 s | **2.72 s** |

**활용률을 올리라는 건 "더 느리고 균일하게 가라"는 뜻이 될 수 있다.** 속도 지표가 아니라
*1.3에 얼마나 붙어 가는가*를 잰 것이다. 판정의 근거로 쓸 수 없다.

`tools/p2_verdict.py`의 판정문을 이 내용으로 교체했다. 측정(가속도, 상한)은 유효하다.

### 남는 사실만

- 2.0–2.5 m 구간 **peak median 1.45 m/s — P2 밴드(1.3–1.5) 안이다**
- 1.3 이상 유지 median **0.40 s**, 체류의 90%가 한 덩어리
- 가속도(사구간 제외) **1.67 m/s²**, 기동 사구간 0.40 s
- 이론상한이 어떤 가정에서도 **0.86~1.07 s**

### 명쾌한 결론

**P2는 이 과제에서 판정 불가다.** "정책 한계"도 "기하 한계"도 아니고 **측정 불가**다.
2.25 m 구간은 이상적인 주행으로도 1.3 이상을 1초 남짓밖에 담지 못한다. 순항 능력을
가르려면 그보다 긴 구간이 필요하고, 그런 구간이 과제의 **3%**뿐이다(§8-15).

**§8-18의 R4 결론 중 살아남는 것과 죽는 것:**

- ❌ *"정책이 여유를 안 쓰므로 sprint만으로 부족하다"* — 근거 철회
- ✅ **"마감이 레버다"** — 근거가 바뀌어 살아남는다. 거리만 늘리면 **요구속도(d/T)가 안
  오르고**, 요구속도가 안 오르면 순항이 필요해지지도, 측정되지도 않는다. 마감 `d/1.4 + 1.5 s`가
  요구속도를 1.4로 만드는 유일한 장치다(§8-11의 시뮬: sprint 15%가 p90을 1.34로)

### 누진보상(사용자 제안) 평가

`goal_reached`를 체류시간에 누진으로 주면 빨리 도착할 유인이 커진다. 계산하면:

| 설계 | 평탄(+1/step) | 누진(체류²) |
|---|---|---|
| 현재 d=2.25, 마감 6 s | 16% | **34%** |
| 마감 조임 d=3.5, 마감 4 s | 전부 아니면 전무 | 전부 아니면 전무 |

**기울기를 2배로 만든다 — 공짜로 얻는 개선이다.** 다만 **속도를 측정 가능하게 만들지는
못한다.** 요구속도 분포가 그대로라 §8-15의 3% 문제가 남는다. **마감과 상호배타가 아니라
보완이다: 마감이 측정을 열고, 누진이 그 안에서 기울기를 키운다.**

---

## 8-20. 체크포인트 — 문서 전수 열람 후 현황과 계획 (2026-08-04 05:3x)

`MASTERPLAN.md` `missions.md` `masterplan2/3.md` `ebatch/gbatch/hbatch.md` `non-codex.md`
`OPERATING.md` `ROBOT_DEPLOY_E0_GUIDE.md` `MISSION_DEPLOY_AUDIT.md` 및 ibatch 전문(2096행)
열람. 코드는 `goal_pose.py` `eval_goal_pose.py` `make_v7_arms.py` `utils/terrain.py` 확인.

### (A) ⛔ 지형은 폐기 대상이 아니다 — 다만 **눈감고 판정될 참이었다**

사용자 동기(실기에서 발을 안 들어 걸림)는 **문서에 그대로 있고 근거도 맞다.**

```python
# goal_pose.py:694 — 모서리 하나라도 지면 1 cm 이내면 '접촉'
feet_contact = any(feet_edge_pos[:,2] - terrain_heights(...) < 0.01)
# goal_pose.py:1046 — feet_swing은 '스윙 중 비접촉'에만 지불
```

**평지에서는 1.1 cm만 들면 `feet_swing`(+3.0) 만점이다.** 그 이상 들 이유가 보상에 없다.
지형 요철은 그 1.1 cm를 걸리게 만들어 **높이를 지정하지 않고** 더 들게 한다.

폐기된 것은 **`I2b_terrain`이라는 시험**이지 지형이 아니다:

| | I2b_terrain | **I3_rough** |
|---|---|---|
| 거칠기 | ±10 cm, 10 cm 격자 → 국소 경사 ~45° (**자갈밭**) | **±2 cm** (RoboCup 인조잔디) |
| 종류 | random 50% + discrete 50% (**등반**) | random 100% |
| DR | 미교정 `base_com ±0.1` (§8-10이 허구로 판정) | **교정 ±0.025** |
| I2a_dr과의 차이 | 지형 + DR 4개 (**교란**) | **지형 5줄뿐** |

> **`I3_rough`가 지형 질문의 유일한 유효 시험이고, 이미 구현돼 있고, 지금 돌고 있다.**

**⚠️ 그런데 저장소 전체에 발 클리어런스 측정이 없다.**
`grep -rn "clearance|feet_height|swing_height"` → **`make_v7_arms.py` 주석 2줄이 전부.**
보상도, 지표도, eval 컬럼도 없다.

그리고 §8-12b가 확정한 I3_rough 기각 조건은 **"strict 2%p 하락"** — 그건 **정확도**지
지형이 존재하는 이유가 아니다. **지형을 켜고 끄면서 정작 발 높이는 한 번도 안 본다.**

이번 배치에서 반복해 잡은 패턴이다: 낙상 게이트(측정 불가한 것으로 판정), I2b(자기
시험지), strict(heading에 둔감), 활용률(속도가 아닌 것). **이번엔 실행 전에 잡았다.**

**처방 — I3_rough 판정 전에 클리어런스를 심는다(비용 0, GPU 불필요):**

```
goal_pose.py: feet_edge_pos가 이미 매 스텝 계산된다(:693).
  self.feet_clearance = min(모서리별 edge_z - terrain_h)   # (num_envs, 2)
eval_goal_pose.py: 스윙 구간의 peak clearance를 구간별로 적재
  -> swing_clearance_median / p10
판정: I2a_dr(평지 학습) 대비 I3_rough의 클리어런스가 올라갔는가.
      안 올랐으면 strict가 통과해도 지형은 목적을 달성 못 한 것이다.
```

### (B) "마감"이 뭔가 — 용어 정리

**이미 존재하는 장치다. 새로 만드는 게 아니다.**

```yaml
commands:
  resampling_time_s: [4., 8.]    # 목표가 4~8초마다 새로 뽑힌다
```

목표가 다시 뽑히면 그 구간은 끝난다. 그래서 **그 시간이 사실상의 제한시간 = 마감**이다.

**요구속도 = 거리 ÷ 마감.** 지금은 2.25 m ÷ 6 s = **0.375 m/s**다. 로봇은 이미 1.45 m/s를
낸다 — **과제가 요구하는 것의 4배.** 그래서 더 빨리 갈 이유가 없다.

**"마감을 조인다" = 그 구간에 주는 시간을 줄인다.**

| | 거리 | 마감 | 요구속도 | 못 가면 |
|---|---:|---:|---:|---|
| 현재 | 2.25 m | 6 s | 0.375 m/s | (항상 도착) |
| 거리만 늘림 | 3.5 m | 6 s | 0.58 m/s | (거의 항상 도착) |
| **sprint** | **3.5 m** | **2.5 s** | **1.4 m/s** | **`goal_reached` 0** |

`resampling_time_s`는 지금 **전역**이다. 카테고리별로 뽑으려면 `goal_pose.py:511`을
`cat`(:444)에 조건화하면 된다 — **같은 함수 안이라 몇 줄이다.**

### (C) R4 설계 확정 — 마감과 누진 둘 다

**둘은 역할이 다르고 상호배타가 아니다.**

- **마감**: 속도를 *필요하게* 만든다. 요구속도 분포(d/T)를 올려 **측정도 가능**하게 한다
- **누진**: 그 안에서 *기울기를 키운다*. 요구속도는 안 바꾸므로 측정 문제는 못 푼다

```
R4a = R3승자 + sprint 카테고리(15%) + 카테고리별 마감 + 누진 goal_reached
R4b = R4a - 누진            (누진의 기여를 분리)
```

**sprint 카테고리 명세:**

| 항목 | 값 | 근거 |
|---|---|---|
| 비중 | 15% | §8-11 시뮬: 요구속도 p90을 1.34로 올림. 나머지 85%는 현행이라 P1 분포 유지 |
| 거리 | 2.5–4.0 m | 현 상자(최대 2.5 m) 밖 → `goal_dx/dy`를 카테고리별로 확장 |
| **마감** | **d/1.4 + 1.5 s** | 요구속도 1.4 m/s. +1.5 s는 도착 후 체류 여유(전부-아니면-전무 절벽 방지) |

**누진 `goal_reached`:** 현재 공 안에서 `+1/step` 평탄. 체류시간에 비례해 누진하면
빠른 도착의 이득이 초선형이 된다. 계산: 현재 과제에서 빠름/느림 차이 **16% → 34%**.

> **⚠️ 누진의 위험(실행 전 명시):** 체류가 길수록 step당 보상이 커지면 **도착 후
> 눌러앉는 것**이 상대적으로 더 유리해진다. 이동 자체를 회피하는 방향으로 새면 P2에
> 역효과다. **`R4b`(누진 없음)를 대조군으로 두는 이유가 이것이다.**

### (D) 그리고 §7이 약속한 계측 3종이 안 심겼다

§7 비고 *"계측만 추가: 전환 지표(7-6), 단일지지 생존시간, 발 하중분배"* — **비용 0이라
적어놓고 셋 다 없다.** `grep`으로 확인. 여기에 **발 클리어런스**를 더해 넷이다.

R3(안정화 우선)·I3_rough 판정에 전부 필요하다. **GPU를 안 쓰므로 학습과 병렬로 넣는다.**

---

## 8-21. 라운드별 eval 사양 확정 — 다음에 코드 다시 안 읽어도 되게 (2026-08-04 06:0x)

`eval_goal_pose.py`(3900행) `goal_pose.py` `select_best_checkpoint.py` `make_v7_arms.py`
`utils/terrain.py` 전수 확인. report.json 최상위 지표 **147개**를 뽑아 라운드별로 매핑했다.

### 공통 규칙 — 이걸 어겨서 두 번 헛돌렸다

| 규칙 | 근거 |
|---|---|
| `--config`는 **arm 자기 `sweeps/<arm>.yaml`** | `envs/K1/Goal_Pose_V7.yaml`을 주면 **24개 키**가 바뀐다(§8-16) |
| 바닥은 **`--terrain plane`**으로만 통일 | config 교체는 과제까지 바꾼다 |
| 외력은 **`--force_profile heldout`**으로만 통일 | 각자 자기 외란으로 채점 금지(§8-9) |
| 선택기에 **`--max_iteration 200`** | 라운드 설계값. 안 주면 오래 돈 arm은 tail만 뽑혀 **레버가 아니라 추가 학습**을 잰다 |
| 1순위 판정은 **과제오차** `sqrt(d²+r²θ²)` | strict·위치 median은 heading에 둔감(§8-14, §8-17) |
| 낙상은 **순위 제외, 보고만** | 노출 없는 개수는 판정 불가(§8-12a). 비교는 `force_ab` 다중 seed로 |

### 라운드별 판정 지표와 코드 상태

| 라운드 | 판정 지표 (report.json 경로) | 코드 |
|---|---|---|
| **I3_rough**<br>(지형) | **`swing_apex_m.p10` / `.median` / `.share_below_0p02`** ← 1차<br>`success_rate_strict`, 과제오차 ← 대가 | ✅ **방금 심음**<br>`goal_pose.py:703` `feet_clearance`<br>`eval_goal_pose.py:1322` 스윙 정점, `:2330` 요약 |
| **R3a**<br>캘리브레이션 DR | held-out bias/offset 열화폭<br>+ clean 비열세 | ⚠️ **기전은 이미 있다**<br>`goal_pose.py:358-364, 568, 805`<br>→ **config 키만 추가**(`randomization.joint_encoder_bias` / `joint_target_offset`)<br>❌ IMU 회전은 env 코드 필요 |
| **R3b**<br>지연 DR 20–60 ms | held-out 지연 열화폭 | ❌ **env 코드 필요**<br>현재 `delay_steps`(`:247,:372,:581`)는 **제어주기 안 0–18 ms**,<br>에피소드마다 고정. 20–60 ms는 제어주기 1–3개분 →<br>과거 타깃 버퍼가 있어야 한다 |
| **R4**<br>속도 | `per_start_distance.required_speed_p90` ← **과제가 요구했나(설계검증)**<br>`body_speed.sustained_1p3.cruise_median_s` ← **정책이 했나**<br>`swing_apex_m` ← **빨라지며 발을 덜 드는지 감시** | ❌ env 코드 3곳<br>sprint 카테고리 `goal_pose.py:448` `probs`<br>카테고리별 마감 `:516`<br>누진 `goal_reached` `:868` |
| **R5**<br>goal 불확실성 | bias/jitter **분리** 열화폭<br>+ 속도 유지 게이트(L4) | ✅ **config만**<br>`noise.goal_pos_bias` / `goal_pos`를 0.30–0.50으로<br>(현재 전부 0 — `_OFF_ROBUST` 때문) |

### I3_rough 기각 조건 — §8-12b를 개정한다

§8-12b는 *"strict 하락 2%p 초과면 기각"*만 정했다. **그건 정확도지 지형이 존재하는
이유가 아니다.** 클리어런스를 심었으므로 조건을 둘로 만든다:

```
채택 : swing_apex_m.p10 이 I2a_dr 대비 상승  AND  strict 하락 <= 2%p
기각 : 클리어런스가 안 올랐으면 strict가 통과해도 기각
       -> 지형이 대가 없이 아무것도 안 산 것이므로 켤 이유가 없다
보류 : 클리어런스는 올랐는데 strict가 2%p 넘게 빠짐
       -> 거래다. 실기 걸림 위험 대 정확도로 사용자 판단
```

**p10을 median보다 앞에 둔다.** 걸림은 최악값 사건이라 **낮은 스윙이 얼마나 뜨는지**가
위험이고, 평균 스윙 높이가 아니다.

### 아직 안 심긴 계측 (§7이 "비용 0"이라 적어놓은 것)

| 계측 | 용도 | 상태 |
|---|---|---|
| 발 클리어런스 | 지형 판정 | ✅ **심음** |
| 전환 지표(7-6) | R3 "불안정 시 안정화 우선" | ❌ |
| 단일지지 생존시간 | 낙상 원인 국소화 | ❌ |
| 발 하중분배 | 낙상 원인 국소화 | ❌ (`contact_force`는 있으나 좌우 분배 아님) |

낙상 27건 중 3건만 외력 중이었다(§8-13) — **남은 낙상의 원인은 아직 아무 지표도 못 짚는다.**
아래 셋이 그걸 짚을 후보다.

---

## 8-22. 낙상은 더 캐지 않는다 — 셀 수 없는 것을 잴 수 있는 것으로 바꾼다 (2026-08-04 06:3x)

**[질문] 낙상률이 매우 낮은데 원인 규명에 시간을 쓸 값어치가 있나**

**[답변] 없다. 그리고 §7의 "계측 3종"도 그 명분으로는 정당화되지 않는다.**

I2a_dr의 clean 낙상은 **13,922구간에 4회**다. 경기 기준으로 환산하면:

```
10분 하프 ≈ 구간 100개  ->  0.029 falls/half  ->  약 34 하프(17경기)에 1회
```

**sim에서 이걸 0으로 미는 것은 값어치가 없다.** 이유 셋:

1. **측정 비용이 이득을 넘는다.** "0"을 95%로 주장하려면 무낙상 8,300구간이 필요하다
   (rule of three). 그걸 얻어도 실기 낙상률은 여전히 모른다.
2. **sim 낙상률은 전이되지 않는다.** 실기 낙상은 sim에 없는 것에서 온다 — 관절 zero
   틀어짐(사용자 보고), IMU 장착 오차, 지연, 마찰 편차, 다른 로봇과의 접촉.
3. **실기에서 한 번도 안 돌려봤다.** 실기 낙상 데이터가 0인 이유는 시행이 0이라서다
   (`missions.md` 5–6번: `.pt` export·로봇 복사·E0 smoke 미완).

### 그래서 방향을 바꾼다

**희귀 사건의 답은 그 사건을 더 모으는 것이 아니라, 그 전에 움직이는 연속량이다.**
§8-12a에서 게이트를 폐기한 이유가 바로 낙상이 희귀해서였다. 같은 문제를 계측으로 푼다.

| 심은 것 | 무엇인가 | 왜 낙상 개수보다 나은가 |
|---|---|---|
| **`foot_support.single_support_s`** | 단일지지 구간 길이 분포 (median/p90/p99) | 균형 여유를 쓴 시간. **매 발걸음마다 표본이 생긴다** |
| **`foot_support.*_share`** | 비행/단일/양발 시간 비율 | 보행 구조 자체의 변화를 본다 |
| **`foot_support.load_asymmetry`** | 양발 지지 중 `\|L−R\|/(L+R)` | 여유를 한쪽으로만 쓰는지. **좌우 편향의 연속 지표** |
| **`swing_apex_m`** (§8-21) | 스윙 정점 높이 | 걸림 위험 |

구간당 수천 표본이 나온다. **낙상 4회로 못 하던 순위 매기기를 이걸로 한다.**

구현: `eval_goal_pose.py:1339` 지지상태 추적, `:2404` 요약.
`disturbance_eval["support"]`(외력 종류)와 이름이 겹치지 않게 **`foot_support`**로 명명.
단일지지 구간 길이 로직은 합성 시퀀스로 단위검증했다(5스텝→0.10 s, 3스텝→0.06 s).

### 보류: 전환 지표(7-6)

7-C(불안정 시 안정화 우선)를 위한 것인데, **"불안정"의 정의가 7-C 설계와 함께 나와야 한다.**
지금 내가 정의를 지어내면 나중에 기능과 안 맞는다. **7-C 설계 시 같이 정한다.**

---

## 8-23. R3a 캘리브레이션 DR — 값의 출처와 한계 (2026-08-04 06:4x)

**[질문] 캘리브레이션 값을 찾아라**

전 문서 탐색 결과, **수치는 두 곳에 있고 실측은 어디에도 없다.**

| 출처 | 값 | 상태 |
|---|---|---|
| `hbatch.md:138` `M2_jointdr` | encoder **±0.015 rad**(±0.86°) / target **±0.010 rad**(±0.57°) | **제안일 뿐. 실행된 적 없다** (server-dump에 M 셀 로그 없음) |
| `ibatch.md:504` (사용자 지시에 대한 답변) | IMU 장착 회전 roll/pitch **±1–2°**, yaw **±3°** | 설계만, 미구현 |

**실기에서 zero 틀어짐을 측정한 기록은 없다.** `ROBOT_DEPLOY_E0_GUIDE.md`
`MISSION_DEPLOY_AUDIT.md` `STATE_ESTIMATION.md` 전수 확인.

> **사용자 보고("joint zero가 꽤나 자주 틀어져 posture가 비정상이 된다")가 유일한 근거다.**
> **posture가 눈에 띄게 비정상이 되려면 ±0.86°보다 클 가능성이 높다.** 그래서 `hbatch`의
> ±0.015는 **하한으로 의심된다.**

**가장 값싼 실측 방법 (사용자만 가능):** 다음 zero calibration을 할 때 **캘리가 적용한
보정량 자체를 기록**한다. 그것이 곧 그동안 누적된 틀어짐이다. 관절별로 한 번만 적어두면
DR 범위가 추측에서 실측으로 바뀐다.

### 그리고 랜덤화의 천장을 명시한다 (§ibatch:487의 재확인)

**bias는 관측에 없다. 그래서 정책은 어느 방향으로 틀렸는지 알 수 없다.**
랜덤화가 주는 것은 *offset 보정*이 아니라 **"어떤 offset에서도 안 넘어지는 보수적 자세"**다.
보수적 자세는 §8-17에서 본 대로 속도·정확도를 깎을 수 있다 — **R3a는 clean 비열세를
반드시 함께 봐야 한다.**

**근본 해법은 배포단 zero 추정이다**(§ibatch:495): 양발 접지·정지 상태에서 기구학으로
몸통 자세를 풀면 관절 zero 오차가 **관측 가능**해진다. 학습이 아니라 배포 루틴이고,
랜덤화보다 근본적이다. **§6에 등재돼 있으나 아직 아무도 손대지 않았다.**

---

## 8-24. 지형은 나중에 합친다 — base 승계 규칙 (2026-08-04 06:4x)

**[질문] `_I3_BASE`에 ±2 cm 지형은 안 넣기로 한 거야? 나중에 합칠 계획?**

**나중에 합친다. 채택되면.** 이 프로젝트의 base 승계 규칙 그대로다:

```python
_I1_BASE = merge(_OFF_PATH, _OFF_ROBUST, _OFF_PROTECT, 발교정 + h055)
_I2_BASE = merge(_I1_BASE, _I1_FORCE)   # R1이 외란을 채택 -> 합침
_I3_BASE = merge(_I2_BASE, _I2_DR)      # R2가 DR을 채택 -> 합침
                                         # <- I3_rough가 지형을 채택하면 _I4_BASE로
I3_rough = merge(_I3_BASE, {terrain 5줄})
```

**레버는 단일 arm으로 시험하고, 이긴 것만 다음 base에 합친다.** 지형을 지금 base에 넣으면
"지형 때문인가 다른 것 때문인가"를 영영 못 가른다 — I2b가 정확히 그래서 실패했다(§8-16).

채택 조건은 §8-21에서 개정한 그대로: **클리어런스 p10 상승 AND strict 하락 ≤2%p.**

---

## 8-25. P2 판정 — 충족이다. 과제가 물어본 적이 없었을 뿐 (2026-08-04 07:5x)

`--goal_pattern forward_hold`, I2a_dr model_150, 120 s × 256 envs, **30,499구간**.
목표를 로컬 2 m 앞에 0.8–1.2 s마다 다시 놓아 **도착이 없는 연속보행**을 만들었다.

| | 값 |
|---|---|
| **몸통속도 median** | **1.48 m/s** |
| p90 / p99 / max | 1.64 / 1.77 / **2.21** |
| **1.0 m/s 초과 체류** | **98.3%** (정상 과제에서는 3.1%였다) |
| 구간최고 median / p90 | 1.61 / 1.74 |

**P2(1.3–1.5 m/s) 충족이다.** median 1.48이 밴드 안이고, 시간의 98.3%를 1.0 이상으로 간다.
과도구간이 아니라 **순항**이다.

> **§8-14~8-19에서 "판정 불가"라고 한 것이 이것으로 닫힌다.** 원인도 확정된다 —
> 정책은 처음부터 할 수 있었고, **과제가 요구한 적이 없었다**(요구속도 최대 0.62 m/s).

### ⚠️ 순항 0.86 s는 정책의 한계가 아니라 프로브의 상한이다

`cruise_1p3`는 구간 경계에서 리셋된다(`:1805`). 이 프로브의 실측 구간 길이는
**30,720 env-s / 30,499 = 1.007 s**이고, 관측된 최댓값이 **1.18 s**로 resample 상한
1.2 s에 붙어 있다. **즉 0.86 s는 "한 구간의 86%"이지 "0.86초밖에 못 버틴다"가 아니다.**
읽는 도구가 이제 구간 길이를 같이 찍는다.

### 스윙 정점 — 지형 논거가 생각보다 약하다

| | 값 |
|---|---|
| p10 / median / p90 | **4.6 / 6.2 / 7.6 cm** |
| 2 cm 미만 | **0.6%** |
| (참고) `feet_contact` 판정선 | 1 cm |

**평지 학습 정책이 이미 median 6.2 cm를 든다.** 지형 arm의 논거는 *"`feet_swing`이
1.1 cm에서 만점이라 더 들 이유가 없다"*였는데, **보상이 요구하지 않아도 정책은 들고 있다.**
RoboCup 인조잔디 ±1–2 cm에 대해 2 cm 미만 스윙이 0.6%뿐이다.

> **다만 이 값은 1.48 m/s에서 잰 것이다.** 빠르게 걸으면 발이 높이 올라가는 것은
> 당연하다. **정상 과제(요구 0.375 m/s)의 클리어런스는 아직 모른다** — 그게 I3_rough를
> 판정할 진짜 대조군이고, `cruise_i2a` 3 seed는 계측 이전이라 값이 없다.

### ⛔ 예상 못 한 발견 — 양발 지지가 하중을 나누지 않는다

| | 값 |
|---|---|
| 비행 / 단일지지 / 양발 | 0.5% / **90.2%** / 9.3% |
| 단일지지 구간 median / p90 / p99 | 0.22 / 0.50 / 0.90 s |
| **하중 비대칭 `\|L−R\|/(L+R)`** | **median 0.99, p90 0.99** |

**"양발 접지" 9.3% 동안에도 하중의 99%가 한 발에 있다.** 즉 로봇은 사실상 **시간의
99%를 한 발로** 버틴다. 체중 이동이 거의 순간적이고, **타이밍 오차를 흡수할 양발 구간이
없다.**

원인은 `feet_contact`의 정의다 — **"모서리 하나라도 지면 1 cm 이내"는 근접 판정이지
하중 판정이 아니다.** 닿았지만 안 실린 발이 접촉으로 세어진다.

**이건 sim2real 위험이다.** 실기는 지연·바닥 편차·캘리브레이션 오차를 양발 구간에서
흡수하는데, 그 구간이 없다. §8-13에서 낙상 27건 중 24건이 외력과 무관했던 것도 여기서
설명될 수 있다.

> **단, 1.48 m/s에서 잰 값이다.** 빠른 보행은 양발 구간이 짧은 게 정상이다.
> **정상 과제 속도에서 같은 값을 재기 전에는 결함으로 단정하지 않는다.**

### 다음 한 수 (6분, GPU 1개)

**정상 과제 프로토콜로 같은 계측을 돌린다.** 이 한 번이 세 개를 동시에 준다:

1. **I3_rough 판정의 클리어런스 대조군** — 지금 없는 그것
2. **정상 속도에서의 하중 비대칭** — 위 발견이 결함인지 고속의 성질인지
3. 단일지지 구간 분포의 기준선

---

## 8-26. 정상 과제 기준선 — 지형은 살고, 하중은 R4의 비용이 된다 (2026-08-04 08:2x)

`logs/base_i2a0` = I2a_dr model_150, 정상 과제, `--terrain plane`, clean, 120 s.

### 검산 먼저 — 계측 추가가 시뮬을 건드리지 않았다

| | force_ab clean_seed0 | base_i2a |
|---|---|---|
| 위치 / heading / 과제오차 | 2.62 / 1.84 / 4.15 | **2.62 / 1.84 / 4.15** |
| 낙상 / 구간 | 2 / 4,651 | **2 / 4,651** |

**완전히 동일하다.** 롤아웃 루프에 넣은 계측이 RNG 소비나 물리를 바꾸지 않았다는 뜻이고,
오늘 추가한 지표 전체에 대한 가장 강한 검증이다.

### ① 스윙 정점 — 지형의 논거는 살아 있다

| | 정상 과제 | 고속(1.48 m/s) |
|---|---:|---:|
| p10 | **2.6 cm** | 4.6 cm |
| median | **3.0 cm** | 6.2 cm |
| 2 cm 미만 | 0.2% | 0.6% |
| 3 cm 미만 | **41.3%** | 1.0% |

**정상 속도에서 절반으로 떨어진다(6.2 → 3.0 cm).** 인조잔디 ±1–2 cm에 대해 최악 10%
스윙의 여유가 **0.6–1.6 cm**뿐이고, **41.3%가 3 cm 미만**이다.

2 cm 미만 0.2%는 500걸음에 1회 = 초당 2걸음이면 **약 4분에 1회**다. 10분 경기에 2–3회.

> **`steady_i2a`만 봤으면 "6.2 cm면 충분"으로 잘못 결론 낼 뻔했다.** 고속에서 발이 높이
> 올라가는 것은 당연하고, 판정은 **정상 속도**에서 해야 한다. → **I3_rough 계속 진행.**

### ② 하중 비대칭 — 고속의 성질이었다. 그리고 R4의 예고된 비용이다

| | 정상 과제 | 고속 |
|---|---:|---:|
| 양발 접지 비율 | **46.1%** | 9.3% |
| 하중 비대칭 median | **0.56** | **0.99** |
| 단일지지 구간 median | 0.14 s | 0.22 s |

**정상 속도에서는 실질 양발 구간이 46%이고 하중도 나뉜다(0.56 ≈ 78:22). 구조적 결함이
아니다.** §8-25에서 결함으로 단정하지 않은 것이 옳았다.

**그러나 1.48 m/s에서는 0.99다.** R4가 성공해 로봇이 빨라지면 **그 한발 지지 영역으로
들어간다.** → **R4 판정에 `foot_support`를 필수로 포함한다.**

### ③ 도구 결함 — 가드가 읽던 필드가 존재하지 않았다

`show_probe`의 forward_hold 가드가 `results["commands"]`를 봤는데 **그 키는 없다** —
`commands`는 `effective_protocol` 안에만 있고 그건 해시만 남는다. 그래서 가드가 한 번도
걸리지 않았고, `steady_i2a`에 **위치 55.66 cm / strict 0.0%**가 찍혔다. 막으려고 만든
숫자가 그대로 나온 것이다.

**수정:** `summarize`가 `goal_pattern`과 `sampled_commands`를 results에 남긴다. 옛 리포트는
구간 길이로 판별한다(정상 4–8 s vs 프로브 0.8–1.2 s). 세 형식으로 검증했다.

---

## 8-27. R4 설계 — 실측으로 다시 계산했더니 §8-11의 수치가 불가능했다

### ⛔ 먼저: "sprint 목표속도 1.2–1.6 m/s"는 물리적으로 불가능하다

§8-11은 요구속도 `d/T`만 계산하고 **그 값이 달성 가능한지를 보지 않았다.**
실측(가속도 1.67 m/s², 기동 사구간 0.40 s, 순항 1.48 m/s)으로 **정지→정지 평균속도**를
계산하면:

| 거리 | 최소 시간 | **평균속도** | 순항 시간 |
|---:|---:|---:|---:|
| 2.5 m | 2.98 s | **0.84 m/s** | 0.80 s |
| 3.5 m | 3.65 s | **0.96 m/s** | 1.48 s |
| 4.0 m | 3.99 s | **1.00 m/s** | 1.82 s |

**P2의 1.48 m/s는 순항 속도이고, 평균은 가감속·사구간 때문에 훨씬 낮다.**
요구속도를 1.2–1.6으로 잡으면 **도달 불가능하고, 정책은 sprint 구간을 통째로 포기하는
법을 배운다.**

### 설계 — 마감을 실측 안에서 고른다

```
sprint 구간:  거리 d ~ U(2.5, 4.0) m
              마감 T = d / v_req + 1.0 s,   v_req ~ U(0.85, 1.05) m/s
```

`+1.0 s`는 **절벽 방지**다. 마감을 딱 맞추면 체류가 0이라 보상이 이분법이 된다.

계산된 판별력 (d = 3.5 m):

| v_req | 마감 | 전력 주행 체류 | avg 0.80 주행 체류 |
|---:|---:|---:|---:|
| 0.85 | 5.12 s | 1.47 s = **73 step** | 0.34 s = 17 step |
| 0.95 | 4.68 s | 1.03 s = **52 step** | **0 — 실패** |
| 1.05 | 4.33 s | 0.68 s = 34 step | **0 — 실패** |

**v_req 범위가 "느려도 되는 구간"부터 "전력이어야 하는 구간"까지 걸친다** — 학습 신호가
있으면서 압력도 있다. 현재 과제(빠름 159 vs 느림 138 step, 16% 차이)와 비교하면
**차이가 3~5배로 벌어진다.**

> **주의: `required_speed_p90`은 ~0.75로 읽힐 것이다**(마감에 +1.0 s가 붙으므로 `d/T`가
> `v_req`보다 낮다). §8-11이 목표로 삼은 1.34는 이제 설계 기준이 아니다. **판정은
> sprint 구간의 도착률과 body_speed로 한다.**

### arm 구성

| arm | 처치 |
|---|---|
| **R4a_sprint** | `_I4_BASE` + sprint 카테고리 15% + 카테고리별 마감 |
| **R4b_sprint_prog** | R4a + **누진 `goal_reached`**(체류시간에 비례) |

sprint의 거리와 마감은 **분리 불가능한 하나의 레버**다 — 거리만 늘리면 요구가 안 생기고
(§8-19), 마감만 조이면 갈 거리가 없다. bundle임을 명시하고 간다.

**누진을 따로 뺀 이유:** 체류가 길수록 step당 보상이 커지면 **도착 후 눌러앉는 것**이
유리해져 이동 회피로 샐 수 있다. R4a가 대조군이다.

### 구현 위치 (전부 `_resample_commands` 한 함수 안)

| 무엇 | 위치 |
|---|---|
| `sprint`를 `probs`에 추가 | `goal_pose.py:448` |
| sprint 전용 거리 추첨 | 같은 블록 (현재 `goal_dx/dy`는 전역) |
| 카테고리별 마감 | `goal_pose.py:516` (`cmd_resample_time`) |
| 누진 `goal_reached` | `goal_pose.py:868` |

### 판정 지표 (§8-21 사양에 추가)

| 지표 | 무엇을 답하나 |
|---|---|
| sprint 구간 **도착률** | 과제가 요구한 것을 정책이 해내는가 |
| sprint 구간 **body_speed** | 실제로 빨라졌는가 |
| **`foot_support.load_asymmetry`** | **속도가 양발 구간을 죽이는가** ← §8-26 |
| **`swing_apex_m.p10`** | 빨라지며 발을 덜 드는가 |
| 정상 구간 과제오차 | **비열세** — 85%는 그대로여야 한다 |

---

## 8-28. §8-27 정정 — 산수는 맞았고 대응이 틀렸다 (2026-08-04 08:5x)

§8-27은 "요구속도 1.2–1.6이 불가능하다"를 근거로 **목표를 0.85–1.05로 낮췄다.**
산수는 맞지만 **대응이 틀렸다. 목표를 낮출 게 아니라 거리를 늘렸어야 한다.**

### 왜 평균이 순항보다 낮은가 — 그리고 언제 수렴하는가

가감속에 **고정으로 1.31 m + 2.17 s**가 든다(사구간 0.40 + 가속 0.886 + 감속 0.886).
거리가 짧으면 그 고정비가 전부를 먹는다:

| 거리 | 평균속도 | 순항 시간 | **순항 비중** |
|---:|---:|---:|---:|
| 2.5 m | 0.84 | 0.80 s | **27%** |
| 3.5 m | 0.96 | 1.48 s | 40% |
| 6.0 m | 1.12 | 3.17 s | 59% |
| **9.0 m** | **1.22** | **5.19 s** | **71%** |
| 12 m | 1.28 | 7.22 s | 77% |

**평균 1.2 m/s는 8.2 m부터 가능하다.** (1.4는 33 m, 1.6은 순항 1.48로 영영 불가.)

**§8-27이 잡은 2.5–4.0 m는 순항이 구간의 27–46%뿐인 거리다.** 그 안에서 목표를 낮추는 건
**임의의 상자 안에서 최적화한 것**이고, 상자 자체를 의심했어야 했다.

### 실현 가능성 — 확인했다

| 제약 | 확인 |
|---|---|
| env 간 충돌 | `create_actor(..., i, ...)`의 collision group이 **env 인덱스** → 서로 통과. 9 m 보행 무해 |
| `episode_length_s` | **30 s** → 11 s 구간이 들어간다 |
| `env_spacing 1.0` | 격자 배치일 뿐, 충돌이 없으므로 제약 아님 |
| ⚠️ trimesh 지형 | **유한 패치다.** 지형 arm과 sprint를 합칠 때 패치 크기를 반드시 확인할 것 |

### 개정 설계

```
sprint 구간:  거리 d ~ U(6.0, 9.0) m        <- RoboCup 필드 길이가 9 m다
              마감 T = d / v_req + 1.0 s,  v_req ~ U(0.90, 1.10) m/s
              추첨 비중 10%  ->  전체 시간의 14%
```

판별력 (d = 9 m):

| v_req | 마감 | 최소시간 | 전력 체류 | avg 0.90 체류 |
|---:|---:|---:|---:|---:|
| 0.90 | 11.00 s | 7.37 s | **182 step** | 30 step |
| 1.00 | 10.00 s | 7.37 s | **132 step** | **0 — 실패** |
| 1.10 | 9.18 s | 7.37 s | 91 step | **0 — 실패** |

**v_req가 "6배 기울기"부터 "절벽"까지 걸친다.** 현재 과제의 16% 차이와 비교가 안 된다.

### 이게 P2를 실제로 여는가

sprint가 전체 시간의 14%, 그 중 71%가 순항 → **전체 시간의 약 10%를 1.3 m/s 이상으로**
보낸다. 현재는 1.0 초과가 3.5%다. **과제 위에서 P2가 처음으로 측정 가능해진다.**

**추첨 비중을 15%가 아니라 10%로 낮춘 이유:** sprint 마감이 길어(8.5–11 s) 추첨 10%가
이미 시간의 14%다. 15%면 20%가 되어 정상 과제 분포를 그만큼 밀어낸다 — P1 비열세를
지키려면 시간 점유율로 보는 것이 맞다.

---

## 8-29. §8-28도 기각 — 실기는 2 m 넘는 목표를 보낸 적이 없다 (2026-08-04 09:0x)

§8-28은 "sprint 거리 6–9 m"를 제안했다. **실기 입력 경로를 확인하지 않고 낸 설계다.**
`missions.md:406-450`:

```
2. 2 m 이내면 waypoint 자체를 carrot으로 쓴다.
3. 2 m보다 멀면 waypoint 방향으로 정확히 2 m인 radial carrot을 쓴다.
6. deploy가 x±2 m / y±1.5 m를 per-axis clamp한 뒤 E0 observation에 넣는다.
```

Brain이 robot frame에서 `policy_envelope(2.0 × 1.5)` 안으로 **방향 보존 스케일링**을 하고,
deploy가 한 번 더 clamp한다. **실기가 정책에 주는 goal_rel은 구조적으로 |x| ≤ 2.0,
|y| ≤ 1.5를 넘을 수 없다.**

`normalization.goal_pos = 0.5`이고 obs clip이 없으므로 9 m 목표는 obs 4.5다 — **실기가
절대 만들 수 없는 값을 학습시키는 것**이다.

### 그리고 더 중요한 것 — `forward_hold`는 인공 프로브가 아니었다

실기의 동작은 **"진행 방향 2 m에 carrot을 놓고, 로봇이 다가가면 다시 2 m 앞으로"**다.
`forward_hold`가 한 것이 정확히 그것이다(`goal_dx = [2,2]`, 0.8–1.2 s마다 재배치).

> **즉 §8-25의 1.48 m/s는 인공 조건이 아니라 배포 조건에서 잰 값이다.**
> **P2는 실기가 실제로 만드는 입력 분포에서 충족된다.**

### 그래서 R4의 전제가 바뀐다

| | 학습 과제 | **배포 과제** |
|---|---|---|
| 목표 | 고정 waypoint, 도착 후 정지 | **2 m carrot, 계속 갱신** |
| 요구속도 | 최대 0.62 m/s | 연속 보행 |
| 실측 body speed | median 0.02, 1.0 초과 3.5% | **median 1.48, 1.0 초과 98.3%** |

**학습과 배포가 구조적으로 다른 과제이고, 정책이 운 좋게 일반화하고 있다.**
`path`(전진하는 lookahead)가 배포와 같은 모양인데 §P7에서 학습에서 뺐다.

**따라서 R4는 "긴 거리 sprint"가 아니라 "배포 조건을 학습에 넣기"다.** 그리고 그것은
**정의상 분포 안**이다(목표가 항상 ≤2 m).

### 이 분석이 뒤집는 것과 뒤집지 않는 것

**뒤집힌 것 — 전부 미실행 설계다:**

| 항목 | 상태 |
|---|---|
| §8-11 sprint (2.5–4 m, 목표 1.2–1.6) | 기각 — 그 거리에서 달성 불가 |
| §8-27 sprint (2.5–4 m, v_req 0.85–1.05) | 기각 — 임의의 상자 안 최적화 |
| §8-28 sprint (6–9 m) | **기각 — 실기가 만들 수 없는 입력** |

**뒤집히지 않은 것 — 측정과 판정은 전부 유효하다:**

R0/R1/R2 판정, §8-12a 선택기 수정, §8-14 비닝, §8-19 활용률 철회,
**§8-25 P2 충족(1.48 m/s — 오히려 근거가 강해졌다)**, §8-26 기준선(클리어런스 3.0 cm,
하중 0.56), 지형 논거.

**설계 3건이 죽었고 측정은 0건이 죽었다.**

### 내 잘못

§8-27과 §8-28을 **배포가 뭘 보내는지 확인하지 않고** 냈다. 그 확인은 `missions.md`
grep 한 번, 2분이었다. 순서가 거꾸로였다 — 실기 입력 분포가 학습 과제 설계의
**제약 조건**인데 마지막에 봤다.

---

## 8-30. 쇄신 계획 — 관절 영점이 이 프로젝트의 최대 위험이다 (2026-08-04 09:1x)

### jointcal3 결과 — 대각선 두 칸

| | 과제오차 | strict | 낙상 | 2 cm 미만 스윙 |
|---|---:|---:|---:|---:|
| I2a_dr, **drift OFF** | 4.15 cm | 94.9% | 2 / 4,651 | **0.2%** |
| jointcal3, **drift ON(±3°)** | 5.59 cm | 83.9% | **12 / 4,641** | **6.4%** |

**낙상 0.43 → 2.59/1000 = 6.0배 (단측 p = 0.0064). 클리어런스 2 cm 미만은 32배.**

> 이 비교에는 '정책 차이'와 'drift 유무'가 섞여 있어 **DR의 이득/대가는 아직 판정 못
> 한다**(2×2의 나머지 두 칸이 없다). 그러나 방향은 명확하다 — **±3° 관절 영점 오차가
> 걸린 상태는 안 걸린 상태보다 크게 나쁘다.**

**새 발견: 영점 오차가 발 클리어런스를 무너뜨린다.** 발 위치 추정이 틀리니 스치는
스윙이 32배가 된다. **지형(±2 cm)보다 영점 오차(±3°)가 걸림 위험에 훨씬 크게 기여한다.**

### 그래서 위험 순위를 다시 매긴다

| 위험 | 실측 근거 | 크기 |
|---|---|---|
| **관절 영점 오차** | 낙상 6배, 클리어런스 32배 | **최대. 그리고 사용자가 "꽤나 자주" 발생한다고 보고했다** |
| 지형 요철 | 2 cm 미만 스윙 0.2% (평지 학습) | 중간 — 4분에 1회 수준 |
| held-out 외력 | 낙상 무증가(§8-13) | **작다 — 5,080회 맞고도 변화 없음** |
| 속도 | 배포 조건에서 1.48 m/s(§8-25) | **위험 아님 — P2 충족** |

**이 순위는 오늘 처음 실측으로 매겨졌다.** R 트리는 외력 → DR → 지형 → 속도 순이었는데,
실측은 **영점이 압도적**이라고 말한다.

### 쇄신 계획

**1. R4(속도)를 보류한다.** P2는 배포 조건에서 충족됐다(§8-29). 설계를 세 번 냈고 세 번
   기각됐는데, 그 이유는 **애초에 풀 문제가 아니었기 때문**이다. 재개 조건: 실기에서
   속도가 실제로 부족한 것이 관측될 때.

**2. 2×2를 닫는다 (12분).** DR이 이득인지 대가인지. 이것만이 "랜덤화로 영점을 흡수할 수
   있는가"라는 사용자 질문에 답한다.

**3. ⭐ 실기 영점 오차를 측정한다 — 사용자만 가능하고, 가장 값싸고, 가장 중요하다.**
   다음 zero calibration 때 **보정량을 관절별로 기록**한다. 그것이 누적된 실제 오차다.
   지금 ±3°는 근거 없는 가정이고, **이 프로젝트 최대 위험의 크기를 아무도 모른다.**

**4. 배포단 영점 추정을 검토한다(§ibatch:495).** 랜덤화는 방향을 모르므로 **보정이 아니라
   보수적 자세**만 준다. 양발 접지·정지 상태에서 기구학으로 몸통 자세를 풀면 영점 오차가
   **관측 가능**해진다. 위험이 이 정도라면 이게 학습보다 근본적이다.

**5. 실기에 올린다.** 정확도 2.6 cm도, 낙상 0.43/1000도, 속도 1.48 m/s도 **실기에서 한 번도
   확인된 적이 없다.** 위 1–4의 우선순위도 결국 실기 데이터로만 검증된다.

### R 트리 개정

```
보류  R4 속도            <- P2 충족. 실기에서 부족이 관측되면 재개
진행  2x2 닫기 (12분)    <- DR 이득/대가
진행  I3_rough 지형 판정  <- 클리어런스 기준선(3.0 cm / 2cm미만 0.2%) 확보됨
대기  실기 영점 실측      <- 사용자 작업. 이게 없으면 DR 범위가 계속 추측이다
검토  배포단 영점 추정    <- 랜덤화보다 근본적일 수 있다
최우선 실기 배포          <- 검증이 0인 상태
```

---

## 8-31. 실기 "발이 모인다" — 정책 성질이고 sim이 그대로 재현한다 (2026-08-04 09:4x)

실기 증상: 잘 서 있다가 정책이 인계받으면 **다리가 모이면서 발광하고, 세 걸음 만에
발끼리 부딪혀 넘어진다.** 목표 0.6 m 전진.

### 소프트웨어를 전부 배제했다

| 확인 | 결과 |
|---|---|
| 관측 54차원 정렬 | ✅ sim과 완전 일치 (0:3 gravity / 3:6 ang_vel / 6:16 commands / 16·17 gait / 18:30 dof_pos / 30:42 dof_vel / 42:54 actions) |
| `default_qpos` | ✅ 일치 (Hip_Pitch −0.2, Knee 0.4, Ankle_Pitch −0.25, 나머지 0, 좌우 대칭) |
| 정책 실행 게인 | ✅ 일치 (Hip/Knee 100/2, **Ankle 50/1**). 250은 prepare 전용 |
| `feet_offset_x/y_target` 채널 | ✅ 학습 `[0,0]`, deploy `0.0` |
| gait clock / 주파수 | ✅ 적분기+클램프, 2.0 Hz는 sim 범위 [1.8, 2.4] 안 |
| `goal_clamp` | ✅ 2.0/1.5로 체크포인트에 맞게 동결 |

### 그리고 sim이 증상을 그대로 냈다

| | 값 |
|---|---:|
| **sim 발 간격(양발 접지 중)** | **p10 7.0 / median 7.0 / p90 11.5 cm** |
| **발 폭** (`feet_edge_pos` y ±0.035) | **7.0 cm** |
| `feet_distance_ref` | 18.0 cm |
| **실기 실측** | **19–21 cm** |

**정책은 발 중심간 7.0 cm로 걷는다. 발 폭이 정확히 7.0 cm다 — 좌우 발이 맞닿는
간격이다.** 실기는 19–21 cm에서 시작해 정책이 7 cm로 끌어당기고, 도달하기 전에 부딪힌다.

**즉 관절 영점도 지연도 아니다. 정책이 원래 그렇게 걷는다.**

### 원인 — 아무도 발을 벌리라고 하지 않았다

```yaml
feet_offset_x: -0.        # disabled at milestone 2
feet_offset_y: -0.        # disabled at milestone 2
```

**좌우 간격을 요구하는 보상이 꺼져 있다.** 켜진 발 관련 보상은 `foot_yaw_L/R`,
`feet_yaw_diff/mean`, `feet_roll/pitch`, `feet_slip`, `feet_swing` — **전부 각도와 접촉이지
간격이 아니다.** 좁은 자세는 좌우 CoM 이동을 줄여 `feet_slip`과 에너지를 아끼므로,
평지·무지연 sim에서는 이득만 있고 대가가 없다.

`self_collisions: 0`(=활성)이라 sim에서도 충돌은 모델되지만, 정책은 그 간격을 실로 꿰듯
통과한다. 실기는 지연·바닥 편차·영점 오차가 겹쳐 통과하지 못한다.

### 처방 — 기계는 이미 다 있다

```python
_reward_feet_offset_y  (goal_pose.py:1035)  # 구현돼 있고 scale만 0
commands[:, 9] = feet_offset_y_target       # 관측 채널 살아 있음
feet_distance_ref: 0.18                     # 기준값 있음
feet_offset_vel_scale_y: 0.3                # 횡속도 감쇠도 있음
```

**`rewards.scales.feet_offset_y`를 켜기만 하면 된다.** 단, 현재 보상 함수는 **오차를
그대로 돌려주므로**(부호 없는 `clip(|error|, 0, 0.1)`) **scale은 음수여야** 벌점이 된다.

**⚠️ 이건 R4(속도)보다 우선한다.** 지금 실기에서 로봇을 넘어뜨리고 있는 원인이고,
sim에서 재현되므로 **sim에서 고칠 수 있다.**

---

## 8-32. R3b 판정 — 자세 폭을 고쳤고 목적함수가 오히려 좋아졌다 (2026-08-04 10:1x)

`_I3_BASE + rewards.scales.feet_offset_y` 단일 레버, 200 iter, 120 s × 256 envs.

| | I2a_dr (실기 탑재분) | **stance10 (−10)** | stance30 (−30) |
|---|---:|---:|---:|
| **발 간격 p10** | **7.0 cm** | **16.5** | 17.0 |
| **발 간격 median** | **7.0 cm** | **17.5** | 18.0 |
| **과제오차** | 4.15 | **3.82** | 3.89 |
| 위치 median / p90 | 2.62 / 4.52 | **2.53** / 4.72 | 2.84 / 5.31 |
| heading | 1.84° | **1.64°** | 1.52° |
| strict | **94.9%** | 91.3% | 84.8% |
| 낙상 | 2 / 4,651 | **2 / 4,647** | 5 / 4,644 |
| **하중 비대칭** | 0.56 | **0.37** | 0.41 |
| 양발 접지 | 46.1% | **51.4%** | 51.8% |

### 사전 고정 기준 — stance10 전부 통과

- 발 간격 median 18 근처 → **17.5** ✅
- **p10 > 발 폭 7 cm → 16.5** ✅ **발끼리 부딪힐 수 없다**
- 과제오차 4.15 대비 10% 이내 → **3.82, 8% 개선** ✅
- 낙상 비열세 → 2 vs 2 ✅

**대가 없이 고쳐졌다.** 위치와 heading이 둘 다 좋아졌다.

### 덤 — §8-26의 우려가 같이 풀렸다

**하중 비대칭 0.56 → 0.37**, 양발 접지 46.1% → 51.4%. "실질 양발 구간이 없어 지연·바닥
편차를 흡수할 여유가 없다"고 했던 것을 **넓은 자세가 같이 고쳤다.** 좁은 자세가
원인이었던 셈이다.

### 짚어둘 것 둘

**① strict −3.6%p (94.9 → 91.3).** 원인은 위치 p90(4.52 → 4.72)이다. 게이트(p90 ≤ 10 cm)는
여유로 통과하고 median은 오히려 좋아졌으므로 **5 cm 문턱 근처의 꼬리만 이동**했다.
§8-14의 strict 둔감함이 반대 방향으로 나타난 경우다.

**② 클리어런스가 약간 나빠졌다** (3 cm 미만 41.3% → **51.3%**). 넓게 서면 발을 덜 든다.
**I3_rough 지형 판정의 기준선을 이 값으로 갱신해야 한다** — 옛 기준선(41.3%)으로 재면
지형의 효과를 과대평가한다.

**stance30 기각.** 목표(18.0)는 정확히 맞췄지만 위치 2.84, strict 84.8%, 낙상 5,
속도까지 떨어졌다. 과하다.

### 스케일 산정이 맞았다

첫 제안 −0.5는 per-step −0.001로 `constellation` +0.070 옆에서 무의미했을 것이다.
실측으로 다시 잡은 −10(= goal_reached 크기)이 정확히 맞았고, −30(= feet_swing 크기)은
과했다. **보상 스케일은 다른 항의 실제 기여와 나란히 놓고 정해야 한다.**

---

## 8-33. L 배치 — 처음으로 스크리닝이 아닌 학습 (2026-08-04 10:4x)

GPU 2장 × 10시간 = **카드당 약 13,000 iteration**(실측 2.3–2.7 s/it). 지금까지 전 라운드가
E0 warm start에서 **200 iteration**이었다. 그건 "레버 방향이 맞나"를 묻는 예산이지
"어디로 수렴하나"가 아니다.

### 왜 지금 긴 실행이 값어치가 있나

**① H 배치의 12,000 iteration 붕괴는 원인이 밝혀졌다.** §3-1의 asset 오염과 §8-10의
`base_com ±0.1` 허구. 둘 다 고쳐졌으므로 **"여기서는 긴 학습이 나빠진다"는 물려받을 게
아니라 다시 시험할 주장**이다.

**② 케이던스는 200으로 판정할 수 없다.** 보행의 정의 파라미터를 바꾸는데, I1c는
고정 2 Hz 정책에서 warm start해 200 iteration 안에 stride 타이밍을 다시 배우라고 요구했다.
그 낙상 악화는 `watch_eval` 자신의 docstring이 경고하는 **재적응 과도구간**과 같은
모양이다 — I1b는 125·150에서 선을 넘고 175에서 깨끗했다. **12,000은 "케이던스가 나쁘다"와
"케이던스가 아직 안 내려앉았다"를 처음으로 구분할 수 있는 예산이다.**

### 두 arm — `cadence_coupling` 하나만 다르다

```
_L_BASE = _I3_BASE + feet_offset_y −10 (R3b 채택) + base_height_target 0.52 + save_interval 250
L1_long     = _L_BASE
L2_cadence  = _L_BASE + commands.cadence_coupling.enabled
```

생성해 대조했다: **L2 vs L1 = `cadence_coupling` 단 하나.**

**`base_height_target` 0.55 → 0.52는 레버가 아니라 결함 교정이라 양쪽에 다 넣는다.**
URDF 실측으로 엉덩이→발바닥 0.4607 m, Trunk→엉덩이 0.062 m이므로 **최대 높이가 0.5227 m**다.
0.55는 도달 불가이고, −20 페널티가 만족될 수 없어 **포화점 없는 "더 서라" 압력**만 남는다.
덤으로 `cadence_coupling`의 `max_stride_m 0.28`이 *"~0.55 × leg length (base_height_target
0.52)"*로 사이징된 값이라, 0.52 복귀가 그 전제도 되돌린다.

`save_interval` 25 → **250**: 12,000이면 480개가 쌓이는데 선택기는 어차피 tail 12개만 쓴다.

### 안전장치 — 10시간 무인 실행에 필수

`watch_eval`이 20 s 스크리닝을 돌려 `STOP` 파일을 쓰고, `runner.py:341`이 **체크포인트를
저장한 뒤 멈춘다.** 기준은 실행 전에 고정한다:

```
--ref 0.0253   (I3b_stance10의 위치 median)   --stop-ratio 1.3   --strikes 2
```

`--strikes 2`가 중요하다. **한 번 선을 넘었다고 죽이면 안 된다** — I1a·I1c·I1d가 iteration
50의 단일 판독으로 죽었고, I1b는 같은 과도구간을 걸어 나왔다.

> ⚠️ stance10 학습에는 `watch_eval`이 안 붙어 있었다(`train_v7.py`만 띄우면 별도 프로세스라
> 자동으로 붙지 않는다). **10시간짜리에 이게 없으면 8시간을 헛돌 수 있다.**

### 평가 계획 (실행 전 고정)

| # | 무엇 | 비용 |
|---|---|---|
| 0 | **높이 주장 검증** — stance10에 `trunk_height_m` 실측 | 6분, 발사 전 |
| 1 | 각 arm 체크포인트 선택 `--terrain plane --max_iteration` 없음(긴 실행이므로 tail 사용) | 35분 ×2 |
| 2 | `show_probe` — stance/높이/클리어런스/지지 | 즉시 |
| 3 | `force_ab` 짝지어, 3 seed, `--out_run` 공유 | 36분 ×2 |
| 4 | `forward_hold` 프로브 — 정상상태 속도 | 6분 ×2 |

**판정 기준:**

| 지표 | L1 합격선 | L2(케이던스) 합격선 |
|---|---|---|
| 과제오차 | stance10의 **3.82 cm 이하** | L1 이하 |
| **몸통 높이(이동중)** | 목표 0.52 대비 처짐 **감소** | L1 이하 |
| 낙상 | 2/4,647 비열세 | **L1 비열세** ← I1c가 무너진 지점 |
| 발 간격 | 16 cm 이상 유지 | 유지 |
| **body_speed p90** | — | **L1보다 상승** ← 케이던스의 존재 이유 |
| 클리어런스 p10 | 2.5 cm 이상 | 유지 |

**L1이 stance10보다 나쁘면** 긴 학습이 여전히 해롭다는 뜻이고, 그 자체가 답이다 —
**200 iteration 방법론을 유지할 근거**가 된다.

---

## 8-34. L 배치 1차 — 셋 다 정지, 그리고 기준이 틀렸다 (2026-08-04 16:5x)

`watch_eval`이 셋 다 죽였다. 안전장치는 작동했으나 **기준값이 잘못돼 있었다.**

### L1 체크포인트 정식 프로토콜 (120 s)

| | 위치 | 과제오차 | strict | 낙상 |
|---|---:|---:|---:|---:|
| **stance10 @200** | **2.53** | **3.82** | 91.3% | **2** |
| L1 @500 | 2.84 | 4.02 | 86.1% | **18** |
| L1 @1000 | 2.82 | 4.32 | 91.8% | 11 |
| L1 @1500 | 2.94 | 3.97 | 88.3% | 10 |
| L1 @2500 | 4.09 | 4.91 | 69.1% | 11 |

**stance10 @200이 모든 L1 체크포인트보다 낫다.** 스크리닝의 "1000에서 strict 92.3%"는
정식 프로토콜에서 91.8%로 사라졌고 낙상은 5배다 — **20초 스크리닝으로 순위를 매기면
안 된다**는 것을 다시 보여준다.

### ⛔ 그러나 "긴 학습이 나쁘다"로 읽으면 안 된다

**낙상이 @500에서 이미 18회다.** 점진적 악화가 아니라 **시작부터 튀어 있다.** 이건
학습 길이가 아니라 **발 질량 +29%(0.383 → 0.494 kg)에 대한 재적응**의 모양이다.

그리고 **`watch_eval --ref 0.0253`이 틀린 기준이었다.** 0.0253은 **옛 발 관성**에서 나온
값인데, 발이 29% 무거워진 로봇에게 그 기준을 들이대고 2500에서 죽였다. 새 동역학에는
새 기준이 필요하다.

L1c(순수 대조: stance10 config 무변경 연장)가 it 825에서 pos 0.0267 / strict 92.8%로
**안 무너지고 있다.** 같은 시점 L1은 이미 나빴다. 자산 변경이 원인이라는 방향이다.

### ⛔ 그리고 정책은 기억이 없다 — 영점 보정이 구조적으로 불가능하다

`utils/model.py`: `Linear(54→256→128→128→12)`. **순환도 프레임 스택도 없다**
(`num_observations: 54` = 단일 프레임).

**정책은 `dof_pos + bias`를 보지만 `bias`를 절대 못 본다.** 서로 다른 bias가 같은 순간
관측을 만들면 구분할 방법이 없다. **랜덤화가 줄 수 있는 것은 보정이 아니라 "어떤
offset에서도 안 넘어지는 보수적 자세"뿐이고, 이건 논증이 아니라 구조적 사실이다.**

§8-23에서 "방향을 모른다"고 쓴 것의 근거가 이것이다.

---

## 8-35. L1 vs L1c — 200 iteration은 너무 짧았고, 발 관성 교정은 대가를 치른다 (2026-08-04 20:2x)

같은 iteration, 같은 120 s 프로토콜, 같은 seed. 차이는 **발 관성 교정 + `base_height_target`
0.52** 뿐이다.

| iter | **L1** (관성 +29%, height 0.52) | **L1c** (stance10 config 무변경) |
|---:|---|---|
| 500 | 4.02 cm / 86.1% / 낙상 18 | **3.50 / 96.2% / 6** |
| 1000 | 4.32 / 91.8% / 11 | **3.25** / 94.6% / 6 |
| 1500 | 3.97 / 88.3% / 10 | 3.38 / 94.2% / 11 |
| 2000–2500 | 4.91 / 69.1% / 11 | 4.36 / 85.4% / 11 |
| *@200 기준* | *3.82 / 91.3% / **2*** | *동일* |

### ① L1의 악화는 학습 길이가 아니라 자산 변경이다

**같은 iteration에서 L1c가 전부 이긴다.** @500에서 낙상 18 vs 6, strict 86.1% vs 96.2%.

§8-34에서 `--ref 0.0253`(옛 발 관성의 값)으로 L1을 2500에 죽인 것은 **틀린 판단**이었다.
발이 29% 무거워진 로봇을 옛 자산의 기준으로 채점한 것이고, 이 프로젝트가 반복해 온
"학습한 적 없는 조건으로 채점" 부류다. `L4_settle`이 그 재적응이 결국 이득으로 돌아서는지를
12000까지 본다 — 기준은 L1 자신의 @500(0.0284), `--stop-ratio 1.6 --strikes 4`.

### ② ⛔ 200 iteration은 최적점이 아니었다 — 방법론 수정

| | @200 | **@500** | **@1000** | @1500 | @2000 |
|---|---:|---:|---:|---:|---:|
| 과제오차 | 3.82 | **3.50** | **3.25** | 3.38 | 4.36 |
| strict | 91.3% | **96.2%** | 94.6% | 94.2% | 85.4% |
| 낙상 | **2** | 6 | 6 | 11 | 11 |

**`L1c @1000`이 이 프로젝트가 만든 최고 정책이다** — 과제오차 **3.25 cm**, stance10보다 15%
좋다. **최적 구간은 500–1500이고 200은 너무 짧았다.**

§7이 "라운드당 200"으로 고정한 근거는 *"H 열화가 iteration 100에 이미 확정적"*이었는데,
그 H는 오염된 asset 위에서 돌았다(§3-1). **전제가 무너졌으므로 200도 무너진다.**

### ③ 정확도와 낙상이 반대로 간다

낙상은 @200이 2회로 최고이고 @1000에서 6, @1500부터 11이다. **P1이 낙상 0을 최우선으로
두므로 최종 채택은 이 교환을 명시적으로 판단해야 한다** — 지금까지는 과제오차만 보고
골라 왔다.

---

## 8-36. L1c 체크포인트 전수 — 최적은 500–1750, 낙상은 따로 논다 (2026-08-04 20:4x)

`sweeps/I3b_stance10.yaml` 무변경 연장. 전부 120 s, plane, seed 0.

| iter | 위치 cm | **과제오차** | strict | **낙상** | 발간격 | 이동중 높이 |
|---:|---:|---:|---:|---:|---:|---:|
| *@200* | *2.53* | *3.82* | *91.3%* | ***2*** | *17.5* | *0.500* |
| 500 | **2.30** | 3.50 | **96.2%** | 6 | 17.5 | 0.510 |
| 750 | 2.32 | 3.69 | 93.9% | 6 | 18.0 | 0.510 |
| **1000** | 2.41 | **3.25** | 94.6% | 6 | 18.0 | 0.510 |
| 1250 | 2.89 | 3.60 | 90.0% | 6 | 18.0 | 0.510 |
| 1500 | 2.57 | 3.38 | 94.2% | 11 | 17.5 | 0.510 |
| **1750** | 2.54 | 3.73 | 94.0% | **3** | 18.0 | 0.510 |
| 2000 | 3.18 | 4.36 | 85.4% | 11 | 18.0 | 0.510 |

### 정확도: 500–1750이 고른 고원, 2000에서 절벽

과제오차 3.25–3.73, strict 90–96%가 **500부터 1750까지 평평하다.** 그 안의 순위(1000이 최저)는
**단일 seed의 잡음**으로 봐야 한다 — 1250이 3.60, 1500이 3.38로 오르내린다. **@2000에서만
확실히 무너진다**(4.36 / 85.4%).

**§7의 "라운드당 200"은 이 고원 시작 전에 끊는 값이었다.** 근거였던 *"H 열화가 iteration
100에 확정적"*의 H는 오염된 asset 위에서 돌았으므로(§3-1) 전제가 성립하지 않는다.

### ⛔ 낙상은 정확도와 독립이다 — 그리고 @200이 유일하게 낮다

낙상 2 → 6 → 6 → 6 → 6 → 11 → **3** → 11. **고원 구간 내내 6 안팎**이고 1750에서 3으로
떨어졌다가 2000에서 11이다. 정확도 곡선과 상관이 없다.

**@200의 낙상 2는 다른 체크포인트 어디에서도 재현되지 않는다.** §8-12a에서 확인했듯 이
노출(4,650구간)에서 2 vs 6은 통계적으로 약한 차이지만(Poisson 95% 구간이 겹친다),
**7개 체크포인트가 전부 3 이상**이라는 것은 우연으로 보기 어렵다.

**P1이 낙상 0을 최우선으로 두므로 채택은 이 교환을 명시해야 한다:**

```
정확도 우선  -> 500-1750 아무 곳 (과제오차 -12~-15%, 낙상 3배)
낙상 우선    -> @200 유지        (과제오차 3.82, 낙상 2)
```

높이(0.510)와 발간격(17.5–18.0)은 전 구간에서 안정적이다 — **8-32의 stance 수정과 8-33의
높이 진단이 학습을 더 해도 유지된다.**

### 다음: force_ab로 낙상을 제대로 잰다

단일 seed 낙상 수로는 판정할 수 없다(§8-12a). `L1c@1000` vs `stance10@200`을 **3 seed ×
clean/force = 12셀**로 돌린다. 노출이 12배가 되므로 2 vs 6이 실재하는지 갈린다.

---

## 8-37. force_ab 12셀 완료 — 낙상 차이는 실재한다 (2026-08-05 19:0x)

`L1c@1000` vs `stance10@200`, 3 seed × clean/force. 노출이 arm당 **27,680 / 27,672 구간**으로
사실상 동일하다.

| arm | mode | 과제오차 cm (median [min,max]) | heading° | 낙상 합 | /1000 |
|---|---|---|---|---|---|
| L1c@1000 | clean | 3.36 [3.25, 3.45] | 1.25–1.37 | 20 / 13,819 | 1.45 |
| L1c@1000 | force | 3.40 [3.30, 3.43] | 1.27–1.39 | 13 / 13,861 | 0.94 |
| stance10@200 | clean | 3.86 [3.82, 3.90] | 1.64–1.75 | 10 / 13,862 | 0.72 |
| stance10@200 | force | 3.83 [3.82, 3.85] | 1.65–1.71 | 6 / 13,810 | 0.43 |

### ① 정확도 우세: 확정

L1c는 3 seed 전부에서 3.25–3.45, stance10은 3.82–3.90. **구간이 겹치지 않는다.**
heading도 마찬가지로 겹치지 않는다(1.25–1.39 대 1.64–1.75). seed 산포를 넘는 차이다.

단, 단일 seed로 봤던 **3.25는 낙관적 끝값**이었다. 3 seed median은 **3.36**이다.
§8-36에서 "@1000이 최적"이라 쓴 근거가 한 seed였다는 뜻이고, 우세폭은 13%로 줄었다.

### ② ⛔ 낙상 차이도 실재한다 — 2.06배

합산 **33 대 16**, 노출 동일. 이항 양측 **p = 0.021**.
§8-36에서 "@200만 유일하게 낮다, 노출이 부족해 판정 불가"라 유보했던 것이 **판정됐다.
정확도를 얻은 만큼 낙상을 잃는다.**

| | 과제오차 | 낙상 /1000 |
|---|---|---|
| L1c@1000 | **3.36** (13% 우세) | 1.19 (**2.06배 나쁨**) |
| stance10@200 | 3.86 | **0.58** |

이건 §8-36 ③("정확도와 낙상이 반대로 간다")을 통계로 확정한 것이다. 배포 선택은
이제 취향이 아니라 **명시적 교환**이다 — 5 mm의 도착 정확도를 낙상 2배로 산다.

### ③ 외력은 12배 노출에서도 판별력이 없다

clean 30 대 force 19 (양 arm 합산), **p = 0.152**. 방향이 오히려 반대다(외력 쪽이 덜 넘어진다).
arm별로도 p = 0.30 / 0.45. **§8-13의 "held-out 외력은 아무것도 가져가지 않는다"가
12배 노출에서 재확인됐다.** 앞으로 force_ab에 GPU를 쓰지 않는다 — seed를 늘리는 게 낫다.

---

## 8-38. ⛔ 세 arm 전부 iteration 500이 봉우리다 — 긴 학습이 틀렸다 (2026-08-05 19:1x)

`L4_settle`이 **12000까지 완주**했다(watch가 안 멈췄다). L1과 config·seed가 같아 궤적이
그대로 복제됐다(it 500 0.0278, 750 0.0283, 1500 0.0291 — 전부 일치). 즉 L4는 L1의 연장이고,
**L1이 죽은 2500 이후가 이번에 처음 관측됐다.**

| it | 500 | 750 | 1500 | 2250 | 4500 | 7500 | 9750 | 12000 |
|---|---|---|---|---|---|---|---|---|
| pos m | **0.0278** | 0.0283 | 0.0291 | 0.0376 | 0.0430 | 0.0464 | 0.0459 | 0.0396 |
| 낙상 | **1** | 1 | 2 | 0 | 10 | 49 | 12 | **63** |
| strict | 0.849 | **0.888** | 0.880 | 0.762 | 0.656 | 0.546 | 0.536 | 0.669 |

**회복하지 않는다.** 9500 iteration을 더 돌려 도착 오차 42% 악화, 낙상 63배.
"발 관성 재적응이 나중에 이익으로 돌아설 것"이라는 §8-34의 가설은 **기각**이다.

### 세 arm 전부 같은 모양이다

| arm | it 500 | 그 뒤 |
|---|---|---|
| L1_long / L4_settle | pos 0.0278, 낙상 1, strict 85% | 단조 악화 → 12000에서 0.0396 |
| L3_phasefree | pos 0.0306, 낙상 10, strict 73% | 750 0.0384 → 1000 0.0398 (낙상 40) |
| L2_cadence | pos 0.0758, **낙상 1457**, strict 20% | 750에서 0.1844 — 진짜 실패 |

**arm에 무관하다.** 보상 구성을 바꿔도(L3의 phase-free), 케이던스를 바꿔도(L2) 봉우리는
500이다. 이건 arm의 성질이 아니라 **warm start + 이 config의 성질**이다.

### ⛔ 그리고 500은 우리가 측정한 가장 이른 지점이다

`watch_eval`의 첫 평가가 500이라 그 아래를 본 적이 없다. `save_interval: 250`이므로
**model_250이 존재한다.** 봉우리가 500인지 그보다 앞인지 모르는 상태다.

관련해서 `select_best_checkpoint.py`의 `--tail_frac` 기본값 0.6은 도움말에
*"early checkpoints are never best"*라고 적혀 있다. **이번 결과가 그 전제를 반증한다.**
(다만 L1_long 11개 / L3 5개는 `max_candidates` 12 이하라 tail_frac이 물지 않는다.
48개인 L4에는 물었을 것이다.)

### 지금 도는 것

`logs/peak/L1peak`, `logs/peak/L3peak` — 각 arm 전 체크포인트를 **자기 run의 config.yaml**로
정식 프로토콜 채점(`--tail_frac 1.0`). 250이 500보다 좋은지, L3(phase-free)가 배포 가능한지를
한 번에 가른다.

### 방법론 수정

- **10시간짜리 긴 학습을 이 config로 다시 걸지 않는다.** 봉우리는 500 안쪽이다.
- `watch_eval`의 `--strikes 4` 연속 조건은 **임계 근처에서 진동하면 영원히 안 멈춘다.**
  L4는 0.0454 임계를 여러 번 넘고도 12000까지 갔다. 누적 strike로 바꾸거나
  `--max_iterations`로 직접 자르는 편이 낫다.

---

## 8-39. MuJoCo 교차검증 — 두 시뮬레이터 모두에서 안 넘어진다 (2026-08-05 20:3x)

Isaac에서 학습한 `I3b_stance10 model_200`을 **MuJoCo(3.2.3)**에서 돌렸다. 관측은 다시
유도하지 않고 **하드웨어에서 검증된** `deploy/utils/policy_goal_pose.py::GoalPosePolicy`를
그대로 적재했다 — 조립을 새로 하면 "MuJoCo에서 못 걷는다"가 정책 탓인지 내 조립 탓인지
갈리지 않기 때문이다. `K1_serial.xml`은 팔이 자유 관절이라 배포처럼 22관절 전부를 PD로
잡았고, 그래서 이 실행은 팔이 고정된 학습 sim보다 **실기에 가깝다.**

| 조건 | 구간 | 낙상 | 위치 cm | heading° | 과제오차 cm |
|---|---|---|---|---|---|
| Isaac Gym (기준) | 4,647 | 2 | 2.53 | 1.67 | **3.86** |
| MuJoCo 벤더 토크 | 72 | **0** | 5.99 | 2.22 | 6.76 |
| MuJoCo 학습 토크 | 73 | **0** | 5.99 | 2.78 | 6.70 |
| MuJoCo + 배포 필터 | 69 | **0** | 5.98 | 3.37 | 6.73 |
| MuJoCo 연속보행 60 s ×2 | — | **0** | — | — | — |

### ① 정책은 Isaac 특유의 물리를 이용하고 있지 않다

다른 엔진, 다른 접촉 모델, 팔이 자유인 상태에서 **180초 72구간 낙상 0**, 연속보행 60초도
낙상 0. 가설 (a)는 배제된다. 정확도는 3.86 → 6.76 cm로 **75 % 나빠지지만 안정성은 안 잃는다.**

### ② ⛔ 토크 상한 가설 기각

`K1_serial.xml`의 다리 forcerange는 **45/30/30/45/20/20**으로 deploy config의
`common.torque_limit`과 일치하고, 학습이 쓰는 URDF effort는 **30/20/20/40/20/15**로 낮다.
벤더 MJCF와 배포 설정이 독립적으로 같은 값을 말하므로 실기 상한은 전자로 보인다.
**그런데 둘을 바꿔 돌려도 6.76 vs 6.70, 낙상 둘 다 0이다.** 정상 보행에서 정책이 토크
상한 근처에 가지 않는다는 뜻이다. **배포 세션에 요청했던 토크 로깅(R1)은 우선순위를 내린다.**

### ③ ⛔ 배포측 저역통과 필터 가설도 기각

`deploy_goal_pose.py:1273`이 500 Hz에서 `filtered = 0.8*filtered + 0.2*target`을 건다.
tau 8.9 ms, fc 17.8 Hz, 정책 1 tick 뒤 도달률 89.3 %. **학습 sim에는 이 필터가 없다**
(`goal_pose.py:589`의 `delay_steps`는 0-18 ms 순수지연이지 1차 lag이 아니다).
학습이 모르는 지연이라 유력한 용의자였는데, 넣고 돌려도 **6.73 cm / 낙상 0**이다.

### 남은 용의자 (실기 세 걸음을 설명해야 하는 것)

두 시뮬레이터 모두에서 안 넘어진다. 그러므로 원인은 **둘 다에 없는 것**이다:

1. ⛔ **로봇에 올라간 모델이 i2a일 가능성** — `0cf9285`가 "Not yet on the robot"으로
   끝나고, "세 걸음 가고 발끼리 부딪힌다"는 그 커밋이 **i2a의 증상**으로 적은 것이다.
   가장 싸고 가장 유력하다. **이것부터 확인한다.**
2. 평행 발목 기구 — 어느 sim 자산에도 없다(`serial`/`locomotion` 차이는 팔 자유도다)
3. 실기 상태추정/IMU 품질
4. 영점 드리프트 실측 크기
5. SDK 왕복 지연의 실제 크기
6. 인조잔디 대 평면

### 방법론

**MuJoCo 교차검증이 가설 두 개를 GPU 없이, 로봇 없이, 6분 만에 기각했다.** 앞으로 실기
가설은 여기서 먼저 거른다. `python play_mujoco_goalpose.py --duration 180 --policy <pt>`.

---

## 8-40. 감지 열화 스윕 — 여유가 없는 채널은 하나뿐이다 (2026-08-05 21:1x)

§8-39가 강체 동역학을 용의선상에서 지웠다. 그리고 사용자가 논리의 구멍을 지적했다:
**MuJoCo는 정상 발로 돌았고 거기서 정책이 걸었으니, 발이 원인이면 MuJoCo에서 이미
터졌어야 한다.** 맞다. 발 관성 가설도 기각이다(오타는 실재하고 고쳐야 하지만, 증상의
원인은 아니다).

그래서 남은 것을 쟀다. **두 시뮬레이터 모두 `obs[0:6]`(projected_gravity, base_ang_vel)을
정확히 준다.** 실기에서는 IMU와 상태추정에서 온다. 열화는 정책이 읽는 값에만 걸고 물리와
낙상 판정은 참값을 썼다 — "정책이 속아서 넘어졌다"와 "실제로 기울었다"를 섞지 않기 위해서다.
연속보행(`--goal-hold`) 60초, 낙상/분.

| 열화 | 학습에 있는 값 | 무너지는 지점 | 여유 |
|---|---|---|---|
| IMU 기울기 잡음 | `noise.gravity` std 0.01 (≈0.57°) | 5° 무사 → **10°: 6/분** | **17배** |
| IMU 기울기 **바이어스** | **없음**(평균 0 잡음뿐) | **10°까지 전부 0** | 큼 |
| 자이로 잡음 | `noise.ang_vel` std 0.1 | 0.5 무사 → **1.0: 11/분** | **10배** |
| `dof_vel` 잡음 | `noise.dof_vel` std 0.1 | 0.5 무사 → **1.0: 2/분, 3.0: 22/분** | **10배** |
| **관측 지연** | ⛔ **0 ms** | 10 ms 무사 → **20 ms: 2/분, 40 ms: 49/분** | **없음** |

### ⛔ 관측 지연만 여유가 0이다

나머지 셋은 학습값의 10–17배까지 버틴다. **관측 지연은 학습이 아예 모델링하지 않는다** —
`goal_pose.py:589`의 `delay_steps`는 **액션** 쪽 0–18 ms 순수지연이고, 관측 쪽 지연은 0이다.
(`noise.goal_obs_hold_steps [2,3]`은 목표 채널에만 걸리고 IMU 채널에는 안 걸린다.)

40 ms에서 분당 49회는 **사실상 못 걷는다**는 뜻이고, ROS2 + SDK 왕복에서 20–40 ms는
흔한 값이다. 지금까지 나온 후보 중 유일하게 **크기와 방향이 둘 다 맞는다.**

### 그리고 이미 측정 수단이 배포에 있다

`deploy_goal_pose.py:1252`가 `policy_debug`에 **`low_state_age_sec`**를 10 Hz로 싣는다.
코드 수정 없이 오늘 읽을 수 있다.

⚠️ 단, `_publish_policy_debug`는 첫 줄이 `if self.goal_source_mode != "ros": return`이다.
**`fixed` 모드로 돌리면 아무것도 안 나온다.** 지금까지의 실기 시험이 전부 `fixed`였다.

⚠️ 그리고 `low_state_age`는 **수신측 노후도**다. 로봇 내부의 센서→SDK 구간은 안 잡힌다.
따라서 이 값은 실제 관측 지연의 **하한**이다.

### 다음

- M6: 관측 지연 랜덤화를 `goal_pose.py`에 넣는다. `obs[0:6]`에 링 버퍼 + per-env 지연.
  공유 파일이므로 병렬 커밋 확인과 전 경로 통독이 먼저다.
- 배포: `ros` 모드로 한 번 돌려 `low_state_age_sec`의 median/p99를 받는다.

---

## 8-41. 실기 LowState 타이밍 실측 — 전송 지연은 1 ms다 (2026-08-05 22:0x)

로봇(192.168.10.102)에 직접 붙어 쟀다. **관절을 잡지 않았다** — LowState 구독만 했으므로
CUSTOM 모드도, 사람이 옆에 서 있을 필요도 없었다.

| | median | p90 | p99 | max |
|---|---|---|---|---|
| LowState 도착 간격 | **2.00 ms** | 2.08 | 2.59 | 19.04 |
| 50 Hz 추론 시점의 나이 | **0.99 ms** | 1.77 | 2.12 | 10.12 |

**499.2 Hz, 14,074 표본 / 28.2초.**

### ⛔ 관측 지연 가설도 죽는다 (전송 구간에 한해)

§8-40의 무너지는 지점은 **25-30 ms**였다. 실측은 **1 ms**로 **25배 아래**다. p99도 2.1 ms,
최악값도 10.1 ms로 안전 구간 안이다. **오늘 네 번째로 죽은 가설이다** — 토크 상한,
배포 필터, 발 관성, 그리고 관측 지연(전송).

### 남는 단서 두 개

⚠️ **이 값은 수신측 노후도다.** 로봇 내부의 센서 → IMU 필터 → SDK 발행 구간은 안 잡힌다.
전송이 1 ms라는 것은 **네트워크와 SDK가 문제가 아니라는 뜻**이지, 정책이 보는 중력벡터가
1 ms 전의 자세라는 뜻이 아니다. 펌웨어 내부 지연은 여전히 미측정이다.

⚠️ **그리고 이 로봇은 세 걸음 증상이 난 그 로봇이 아니다.** 사용자 확인:
실기 시험은 다른 로봇에서 했다. 이 로봇에는 goal-pose 배포 스택 자체가 없다
(`deploy_goal_pose.py`, `run_e0.sh`, `goal_pose_*.pt` 전부 부재. `~/Workspace/deploy`는
오늘 17:12에 생겼고 비어 있다). 같은 SDK/펌웨어면 비슷할 가능성이 높지만 증명은 아니다.

### 방법론

**프로브가 CUSTOM 모드 없이 답을 냈다.** `low_state_age`의 지배항은 발행 주기와 지터이고
그건 관절을 잡는 것과 무관하다. 앞으로 실기 계측은 "구독만으로 되는가"를 먼저 묻는다 --
로봇을 움직이는 요청은 사람이 옆에 서 있어야 하고, 그래서 비싸다.

---

## 8-42. 야간 signature 탐색 — 증상을 재현 목표로 바꾼다 (2026-08-06 새벽)

가설을 하나씩 껐다 켜는 방식이 한계에 왔다(§8-39~41에서 여섯 개가 전부 "낙상 0"으로
기각됐다). 그래서 목표를 바꿨다: **실기 증언의 signature를 재현한다.**

### 실기 기준 (만충 배터리, 2.4초 91표본, `--log-timing`)

⚠️ **통제부터**: 실기는 2.4초인데 sim은 25-40초로 비교하고 있었다. MuJoCo를 같은
길이(91표본) 슬라이딩 창 51개로 잘라 분포를 냈다. **10개 지표 전부 p5-p95 밖**이다.
창 길이 문제가 아니라 진짜 다른 regime이다.

| 지표 | 실기 | MuJoCo 창 p5–p95 |
|---|---|---|
| Hip_Roll 폭 L/R | 29.8 / 45.4° | 5.7–6.9 / 10.0–14.0 |
| roll 액션 폭 L/R | 0.91 / 1.31 | 0.23–0.26 / 0.18–0.26 |
| **발목 roll 속도 L/R** | **7.03 / 8.87** | 2.21–2.57 / 1.85–2.09 |
| 고관절 pitch 속도 | 1.88 | 4.47–4.63 (**실기가 느리다**) |
| 발목 pitch 토크 | 6.92 | 10.12–11.05 (**실기가 낮다**) |
| 몸통 roll 폭 | 35.3° | 4.9–8.1 |
| **추종률**(명령 대비 도달) | **0.61** | 0.83–0.92 |

### 확정된 사실 넷

1. **다리가 실제로 교차한다.** 실기 관절 로그를 MuJoCo 운동학에 재생하니 좌우 발
   겹침이 **9.9%**, p1 **−16.35 cm**다(MuJoCo 2.3% / −0.41). 증언 "다리가 모이면서
   발끼리 부딪혀 넘어져"가 데이터에 있다.
2. **발은 번갈아 든다.** 발 높이차 |Δ|>2 cm가 71%(MuJoCo 76%). 이중지지 고착 아니다.
3. **추종률이 전 관절 균일하게 0.61**이다(MuJoCo 0.93). 축 특이적이 아니다.
4. ⛔ **발목 roll이 −5 W로 역구동된다**(MuJoCo −0.55/+1.17). 모터가 미는 게 아니라
   외부가 흔들고 모터가 저항한다. 추종 오차폭 110°가 명령폭 81°보다 크다.

### 레버별 재현도

| 레버 | 재현한 것 | 재현 못한 것 |
|---|---|---|
| **배포 필터 @50-70 Hz** | 추종률 0.64-0.67, 낙상 56-60/분, 한쪽 발목 역구동 −3.41 W | 발 들기 과억제(26% 대 71%) |
| **obs 지연 20 ms** | 다리 교차 9.2%, signature 최저 | 추종률 정상 |
| 다리 이득 0.35 | 낙상 44/분 | 발목 속도 |
| 접촉 강성 ↑ | — | 역구동 안 생김 |
| IMU 바이어스 10° | 점수는 좋음 | **교차를 0.4%로 없앤다** |
| 관절 영점/정책주기/토크/발관성/마찰 | — | 교차 재현 실패 |

**어느 것도 `dq_ankR`(실기 7.0/8.9)을 재현하지 못한다** — 모든 조합이 1.4-3.5다.

### ⛔ 배포 필터의 실효 주파수 — 정량적으로 맞았다

`deploy_goal_pose.py:1273`의 `filtered = 0.8*filtered + 0.2*target`은 **500 Hz 발행을
가정**하고 고른 계수다. 그런데 `_publish_cmd`는 파이썬 루프에 `time.sleep(0.001)`이고,
추론 루프는 설계 20 ms인데 **실측 25.3 ms**로 이미 밀린다.

| 발행률 | 차단주파수 | 2 Hz 감쇠 |
|---|---|---|
| 500 Hz (설계) | 17.8 Hz | 0.99 |
| 100 Hz | 3.6 Hz | 0.87 |
| **50 Hz** | **1.8 Hz** | **0.66** |

**미리 계산한 0.66, MuJoCo 실측 0.64, 실기 0.61.** 예측이 맞았다.
`--filter-hz`로 MuJoCo에서 재현되고, 그때 낙상이 0 → 60/분으로 뛴다.

**배포에 `pub_hz` 계측을 넣었다.** 배터리가 차면 바로 읽힌다 -- 실제 발행률이
50 Hz 근처면 이것이 근본 원인이다.

### 학습 쪽 대응 (전부 실물 자산 위)

- `noise.obs_delay_steps` -- 관측 지연 랜덤화를 env에 넣었다(링 버퍼, per-env,
  리셋마다 재추첨 + 히스토리 클리어). 학습이 유일하게 모델링하지 않던 축이다.
- `randomization.ankle_gain` -- 발목 이득 오차 (M9_anklegain, 0.6-1.3배)
- `M8_jointcal2` -- 관절 영점 ±2도 (실측 차동 1.29도를 덮는다)

### §8-42 이어서 — 방법론 오류 두 개를 고치고 순위가 뒤집혔다

⛔ **오류 1: 창 길이.** 폭(range)과 표류(drift)는 창 길이에 민감하다 -- 폭은 창이
길수록 커지고 표류는 길수록 상쇄된다. 실기는 2.4초 91표본인데 sim은 25-40초 **전체**로
재고 있었다. 그래서 `lag40`의 표류가 전체 적합에서 2.8도로 나왔지만 **91표본 창에서는
median 12.8도**로 실기 13.5도와 거의 같다. "표류를 아무것도 재현 못한다"가 계산 방식의
산물이었다. 창별 median으로 고쳤다.

⛔ **오류 2: 주파수 인공물.** `f_act_hipP_R = 0.30 Hz`를 지표로 썼는데, 2.4초 창의
주파수 분해능이 0.42 Hz라 스캔 하한 아래를 못 가른다 -- 표류하는 신호는 무조건 하한에
몰린다. "0.30 Hz 진동"이 아니라 "주기 없는 표류"였고, 그것이 모든 실행에서 최악 지표로
잡혀 점수를 지배했다. 표류 지표로 교체했다.

**고친 뒤 순위 (최고 점수 7.16 -> 2.76, 상위 10개가 전부 관측 지연 기반):**

| 실행 | 점수 |
|---|---|
| `lag20 + 발목roll 감쇠 0.1` | **2.761** |
| `lag20 + 발목roll 감쇠 0.2` | 3.102 |
| `lag26` 단독 | 3.646 |
| … | |
| 기본 | ~12.4 |

### ⛔ 증상이 체크포인트마다 반대다 -- 내가 뒤섞었다

첫날 증언은 **i2a**: "다리가 **모이면서** 발끼리 부딪혀서 넘어져".
오늘 밤 로그는 **stance10**: "덜덜 떨면서 다리가 **벌어져**".

| | 실기(stance10) | MuJoCo |
|---|---|---|
| 좌우 발 간격 median | **14.06 cm** | 7.47 cm |
| 벌어짐(L−R) median / 폭 | **+5.9° / 56.9°** | −0.4° / 22.6° |
| R_Hip_Roll 표류(2.4초) | **−19.6°** | +1.4° |

`feet_offset_y −10`이 i2a의 "모임"을 고쳤지만 **반대쪽으로 과했다.** 그래서
`_reward_feet_cross`를 **양방향 구간 제약**으로 만들었다(0.07 m 아래 / 0.26 m 위만 벌한다).

### 관측 지연 구현 검증 (Isaac, 실물 자산, 20초 평가)

| 지연 | 위치오차 | strict | 낙상/구간 |
|---|---|---|---|
| 0 | 5.72 cm | 36.9 % | 439 / 640 |
| **60 ms** | 4.92 cm | 32.5 % | **18,855 / 231** |

낙상 43배. **구현이 물리에 확실히 반영된다.**

### 큐 (모두 실물 자산 위)

`M9_anklegain`(진행) → `MB_obsdelay` → `M3_scratch_phase` / `M8_jointcal2` → `MA_feetcross`

---

## 8-43. ⛔ 근본 원인 후보와 그 수정 — 필터가 루프 속도에 의존한다 (2026-08-06 01:xx)

### 문제

`deploy_goal_pose.py`의 발행 루프가 관절 목표에 필터를 건다:

```python
filtered = filtered * 0.8 + target * 0.2      # 발행 루프마다 1회
```

**계수 0.2는 500 Hz 발행을 가정한 값**이고, 그때 시정수는 10 ms다. 그런데 그 루프는
파이썬이고 `time.sleep(0.001)`이라 실제 발행률이 훨씬 낮을 수 있다 -- **추론 루프는
설계 20 ms인데 실측 25.3 ms**로 이미 밀린다.

루프가 느려지면 **같은 계수가 훨씬 긴 시정수를 만든다**:

| 발행률 | 시정수 | 2 Hz 보행 감쇠 |
|---|---|---|
| 500 Hz (설계) | 10 ms | 0.99 |
| 100 Hz | 50 ms | 0.87 |
| **50 Hz** | **100 ms** | **0.66** |
| 30 Hz | 167 ms | 0.50 |

### 증거

**미리 계산한 0.66, MuJoCo 실측 0.64, 실기 실측 0.61.** 예측이 먼저였다.

MuJoCo에서 필터 발행률만 낮추면:

| | 추종률 | 낙상/분 |
|---|---|---|
| 필터 없음 | 0.93 | 0 |
| 100 Hz | 0.74 | 30 |
| **50 Hz** | **0.64** | **60** |
| 30 Hz | 0.49 | 70 |
| **실기** | **0.61** | 낙상 |

그리고 실기 로그의 명령→관절 1차 동정에서 시정수 median이 **400 ms**다(MuJoCo 84.8).
Hip_Roll 두 개는 `a > 1`로 1차 모델이 아예 안 맞는다 -- 발산/표류 중이다.

### 수정 — 루프 속도와 무관하게 만든다

```python
alpha = 1 - exp(-dt_측정 / tau_목표)        # tau_목표 = 10 ms (설계 의도)
filtered = filtered * (1 - alpha) + target * alpha
```

실측 `dt`로 계수를 매번 다시 계산하면 시정수가 고정된다. 30–500 Hz 어디서든 10 ms다.

### 수정의 효과 (MuJoCo)

| 발행률 | 고정계수(현재) | **시정수 고정(수정)** |
|---|---|---|
| 100 Hz | 추종 0.74 / 낙상 30 | 0.89 / **0** |
| 50 Hz | 0.64 / 60 | 0.88 / **0** |
| 30 Hz | 0.49 / 70 | 0.85 / **2** |

**루프가 30 Hz까지 느려져도 수정하면 아무 일도 안 일어난다.** signature 점수도
필터 없는 것과 같아진다(27.6 대 28.5).

### 남은 확인 — 로봇에서만 닫힌다

- `deploy_goal_pose.py`에 **`pub_hz` 계측**을 넣었다. `--log-timing` CSV에 실린다.
  **실제 발행률이 50 Hz 근처면 이것이 근본 원인이다.**
- 수정은 `--rate-fixed-filter`로 **기본 꺼짐**이다. 지금 동작이 하드웨어에서 시험된
  것이고 수정본은 아니므로, 같은 로그에서 A/B 해서 고르라는 뜻이다.

⚠️ **이것이 유일한 원인이라는 뜻은 아니다.** 관측 지연 20-28 ms도 독립적으로
signature를 재현하며(창 보정 후 상위 10개가 전부 지연 기반), 학습은 관측 지연을
전혀 모델링하지 않는다. 둘은 "실기 제어 루프가 학습이 가정한 것보다 느리다"는
같은 가족이고, 필터는 그중 **코드로 확인되고 코드로 고쳐지는** 부분이다.

### §8-43 이어서 — 두 축이 각각 다른 증상을 설명한다 (그리고 한 레버는 부호가 틀렸다)

| 조건 | 점수 | 다리 교차 | 발목 파워 | 낙상/분 |
|---|---|---|---|---|
| 기본 | 28.5 | 2.1 % | −0.63 / +1.47 W | 0 |
| **필터 50 Hz** | 6.5 | 4.9 % | **−0.43 / −3.23** ✅ | **60** |
| **obs 지연 20 ms** | 8.9 | **9.2 %** ✅ | +2.72 / +4.47 | 1.5 |
| 지연 + 발목감쇠 0.1 | **2.76** | 15.4 % | **+16.6 / +18.0** ⛔ | 9 |
| 필터70+지연24+감쇠 | 2.93 | 7.6 % ✅ | +3.34 / +3.47 | 54 |
| **실기** | — | **9.9 %** | **−4.95 / −5.17** | 낙상 |

⛔ **발목 감쇠 레버는 점수가 가장 좋지만 부호가 반대다.** 감쇠를 낮추면 발목이 자유롭게
흔들려 `dq`가 커지지만, 그때 모터는 **구동**한다(+17 W). 실기는 **역구동**이다(−5 W).
점수(로그비 제곱합)는 크기만 보므로 이런 경우를 못 거른다 -- **점수만 보고 채택하면 안 된다.**

**필터만이 역구동 부호를 맞춘다.** 정리하면:

- **필터 속도 의존** → 추종률 결손, 낙상, 발목 역구동
- **관측 지연** → 다리 교차, Hip_Roll 표류

둘 다 "실기 제어 루프가 학습이 가정한 것보다 느리다"이고, 각각 수정이 있다:
`--rate-fixed-filter`(MuJoCo에서 증상 제거 확인)와 `MB_obsdelay`(학습 중).

---

## 8-44. ⛔ 근본 원인 확정 — 우리 과제는 보행을 요구한 적이 없다 (2026-08-07 03:xx)

§8-43까지의 후보들(필터 속도 의존, 관측 지연)은 **실기에서 왜 무너지는가**를 설명한다.
이 절은 그보다 앞선 질문에 답한다: **왜 정책은 애초에 걷는 법을 배우지 않았는가.**

### 측정

배포된 정책(`I3b_stance10`)과 M 배치 9개 arm 전부가
`goal_mode_mixture: {waypoint: 1.0, path: 0.0}` 위에서 돌았다. `_OFF_PATH`가
`_I1_BASE`부터 상속돼 한 번도 꺼진 적이 없다. 그 과제를 평가 출력에서 직접 재면:

| 항목 | 값 | 출처 |
|---|---|---|
| 목표까지 거리 median | 0.74 m | 4,647 구간 |
| 0.5 m 미만 구간 | **42 %** | 〃 |
| **요구 속도 median** | **0.119 m/s** | `feasibility.required_speed_median` |
| 요구 속도 p90 | 0.312 m/s | 〃 |
| **실제 몸통속도 median** | **0.03 m/s** | `body_speed.median` |
| 순항(고속) 체류 | **0.59 %** | `cruise_share_of_valid` |
| 위치오차 median | **2.84 cm** ✅ | `pos_err_m.median` |
| 낙상 | 4 / 4,657 ✅ | `gates.falls` |

**평가의 절반이 정지 상태인데 게이트는 통과였다.** 2.84 cm라는 좋아 보이는 숫자가
거기서 나왔고, 그 정책은 실기에서 세 걸음을 못 갔다.

베이스 config의 주석이 이미 이렇게 경고하고 있었다 — *"waypoint … measured required
speed was median 0.116 / max 0.574 m/s over 4647 segments, so it can never teach or
measure a 1.3 m/s walk."* 우리는 그 문장을 읽고도 그 과제만 돌렸다.

### 왜 이것이 남은 제약을 전부 만족시키는가

- **서 있기는 완벽하다** — 과제의 42 %가 사실상 서 있기다. 가장 많이 연습한 것이다.
- **보행은 1–3 걸음에서 무너진다** — 지속 보행을 요구받은 적이 없다.
- **남들은 된다** — 속도를 요구하는 과제로 학습한다.
- **E1(path 모드)로 학습한 사람은 걷는다.**
- **sim 지표는 좋았다** — 평가가 학습과 **같은 과제**라 지표가 보행을 잰 적이 없다.

### 판정과 조치

이것은 §8-43의 두 축(필터·지연)을 **대체하지 않는다.** 저쪽은 실기 고유의 열화이고
이쪽은 학습 과제의 결손이다. 둘 다 참일 수 있고, 지금 증거로는 둘 다 참이다.

**N 배치**(2026-08-07 03:45 착수, 6000 iteration, warm start = 배포된 정책 그 자체):

| arm | N0 대비 레버 | GPU |
|---|---|---|
| `N0_ctrl` | (대조군) path 꺼짐 | 1 |
| `N1_path` | `goal_mode_mixture` 0.65/0.35 | 0 |
| `N2_pathgrid` | N1 + `speed_grid.enabled` | 0 |
| `N3_pathcross` | N1 + `feet_cross -20` | 0(대체) |

config diff로 **레버가 정확히 하나씩임을 확인**했다. 공통 결함 정정 두 가지가
모든 arm에 들어간다(레버가 아니다):

- `base_height_target` 0.55 → **0.52**. 실물 URDF(`K1_robot_boxfoot`)를 직접 재면
  Trunk→Hip_Pitch 0.077, Hip→발링크 0.4367, 발바닥은 링크원점 아래 0.024이므로
  **다리를 완전히 편 최대 신장이 0.5377 m**다. 0.55는 도달 불가능하고
  `square(h − target)`은 포화점이 없으므로 −20 스케일의 "더 서라"가 영구히 남는다.
  무릎을 펴는 방향이고 보행에 정확히 해롭다. L 배치가 같은 정정을 했는데 M 배치가
  `_I3_STANCE_BASE`로 리베이스하면서 잃었다.
- `save_interval` 25 → 100.

### path 모드가 실제로 켜졌다는 확인 (텐서보드, iteration 300)

과거에 "고쳤다고 생각했는데 코드에 안 닿았다"를 여러 번 냈으므로 config diff로
끝내지 않고 런타임 신호로 확인했다.

| 보상 항 | N1_path | N0_ctrl | 비 |
|---|---|---|---|
| `goal_reached` | **1.36** | **9.85** | **0.14×** ← carrot이 달아나니 도착이 드물다 |
| `feet_slip` | −0.70 | −0.35 | 2.0× |
| `power` | −6.83 | −4.51 | 1.5× |
| `torques` | −5.71 | −4.43 | 1.3× |
| `constellation` | 91.8 | 99.9 | 0.92× |

N1이 같은 시간에 **더 많은 기계적 일을 한다** — 속도 수요가 실제로 생겼다는 signature다.

### 평가 쪽 수정 — 지금까지의 판정 전부가 한 축을 안 봤다

`tools/eval_round.sh`가 모든 arm을 **두 프로토콜**로 잰다:

1. **정확도** — 모든 arm을 `sweeps/N0_ctrl.yaml` 하나로. arm마다 자기 config로
   채점하면 비교가 안 된다(v7 배치가 E1을 path 과제로, E0을 waypoint 과제로 재고
   순위를 매긴 전례).
2. **지속 보행** — `--goal_pattern forward_hold`. 목표를 항상 2 m 앞에 다시 두어
   도착을 없애면 `body_speed`가 과도구간이 아니라 지속 속도가 된다. 이 패턴은
   자기가 `goal_mode_mixture`를 waypoint 1.0으로 덮으므로 그 자체가 공통 프로토콜이다.

`tools/round_table.py`가 둘을 한 표로 합친다. 없는 키는 0이 아니라 `-`로 찍는다 —
`results["commands"]`라는 없는 키를 읽어 프로브 가드가 한 번도 발동하지 않은 전례가 있다.

### ⛔ 이 라운드에서 잃은 것 — GPU 13.8시간

08-06 14:05에 큐 투입 명령이 한 번 실패했고(도구 오류), 재시도하지 않은 채
08-07 03:44까지 **두 장 다 0 %로 놀았다.** 원인은 스케줄러가 아니라 "다음에 확인하겠다"에
의존하는 구조였다. 그래서 감시를 코드로 옮겼다:

- `tools/idle_watch.sh` — 30초마다 세 신호(util 이력 / 살아있는 학습 프로세스 / 큐 깊이)를
  교차확인해 `queue/idle_state.json` 갱신. **한 번의 `nvidia-smi`는 유휴의 증거가 아니다**
  (epoch 경계에서 0 %가 찍힌다).
- `tools/autopilot.sh` — 유휴 30분이면 `queue/plan/gpu<N>/`의 다음 작업을 큐로 승격.
  30분을 기다리는 이유는 그 30분이 결과를 보고 다음을 정할 기회이기 때문이다.
  상태 파일이 180초 이상 낡으면(감시자 사망) 승격하지 않는다.
- `tools/round_status.sh` — ssh 한 번으로 읽는 요약. 감시자가 죽었으면 직접 샘플로 대체한다.

---

## 8-45. 관측에 무엇을 더할 수 있는가 — 정책은 자기 발을 본 적이 없다 (2026-08-07 04:xx)

§8-44가 **과제**(P1)를 고쳤다면 이 절은 **관측**을 본다. 실기 문제의 나머지 갈래
P2("제어 루프가 학습 가정보다 느리다" — 다리 교차, Hip_Roll 표류, 발목 역구동)에
관측으로 답할 수 있는 것이 무엇인지 가른다.

### 지금 정책이 보는 것과 못 보는 것

actor 관측 54채널: `projected_gravity(3) · base_ang_vel(3) · commands(10) ·
cos/sin(gait)(2) · dof_pos(12) · dof_vel(12) · 이전 action(12)`.
critic은 여기에 privileged 14채널(`base_mass_scaled(4) · base_lin_vel(3) ·
height(1) · push_force(3) · push_torque(3)`).

여기서 세 가지가 눈에 띈다.

1. **이력이 없다.** 단일 프레임이다. 기억이 없는 정책은 자기 지연을 추정할 수 없고,
   지연을 랜덤화해도 할 수 있는 최선이 "보수적으로 굴기"뿐이다. `MB_obsdelay`는
   지연을 **겪게** 했지만 그것을 **식별할 수단**은 주지 않았다.
2. **⛔ 발 상태가 하나도 없다.** 발에 관한 보상이 여섯 개인데
   (`feet_offset_y`, `feet_cross`, `feet_swing`, `feet_slip`, `foot_yaw_L`, `foot_yaw_R`)
   정책은 그중 무엇도 직접 보지 못한다. 12개 관절각에서 FK로 유추해야 한다.
   `MA_feetcross`에서 `feet_cross`가 학습 중 **−0.183 → −0.287로 나빠진 것**이
   그 어려움의 증거다 — 벌은 받는데 무엇을 고쳐야 하는지 보이지 않는다.
3. **비평자가 우리가 거는 랜덤화를 거의 못 본다.** 접촉력도, 그 env의 관측지연도,
   발목 이득도 안 보인다. 그러면 관측이 같은데 조건이 다른 env들의 가치를 하나로
   평균해야 하고, DR이 셀수록 가치추정이 흐려진다. 비대칭 actor-critic의 요점이
   정확히 그걸 피하는 것이다.

### 정보량 기준으로 줄을 세운다

| 후보 | 새 정보인가 | 실기 가용 | 판정 |
|---|---|---|---|
| **이력 k프레임** | ✅ 시간축 | 버퍼만 있으면 됨 | **N4_hist** |
| **실측 토크** | ✅ 위치·속도의 함수가 아님 | `motor.tau_est` 이미 수신 중 | **N5_tau** |
| 발 오프셋 | ❌ 관절각의 결정론적 함수 = 특징공학 | FK로 계산 | **N6_foot** |
| 비평자 확장 | ✅ (배포 무관) | 불필요 | **N7_critic** |
| base_lin_vel을 actor에 | ✅ | ❌ 추정기 없음 | 기각 — privileged로 유지 |
| 발 접촉 | ✅ | ❌ SDK에 힘센서 없음 | 기각 — N7의 비평자 채널로만 |
| 관절 추종오차 | ✅ | ✅ | 이력에 **포섭**됨(이전 action + 현재 위치) |
| 지형 높이 스캔 | ✅ | ❌ 인지 없음 | 기각 |
| 팔 관절 상태 | ❌ URDF에서 고정 | — | 기각 |

이력 하나가 개별 제안 여럿(추종오차, 이전 action 여러 개, 목표 속도)을 포섭한다.

### 설계 — 비교가 '관측'을 재도록

네 arm 모두 **`N1_path` 위**에 얹는다. 이 증상들은 걸을 때 나오고, waypoint 과제는
요구 속도 median 0.12 m/s라 발현될 기회 자체가 적다. `MA_feetcross`와 `MB_obsdelay`가
그 위에서 돈 것이 이미 낭비였다.

⛔ **warm start 문제.** `utils/runner.py::_load`는 `load_state_dict(strict=False)`인데
strict=False는 *없는 키/남는 키*만 봐준다 — **모양이 다른 키는 그대로 예외**다.
관측을 넓히면 warm start가 실패하고, 처음부터 학습하면 그 비교가 관측이 아니라
**학습 예산**을 재게 된다.

`tools/expand_checkpoint.py`가 첫 층만 넓히고 새 열을 0으로 채운다. 새 채널의 기여가
정확히 0이므로 수술 직후 정책이 원본과 **출력이 완전히 같다**. 근사가 아니라 항등이고,
`--verify`가 새 채널에 난수를 넣어 실제로 검사한다:

| | float32 | float64 |
|---|---|---|
| actor 최대차 | 0.000e+00 | **0.000e+00** |
| critic 최대차 | 3.433e-05 | **2.842e-14** |

float32의 critic 잔차는 로직이 아니라 **결합법칙**이다 — critic의 0 아닌 가중치가
입력 벡터의 양 끝(obs 앞부분과 오른쪽으로 밀린 privileged)으로 갈라져 GEMM 누산
순서가 달라지고 4개 층을 지나며 커진다. actor는 가중치가 앞쪽에 연속이라 float32에서도
정확히 0이다. float64로 재서 둘을 갈랐다.

**critic이 이 도구가 존재하는 가장 큰 이유다.** 입력이 `concat(obs, privileged)`라
obs가 넓어지면 privileged 열이 가운데에서 오른쪽으로 밀린다 — 뒤에 0을 붙이는 것으로는
안 되고 재배치가 필요하다.

### 검증 (GPU 스모크, 64 env)

| arm | obs | priv | 결과 |
|---|---|---|---|
| `N0_ctrl`(기존 경로) | 54 | 14 | ✅ 무회귀 |
| `N4_hist` | 270 | 14 | ✅ (아래 결함 수정 후) |
| `N5_tau` | 66 | 14 | ✅ |
| `N6_foot` | 56 | 14 | ✅ |
| `N7_critic` | 54 | 17 | ✅ |

⛔ **스모크가 결함 하나를 잡았다.** 리셋은 `_compute_observations` **뒤에** 돌고,
이력이 켜져 있으면 그 시점 `obs_buf`는 이미 k프레임이 붙어 넓어져 있다. 기존 지연
링 버퍼 리셋은 한 프레임 폭을 가정하고 있어서 `shape [270] vs [3,1,54]`로 죽었다.
**이력과 지연을 둘 다 켠 조합에서만** 터지므로 코드를 읽는 것만으로는 안 나왔을 것이다.

`_assert_obs_width`가 config의 `num_observations`와 실제 폭을 첫 스텝에 대조한다.
조용히 잘린 채 몇 시간을 학습하는 것보다 죽는 편이 낫다.

### 배포 쪽

`deploy/utils/policy_goal_pose.py`도 같은 레이아웃으로 맞췄다. config 조합에서 폭을
계산해 `num_observations`와 대조하고 어긋나면 정책을 올리기 전에 죽는다 — **순서가
어긋난 관측은 예외를 내지 않고 그냥 이상하게 걷기 때문에** 정적 검사로만 막을 수 있다.

`N7_critic`은 **배포되는 정책의 입력이 한 글자도 안 바뀐다.** 이득이 나오면 sim2real
위험 없이 그냥 가져올 수 있다.

### 아직 안 한 것

**제어 주기 자체를 관측에 넣기.** 실기는 `pub_hz`와 `low_state_age`를 재고 있으므로
측정된 루프 주기를 채널로 줄 수 있다. 다만 그걸 학습시키려면 sim에서 **제어 주기를
랜덤화**해야 하는데 지금 env는 그걸 지원하지 않는다. 이력이 같은 것을 암묵적으로
해내므로 N4의 결과를 보고 정한다.

---

## 8-45. ⛔ 실기 증상을 MuJoCo에서 재현하고 수정으로 지웠다 (2026-08-07 04:xx)

§8-43이 지목한 "배포 필터가 루프 속도에 의존한다"를 **실물 질량 자산 위에서** 시험했다.
배포된 정책(`I3b_stance10 model_200`) 그대로, 120 s × 3조건.

| 조건 | 구간 | **낙상** | 위치 cm | 과제 cm | **도착** | hip roll 범위 |
|---|---:|---:|---:|---:|---:|---|
| ① 기준 (필터 500 Hz) | 47 | **0** | 5.68 | 6.70 | **47** | L 60.9° R 56.6° |
| **② 느린 루프 50 Hz** | 68 | **66** | **35.43** | **37.62** | **2** | L 76.2° R 70.7° |
| ③ 50 Hz + 시정수 고정 | 47 | **0** | 5.80 | 6.78 | **47** | L 58.1° R 58.2° |

**②가 실기 증상 그 자체다** — 68구간 중 66회 낙상, 도착 2회. "세 걸음 가고 무너진다".
**③이 기준선으로 완전히 되돌린다.** 바뀐 것은 계수 하나:

```
② a = 0.2 고정        @50 Hz -> 시정수 89.6 ms, 차단 1.78 Hz  (2 Hz 보행이 통과 못 함)
③ a = 1 - e^(-dt/0.01) -> 시정수 10 ms 고정,  차단 15.9 Hz
```

**`--rate-fixed-filter`가 옳은 수정임이 증명됐다.** 남은 미지수는 실기 루프의 실제 주파수
하나뿐이다(R6, `pub_hz` 30초 실측).

### 같이 기각된 것 — 질량·관성 오차

§8-39의 교차검증은 `K1_serial.xml`(**18.7142 kg = 학습 자산과 같은 잘못된 값**)로 돌았고,
`--real-asset` 도움말이 스스로 *"같은 잘못된 로봇 두 개를 비교한 것"*이라 경고했다.
실물(19.666 kg / 발 +29 % / Ixx +14.2 % / Izz +19.3 % / CoM −10.8 mm)로 다시 돌리면:

| | 잘못된 질량(§8-39) | 실물 질량(이번) |
|---|---:|---:|
| 과제오차 | 6.73 cm | **6.70 cm** |
| 낙상 | 0 | **0** |

**차이가 없다. 질량·관성 오차는 실기 실패의 원인이 아니다.** 토크 상한(§8-39②)·배포
필터 존재 자체(§8-39③)에 이어 세 번째 기각이다. 남은 것은 **속도 의존 필터**와
감지 열화다.

### 떨림 — 없다. 반전율만 보면 오독한다

무릎 토크 부호 반전이 **22.9회/초**라 채터링처럼 보인다. 스펙트럼이 뒤집는다:

| 관절 | 1–3 Hz | 3–10 Hz | 10–20 Hz | **20–25 Hz** | 최대주파 |
|---|---:|---:|---:|---:|---:|
| R_Knee | 40.6 % | 43.2 % | 9.6 % | **1.8 %** | **2.0 Hz** |
| L_HipR | 66.5 % | 28.5 % | 3.2 % | **0.6 %** | **2.0 Hz** |
| R_AnkR | 28.3 % | 61.9 % | 4.6 % | **0.5 %** | 4.0 Hz |

25 Hz는 50 Hz 루프의 Nyquist다. **거기 에너지가 0.3~1.8 %뿐이고 전 관절 최대주파가
보행 주파수(1.8~2.0 Hz) 자체다.** 조화성분이 많은 파형이 영점을 자주 지나는 것이지
떨림이 아니다. 관절속도의 10 Hz 초과 성분도 3.4~7.4 %다.

> **교훈: 반전율(flips_per_s)만으로 떨림을 판정하면 안 된다.** 스펙트럼을 같이 봐야 한다.

### 눈에 걸리는 것 — Hip roll만 토크 상한에 붙는다

`R_HipR over_limit 4.8 %`, `L_HipR 2.7 %` (상한 30 N·m). 다른 관절은 0~0.5 %다.
**측면 균형 담당 관절만 포화한다.** 실기 추종률 median 0.61(MuJoCo 0.93)과 방향이 맞고,
②에서 7.4 %로 더 올라간다 — 필터가 명령을 깎으면 정책이 더 크게 명령한다는
`--leg-gain` 도움말의 기전 그대로다.

### 영상이 발을 잘못 보여주고 있었다 (물리는 무관)

발에 geom이 둘이다 — 상세 mesh(`group=1`, `contype=0`, 시각 전용)와 충돌
box(**group 미지정 = 0**, 같은 불투명 회색). MuJoCo가 둘 다 그려 **box가 mesh를 덮어**
납작한 슬래브로 보였다. 형상 자체는 맞다:

| | 길이 | 폭 |
|---|---|---|
| 충돌 box | 16.0 cm | 7.0 cm |
| 시각 mesh (STL 실측) | 16.5 cm | 7.8 cm |
| 학습 `feet_edge_pos` 교정본 | **16.0 cm** | **7.0 cm** |

T1 자산도 아니다(T1은 `AL1.STL`/`Ankle_Cross_Left.STL` 계열, 발 파일을 공유하지 않는다).
렌더에서 group 0을 숨기게 고쳤다(`44c5247`). **물리는 안 건드렸으므로 위 결과는 유효하다.**

---

## 8-46. ⛔ 관절 영점 오차를 한 번도 시뮬레이션한 적이 없다 (2026-08-07 05:xx)

사용자 질문: "joint randomize 말고 joint zero를 여러 가지로 심하게 틀어진 환경을
학습에서 먹도록 하려면 어떻게 설계해야 하나."

### 먼저 결함 — 지금 것은 영점 오차가 아니다

`joint_encoder_bias` 와 `joint_target_offset` 은 **독립적으로** 뽑힌다
(`goal_pose.py:394` 와 `:398`, 각각 별도의 `torch.rand_like`). config 주석은
*"실제 영점 드리프트는 둘 다 일으키므로 같은 크기로 같이 켠다"* 고 말하는데,
"같이 켠다"가 "같은 범위를 준다"로 구현돼 있고 **같은 값**이 아니다.

실기에서 관절 j 의 엔코더 영점이 b 만큼 틀어지면:

    측정값     q_meas   = q + b
    정책 관측  q_meas − q_default = q + b − q_default
    온보드 PD  tau = kp·(target_cmd − q_meas) = kp·(target_cmd − q − b)
    평형       q_eq = target_cmd − b = q_default + scale·a − b

sim 은 PD 를 **참값** `dof_pos` 로 돈다(`:643`):

    관측       dof_pos + joint_encoder_bias − default        (:866)
    평형       q_eq = default + scale·a + joint_target_offset (:628)

두 식이 같아지는 조건은 하나뿐이다:

> **`joint_encoder_bias = +b` 이고 `joint_target_offset = −b`.**
> 근사가 아니라 정확한 등가다. 독립 draw 에서 이 조합은 사실상 나오지 않는다.

그래서 `I3a_jointcal3`, `M8_jointcal2` 가 학습한 것은 영점 드리프트가 아니라
**"엔코더가 X, 구동기가 무관한 Y 만큼 동시에 틀어진 두 개의 다른 고장"** 이다.

그리고 둘은 질적으로 다르다. 상관된(진짜 영점) 경우 관측 좌표계 q̃ = q + b 에서
q̃_eq = default + scale·a 가 되어 **관절 루프만 보면 완전히 정상**이다. 깨지는 것은
참 기구학에 붙은 것들뿐이다 — IMU 는 참 자세를 보고, 접촉은 참 발 위치에서 일어나고,
실제 서 있는 자세가 정책이 믿는 자세와 다르다. 독립 draw 는 관절 루프 **자체**를
어긋나게 만들어서 훨씬 거칠다. 2026-08-05 MuJoCo 스윕이 본 **±3° 절벽도 그 거친 쪽
기준**이므로, 진짜 영점 오차의 허용 폭은 다시 재야 한다.

### 설계 — 네 갈래

**(1) 상관.** `target_offset = −encoder_bias`. 위 유도의 직접 결론.

**(2) 구조화된 고장 모드.** 실제 영점 틀어짐은 12개 관절의 독립 추첨이 아니다.

| 모드 | 물리적 원인 | 비율 |
|---|---|---|
| `single` | 부딪힘, 벨트 슬립, 백래시 — 한 관절만 크게 | 0.30 |
| `leg_common` | 잘못된 자세에서 재캘리브레이션 — 한 다리 상관 | 0.20 |
| `anti_mirror` | 좌우 반대 ← **실기 증상을 재현한 모양** | 0.20 |
| `mirror` | 캘리브레이션 루틴의 계통 오차 | 0.10 |
| `iid` | 조립 공차 | 0.20 |

⛔ **2026-08-08 정정 — 이 자리에 있던 인용이 유령이었다.**
원문은 *"`anti_mirror` 의 근거는 **§8-43** 이다: Hip_Roll 에 −5°는 **실기 증상을 재현**했고
+5°는 낙상 0이었다"* 였다. **§8-43 전문에 그 실험이 없다.** 그리고 §8-42 의 레버표는
오히려 *"관절 영점 … 교차 재현 실패"* 라고 적는다 — 즉 "실기 증상을 재현" 은 **거짓**이다.

**실제 출처와 실제 숫자** (`logs/mujoco/sig/hiprbias*.json`, 정책 `I3b_stance10/model_200`,
벤더 MJCF 발 관성·토크 상한, 각 **20 s 단일 실행**):

| 조건 | 낙상 |
|---|---:|
| base | **0** |
| Hip_Roll 영점 **−3°** | **0** |
| Hip_Roll 영점 **+3°** | **0** |
| Hip_Roll 영점 **−5°** | **6** |
| Hip_Roll 영점 **+5°** | **0** |

**살아남는 것**: 부호 비대칭은 실재한다(−5 는 넘어지고 +5 는 안 넘어진다).
**같이 적어야 하는 것 셋** — 원 인용이 전부 빠뜨렸다:
1. **±3° 는 양쪽 다 낙상 0** 이다. 효과는 3°와 5° 사이에서 나타난다
2. **n = 1** 이다. 그리고 §8-47 이 *"`--goal-hold` 는 노이즈 0 이면 완전 결정론적이라
   seed 를 바꿔도 같은 실행"* 이라고 자기 정정했으므로 **seed 를 늘려도 표본이 안 는다**
3. 그 실험은 **한 관절(Hip_Roll)** 만 흔들었다. `anti_mirror` 는 **12관절 좌우 반대**다 —
   외삽이다

⇒ `anti_mirror` 비중 **0.20 은 이 데이터로 정당화되지 않는다.** 부호 비대칭이 실재한다는
것까지가 근거이고, 그 비중이 0.20 이어야 할 이유는 없다. 다시 정할 것.

(원 논거의 나머지는 유효하다:) 부호가 갈리는 고장은 iid 균일에서 거의 뽑히지 않는다.
반대로 독립 균일 ±10° 는 "모든 관절이 조금씩 아무 방향으로"인데, 물리적으로 가장
일어나기 어려우면서 동시에 가장 파괴적이다 — 다리 여섯 관절이면 발이 정책 예상에서
22 cm 어긋나 지지다각형(발 반길이 9.4 cm)을 넘는다.

**(3) per-env 커리큘럼.** ±3° 가 이미 절벽이므로 고정 ±10° 는 학습이 아예 안 선다
(config 가 인용한 Margolis: *"커리큘럼 없이는 느리게도 못 걷는다"*). 전역 스칼라를
쓰지 않는다 — 우리 config 자신이 `speed_curriculum` 을 두고 *"4096 env 의 신호를
전부 버리는 단일 전역 float 이라 legged_gym 의 지형 커리큘럼보다도 조악하다"* 고
적어 뒀다. env 마다 수준을 들고 **넘어지지 않으면 승급, 넘어지면 강등**한다.
그러면 함대가 매 순간 쉬운 것부터 어려운 것까지 퍼져 있고 분포가 스스로 경계를 찾는다.

**(4) 관절별 민감도.** ±10° 가 Ankle_Roll 과 Hip_Yaw 에서 같은 의미가 아니다.
Hip_Roll 은 ±2° 에서 이미 토크 20 % 포화였으므로 가중치 0.5, 발목은 0.7.

### 왜 이것만으로는 부족한가 — 관측이 있어야 한다

영점 오차는 **관측에 없다.** 정책은 자기가 어느 방향으로 틀어졌는지 볼 수 없으므로,
기억 없는 단일 프레임 정책이 배울 수 있는 것은 "모든 오차에 대해 무난한 보수적 자세"
뿐이다(§8-17: 보수성은 속도를 깎는다). **이력(§8-45 `N4_hist`)이 있어야** 지속적이고
계통적인 추종 신호에서 오차를 **식별해서 상쇄**할 수 있다 — RMA 계열이 하는 일이다.
그래서 영점 arm 과 이력 arm 은 결국 합쳐야 한다.

### 검증 (`tools/test_joint_zero.py`, Isaac Gym 없이)

메서드 소스만 떼어 스텁에 붙여 돌린다 — 로직을 복사해 오면 검사 대상이 학습이 쓰는
코드가 아니게 된다. 15개 검사 전부 통과:

| 검사 | 결과 |
|---|---|
| `target_offset == −encoder_bias` | 최대차 **0.000e+00** |
| `single`: env 마다 정확히 관절 1개 | 4096/4096 |
| `leg_common`: 한 다리만, 그 안에서 부호 동일 | ✅ |
| `anti_mirror` / `mirror`: 좌우 짝 정확 | 최대차 **0.000e+00** |
| 진폭이 `max_deg × joint_weight` 이내 | Hip_Roll 5.00°/5.0° |
| 가중치가 실제로 반영 | Hip_Roll/Hip_Pitch = **0.500** |
| 커리큘럼 승급/강등 | 0.5 → 0.55 / 0.45 |
| level 이 [0,1] 밖으로 안 나감 | ✅ |
| 모드 혼합 비율 | single 실측 **0.301** (목표 0.30) |

GPU 스모크: `N8_zeroiid` ✅ `N9_zerostruct` ✅ `N0_ctrl`(무회귀) ✅

### arm

- `N8_zeroiid` — 상관 수정만. 모드는 iid 하나라 기존과 모양이 같고 달라지는 것은
  `target_offset = −encoder_bias` 뿐이다.
- `N9_zerostruct` — N8 과의 차이가 **모드 혼합 하나**.

둘 다 `N1_path` 위. 영점 오차의 대가는 걸을 때 나온다.

---

## 8-47. N 배치 1라운드 — path는 걷고, 배포 정책은 실물 자산에서 무너진다 (2026-08-07 09:xx)

`tools/eval_round.sh` 첫 라운드. 모든 arm을 **두 프로토콜**로 잰 첫 번째 결과다.
`forward_hold`의 `required_speed_median`이 **2.04**로 나왔다 — waypoint 과제에서
0.12였던 그 값이다. 프로브가 실제로 속도를 요구했으므로 보행 숫자가 유효하다.

### ⛔ 배포 정책을 실물 자산으로 재면 무너진다

| 채점 대상 | 낙상률/구간 (waypoint) | 낙상률/구간 (보행) |
|---|---|---|
| `I3b_stance10` (배포 정책) | **0.368** | **0.604** |
| 실물 자산으로 학습한 arm 전부 | 0.0009–0.0035 | 0.0005–0.0050 |

**세 자릿수 차이다.** 이 정책은 지금까지 항상 **자기가 학습한 자산**
(`K1_locomotion_armsdown.urdf`, 18.714 kg)으로만 채점돼서 낙상 4건이 나왔다.
공통 프로토콜은 `K1_robot_boxfoot.urdf`(19.666 kg, 실물)를 쓴다. 평가 동역학에서
다른 것은 **자산 하나뿐**이다 — 종료 조건도 노이즈도 외란도 같다.

실기에서 세 걸음 만에 넘어진 것의 sim 대응물이 이것이다. §8-43의 필터 축과
경쟁하지 않는다: 저쪽은 배포 코드의 결함이고 이쪽은 정책이 학습한 로봇이 다르다는 것이다.

⚠️ `I3b_stance10`의 보행 속도 median이 **1.12 m/s**로 높게 나온다. 이것을 속도로
읽으면 안 된다 — 같은 행의 낙상이 **26,042**다. 앞으로 고꾸라지는 것도 body_speed로
잡힌다. **속도만 보면 안 된다는 반례가 같은 표 안에 있다.**

### path 레버 — 같은 예산, 같은 체크포인트 번호, 레버 하나

`N0_ctrl`과 `N1_path`는 `goal_mode_mixture` 하나만 다르고 둘 다 6000 iteration,
둘 다 배포 정책에서 warm start 했다. 둘 다 `model_6000`으로 비교한다:

| | N0_ctrl (path off) | N1_path (path on) |
|---|---|---|
| 보행 속도 median | 1.10 m/s | **1.48 m/s** (+35 %) |
| 1.0 m/s 초과 체류 | 78.9 % | **98.3 %** |
| 보행 낙상률 | 0.0035 | **0.0007** (5배 낮다) |
| 정확도 median (best.pth) | **2.82 cm** | 7.93 cm |
| strict (best.pth) | **86.4 %** | 25.5 % |

**path 모드는 더 빠르면서 동시에 걷는 중 덜 넘어진다.** §8-44의 처방이 맞았다.
대가는 waypoint 정확도이고, 그것도 dwell 덕분에 v7 배치의 6.3 → 37.9 cm가 아니라
2.8 → 7.9 cm로 줄었다.

### ⛔ 선택기가 뽑은 체크포인트와 걷는 체크포인트가 다르다

| N1_path 체크포인트 | 보행 속도 | 보행 낙상 |
|---|---|---|
| `best.pth` (정확도로 선택) | 1.15 m/s | **1,177** (rate 0.038) |
| `model_6000` (마지막) | **1.48 m/s** | **10** (rate 0.0007) |

**50배 차이다.** 선택기는 waypoint 정확도로 순위를 매기므로 "잘 서는" 체크포인트를
고르고, 그것이 보행에서는 최악이다. `eval_round.sh`의 4/4단계를 정확히 이 가능성
때문에 넣었고 첫 라운드에서 걸렸다. **정확도 한 축으로 뽑아 배포하면 안 된다.**

### 부수 결과

- `N0_ctrl` 자체가 이 라운드 최고의 waypoint 정책이다(2.82 cm / 86.4 % strict / 낙상 8).
  배포 정책보다 낫고, 레버는 예산 6000 + `base_height_target` 0.52 정정뿐이다.
- `MA_feetcross` 2.69 cm / 83.6 % / 낙상 4 — 정확도는 최고지만 보행이 0.82 m/s에
  1.0 m/s 초과가 1.5 %뿐이다.
- `MB_obsdelay`는 보행에서 **1.0 m/s를 한 번도 넘지 못한다(0.0 %)**. 관측 지연만
  주고 그것을 식별할 수단(이력)을 안 주면 정책이 할 수 있는 것은 느려지는 것뿐이라는
  §8-45의 예측과 일치한다.
- 발 간격은 모든 arm이 16–19 cm로 목표(10 cm 이상)를 넘겼다. 실기의 7.0 cm 문제는
  적어도 sim에서는 재현되지 않는다.

### ⛔ 이 라운드에서 낸 실수

`eval_round.sh`가 run 디렉터리를 `ls -1td | head -1`(시간순 최신)로 골랐는데,
내가 04:44에 돌린 **3-iteration 스모크**가 더 최신이라 체크포인트 0개짜리
`N0_ctrl` 디렉터리를 집었다. `nn/`은 존재하므로 가드를 통과했고, 체크포인트가 없으니
`--checkpoint`가 빈 문자열이 되어 `eval_goal_pose`의 기본값(logs 아래 아무 최신 모델)로
떨어졌다 — **죽지 않고 엉뚱한 정책을 `N0_ctrl` 이름으로 채점했다.** 47.70 cm라는
숫자가 거기서 나왔다.

수정: `tools/pick_run.sh`(체크포인트 N개 이상인 run만 선택), `eval_round.sh`에
2개 미만 skip 가드. 재채점한 값이 위 표의 2.82 cm다.
**시간순 최신은 "내가 원하는 run"의 안전한 대용이 아니다.**

---

## 8-47. 필터 수정이 발행률 전 구간을 덮는다 — `pub_hz` 측정이 불필요해졌다 (2026-08-07 06:xx)

§8-45가 50 Hz에서 증상을 재현하고 수정으로 지웠다. 그 뒤 **발행률을 스윕**하고
**관측 지연을 같이** 걸었더니, 내가 §8-45에서 제시한 판정 기준이 무효가 됐다.

### ① 발행률 스윕 (goal_reach, 지연 0, 실물 질량)

| 발행 Hz | 시정수 | 2 Hz 통과 | 낙상/구간 | 과제 cm |
|---:|---:|---:|---|---:|
| 50 | 89.6 ms | 0.66 | **66/68** | 37.62 |
| 75 | 59.8 | 0.80 | 43/56 | 6.60 |
| 100 | 44.8 | 0.87 | 10/38 | 6.44 |
| 150 | 29.9 | 0.94 | 4/36 | 6.76 |
| 200 | 22.4 | 0.96 | **0/41** | 6.52 |
| 500 | 10.0 | 0.99 | **0/47** | 6.70 |

**정확도는 6.4~6.8 cm로 내내 같고 낙상만 움직인다.** 필터는 정확도를 깎는 게 아니라
넘어뜨린다 — sim 지표를 아무리 봐도 안 보였던 이유다.

여기서 나는 *"실기 `pub_hz` ≥ 200이면 필터 무죄"*라고 적었다. **틀렸다.**

### ② ⛔ 관측 지연을 같이 걸면 단조가 깨진다

연속보행(`--goal-hold`) 120 s, 지연 20 ms, IMU 잡음 0.5°(§8-40 기준 안전범위), seed 3개:

| 발행 Hz | 낙상 seed 0/1/2 | median |
|---:|---|---:|
| 150 | 0 / 6 / 0 | **0** |
| **200** | **31 / 29 / 24** | **29** (14.5/분) |
| 500 | 0 / 0 / 0 | 0 |

**200 Hz가 150 Hz보다 일관되게 나쁘다.** 위상으로는 설명이 안 된다 — 200 Hz 쪽이 총
지연이 더 작은데(30° 대 35°) 결과가 나쁘다.

> ⚠️ 방법론: 처음엔 이것을 "분산"이라 보고 seed를 늘렸는데 **50/50/50, 0/0/0, 25/25/25로
> 전부 같았다.** `--goal-hold`는 목표가 고정이고 노이즈가 0이라 **결정론적**이다.
> seed를 바꿔도 같은 실행이다. 실제 변동을 만들려면 잡음을 켜야 한다.
> **`--seed`가 있다고 변동이 생긴다고 가정하면 안 된다.**

### ③ 그리고 수정 필터가 그 이상까지 덮는다

같은 조건(지연 20 ms + 잡음 + seed 3개)에서 `--fix-filter`:

| 발행 Hz | 현재 (계수 0.2 고정) | **수정 (시정수 10 ms 고정)** |
|---:|---|---|
| 150 | 0 / 6 / 0 | **0 / 0 / 0** |
| **200** | **31 / 29 / 24 (med 29)** | **0 / 0 / 0** |

**200 Hz의 이상 현상은 관측 지연 탓이 아니라 고정 계수 0.2 자체가 만든 것이다.**
시정수를 고정하면 사라진다.

### 판정

- **`--rate-fixed-filter`는 발행률과 무관하게 안전하다.** 50 · 150 · 200 · 500 Hz 전부 낙상 0
- **따라서 R6(`pub_hz` 실측)은 수정을 적용할 것이라면 불필요하다.** 어느 값이 나오든
  수정본은 그 값에서 안전하다. 측정은 "왜 그랬는지" 사후 확인용으로만 값어치가 있다
- **실기에서 당장 켤 것: `--rate-fixed-filter`.** 500 Hz에서 켜나 안 켜나 같으므로
  (§8-45: 6.70 대 6.78, 낙상 0/0) **손해가 없고**, 느린 루프면 즉시 살린다

### 부수 기각 — 질량·관성

§8-39의 교차검증은 `K1_serial.xml`(18.7142 kg = 학습 자산과 같은 값)로 돌았다.
실물(19.666 kg / 발 +29 % / Ixx +14.2 % / Izz +19.3 %)로 다시 돌리면 과제오차
**6.73 → 6.70 cm**, 낙상 0 → 0. **차이가 없다.**

### 부수 — 떨림은 없다

무릎 토크 부호 반전이 22.9회/초라 채터링처럼 보이지만, 전 관절 스펙트럼 최대가
**보행 주파수(1.8~2.0 Hz)** 자체이고 **Nyquist(25 Hz) 대역은 0.3~1.8 %**다.
조화성분이 많은 파형이 영점을 자주 지나는 것이다. **반전율만으로 떨림을 판정하면 안 된다.**

포화하는 관절은 hip roll 하나뿐이다(`R_HipR` 4.8 %, 다른 관절 0~0.5 %). 느린 필터에서
7.4 %로 오른다 — 명령이 깎이면 정책이 더 크게 명령하는 기전 그대로다.

---

## 8-48. NC_actfilter — 학습이 배포 필터를 겪게 한다 (2026-08-07 16:xx)

Codex의 필터 조사(§8-45/§8-47)를 감사한 결과(`FILTER_AUDIT-non-codex.md`) 나온
학습 쪽 조치. 감사에서 세 가지가 갈렸다:

- **옳은 것**: ②/③ 대조(계수 하나로 66/68 → 0/47), 질량·관성 기각(6.73 → 6.70),
  떨림 기각(Nyquist 대역 0.3–1.8 %), 렌더 진단.
- **⛔ 인과 전제가 코드와 다른 것**: "`time.sleep(0.001)`이라 발행률이 훨씬 낮을 수
  있다"는 루프를 잘못 읽은 것이다. 그 sleep 은 **데드라인 폴링**이고 발행률은
  `1/common.dt`로 정해진다 — `Goal_Pose_E0.yaml:25` `dt: 0.002` → **500 Hz 목표**다.
  "50 Hz로 돈다"는 가정이지 측정이 아니다.
- **⛔ 안 풀린 반증**: `deploy_base_walk.py:176`이 **같은 필터**, `:168`이 **같은 루프
  구조**, `Base_Walk.yaml:2`가 **같은 dt 0.002**인데 base_walk 는 실기에서 걷는다.
  실마리는 `gait_frequency` 2.0(우리) vs 1.0(base_walk) — 느린 루프에서만 갈린다
  (2 Hz 통과율 0.66 vs 1 Hz 0.87). 즉 가설 전체가 "루프가 실제로 느리다" 하나에
  매달리고, 그건 `pub_hz` 실측(R6)으로만 닫힌다.

### 학습에 반영할 것 — 필터가 학습에 아예 없다

`goal_pose.py:793`에서 PD 가 **날것 목표**를 받는다. 액션 쪽에 있는 것은
`delay_steps`(제어주기 안 0–18 ms 순수지연)뿐이고 저역통과는 어디에도 없다.
**`--rate-fixed-filter`를 켜도 이 격차는 남는다** — 그 수정은 시정수를 10 ms 로
고정하는 것이지 0 으로 만드는 것이 아니다.

`control.action_filter_tau`(기본 `[0, 0]` = 정확한 no-op)를 넣었다. 물리 서브스텝마다

    alpha = 1 - exp(-sim_dt / tau);   filtered += alpha * (target - filtered)

를 적용한다. **근사가 아니다**: `sim.dt = 0.002`이고 배포 `common.dt = 0.002`라
물리 서브스텝이 로봇 발행 루프와 **같은 500 Hz**다.

| tau | alpha | 한 제어스텝 도달 | 차단 |
|---|---|---|---|
| 0 ms | 1.0000 | 100 % | ∞ (현재 학습) |
| 10 ms | 0.1813 | 86.5 % | **15.9 Hz** ← 배포 수정본과 일치 |
| 30 ms | 0.0645 | 48.7 % | 5.3 Hz |

`NC_actfilter` = `N1_path` + `action_filter_tau [0, 0.030]` (config diff 로 레버가
그 키 하나임을 확인). **이 arm 이 논쟁 자체를 무의미하게 만든다**: 한 정책이 필터
없음(0) / 수정본(10 ms) / 느린 루프(30 ms)를 모두 겪으므로 `pub_hz` 가 얼마로
나오든 그 축에서 무관해진다 — **로봇 측정을 기다리지 않고 지금 걸 수 있는 유일한
레버다.**

상한 30 ms 의 근거: Codex 스윕에서 150 Hz(tau 30 ms)까지는 낙상이 한 자릿수,
100 Hz(tau 45 ms)에서 두 자릿수로 갔다. 30 ms 는 "느리지만 아직 걸을 수 있는"
구간의 끝이다. 더 크게 잡으면 학습 불가능한 조건을 섞게 되고, 그건 §8-46 의
±10도 영점 오차와 같은 부류의 실수다.

### ⛔ 이 절에서 내가 낸 사고 — 도는 학습 두 개를 지웠다

스모크 잔여 정리를 임시 `rm -rf` 루프로 했는데, 판정 기준이 "체크포인트 0개 =
스모크"였다. **방금 시작한 진짜 학습도 체크포인트가 0개다**(save_interval 100 이면
첫 저장까지 4분). 그래서 큐가 막 집어간 `NZ_zeroiid`(1분)와 `N4_hist`(2분)를 지웠고
둘 다 `FileNotFoundError`로 죽었다. GPU 손실은 ~2분이지만 원인은 절차다.

`tools/clean_smoke_runs.sh` 로 고정했다. 세 조건을 **전부** 만족해야 지운다:
체크포인트 0개 **그리고** 20분 이상 오래됨 **그리고** 살아 있는 `train_v7.py` 가
그 arm 을 돌고 있지 않음. 기본은 dry-run 이고 `--yes` 를 붙여야 지운다.

---

## 8-49. ⛔ 복구 재진입이 get-up 을 기다린 적이 없다 (2026-08-07 실기)

사용자가 로봇을 눕혀 복구를 시험했다. 네 번 다 **펌웨어 관절 보호(빨간불)** 로
DAMPING 에 떨어졌다. 로그가 사슬을 그대로 보여준다.

```
ModeMonitor disabled (cannot import name 'RobotStatesMsg' from 'booster_interface.msg')
cannot read /robot_states; entering CUSTOM without confirming the robot is standing
[recovery] standing again after 4.9 s; re-entering CUSTOM
[mode-timing] latched CUSTOM entry done in 3.04 s, joints moved 1.5934 rad
[recovery] get-up never became available (state=IS_READY); stopping.
```

### ① 서 있는지 확인하는 게이트가 **경고만 찍고 통과**했다

`_ensure_standing_before_custom` 은 `mode_monitor.available == False` 면 그냥
return 한다. 안전 전제조건이 옵션이면 필요한 순간에 꺼져 있다.

`ros2 topic info /robot_states` 는 타입이 **정확히
`booster_interface/msg/RobotStatesMsg`** 라고 답한다. 그런데 그 패키지의
`__init__.py` 는 그것을 export 하지 않는다(FallDownState 등 17개만 있다).
**메시지는 생성돼 있고 `__init__` 만 빠진 것이다.** 그래서 공개 import 가
실패하면 `_robot_states_msg` 모듈을 직접 집도록 고쳤다.

### ② `IS_READY` 는 get-up 의 완료가 아니다

판정이 `state == IS_READY and elapsed > 2.0` 한 줄이었다. 실측 get-up 은
**HAS_FALLEN → IS_READY 8.0 s** 인데 **4.9 s 에 재진입**했다. 사용자 관찰이
정확했다 — 로봇은 그 시점에 **다리를 오므려 서기를 하는 중**이고, 그것이 끝나야
stand mode 다.

시간 상수를 추측하는 대신 **로봇이 멈추는 것을 본다**: 다리 12관절의 `|dq|` 가
0.15 rad/s 아래로 0.5 s 지속. `LowState` 에서 직접 읽히므로 `/robot_states` 가
없는 빌드에서도 쓸 수 있다. 최소 대기도 2.0 → 8.0 s(실측값).

**직립도 같이 요구한다.** `/fall_down` 은 ~1 Hz 라 DAMPING 직후에도 낡은
`IS_READY` 를 들고 있을 수 있고, **그때 다리는 힘이 빠져서 오히려 조용하다** --
"IS_READY + 조용함"만으로는 무너지는 중인 로봇을 서 있다고 읽는다. `tilt` 는
LowState 에서 500 Hz 로 오므로 그 창이 없다.

### ③ 램프가 거리를 무시했다

| 진입 | 이동량 | 옛 램프 | 속도 | 새 램프 |
|---|---|---|---|---|
| 최초 (`b`) | 0.4894 rad | 2.0 s | 0.24 rad/s | **2.0 s (그대로)** |
| 복구 #1 | 1.5934 rad | 2.0 s | **0.80 rad/s** | 5.31 s |
| 복구 #3 | 1.6773 rad | 2.0 s | **0.84 rad/s** | 5.59 s |

`clamp(이동량 / 0.3, 2.0, 8.0)`. **안 망가진 경로(최초 진입)는 안 바꾼다** --
0.4894 rad 은 하한에 걸려 2.0 s 그대로다. 96도를 2초에 끄는 복구 재진입만 늘어난다.

### ④ `IS_READY` 를 "get-up 불가"로 읽어 중단했다

이미 서 있으면 SDK 가 `is_recovery_available=False` 로 답하는 것이 **정상**이다
("일으킬 수 없다"가 아니라 "일으킬 것이 없다"). 그것을 실패로 읽고 10 초 뒤
멈췄다. GetUp 을 건너뛰고 완료 판정으로 넘어가게 했다 -- 거기서 정지/직립을
확인하므로 이르게 들어가지 않는다.

### ⛔ 아직 안 고친 것 — `b` 구간이 붙잡는 자세

`--hold-diag 15` 로 잰 결과: **관절 추종은 완벽한데(전 관절 max 0.0108 rad =
0.6°, 발목 ≤1.5°) 몸통 tilt 가 4.9–13.8°** 로 15 초 내내 흔들린다. 관절이
명령대로 가 있는데 몸이 기울어 있다 = **명령한 자세 자체가 곧게 서지 못한다.**

평면 다리에서 발이 평평하고 몸통이 수직이려면 `hip + knee + ankle = 0` 이어야 한다:

| | Hip | Knee | Ankle | 합 |
|---|---|---|---|---|
| 첫 deploy (`73e71f3` 이전, `prepare` 자세) | −0.1 | 0.2 | −0.1 | **0.00** |
| 지금 (`common.default_qpos`) | −0.2 | 0.4 | −0.25 | **−0.05** |

`b`~`r` 구간에는 **균형을 닫는 주체가 없다** -- 관절 위치 서보만 돈다. 첫
deploy 는 합이 0 인 자세를 잡아 개루프로 버텼고, 지금은 **정책이 있어야만 설 수
있는 자세**를 정책 없이 붙잡는다. `r` 을 누르면 멀쩡해지는 이유가 이것이다.

수정 방향: `b` 에서는 합=0 인 prepare 자세를, `r` 에서 RL 자세로 짧게 램프하며
게인 교체. **아직 안 넣었다** -- 위 넷과 변수를 섞지 않기 위해서다.

### 기록 사고

위 코드 넷은 **`544642a "ND_dwell + eval_round 실패 전파"` 안에 들어가 있다.**
병렬 세션이 `git add -A` 로 내 작업 트리를 같이 커밋했다. 코드는 정상이지만 그
커밋 메시지에 이 변경의 근거가 없어서 여기에 남긴다.

---

## 8-49. 병렬 세션 감사를 받아 내 결론 두 개를 철회한다 (2026-08-07 19:xx)

`STATUS_20260807_1800.md`(병렬 Codex 세션)를 읽고 서버에서 직접 재확인했다.
지적 세 개 중 **셋 다 맞고 전부 내 오류다.**

### ⛔ 철회 1 — "N0_ctrl이 세 축 모두에서 path arm 전부를 지배한다"

§8-47과 그 이후 보고에서 이렇게 썼다. **틀렸다.** 비교가 이랬다:

| | N0_ctrl | N1/N2 | 어느 체크포인트인가 |
|---|---|---|---|
| 보행 속도 | 1.10 | 1.48 / 1.54 | 둘 다 `model_6000` |
| 보행 낙상률 | 0.0035 | 0.0007 / 0.0005 | 둘 다 `model_6000` |
| **정확도** | **2.82** | 47.70 / 42.21 | **N0는 `best(2200)`, N1/N2는 `model_6000`** ⛔ |

**정확도 행만 체크포인트가 다르다.** 속도와 보행 낙상률에서는 path arm 이 N0 를
확실히 이긴다(속도 1.4배, 낙상률 5–7배 낮음). 지배는 **정확도 한 축에서만** 성립한다.

그리고 `N0_ctrl model_6000` 의 정확도는 **측정된 적이 없다** — 서버 확인 결과
`*_final.accuracy` 디렉터리는 `N1_path` 것 하나뿐이다. `eval_round.sh` 의 4단계가
마지막 체크포인트를 **보행으로만** 재고 정확도로는 안 재기 때문이다.
`004-eval_N0final_acc` 로 큐에 걸었다(3분).

이건 §8-47에서 내가 직접 *"한 축으로 뽑아 다른 축으로 채점하는 것이 이 저장소가
반복한 오류"* 라고 쓴 바로 그 오류다. 같은 보고서의 요약표에서 그걸 저질렀다.

### ⛔ 철회 2 — "이 arm 들은 N1 의 최종 정책에서 출발한다"

`tools/stage_obs_arms.sh` 주석이 그렇게 말하는데 코드는 `CK="$D/nn/best.pth"` 였고,
서버 실측:

    best.pth -> model_100.pth

`best.pth` 는 `select_best_checkpoint.py --link_best` 가 매번 다시 만드는 **심볼릭
링크**이고, N1_path 의 정확도 승자는 **붕괴 전인 iteration 100** 이다. 그래서
`NA_histzero` / `NZ_zeroiid` / `NC_actfilter` 세 arm 이 이미 그 계보로 돌았다.

결과 두 가지:
1. **"N1 대 NA/NZ/NC" 는 한 레버 비교가 아니다.** 출발점이 다르다.
2. **출발점이 가변이다.** 평가를 한 번 더 돌리면 앞으로 나갈 arm 의 warm start 가
   조용히 바뀐다 — 재현 불가능한 실험이 된다.

`WARM_IT` 로 iteration 번호를 못 박도록 고쳤다.

⭐ 다만 이 사고가 **우연히 더 나은 실험을 만들었다**: model_100 계보 세 arm 이
붕괴 전 지점에서 출발해 6000 iteration 을 더 돌았는데, 텐서보드로 보면
`goal_reached` 가 NC 1.78 / NZ 1.78 로 **N1(1.03)의 1.7배**다. `NA_histzero` 도
best 가 iteration 3300(5.47 cm)으로 안 무너졌다. 즉 **붕괴는 학습 예산의 함수가
아니라 출발점의 함수일 수 있다.** 그 그룹의 대조군(model_100 + plain N1 6000)이
없는 것이 지금 가장 싸게 메울 구멍이다.

### ⛔ 철회 3 — `ND_dwell` duty (§8-48에서 이미 정정)

`interval_s` 는 간격이 아니라 **주기**다(`goal_pose_v7.py:317` 의 `path_dwell_next`
가 dwell 중에도 감소한다). duty = duration/interval. `[4,8]` 은 40 % 가 아니라
66.7 % 였고 겹침으로 영구 dwell 이 되는 env 가 6 % 생겼다. `[8,12]` 로 고쳤다.

### 이 세 개의 공통 패턴

**보고 있는 것은 검증하고, 그 옆에 있는 것은 가정했다.**

- config diff 는 봤는데 그 키의 **코드 의미**는 안 봤다 (ND_dwell)
- 수술의 **순전파 항등**은 검증했는데 옵티마이저 상태는 안 봤다 (NA rc1)
- 작업이 **도는지**는 봤는데 `best.pth` 가 무엇인지는 안 봤다 (warm start)
- 감시자를 **만들었는데** 그 발동 경로를 한 번도 안 돌려봤다 (pgrep 버그)
- 표를 **만들면서** 행마다 체크포인트가 같은지는 안 봤다 (지배 주장)

매번 실패한 조각이 내가 확인한 것에서 **한 걸음 옆**에 있었다.

---

## 8-48. ⛔ 판정 — RL hold 자세는 개루프로 설 수 없다. 실기 두 값이 다 재현됐다 (2026-08-08)

분석 세션의 판정 측정 의뢰. `tools/hold_diag_mujoco.py`, `K1_serial_realmass.xml`
(실물 19.666 kg / 발 0.494 kg), **prepare 게인**, 정책 없이 PD 만, 10 s.

### ① 전제 확인 — 실제로 닿는 geom (MuJoCo API 직접 질의)

| geom | type | contype | conaffinity | 판정 |
|---|---|---:|---:|---|
| 31 / 42 | mesh | **0** | **0** | 시각 전용, **닿지 않음** |
| 32 / 43 | **box** | 1 | 1 | **유일한 충돌체** |

**접촉 x 범위 = [−0.0660, +0.0940] m** (pos 0.014 ± size 0.08).
학습 `feet_edge_pos` 교정본과 **정확히 일치**한다.

> ⛔ **RETRACTIONS C25 정정.** C25 는 *"geom 이 셋이고 충돌 메시가 기록에서 누락됐다,
> 실제 접촉면은 메시이고 box 는 안 닿는다"* 고 적었다. **현재 파일에서는 틀렸다.**
> 두 번째 mesh geom 은 XML 에서 **주석 처리**돼 있어(`<!-- <geom type="mesh" ...> -->`)
> MuJoCo 가 생성하지 않는다. 모델에 존재하는 발 geom 은 **둘**이고 닿는 것은 box 다.
> 검증 셋: (a) `geom_contype/conaffinity` 직접 질의, (b) A 자세가 CoP +0.0048 m,
> 수직력 192.9 N(=mg 정확히)으로 안정적으로 섬. **따라서 "두 시뮬레이터가 다른 발로
> 돌았다"도 성립하지 않는다** — Isaac 과 MuJoCo 가 같은 box 를 쓴다.

### ② 자유 조건 — 실기 13.8° 재현

| 자세 | pitch 사슬 합 | tilt @1 s | tilt 최종 | 관절 추종오차 |
|---|---:|---:|---:|---:|
| **A** prepare | 0.00 rad | **0.05°** | **0.07°** | 0.47° |
| **B** RL | −0.05 rad | **14.11°** | **91.88°** (완전 전도) | 9.12°(전도 중) |

**B 의 1 초 tilt 14.11° 대 실기 13.8° — 0.3° 차이.** A 는 0.07° 로 완벽히 선다.

### ③ 발 평평 조건 — 실기 4.9° 재현

발 용접은 두 번 실패했다(아래 ⑤). 대신 **기구학으로 정확히** 답했다 — 발바닥이 지면에
평평하면 몸통은 pitch 사슬 합만큼 기운다:

| 자세 | 발바닥 pitch (몸통 수직 기준) |
|---|---:|
| A | **−0.00°** |
| B | **+2.86°** |

**2.86° + 로봇 자체 standing 1.6° = 4.5° 대 실기 4.9°.** 분석 세션의 정적 예측과 일치.

### ④ 기전 — 토크 한계가 아니라 **지지면 한계**다

B 의 전도 전 구간(발 앞경계 +0.094 m, 발목 한계 20 N·m):

| t (s) | tilt° | **CoP x (m)** | 발목 토크 | 수직력 N |
|---:|---:|---:|---:|---:|
| 0.20 | 3.30 | 0.0350 | 4.4 | 192.0 |
| 0.40 | 7.68 | **0.0826** | 8.6 | 190.0 |
| 0.60 | 9.75 | **0.0827** | 8.8 | 192.8 |
| 1.00 | 14.11 | **0.0825** | 8.7 | 191.4 |
| 1.20 | 19.62 | 0.0819 | 8.4 | **184.9** ← 뒤꿈치 뜨기 시작 |
| 1.40 | 32.05 | 0.0808 | 7.0 | **152.6** |

대조 A: CoP **+0.0048 m**(발 중앙 부근), 발목 토크 **1.1 N·m = 한계의 5 %**, 수직력 192.9 N 일정.

**CoP 가 0.4 초 만에 +0.083 m 로 올라가 앞경계(+0.094)의 88 % 지점에 붙어 고정된다.**
그 뒤 tilt 만 자라고 CoP 는 더 못 간다 — **지면은 접촉면 안에서만 밀 수 있기 때문이다.**

⇒ **가설 H 확증. 단, 기전은 구동이 아니라 기하다.** 발목 토크는 8.8/20 = **44 %** 로
여유가 있다. 토크를 더 줘도 CoP 를 발 밖으로 보낼 수 없으므로 복원 모멘트가 안 는다.
(검산: 발당 수직력 ≈ 96 N × 0.083 m ≈ 8.0 N·m ≈ 관측 8.8.)

### ⑤ 못 한 것 — 정직하게

**발 용접(`--weld-feet`)은 두 번 다 실패했다.** MuJoCo weld 가 `qpos0` 기준 상대자세를
강제해 시험 자세를 끌어당겼고(수직력 체중의 4 배, 추종오차 90°), `eq_data` 를 현재
자세로 재앵커해도 발산했다(수직력 1714 N = 체중 9 배). **weld 앵커 의미를 더 파는 대신
기구학으로 답했다** — ③ 이 그것이고, 구속 솔버 잡음이 안 섞이므로 오히려 깨끗하다.
**"발 용접 동역학"은 미확인으로 남는다.**

### ⑥ 부수 정정 — config 주석의 여유값이 옛 발이다

`Goal_Pose_E0.yaml` prepare 주석: *"발끝까지 여유가 12.15 cm"*. 그 12.15 cm 는
**V7 베이스의 옛 `feet_edge_pos`(x max 0.1215)** 다. 실제 충돌 box 의 앞경계는
**+0.094 m = 9.4 cm** 이고, 교정된 `feet_edge_pos` 와 같다. **여유가 23 % 더 작다** —
가설이 약해지는 게 아니라 강해진다.

### 수정안

**prepare 자세를 hold 구간의 유일한 자세로 쓴다**(이미 `prepare.default_qpos` 에 있고
합이 0 이다). RL 자세(합 −0.05)를 균형 제어기 없이 붙잡는 구간이 있으면 안 된다.
`b`→`r` 사이에 RL 자세로 미리 옮기는 경로가 있다면 그것을 제거하고, 전환은 정책이
돌기 시작한 뒤에 한다.

---

## 8-51. 감시가 "채점됐다"를 두 번 놓쳤다 — 판정 근거를 이름에서 데이터로 옮겼다 (2026-08-08 14:xx)

(§8-50 은 이 파일에 없다. CLAUDE.md 가 §8-50 으로 가리키는 철회는 §8-49(4275행)와
`RETRACTIONS.md` 에 있다.)

### 무엇이 잘못됐나

`round_status.sh` 의 `채점 상태` 표가 **이미 채점이 끝난 run 셋을 "미채점" 으로 찍었다** —
`N9_zerostruct` · `NB_zerocritic` · `NA_histzero`. 셋 다 model_6000 으로 두 축 다 채점돼
있었고 리포트도 `logs/eval_rounds/lineageB/` 에 그대로 있었다.

원인은 판정 근거를 **파일 이름 규칙**에 둔 것이다. 코드는
`logs/eval_rounds/*/<run>.accuracy` 를 글로브했는데, 현행 작업 스크립트가 쓰는 이름은
`<run>_final.accuracy` 이거나 아예 `<arm>_final.accuracy`(타임스탬프 없음)다. 글로브가
하나도 안 맞는다. **같은 거짓음성을 오늘 하루에 두 번 냈다** — 오전에 "run 안의 `eval/`
만 본다" 를 고쳤는데, 그 수정 자체가 또 이름 규칙에 기댔다.

이게 왜 위험한가: 그 표를 믿고 재채점하면 **GPU 를 그냥 버린다**. 유휴 루프의 4번
분기가 정확히 "미채점 run 이 있으면 채점을 건다" 이다.

### 수정

판정을 규칙이 아니라 **데이터**에 둔다. 리포트는 자기 `checkpoint` 경로를 `report.json`
안에 적으므로, 그 경로에 run 이름이 들어 있는지로 본다. 파일명을 어떻게 붙이든 맞는다.
덤으로 **어느 iteration 으로 채점됐는지**를 같이 찍는다:

```
2026-08-07-16-48-06_NZ_zeroiid   ckpt=60  채점됨 10건 [best,model_1700,model_6000]
2026-08-07-21-00-45_NE_ctrl100   ckpt=60  채점됨 12건 [best,model_100,model_6000]
```

`best.pth` 가 arm 마다 다른 iteration 을 가리켜 "레버 차이"와 "학습량 차이"가 교락된
사고(RETRACTIONS C3)가 이 열에서 **눈으로** 보인다.

같은 커밋에서 고친 것 둘 더 (전부 "신호가 있어야 하는데 안 나가는" 같은 모양):

- 워커를 `gpu_queue.sh` 로만 세서 **작은 레인 워커 둘이 어느 숫자에도 없었다.** 죽어도
  신호가 안 난다. 레인은 스크립트 이름이 아니라 환경변수 `LANE` 에 있으므로
  `/proc/<pid>/environ` 을 읽어 레인별로 센다. 수정 후 `queue_workers_alive` 가 2 → **4**.
- `autopilot` 의 큰 레인 워커 복구가 `$4 == 카드번호` 만 봐서 **같은 카드의 작은 레인
  워커를 큰 레인 워커로 오인**한다 → 큰 레인이 죽어도 복구가 발동하지 않는다. 고아
  `.running` 복구도 무조건 gpu0 큰 레인이었다 — `.owner` 의 카드·레인으로 되돌린다.

커밋 `08d7080`.

### `eval_round.sh` 4단계도 같이 고쳤다

4단계(마지막 체크포인트)가 **보행만, 그것도 60 s** 로 쟀다. 둘 다 틀렸다.

1. 정확도가 없어서 마지막 체크포인트끼리 arm 을 비교하려면 매번 별도 작업을 한 번 더
   걸어야 했다 — `N0_ctrl` · `N1_path` · `NF_dwellclock` 에서 **세 번** 그랬다.
2. 60 s 는 2·3단계(120 s)와 프로토콜 길이가 다르다. 길이가 다른 두 수를 나란히 놓은 것이
   "118배" 오류였고 `claim_check` 도 `duration_s` 불일치를 ⛔ 로 막는다. 즉 **예전 4단계
   결과는 3단계와 비교 불가였다.**

→ 120 s, 두 축으로 맞췄다. arm 당 채점 시간은 늘지만 비교가 성립한다.

### 같은 시각에 건 것: `NG_armature` 채점 + armature 2×2

`NG_armature`(= `NE_ctrl100` + `asset.armature` 0.0→0.02, warm start 동일) 가 6000 을
끝냈다. 채점을 걸면서 **숫자를 보기 전에** 한계를 못 박아 둔다:

⛔ 공통 프로토콜(`sweeps/N0_ctrl.yaml`)의 `asset.armature` 는 **0.0** 이고
`make_eval_cfg` 는 `observation.*` 만 옮긴다. 즉 **레버가 채점 환경에 없다.**
`NC_actfilter` 와 같은 결함이다(그때는 채점이 끝난 뒤에 알았다). 023 이 답하는 질문은
"armature 0.02 로 학습한 정책을 0.0 물리에 옮기면?" 이지 "armature 를 켜면 좋아지는가?"
가 아니다.

그래서 024 를 같이 걸었다 — 공통 프로토콜에서 **`asset.armature` 한 키만** 0.02 로 바꿔
`NG_armature` 와 `NE_ctrl100` 을 **같은 물리**에 놓는다(바뀐 줄이 하나임을 sed 전에 세고
diff 를 로그에 남기며, 두 줄이 아니면 `exit 1`). 이미 있는 `NE@0.0` 과 023 의 `NG@0.0`
을 합치면 2×2 가 닫힌다:

| | 채점 물리 0.0 | 채점 물리 0.02 |
|---|---|---|
| **NE_ctrl100** | 40.92 cm (기존) | 024 |
| **NG_armature** | 023 | 024 |

한쪽만 재면 "정책 차이"와 "채점 물리 차이"가 교락된다. 채점 물리가 다르므로 라운드
디렉터리도 분리했다(`logs/eval_rounds/armature02`) — 같은 폴더에 두면 `round_table` 이
서로 다른 물리의 행을 한 표에 세운다.

### 다음

- 023/024 결과가 나오면 **두 축 + 2×2** 로 보고한다. `claim_check` 를 먼저 돌린다.
- `NH_zeroclock`(영점 + 보행시계 정지, 두 레버 합산) 은 사전등록 기준으로 판정한다:
  **< 25.61 cm 이고 낙상간격 > 13,690 s → 합산 성립.** 하나만 → 두 레버가 경쟁.
  둘 다 아니면 → 상호작용이 음(−).

---

## 8-52. ⛔ armature — 두 물리는 서로 전이되지 않는다. 그리고 자산 셋이 서로 다른 값을 말한다 (2026-08-08 15:5x)

### 2×2 (같은 체크포인트 model_6000, 같은 공통 프로토콜, 채점 물리만 다르다)

`claim_check` 결과: 0.02 짝비교는 **⛔ 하나도 없다**(protocol_sha·env_code_sha 까지 동일).
0.0 쪽과의 비교는 ⛔ 둘이 떴고 직접 확인해서 무해함을 확인했다 — cfg diff 가
`description` + `history_stride: 1`(기본값, `history_steps<=1` 이면 코드가 먼저 return)
두 줄뿐이고, env 커밋 차이는 ① 측정 전용 누산기(no_grad) ② stride 코드(k≤1 우회)
③ `joint_zero.target_fall_rate`(채점 cfg 는 `joint_zero.enabled: false`) 뿐이다.

| 정책 \ 채점 물리 | armature **0.0** | armature **0.02** |
|---|---|---|
| `NE_ctrl100` (0.0 학습) | 40.92 cm / 낙상간격 **788 s** / 순항 22.8 % | 42.80 cm / **62 s** / 19.2 % |
| `NG_armature` (0.02 학습) | 35.91 cm / **1.5 s** / 0.013 % | 40.96 cm / **3,740 s** / **47.2 %** |

**두 물리는 서로 전이되지 않는다.** 각 정책은 자기가 학습한 물리에서만 선다 —
NG 는 0.0 에서 1.5 초마다 넘어지고 0.02 에서 3,740 초를 간다(2,500배). NE 는 반대로
788 초 → 62 초(13배 악화). 정확도 축은 40 cm 근방에서 거의 안 움직인다. 즉 armature 는
**정확도가 아니라 안정성의 축**이고, 그 크기가 우리가 지금까지 조정해 온 어떤 레버보다 크다.

⭐ `NG_armature @ 0.02` 는 지금까지 잰 **보행 셀 중 최고**다: 순항체류 47.2 %,
낙상간격 3,740 s, >1.0 m/s 98.4 %, 속도 1.32 m/s, 발간격 18.7 cm. 대가는 토크다 --
토크점유 87.8 % / 포화 5.01 %(NE 73.4 / 2.21).

### ⛔ 그런데 "0.02 가 맞다"도 아직 아니다 — 자산 셋이 서로 다르다

`070-NG_armature` 작업 주석의 전제 *"저장소의 다른 config 는 전부 0.02"* 는 **틀렸다.**
직접 세었다:

- **0.0**: `T1/Base_Walk` · `T1/Parameter_Walk` · `T1/Kicking` · `K1/Parameter_Walk` ·
  `K1/Kick` · `K1/Goal_Pose` · `K1/Goal_Pose_V7` · **`K1/Goal_Pose_V8`** — 8개
- **0.02**: `K1/Goal_Pose_V3` · `K1/Safe_Fall` · `K1/Get_Up`("official Booster USD value") — 3개

즉 **보행 계열은 0.0 이 다수**이고, 0.02 는 get-up/safe-fall 계열이다.

그리고 세 번째 값이 있다. **MuJoCo 자산 `resources/K1/K1_serial.xml`** — 실기 재현에
쓰는 바로 그 자산 — 은 **관절별**로 적어 뒀다:

| 관절 | armature |
|---|---:|
| Head yaw/pitch | 0.002 |
| Shoulder/Elbow | 0.05 |
| **Ankle Pitch/Roll** | **0.05** |
| **Hip P/R/Y, Knee** | **속성 없음 = 0** |

`K1_serial_realmass.xml` 도 같다(0.05 가 8개). **어느 자산도 "전 관절 0.02" 를 말하지
않는다.** NG 는 22관절 전부에 0.02 를 걸었으니 0.0 과도 자산과도 다른 세 번째 물리다.

⭐ 그리고 이 관절별 분포가 `020-eval_dofvel` 실측과 정확히 맞물린다: **발목 roll 이
속도 한계를 시간의 27~55 % 동안 넘고 peak 88 rad/s**, 다른 관절은 1 % 미만.
자산이 반사관성을 준 유일한 다리 관절이 발목이고, 우리 학습은 거기에 0 을 줬다.

### 배포 계보도 0.0 이다

`2026-08-04-09-48-36_I3b_stance10/config.yaml:30` = `armature: 0.0`. 즉 **실기에 올라간
정책도 armature 0 물리에서 학습됐다.** 실기가 0.05(발목) 쪽이라면 배포 정책은 위 표의
`NE @ 0.02` 칸과 같은 처지다 -- 그 칸이 낙상간격 62 초다. ⚠️ 다만 이것을 실기 증상의
설명으로 승격하지 마라: 62 초는 "세 걸음"이 아니고, §8-45 가 배포 필터로 증상 자체를
이미 재현·제거했다. armature 는 **추가 후보**이지 대체 설명이 아니다.

### 다음 (사전 등록)

1. **관절별 armature 를 Isaac 에 넣는다.** 지금은 `asset_options.armature` 스칼라라
   전 관절 동일하다(goal_pose.py:51). `asset.armature_by_joint` 키를 추가하고
   **키가 없으면 완전한 no-op** 으로 둔다(`randomization.ankle_gain` 이 쓴 패턴 그대로).
   그 위에서 `NJ_armasset` = MuJoCo 자산과 같은 분포(발목·팔 0.05 / 머리 0.002 / 나머지 0).
   대조군은 `NG_armature`(전 관절 0.02) 와 `NE_ctrl100`(0). 스칼라 세 점이 아니라
   **자산이 말하는 분포**를 재는 것이 요점이다.
2. **벤더 값을 확인한다.** armature = (기어비)² × 로터관성. K1 액추에이터 사양이 있으면
   시뮬로 추정할 필요가 없다. 이건 GPU 가 아니라 문서/제조사 쪽이라
   `DEPLOY_REQUESTS_FROM_TRAINING.md` 에 R7 로 올린다.
3. 판정: `NJ_armasset` 이 `NG@0.02` 의 순항체류 47.2 % / 낙상간격 3,740 s 에 준하면
   → 자산 분포로 학습해도 잃는 것이 없다 = 그쪽이 근거 있는 기본값이다.
   크게 낮으면 → 이득의 출처가 "발목 반사관성"이 아니라 "전 관절 관성 증가"라는 뜻이고,
   그건 실기에 없는 물리를 학습에 넣어 안정성을 산 것이라 배포에 못 쓴다.

---

## 8-53. ⭐ NH_zeroclock 9.33 cm — 합산이 성립한다. 지금까지 최고 셀 (2026-08-08 16:2x)

두 축 모두 대조군을 이긴 레버 둘(`joint_zero(iid)` · `pause_gait_during_dwell`)을 합친
arm 이다. 사전등록한 기준을 **둘 다 통과**했다.

계보 A, 전부 `model_6000`, 같은 공통 프로토콜(`claim_check` 로 확인):

| model_6000 | 위치오차 | strict | 정확도축 낙상간격 | 표본탈락 | 보행속도 | 순항체류 | 보행축 낙상간격 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `N1_path` (대조군) | 47.70 cm | — | 3,917 s | — | 1.48 | — | — |
| `NF_dwellclock` | 31.96 cm | 4.6 % | 13,690 s | 0.043 % | 1.48 | 35.6 % | 1,662 s ⚠60 s |
| **`NH_zeroclock`** | **9.33 cm** | **18.9 %** | **27,300 s** | 0.022 % | 1.42 | 32.4 % | **9,978 s** |

**정확도 3.4배, 낙상간격 2배.** 속도·순항체류는 NF 와 사실상 같다(1.42 대 1.48,
32.4 % 대 35.6 %). 즉 **판 것이 없다.** 사전등록 기준 "< 25.61 cm 이고 낙상간격 >
13,690 s → 합산 성립, 배포 후보 1순위" 를 그대로 만족한다.

⚠️ 조건 확인(그냥 나란히 쓰지 않는다):
- `ckpt_iter` 는 둘 다 6000 이다. NF 의 `best.pth` 행(7.34 cm)은 **model_100** 이라
  같은 표에 세우면 안 된다 -- 그 행은 낙상 157 · 표본탈락 3.3 % 다.
- `env_code_sha` 가 갈렸다(NF `30aecafb`, NH accuracy `9e845026`, NH walk `9e9eefa3`).
  세 커밋을 직접 봤다: `9e84502` 는 `torch.no_grad()` 측정 누산기뿐이고,
  `83c1538`·`9e9eefa` 는 관절별 armature 인데 **키가 없거나 빈 dict 면 no-op** 이다
  (음성 대조 실측: `armature_by_joint: {}` config 로 돌리면 `[armature]` 줄 0개).
- 보행축 낙상간격은 NF 가 **60 s**, NH 가 120 s 프로토콜이다. `낙상간격` 은 길이로
  정규화된 값이라 비교 가능하지만, 이 60 s 는 오늘 고친 `eval_round.sh` 4단계의
  옛 동작이다(§8-51). 앞으로는 안 생긴다.
- ⛔ 사전등록 문턱 자체에 결함이 있었다: `25.61 cm` 는 **계보 B**(`NZ_zeroiid`) 값이라
  계보 A 인 NH 에 그대로 쓰면 RETRACTIONS C9 을 반복한다. 다행히 `13,690 s` 는 계보 A
  (`NF`)의 **정확도축** 값이고 NH 의 같은 축 값(27,300 s)과 맞물린다. 위 표는 계보 A
  안에서만 비교했다.

### 이것이 `NI_conw` 의 전제를 부분적으로 반증한다

§8-51 에서 "25.61 cm 는 보상 구조의 정직한 최적해에 가깝다"고 계산했다. NH 는 보상을
한 글자도 안 바꾸고 **9.33 cm** 로 갔다. 즉 **25 cm 대의 병목은 보상이 아니라
보행시계/영점이었다.** `NI_conw`(NZ + `constellation_weight`)는 계속 돌리되, 그 결과를
"보상 레버의 효과"로 읽을 때 이 사실을 같이 적어야 한다.

그러나 같은 계산이 9.33 cm 에서는 **더** 유효하다. 계산의 핵심은 `goal_reached` 문턱이
**10 cm** 이고 그 안쪽이 평평하다는 것이었는데(10→5 cm 가 도착 보너스의 0.5 %),
NH 가 멈춰 선 곳이 정확히 그 문턱 바로 밖이다. 게이트는 5 cm 다.

### 다음: `NK_clockconw` = NH + `constellation_weight` 하나

큐에 올렸다(계보 A, warm start `I3b_stance10/model_200` 로 NH 와 동일하게 못 박음).
판정: **< 9.33 cm 이고 낙상간격이 27,300 s 의 절반 이상**이면 게이트 앞 마지막 구간의
병목이 보상이 맞다. 안 변하면 남은 용의자는 **횡방향**이다(§8-51: 접근방향 잔차
-0.02 m 대 횡방향 |오차| 0.17 m, `feet_distance_ref` 0.18 m).

---

## 8-54. NI_conw — 보상 레버는 정확도를 사고 **안정성을 판다**. §8-53 의 한 문장을 정정한다 (2026-08-08 20:5x)

계보 B, `model_6000`, 대조군 `NZ_zeroiid`, 레버는 `rewards.constellation_weight`
0.2 → 1.0 **하나**(diff 확인).

| 축 | 지표 | `NZ_zeroiid` | `NI_conw` | |
|---|---|---:|---:|---|
| 정확도 | 위치오차 med | 25.61 cm | **9.61 cm** | 2.7배 좋다 |
| 정확도 | strict | 8.64 % | **11.59 %** | |
| 정확도 | 낙상간격 | 27,340 s | **4,590 s** | ⛔ 6배 나쁘다 |
| 정확도 | 표본탈락 | 0.022 % | 0.130 % | |
| 보행 | body_speed | 1.45 | **1.77 m/s** | 빨라졌다 |
| 보행 | 순항체류 | 26.7 % | **13.3 %** | ⛔ 절반 |
| 보행 | 낙상간격 | 14,970 s | **966 s** | ⛔ 15배 나쁘다 |

`claim_check`: `env_code_sha` 만 갈렸다(`5a985b1b` 대 `9e9eefa3`). 직접 확인했다 --
그 구간 `envs/` 변경은 **114줄 전부 추가**이고, 추가된 실행 줄이 **전부
`with torch.no_grad():` 블록 안**(토크·관절속도 점유 누산기)이거나 내 관절별 armature
코드(키가 없으면 no-op, 음성 대조 실측)다. 즉 무해하다.

### ⛔ §8-53 의 한 문장을 정정한다

§8-53 에 이렇게 썼다: *"25 cm 대의 병목은 보상이 아니라 보행시계/영점이었다."*
**너무 센 문장이었다.** NI 가 보상만 건드려 25.61 → 9.61 cm 로 갔다. 보상도 병목이었다.

정확한 문장은 이것이다: **25.6 cm 에서 ~9.5 cm 로 내려가는 길이 둘 있고, 값이 다르다.**

| 경로 | 정확도 | 그 대가 |
|---|---:|---|
| 보상 (`NI_conw`, constellation_weight) | 9.61 cm | ⛔ 낙상간격 6~15배 악화 |
| 보행시계+영점 (`NH_zeroclock`) | 9.33 cm | **없다** — 자기 대조군보다 낙상간격 2배 **좋다** |

두 경로가 같은 정확도에 도달하는데 **한쪽만 공짜다.** 그래서 배포 후보는 여전히
`NH_zeroclock` 하나다. (⚠️ 두 수는 계보가 달라 정확도 9.61 대 9.33 을 직접 겨루면 안 된다
-- 각자 자기 대조군 대비로만 읽었다.)

### 그래서 `NK_clockconw` 가 판정 실험이 된다

`NK` = NH + `constellation_weight` 하나(도는 중). 위 표가 예측을 갈라 준다:
- **낙상간격이 NH(27,300 s)보다 크게 낮아지면** → 보상 레버의 안정성 비용이 계보·기반과
  무관한 성질이다. 그러면 5 cm 게이트는 보상 sharpening 으로 가면 안 된다.
- **정확도가 9.33 cm 아래로 내려가면서 낙상간격이 유지되면** → NH 기반에서는 비용이
  없다는 뜻이고, 게이트 통과 후보가 된다.
- 둘 다 아니면 → 9 cm 근방의 병목은 보상이 아니다. 남은 용의자는 **횡방향**이다
  (§8-51: 접근방향 잔차 -0.02 m 대 횡방향 |오차| 0.17 m ≈ `feet_distance_ref` 0.18 m).

---

## 8-55. N3_pathcross 는 무효(null)에 가깝다 — 그리고 **레버가 겨냥한 양을 평가가 못 보고 있었다** (2026-08-08 21:1x)

계보 A, `model_6000`, 대조군 `N1_path`, 레버는 `rewards.scales.feet_cross` -0.0 → -20.0 하나.

| 축 | 지표 | `N1_path` | `N3_pathcross` |
|---|---|---:|---:|
| 정확도 | 위치오차 med | 47.70 cm | 44.58 cm |
| 정확도 | strict | 10.14 % | 8.91 % |
| 정확도 | 낙상간격 | 3,917 s | 5,488 s |
| 보행 | body_speed | 1.48 | 1.50 m/s |
| 보행 | 순항체류 | 26.6 % | 28.9 % |
| 보행 | 낙상간격 | 1,496 s | 1,360 s |

정확도 7 % 개선, 낙상간격 40 % 개선(정확도축), 보행은 사실상 동일. **어느 축에서도
판정을 뒤집는 크기가 아니다.** ⚠️ 보행축은 `N1_path` 쪽이 60 s 프로토콜이라(구 4단계)
원시 낙상 수는 비교 불가이고 낙상간격만 읽었다.

### ⛔ 그런데 진짜 문제는 이 표가 **레버가 겨냥한 양을 담고 있지 않다**는 것이다

`feet_cross` 는 정확도를 올리려고 넣은 보상이 아니다. 실기 증언 *"다리가 모이면서
발끼리 부딪혀서 넘어져"* 를 겨냥한 것이고, 실기 로그를 MuJoCo 운동학에 재생하면
**9.9 % 구간에서 좌우 발이 겹치고 p1 이 -16.35 cm** 다. 그 양을 봐야 판정이 된다.

그런데 라운드 표의 `발간격(cm)` = `_v7_extras.feet_width` 는 **결함이 둘**이다:

1. `_v7_extras` 는 롤아웃 끝에 **한 번** 호출된다 = 마지막 한 프레임(RETRACTIONS C11).
2. `(fy[:,0] - fy[:,1]).abs()` — **절댓값이라 교차의 부호가 지워진다.** 깊게 교차한
   자세(-16 cm)와 정상적으로 벌어진 자세(+16 cm)가 **같은 값**으로 찍힌다.

즉 **교차를 원리적으로 못 보는 열**로 교차 보상을 채점하고 있었다.

### 고쳤다 (커밋 `feat: 발 간격 롤아웃 누산`)

post-physics 의 `_refresh_feet_state()` 뒤에서 부호 있는 횡방향 간격을 누산한다
(토크·관절속도 누산기와 같은 자리·같은 방식, `torch.no_grad()` 라 학습에 영향 없다):
평균 / 절댓값 평균 / **최소** / 최대 / 음수·양수 체류 비율 / **접촉체류**(|간격| < 발 폭 7 cm).
부호 규약은 코드가 정하지 않는다 — 음수·양수 체류를 **둘 다** 찍어 읽는 사람이 판단한다.

**스모크(`NH_zeroclock` model_6000, 8 s, 400스텝, 256 env)에서 바로 드러났다**:

| 지표 | 값 |
|---|---:|
| 평균 간격 | **18.8 cm** (안전해 보인다) |
| 음수 체류(교차) | 0.0 % |
| **최소 간격** | **3.86 cm** ⛔ 발 폭 7 cm 미만 |
| 접촉체류 | 0.012 % |

**평균은 18.8 cm 로 완벽한데 가장 좁은 순간은 3.9 cm 다.** 실기에서 본 "평균 보폭은
정상인데 꼬리가 교차한다"와 같은 모양이고, 옛 열(마지막 프레임 0.1926)로는 영원히
"문제 없음"이었다. ⚠️ 8초 스모크 한 arm 이라 크기를 결론짓지 마라 — 확정된 것은
**이제 그 축이 보인다**는 것과, 옛 열로는 구조적으로 볼 수 없었다는 것이다.

### 다음

- 앞으로 도는 채점은 자동으로 이 열을 갖는다. **arm 간 비교는 `접촉체류(%)` 와
  `최소간격(cm)` 으로 한다** — 평균은 꼬리를 못 본다.
- `N3_pathcross` 재채점은 **급하지 않다.** 정확도·보행 두 축에서 무효였고, 재채점의
  가치는 "교차 보상이 꼬리를 실제로 줄였는가" 하나인데 그건 다음 라운드에서 같은 자로
  같이 잰다.

---

## 8-56. ⭐ NJ_armasset — 자기 물리에서 **낙상 0**, 그리고 물리가 어긋난 칸에서만 발이 교차한다 (2026-08-08 22:5x)

§8-52 가 연 질문("전 관절 0.02 가 아니라 **자산이 말하는 분포**면?")에 답이 나왔다.
`claim_check`: 자산 물리 짝비교는 **⛔ 하나도 없다**(protocol_sha·env_code_sha 동일 `07a1addf`).

| 정책 \ 채점 물리 | armature 없음(0.0) | **자산 분포**(발목 0.05) |
|---|---|---|
| `NE_ctrl100` (0 학습) | 40.92 cm / 낙상간격 788 s / 순항 22.8 % / 1.38 m/s | 42.74 cm / **428 s** / 25.8 % / **0.96 m/s** |
| `NJ_armasset` (자산 분포 학습) | 27.26 cm ⚠️표본탈락 **65.1 %** / **1 s** / 0.16 % | 38.75 cm / **낙상 0** / **49.0 %** / **1.40 m/s** |

`NJ@자기물리` 는 120 s forward_hold 에서 **낙상률 0.0000** 이고 순항체류 49.0 % 로
지금까지 최고다(`NG@0.02` 47.2 % / 3,740 s). §8-52 의 사전등록 기준을 **넘겨서** 충족한다:
*"NG@0.02 에 준하면 이득의 출처는 발목 반사관성이다"* → 22관절에 0.02 를 뿌릴 필요가
없다. **자산이 지목한 발목에만** 주면 되고, 그쪽이 근거가 있다.

### ⭐ 어제 없던 열이 바로 값을 했다

| | 접촉체류(%) | **최소간격(cm)** |
|---|---:|---:|
| `NE@자산물리` | 0.09 | **-26.91** ⛔ 교차 |
| `NJ@0.0` | 0.14 | **-17.09** ⛔ 교차 |
| **`NJ@자기물리`** | **0.00** | **+5.21** |

**물리가 어긋난 두 칸은 발이 깊게 교차하고, 맞는 칸은 한 번도 교차하지 않는다.**
실기 증언이 *"다리가 모이면서 발끼리 부딪혀"* 이고 실기 로그 재생 p1 이 **-16.35 cm**
인데, `NJ@0.0` 의 **-17.09 cm** 가 거의 같은 값이다.
⛔ 어제까지의 열(마지막 프레임·절댓값)로는 세 칸이 전부 "발간격 18~21 cm, 문제 없음"
이었다(§8-55). 이 판정은 **어제였다면 원리적으로 불가능했다.**

### 발목을 가리키는 독립 증거가 셋이 됐다

1. MuJoCo 자산 `K1_serial.xml` 이 반사관성을 준 **유일한 다리 관절**이 발목이다(0.05).
2. `020-eval_dofvel`: 속도 한계를 넘는 **유일한 관절**이 발목 roll 이다(27~55 %, peak 88 rad/s).
3. 위 표: 물리 불일치일 때만 발이 교차하고, 그 값이 실기 p1 과 거의 같다.

⚠️ **그래도 "실기가 0.05 다"는 아직 아니다.** 2x2 가 말하는 것은 "물리 선택이 지배적"
이지 "어느 쪽이 맞다"가 아니다. 그것을 닫는 것은 sim 이 아니라 **벤더 사양**
(armature = 기어비² × 로터관성)이고 `DEPLOY_REQUESTS_FROM_TRAINING.md` R7 이다.
그리고 정확도 축은 모든 칸에서 38~43 cm 로 거의 안 움직인다 -- armature 는
**안정성·속도의 축**이다(§8-52 와 같은 결론).

### 다음: `NP_clockarm` = NH + 자산 armature (큐)

두 갈래가 각각 자기 축에서 최고를 냈는데 아직 합친 적이 없다 --
`NH_zeroclock`(정확도 9.33 cm) 과 `NJ_armasset`(안정성, 낙상 0). NP 는 NH 에
`asset.armature_by_joint` 레버 **하나**를 더한 것이고 계보는 A(배포 경로)다.
⚠️ 채점은 **두 물리에서** 한다 -- 한쪽만 재는 함정에 §8-52·§8-56 에서 두 번 걸렸다.
판정 기준 세 갈래는 `make_v7_arms.py` 의 `NP_ARMS` 주석에 사전 등록했다.

---

## 8-57. NK_clockconw — 보상 sharpening 은 **최고 셀에서 이득이 0 이고 대가만 남는다**. 두 계보에서 같은 크기로 재현됐다 (2026-08-09 00:3x)

§8-54 가 건 판정 실험이다. 계보 A, `model_6000`, 대조군 `NH_zeroclock`,
레버는 `rewards.constellation_weight` 0.2 → 1.0 **하나**.

| 축 | 지표 | `NH_zeroclock` | `NK_clockconw` | |
|---|---|---:|---:|---|
| 정확도 | 위치오차 med | **9.33 cm** | 11.77 cm | ⛔ 나빠졌다 |
| 정확도 | strict | **18.9 %** | 10.7 % | ⛔ 거의 절반 |
| 정확도 | 낙상간격 | 27,300 s | 13,760 s | ⛔ 절반 |
| 보행 | body_speed | 1.42 | **1.71 m/s** | 빨라졌다 |
| 보행 | 순항체류 | **32.4 %** | 13.8 % | ⛔ 절반 이하 |
| 보행 | 낙상간격 | **9,978 s** | 680 s | ⛔ **15배 악화** |

### 두 계보에서 **같은 크기로** 재현됐다

| 레버 효과 | 계보 B (`NZ` → `NI_conw`) | 계보 A (`NH` → `NK`) |
|---|---|---|
| 보행 속도 | 1.45 → 1.77 (1.22배) | 1.42 → 1.71 (1.20배) |
| 보행 낙상간격 | 14,970 → 966 s (**15.5배 악화**) | 9,978 → 680 s (**14.7배 악화**) |
| 순항체류 | 26.7 → 13.3 % (절반) | 32.4 → 13.8 % (절반 이하) |
| 정확도 | 25.61 → 9.61 cm (**2.7배 개선**) | 9.33 → 11.77 cm (**악화**) |

**대가는 계보와 무관하게 같다**(속도 +20 %, 낙상간격 15배 악화, 순항체류 절반).
**이득만 기반에 의존한다** -- 25 cm 대에서는 정확도를 2.7배 샀고, 9 cm 대에서는
살 것이 남아 있지 않아 대가만 남았다.

⛔ 그러므로 **`constellation_weight` 는 5 cm 게이트로 가는 길이 아니다.** §8-54 의
세 갈래 중 첫째("게이트 앞 마지막 구간의 병목이 보상이다")가 **기각**됐다.

⚠️ `claim_check` 의 `env_code_sha` ⛔(`9e845026` 대 `07a1addf`)는 직접 확인했다 --
그 구간 `envs/` 변경은 내 커밋 셋(관절별 armature + 발 간격 누산기)뿐이고, 전자는
`armature_by_joint` 가 빈 dict 라 `if armature_by_joint:` 에서 걸러지며(음성 대조
실측: `[armature]` 줄 0개) 후자는 `torch.no_grad()` 안에서 `_ft_*` 에만 쓴다.

### 남은 길

NH 자신의 리포트가 이미 답을 가리키고 있었다: **최근접 거리 median 3.9 cm 로 이미
게이트 안에 들어갔다가 9.33 cm 로 나온다**, 그리고 *"도착 후 안 멈춤 -- 20 % 가
위치·방향은 맞았는데 속도 > 0.10 m/s"*. 즉 병목은 **"가서 서기"** 다.
그것을 겨냥한 `NM_goalstop`(= NH + `goal_stop` 0 → -3)이 돌고 있다(3,2xx/6,000).
`NP_clockarm`(= NH + 자산 armature)도 같이 돌고 있다(1,4xx/6,000).

---

## 8-58. 분석 세션의 벤더 사양이 §8-56 의 읽는 법을 바꾼다 + Isaac eval 영상이 22 GB 라 못 만들고 있었다 (2026-08-09 01:4x)

### ① 내가 잰 것은 "armature 를 켰다" 가 아니라 **"발목에만 켰다"** 였다

분석 세션이 벤더 공식 저장소에서 관절별 armature 를 확보했다(RETRACTIONS **C33**,
[VENDOR_ACTUATOR_SPEC_K1.md](VENDOR_ACTUATOR_SPEC_K1.md)):

| 관절 | 벤더 | 내 `_ARM_ASSET`(MuJoCo 추측) |
|---|---:|---:|
| Hip P/R/Y | 0.0283~0.0478 | **0** |
| **Knee** | **0.0956** (최대) | **0** |
| Ankle P/R | 0.0565 | 0.05 ✅ |

즉 `NJ_armasset`·`NP_clockarm` 은 **발목만 벤더에 가깝고 힙·무릎은 0** 이다. 그런데
벤더 기준 **가장 큰 값이 무릎**이다.

**§8-56 에서 살아남는 것과 죽는 것을 나눈다:**
- ✅ **살아남는다**: "발목에만 반사관성을 줘도 자기 물리에서 낙상 0 / 순항체류 49 % 가
  나온다" 는 실측이다. 그리고 "물리가 어긋난 칸에서만 발이 깊게 교차한다" 도 실측이다.
- ⛔ **약해진다**: 내가 쓴 *"자산이 지목한 발목에만 주면 되고, **그쪽이 근거가 있다**"*.
  MuJoCo 자산은 벤더가 아니었다. 힙·무릎 0 은 **벤더와 다르다.**
  `NQ_armvendor`(분석 세션이 큐에 올림, NP 와 레버 하나 차이)가 그 차이를 잰다.
- ⛔ **C34 로 하나 죽고 하나 강해졌다**: 벤더 Hip_Pitch 속도한계는 14.66 rad/s 라
  (우리 `boxfoot` 7.1 은 절반, 학습계열 18 은 과다 -- **둘 다 틀렸다**) 고관절 가설은
  죽었다. **발목 roll 하나만 남는다**(벤더 17.59, 측정 peak 76~92, 초과 27~55 %).
  §8-56 에서 내가 "발목을 가리키는 독립 증거 셋" 이라고 쓴 것 중 ②가 **더 강해졌다.**

⭐ 그리고 새 사실 하나: **벤더는 게인을 armature 에서 유도한다** — `kp = armature·(2π·4Hz)²`.
내 arm 들은 armature 만 바꾸고 `control.stiffness` 는 그대로다. 즉 **벤더 관점에서는
짝이 맞지 않는 조합**을 학습시킨 것이고, 물리 불일치가 그렇게 파괴적인 이유의 후보다.
(이것은 아직 실험으로 확인 안 했다. 확인하려면 armature 와 kp 를 같이 옮긴 셀이 필요하다.)

### ② ⛔ 내 Isaac 채점은 **전부 영상 없이** 돌았다 — 만들 수가 없었다

사용자 규칙은 *"모든 eval 은 2분짜리 영상을 남긴다"* 인데, Isaac 쪽은 그 규칙을
**구현이 못 받는다.** `base_task.render()` 가 프레임을 **1280×720 RGBA 로 매 스텝
파이썬 리스트에 쌓는다** — 장당 3.69 MB, 50 Hz × 120 s = 6,000 장 = **22 GB**.
기본값 8 초도 1.5 GB 다. 그래서 023~030 채점이 전부 영상 없이 돌았고,
**판정을 지는 셀에 영상이 없다**: `NH_zeroclock`(9.33 cm) · `NJ_armasset`(낙상 0) ·
`NE_ctrl100@자산물리`(발 교차 -26.91 cm).

고쳤다: `--record_video_stride` / `--record_video_size`. 기본값은 예전 그대로라
기존 명령은 안 바뀐다. `--record_video_s 120 --record_video_stride 2
--record_video_size 640x360` 이면 25 fps · 3,000 장 ≈ **2.8 GB** 로 들어온다.

⚠️ 프레임만 솎으면 **오버레이가 어긋난다** -- eval 은 `zip(camera_frames,
overlay_states)` 로 위치를 맞추는데 overlay 는 매 스텝 쌓인다. 마커가 엉뚱한 시점에
그려진 영상이 조용히 나오고, **눈으로 판정하라고 만든 영상이라 숫자가 틀리는 것보다
나쁘다.** env 가 `recorded_this_step` 을 알려 주고 eval 이 안 담은 스텝의 오버레이를
도로 뺀다(옛 env 는 `getattr` 기본값 True 로 예전과 동일).

**다음**: 위 세 판정 셀을 120 s 로 렌더해서 `logs/eval_rounds/VIDEO_INDEX.md` 에
적는다(MuJoCo 쪽 인덱스는 분석 세션이 이미 만들었다 -- Isaac 쪽은 비어 있다).

---

## 8-59. ⛔ 배포의 시계는 벽시계가 아니다 — 궤적 전체가 1.26배 시간 팽창해 있었다 (2026-08-09, 배포 세션)

분석 세션이 `real_walk.csv` 의 보행시계 1.51 Hz 를 지적했다. **독립 검증했고 맞다.
그리고 원인까지 내려갔다.**

### 실측 (91행, 2.379 s, walking=1, `gait_freq` 명령 2.0)

| 양 | 실측 | 설계 |
|---|---:|---:|
| `tick_dt_s` median | **0.0252 s** | 0.020 |
| `gait_process` 스텝당 증가 | 0.0400 | 0.0400 |
| 누적 위상 / 시간 | 3.596 주기 / 2.379 s = **1.512 Hz** | 2.0 |
| `low_state_age` median / p99 | **0.0010 / 0.0089 s** | — |

⭐ **`low_state_age` 가 1 ms 다.** 받는 데이터는 신선하다 — **느린 것은 루프다.**
데이터 부족이 아니라 루프가 굶는 것이라는 뜻이라, 원인 후보가 크게 좁아진다.

### 원인 — `Timer.get_time()` 이 벽시계가 아니다

`deploy/utils/timer.py` 전문:

```python
def tick_timer_if_sim(self):   self.counter += 1        # ← LowState 콜백에서만
def get_time(self):            return self.counter * 0.002
```

호출 지점은 `_low_state_handler` **한 곳뿐**이다. 즉 `time_now` 는
**LowState 개수 × 0.002** 이고, "LowState 가 정확히 500 Hz 로 온다" 는 **가정**이다.

⛔ **그런데 `advance_gait_clock` 의 docstring 은 이렇게 적고 있다**:

> *"Elapsed time is **measured** rather than assumed, so 2 Hz is 2 Hz in wall clock
> under loop jitter"*

**거짓이다.** 재는 대상이 그 자체로 가정이다. 문서가 코드를 잘못 설명한 채 오래 살았다.

### 사슬이 수치로 닫힌다

```
LowState 실측 396 Hz (설계 500, 비율 0.792)
  → Timer.counter 가 그만큼만 오른다
    → 정책 tick = 10 메시지 = 가상 0.020 s = 실제 10/396 = 0.0253 s
                                              (실측 0.0252, 오차 0.4 %)
      → 보행시계가 가상시간으로 적분 → 벽시계 2.0 × 0.792 = 1.584 Hz
                                        (실측 1.512, 잔차는 4×interval 클램프와 지터)
```

### ⭐ 이것이 배포의 **모든** 시간량을 같은 배율로 틀어 놓는다

`timer.get_time()` 을 읽는 곳은 셋이고 **전부** 걸린다:

1. **`next_inference_time`** — 정책이 50 Hz 가 아니라 **39.6 Hz** 로 돈다.
   ⇒ 행동 사이에 로봇이 학습보다 **26 % 더 멀리 넘어진다.** 물리는 안 느려지므로
   이것이 가장 무겁다.
2. **`advance_gait_clock`** — 보행 1.51 Hz (명령 2.0).
3. **`next_publish_time`** (`+= common.dt`) — 발행률이 LowState 율과 1:1 이므로
   **396 Hz**. EMA 0.8/0.2 의 tau 가 0.0113 s(설계 0.00896), fc **14.1 Hz**(설계 17.8).
   ⇒ `--rate-fixed-filter` 가 겨냥한 그 열화가 **실재하고 크기도 계산된다.**

### ⛔ 아직 안 갈린 것 — 고치는 방향이 여기에 달렸다

**로봇이 LowState 를 396 Hz 로 내보내는가, 아니면 500 Hz 인데 우리가 20 % 를 흘리는가?**

`low_state_age` 는 이것을 못 가른다(흘려도 우리가 보는 것은 늘 최신이라 age 는 작다).

- **로봇이 396 이면** → `Timer` 가 구조적으로 틀렸다. 벽시계(`time.monotonic()`)로
  바꾸는 것이 맞다. 그러면 보행은 2 Hz 로 복구되지만 **정책 tick 은 여전히 39.6 Hz** 다
  (LowState 가 게이트라서). 즉 시계만 고쳐서는 ①이 안 낫는다.
- **500 인데 우리가 흘리면** → 흘리는 원인을 고치는 것이 낫다. 유력 후보는 우리
  자신의 rclpy executor 스레드다 — `--goal-source fixed` 에서도 `FallMonitor` 와
  `ModeMonitor` 가 각각 `SingleThreadedExecutor` 를 스레드로 띄운다.
  `base_walk` 는 `rclpy` 를 **import 조차 안 한다.**

**판별 측정**: `--log-timing` 30초 (R6). `pub_hz` 열이 이미 있다.
그리고 **모니터 둘을 끄고 같은 측정을 반복**하면 그 스레드가 원인인지 한 번에 갈린다.

### 방법론

이 91행 CSV 는 **2026-08-05 부터 저장소에 있었다.** `tick_dt_s` 열도 처음부터 있었다.
넉 달치 가설을 세우는 동안 **아무도 그 열을 읽지 않았다.** 새 측정을 요청하기 전에
이미 있는 로그의 열을 전부 훑는 것이 먼저다.

---

## 8-60. ⭐⭐ 발목 roll 은 ROM 문제가 아니라 **부족감쇠 한계사이클**이다 (2026-08-09 실기 실측)

### 실측 1 — 물리 스톱 (사용자가 손으로 딱딱한 벽까지, `tools/read_joint_live.py`)

| | 물리 스톱 | URDF/벤더 ±0.345 |
|---|---|---|
| L_Ankle_Roll | **±0.41** (23.6°) | 1.19배 |
| R_Ankle_Roll | **±0.39** (22.1°) | 1.12배 |

⇒ URDF 가 **12~19 % 빡빡하다.** 자산은 ±0.40 으로 고쳐야 한다. **다만 이건 곁가지다.**

### ⛔ 실측 2 — 보행 로그가 그 물리 스톱을 1.40 배 넘는다

`real_walk.csv` t=17.0~18.0 s(붕괴 국면):

| | 로그 \|최대\| | 물리 스톱 대비 |
|---|---|---|
| L | 0.5779 (33.1°) | **1.40배** |
| R | 0.5375 (30.8°) | **1.39배** |

물리적으로 갈 수 없는 각도다. 그런데 **그 순간 토크가 −1.88 N·m** 인 표본도 있다 --
변형으로 설명할 수 없다.

### ⭐ 정체 — 매 샘플 부호가 뒤집힌다

| | 부호반전 | \|dq\| max |
|---|---|---|
| L | 40샘플 중 **20회 (51 %)** | 22.2 rad/s |
| R | 40샘플 중 **19회 (49 %)** | **26.5 rad/s** |

URDF 속도한계 18, 벤더 17.59 를 **넘는다.** 매끄러운 궤적이 아니라 **한계사이클**이고,
±0.55 는 ROM 이 아니라 **20 rad/s 로 스톱에 처박히는 오버슈트**다. 명령 토크가 아니라
관성이 한 일이라 낮은 토크에서도 넘는다.
⇒ 사용자가 말한 **"다리가 떨면서"** 와 정확히 맞는다.

### 원인 — 발목 감쇠가 벤더의 1/4 이다

벤더 armature 0.056506, kp 50 기준:

| | kd | ζ |
|---|---:|---:|
| **배포 `common.damping`** | **1.0** | **0.297** ⛔ |
| 배포 `prepare.damping` | 5.0 | 1.487 |
| **벤더 유도값** | **4.26** | 1.267 |

고유진동수 **4.73 Hz**. 정책 tick 이 39.6 Hz(§8-59)라 여유가 8배뿐이고, ζ=0.30 이면
그 위에서 링잉이 난다.

### ⭐⭐ 교차확인이 이미 있었다

**같은 관절이 `prepare` 게인(kd 5, ζ 1.49)에서는 조용했다** -- `--hold-diag` 15초
추종오차 **≤1.5°**. **kd 를 1 로 바꾸는 순간(= `r`)** 진동이 시작된다.

### ⛔ sim 은 이 실패를 원리적으로 못 보여준다

학습 `asset.armature = 0.0` → 발목에 **관성이 없다** → 오버슈트할 질량이 없다.
kd=1 의 부족감쇠는 **관성이 있어야만** 드러난다. 40+ arm 을 돌려도 이 모드는 안 나온다.
§8-52("armature 가 안정성의 1차 변수")와 같은 뿌리다.

### 다음

기준선(`--hold-diag` tilt + `--log-timing` tick_dt)을 먼저 잡고, 그 다음
**발목 kd 만 1 → 4.26** A/B. 진동이 사라지면 원인 확정이다.
⚠️ 다만 그것은 정책이 학습한 임피던스가 아니다(sim 은 kd 1 + armature 0). 진단으로는
결정적이지만 **최종 해법은 armature 를 넣고 게인과 짝지어 재학습**하는 쪽이다.
