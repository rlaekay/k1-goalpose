# 모드 전환 파이프라인 3중 독립 감사 (2026-08-08)

> ## ⛔⛔ 2026-08-09 정정 — **미수정 3건은 전부 닫혔다**
>
> 이 문서가 "⛔ 미수정" 이라고 적은 세 건(`b`~`r` 낙상 / 발행 스레드 사망 / 관절배치
> 가드)은 **커밋 `54e0885`(2026-08-08 03:02, 이 감사 6분 뒤)에서 전부 수정됐다.**
> 3중 감사 수렴에서 코드로 직접 확인했다:
> - `deploy_goal_pose.py:1614-1615` — `r` 프롬프트 루프가 `in_recovery()` → `_step_recovery()` 를 부른다
> - `:1831-1832` — 발행 스레드가 `BaseException` 을 잡아 `_publish_error` 에 싣고,
>   `:899` 의 `_require_fresh_low_state` 가 그것을 본다
> - `:725` / `:894-896` — `_layout_error` 로 DDS 콜백의 raise 를 메인까지 전달한다
>
> ⛔ **그런데 이 문서와 `CLAUDE.md` 가 22시간째 "미수정" 이라고 말했고, 그 때문에
> 배포 세션이 존재하지 않는 결함을 이유로 로봇 시험을 미뤘다.** 최우선 목표가
> "4발자국"인데 그것을 막은 것이 **낡은 문서**다. 상세: `AUDIT_PROCESS_20260809.md`.


사용자 지시: *"mode change 관련해서 전체 파이프라인을 investigate 해. 논리적으로
빈틈이 있으면 안돼. 모든 경우의 수를 따져봐야 해"* + *"넌 내게 신뢰도가 바닥이기
때문에 **전체 프로세스를 독립적으로 직렬로 3번 돌려서 나오는 결론이 모두 일치할
때만** 내게 최종 보고를 해"*

`deploy_goal_pose.py`(1,905행) + `utils/` + config + `run_e0.sh` + 테스트를
**세 번 독립적으로** 감사했다. 각 회차는 앞 회차의 결과를 **받지 않았고**, 방법도
달랐다(전문 정독 / AST 정적 전수 / 실제 메서드를 스텁 SDK 로 구동).

⛔ **이 문서는 3/3 합의만 결론으로 적는다.** 2/3 이하는 "갈렸다"로 적고 결론에 안 쓴다.

---

## 1. 3/3 만장일치 — 결론으로 쓸 수 있는 것

| # | 항목 | 1회 | 2회 | 3회 | 상태 |
|---|---|---|---|---|---|
| 1 | 복구 재진입 뒤 정책이 **prepare 게인**으로 돈다 | ⛔1 | ⛔-1 | ⛔1 | ✅ **수정됨** (`d2a17b2`) |
| 2 | 재진입 첫 틱 **자세 계단** (램프 없음) | ⛔2 | ⛔-1 | ⚠️1 | ✅ **수정됨** |
| 3 | `b`~`r` 대기 중 낙상하면 복구가 **영원히 안 돈다** | ⛔3 | ⛔-2 | ⛔2 | ✅ **수정됨** (`54e0885`) |
| 4 | 발행 스레드가 예외로 죽어도 **아무도 모른다** | ⛔7 | ⛔-4 | ⚠️5 | ✅ **수정됨** (`54e0885`) |
| 5 | `--parallel-torque` 가 복구 구간에서 kp=0/tau 동결 | ⛔6 | ⚠️-1 | ⚠️2 | ✅ **수정됨** |
| 6 | `_verify_joint_layout` 가드가 스스로 무력화된다 | ⛔8 | ⚠️-6 | ⛔5 | ✅ **수정됨** (`54e0885`) |
| 7 | `policy.reset()` 이 **존재하지 않는 메서드**였다 | ⚠️1 | ⚠️-9 | ⚠️9 | ✅ **수정됨** |
| 8 | `cleanup()` 이 join 실패를 무시하고 채널을 닫는다 | ⚠️6 | ⚠️-5 | ⚠️6 | ⚠️ 미수정 |
| 9 | `--hold-prepare` 등 **죽은 스위치**(no-op) | ⚠️9 | ⚠️-8 | ⚠️9 | ⚠️ 미수정 |

### 1-①·②·⑤·⑦ (수정 완료, 커밋 `d2a17b2`)

원인은 하나였다. **`start_rl_gait_conditionally` 의 "prepare→RL 램프 + 게인 교체"
블록이 그 함수 안에 인라인이었고, 그 함수의 호출자가 `__main__` 하나였다.**
복구 재진입(`_step_recovery` 의 `reenter`)은 `_enter_custom_latched` 만 부르고
그 함수는 prepare 게인을 싣는다. 그래서 **첫 낙상 이후 영구히**:

- 다리 kp 가 실기 config 기준 hip/knee **350**(학습 100), ankle **250**(학습 50) = **5배**
- 자세 램프가 없어 첫 추론이 계단을 만든다
- `_policy_gains_active` 가 True 로 돌아올 길이 없어 `--parallel-torque` 가 자기무력화

수정: `_start_policy_control(reason)` 으로 추출해 **진입/재진입 양쪽**이 부른다.
`GoalPosePolicy.reset()` 을 실제로 만들었다(그 전엔 `hasattr` 가드 때문에 조용한 no-op).

**⚠️ 계단의 크기는 3회가 갈렸다: 0.57 / 0.50 / 0.2 rad.** 존재는 합의, 크기는 미확정.

### 1-③ `b`~`r` 대기 중 낙상 — ✅ **수정됨** (`54e0885`: `:1614-1615`)

`_step_recovery` 는 `run()` 루프에서만 불리고, `run()` 은
`start_rl_gait_conditionally` 가 **반환한 뒤** 시작된다. 그런데 낙상 트리거는
`_custom_mode_started=True`(ChangeMode 이전)부터 무장돼 있다.
⇒ `r` 프롬프트(무한 대기)에서 넘어지면 "FALL DETECTED" 만 찍히고, DAMPING 없이
쓰러진 로봇을 prepare 게인으로 무기한 붙잡는다. 실측 `r` 대기가 **124 s** 였던
구간이 있다. 그 상태에서 `r` 을 누르면 **넘어진 채로 RL 램프가 실행된다.**

### 1-④ 발행 스레드 사망 — ✅ **수정됨** (`54e0885`: `:1831-1832` + `:899` 검사)

`_publish_cmd` 에 try/except 가 없다. 3회 중 2회가 프로브로 확인: `_send_cmd`
예외 **1회**로 스레드 즉사, `running` 은 True, 감지 경로 0 ⇒ LowCmd 가 한 프레임도
안 나가는데 정책은 계속 추론한다.

⚠️ **이것이 위 ①의 수정과 상호작용한다**: `_start_policy_control` 의 자세 램프는
`dof_target` 만 쓰고 **발행은 스레드가 한다.** 스레드가 죽어 있으면 램프가 조용히
무효가 된다. (테스트에서 실제로 재현했다.)

### 1-⑥ `_verify_joint_layout` — ✅ **수정됨** (`54e0885`: `_layout_error` 로 메인 전달)

`_joint_layout_checked = True` 가 `raise` **보다 먼저** 세팅되고, `raise` 는
**DDS 콜백 스레드**에서 난다. ⇒ 메인은 모른 채로 진행하고, 두 번째 콜백은 검사를
건너뛴다. docstring 이 경고하는 바로 그 실패(관절 배치가 어긋난 채 CUSTOM 진입).

---

## 2. 3회가 갈린 것 — 결론으로 쓰지 않는다

| 항목 | 1회 | 2회 | 3회 |
|---|---|---|---|
| SIGTERM 핸들러 없음 | — | ⛔-5 | ⛔3 |
| 복구 중 abort 파일 미검사 | ⛔4 | ⛔-3 | — |
| `ChangeMode(kDamping)` 실패 처리 | ⛔5 | — | ⛔4 |
| 연속 복구 횟수 상한 없음 | ⚠️4 | — | ⚠️4 |

SIGTERM 만 예외적으로 고쳤다(`d2a17b2`) — **"핸들러가 등록돼 있지 않다"는 코드로
1초에 확인되는 사실이고 반박 여지가 없기 때문**이다. 나머지 셋은 미수정으로 둔다.

---

## 3. 커밋 `8d763fb` 판정 — 갈렸고, **코드로** 갈랐다

**핵심 주장은 3/3 참**: 락 커버리지 완전(락 밖 `low_cmd` 접근 0건) / 데드락 경로 0 /
`streaming` 분기 구조적으로 옳음 / `init_Cmd_T1` 생략 안전(`cmd_type`·`weight` 를
그 함수 밖에서 아무도 안 쓴다, 전수 grep).
음성 대조도 기계 확인: `8d763fb^` 에서 락 안에 있던 `low_cmd` 접근 **0개**.

**"새 결함을 만들었나" 는 2 대 1 로 갈렸다** (1회 예 / 2회 아니오 / 3회 예).
표결로 정하지 않고 `git show 8d763fb^` 로 갈랐다:

```
이전:  if self._parallel_torque:                              # 게이트 없음
현재:  if self._parallel_torque and self._policy_gains_active:
```
`_policy_gains_active = True` 를 세우는 곳은 한 군데뿐이고 그 함수는 `__main__` 에서
1회만 불렸다. ⇒ **첫 낙상 이후 `--parallel-torque` 영구 정지. 회귀 맞다.**
(이전 판에는 게이트가 없어 복구 뒤에도 계속 작동했다.) `d2a17b2` 에서 수정됐다.

---

## 4. 테스트 공백 — 3/3 지적

`deploy/tests/test_reentry_race.py` 는 복구 상태기계를 **한 번도 돌지 않는다**
(`_step_recovery`/`run()`/`start_rl_gait_conditionally`/`cleanup()` 미호출).
⇒ 위 ①~⑤를 **원리적으로** 잡을 수 없었다. 추가로 `PREP_KP` 를 균일 250 으로 둬서
**게인 인덱스 밀림이 통과한다.**

`deploy/tests/test_recovery_reentry.py` (신규, 14검사) 가 그 구멍을 덮는다:
`_step_recovery` 를 진짜로 돌고, 게인을 비균일로 만들고, 발행 스레드를 진짜로 띄운다.
**음성 대조 11/14 실패**로 검출력을 확인했다.

```bash
cd htwk-gym/deploy && python tests/test_recovery_reentry.py && python tests/test_reentry_race.py
```

---

## 5. 미확인 — 로봇 접속이 필요하다

3회 모두 같은 것을 못 닫았다. **전부 로봇에서만 닫힌다.**

1. SDK 가 CUSTOM 밖에서 LowCmd 를 무시하는가 (③·④의 위험도가 여기 달림)
2. LowCmd 중단 워치독이 있는가 (발행 정지 시 모터가 마지막 명령을 유지하는가)
3. pybind11 콜백 밖 예외가 삼켜지는가 `std::terminate` 하는가 (⑥의 심각도)
4. `ChangeMode` 실제 실패율 / kWalking→kCustom 합법성
5. 실기 LowState 실제 레이트

---

## 6. 배포 세션에 넘기는 것

**최우선 목표가 "4발자국 이상 걷기"라는 것을 전제로 정렬한다.**

⛔⛔ **2026-08-09 정정: 아래 1~3 은 전부 `54e0885` 로 닫혔다. 막을 것이 없다.**
이 절이 22시간째 "먼저 이것부터 고쳐라"라고 말해서 로봇 시험이 미뤄졌다.

1. ~~1-③ (`b`~`r` 낙상)~~ → ✅ 닫힘
2. ~~1-④ (발행 스레드 사망)~~ → ✅ 닫힘
3. ~~1-⑥ (관절 배치 가드)~~ → ✅ 닫힘
4. ⚠️ 남은 것은 §1 표의 8·9(cleanup join / 죽은 스위치)뿐이고 **둘 다 로봇 시험을
   막지 않는다.**

⇒ **지금 바로 로봇에 올려도 되는 상태다.** 비용 순 세 가지(수렴 보고서 §6):
   ① `md5sum` 2분(로봇 위 모델의 정체 = R3, 나흘 미해결)
   ② 발목 roll 을 **전원 없이** 손으로 스톱까지 밀어 인코더 읽기 2분
   ③ `--log-timing` 30초 — `tick_dt_s` med > 23 ms 면 **보행시계 시간팽창 확증**

⭐ **이미 고친 것(`d2a17b2`)이 4발자국에 직접 걸린다**: 수정 전에는 **한 번 넘어진
뒤의 모든 시도가 5배 게인 위에서 돌았다.** "첫 시도는 좀 걷더니 그 뒤로 더 나빠진다"
가 관측됐다면 그것이 원인일 수 있다 — 정책 문제가 아니라 게인 문제다.
