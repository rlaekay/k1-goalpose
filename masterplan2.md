# K1 Goal-Pose — Masterplan 2 (v7 통합 아키텍처)

> 작성 2026-07-26. [MASTERPLAN.md](MASTERPLAN.md)는 v0~v6의 실기(實記)로 그대로 둔다.
> 이 문서는 armA/B/C/D + v3 평가 결과로 **버전을 통폐합한 뒤의 단일 아키텍처**를 정의한다.
> 항목별 사용자 피드백 답변은 [MASTERPLAN_feedback.md](MASTERPLAN_feedback.md)에 있다.

> ## ⚠️ 2026-07-27 갱신 — 이 문서의 §3-2 / §4-1 / §6 일부는 실측으로 정정됐다
>
> v7 4개 arm 결과는 [masterplan3.md](masterplan3.md)에 있다. 특히:
> - **평가가 arm별 config가 아니라 베이스 config로 돌았다** — 이 문서의 v7 게이트 예상치는 무효
> - **path 학습이 waypoint 정확도를 파괴한다**(6.3 → 37.9 cm). §3-2의 "waypoint 50% 유지로
>   armB의 정지 능력을 보존한다"는 **틀렸고**, dwell 메커니즘 + path 비중 0.35로 수정됐다
> - **속도 목표는 달성됐다**(구간최고 median 1.20 m/s)

---

## 1. 이번 평가가 확정한 것 두 가지

### 1-1. armB 압승, 원인은 레버 하나 (`goal_reached`)

5개 버전 × 256 env × 120 s, 결정론적 정책, 외란 OFF, 관측노이즈 ON, seed 0.
아래 수치는 각 `segments.csv` 원자료(총 22,506 구간)를 재집계한 값이다.

| | **armB** | armA | armC | armD | v3 | 게이트 |
|---|---|---|---|---|---|---|
| 위치오차 median | **3.9 cm** ✅ | 7.6 ❌ | 8.3 ❌ | 24.3 ❌ | 13.6 ❌ | ≤5 cm |
| 위치오차 p90 | **6.7 cm** ✅ | 14.7 ❌ | 16.7 ❌ | 49.5 ❌ | 30.1 ❌ | ≤10 cm |
| heading median | 7.3° ✅ | 2.1 ✅ | 1.8 ✅ | 2.8 ✅ | 3.3 ✅ | ≤10° |
| 엄격 성공률 | **52.8%** | 13.9 | 13.0 | 6.2 | 17.5 | — |
| 도착 후 안 멈춤 | **0.5%** | 33.4 | 28.5 | 6.2 | 2.7 | — |
| 한 번도 도달 못함 | **0.2%** | 19.3 | 25.3 | 60.0 | 49.9 | — |
| 낙상 | 37 ❌ | 34 ❌ | 10 ❌ | 21 ❌ | **3** ❌ | 0 |

armA와 armB의 config 차이는 **`rewards.scales.goal_reached: 0 → 1.0` 단 한 줄**이다.
따라서 위 표의 A↔B 격차는 전부 이 항에 귀속된다.

```python
stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1   # 몸통 선속도
return ((self.goal_dist < 0.1) & stopped).float()              # +1 / step
```

**작동 원리**: 4~8 s 구간을 50 Hz로 채우면 최대 200~400점이 걸린 큰 항이다. 조건을 만족하는
순간 **평평한 고원**이 생겨 "도착해서 멈춰 있는 상태"가 **흡수 상태(absorbing state)** 가
된다. `constellation` 단독은 `exp(-w·d_con)`이라 목표 근처에서 기울기가 0에 수렴하고,
"여기서 멈춰라"는 신호가 없다. 그래서 armA/C는 도착속도 0.1 m/s 부근에서 맴돈다(47~49%).

**전 카테고리 우위** (위치오차 median, m):

| | stand | turn | straight | lateral | combined |
|---|---|---|---|---|---|
| armA | 0.073 | 0.075 | 0.076 | 0.074 | 0.082 |
| **armB** | **0.033** | **0.038** | **0.039** | **0.040** | **0.040** |

부분 우위가 아니라 전면 우위 → **armB를 v7의 backbone으로 채택.**

### 1-2. 이 과제는 속도를 요구한 적이 없다

| arm | 접근속도 median | p95 | **요구속도 median** | **요구속도 최댓값** |
|---|---|---|---|---|
| armA | 0.106 | 0.341 | 0.119 | **0.574** |
| armB | 0.109 | 0.351 | 0.116 | **0.574** |
| armC | 0.114 | 0.350 | 0.132 | 0.569 |
| armD | 0.073 | 0.276 | 0.116 | 0.606 |
| v3 | 0.043 | 0.256 | 0.063 | 0.589 |

요구속도 = `start_dist / segment_duration`. **4647개 구간 전체에서 최댓값이 0.574 m/s다.**

구조적 원인:
- `goal_dx ∈ [-2,2]`, `goal_dy ∈ [-1.5,1.5]` → 최대 거리 2.5 m
- `resampling_time_s ∈ [4,8]` → 최소 4 s
- ⇒ 이론적 상한 2.5/4 = **0.625 m/s** (실측 0.574와 일치)
- 게다가 `_resample_goals`가 **stand는 dx=dy=dθ=0, turn은 dx=dy=0**으로 만든다
  ([goal_pose.py:428-430](htwk-gym/envs/K1/goal_pose.py#L428))
  → **전체 구간의 29.5%가 이동거리 정확히 0** (실측: 1372/4647)

**결론: 목표 1.3~1.5 m/s는 이 분포에서 학습도 측정도 불가능했다.** 정책이 0.35 m/s에
머문 것은 정책의 한계가 아니라 **과제의 한계**다. 이것이 v7의 `path` goal mode를 만든 이유다.

---

## 2. 버전 통폐합 결정

| 버전 | 판정 | 근거 | v7으로 |
|---|---|---|---|
| **armA** | ❌ 폐기 | armB에 전 카테고리 열세 | — |
| **armB** | ✅ **채택 (backbone)** | 전 지표 1위 | 보상 세트 전체 |
| **armC** | ❌ 삭제 | 주 게이트 전패, 43% 더 학습하고도 짐. 낙상 우위는 iteration과 교란되고, v3가 500 Hz에서 낙상 3회를 내어 대안 존재 | — |
| **armD** | ❌ 해체 | 12레버 동시 변경, 24.3 cm | 지각노이즈·`goal_reached`·`stand_posture`만 흡수 |
| **v3** | ⚠️ **흡수** | armD 기반 위에서 24.3→13.6 cm, 낙상 21→3. 스택은 유효하나 좋은 기반에서 미검증 | 대칭손실·미니배치PPO 채택 / 거리커리큘럼 폐기 |

### 2-1. armD 해체의 근거 — 범인은 지각 노이즈가 아니라 PD 게인

위치오차 median (m):

| 카테고리 | armA | **armD** | 이동 필요? |
|---|---|---|---|
| **stand** | 7.3 cm | **5.0 cm** ← D가 더 좋음 | ❌ |
| turn | 7.5 | 20.0 | ❌ |
| straight | 7.6 | **29.8** | ✅ |
| lateral | 7.4 | **25.2** | ✅ |
| combined | 8.2 | **31.7** | ✅ |

지각 노이즈가 원인이라면 **stand도 같이 나빠져야 한다**(stand 목표도 같은 노이즈를 받는다).
그런데 stand는 오히려 좋아졌고 **이동이 필요한 모든 카테고리만 붕괴**했다.
⇒ 보행 자체가 깨졌다 ⇒ **`control.stiffness` 100→200, `damping` 2→5 (Hip/Knee 2배)가 주범.**
warm start 정책은 100/2에서 학습됐으므로 같은 액션이 2배 토크를 내면 액션↔결과 관계가 무효가
된다. 서 있기는 토크 여유가 커져 오히려 개선되는 것까지 관측과 일치한다.
**공범**: `goal_dx/dy` 3 m/2 m 확대 → 0.5 m 이상에서 미도달 87~97%.

### 2-2. v3 흡수의 근거 — v3는 armA가 아니라 armD 위에 쌓였다

`Goal_Pose_V3.yaml`은 armD의 12레버를 **전부 상속**한다(PD 200/5, goal 3m/2m, 4~10 s,
지각노이즈, armature 0.02). 따라서 v3의 성적은 armD와 비교해야 한다:

| | armD (v3의 기반) | **v3** | 개선 |
|---|---|---|---|
| 위치 median | 24.3 cm | 13.6 cm | **−44%** |
| 엄격 성공률 | 6.2% | 17.5% | **+2.8배** |
| **낙상** | 21 | **3** | **−86%** |
| stand | 5.0 cm | **2.4 cm** | 전 버전 최고 |

v3의 스택(커리큘럼+대칭+미니배치PPO)은 **망가진 기반을 절반 가까이 복구했다.**
낙상 3회는 전 버전 최저이며, armC가 물리 주파수를 희생해 얻은 10회보다 좋다.

**다만 `goal_curriculum`은 폐기한다.** 성공률이 낮으면 **목표 거리를 줄이는** 구조
(`init_level: 0.35`)라서 v3의 요구속도 median이 0.063 m/s로 전 버전 최저가 됐다.
**속도를 올리려는 목표와 방향이 정반대다.**

### 2-3. 발견: 대칭 손실은 한 번도 켜진 적이 없다

`Goal_Pose.yaml`의 `algorithm.symmetric_coef: 10.`은 **읽히지 않는 죽은 키**다
([runner_v3.py:13](htwk-gym/utils/runner_v3.py#L13)). 실제 키는 `symmetry_coef`이며
**RunnerV3에서만** 동작한다. armA~D는 전부 `train.py`(기본 Runner)로 돌았으므로
**대칭 손실 = 0**이었다. armA의 "왼발이 항상 살짝 앞" 관찰이 여기서 설명된다 —
warm start에서 물려받은 대칭이 학습 중 서서히 무너진 것이고, 이를 막는 항이 없었다.

⇒ v7은 `train_v7.py`(RunnerV3) + `symmetry_coef: 0.5`로 **처음으로 실제 활성화한다.**

---

## 3. v7 아키텍처

파일: [goal_pose_v7.py](htwk-gym/envs/K1/goal_pose_v7.py) ·
[Goal_Pose_V7.yaml](htwk-gym/envs/K1/Goal_Pose_V7.yaml) ·
[train_v7.py](htwk-gym/train_v7.py) · 커밋 `850a97b`

상속: `GoalPoseV7 → GoalPoseV3 → GoalPose`. 기존 코드는 **동결**, 전부 override로만 추가.
모든 신규 기능은 스위치이며, `path: 0.0` + `disturbance.enabled: false` + 신규 스케일 0이면
**v7은 armB로 정확히 환원된다.**

### 3-1. 관측 — 변경 없음 (54차원, warm start 보존)

| 구간 | 차원 | 실기 출처 |
|---|---|---|
| projected_gravity | 3 | IMU (AHRS) |
| base_ang_vel | 3 | 자이로 |
| **commands** | **10** | **앞 3개만 BT가 공급**, 나머지 7개는 상수 |
| gait clock cos/sin | 2 | 내부 카운터 |
<!--이게 뭐야? gait는 다른 뜻 아닌가. 한발짜국 시간? sincos는 뭔디-->
> **[답변]** **gait = 보행 주기, clock = 그 주기 안에서 지금 어디쯤인지 알려주는 시계다.**
> 
> `gait_frequency`가 1.8~2.4 Hz니까 **한 주기가 0.42~0.56초**다. 이게 "한 발짝"이 아니라
> **좌우 한 걸음씩, 왕복 한 사이클**이다. `gait_process`는 이 주기를 0→1로 훑는 위상값이고
> 매 스텝 `fmod(gait_process + dt*gait_frequency, 1)`로 증가한다.
> 
> **왜 sin/cos 두 개인가**: 위상을 0~1 스칼라 하나로 주면 **0.99와 0.01이 실제로는 이웃인데
> 숫자로는 정반대**가 된다. 신경망이 이 불연속을 학습으로 메워야 해서 낭비다.
> `(cos 2πp, sin 2πp)`로 주면 위상이 **단위원 위의 점**이 되어 0.99와 0.01이 물리적으로
> 바로 옆이 된다. 각도를 다룰 때의 표준 인코딩이고, 아래 §6-8의 heading sin/cos 얘기와
> 정확히 같은 원리다.
> 
> **용도**: `feet_swing` 보상이 이 시계를 보고 "지금은 왼발이 떠 있어야 할 위상"을 판정한다.
> 즉 정책에게 **박자를 세어주는 메트로놈**이고, 이것 때문에 걸음이 제멋대로 흩어지지 않는다.
> `stand` 카테고리에서 `gait_frequency = 0`으로 두는 이유도 이것이다 — 메트로놈을 끄면
> 발을 뗄 이유가 사라져서 가만히 서 있는 게 최적이 된다.

| dof_pos / dof_vel / 직전 actions | 12×3 | 엔코더 / 내부 |

**BT ↔ 정책 인터페이스 = (Δx, Δy, Δθ) 3개.** Δx/Δy는 **로봇 로컬 프레임** [m],
Δθ는 ±π 래핑 [rad]. 매 제어 주기마다 현재 자세로 재투영해야 한다.

### 3-2. 목표 생성 — waypoint 50% / path 50%
<!--waypoint모드는 그냥 2-3m떨어진 goal까지 가는 훈련이고 pathmode는 waypoint mode를 path위에서 굴린다는 뜻이야? 나는 continuously fed waypoint를 의미한건데 현재 pathmode가 그걸 잘 이행하는지 확인해. 별개로, 논문과 github를 뒤져서 비슷한 목표를 가졌던 사람들은 path혹은 별개의 방법으로도 어떻게 했는지 조사해. 그리고 네 현재 코드를 피드백 하고, 그걸 critic한 버전으로 수정해-->
> **[답변]** **이해가 정확하다. 그리고 "잘 이행하는지 확인해"의 답은 — 이행하지 못하고 있었다.**
> 
> **(1) 코드 감사 결과: `lookahead_m`이 완전한 데드코드였다.**
> `_reroll_paths`가 `self.lookahead`를 [0.5, 3.0]에서 뽑아 저장하는데, **그 변수를 읽는
> 코드가 어디에도 없었다.** 실제 동작은 "목표를 로봇 위치에 놓고 `path_speed`로 도망가게
> 한다"였다. 즉 **lookahead(앞서가는 점)가 아니라 pacer(페이스메이커)**였다.
> 
> 스모크 테스트 실측이 이걸 증명한다: **gap median 0.41 m** — 설정한 최소 lookahead
> 0.5 m보다도 작다. 목표가 로봇 위에 거의 올라타 있었다.
> 
> **이게 왜 심각한가**: constellation은 `exp(-w·d²)`라 **d≈0에서 기울기도 0**이다.
> 목표가 로봇 위에 있으면 보상이 최대치에 붙어서 **"움직일 이유"가 사라진다.**
> 속도를 만들려고 만든 장치가 정작 가장 중요한 지점에서 gradient를 죽이고 있었다.
> 
> **(2) 선행연구 조사**
> - **Rudin et al., IROS 2022** (arXiv:2209.12827, 우리 코드에 `final_window_s`로 이미
>   들어와 있는 그 논문): *"Instead of continuously tracking a precomputed path, the robot
>   needs to reach a target position within a provided time"* — 이들은 **경로 추종을 일부러
>   버리고** 시간제한 목표로 갔다. 이유: 경로를 강제하면 정책이 경로/보행법을 스스로
>   고를 자유를 잃는다.
> - **wheeled-legged navigation (ETH)**: *"local target point is a look-ahead point set
>   1.0 m ahead of the current position along the path"* + 횡방향 편차·각도 편차 페널티.
>   이게 **고전적 pure pursuit**이고, 사용자가 말한 continuously fed waypoint에 가장 가깝다.
> - **핵심 통찰**: pure pursuit만으로는 **속도 요구가 안 생긴다.** 목표가 "내 투영점에서
>   L만큼 앞"이면 내가 기어가면 목표도 기어간다. 그래서 Rudin은 시간제한을, 우리는
>   pacer를 쓴 것이다. **둘 다 필요하다.**
> 
> **(3) 수정: pacer + pure-pursuit floor + leash 3중 제약**
> ```
> u_goal = clamp( u_prev + path_speed·dt/|ds/du|,           # pace  : 속도 요구
>                 u_proj + lookahead_min/|ds/du|,           # floor : 항상 앞서 있음
>                 u_proj + lookahead_max/|ds/du| )          # leash : gradient 보존
> ```
> `u_proj`는 **로봇을 경로에 투영한 위상**이다(지역 탐색 9점 — figure-8 같은 자기교차
> 곡선에서 전역 최근접점을 쓰면 반대쪽 고리로 튄다).
> 
> 수치 검증(반지름 2 m 원, floor 0.8 / leash 1.28):
> | 로봇 | gap 최소 | gap 최대 | 결과 |
> |---|---|---|---|
> | pace보다 느림(0.3 m/s) | 0.80 | 1.28 | leash에 주차 → gradient 살아있음 |
> | pace보다 빠름(1.5 m/s) | 0.80 | 0.80 | floor에 고정 → **carrot을 끌고 감** |
> 
> **부수 효과가 중요하다**: 빠른 로봇은 carrot을 `path_speed`보다 **빠르게** 끌고 간다.
> 즉 `path_speed`가 상한이 아니라 **하한(페이스 바닥)**이 되고, 실제 달성 속도 분포가
> **명령이 아니라 물리적 한계**를 드러낸다. §6-2의 MaxSpeed 문제가 여기서 같이 풀린다.

**waypoint 모드** = 기존 과제 그대로. armB가 잘하는 "도착 후 정지"를 잃지 않기 위해 유지.

**path 모드 (신규)** — 속도 요구를 만드는 장치:
- 경로 5종: figure-8 · circle · spiral · rose/star(5엽) · pseudo-random wander
- 경로 크기 1.5~4.0 m, lookahead **0.5~3.0 m**
- lookahead point가 `path_speed`로 경로를 따라 **전진한다**
- goal heading = **경로 접선**
- **목줄(leash)**: 간격이 `lookahead_max_m`(3.5 m)를 넘으면 목표가 **멈춰서 기다린다**

> **왜 목줄이 필요한가**: 못 따라가는 로봇에게 무한히 도망가는 목표를 주면 `exp(-w·d²)`가
> 평평해져 gradient가 죽는다. 목줄이 있으면 뒤처짐은 단순히 "목표가 정지"로 귀결되어
> waypoint 모드와 동일한 잘 정의된 문제가 된다.

> **정지 목표와 전진 목표의 결정적 차이**: 정지 목표는 **아무 속도로나** 도달 가능하다.
> 전진하는 목표는 **그 속도를 내야만** 간격이 유지된다. 목표까지의 거리가 같아도 속도 요구가
> 완전히 다르다. **거리가 아니라 목표의 속도가 정책의 속도를 결정한다.**

### 3-3. 속도 커리큘럼 (v3 거리 커리큘럼의 대체)

```
keep-up 판정 = (lookahead 간격 < keepup_gap_m 2.0 m)
EMA > up_threshold 0.8   → speed_level += 0.05
EMA < down_threshold 0.5 → speed_level -= 0.05
path_speed ~ U(0.3, 0.3 + 1.3·speed_level)     상단 최대 1.6 m/s
```

<!--근데 기존의 goalpose를 속도 커리큘럼 없이 최고/최적 속도를 내도록 유도할 수 있나? curriculum이 자연스러운 linear/angular position도 컨트롤하지 못했는데 속도라고 잘 할까? 논문과 사례들을 뒤져서 이 문제를 잘 해결한 사례를 가져오고, 편법 말고 legit하게 해결한 사례를 꼭 찾아와서 적용해. 없으면 스스로 고민해보고 댓글 달아. -->
> **[답변]** **찾았고, 그 결과가 제 직전 제안(E3 무커리큘럼)을 반증한다. 정직하게 보고한다.**
> 
> **Margolis et al., "Rapid Locomotion via Reinforcement Learning" (RSS 2022 / IJRR 2024)**
> — MIT Mini Cheetah로 **지속속도 3.9 m/s**(평균 3.8). 이 분야에서 최고속도를 정면으로
> 다룬 대표 연구이고, 결론이 우리 질문에 정확히 답한다.
> 
> **(a) "커리큘럼 없이 되나?" → 안 된다. 논문의 ablation이 명시적이다.**
> > *"In the absence of any curriculum, no useful locomotion behavior is learned,
> > **even at low speeds**."* — 로봇이 **제자리에서 떨기만(jittering in place)** 한다.
> 
> 이유: 넓은 범위에서 균일 샘플링하면 대부분의 명령이 고속인데, 고속 보행은 어려워서
> **보상을 못 모은다.** 학습 신호 자체가 없다.
> → **제가 직전에 제안한 E3(무커리큘럼 + 넓은 고정분포)는 이 실패 모드에 정확히 해당한다.**
> E3는 armB에서 warm start한다는 점(논문은 from scratch)과 leash가 있다는 점이 다르지만,
> **"안전한 단순화"가 아니라 실패 가능성이 실증된 도박**이다. E3 설명을 그렇게 고쳤다.
> 
> **(b) "위치도 못 잡았는데 속도라고 잘할까?" → 스칼라 커리큘럼이면 못 한다. 이게 핵심이다.**
> 논문이 Box(사각형 범위 확장) 대신 **Grid**를 쓴 이유:
> > *"simultaneous high linear and angular velocities are more demanding due to
> > centrifugal forces"* — 실현가능 경계가 대략 **ω ~ 1/v** 쌍곡선이다.
> 
> **직선 속도와 회전 속도는 독립이 아니다.** 스칼라 `speed_level` 하나로는 이 경계를
> 표현할 수 없고, 그래서 "속도는 올렸는데 회전이 섞이면 무너지는" 상태에 갇힌다.
> 지적하신 "위치도 못 잡았는데"가 바로 이 증상이다.
> 
> **(c) 적용: (속도 × 곡률) 2D grid-adaptive 커리큘럼으로 교체**
> 경로 위에서는 요각속도가 자유롭지 않다 — **ω = v·κ**. 따라서 **(path_speed, curvature)가
> 곧 논문의 (v_x, ω_z) 그리드**다. 곡률은 경로 크기로 명령한다(κ ≈ 1/path_scale).
> ```
> speeds:     [0.3, 0.6, 0.9, 1.2, 1.5, 1.8]     m/s
> curvatures: [0.25, 0.4, 0.6, 0.8, 1.0]         1/m  (반경 4.0 .. 1.0 m)
> 시드 = (0.3, 0.25) 한 칸만 활성 → 추종 성공 시 4-이웃 칸 활성화 → 활성 칸에서 균일 샘플
> ```
> 이게 논문의 grid-expansion을 그대로 옮긴 것이고, **동시에 아래 커플링 요구를 만족한다.**
> 
> **정직한 한계**: 논문은 사족보행 + 직접 속도명령이고 우리는 이족 + 목표추종이다.
> 경계 모양이 같으리란 보장은 없다. 다만 **"스칼라로는 경계를 표현할 수 없다"**는 논거는
> 로봇 종류와 무관한 기하학적 사실이다.


수렴한 `speed_level`이 곧 **"이 정책이 지속 가능한 최고속도"의 학습된 추정치**다.
방향이 v3와 정반대: v3는 못하면 목표를 가깝게 해서 속도 요구를 **낮췄고**,
v7은 잘하면 속도를 **올린다**.
<!--속도의 커플링을 의도적으로 줄 수는 없어도 커플링이 잘 나타나는 path를 만들거나 해서 병진 회전운동이 커플링되어있어도 정확하게 따라가는 dist도 training해야한다고 강력하게 생각함.-->
> **[답변]** **이 직관이 위 논문의 핵심 발견과 정확히 일치한다. 강력하게 동의하고 반영했다.**
> 
> Margolis가 Box를 버리고 Grid로 간 이유가 문자 그대로 이것이다 — 병진과 회전이 커플링돼
> 있어서 각각의 상한을 따로 정하면 실현가능 영역을 잘못 그린다.
> 
> **반영 1 — 커플링이 곧 커리큘럼 축이 됐다.** 위 (c)의 (속도 × 곡률) 그리드에서
> `ω = v·κ`이므로, 곡률 축을 올리는 것이 곧 **"같은 속도에서 더 강한 커플링"**을 요구하는
> 것이다. 커플링을 우연에 맡기지 않고 **명시적으로 커리큘럼의 한 축으로 승격**했다.
> 
> **반영 2 — serpentine 경로 추가 (shape 5).** `x = 0.45u, y = sin u`.
> 병진은 단조증가하는데 **곡률 부호가 반주기마다 뒤집힌다.** 병진 속도를 유지한 채
> 요각속도를 좌우로 반전시켜야 하므로, 두 운동이 가장 심하게 간섭하는 정석적 시험 경로다.
> (기존 figure-8과 wander도 부호 반전이 있지만 곡률 크기가 들쭉날쭉해서 통제가 안 된다.)
> 
> **반영 3 — 추종오차 정의를 고쳤다.** path 모드에서 목표는 **항상 lookahead_min만큼 앞에
> 있어야 하므로** 목표거리는 원리적으로 0이 될 수 없다. 그래서 거리를 추종오차로 쓰면
> 안 된다. 새로 `path_lag = max(gap − lookahead_min, 0)`를 정의해 **"carrot보다 얼마나
> 뒤처졌는가"**만 측정하고, 커리큘럼 승급 판정도 이 값으로 한다. 지적하신
> "커플링돼 있어도 정확하게 따라가는 dist"가 이 `path_lag`다.


### 3-4. 보상

**armB 세트 유지** (`constellation: 3.5` + `goal_reached: 1.0`), 여기에:

| 항 | 값 | 근거 |
|---|---|---|
| `stand_posture` | **−1.0** | armD의 −2.0은 과함 — 발을 붙박이로 만들어 몸통만 흔들리게 함 |
| `dof_pos_margin` | −0.5 | 한계의 85% 초과분만 2차 페널티 |
| `dof_vel_margin` | −0.02 | 〃 |
| `torque_margin` | −0.002 | 〃 |
| `electrical_power` | −2e-3 | `\|τ·dq\| + k·τ²` |

> **관절보호 설계 근거 (codex 의견 + 반론)**: codex의 "margin 기반 연속 페널티"는 옳으나,
> `soft_dof_pos_limit`를 낮추는 방식은 **기존 `dof_pos_limits` 보상(스케일 −1.0)의 크기까지
> 바꿔서** A/B를 오염시킨다. 그래서 **별도 항**으로 분리했다. 정상 대역(한계 85% 이내)에서
> 정확히 0이므로 건강한 보행을 왜곡할 수 없고 독립적으로 켜고 끌 수 있다.
>
<!--관절이 일정 각도를 넘어가면 soccer mode 등에선 protect mode로 진입 했었는데 custom mode에서도 그러는지 조사가 필요-->
> **[답변]** **매뉴얼(K1 Instruction Manual V1.6)에서 확인했다. 결론: 명시적으로는 확답이 안 되고,
> "보장받지 못한다"고 가정하고 설계해야 한다.**
> 
> 매뉴얼 원문 3곳:
> - **PROTECT Mode**: *"PROTECT mode will automatically kick in on errors
>   (i.e, **exceeding joint limit** or falling)."* / *"Joints under PROTECT Mode behave the
>   same as DAMP mode."* → 관절한계 초과 시 자동 진입하고, **관절이 DAMP처럼 힘을 뺀다.**
> - **CUSTOM Mode**: *"K1 gives up control on **all joints** to developer, who controls K1
>   through its SDK."*
> - **전이 규칙**: *"CUSTOM mode can only switch to PREP or DAMP mode."*
> 
> **모순 지점이 바로 여기다.** PROTECT는 "에러 시 자동 진입"이라고 무조건적으로 서술되지만,
> CUSTOM의 전이 규칙에는 **PROTECT가 없다.** 그리고 CUSTOM은 "모든 관절 제어권을 개발자에게
> 넘긴다"고 명시한다. 둘 중 무엇이 우선하는지 매뉴얼은 말하지 않는다.
> 
> **따라서 실기 확인 전까지는 "CUSTOM에서 PROTECT가 안 걸린다"고 가정하는 게 안전하다.**
> 근거: (a) 전이표에 없고, (b) "모든 관절 제어권 이양"이라는 문구가 더 구체적이며,
> (c) 매뉴얼이 CUSTOM에서 **호이스트 상시 사용을 권고**한다(*"it is recommended to use a
> Hoist at all times under CUSTOM mode"*) — 펌웨어가 알아서 지켜준다면 이런 경고가 붙을 이유가 없다.
> 
> **실기 확인 절차(호이스트 필수)**: CUSTOM 진입 → SDK로 한 관절을 한계각 근처까지 천천히
> 지령 → 모드 상태와 관절 토크가 죽는지 관측. 한 관절, 저속, 매달아 놓고.
> 
> **설계 함의**: 우리가 넣은 `dof_pos_margin`(한계 85%부터 2차 페널티)이 **"있으면 좋은 것"이
> 아니라 유일한 방어선일 수 있다.** 그리고 배포단에 우리 자신의 런타임 guard가 반드시 필요하다.
> §6-11에 항목으로 추가했다.

> **에너지에 대한 반론**: codex의 "Wh/m 제약"은 **실기 전류/전압 로그가 있어야** 의미가 있다.
> 없는 상태에서 제약을 걸면 부정확한 시뮬 파워 모델에 과적합한다. 그래서 제약이 아니라
> **약한 페널티**로만 넣었다. 내용은 기존 `power`(양의 기계파워만)의 두 결함을 메운다:
> `|τ·dq|`는 **제동 시에도 전류가 흐름**을 반영(부호 무시), `k·τ²`는 **저속·고토크에서
> 지배적인 동손(I²R)**을 반영.
<!--real log 없이 시뮬에서만 real에서도 낭낭하게 적용가능한 제약법에 관한 논문 리서치해서 수정하진 말고 나에게 보고만 해.-->
> **[답변]** **지시대로 코드는 건드리지 않고 조사 결과만 보고한다.**
> 
> **핵심 논문: Kim et al., "Not Only Rewards But Also Constraints: Applications on Legged
> Robot Locomotion" (arXiv:2308.12517).** 우리 상황에 가장 직접적이다.
> - 보상 가중치를 손으로 맞추는 대신 **제약(constraint)으로 표현**한다. 차이가 결정적인데,
>   **보상 가중치는 단위가 없어 서로 비교 불가**지만 제약은 "관절속도 20 rad/s 이하"처럼
>   **물리 단위로 직접 쓴다.** 실기 스펙시트에서 바로 읽어올 수 있다는 뜻이다 —
>   **실기 로그가 없어도 제약값은 알 수 있다.**
> - 우리에게 주는 함의: `torque_margin: -0.002` 같은 무단위 계수를 튜닝하는 대신
>   "토크는 한계의 85%를 초과하지 않는다"를 **제약으로** 걸고 Lagrange 승수를 학습시키면,
>   튜닝 없이 스펙을 지킨다.
> 
> **보조 논문**
> - **"Actuator-Constrained RL for High-Speed Quadrupedal Locomotion" (arXiv:2312.17507)**:
>   **actuator의 토크-속도 곡선**(모터 스펙에서 나옴, 실기 로그 불필요)을 제약으로 넣어
>   고속 보행에서 실기 전이. 우리 K1도 관절별 effort/velocity가 URDF에 이미 있다.
> - **"Sim-to-Lab-to-Real" (Artificial Intelligence, 2022)**: sim과 real 사이에 **중간
>   단계(Lab)**를 두고 shielding으로 hard constraint를 강제. 실기 로그를 "얻기 전에"
>   안전하게 얻는 방법론.
> - **RMA (arXiv:2107.04034)**: 실기 로그 없이 전이하는 정석 — 시뮬에서 뽑은 잠재 파라미터를
>   실기에서 온라인 추정. 다만 이건 안전 제약이 아니라 적응 기법이다.
> 
> **공통 결론(문헌이 일치하는 부분)**: 실기 로그 없이 안전을 확보하는 정공법은
> **"로그에서 배우는 것"이 아니라 "스펙에서 제약을 세우고, 그 스펙을 domain randomization으로
> 불확실하게 만드는 것"**이다. 즉 토크 한계 자체를 0.8~1.0배로 랜덤화해서
> "스펙이 정확히 얼마인지 모른다"는 사실까지 학습에 넣는다. 이게 §6-5에 이미 넣어둔
> 배터리 항목과 정확히 같은 처방이라는 점이 교차검증이 된다.
> 
> **과랜덤화 경고**: *"excessive randomization leads to conservative policies"*.
> armD가 12레버를 한꺼번에 넓혀 무너진 것과 같은 실패라서, 램프업이 필요하다(§6-4).


**✅ 적용완료(`bd58e53`) — `V7_full` arm에서 학습 중**: `goal_reached`의 정지 판정에 각속도가 빠져 있었다.
```python
stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1   # 선속도만!
```
→ 몸통을 회전시키면서도 보너스를 계속 받을 수 있다. **armB의 "제자리걸음 시 orientation
흔들림"과 armC의 "흔들림이 B보다 덜함" 두 관찰이 모두 이 한 줄을 가리킨다.**
처방: `stopped = (|v_xy| < 0.1) & (|ω| < 0.3)`. armB의 정지 능력(안 멈춤 1% vs 49%)을
유지한 채 흔들림만 제거할 수 있다.
<!--이건 실제로 B와 C의 차이에 근거한거지? 빨리 반영 안하고 뭐하냐 병신아-->
> **[답변]** **근거 먼저: A↔B가 1레버 비교라 이게 주근거이고, C는 보조 근거다.**
> - **armA vs armB**: config 차이가 `goal_reached` **단 하나**. 관찰 — B가 A보다 제자리에서
>   더 흔들린다. → 이건 **깨끗한 단일변인 비교**다.
> - **armC vs armB**: C는 `goal_reached`가 없고 흔들림이 B보다 적다. **다만 C는 200 Hz라
>   물리도 다르므로 교란이 있다.** 그래서 보조 근거로만 쓴다.
> - 두 비교가 **같은 한 줄**을 가리키고, 코드에도 원인이 그대로 보인다:
>   `root_states[:, 7:9]`는 **선속도 x,y뿐**이라 각속도가 판정에 없다.
> 
> **이미 반영돼 있다. 지금 GPU에서 그 코드가 돌고 있다.** 커밋 `bd58e53`:
> ```python
> def _reward_goal_reached(self):          # goal_pose_v7.py
>     base = super()._reward_goal_reached()
>     thr = self.cfg["rewards"].get("stop_ang_speed_threshold", 0.0)
>     if thr <= 0.0: return base            # 0 = armB와 정확히 동일
>     settled = torch.norm(self.root_states[:, 10:13], dim=-1) < thr
>     return base * settled.float()
> ```
> `Goal_Pose_V7.yaml`에 `stop_ang_speed_threshold: 0.3`.
> 
> **다만 4개 arm 중 `V7_full`에만 켜져 있다.** E0/E1/E2는 `_OFF_PROTECT`에 이 키가 들어
> 있어서 0(=armB 동작)이다. 의도적이다 — 이 한 줄의 효과를 **V7_full ↔ E1/E2 대조로
> 분리 측정**하려는 것이고, 모든 arm에 켜면 또 다시 "여러 개를 동시에 바꿔 귀속 불가"가 된다.
> 
> 전달이 부실했던 건 제 잘못이다. "미적용 1순위"라고 써둔 문서를 안 고쳐서 아직 안 한 것처럼
> 보였다. 아래 §6-1도 함께 정정했다.


### 3-5. 외란 — URDF 충격량 기반 2종 분리

**계산 근거**: K1 총질량 **18.71 kg**(URDF 전 링크 합산). 동일 로봇 2대 충돌의 환산질량
m_eff = m₁m₂/(m₁+m₂) = **9.36 kg**. 서로 0.5 m/s로 마주 오면 Δv = 1.0 m/s →
충격량 J = **9.36 N·s** → 접촉 0.1 s 기준 평균력 **≈ 94 N**, 0.15 m 편심 타격 시 **≈ 14 N·m**.

| 종류 | 힘 | 토크 | 지속 | 모델 대상 |
|---|---|---|---|---|
| **collision** | 40~150 N | 3~20 N·m | 0.05~0.15 s | **로봇 2대 충돌** |
| **support** | 3~15 N | 0.2~2 N·m | 1.5~3.0 s | 사람이 잡아 지지 |

50:50 혼합, env별 3~8 s 간격. 기존 최대 15 N 대비 **최대 힘 10배**.

> **함께 고친 설계 결함**: 기존 `_push_robots`는 `common_step_counter % interval == 0`이라
> **4096개 env 전부가 같은 스텝에 동시에** 밀렸다. 배치 내 외란이 완전 상관되어 표본
> 다양성이 사실상 1이었다. v7은 **env별 독립 타이머**를 쓴다.

### 3-6. 지각 모델 — 주파수 성분별 4층

| 항 | 주파수 | 크기 | 모델 대상 |
|---|---|---|---|
| `goal_pos` 지터 | 매 step | 4 cm | 측정 잡음 |
| `goal_pos_bias` | 구간당 1회 | 5 cm | 정위 계통오차 |
| `goal_obs_hold_steps` | 2~3 step | — | 카메라 20 fps vs 제어 50 Hz |
| **`goal_bt_flicker`** | **~5 s에 1회** | **1 m 점프** | **BT 분기 / 공 재검출** |

> **전부 관측에만 적용하고 보상은 참값을 읽는다.** 보상까지 흔들면 정책은 "흔들리는 목표를
> 쫓는 것"을 배워 더 경련한다. 관측만 흔들면 정책은 **"흔들림을 걸러내는 것"**을 배운다.
> 가우시안 지터는 저역통과로 지워지지만 BT 분기는 **한 번에 크게 점프**한다 — 통계가 다르므로
> 별도 항이 필요했다.
<!--보상은 참값을 읽는다는게 무슨 뜻이지? 흔든다는게 뭘까롱.. 이 문단을 12살 아이에게 설명하듯이 분해해줘-->
> **[답변]** **보물찾기로 설명한다.**
> 
> 로봇에게는 두 가지가 있다. **눈**(관측)과 **점수판**(보상).
> 
> - **눈**: 로봇이 "보물이 저기 있는 것 같다"고 **믿는** 위치. 관측 벡터의 `commands[0:3]`.
> - **점수판**: 심판이 "보물은 사실 여기 있었다"를 기준으로 매기는 점수. `goal_dist` 계산.
> 
> **"흔든다(노이즈를 넣는다)"** = 로봇의 **눈에만** 거짓말을 섞는 것이다. 실제 보물은
> 가만히 있는데, 로봇 안경에 김이 서리거나 순간적으로 엉뚱한 데를 가리키게 만든다.
> `goal_bt_flicker`는 5초에 한 번쯤 **"보물이 1 m 옆으로 순간이동한 것처럼 보이게"** 한다.
> 실제 보물은 **안 움직인다.**
> 
> **"보상은 참값을 읽는다"** = 심판은 안경을 안 쓴다. **진짜 보물 위치로만 채점한다.**
> 
> **왜 이렇게 나누는가 — 두 경우를 비교하면 명확하다.**
> 
> | | 눈과 점수판 둘 다 흔들면 | 눈만 흔들면 (우리 방식) |
> |---|---|---|
> | 로봇이 배우는 것 | "보이는 대로 쫓아가면 점수" | "보이는 게 튀어도 진짜 위치로 가야 점수" |
> | 결과 행동 | 헛것을 보고 **홱홱 방향을 바꾼다** = 경련 | **튄 관측을 무시하는 법**을 배운다 = 안정 |
> 
> 둘 다 흔들면 로봇 입장에서는 헛것을 쫓는 게 **정답**이 돼버린다. 심판도 헛것 기준으로
> 점수를 주니까. 그러면 실제 로봇은 공 인식이 한 번 튈 때마다 몸을 홱 돌리는 물건이 된다.
> 
> 눈만 흔들면 헛것을 쫓을 때마다 **점수가 깎인다.** 그래서 로봇은 "가끔 눈이 튄다"는 걸
> 전제로 **여러 프레임을 종합해 진짜 위치를 추측하는 법**을 스스로 배운다. 우리가
> 원하는 게 정확히 이것이다 — BT가 흔들려도 안 흔들리는 보행.
> 
> **한 줄**: 시험문제(관측)는 지저분하게 내되, 채점(보상)은 정답으로 한다.


### 3-7. 팔 — URDF 마운트 수정

**원인**: K1 URDF의 팔 관절은 **전부 `type="fixed"`**이고 `collapse_fixed_joints: true`라
로드 시 부모 링크로 병합된다. `num_actions: 12`는 다리뿐이다.
⇒ **정책으로도 보상으로도 팔을 못 움직인다. URDF 수정만이 유일한 방법이었다.**

[K1_locomotion_armsdown.urdf](htwk-gym/resources/K1/K1_locomotion_armsdown.urdf):
`Shoulder_Roll`의 **고정 마운트 rpy**를 ∓80°로 회전.

| | 기존 (T자) | armsdown |
|---|---|---|
| 손 위치 (Trunk 기준) | y=±0.31, z=+0.17 | y=±0.174, **z=+0.008** |
| 팔의 yaw 관성 기여 | 0.180 kg·m² | **0.076 kg·m² (−58%)** |
| 팔 무게중심 높이 | +0.172 m | **+0.083 m (−8.9 cm)** |

> **90°가 아니라 80°인 이유**: 다리가 y=±0.096이고 팔(Arm_2/Arm_3/hand)에 충돌 지오메트리가
> 있다. 완전 수직이면 y=±0.145로 간격 4.9 cm에 불과하다. 10° 외측 splay로 7.8 cm 확보하며,
> 사람의 carrying angle과도 일치한다.

**⚠️ 이건 동역학 변경이다.** yaw 관성 −58%, 전체 무게중심 약 1.4 cm 하강 → **재학습 필요.**
(단, 회전 민첩성과 안정성 양쪽에 유리한 방향이다.)

**❓ 확인 필요**: "shoulder pitch/roll은 건들지 말 것"과 관련해, 기하학적으로 T자를 내리려면
shoulder roll 회전이 불가피하다. 여기서 한 것은 **관절 구동이 아니라 고정 마운트 각도 변경**
이므로 "보행 중 shoulder가 움직이지 않는다"는 요구는 지켜진다. 그 뜻이 아니었다면 알려주시라.
<!--의도한대로 한거 맞음-->
> **[답변]** 확인 감사하다. 90°로 다시 올리면서 같은 원칙(관절 구동이 아니라 고정 마운트)을 유지했다.


**미구현 — 속도 연동 후방 스윙** (다음 작업):
1. `Left/Right_Elbow_Pitch`, `Left/Right_Elbow_Yaw` 4개를 `revolute`로 전환
2. **비학습 DOF 지원**: `num_dofs` 16, `num_actions` 12 유지. 이 4개는
   `dof_pos_ref = clip((|v|−0.5)/0.5, 0, 1) · θ_max`로 **스크립트 구동**
3. 관측/액션 벡터에서 제외 → **obs 54 유지 → warm start 보존** ← 이 설계의 핵심 이점
4. 손댈 곳: `goal_pose.py`의 `_init_buffers`(default_dof_pos), `step()`의 `dof_targets`
<!--이거 언제 구현할거야? 미구현이면 일단 스케쥴은 하라니까. 언제 어떤 학습에서 **가 확인이 되면 그 때 진행하겠다. 이렇게 돼야지. 그리고, 팔에 대해선 더 고민을 해보자. 만약 shoulder 90도가 아니라면 생기는 문제: 애초에 로봇 간 팔 걸림을 막기 위해서 팔을 고정하는건데 양 옆으로 ego 영역이 너무 커짐. 일단 팔뚝은 straight down, always 90deg로 하고, 팔꿈치관절이랑 shoulder yaw만 바꿔서 hand(end of arm)쪽을 로봇의 hip 뒤에 자연스럽게 위치할 수 있도록 하자. 이건 0.5로 자르지 말고, goal reached랑 같이 판단하는건 어때? 이게 만약 actor에 없는 정보라면 스스로 goal reached라고 판단하는기능이 없나? 그냥 그러면 비학습 dof지원으로 로봇이 stale할 때는 팔꿈치를 펴서 정렬하고, 로봇이 움직이기 시작하면(goal reached가 깨진다든지(BT에서 이정도는 구현 가능), 아니면 Imu값을 보고 판단해도 좋고) 팔끝을 뒤로 숨기는거로 하자. 어차피 보행 동역학만 영향을 주면 되는거잖아.-->
> **[답변]** **90도 지적이 옳았다. 반영했다. 다만 "팔꿈치로 뒤로 숨기기"는 이 URDF에서 기구학적으로
> 불가능하다는 걸 발견했다 — 이게 이번 조사의 가장 중요한 결과다.**
> 
> **(1) 90도로 변경 완료.** ego 반폭(팔의 가장 바깥 지점)이 실측으로:
> | | 기존 T자 | 80° splay | **90° 수직(채택)** |
> |---|---|---|---|
> | ego 반폭 | 0.396 m | 0.186 m | **0.154 m** |
> | 팔 I_zz | 0.1796 | 0.0759 | **0.0695 kg·m²** |
> | 팔 I_xx | 0.2667 | — | **0.1000 kg·m²** |
> 말씀대로 splay는 "로봇 간 팔 걸림 방지"라는 원래 목적을 갉아먹는다. 다리 반폭이 0.096이니
> 90°에서도 팔이 0.154로 더 바깥이지만, 그건 어깨가 애초에 0.145에 달려 있어서 어쩔 수 없다.
> 
> **(2) ⚠️ 팔꿈치로는 손을 못 움직인다 — URDF 기구학 감사 결과**
> ```
> Left_Elbow_Pitch : axis=[0 1 0]   offset=[0, 0.044428, 0]   <- 축이 팔 방향과 평행
> Left_Elbow_Yaw   : axis=[0 0 1]   offset=[0, 0.1215,   0]   <- 팔뚝(0.762 kg) 뒤에 위치
> ```
> - `Elbow_Pitch`의 회전축 `[0,1,0]`이 팔뚝 오프셋 `[0,0.044,0]`과 **평행**하다.
>   축과 링크가 나란하면 그건 **비틀림(twist)이지 굽힘이 아니다.** 아무리 돌려도 팔이 안 접힌다.
> - `Elbow_Yaw`는 팔뚝 0.1215 m **뒤쪽**에 붙어 있어서, 돌려도 **손(0.168 kg)의 방향만**
>   바뀌고 팔뚝 질량(0.762 kg)은 제자리다.
> → **두 팔꿈치 관절 중 어느 것도 팔의 질량을 이동시키지 못한다.** FK로 확인했다.
> 
> 즉 이 URDF에서 팔 끝 위치를 바꿀 수 있는 관절은 **Shoulder_Pitch와 Shoulder_Roll뿐**이다.
> (벤더 URDF의 축 정의가 실제 팔꿈치와 안 맞는 것으로 보인다. K1_serial.urdf도 동일해서
> 오타가 아니라 일관된 모델링이다. 실기 팔을 실제로 굽혀보고 확인할 가치가 있다.)
> 
> **(3) 그래서: Shoulder_Pitch +25°로 손을 힙 뒤에 놓았다.**
> ```
> 손 위치 (Trunk 기준):  x = -0.074 m  (힙 뒤 7.4 cm)   y = ±0.145   z = +0.021
> ```
> Shoulder_Pitch 한계는 [-190°, +70°]라 +25°는 여유가 충분하다.
> **ego 반폭은 pitch와 무관**하므로(y가 안 변함) 걸림 방지 목적을 해치지 않는다.
> "shoulder는 건드리지 말라"는 원 지시와 충돌하지만, (a) 기구학적으로 대안이 없고
> (b) 이미 승인하신 대로 **관절 구동이 아니라 고정 마운트 각도**라 보행 중 움직이지 않는다.
> 
> **(4) 동적 스윙(정지 시 펴고, 이동 시 숨기기) — 하지 말자고 제안한다.**
> 근거: 위 (2) 때문에 동적으로 움직일 수 있는 건 **어깨뿐**이고, 어깨를 보행 중에 움직이면
> 그건 "팔 고정"이라는 전제 자체가 깨진다. 그리고 말씀하신
> *"어차피 보행 동역학만 영향을 주면 되는거잖아"*가 결정적인데 — **정적 자세만으로 이미
> I_zz −61%, I_xx −62%, 팔 COM 8.2 cm 하강**을 얻었다. 동적 스윙이 추가로 바꿀 관성은
> 이보다 훨씬 작다. **비용(비학습 DOF 인프라 + warm start 위험) 대비 이득이 안 맞는다.**
> 
> **(5) 그래도 하겠다면 — 판정 신호에 대한 답.**
> `goal_reached`로 판정하자는 아이디어는 좋다. 다만 **actor는 `goal_reached`를 모른다**
> (보상 항이지 관측이 아니다). 그런데 **알 필요가 없다**: 이건 학습되는 행동이 아니라
> 스크립트 구동이므로, `_reward_goal_reached`가 쓰는 것과 **같은 조건을 env가 직접 계산**해서
> 비학습 DOF 타깃에 넣으면 된다. 관측을 안 늘리므로 warm start도 안 깨진다.
> 실기에서는 말씀대로 BT가 goal_reached를 알고 있고, IMU로도 판정 가능하다.
> **0.5 m/s 하드컷 대신 `(goal_dist < 0.1) & (|v| < 0.1) & (|ω| < 0.3)`** — 즉 v7에서 이미
> 쓰는 정지 판정을 그대로 재사용하면 기준이 하나로 통일된다.
> 
> **(6) 일정 — 조건부로 명시한다.**
> > **v7 4개 arm의 clean eval에서 (a) E0의 낙상이 armB(37회) 이하이고 (b) 위치오차 median이
> > 5 cm 이내이면, 팔 URDF는 회수된 것으로 판정하고 다음 배치(§8)에 그대로 싣는다.
> > 동적 스윙은 그 배치의 결과에서 "정지 자세의 팔 위치가 RLKick 핸드오프를 방해한다"는
> > 증거가 나올 때만 착수한다. 증거가 없으면 영구 보류한다.**


---

## 4. 평가 하네스 변경

[eval_goal_pose.py](htwk-gym/eval_goal_pose.py) — **속도가 이제 직접 보인다.**

**영상 HUD** (외부 폰트/cv2 의존 없이 5×7 비트맵 폰트 내장 → 렌더 실패 위험 0):
```
VEL  1.34 M/S          <- |v_xy|
  VX +1.34  VY -0.12   <- 몸통 프레임 성분 (부호 포함 → 후진 속도 확인 가능)
  WZ +0.45 RAD/S       <- yaw rate
DIST  1.62 M           <- position error
HEAD  -7.3*            <- heading 오차
PUSH 12.5N 1.8NM       <- 외란 (인가 중일 때만)
```

**리포트 "몸통 속도" 섹션**: median / p90 / p99 / 순간최대 / 구간최고속도(median·p90·max) /
0.5 m/s 초과 시간비율 / 1.0 m/s 초과 시간비율.
1.0 m/s 초과가 1% 미만이면 **"정책 한계가 아니라 과제 한계"라는 경고를 자동 출력**한다.

**segments.csv**: `peak_speed_mps`, `mean_speed_mps` 추가. 카테고리에 `path` 추가.

### 4-1. 앞으로 eval은 3조건으로 돌린다

| 조건 | 목적 | 판정 |
|---|---|---|
| **clean** (외란 OFF) | 게이트 판정 — 기존과 동일 | 위치/heading/낙상 |
| **perturbed** (`--keep_perturbations`) | 강건성. clean 대비 열화폭이 곧 강건성 지표 | 낙상, 위치 열화 |
| **stress: jitter** (`--stress jitter`, **구현완료** `bd58e53`) | 참값 목표를 매 스텝 ±3 m 재추첨 | **낙상률·직립비율·\|ω\| 만** |

> **stress 조건에서 위치오차를 재면 안 된다**: 50 Hz 완전 랜덤이면 참값 목표의 기댓값이
> 원 중심이라 위치오차라는 지표가 정의되지 않는다. **"넘어지지 않는가 / 몸통이 발산하지
> 않는가"만** 측정하는 별도 모드여야 한다.
<!--맞아. goal이 안정되지 않은 상황에서 위치오차는 무의미하지. 넘어지지 않는가/몸통이 발산하지 않는가에 대해 구현해두도록 해. 어차피 이번 v7 4개 train끝나면 또 gpu 꽉 채워서 학습 해야해. 미리 팔과 함께 준비 해두도록 해.-->
> **[답변]** **이미 구현해서 커밋했다(`bd58e53`). 문서가 "미구현"으로 남아 있던 게 오류다 — 표도 고쳤다.**
> 
> `--stress jitter`로 동작한다:
> - 참값 목표를 **매 제어스텝(50 Hz) ±3 m 균일 재추첨**
> - `goal_categories`를 끄고(안 그러면 30%가 이동거리 0), path 모드도 끈다(jitter는 waypoint 스트레스)
> - **구간 통계 수집을 전면 중단**한다. 구간 길이가 1스텝이라 모든 per-segment 지표가 무의미하다
> - 측정 항목: **낙상 수 / 낙상률(회·env⁻¹·분⁻¹) / 직립 유지 시간 비율 / 몸통 각속도 |ω| 분포**
> - 자동 판정: 낙상률 > 0.5/env·분이면 실패, |ω| p90 > 3 rad/s면 "경련 중"으로 판정
>   (정상 보행 |ω|는 대략 1~2 rad/s)
> 
> "몸통이 발산하는가"의 지표로 **|ω|**를 고른 이유: 목표를 쫓아 몸을 홱홱 돌리는 실패 모드가
> 선속도가 아니라 **각속도**에 먼저 나타나기 때문이다. 직립 비율은 `projected_gravity_z < -0.7`
> (수직에서 45° 이내)로 잡는다.
> 
> 다음 배치 준비는 §8에 4종으로 정리했다.


> **기존 모든 숫자는 외란 OFF 성적이다.** 리포트 헤더의 "외란 OFF"가 그 뜻이다.

---

## 5. 실행

```bash
# 학습 (RunnerV3: 미니배치 PPO + 대칭 손실)
python train_v7.py --task=K1/Goal_Pose_V7 --config envs/K1/Goal_Pose_V7.yaml \
    --headless True --num_envs 4096 --max_iterations 20000 \
    --sim_device cuda:N --rl_device cuda:N

# 평가 (외란 OFF = 게이트)
python eval_goal_pose.py --task K1/Goal_Pose_V7 --checkpoint <run>/nn/<best>.pth \
    --record_video --record_video_s 60 --sim_device cuda:N --rl_device cuda:N

# 평가 (외란 ON = 강건성)
python eval_goal_pose.py ... --keep_perturbations
```

> **⚠️ warm start 주의**: v7은 URDF(팔)가 바뀌어 **동역학이 다르다.** model_20000에서
> 이어받는 것 자체는 텐서 모양이 같아 동작하지만(obs 54 유지), armD의 교훈대로 **초기
> 분포가 흔들릴 것**이다. 첫 수백 iter의 episode length를 반드시 확인하고, 무너지면
> 외란 강도를 낮춰 램프업하거나 from-scratch로 전환한다.

---

## 6. 미해결 / 다음 순서

| # | 항목 | 근거 | 우선도 |
|---|---|---|---|
| ~~1~~ | ~~`goal_reached`의 `stopped`에 각속도 추가~~ **완료** — `stop_ang_speed_threshold: 0.3`, `V7_full`에서 학습 중 | A↔B 단일변인 비교가 주근거, C는 보조 | ✅ |
| 2 | **`MaxSpeed` 상한 측정** | 새 lookahead floor 덕에 빠른 로봇이 carrot을 명령속도보다 빠르게 끌고 가므로, **별도 실험 없이** 명령 vs 실제 표의 포화점으로 나온다 | **높음**(F2에 포함) |
| 3 | 팔 **속도 연동 후방 스윙** (§3-7 4단계) | 팔 내리기로 이미 재학습이 필요하므로 함께 넣는 게 효율적 | 중 |
| 4 | 외란 **커리큘럼 램프업** | 150 N을 warm start 초기에 주면 armD 실패 재현 위험 | 중 |
| 5 | **배터리 보호** — 에피소드별 `torque_limits` 0.8~1.0배 랜덤화 | 전압 강하 = 실효 토크 상한 저하. 문헌 조사(§3-4 답변)도 "스펙을 랜덤화하라"로 수렴 | 중 |
| 11 | **런타임 guard 자체 구현** | 매뉴얼상 CUSTOM에서 PROTECT 자동 진입이 **보장되지 않는다**(§3-4 답변) | **높음** |
| ~~6~~ | ~~eval stress: jitter 모드 구현~~ **완료** (`--stress jitter`) | 판정 계획은 §8-F3 | ✅ |
<!--왜 설계만 한거야. 테스트 해볼 계획도 함께 세워야지-->
> **[답변]** **맞는 지적이다. 구현은 됐고(위), 빠져 있던 건 "언제 무엇으로 판정하는가"였다.
> §8에 배치 계획과 함께 판정 기준을 넣었다.**
> 
> stress jitter의 판정 계획:
> 1. **기준선부터 만든다.** v7 4개 arm의 best checkpoint에 각각 stress를 돌린다
>    (`train_and_eval.sh`가 `STRESS=1`로 이미 자동 실행하므로 별도 작업 없이 나온다).
> 2. **비교 대상**: E2_robust(BT flicker 학습 O) vs E0/E1(flicker 학습 X).
>    flicker 학습이 실제로 jitter 강건성을 만드는지가 이 조건의 존재 이유다.
> 3. **성공 판정**: E2의 낙상률이 E0 대비 유의하게 낮고, |ω| p90이 3 rad/s 미만.
> 4. **실패 시 처방**: `noise.goal_bt_flicker.prob_per_step`을 0.004 → 0.01로 올리고
>    `action_rate` 페널티를 강화한다(현재 -1.5).

| 7 | `constellation_radius` 1.0 → 0.6~0.7 | `2r²=2`라 180° 회전 = 2 m 거리와 동등 → 회전 우선 행동 유발 | 낮음 (path mode가 완화할 수 있어 **먼저 관측**) 
<!--이런 분석 좋다-->
> **[답변]** `constellation_radius`는 path mode 결과를 보고 판단한다 — path에서는 heading이 경로 접선으로 주어져 heading 오차 자체가 작아지므로, 이 불균형이 자동으로 완화될 수 있다. 한 번에 두 개를 바꾸지 않기 위해 관측 후 조정한다.

| 8 | heading **sin/cos 2채널화** | obs 54→55로 warm start 파괴. heading은 이미 게이트 통과라 급하지 않음 | 낮음 (obs 재설계 시 함께) |
<!--어차피 문제 없다고 한적이 있는데 이거 왜그런지 12살 아이에게 설명하듯 elaborate please-->
> **[답변]** **시계 바늘로 설명한다.**
> 
> heading(로봇이 향한 방향)은 **각도**다. 각도의 골치아픈 성질: **359°와 1°는 실제로 2°밖에
> 안 떨어져 있는데, 숫자로는 358이나 차이 난다.**
> 
> 우리는 heading을 −π~+π로 쓰니까 **−179°와 +179°가 이웃인데 숫자는 정반대**다.
> 신경망 입장에서는 입력이 갑자기 확 뛰는 지점이 생긴다. 이걸 **wrap-around 불연속**이라 한다.
> 
> **해결책이 sin/cos 2채널**이다. 각도 θ 하나 대신 `(cos θ, sin θ)` 두 개를 준다.
> 그러면 −179°와 +179°가 **원 위에서 실제로 바로 옆 점**이 된다. 점프가 사라진다.
> (위 §3-1의 gait clock이 이미 이 방식을 쓰고 있다.)
> 
> **그런데 왜 우리는 안 급한가 — 두 가지 이유다.**
> 
> **첫째, 보상에는 이미 불연속이 없다.** constellation 보상은
> `d_con = d² + 2r²(1 − cos θ)` 형태인데, 여기 **`cos θ`가 들어 있다.**
> `cos(−179°)`와 `cos(+179°)`는 거의 같은 값이다. 즉 **채점 기준은 이미 매끄럽다.**
> 불연속은 **관측(로봇의 눈)에만** 남아 있다.
> 
> **둘째, 실측으로 그 지점에 잘 안 간다.** heading 오차 median이 armB 7.3°, armC 1.8°다.
> ±180° 근처에 오차가 쌓여야 이 불연속이 문제가 되는데, **오차가 애초에 그 근처로 안 간다.**
> 전 버전이 heading 게이트(10° 이내)를 통과했다.
> 
> **비용이 큰 이유**: 채널을 하나 늘리면 관측이 54 → 55가 되고, 그러면
> **armB 체크포인트의 첫 레이어 크기가 안 맞아 warm start가 통째로 깨진다.**
> 1만 iteration 넘게 쌓은 걸 버리고 처음부터 시작해야 한다.
> 
> **결론**: 이득은 작고(이미 통과한 지표) 비용은 크다(재학습). 그래서
> "관측 구조를 어차피 갈아엎을 때 같이" 하는 게 맞다. **문제가 없다는 뜻이 아니라,
> 지금 고칠 만한 문제가 아니라는 뜻이다.**

<!--이건 별로임 버려.-->
> **[답변]** 폐기했다. §6 표에서 삭제했다.

| 10 | Rudin **timed reward** (`final_window_s`) | v3에서도 0이라 **켠 적이 없다** — 효과 미측정 | 낮음 |
<!--변인통제 후 실험을 할 수 있는 버전을 만들어. v7 4가지 학습 모드 중 가장 평이한거에서 변화시키면 되겠지? 팔, stress와 함께 v7이후에 동시에 학습할 버전 중 하나로 하면 되겠네. 네번째 버전은 v7의 피드백을 수렴한 버전으로 남겨두자.-->
> **[답변]** **정확히 그 구조로 §8에 배치를 짰다.** 기준 arm은 **E0_armB_armsdown**이 맞다 —
> path·외란·보호항이 전부 꺼져 있어 v7 계열 중 가장 평이하고, armB에서 딱 두 가지
> (팔 URDF + 대칭손실)만 다르므로 변인통제의 기준선으로 적합하다.
> 
> 다음 배치 4종: **F1**(timed reward 단독) · **F2**(grid 커리큘럼 + 새 lookahead) ·
> **F3**(stress 강건성 강화) · **F4**(v7 피드백 수렴 — 결과 보고 채움).
> F4를 비워두라는 지시대로 §8에 슬롯만 잡아뒀다.


### 감시 항목

- **armB의 좁은 스탠스**: `goal_reached`의 부산물로 발이 모인다. 지지다각형이 작아
  **횡방향 외란에 약할 수 있다**(armB 낙상 37회 > armA 34회, 인과 미확인).
  v7은 외란을 10배 키웠으므로 여기가 압력을 받는다. 무너지면 `feet_distance_ref`(0.18 m)
  기반 하한 페널티를 건다.
- **낙상 게이트는 전 버전 미달**(최선 v3의 3회). v7에서 외란을 키웠으므로 **일시적으로
  나빠지는 것이 정상**이다. clean 조건에서 0에 수렴하는지로 판정한다.

---

## 7. 한 줄 요약

> armB의 `goal_reached` 하나가 도달·정지를 해결했고, 남은 문제는 **과제가 속도를 요구한
> 적이 없다는 것**이었다. v7 = armB의 보상 + 전진하는 lookahead 목표 + 속도 커리큘럼 +
> 실측 충격량 기반 외란 + 내려진 팔 + 처음으로 실제 켜진 대칭 손실.


---

## 8. 다음 GPU 배치 (v7 4개 종료 직후)

기준 arm은 **E0_armB_armsdown** — path·외란·보호항이 모두 꺼져 있어 v7 계열 중 가장
평이하고, armB에서 딱 두 가지(팔 URDF + 대칭손실)만 다르므로 변인통제의 기준선이 된다.

| arm | = E0 + | 답하는 질문 | 판정 |
|---|---|---|---|
| **F1_timed** | `final_window_s: 2.0` | Rudin timed reward가 실제로 효과가 있나. **v3에서도 0이라 한 번도 켜진 적이 없다** | E0 대비 위치오차·도착속도 |
| **F2_grid** | grid 커리큘럼 + 새 lookahead floor + serpentine | 스칼라→2D 그리드가 (v,ω) 경계를 잡나. **명령 vs 실제 속도 포화점 = 물리적 상한** | 추종비 0.75 꺾임점, `path_lag` |
| **F3_stress** | flicker 강화(0.01) + 외란 램프업 | jitter 강건성이 학습으로 만들어지나 | stress 낙상률·\|ω\| p90 (E0 대비) |
| **F4_TBD** | *(v7 결과 보고 채움)* | — | — |

> **F2가 이번 배치의 핵심**이다. §3-3 답변의 grid-adaptive와 §3-2 답변의 lookahead floor가
> 둘 다 여기서 처음 실전 검증된다. 둘을 한 arm에 묶은 것은 **분리할 수 없기 때문**이다 —
> lookahead floor가 없으면 `path_lag`가 정의되지 않고, `path_lag`가 없으면 grid의 승급
> 판정이 불가능하다. 서로의 전제다.

**팔 URDF는 4개 arm 전부에 들어간다**(E0가 이미 쓰고 있으므로 기준선이 유지된다).

**착수 조건**: v7 4개의 clean eval에서 E0의 낙상 ≤ armB(37회) **그리고** 위치오차
median ≤ 5 cm. 못 넘기면 팔 URDF가 warm start를 깬 것이므로, F 배치 대신
**외란/팔 램프업 재학습**을 먼저 한다.

---

## 9. 계측 커버리지 (§7 답변의 감사 결과)

"모든 의도가 디버깅 가능한가"를 감사한 결과 8개 중 3개가 계측 불가였다. 메운 내용:

| 구멍 | 왜 문제였나 | 메운 방법 |
|---|---|---|
| 속도 커리큘럼 진행 | `speed_level` 스칼라가 로그에 안 남아, 커리큘럼이 **멈춘 건지 오른 건지** 사후 판별 불가 | grid 활성 칸 수 + 칸별 성공률/시도수를 `extras`로 노출 |
| 관절/액추에이터 보호 | 페널티는 걸었는데 **한계에 얼마나 근접했는지**를 아무도 안 봤다. 0이면 안 걸린 건지 잘 지킨 건지 모름 | 한계 대비 접근률(p95)·포화 시간비율 |
| 대칭 손실 | 켰다는 사실만 알고 **좌우 대칭이 실제로 좋아졌는지** 지표가 없었다. armA의 "왼발이 앞" 관찰을 재검증할 방법이 없음 | 좌우 발 전후 위치차의 시간평균 |

> **강제 구조**: 규칙 문서로는 안 지켜진다. 그래서 `smoke_v7.py`가 **학습 전 게이트**이고
> (`run_v7_suite.sh`가 실패 시 학습을 시작조차 안 함), `train_and_eval.sh`가 **학습 후
> clean + stress eval을 자동 실행**한다. 사람이 기억할 필요가 없다.

<!--모든 의도에 대한 디버깅이 가능한 로거/eval을 만들어뒀는지 점검해보고 안 만들어졌으면 현재 학습중인 버전에 fit하게 eval/logger를 만들 것. 이건 앞으로 어떤 학습을 하든 꼭 병행할수박에 없도록 만들어-->
> **[답변]** **감사했다. 결과: 8개 의도 중 3개가 계측 불가였다. 표로 정리하고 구멍을 메웠다.**
> 
> | v7의 설계 의도 | 검증 지표 | 상태 |
> |---|---|---|
> | path 추종 | `path_lag`, 명령 vs 실제 속도 표 | ✅ (이번에 추가) |
> | 속도 요구 생성 | 몸통속도 median/p90/p99, 1.0 m/s 초과 시간비율 | ✅ |
> | 속도 커리큘럼 진행 | grid 활성 칸 수, 칸별 성공률 | ⚠️ **구멍이었음 → 추가** |
> | 외란 2종 | HUD 실시간 N·N·m, 스모크에서 대역 검증 | ✅ |
> | BT flicker 강건성 | stress jitter 리포트(낙상률·\|ω\|) | ✅ |
> | 관절/액추에이터 보호 | 한계 접근률·포화 시간 | ⚠️ **구멍이었음 → 추가** |
> | 대칭 손실 | 좌우 발 전후 비대칭 | ⚠️ **구멍이었음 → 추가** |
> | 도착 후 정지 | 도착속도, 안 멈춤 비율 | ✅ |
> 
> **"앞으로 꼭 병행할 수밖에 없게" 만드는 방법**이 핵심 요구인데, 이건 문서 규칙으로는
> 안 지켜진다. 그래서 **`tools/smoke_v7.py`가 학습 전 게이트**이고
> (`run_v7_suite.sh`가 실패 시 학습을 아예 시작하지 않는다), **`train_and_eval.sh`가 학습
> 후 clean/stress eval을 자동 실행**한다. 즉 **사람이 기억할 필요가 없는 구조**로 이미 묶여
> 있고, 여기에 위 3개 지표를 넣어 커버리지를 채웠다.
