# K1 Goal-Pose — Masterplan 2 (v7 통합 아키텍처)

> 작성 2026-07-26. [MASTERPLAN.md](MASTERPLAN.md)는 v0~v6의 실기(實記)로 그대로 둔다.
> 이 문서는 armA/B/C/D + v3 평가 결과로 **버전을 통폐합한 뒤의 단일 아키텍처**를 정의한다.
> 항목별 사용자 피드백 답변은 [MASTERPLAN_feedback.md](MASTERPLAN_feedback.md)에 있다.

---

## 1. 이번 평가가 확정한 것 두 가지

### 1-1. armB 압승, 원인은 레버 하나 (`goal_reached`)

5개 버전 × 256 env × 120 s, 결정론적 정책, 외란 OFF, 관측노이즈 ON, seed 0.
아래 수치는 각 `segments.csv` 원자료(총 22,506 구간)를 재집계한 값이다.

| | **armB** | armA | armC | armD | v3 | 게이트 |
|---|---|---|---|---|---|---|
| 위치오차 median | **3.9 cm** ✅ | 7.6 ❌ | 8.3 ❌ | 24.3 ❌ | 13.6 ❌ | ≤5 cm |
| 위치오차 p90 | **6.7 cm** ✅ | 14.7 ❌ | 16.7 ❌ | 49.5 ❌ | 30.1 ❌ | ≤10 cm |
| heading median | 7.3° ✅ | 2.1 ✅ | 1.8 ✅ | 2.8 ✅ | 3.3 ✅ | ≤10° |
| 엄격 성공률 | **52.8%** | 13.9 | 13.0 | 6.2 | 17.5 | — |
| 도착 후 안 멈춤 | **0.5%** | 33.4 | 28.5 | 6.2 | 2.7 | — |
| 한 번도 도달 못함 | **0.2%** | 19.3 | 25.3 | 60.0 | 49.9 | — |
| 낙상 | 37 ❌ | 34 ❌ | 10 ❌ | 21 ❌ | **3** ❌ | 0 |

armA와 armB의 config 차이는 **`rewards.scales.goal_reached: 0 → 1.0` 단 한 줄**이다.
따라서 위 표의 A↔B 격차는 전부 이 항에 귀속된다.

```python
stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1   # 몸통 선속도
return ((self.goal_dist < 0.1) & stopped).float()              # +1 / step
```

**작동 원리**: 4~8 s 구간을 50 Hz로 채우면 최대 200~400점이 걸린 큰 항이다. 조건을 만족하는
순간 **평평한 고원**이 생겨 "도착해서 멈춰 있는 상태"가 **흡수 상태(absorbing state)** 가
된다. `constellation` 단독은 `exp(-w·d_con)`이라 목표 근처에서 기울기가 0에 수렴하고,
"여기서 멈춰라"는 신호가 없다. 그래서 armA/C는 도착속도 0.1 m/s 부근에서 맴돈다(47~49%).

**전 카테고리 우위** (위치오차 median, m):

| | stand | turn | straight | lateral | combined |
|---|---|---|---|---|---|
| armA | 0.073 | 0.075 | 0.076 | 0.074 | 0.082 |
| **armB** | **0.033** | **0.038** | **0.039** | **0.040** | **0.040** |

부분 우위가 아니라 전면 우위 → **armB를 v7의 backbone으로 채택.**

### 1-2. 이 과제는 속도를 요구한 적이 없다

| arm | 접근속도 median | p95 | **요구속도 median** | **요구속도 최댓값** |
|---|---|---|---|---|
| armA | 0.106 | 0.341 | 0.119 | **0.574** |
| armB | 0.109 | 0.351 | 0.116 | **0.574** |
| armC | 0.114 | 0.350 | 0.132 | 0.569 |
| armD | 0.073 | 0.276 | 0.116 | 0.606 |
| v3 | 0.043 | 0.256 | 0.063 | 0.589 |

요구속도 = `start_dist / segment_duration`. **4647개 구간 전체에서 최댓값이 0.574 m/s다.**

구조적 원인:
- `goal_dx ∈ [-2,2]`, `goal_dy ∈ [-1.5,1.5]` → 최대 거리 2.5 m
- `resampling_time_s ∈ [4,8]` → 최소 4 s
- ⇒ 이론적 상한 2.5/4 = **0.625 m/s** (실측 0.574와 일치)
- 게다가 `_resample_goals`가 **stand는 dx=dy=dθ=0, turn은 dx=dy=0**으로 만든다
  ([goal_pose.py:428-430](htwk-gym/envs/K1/goal_pose.py#L428))
  → **전체 구간의 29.5%가 이동거리 정확히 0** (실측: 1372/4647)

**결론: 목표 1.3~1.5 m/s는 이 분포에서 학습도 측정도 불가능했다.** 정책이 0.35 m/s에
머문 것은 정책의 한계가 아니라 **과제의 한계**다. 이것이 v7의 `path` goal mode를 만든 이유다.

---

## 2. 버전 통폐합 결정

| 버전 | 판정 | 근거 | v7으로 |
|---|---|---|---|
| **armA** | ❌ 폐기 | armB에 전 카테고리 열세 | — |
| **armB** | ✅ **채택 (backbone)** | 전 지표 1위 | 보상 세트 전체 |
| **armC** | ❌ 삭제 | 주 게이트 전패, 43% 더 학습하고도 짐. 낙상 우위는 iteration과 교란되고, v3가 500 Hz에서 낙상 3회를 내어 대안 존재 | — |
| **armD** | ❌ 해체 | 12레버 동시 변경, 24.3 cm | 지각노이즈·`goal_reached`·`stand_posture`만 흡수 |
| **v3** | ⚠️ **흡수** | armD 기반 위에서 24.3→13.6 cm, 낙상 21→3. 스택은 유효하나 좋은 기반에서 미검증 | 대칭손실·미니배치PPO 채택 / 거리커리큘럼 폐기 |

### 2-1. armD 해체의 근거 — 범인은 지각 노이즈가 아니라 PD 게인

위치오차 median (m):

| 카테고리 | armA | **armD** | 이동 필요? |
|---|---|---|---|
| **stand** | 7.3 cm | **5.0 cm** ← D가 더 좋음 | ❌ |
| turn | 7.5 | 20.0 | ❌ |
| straight | 7.6 | **29.8** | ✅ |
| lateral | 7.4 | **25.2** | ✅ |
| combined | 8.2 | **31.7** | ✅ |

지각 노이즈가 원인이라면 **stand도 같이 나빠져야 한다**(stand 목표도 같은 노이즈를 받는다).
그런데 stand는 오히려 좋아졌고 **이동이 필요한 모든 카테고리만 붕괴**했다.
⇒ 보행 자체가 깨졌다 ⇒ **`control.stiffness` 100→200, `damping` 2→5 (Hip/Knee 2배)가 주범.**
warm start 정책은 100/2에서 학습됐으므로 같은 액션이 2배 토크를 내면 액션↔결과 관계가 무효가
된다. 서 있기는 토크 여유가 커져 오히려 개선되는 것까지 관측과 일치한다.
**공범**: `goal_dx/dy` 3 m/2 m 확대 → 0.5 m 이상에서 미도달 87~97%.

### 2-2. v3 흡수의 근거 — v3는 armA가 아니라 armD 위에 쌓였다

`Goal_Pose_V3.yaml`은 armD의 12레버를 **전부 상속**한다(PD 200/5, goal 3m/2m, 4~10 s,
지각노이즈, armature 0.02). 따라서 v3의 성적은 armD와 비교해야 한다:

| | armD (v3의 기반) | **v3** | 개선 |
|---|---|---|---|
| 위치 median | 24.3 cm | 13.6 cm | **−44%** |
| 엄격 성공률 | 6.2% | 17.5% | **+2.8배** |
| **낙상** | 21 | **3** | **−86%** |
| stand | 5.0 cm | **2.4 cm** | 전 버전 최고 |

v3의 스택(커리큘럼+대칭+미니배치PPO)은 **망가진 기반을 절반 가까이 복구했다.**
낙상 3회는 전 버전 최저이며, armC가 물리 주파수를 희생해 얻은 10회보다 좋다.

**다만 `goal_curriculum`은 폐기한다.** 성공률이 낮으면 **목표 거리를 줄이는** 구조
(`init_level: 0.35`)라서 v3의 요구속도 median이 0.063 m/s로 전 버전 최저가 됐다.
**속도를 올리려는 목표와 방향이 정반대다.**

### 2-3. 발견: 대칭 손실은 한 번도 켜진 적이 없다

`Goal_Pose.yaml`의 `algorithm.symmetric_coef: 10.`은 **읽히지 않는 죽은 키**다
([runner_v3.py:13](htwk-gym/utils/runner_v3.py#L13)). 실제 키는 `symmetry_coef`이며
**RunnerV3에서만** 동작한다. armA~D는 전부 `train.py`(기본 Runner)로 돌았으므로
**대칭 손실 = 0**이었다. armA의 "왼발이 항상 살짝 앞" 관찰이 여기서 설명된다 —
warm start에서 물려받은 대칭이 학습 중 서서히 무너진 것이고, 이를 막는 항이 없었다.

⇒ v7은 `train_v7.py`(RunnerV3) + `symmetry_coef: 0.5`로 **처음으로 실제 활성화한다.**

---

## 3. v7 아키텍처

파일: [goal_pose_v7.py](htwk-gym/envs/K1/goal_pose_v7.py) ·
[Goal_Pose_V7.yaml](htwk-gym/envs/K1/Goal_Pose_V7.yaml) ·
[train_v7.py](htwk-gym/train_v7.py) · 커밋 `850a97b`

상속: `GoalPoseV7 → GoalPoseV3 → GoalPose`. 기존 코드는 **동결**, 전부 override로만 추가.
모든 신규 기능은 스위치이며, `path: 0.0` + `disturbance.enabled: false` + 신규 스케일 0이면
**v7은 armB로 정확히 환원된다.**

### 3-1. 관측 — 변경 없음 (54차원, warm start 보존)

| 구간 | 차원 | 실기 출처 |
|---|---|---|
| projected_gravity | 3 | IMU (AHRS) |
| base_ang_vel | 3 | 자이로 |
| **commands** | **10** | **앞 3개만 BT가 공급**, 나머지 7개는 상수 |
| gait clock cos/sin | 2 | 내부 카운터 |
| dof_pos / dof_vel / 직전 actions | 12×3 | 엔코더 / 내부 |

**BT ↔ 정책 인터페이스 = (Δx, Δy, Δθ) 3개.** Δx/Δy는 **로봇 로컬 프레임** [m],
Δθ는 ±π 래핑 [rad]. 매 제어 주기마다 현재 자세로 재투영해야 한다.

### 3-2. 목표 생성 — waypoint 50% / path 50%

**waypoint 모드** = 기존 과제 그대로. armB가 잘하는 "도착 후 정지"를 잃지 않기 위해 유지.

**path 모드 (신규)** — 속도 요구를 만드는 장치:
- 경로 5종: figure-8 · circle · spiral · rose/star(5엽) · pseudo-random wander
- 경로 크기 1.5~4.0 m, lookahead **0.5~3.0 m**
- lookahead point가 `path_speed`로 경로를 따라 **전진한다**
- goal heading = **경로 접선**
- **목줄(leash)**: 간격이 `lookahead_max_m`(3.5 m)를 넘으면 목표가 **멈춰서 기다린다**

> **왜 목줄이 필요한가**: 못 따라가는 로봇에게 무한히 도망가는 목표를 주면 `exp(-w·d²)`가
> 평평해져 gradient가 죽는다. 목줄이 있으면 뒤처짐은 단순히 "목표가 정지"로 귀결되어
> waypoint 모드와 동일한 잘 정의된 문제가 된다.

> **정지 목표와 전진 목표의 결정적 차이**: 정지 목표는 **아무 속도로나** 도달 가능하다.
> 전진하는 목표는 **그 속도를 내야만** 간격이 유지된다. 목표까지의 거리가 같아도 속도 요구가
> 완전히 다르다. **거리가 아니라 목표의 속도가 정책의 속도를 결정한다.**

### 3-3. 속도 커리큘럼 (v3 거리 커리큘럼의 대체)

```
keep-up 판정 = (lookahead 간격 < keepup_gap_m 2.0 m)
EMA > up_threshold 0.8   → speed_level += 0.05
EMA < down_threshold 0.5 → speed_level -= 0.05
path_speed ~ U(0.3, 0.3 + 1.3·speed_level)     상단 최대 1.6 m/s
```

수렴한 `speed_level`이 곧 **"이 정책이 지속 가능한 최고속도"의 학습된 추정치**다.
방향이 v3와 정반대: v3는 못하면 목표를 가깝게 해서 속도 요구를 **낮췄고**,
v7은 잘하면 속도를 **올린다**.

### 3-4. 보상

**armB 세트 유지** (`constellation: 3.5` + `goal_reached: 1.0`), 여기에:

| 항 | 값 | 근거 |
|---|---|---|
| `stand_posture` | **−1.0** | armD의 −2.0은 과함 — 발을 붙박이로 만들어 몸통만 흔들리게 함 |
| `dof_pos_margin` | −0.5 | 한계의 85% 초과분만 2차 페널티 |
| `dof_vel_margin` | −0.02 | 〃 |
| `torque_margin` | −0.002 | 〃 |
| `electrical_power` | −2e-3 | `\|τ·dq\| + k·τ²` |

> **관절보호 설계 근거 (codex 의견 + 반론)**: codex의 "margin 기반 연속 페널티"는 옳으나,
> `soft_dof_pos_limit`를 낮추는 방식은 **기존 `dof_pos_limits` 보상(스케일 −1.0)의 크기까지
> 바꿔서** A/B를 오염시킨다. 그래서 **별도 항**으로 분리했다. 정상 대역(한계 85% 이내)에서
> 정확히 0이므로 건강한 보행을 왜곡할 수 없고 독립적으로 켜고 끌 수 있다.
>
> **에너지에 대한 반론**: codex의 "Wh/m 제약"은 **실기 전류/전압 로그가 있어야** 의미가 있다.
> 없는 상태에서 제약을 걸면 부정확한 시뮬 파워 모델에 과적합한다. 그래서 제약이 아니라
> **약한 페널티**로만 넣었다. 내용은 기존 `power`(양의 기계파워만)의 두 결함을 메운다:
> `|τ·dq|`는 **제동 시에도 전류가 흐름**을 반영(부호 무시), `k·τ²`는 **저속·고토크에서
> 지배적인 동손(I²R)**을 반영.

**🔧 미적용 — 다음 수정 1순위**: `goal_reached`의 정지 판정에 각속도가 빠져 있다.
```python
stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1   # 선속도만!
```
→ 몸통을 회전시키면서도 보너스를 계속 받을 수 있다. **armB의 "제자리걸음 시 orientation
흔들림"과 armC의 "흔들림이 B보다 덜함" 두 관찰이 모두 이 한 줄을 가리킨다.**
처방: `stopped = (|v_xy| < 0.1) & (|ω| < 0.3)`. armB의 정지 능력(안 멈춤 1% vs 49%)을
유지한 채 흔들림만 제거할 수 있다.

### 3-5. 외란 — URDF 충격량 기반 2종 분리

**계산 근거**: K1 총질량 **18.71 kg**(URDF 전 링크 합산). 동일 로봇 2대 충돌의 환산질량
m_eff = m₁m₂/(m₁+m₂) = **9.36 kg**. 서로 0.5 m/s로 마주 오면 Δv = 1.0 m/s →
충격량 J = **9.36 N·s** → 접촉 0.1 s 기준 평균력 **≈ 94 N**, 0.15 m 편심 타격 시 **≈ 14 N·m**.

| 종류 | 힘 | 토크 | 지속 | 모델 대상 |
|---|---|---|---|---|
| **collision** | 40~150 N | 3~20 N·m | 0.05~0.15 s | **로봇 2대 충돌** |
| **support** | 3~15 N | 0.2~2 N·m | 1.5~3.0 s | 사람이 잡아 지지 |

50:50 혼합, env별 3~8 s 간격. 기존 최대 15 N 대비 **최대 힘 10배**.

> **함께 고친 설계 결함**: 기존 `_push_robots`는 `common_step_counter % interval == 0`이라
> **4096개 env 전부가 같은 스텝에 동시에** 밀렸다. 배치 내 외란이 완전 상관되어 표본
> 다양성이 사실상 1이었다. v7은 **env별 독립 타이머**를 쓴다.

### 3-6. 지각 모델 — 주파수 성분별 4층

| 항 | 주파수 | 크기 | 모델 대상 |
|---|---|---|---|
| `goal_pos` 지터 | 매 step | 4 cm | 측정 잡음 |
| `goal_pos_bias` | 구간당 1회 | 5 cm | 정위 계통오차 |
| `goal_obs_hold_steps` | 2~3 step | — | 카메라 20 fps vs 제어 50 Hz |
| **`goal_bt_flicker`** | **~5 s에 1회** | **1 m 점프** | **BT 분기 / 공 재검출** |

> **전부 관측에만 적용하고 보상은 참값을 읽는다.** 보상까지 흔들면 정책은 "흔들리는 목표를
> 쫓는 것"을 배워 더 경련한다. 관측만 흔들면 정책은 **"흔들림을 걸러내는 것"**을 배운다.
> 가우시안 지터는 저역통과로 지워지지만 BT 분기는 **한 번에 크게 점프**한다 — 통계가 다르므로
> 별도 항이 필요했다.

### 3-7. 팔 — URDF 마운트 수정

**원인**: K1 URDF의 팔 관절은 **전부 `type="fixed"`**이고 `collapse_fixed_joints: true`라
로드 시 부모 링크로 병합된다. `num_actions: 12`는 다리뿐이다.
⇒ **정책으로도 보상으로도 팔을 못 움직인다. URDF 수정만이 유일한 방법이었다.**

[K1_locomotion_armsdown.urdf](htwk-gym/resources/K1/K1_locomotion_armsdown.urdf):
`Shoulder_Roll`의 **고정 마운트 rpy**를 ∓80°로 회전.

| | 기존 (T자) | armsdown |
|---|---|---|
| 손 위치 (Trunk 기준) | y=±0.31, z=+0.17 | y=±0.174, **z=+0.008** |
| 팔의 yaw 관성 기여 | 0.180 kg·m² | **0.076 kg·m² (−58%)** |
| 팔 무게중심 높이 | +0.172 m | **+0.083 m (−8.9 cm)** |

> **90°가 아니라 80°인 이유**: 다리가 y=±0.096이고 팔(Arm_2/Arm_3/hand)에 충돌 지오메트리가
> 있다. 완전 수직이면 y=±0.145로 간격 4.9 cm에 불과하다. 10° 외측 splay로 7.8 cm 확보하며,
> 사람의 carrying angle과도 일치한다.

**⚠️ 이건 동역학 변경이다.** yaw 관성 −58%, 전체 무게중심 약 1.4 cm 하강 → **재학습 필요.**
(단, 회전 민첩성과 안정성 양쪽에 유리한 방향이다.)

**❓ 확인 필요**: "shoulder pitch/roll은 건들지 말 것"과 관련해, 기하학적으로 T자를 내리려면
shoulder roll 회전이 불가피하다. 여기서 한 것은 **관절 구동이 아니라 고정 마운트 각도 변경**
이므로 "보행 중 shoulder가 움직이지 않는다"는 요구는 지켜진다. 그 뜻이 아니었다면 알려주시라.

**미구현 — 속도 연동 후방 스윙** (다음 작업):
1. `Left/Right_Elbow_Pitch`, `Left/Right_Elbow_Yaw` 4개를 `revolute`로 전환
2. **비학습 DOF 지원**: `num_dofs` 16, `num_actions` 12 유지. 이 4개는
   `dof_pos_ref = clip((|v|−0.5)/0.5, 0, 1) · θ_max`로 **스크립트 구동**
3. 관측/액션 벡터에서 제외 → **obs 54 유지 → warm start 보존** ← 이 설계의 핵심 이점
4. 손댈 곳: `goal_pose.py`의 `_init_buffers`(default_dof_pos), `step()`의 `dof_targets`

---

## 4. 평가 하네스 변경

[eval_goal_pose.py](htwk-gym/eval_goal_pose.py) — **속도가 이제 직접 보인다.**

**영상 HUD** (외부 폰트/cv2 의존 없이 5×7 비트맵 폰트 내장 → 렌더 실패 위험 0):
```
VEL  1.34 M/S          <- |v_xy|
  VX +1.34  VY -0.12   <- 몸통 프레임 성분 (부호 포함 → 후진 속도 확인 가능)
  WZ +0.45 RAD/S       <- yaw rate
DIST  1.62 M           <- position error
HEAD  -7.3*            <- heading 오차
PUSH 12.5N 1.8NM       <- 외란 (인가 중일 때만)
```

**리포트 "몸통 속도" 섹션**: median / p90 / p99 / 순간최대 / 구간최고속도(median·p90·max) /
0.5 m/s 초과 시간비율 / 1.0 m/s 초과 시간비율.
1.0 m/s 초과가 1% 미만이면 **"정책 한계가 아니라 과제 한계"라는 경고를 자동 출력**한다.

**segments.csv**: `peak_speed_mps`, `mean_speed_mps` 추가. 카테고리에 `path` 추가.

### 4-1. 앞으로 eval은 3조건으로 돌린다

| 조건 | 목적 | 판정 |
|---|---|---|
| **clean** (외란 OFF) | 게이트 판정 — 기존과 동일 | 위치/heading/낙상 |
| **perturbed** (`--keep_perturbations`) | 강건성. clean 대비 열화폭이 곧 강건성 지표 | 낙상, 위치 열화 |
| **stress: jitter** (신규, 미구현) | 반경 3 m 원 안 50 Hz 랜덤 목표 | **낙상/발산만** |

> **stress 조건에서 위치오차를 재면 안 된다**: 50 Hz 완전 랜덤이면 참값 목표의 기댓값이
> 원 중심이라 위치오차라는 지표가 정의되지 않는다. **"넘어지지 않는가 / 몸통이 발산하지
> 않는가"만** 측정하는 별도 모드여야 한다.

> **기존 모든 숫자는 외란 OFF 성적이다.** 리포트 헤더의 "외란 OFF"가 그 뜻이다.

---

## 5. 실행

```bash
# 학습 (RunnerV3: 미니배치 PPO + 대칭 손실)
python train_v7.py --task=K1/Goal_Pose_V7 --config envs/K1/Goal_Pose_V7.yaml \
    --headless True --num_envs 4096 --max_iterations 20000 \
    --sim_device cuda:N --rl_device cuda:N

# 평가 (외란 OFF = 게이트)
python eval_goal_pose.py --task K1/Goal_Pose_V7 --checkpoint <run>/nn/<best>.pth \
    --record_video --record_video_s 60 --sim_device cuda:N --rl_device cuda:N

# 평가 (외란 ON = 강건성)
python eval_goal_pose.py ... --keep_perturbations
```

> **⚠️ warm start 주의**: v7은 URDF(팔)가 바뀌어 **동역학이 다르다.** model_20000에서
> 이어받는 것 자체는 텐서 모양이 같아 동작하지만(obs 54 유지), armD의 교훈대로 **초기
> 분포가 흔들릴 것**이다. 첫 수백 iter의 episode length를 반드시 확인하고, 무너지면
> 외란 강도를 낮춰 램프업하거나 from-scratch로 전환한다.

---

## 6. 미해결 / 다음 순서

| # | 항목 | 근거 | 우선도 |
|---|---|---|---|
| 1 | `goal_reached`의 `stopped`에 **각속도 추가** | armB 흔들림 ↔ armC 흔들림 없음, 두 관찰이 같은 한 줄을 가리킴 | **높음** |
| 2 | **`MaxSpeed` 단독 탐색** — K1의 물리적 상한 측정 | 1.3~1.5 m/s가 가능한 목표인지 **아무도 측정한 적 없다**. 상한을 모르면 목표 합리성 판정 불가 | **높음** |
| 3 | 팔 **속도 연동 후방 스윙** (§3-7 4단계) | 팔 내리기로 이미 재학습이 필요하므로 함께 넣는 게 효율적 | 중 |
| 4 | 외란 **커리큘럼 램프업** | 150 N을 warm start 초기에 주면 armD 실패 재현 위험 | 중 |
| 5 | **배터리 보호** — 에피소드별 `torque_limits` 0.8~1.0배 랜덤화 | 전압 강하 = 실효 토크 상한 저하. SOC를 obs에 넣는 것보다 저렴하고 물리적으로 정확 | 중 |
| 6 | eval **stress: jitter 모드** 구현 (§4-1) | 사용자 요청, 설계만 완료 | 중 |
| 7 | `constellation_radius` 1.0 → 0.6~0.7 | `2r²=2`라 180° 회전 = 2 m 거리와 동등 → 회전 우선 행동 유발 | 낮음 (path mode가 완화할 수 있어 **먼저 관측**) |
| 8 | heading **sin/cos 2채널화** | obs 54→55로 warm start 파괴. heading은 이미 게이트 통과라 급하지 않음 | 낮음 (obs 재설계 시 함께) |
| 9 | `armature: 0.02` 단독 검증 | armD에서만 있었고 단독으로 평가된 적 없음 | 낮음 |
| 10 | Rudin **timed reward** (`final_window_s`) | v3에서도 0이라 **켠 적이 없다** — 효과 미측정 | 낮음 |

### 감시 항목

- **armB의 좁은 스탠스**: `goal_reached`의 부산물로 발이 모인다. 지지다각형이 작아
  **횡방향 외란에 약할 수 있다**(armB 낙상 37회 > armA 34회, 인과 미확인).
  v7은 외란을 10배 키웠으므로 여기가 압력을 받는다. 무너지면 `feet_distance_ref`(0.18 m)
  기반 하한 페널티를 건다.
- **낙상 게이트는 전 버전 미달**(최선 v3의 3회). v7에서 외란을 키웠으므로 **일시적으로
  나빠지는 것이 정상**이다. clean 조건에서 0에 수렴하는지로 판정한다.

---

## 7. 한 줄 요약

> armB의 `goal_reached` 하나가 도달·정지를 해결했고, 남은 문제는 **과제가 속도를 요구한
> 적이 없다는 것**이었다. v7 = armB의 보상 + 전진하는 lookahead 목표 + 속도 커리큘럼 +
> 실측 충격량 기반 외란 + 내려진 팔 + 처음으로 실제 켜진 대칭 손실.
