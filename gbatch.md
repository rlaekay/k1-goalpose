# G 배치 — 정리와 실행 계획

> 2026-07-28. MASTERPLAN(1) / MASTERPLAN_feedback / masterplan2 / masterplan3 **전체를
> 다시 읽고** 정리한 문서. 목적·교훈·미구현 항목·도구 상태를 한 곳에 모으고,
> G 배치에서 실제로 무엇을 확인할 수 있는지를 확정한다.

---

## 1. 원래 목적 — 절대 잊으면 안 되는 것

**최종 목표는 RoboCup 경기장에서 쓰는 보행 정책이다.** 시뮬레이터 점수가 아니다.
MASTERPLAN §범위 고정에서 정한 것:

| 항목 | 내용 |
|---|---|
| 과제 | 평지에서 **SE(2) goal pose**(x, y, θ)로 걸어가 **도착해서 멈춘다** |
| 성공 기준 | 위치 median ≤ 5 cm, p90 ≤ 10 cm, heading median ≤ 10°, **낙상 0** |
| 인터페이스 | BT가 주는 것은 **(Δx, Δy, Δθ) 3개뿐**. 나머지는 온보드 센서 |
| 다음 단계 | 도착 자세에서 **RLKick으로 핸드오프** — 그래서 "멈추는 자세"가 중요 |

**잊으면 안 되는 제약 3개**

1. **관측은 54차원이고, 늘리는 순간 warm start가 전멸한다.** 1만 iteration 넘게 쌓은
   걸 버리게 된다. heading sin/cos를 계속 미루는 이유이자, SmoothTurn을 죽은 슬롯
   재사용으로 넣은 이유다.
<!--smooth turn을 학습히키는데 trajectory와 stack of waypoints가 사용되나? 그러면 실험 시간이 더 길어? 확인해서 시각화 작업 해둬 video로 보게(isaac gym rgba? rgb문제로 터졌었는데 이번엔 관련 조항 확인해서 제대로 해)-->
> **[답변]** **trajectory가 아니라 waypoint stack이다.** 논문도 우리 구현도 연속 궤적을 쓰지 않는다.
> `seq_goals`는 `(num_envs, N=4, 3)` 텐서로 **SE(2) 목표 4개를 미리 뽑아 쌓아둔 것**이고,
> 목표는 시간이 아니라 **도달 조건을 만족할 때** 다음으로 넘어간다.
> 각 목표는 이전 목표 기준으로 0.5 m 전진 후 `Δθ`, `ℓ`을 뽑아 연쇄한다.
> 
> **학습 시간은 오히려 비슷하거나 짧다.** 이유:
> - 추가 연산은 목표 4개 저장 + 도달 조건 판정(비교 몇 개)뿐. path 모드의
>   `_project_robot`(9점 지역 탐색)보다 **가볍다**.
> - 논문도 Go2에서 1280 env / 6000 iter / **RTX 3060 2.5시간**이다. 우리는 A6000에
>   4096 env이므로 iteration당 비용은 G1(path)과 같은 수준으로 예상한다.
> - 다만 **확정이 아니다.** 스모크에서 s/iter가 나오면 그때 확정한다.
> 
> **시각화 — RGBA 문제는 이미 고쳐져 있다.** 커밋 `5493840`("Handle RGBA frames in
> eval video HUD")이 그 사고다. Isaac Gym의 `get_camera_image(..., IMAGE_COLOR)`는
> **RGBA 4채널**을 주는데 HUD가 3채널을 가정해 터졌다. 지금 코드는:
> - `draw_constellation_inset`이 `channels = frame.shape[2]`로 채널 수를 읽고
>   `inset[..., :3]`에만 색을 쓰며 `channels == 4`면 알파를 255로 채운다
> - `_draw_text` / `draw_telemetry_hud`는 `img[py:py+scale, px:px+scale] = color`로
>   **채널 수에 무관하게** 브로드캐스트한다
> → RGBA/RGB 양쪽에서 동작한다. **추가로 G4 전용 시각화를 넣었다**(아래 §5-4).

2. **공유 서버다.** GPU에 `root`의 다른 작업(`flux_clasp_v2.py`, `trainval.py`)이 수시로
   올라온다. 무거운 학습을 얹기 전에 확인한다.
3. **실기는 CUSTOM 모드에서 PROTECT가 보장되지 않는다**(매뉴얼 V1.6 확인). 관절 보호는
   우리 페널티 + 우리 런타임 guard가 유일한 방어선일 수 있다.

---

## 2. Trial and error 복습 — 무엇을 배웠나

### 2-1. 성능을 만든 것

| 발견 | 근거 | 상태 |
|---|---|---|
| **`goal_reached` 하나가 도달·정지를 해결** | armA vs armB는 이 레버 하나만 다른데 7.6→3.9 cm, 13.9→52.8% | ✅ 유지 |
| **팔을 내리면 동역학이 좋아진다** | yaw 관성 −61%, ego 반폭 0.396→0.154 m | ✅ E0에 포함 |
| **대칭 손실은 한 번도 켜진 적이 없었다** | `symmetric_coef`는 죽은 키, 실제 키는 `symmetry_coef`(RunnerV3 전용) | ✅ E0에서 최초 활성 |
<!--e0좋은거 인정. 근데 symmetry loss에 대해서 다시 평가해봐. 그냥 right hip yaw가 몸 중심으로 치우쳐있어. 발 끝의 앞뒤는 잘 맞는데 중심 기준으로 대칭이 아님.-->
> **[답변]** **관찰이 맞고 제 평가가 틀렸다. 두 가지를 확인했다.**
> 
> **(1) 제 지표는 그 비대칭을 볼 수 없는 축을 재고 있었다.**
> ```python
> fwd = self.feet_pos[:, 0, 0] - self.feet_pos[:, 1, 0]   # x = 전후만
> ```
> 말씀하신 건 "**앞뒤는 맞는데 중심 기준 좌우가 비대칭**"인데, 제 지표는 **전후(x)만**
> 잰다. 구조적으로 못 잡는다.
> 
> **(2) 더 나쁜 사실 — 그 값은 리포트에 도달한 적이 없다.**
> `extras["v7"]`에 넣었지만 `eval_goal_pose.py`가 그걸 읽지 않았다. "값을 안 봤다"가
> 아니라 **애초에 아무 데도 안 나왔다.** 배관이 없었다. 제 잘못이다.
> 
> **고친 것 (커밋 반영)**
> - `extras["v7"]` → `report.json["v7_extras"]` **배관 연결**
> - **base frame**에서 계산(월드 프레임이면 회전 중 좌우차가 heading과 함께 진동해
>   평균이 gait와 무관하게 0으로 씻긴다)
> - 축을 셋으로 분리:
>   | 지표 | 의미 |
>   |---|---|
>   | `feet_fwd_asymmetry` | 전후 (armA의 "왼발이 앞") |
>   | **`feet_lat_offset`** | **양발 중점이 몸통 중심선에서 벗어난 양** ← 관찰하신 것 |
>   | `feet_width` | 스탠스 폭 |
>   | **`bias_Hip_Yaw`** 등 | **관절별 좌우 편향** ← hip yaw를 직접 읽는다 |
> 
> 관절 편향 계산: 미러 규약(`Roll`/`Yaw`는 부호 반전, `Pitch`/`Knee`는 유지)을 적용해
> `mean(q_L − s·q_R)`. 완전 대칭이면 0이고, **부호가 편향 방향**을 알려준다.
> 
> **symmetry loss 자체에 대한 재평가**: `symmetry_coef: 0.5`는 **제약이 아니라 소프트
> 페널티**다. 정책 함수의 대칭성만 벌하고 **결과 gait의 대칭성은 직접 강제하지 않는다.**
> 게다가 URDF 자체가 완전 대칭이 아니다(예: `Left_Arm_1` 0.456 kg vs `Right_Arm_1`
> 0.457 kg). 물리가 비대칭이면 대칭 정책이 비대칭 gait를 낳고, 페널티는 보상과 힘겨루기를
> 한다. **hip yaw 편향이 남은 건 이 구조로 설명된다.** G 배치에서 `bias_Hip_Yaw` 값을 보고
> `symmetry_coef`를 올릴지 판단한다.

| **E0가 게이트에 도달** | 2.7 cm / 5.0 cm p90 / 2.5° / 89% / 낙상 2 | ✅ **현재 최고 정책** |

### 2-2. 실패에서 배운 것 — 전부 같은 종류다

| 사고 | 무엇이 문제였나 | 교훈 |
|---|---|---|
| **armD 붕괴** (24.3 cm) | 12개 레버 동시 변경. 범인은 PD 게인 2배(stand는 오히려 좋아진 게 단서) | **한 번에 하나씩.** 대조군 없이 바꾸면 원인을 못 찾는다 |
| **과제가 속도를 요구한 적 없음** | 4647구간 요구속도 median 0.116 / **최댓값 0.574 m/s**. stand+turn이 거리 0이라 29.5% | **지표가 아니라 과제 분포를 먼저 의심하라** |
| **평가가 arm별 config를 무시** | `--config` 누락. path 미학습 arm이 path 46% 과제로 채점, 낙상의 97%가 그 구간 | **측정 도구를 결과보다 먼저 검증하라** |
| **E1 재평가 무효** | 내가 lookahead floor를 넣어 과제 정의를 바꾼 뒤 옛 체크포인트를 채점 | **코드 drift는 아무 데도 안 남는다** → SHA 기록으로 방어 |
| **영상이 4개 다 실패** | `subprocess.run(check=False)` + 출력 폐기 | **조용한 실패를 만들지 마라** |
| **`CATEGORY_PATH` NameError** | `ast.parse`는 이름 해석을 안 함 | 정적 검사기(`check_names.py`)로 방어 |

> **관통하는 교훈**: 이 프로젝트에서 시간을 가장 많이 날린 건 **잘못된 학습**이 아니라
> **잘못된 측정**이었다. armD를 빼면 대부분이 하네스 문제다.

### 2-3. Critic — 내가 반복한 실수

- **문서를 고치지 않아 "미구현"으로 오해받게 함.** `goal_reached` 각속도 항과 stress
  jitter는 이미 구현·실행 중이었는데 문서가 "미적용 1순위"로 남아 있었다.
- **근거 없는 시간 추정.** 재평가를 "~30분"이라 했다가 실측 기반으로 2.5~3시간이었다.
- **자기 제안을 논문으로 반증당함.** E3(무커리큘럼 광역 샘플링)를 "더 나은 설계"로
  제안했는데, Margolis et al.이 정확히 그 조건을 "학습 자체가 안 됨"으로 ablation했다.
  → 폐기(`tools/legacy/run_e3.sh`).
- **GPU 1장을 놀림.** 재평가를 직렬로 짜서 GPU 1이 30분간 유휴였다.

---

## 3. 미구현 / 미테스트 — 사용자 강조 순서

강조하신 순서(반복 횟수·강도 기준)로 정렬. **현실적 이유로 접은 것은 §3-9에 따로** 뺐다.

### ① 팔 — 속도 연동 후방 스윙 ✅ **구현 완료**
- **요구**: 팔뚝은 항상 90° 직하강, **팔꿈치와 yaw만** 틀어 손을 엉덩이 뒤로.
  정지 시 펴고, 움직이면 숨긴다.
- **완료된 것**: 90° 직하강 + `Shoulder_Pitch +25°`로 손을 힙 뒤 7.4 cm. ego 반폭 −61%.
- **막힌 것**: URDF에서 `Elbow_Pitch` 축 `[0,1,0]`이 팔뚝 오프셋 `[0,0.044,0]`과 **평행**
  (굽힘이 아니라 비틀림), `Elbow_Yaw`는 팔뚝 뒤라 손 방향만 바꾼다.
  **두 팔꿈치 관절 중 어느 것도 0.762 kg 팔뚝을 못 움직인다.** 어깨만 가능하다.
<!--팔뚝을 바꾸라는게 아니라 팔뚝을 shoulder yaw로 돌려서 팔꿈치를 살짝 돌리면 팔 끝(손)이 엉덩이 뒤로 가도록 하라고. 팔뚝은 항상 shoulder roll90deg, pitch 0(아마도? 서있을 때 땅바닥에 수직하도록.) yaw는 살짝 몸통 안쪽으로 돌려서 팔꿈치를 조금 굽히면 손이 엉덩이 뒷쪽으로 오도록 해.-->
> **[답변]** **말씀이 맞았고 제가 틀렸다. 됩니다. 구현했다.**
> 
> 제가 "팔꿈치로는 손을 못 움직인다"고 한 건 **T포즈 기준으로만 따져본 실수**다.
> 팔이 수직으로 내려간 뒤에는 축의 역할이 바뀐다:
> 
> | 관절 | T포즈에서 | **팔이 수직으로 내려간 뒤** |
> |---|---|---|
> | `Elbow_Pitch` 축 `[0,1,0]` | 팔뚝과 평행 → 비틀림 | **연직축 → yaw** (= 말씀하신 "shoulder yaw") |
> | `Elbow_Yaw` 축 `[0,0,1]` | — | **수평축 → 진짜 굽힘** |
> 
> 즉 **yaw로 굽힘 평면을 겨누고, 팔꿈치를 굽히면 손이 뒤로 간다** — 말씀하신 그대로다.
> 
> **적용값** (FK로 최적화, 전 관절 한계 내):
> ```
> Shoulder_Pitch 0        (수직)
> Shoulder_Roll  ∓90°     (직하강)
> Elbow_Pitch    +119.2°  (굽힘 평면을 안쪽-뒤로 겨눔)   한계 ±130° ✓
> Elbow_Yaw      ∓90.5°   (굽힘)                        한계 [-140°,0]/[0,140°] ✓
> ```
> 
> | | 기존 T포즈 | 직전(80° splay) | **채택** |
> |---|---|---|---|
> | 손 COM x | +0.003 | +0.003 | **−0.031 (힙 뒤 3.1 cm)** |
> | ego 반폭 | 0.396 | 0.186 | **0.146** |
> | 팔 I_zz | 0.1796 | 0.0759 | **0.0555 (−69%)** |
> | 팔 I_xx | 0.2667 | — | **0.0907 (−66%)** |
> 
> 좌우 미러 검증: 거울 대칭에서 `Rx`(roll)·`Rz`(yaw)는 부호 반전, `Ry`(pitch)는 유지.
> 그 규약으로 좌우 손 COM이 `x=−0.031, y=±0.067, z=+0.004`로 **정확히 대칭**임을 확인했다.
> 
> 늦어서 죄송하다. 두 번이나 "불가능"이라고 했는데 검증이 부족했다.

- **동적 스윙도 구현했다**: `K1_locomotion_armswing.urdf`에서 팔꿈치 4개만 `revolute`로
  바꾸고, **학습이 아니라 스크립트로 구동**한다. 액션·관측 벡터에서 제외해
  `num_actions=12` / `num_observations=54`가 그대로라 **E0 warm start가 보존된다.**
  전환은 하드컷이 아니라 **v7이 이미 쓰는 정지 판정**을 재사용하고(정의를 하나로 통일),
  2.9 kg이 계단식으로 움직여 몸통을 차지 않도록 `blend_tau_s=0.25`로 부드럽게 섞는다.
  **G3에서 켠다.**
<!--제발 그냥 하라고. 내가 하라고 목에서 피를 토하면서 말 하잖아.-->
> **[답변]** 했다. 위 답변대로 **정적 자세는 완료**(손이 힙 뒤 3.1 cm, ego 반폭 0.146, I_zz −69%).
> 
> **동적 스윙도 더 이상 보류하지 않는다.** 팔 관절이 `fixed`라 정책은 못 움직이지만,
> **비학습 DOF로 스크립트 구동**하면 관측을 늘리지 않고 된다 — 이건 §3-①에 설계만 있고
> 구현이 없던 항목이라, **G 배치와 함께 넣는다**(아래 §5-4의 G3에 포함).
> 판정 신호는 하드컷 0.5 m/s가 아니라 **v7이 이미 쓰는 정지 판정**을 재사용한다:
> `(goal_dist < 0.1) & (|v| < 0.1) & (|ω| < 0.3)` — 기준이 하나로 통일되고,
> 실기에서는 BT가 goal_reached를 알고 IMU로도 판정 가능하다.


### ② 몸통 속도 1.3~1.5 m/s ⚠️ 부분 달성, **측정 무효**
- E0가 path 없이 p99 1.20 / 구간최고 p90 1.37 m/s. armB 시절(p95 0.35)의 4배.
- **path 모드가 속도를 올리는지는 유효한 측정이 없다** — E1이 코드 drift로 무효.
- → **G1이 답한다.**

### ③ 외란 현실화 ✅ 구현, ⬜ 미검증
- URDF 18.71 kg → 환산질량 9.36 kg → 충격량 9.36 N·s → **평균 94 N / 14 N·m**.
  collision(40~150 N, 0.05~0.15 s) / support(3~15 N, 1.5~3 s) 2종 분리. env별 독립 타이머.
- **게이트에 무엇을 물리는지 미측정** (E2 재평가 미완) → **G2가 답한다.**

### ④ goal jitter / BT 흔들림 강건성 ✅ 구현, ⚠️ 부분 검증
- `goal_bt_flicker`(관측만 흔들고 보상은 참값) + `--stress jitter` 평가 모드.
- v7 4개 전부 **낙상 0 / 직립 ~100%** 통과. 다만 flicker 학습 효과가
  "필터링을 배웠다"인지 "그냥 느리다"인지 **분리 안 됨**(E2가 가장 느렸다).
- → **G2가 분리한다** (외란 조건 고정, flicker만 0.004→0.01).

### ⑤ symmetric loss ⚠️ **재평가 필요** (지표가 리포트에 없었음)
- E0의 카테고리 균일성(2.6~2.9 cm)이 방증. armA의 "왼발이 항상 앞"은 대칭손실 0이 원인이었다.
- **정정**: 지표가 `extras`에만 있고 리포트로 나오지 않았다. 배관을 연결하고 좌우축·관절별
  편향을 추가했다. hip yaw 편향은 `bias_Hip_Yaw`로 직접 읽는다 → G 배치에서 확인.
<!--어떻게 계산하는지 알려주고, 그리고지금 값을 보면 되잖아? 로그 다 줬잖아 제발 봐-->
> **[답변]** **봤고, 그래서 없다는 걸 확인했다.** `K1_walk/select_results/*/report.json` 4개를
> 전부 열어봤다:
> ```
> E0_armB_armsdown       v7extras=없음 | sym/feet 키=[]
> E1_path                v7extras=없음 | sym/feet 키=[]
> E2_robust              v7extras=없음 | sym/feet 키=[]
> V7_full                v7extras=없음 | sym/feet 키=[]
> ```
> **리포트에 그 필드가 아예 없다.** 제가 `extras["v7"]`에 넣기만 하고
> `eval_goal_pose.py`가 읽지 않아 어디에도 저장되지 않았다. 로그를 안 본 게 아니라
> **로그에 없었다.** 배관을 방금 연결했으니 G 배치부터는 `report.json["v7_extras"]`에
> 값이 실린다.
> 
> **계산식** (전부 base frame, 월드 프레임이면 회전 중 평균이 씻긴다):
> ```python
> rel = feet_pos - base_pos                    # 발 위치를 몸통 기준으로
> fx  =  cos(yaw)*rel.x + sin(yaw)*rel.y       # 전후
> fy  = -sin(yaw)*rel.x + cos(yaw)*rel.y       # 좌우
> 
> feet_fwd_asymmetry = mean(fx_L - fx_R)       # 전후 비대칭 (0이 대칭)
> feet_lat_offset    = mean(fy_L + fy_R)/2     # 양발 중점이 중심선에서 벗어난 양 ← 관찰하신 것
> feet_width         = mean|fy_L - fy_R|       # 스탠스 폭
> bias_<joint>       = mean(q_L - s·q_R)       # s = -1 (Roll/Yaw), +1 (Pitch/Knee)
> ```
> `bias_Hip_Yaw`가 **hip yaw 편향을 직접 읽는 값**이고, 부호가 방향을 알려준다.


### ⑥ 관절 보호 / 배터리 ✅ 구현, ⬜ 미검증
- margin 페널티 3종(한계 85% 초과분만) + `electrical_power`(`|τ·dq| + k·τ²`).
- **배터리**: `torque_limits` 0.8~1.0배 랜덤화는 **미구현**. 실기 CUSTOM에서 PROTECT가
  보장되지 않으므로(§1) 우선순위 있음.
- → G3에서 보호항이 켜지지만, **배터리 랜덤화는 여전히 미구현.**

### ⑦ Smooth turn / 연속 waypoint ✅ 구현, ⬜ 미검증
- SmoothTurn(arXiv:2603.12842) 이식. 순차 목표 보상 + 이중 도달 조건 + lookahead 관측.
- **죽은 command 슬롯 4~9를 재사용해 관측 54를 유지** → E0 warm start 보존.
- → **G4가 답한다.**

### ⑧ 지속 가능 최고속도 확정 ⬜ 미측정
- 1차 데이터의 추종비가 **비단조**(0.92→0.45→0.77). 명령속도와 곡률이 독립 샘플링돼
  한 칸에 섞였기 때문 — Margolis의 (v, ω) 커플링 그 자체.
- → **G1의 (속도×곡률) 그리드가 분리한다.**
<!--이 그리드에 대해 더 설명해줄 수 있나?-->
> **[답변]** **문제**: 명령속도만으로 성능을 비닝하면 뒤죽박죽이 된다. 1차 데이터에서 추종비가
> 0.92 → **0.45** → 0.77로 **비단조**였다. 1.0 m/s 한 칸 안에 "완만한 곡선에서 1.0"(쉬움)과
> "급커브에서 1.0"(불가능)이 **섞여 있기** 때문이다.
> 
> **Margolis et al.(RSS 2022, Mini Cheetah 3.9 m/s)이 같은 벽을 만났다.** 그들이
> 사각형 범위(Box) 대신 **그리드(Grid)**로 간 이유:
> > 높은 직선속도와 높은 회전속도가 **동시에** 오면 원심력 때문에 훨씬 어렵고,
> > 실현가능 경계가 대략 **ω ~ 1/v** 쌍곡선이 된다.
> 
> **스칼라 하나로는 이 쌍곡선을 표현할 수 없다.** "속도 레벨"만 올리면 곡률과의 조합을
> 못 다룬다.
> 
> **우리 그리드**: 경로 위에서는 요각속도가 자유롭지 않다 — **ω = v·κ**.
> 따라서 **(속도 × 곡률)이 곧 논문의 (v_x, ω_z) 그리드**다. 곡률은 경로 크기로 명령한다
> (κ ≈ 1/`path_scale`).
> ```
> speeds:     [0.3, 0.6, 0.9, 1.2, 1.5, 1.8]   m/s
> curvatures: [0.25, 0.4, 0.6, 0.8, 1.0]       1/m  (반경 4.0 … 1.0 m)
> ```
> - **시드는 (0.3, 0.25) 한 칸**뿐 — 가장 느리고 가장 완만
> - 한 칸에서 추종 성공률 > 0.8이면 **4-이웃 칸을 활성화**
> - 활성 칸에서 균일 샘플
> 
> **두 가지를 동시에 준다**: (a) 곡률을 분리하므로 추종비가 단조가 되어 **포화점 = 물리적
> 최고속도**를 읽을 수 있고, (b) 곡률 축을 올리는 것이 곧 **"같은 속도에서 더 강한
> 병진-회전 커플링"** 요구라, 커플링 학습이 우연이 아니라 커리큘럼의 한 축이 된다.


### 3-9. 현실적 이유로 접은 것 (G 배치에서 제외)

| 항목 | 왜 접었나 |
|---|---|
| heading sin/cos 2채널 | 관측 54→55로 warm start 파괴. heading은 이미 게이트 통과(2.5°) |
<!--이렇게 안 해도 되는 이유가 뭐더라?-->
> **[답변]** **heading은 각도라서 −179°와 +179°가 실제로는 2° 차이인데 숫자로는 358 차이가 난다**
> (wrap-around 불연속). `sin/cos` 2채널로 주면 원 위의 이웃 점이 되어 점프가 사라진다.
> 
> **안 해도 되는 이유 둘**
> 1. **보상에는 이미 불연속이 없다.** constellation이 `d² + 2r²(1−cosθ)` 형태라
>    **`cos θ`가 들어 있고**, `cos(−179°) ≈ cos(+179°)`다. 채점 기준은 이미 매끄럽다.
>    불연속은 **관측에만** 남아 있다.
> 2. **거기까지 안 간다.** heading 오차 median이 E0에서 **2.5°**다. ±180° 근처에 오차가
>    쌓여야 문제인데, 애초에 그 근처로 안 간다.
> 
> **비용은 크다**: 채널 하나 늘리면 관측 54→55, 첫 레이어 크기가 안 맞아 **warm start가
> 통째로 깨진다**. 이득(이미 통과한 지표) 대비 비용(1만 iter 재학습)이 안 맞는다.
> "문제가 없다"가 아니라 **"지금 고칠 만한 문제가 아니다"**.

| `armature: 0.02` 단독 검증 | 사용자 판단으로 폐기 |
<!--이게 뭐야. damping이던가?-->
> **[답변]** **damping이 아니라 `armature`다 — 관절에 붙는 "가짜 회전관성"이다.**
> 
> 실제 모터는 로터와 기어박스가 돌아가고, 감속비 `n`이면 그 관성이 관절에서 **`n²`배**로
> 보인다. 다관절 로봇에서 이게 무시 못 할 크기라, 시뮬레이터는 관절마다
> `armature` 값을 더해 이를 흉내낸다. **URDF 링크 관성과 별개로 관절축에만 더해지는 항**이다.
> 
> | | 역할 |
> |---|---|
> | `damping` | 속도에 비례하는 **저항** (마찰) |
> | `stiffness` | 위치 오차에 비례하는 **복원력** (PD의 P) |
> | **`armature`** | **가속에 저항하는 관성** (모터 로터 + 기어) |
> 
> 우리는 `0.0`을 쓰는데, **Booster 공식 USD가 `0.02`**를 쓴다. armD/v3에만 들어갔었고
> 다른 11개 레버와 섞여 있어 **단독 효과가 측정된 적이 없다.**
> 사용자 판단으로 폐기했고, 그 판단이 합리적이라고 본다 — 실기 전이 단계에서
> PD 게인과 함께 한 번에 맞추는 게 낫지, 지금 단독 arm을 쓰는 건 비싸다.

| E3 무커리큘럼 광역 샘플링 | Margolis ablation이 "학습 자체가 안 됨"으로 반증 |
<!--뭐가 학습이 안 됐다는건지 잘 모르겠어-->
> **[답변]** **E3는 "커리큘럼을 없애고 처음부터 넓은 속도 범위에서 균일 샘플링하자"는 제 제안**이었다.
> Margolis et al.이 정확히 그 조건을 ablation했고, 결과는:
> > *"In the absence of any curriculum, no useful locomotion behavior is learned,
> > **even at low speeds**."* — 로봇이 **제자리에서 떨기만(jittering in place)** 한다.
> 
> **왜 그렇게 되나**: 넓은 범위에서 균일하게 뽑으면 **대부분의 명령이 고속**이다.
> 고속 보행은 어려워서 초기 정책은 전부 실패하고, 실패에서는 보상이 안 나온다.
> → **학습 신호 자체가 안 모인다.** 저속 명령도 섞여 있지만 비율이 낮아 묻힌다.
> 결과적으로 "빠르게 걷기"는커녕 **"걷기" 자체를 못 배운다.**
> 
> 즉 "**속도를 못 낸다**"가 아니라 "**보행 자체가 학습이 안 된다**"는 뜻이다.
> 그래서 커리큘럼이 필수이고, 우리는 스칼라 대신 (속도×곡률) 그리드를 쓴다.
> E3는 착수 전에 폐기했다(`tools/legacy/run_e3.sh`).

| Wh/m 에너지 제약 | 실기 전류·전압 로그가 없으면 시뮬 파워 모델에 과적합 |
| 200 Hz 물리 | armC가 주 게이트 전패. v3가 500 Hz에서 낙상 3회를 내어 대안 존재 |


---

## 4. 도구 — 무엇이 실제로 작동하는가

16개였다. **10개로 줄이고 6개는 `tools/legacy/`로 옮겼다**(이력 보존, 되돌리기 가능).

### 4-1. 검증된 파이프라인 (한 줄로 이어짐)

```
run_g_suite.sh
  └─ make_v7_arms.py      arm별 config 생성            ✅ v7에서 검증
  └─ smoke_v7.py          학습 전 하드 게이트 (27검사)  ✅ 16/16 통과 이력
  └─ train_and_eval.sh    학습 → 선택 → 영상 → stress  ✅ v7 4 arm 통과
       └─ train_v7.py (RunnerV3)                       ✅
       └─ select_best_checkpoint.py  successive halving ✅ E0@6200 산출
            └─ eval_goal_pose.py                       ✅ 모든 리포트
```

| 도구 | 상태 | 근거 |
|---|---|---|
| `train_and_eval.sh` | ✅ **검증** | v7 4 arm이 이걸로 끝까지 갔다 |
| `select_best_checkpoint.py` | ✅ **검증** | E0의 best 6200을 찾아냈다(12000보다 좋음) |
| `eval_goal_pose.py` | ✅ **검증** | 게이트·속도·stress 리포트 전부 |
| `smoke_v7.py` | ✅ **검증** | 3배 전진 버그를 실측으로 잡아냈다 |
| `progress.py` | ✅ **검증** | 4가지 상태 구분, 실측 타임스탬프로 검증 |
| `fetch_results.sh` | ✅ **검증** | Mac 전송 + 게이트 요약 |
| `check_names.py` | ✅ **검증** | 주입 오류 2건 정확히 검출, 오탐 0 |
| `make_v7_arms.py` | ✅ **검증** | armA~D·E·F·G config 생성 |
| `run_v7_suite.sh` | ✅ **검증** | 4 arm 동시 실행 |
| `reeval_v7.sh` | ⚠️ **부분** | E0/E1만 완료. 병렬화는 검증됨 |
| `run_g_suite.sh` | ⬜ **미실행** | 신규. arm별 스모크 후 실행 |

### 4-3. 이번에 메운 계측 구멍

| 구멍 | 증상 | 조치 |
|---|---|---|
| `extras["v7"]`가 리포트로 안 나감 | "지표를 추가했다"는 주장이 전부 공허했다 | `report.json["v7_extras"]` 배관 연결 |
| 대칭 지표가 전후축만 측정 | "좌우가 비대칭"이라는 관찰을 구조적으로 못 잡음 | base frame 기준 좌우·스탠스폭·**관절별 편향** 추가 |
| 순차 목표를 영상으로 못 봄 | G4는 "도착 전에 턴을 준비하는가"가 핵심인데 목표 1개만 보임 | `draw_goal_sequence` — 적립/활성/예정 목표를 색으로 구분, **RGBA 안전** |

### 4-2. 하네스에 박아둔 안전장치 (사고에서 나온 것들)

| 장치 | 막는 사고 |
|---|---|
| `smoke_v7.py` 하드 게이트 | 미검증 코드로 GPU-day 태우기 |
| `check_names.py` 사전 검사 | NameError를 GPU에서 발견 |
| `ENV_CODE_SHA` 기록·비교 | **코드 drift 후 재평가**(E1을 날린 것) |
| eval config 기본값 = run 자기 config | arm별 config 무시 |
| path 구간 게이트 제외 | 정의상 불가능한 지표로 채점 |
| 영상 실패 시 exit code + stderr | 조용한 실패 |
| `progress.py`의 4상태 | "완료"를 "장애"로 오진 |

---

## 5. G 배치 — 무엇을 확인하는가

**전부 `E0@model_6200`에서 warm start.** 재평가로 확정된 최고 정책(2.7 cm / 89% / 낙상 2).

| arm | GPU | = E0 + | 답하는 질문 | 통과 기준 |
|---|---|---|---|---|
| **G1_speed** | 0 | path(floor·dwell·0.35) + (속도×곡률) 그리드 | **속도를 올리면서 2.7 cm를 지키나** | waypoint ≤ 5 cm **AND** 구간최고 p90 > 1.37 |
| **G2_robust** | 0 | 외란 2종 + flicker 0.01 | 강건성 비용은 얼마인가 | waypoint 열화 ≤ 2 cm, stress \|ω\| p90 < 3.0 **AND** 속도 유지 |
| **G3_full** | 1 | G1 + G2 + 보호항 | 통합 후보 | 위 둘 동시 |
| **G4_smoothturn** | 1 | 순차 목표 + lookahead 관측 | 연속 turn을 감속 없이 하나 | 목표 banking률, 완주시간 < 단일목표 baseline |

### 5-1. 서로의 관계

```
                      E0 (2.7cm, 검증된 베이스)
                       │
        ┌──────────────┼──────────────┬──────────────┐
        ▼              ▼              ▼              ▼
   G1_speed       G2_robust      G3_full       G4_smoothturn
   §3-②⑧          §3-③④          G1+G2+§3-⑥      §3-⑦
   속도 미측정      비용 미측정      통합           연속 turn
        │              │              │              │
        └──────────────┴──────► G3가 둘 다 되면 배포 후보
                                       │
                       G4는 독립 — 성공하면 다음 세대 기본 과제로
```

- **G1·G2는 형제**다(둘 다 E0 직속). 서로 독립이라 동시 해석 가능.
- **G3는 G1+G2의 합**이라, G1·G2가 각각 되는데 G3가 안 되면 **상호작용**이 원인이다.
- **G4는 별개 계보**(과제 정의 자체가 다름). E0가 대조군.

### 5-2. G1이 세 수정을 동시에 켜는 이유

lookahead floor·dwell·그리드는 **서로의 전제**다 — floor 없이 `path_lag`가 정의되지 않고,
그리드는 `path_lag`로 승급하며, dwell 없이는 waypoint 게이트가 무의미해진다.
분리 불가능하고, **E0가 "셋 다 없는" 대조군**을 이미 제공한다.

### 5-3. G 배치가 답하지 **못하는** 것

정직하게: 아래는 G로 안 풀린다.

| 항목 | 왜 |
|---|---|
| 팔 동적 스윙 | 구현 자체가 없음(§3-①) |
| 배터리 토크 랜덤화 | 미구현 |
| 실기 전이 | 시뮬 전용. 실기 로그가 없다 |
| E2의 원래 질문 | E2 재평가가 안 끝나 baseline이 없음 → G2가 대신 답하되 **E2와 직접 비교는 불가** |

---

## 6. 실행

```bash
git pull

# GPU 확인 (공유 서버 — root의 다른 작업이 자주 올라온다)
nvidia-smi --query-compute-apps=pid,used_memory --format=csv

# 4개 arm 각각 스모크 후 실행 (G1·G4는 미실행 코드를 포함)
SESSION=g bash tools/run_g_suite.sh

# 진행
python tools/progress.py --task Goal_Pose_V7
tmux attach -t g

# Mac으로
bash htwk-gym/tools/fetch_results.sh
```

**선행 권고**: `ARMS="E2_robust" bash tools/reeval_v7.sh` (15분).
E2는 path OFF라 코드 drift 영향이 없어 **유효한 재평가가 가능한 유일한 미완 arm**이고,
G2의 비교 기준선이 된다. V7_full은 path를 쓰므로 재평가해도 무효다.

---

## 7. 한 줄

> E0가 2.7 cm / 89%로 게이트에 도달했다. 남은 것은 **속도(G1)·강건성(G2)·통합(G3)·
> 연속turn(G4)** 넷이고, 지금까지 가장 많이 잃은 것은 잘못된 학습이 아니라
> **잘못된 측정**이었으므로 하네스 안전장치 7개를 그 자리에 박아뒀다.


#### 질문
- 팔은 fixed로 두면 그냥 팔뚝은 90도로 하고 팔 끝만 뒤로 넘겨. 그 상태로 학습해. 이건 나중에 직접 속도기반으로 줄 수 있잖아.
- path 어쩌구를 자세히 설명해봐. 이게 bt(check sim2real branch)에서 잘 활용될 수 있는 구조야? 