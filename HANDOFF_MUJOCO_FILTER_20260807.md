# 인계 — MuJoCo로 실기 붕괴를 재현하고 수정으로 지웠다 (2026-08-07 04:00~07:00)

> 상세 근거는 `ibatch.md` **§8-45 / §8-47**. 이 문서는 그 요약과, 다음 세션이
> 반복하지 않아야 할 방법론 오류를 남긴다.

---

## 1. 한 줄

**배포 필터가 발행 루프 속도에 의존해서 보행을 삼킨다. 수정 플래그는 이미 코드에 있고,
발행률 전 구간에서 안전하다. `--rate-fixed-filter`를 켜면 된다.**

---

## 2. 무엇이 문제인가 — `deploy_goal_pose.py:1430`

```python
self.filtered_dof_target = self.filtered_dof_target * 0.8 + self.dof_target * 0.2
```

정책이 50 Hz로 내는 관절 목표를 모터에 주기 전에 깎는 1차 저역통과다. **계수 0.2는
500 Hz 발행을 가정하고 고른 값**이라 시정수 10 ms를 의도한 것이다. 그런데 이 필터는
정책 tick이 아니라 **발행 루프**에서 돌고, 그 루프는 파이썬 + `time.sleep(0.001)`이다.

**계수는 상수인데 시정수는 상수가 아니다:**

| 발행 Hz | 시정수 | 차단주파수 | **2 Hz 보행 통과율** |
|---:|---:|---:|---:|
| 500 (설계) | 10 ms | 17.8 Hz | 0.99 |
| 100 | 44.8 ms | 3.55 Hz | 0.87 |
| 50 | **89.6 ms** | **1.78 Hz** | **0.66** |

보행이 2 Hz인데 차단이 1.78 Hz면 **보행 신호 자체가 걸린다.** 다리가 3분의 2만 나가고,
정책은 "덜 갔다"를 보고 더 크게 명령하며, 그 액션이 `obs[42:54]`로 되먹임돼
off-distribution으로 밀린다.

---

## 3. 증거 — MuJoCo, 실물 질량 자산, 배포된 정책 그대로

`I3b_stance10 model_200`, `K1_serial_realmass.xml`(19.666 kg), 120 s.

### 발행률 스윕 (goal_reach, 지연 0)

| 발행 Hz | 낙상/구간 | 도착 | 과제 cm |
|---:|---|---:|---:|
| 50 | **66/68** | 2 | 37.62 |
| 75 | 43/56 | 13 | 6.60 |
| 100 | 10/38 | 28 | 6.44 |
| 150 | 4/36 | 32 | 6.76 |
| 200 | **0/41** | 41 | 6.52 |
| 500 | **0/47** | 47 | 6.70 |

**50 Hz 행이 실기 증상 그 자체다** — 68구간 중 66회 낙상, 도착 2회. "세 걸음 가고 무너진다".

> **정확도는 6.4~6.8 cm로 내내 같다.** 필터는 정확도를 깎는 게 아니라 넘어뜨린다.
> sim 지표를 아무리 봐도 안 보였던 이유가 이것이다.

### 수정이 전 구간을 덮는다

연속보행 120 s + 관측지연 20 ms + IMU 잡음 0.5°, seed 3개:

| 발행 Hz | 현재 (계수 0.2) | **수정 (시정수 10 ms 고정)** |
|---:|---|---|
| 150 | 0 / 6 / 0 | **0 / 0 / 0** |
| **200** | **31 / 29 / 24** | **0 / 0 / 0** |
| 500 | 0 / 0 / 0 | — |

**수정본은 50·150·200·500 Hz 전부 낙상 0이다.**

---

## 4. ⛔ 그래서 R6(`pub_hz` 실측)은 수정을 적용할 것이라면 불필요하다

`--rate-fixed-filter`는 **어느 발행률에서도 안전**하고, 500 Hz에서는 켜나 안 켜나
결과가 같다(과제 6.70 대 6.78, 낙상 0/0). **손해가 없다.**

```bash
python deploy/deploy_goal_pose.py --config Goal_Pose_E0.yaml \
    --rate-fixed-filter --log-timing /tmp/t.csv
```

플래그는 `deploy_goal_pose.py:1498`에 이미 있다(`--filter-tau-ms` 기본 10).
`pub_hz`도 이미 로깅 컬럼이다(`:457`, `:1298`). **코드 작업 0.**

### 서 있는 상태로 할 수 있는 것 / 없는 것

| | 서 있기로 가능? |
|---|---|
| **필터 버그 확인** | ❌ **불가능** |
| `pub_hz` 측정 | ✅ 가능, 안전 |
| 영점 틀어짐 역산 | ✅ 가능 |
| 정지 떨림 | ✅ 가능 |

**실측으로 확인했다.** 서 있기 60 s, 필터만 50 Hz 대 500 Hz:

| 발행 Hz | 낙상 | tilt med | 관절속도 rms | 관절 이동폭 |
|---:|---:|---:|---:|---:|
| 50 (걸으면 66/68 낙상) | 0 | 0.562° | 0.0490 | 0.2046 |
| 500 | 0 | 0.576° | 0.0458 | 0.2052 |

**구별이 안 된다.** 저역통과는 상수 신호를 그대로 통과시키므로, 목표가 안 변하는
서 있기에서는 깎을 것이 없다. **그리고 이것이 "서 있기는 완벽한데 세 걸음에서
무너진다"는 실기 signature와 정확히 맞는다.**

→ **안전한 순서**: 세워둔 채 `--log-timing`으로 `pub_hz`를 먼저 읽고, 그 다음 걷기 시험.

---

## 5. 같이 기각된 가설

| 가설 | 기각 근거 |
|---|---|
| **질량·관성 오차** | §8-39는 `K1_serial.xml`(**18.7142 kg = 학습 자산과 같은 잘못된 값**)로 돌았다. 실물(19.666 kg / 발 +29 % / Ixx +14.2 % / Izz +19.3 % / CoM −10.8 mm)로 재실행: 과제 **6.73 → 6.70 cm**, 낙상 0 → 0. **차이 없음** |
| **떨림/채터링** | 무릎 토크 부호 반전이 22.9회/초라 채터링처럼 보이나, 전 관절 스펙트럼 최대가 **보행 주파수(1.8~2.0 Hz)** 자체이고 **Nyquist(25 Hz) 대역은 0.3~1.8 %**. 관절속도 10 Hz 초과 성분도 3.4~7.4 % |
| **관측 지연 단독** | 수정 필터를 켜면 지연 20 ms가 있어도 200 Hz에서 낙상 0. 200 Hz 이상 현상은 지연이 아니라 **고정 계수 0.2**가 만든 것 |

§8-39가 이미 기각한 것(Isaac 특유 물리, 토크 상한, 배포 필터 *존재* 자체)에 셋이 추가됐다.

---

## 6. ⚠️ 남은 이상 현상 — 설명 못 함

관측 지연 20 ms를 얹으면 **발행률과 낙상이 단조가 아니다**: 150 Hz는 median 0,
**200 Hz는 median 29**(분당 14.5회). 위상으로는 설명이 안 된다 — 200 Hz 쪽이 총 지연이
**더 작다**(30° 대 35°).

**수정 필터가 이것을 지우므로 실기 권고에는 영향이 없다.** 다만 원인 미상으로 남는다.
파고 싶으면 175/200/225/250 × 지연 0/10/20/30을 채워 지도를 그리는 것이 다음 수다.

---

## 7. ⛔ 내가 낸 방법론 오류 셋 — 반복 금지

### ① `--seed`가 있다고 변동이 생긴다고 가정했다

연속보행 결과가 비단조라 "분산"이라 보고 seed를 늘렸는데 **50/50/50, 0/0/0, 25/25/25로
전부 같았다.** `--goal-hold`는 목표가 고정이고 노이즈를 0으로 두면 **완전히 결정론적**이다.
seed를 바꿔도 같은 실행이다.

→ **변동을 만들려면 노이즈를 켜야 한다.** 켜고 다시 재니 순서가 유지됐고, 그제서야
"실재하는 비단조"라고 말할 수 있었다.

### ② 요약통계 하나로 떨림을 판정하려 했다

`flips_per_s`(토크 부호 반전율)만 보고 채터링이라 읽을 뻔했다. **스펙트럼을 봐야 한다.**
조화성분이 많은 파형은 영점을 자주 지난다 — 그것은 떨림이 아니다.

### ③ 문턱을 성급히 일반화했다

지연 0 조건에서 "≥200 Hz면 필터 무죄"라고 적었다가, 지연 20 ms를 넣으니 200 Hz에서
분당 14.5회가 나와 **철회했다.** 한 축에서 얻은 문턱을 다른 축이 있는 상황에 그대로
쓰면 안 된다.

---

## 8. 부수 발견 — 영상에서 발이 잘못 보였다 (물리는 무관)

발에 geom이 둘이다: 상세 mesh(`group=1`, `contype=0` — 시각 전용)와 충돌
box(`group` 미지정 = 0). MuJoCo가 둘 다 그려 **box가 mesh를 덮어** 납작한 슬래브로 보였다.
형상 자체는 맞다:

| | 길이 | 폭 |
|---|---|---|
| 충돌 box | 16.0 cm | 7.0 cm |
| 시각 mesh (STL 실측) | 16.5 cm | 7.8 cm |
| 학습 `feet_edge_pos` 교정본 | **16.0 cm** | **7.0 cm** |

T1 자산도 아니다(T1은 `AL1.STL`/`Ankle_Cross_Left.STL` 계열로 발 파일을 공유하지 않는다).
렌더에서 group 0을 숨기게 고쳤다. **물리는 안 건드렸으므로 위 결과는 전부 유효하다.**

---

## 9. 디스크에 남은 것 (서버 `htwk-gym/logs/mujoco/`)

| 파일 | 내용 |
|---|---|
| `i3b_real_120s.{mp4,csv,json}` | 기준선 2분. 47구간 47도착 낙상 0 |
| `i3b_real_slow50.{mp4,csv,json}` | **50 Hz 재현 — 66/68 낙상** |
| `i3b_real_slow50_fixed.{mp4,csv,json}` | 같은 50 Hz + 수정 → 낙상 0 |
| `sweep_hz{60..300}.json` | 발행률 스윕 |
| `x_hz*_lag*.json` | goal_reach 2D (발행률 × 지연) |
| `gh_hz*_lag*[_s*].json` | 연속보행 2D + seed |
| `nz_hz*_s*.json` | 잡음 켠 seed 반복 |
| `fx_hz*_s*.json` | **수정 필터 검증** |
| `st_hz{50,500}.{csv,json}` | 서 있기 대조 |

CSV는 실기 `deploy --log-timing`과 **같은 컬럼**이라 두 로그를 같은 축에 겹칠 수 있다.

---

## 10. 다음 한 걸음

1. **로봇에서 `--rate-fixed-filter --log-timing`으로 한 번 돌린다.** 손해가 없고,
   유죄면 즉시 해결된다. 그 로그의 `pub_hz`가 원인을 사후 확정한다
2. 안 걸으면 → 남은 후보는 실기 상태추정/IMU 품질, 평행 발목 기구, 인조잔디 대 평면
3. 학습 쪽: 배포된 정책도 N0/N1도 `obs_delay_steps`가 `[0,0]`이다 — **관측 지연을
   한 번도 겪은 적이 없다.** §8-42가 그 축만 실기의 다리 교차를 재현했다고 기록했다

## 11. ACK

읽었으면 이 줄 아래에 세션명과 날짜를 적는다.

- (미기재)
