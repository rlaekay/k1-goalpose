# 로봇 세션 실행서 — 4발자국 판정

작성 2026-08-09. **이 문서 하나로 세션을 처음부터 끝까지 돌린다.**
왜 각 단계를 하는지는 [SIM2REAL_TRACKER.md](SIM2REAL_TRACKER.md), 판정 근거는 `ibatch` §8-69~§8-71.

> ✅ **선행 조건 둘 다 충족됐다(2026-08-09 저녁). 지금 §1 부터 실행할 수 있다.**
> - ✅ MJC 발목 사다리 → ⛔ ~~안전창 [15,20]/17~~ **철회**(하네스가 원을 그렸다).
>   직진 재측정에서 **15 가 최소**(4.2낙상), 20 이상은 명확히 나쁨 ⇒ **config = 15**
> - ✅ Teacher `I3b@vendor` Ankle_P 포화 **8.48 / 16.08 %** → 3 % 훨씬 위 ⇒ **두 세션 정합**,
>   박스 근거가 `I3b` 위에서 선다
>
> ⛔ **어제 이 10분이 안 돌아서 하루치 시뮬 분석이 대신 쌓였다**(분석 세션 지적).
> **로봇 실행이 대기 중이면 시뮬 분석의 한계효용은 0 으로 친다.**

---

## 0. 안전 — 매번 확인

- ⛔ **로봇을 매단 채로 `deploy_goal_pose.py` 를 돌리지 마라.** tilt>45°에서 `kDamping`
  (하네스 안에서 툭 떨어짐) → 공중에서 GetUp 이 돈다.
- **정지는 `touch /tmp/e0_abort`.** sshkeyboard 가 tty 를 잡으면 Ctrl-C·SIGINT·SIGTERM 이
  전부 안 먹는다. GIL 데드락이면 SIGKILL 만 듣는다.
- **반드시 `./run_e0.sh`** 로 실행한다. `python3 deploy_goal_pose.py` 직접 실행은 ROS 를
  안 source 해서 **FallMonitor 가 통째로 죽는다.**
- 무부하 측정은 **한 번에 한 관절**, 스톱에 닿으면 바로 놓기, 측정 뒤 보호 상태 확인.
- ⭐ **기록 항목에 "손 완전히 뗌" 과 영상을 넣는다.** §8-68 판정 셋이 이것 때문에 흔들렸다.

---

## 1. 재설치 — 현 HEAD 를 로봇에

로봇의 배포본은 **발목 게이트·토크 박스·시계 레버 이전 빌드**(`d883ed17…`)다.

```bash
cd ~/RoboCup/k1-goalpose && bash tools/install_policy.sh
```

**확인**: 설치 후 로봇 파일의 SHA-256 을 기록한다(시험 기록에 남긴다).
```bash
ssh booster@robot 'shasum -a 256 ~/Workspace/deploy/deploy_goal_pose.py'
```

---

## 2. 읽기 전용 30초 — 로봇에 아무 명령 안 함

```bash
python3 tools/read_joint_live.py --idx 15,21 --seconds 30 --csv /tmp/ankroll_hirate.csv
```

**닫히는 것 셋**:
1. **발목 채널 결함 확정** — 고율(≈400 Hz)에서도 도약이 남으면 채널 결함, 매끄러우면
   엘리어싱(§8-69 의 유일한 미해결분)
2. **콜백 `dt` 분포** — 게이트 예산 구멍(24 ms 지연 시 통과)의 실기 크기
3. ⭐ **4.72 ± 1 Hz 단일가설** — 발목 폐루프 고유진동수. §8-64 는 11~12 Hz 를 찾다 널을
   못 넘어 철회됐다. 사전등록하면 Bonferroni 가 채널 12 → **1** 이라 검정력이 자릿수로 오른다.
   ⛔ Lomb-Scargle(비등간격) 로 하고 귀무분포를 같이 낸다.

⚠️ 이 프로세스는 **구독 전용**이라 배포 프로세스의 `dt` 분포와 **다르다**(RETRACTIONS C16:
§8-41 의 499 Hz 가 그 실수였다). 배포 쪽 `dt` 는 §3 의 timing 로그에서 따로 나온다.

---

## 3. A 실행 — 30초 보행. **이것이 4발자국 판정이다**

```bash
cd ~/Workspace/deploy && PYTHONUNBUFFERED=1 ./run_e0.sh fixed 0.6,0,0 \
  --policy-path ./models/goal_pose_i3b.pt \
  --publish-hz 100 --rate-fixed-filter --filter-tau-ms 10 \
  --hold-diag 3 --max-policy-seconds 30 --test-tilt-abort-deg 25 \
  --log-timing /tmp/i3b_A_30s.csv
```
`b` → 진단 확인 → `r`. **뒤에서 받을 준비만 하고 손은 대지 않는다.**

⚠️ `fixed 0.6,0,0` 은 **로봇 로컬 프레임 상수**라 도달하지 않는다 = 연속 보행.

### 반드시 기록할 것

| 항목 | 어디서 | 왜 |
|---|---|---|
| ⭐⭐ **케이던스 = 총위상/총시간** | `gait_process` 열 | ⛔ **median 금지**(위상은 적분량). B 배율이 여기서 나오고, **학습 arm 두 개(`NT_cross`/`NU_cadence`)의 생사가 이 한 숫자에 달려 있다**(아래) |
| ⭐ **`tick_dt > 80 ms` 비율** | `tick_dt_s` 열 | **케이던스가 2.0 미만이면 원인은 이것 하나다**(아래 유도) |
| **연속 무낙상 시간** | 관찰 + 로그 | 판정축 자체 |
| **발목 `tau` p99** | `tau4`,`tau10` | ⭐ **박스가 실제로 걸렸는지** — 설정 **15** 근처면 정상. ⛔ ~~안전창 [15,20]~~ 은 **철회**(MJC 하네스가 원을 그렸다). 직진 재측정: 12→13.0 · **15→4.2(최소)** · 17→6.6 · 20→15.6 · 25→43.2 ⇒ **낮은 쪽이 낫고 20 이상은 나쁘다** |
| ⭐ **yaw 드리프트** | `heading_err` + 진행 방향 | MJC sim 에서 **개루프 yaw 편향 −0.2 rad/s**. 폐루프(실기)에서도 보이면 **폐루프로도 안 잡히는 편향** = 진짜 결함 |
| 게이트 카운터 | 종료 로그 `[dof-gate]` | 오탐률. 발목 roll 밖 채널이 0 이 아니면 신호 |
| 콜백 `dt` 히스토그램 | 종료 로그 | 예산 구멍 크기(배포 프로세스 값) |
| `--parallel-torque` 상태 | 명령줄 | **꺼짐**이어야 한다(박스가 그 역할을 대신한다) |
| `torque_box_limits` 실효값 | 시작 로그 `[torque-box]` | 무엇을 걸고 쟀는지 |
| **손 완전히 뗌 + 영상** | 사람 | §8-68 재발 방지 |

### 판정

| 결과 | 케이던스 | 읽는 법 |
|---|---|---|
| **30초 완주** | — | ⭐⭐ **목표 달성.** A~D 대부분 종결. §4 로 |
| 완주 | **≥1.578 Hz** | ⚠️ 시계 가설은 **못 가른다**(그 대역은 낙상 0). §4 로 |
| 조기 낙상 | **≥1.578 Hz** | ⭐ **시계 무죄** — 원인은 물리(B) 또는 과제(C). 트래커 5·6·7 로 |
| 조기 낙상 | ≤1.530 Hz | ⚠️ 시계와도 맞고 다른 것과도 맞음. §4 가 필요 |

⛔ **낙상 유무만 쓰지 마라.** 25.7 ms 대역은 시드 산포가 3.5배라 30초 1회로는 못 가른다.
케이던스와 **같이** 읽는다(아래 판정 대역 표).

### ⭐⭐ 케이던스가 학습 쪽 arm 두 개를 가른다 (Teacher 요청)

fresh 빌드의 위상 적분(`policy_goal_pose.py:205-219`)은 **벽시계**다:
```python
dt = min(max(time_now - self._last_time, 0.0), 4.0 * self.policy_interval)  # 클램프 80 ms
self.gait_process = fmod(self.gait_process + dt * self.gait_frequency, 1.0)
```
`_last_time` 이 매 호출 갱신되므로 **∑dt = 총 벽시계 시간**이고 ⇒ **케이던스 = 지령
2.0 Hz 가 정확히 나와야 한다. tick 이 느려도 상관없다.**

⛔ **2.0 미만이 나올 수 있는 경로는 `4×interval = 80 ms` 클램프 하나뿐이다.**
케이던스 1.58 이 나오려면 **벽시계의 21 %** 가 80 ms 초과 공백에서 잘려야 한다.

| A 케이던스 | 뜻 | 학습 쪽 결과 |
|---|---|---|
| **≈2.0** | 시계 버그가 정말 고쳐졌다 | ⛔ `NU_cadence` **불필요**. MJC 의 `NQ` 교차 약점(1.557 Hz 조건)은 **구 빌드 산물**이고 `feet_cross` 논쟁이 닫힌다 |
| **1.5~1.7** | **80 ms 초과 공백이 21 %** = 심각한 스톨 | ⭐ **배포 루프 문제다.** `tick_dt > 80 ms` 비율을 즉시 본다 |

⚠️ fresh 빌드가 정책률 **50.4 Hz**(=19.8 ms)를 실측했으므로 **≈2.0 이 강한 예측**이다.
⛔ **그리고 ≈2.0 이면 A 완주는 시계에 대해 아무것도 말하지 않는다** — 아래 판정 대역에서
**≥1.578 은 낙상 0** 이라 원래 안 넘어질 조건이다. 그래서 B 가 필요하다.

### 판정 대역 (MJC 직진 재측정, **로봇에 들어갈 config 그대로**: 발목 박스 15/무클램프)

| 케이던스 | 주기 | 낙상/120 s | 낙상간격 | **P(30초 무낙주)** |
|---:|---:|---:|---:|---:|
| ≥ **1.578** | ≤25.35 ms | **0.0** | ∞ | **1.000** ⛔ 검정력 0 |
| 1.557 | 25.7 | 4.2 | 28.6 s | 0.35 ⚠️ 경계 |
| **1.530** | 26.14 | **14.2** | 8.5 s | **0.029** ⭐ 결정적 |
| **1.512 (B 목표)** | — | 그보다 나쁨 | — | **< 0.03** ⭐⭐ |

⭐ **실기 두 실행이 이 곡선에서 정확히 갈린다**: 성공 **1.5813 = 무낙상 대역 안**,
붕괴 **1.5116 = 밖**. (옛 곡선에서는 성공도 낙상 대역이었다.) ⚠️ "모순 없음"이고 확증 아님.
⚠️ MJC 단서: 옛 곡선 대비 낙상이 절반인데 **과제(원→직진)와 발목값을 같이 바꿨다**
⇒ "직진이 쉽다"를 이 표로 말하면 안 된다. ✅ 단 25.7 ms 의 4.2 가 발목 사다리
`AnkP=15 → 4.2` 와 **독립 실행에서 정확히 일치**해 두 스윕이 서로를 검증한다.

---

## 4. B 실행 — 시계 가설. **A 를 읽고 배율을 정한다**

⛔ 레버가 **곱셈**이다: `B 케이던스 = A 케이던스 × scale`.

```
scale = 1.512 / (A 에서 잰 케이던스)
```
- A ≈ 2.00 → **0.756**  ·  A ≈ 1.58 → **0.957**
- **A ≈ 1.51 → B 불필요** (A 자체가 붕괴 조건이고, A 완주면 그것으로 시계 기각)

```bash
cd ~/Workspace/deploy && PYTHONUNBUFFERED=1 ./run_e0.sh fixed 0.6,0,0 \
  --policy-path ./models/goal_pose_i3b.pt \
  --publish-hz 100 --rate-fixed-filter --filter-tau-ms 10 \
  --gait-clock-scale <위 계산값> \
  --hold-diag 3 --max-policy-seconds 30 --test-tilt-abort-deg 25 \
  --log-timing /tmp/i3b_B_30s.csv
```

**판정**: A 완주 ∧ B 조기 낙상 ⇒ ⭐ **시계 확증** / 둘 다 완주 ⇒ **시계 기각** /
A 낙상 ⇒ 시계 밖 원인 잔존.

⛔ **`--gait-clock-scale 0.79` 를 쓰지 마라** — 그건 "9걸음 성공" 실행의 팽창(간격 ~70 s)
이라 30초 검정력이 없다. 0.756 이 붕괴 실행 재현이다.

⚠️ 여유가 있으면 **B2 = 0.79**(성공 재현)도 돌린다. **B2 완주 ∧ B1 조기 낙상 = 실기 두
실행을 로봇 위에서 재현**한 것이고, 시계 가설의 가장 강한 형태다.

---

## 5. 선택 — tau A/B (제자리 1초)

발행 필터의 남은 레버. `--filter-tau-ms 10` 대 `24.2`(구 빌드 등가), 나머지 고정.
A/B 가 끝나고 여유가 있을 때만.

---

## 6. 세션 끝나고

1. CSV 를 `realdata/` 로 옮기고 **커밋**한다(그 디렉터리는 `.gitignore` 밖이다).
   되찾을 수 없는 측정이다.
2. `ibatch` 에 판정 단위로 기록 — 사전등록한 판정표의 **어느 칸에 떨어졌는지**를 먼저 쓴다.
3. **결론이 바뀌면 Teacher·Analyst·MJC 에 그 자리에서 보낸다**(CLAUDE.md 규칙).
4. [SIM2REAL_TRACKER.md](SIM2REAL_TRACKER.md) 의 해당 블록을 갱신한다.

---

## 7. ⭐ 두 번째 트랙 — `NQ_armvendor` (§6 뒤, 여유가 있으면)

벤더 물리 채점에서 **`NQ` 가 `I3b` 보다 낙상 180배 적고 속도 12 % 빠르다**(§8-72).
그리고 `path 0.35` 라 **C 블록(3 cm 보폭)까지 같이 푼다** — 진짜 4발자국 후보다.

✅ **발목값 관문은 닫혔다** — MJC G2 리허설이 **`NQ` + 배포 실경로(EMA 185 Hz · 박스 15 ·
벤더 물리 · 직진)** 로 돌아 **50 Hz 명목에서 5시드 낙상 0**, 실기 tick 재생에서 0.4 다.
⇒ **A′ 30초 완주 예상 확률 90~100 %.** 도착 30초도 tilt 1° 이내 / 표류 4.5 cm.

⛔ **그래도 A 실행을 `NQ` 로 바꾸지 마라.** 남은 관문 하나:
1. **실기 1분 서기 스크린** — `NP`(형제 arm)가 sim 은 좋았는데 실기 각속도 **4.90배**였다.
   MJC 가 원리적으로 못 재는 것이 **발목 관측 게이트**(MuJoCo 는 표본 교체 오염을 안 만든다)와
   실기 지면·지연이라, 그 관문은 로봇에서만 닫힌다.
⚠️ 잔여 위험: 실기 tick 재생 셀에서 **발간격 min −0.036(교차)** 이고 5시드 중 2개가 1회씩
낙상한다. 0 이 아니다.

**순서**: §3 A(I3b, 완전 사전등록됨) → **`NQ` 1분 서기 스크린** → 둘 다 통과하면
**`NQ` 30초 보행**. 섞지 마라 — A 가 실패했을 때 정책인지 스택인지 못 가른다.

```bash
# NQ 서기 스크린 (설치 후). 1초가 아니라 60초로 본다 -- NP 는 1초 단일 실행이었다
cd ~/Workspace/deploy && PYTHONUNBUFFERED=1 ./run_e0.sh fixed 0,0,0 \
  --policy-path ./models/goal_pose_nq.pt \
  --publish-hz 100 --rate-fixed-filter --filter-tau-ms 10 \
  --hold-diag 3 --max-policy-seconds 60 --test-tilt-abort-deg 12 \
  --log-timing /tmp/nq_stand_60s.csv
```
**판정**: 몸 각속도 p95 를 `I3b`(0.254) 와 비교. **2배 이내면 통과**, `NP` 수준(4.90배)이면
보행으로 넘어가지 마라.
