# E 배치 결과 보고서 — E0 / E1 / E2 / V7_full

> 2026-07-29. `K1_walk/v7/` 에 올라온 **모든** 리포트 파일(106개)을 전수 검토했다.
> 각 arm의 **가설 → 수정 → 결과 → 판정**을 수치 근거와 함께 제시한다.
> 결론부터: **이 배치의 절반은 측정이 무효이고, 유효한 절반은 하나의 구조적 결함을
> 가리킨다.**

---

> ## ⛔ 정정 공지 (2026-07-30) — 이 문서의 주장 두 개가 틀렸다
>
> **①  아래 §0-1과 §2의 "E0의 2.7 cm는 존재하지 않는다"는 오류다.**
> `K1_walk/select_results/E0_armB_armsdown/report.json`이 실재하고, E0@`model_6200`은
> **자기 config로 평가된 유효 수치 2.72 / 5.01 cm, heading 2.52°, strict 89.29%,
> 낙상 2회, 게이트 3/4 통과**다. 내가 `cd K1_walk/v7` 상태에서 `ls K1_walk/`를 실행해
> 실패한 것을 "없음"으로 잘못 결론냈다. **§0-1, §2, §3-1, §5의 E0 서술은 폐기한다.**
>
> **② §4-2의 "5 cm 게이트는 도달 불가능"도 과잉 주장이다.** 200배 비율 계산은 맞지만
> E0가 같은 보상으로 2.72 cm를 냈다. 따라서 공 내부 기울기 부재는 **필수 수정이 아니라
> 강건화 항목**이다.
>
> **③ §4-3의 외란 크기 평가도 하향된다.** 외력이 decimation 10 스텝 중 1번만
> submit돼 실제 충격량이 명목의 **약 1/10**이었다(Codex 서버 실측).
>
> 정정된 통합본은 **[non-codex.md](non-codex.md)**에 있다. 아래 §4-1(속도 지표 손상)과
> §3-3(E2 감속)은 재검증 결과 유효하다.

## 0. 요약 — 세 줄

1. ~~**E0의 "2.7 cm"는 존재하지 않는 숫자다.**~~ → **철회.** 위 정정 공지 ① 참조.
   E0 = **2.72 / 5.01 cm, 2.52°, strict 89.29%, 낙상 2** — 이 프로젝트 최고 정책이다.
2. **속도 지표가 전 런에서 손상돼 있다.** 리셋 텔레포트를 속도로 세고 있어
   `peak_speed`가 최대 **203 m/s**까지 나온다. 전 구간의 **15.7~17.6%**가 오염됐다.
   이 배치가 올리려던 바로 그 지표다. → **유효. 수정 완료**(`eval_goal_pose.py:909`).
3. ~~**5 cm 게이트는 현재 보상으로 도달 불가능하다.**~~ → **하향.** 200배 비율은 사실이나
   E0가 도달했다. 공 내부 기울기는 강건화 항목이다.

---

## 1. 데이터 유효성 분류 (지시 3)

`env_code_sha`를 git 커밋으로 역추적해 분류했다. 이 필드는 **평가 시점의 `envs/` 트리
마지막 커밋**이지 학습 시점이 아니다 — 그 구분이 이 표의 핵심이다.

| sha | 커밋 | 시각 | 내용 |
|---|---|---|---|
| `5bafed73` | `5bafed7` | 07-27 22:36 | v8 SmoothTurn 도입. **캐럿 재작성 이전** |
| `a7782bb6` | `a7782bb` | 07-28 18:06 | **가상 선도체 재작성(`1d7e734`, 07-28 16:44) 포함** |

### 1-1. 유효 / 무효 판정

| 파일 | 평가 config | sha | 판정 | 사유 |
|---|---|---|---|---|
| `E0.../report.md` (07-27 07:32) | **`envs/K1/Goal_Pose_V7.yaml` (베이스)** | 없음 | ❌ **무효** | E0는 path를 학습한 적이 없는데 평가의 **46%(2136구간)가 path**. 낙상 93회 중 **90회가 path 구간** |
| `E0.../selection.md` | 동일 (베이스) | 없음 | ❌ **무효** | 12개 후보 전부 같은 오염된 과제로 채점. 위치 median 10.4~11.6 cm가 전부 path 포함값 |
| `E1.../report.md` (원본) | 베이스 | 없음 | ❌ **무효** | 동일 |
| `E2.../report.md` (원본) | 베이스 | 없음 | ❌ **무효** | path 44%(2017구간), path 위치오차 144 cm |
| `V7_full.../report.md` (원본) | 베이스 | 없음 | ❌ **무효** | 동일 |
| `reeval_20260727-225001/E2_robust/*` | 자기 config | `5bafed73` | ⚠️ **조건부** | path OFF arm이라 캐럿 코드 drift의 영향 없음. 단 `common_task`는 path를 포함하므로 무효 |
| `reeval_e_batch_gpu1_20260728-201804/*` | 자기 config | `a7782bb6` | ✅ **유효** | own_task 한정 |
| `reeval_20260729-144525/*` | 자기 config | `a7782bb6` | ✅ **유효** | own_task 한정. 위와 **비트 단위 동일**(결정론적 평가) |

### 1-2. 치명적 공백: **E0는 재평가된 적이 없다**

`K1_walk/v7/` 전체에 E0의 `report.json`이 **하나도 없고**, `reeval_*` 어느 디렉터리에도
E0가 없다. 그런데 **G 배치 4종 전부가 E0@`model_6200`에서 warm start**했다.

더 나쁜 것: `selection.md`가 실제로 고른 것은 **`model_6800`**이고,
`make_v7_arms.py:61`의 `ARMSDOWN_CKPT`는 **`model_6200`**을 가리킨다. 두 숫자의 출처가
다르고, 6200을 고른 근거는 이 데이터셋 안에 없다.

### 1-3. `common_task` vs `own_task` — 무엇을 봐야 하나

- **`own_task`**: 그 arm이 실제로 학습한 과제로 채점. **성능 판정은 여기서만.**
- **`common_task`**: 전 arm 공통 과제(path 35% 포함)로 채점. **arm 간 비교용이지만,
  path 미학습 arm에게는 불리한 과제**라 그대로 순위를 매기면 안 된다.

---

## 2. "2.7 cm"의 출처 추적

`gbatch.md` §2-1과 `make_v7_arms.py:164`가 근거로 삼은 **"E0 = 2.7 cm / 5.0 cm p90 /
2.5° / 89% / 낙상 2"**를 전 파일에서 검색했다.

```
$ grep -rn "2\.7 cm" K1_walk/v7/
E2_robust/report.md:90      | stand | 242 | 5% | 2.7 cm | 7.8 cm | 1.6° | 80% | 6% |
E1_path/own_task/report.md:110 | stand | 238 | 5% | 2.7 cm | 5.7 cm | 2.6° | 87% | 3% |
```

**전체 지표로 등장한 적이 한 번도 없다.** 두 번 다 `stand` 카테고리 한 줄이고,
E1과 E2의 것이다. `2.7 / 5.7 p90 / 2.6° / 87%`(E1 stand)가 인용된 숫자와 거의 일치한다.

> **결론**: G 배치 전체의 설계 근거였던 "E0가 게이트에 도달했다"는 명제는
> **정지 카테고리(거리 0) 한 줄을 전체 성능으로 오독한 것**이다.
> E0가 나쁜 정책이라는 뜻은 아니다 — 아래 §3-1이 정직한 수치를 제시한다.
> 하지만 **"게이트 도달"은 사실이 아니었다.**

---

## 3. arm별 가설 → 수정 → 결과 → 판정

전 arm은 `E0 = _OFF_PATH + _OFF_ROBUST + _OFF_PROTECT`를 기준으로 한 레버 묶음씩 켠다
(`tools/make_v7_arms.py:84-121`).

### 3-1. E0_armB_armsdown — "팔을 내리고 대칭손실을 켜면 armB가 개선되는가"

| 항목 | 내용 |
|---|---|
| **가설** | 팔 내림으로 yaw 관성 −69%, ego 반폭 0.396→0.146 m. 여기에 `symmetry_coef`(그동안 죽은 키였음)를 최초 활성화하면 armB(3.9 cm)보다 좋아진다 |
| **수정** | `K1_locomotion_armsdown.urdf` + RunnerV3 대칭손실. path/robust/protect 전부 OFF |
| **측정 상태** | ❌ **오염된 평가 1건뿐. 재평가 없음** |

**정직한 재구성** (오염된 리포트에서 path 구간을 제외하고 카테고리 표를 재집계):

| 카테고리 | n | 위치 median | p90 | heading | 성공(엄격) |
|---|---|---|---|---|---|
| turn | 450 | 6.7 cm | 12.4 cm | 2.8° | 31% |
| combined | 748 | 6.6 cm | 12.5 cm | 2.8° | 30% |
| straight | 523 | 6.4 cm | 11.6 cm | 2.7° | 33% |
| lateral | 508 | 6.2 cm | 11.6 cm | 2.6° | 32% |
| stand | 233 | 4.8 cm | 10.8 cm | 2.9° | 49% |
| **waypoint 합** | **2462** | **≈ 6.4 cm** | **≈ 12.2 cm** | **2.8°** | **32%** |

낙상 93회 중 **path 90 / combined 3** → **waypoint 낙상 3회**.

> **판정: ⚠️ 검증 불가, 추정 6.4 cm.** armB의 3.9 cm보다 **나쁘다**(단 armB는 다른
> 과제 분포에서 측정된 값이라 직접 비교는 조심해야 한다).
> **"E0 = 현재 최고 정책"이라는 근거는 이 데이터셋에 없다.**

---

### 3-2. E1_path — "움직이는 목표가 속도 요구를 만드는가"

| 항목 | 내용 |
|---|---|
| **가설** | 정지 목표는 아무 속도로나 도달 가능하다. 전진하는 목표는 그 속도를 내야만 간격이 유지된다 → 속도가 오른다 |
| **수정** | `goal_mode_mixture.path = 0.35`(자기 config는 46%), lookahead carrot |
| **측정** | ✅ `own_task` 유효 (sha `a7782bb6`) |

| 지표 | 값 |
|---|---|
| waypoint 위치 median | **52.1 cm** ❌ |
| waypoint p90 | 57.9 cm ❌ |
| heading median | 3.3° ✅ |
| 낙상 | 94 ❌ |
| 최고속도 p50 (오염 제외) | **1.47 m/s** |
| 최고속도 p90 (오염 제외) | **2.03 m/s** |

**카테고리별이 진짜 이야기를 한다:**

| 카테고리 | 시작거리 | 위치 median | 도달후이탈 |
|---|---|---|---|
| stand | 0.00 m | **2.7 cm** | 3% |
| turn | 0.00 m | **52.5 cm** | **100%** |
| lateral | 0.74 m | 52.3 cm | 4% |
| straight | 0.99 m | 52.3 cm | 27% |
| combined | 1.35 m | 52.8 cm | 2% |

**시작거리가 0인 `turn`이 52.5 cm로 끝나고 도달후이탈이 100%다.** 제자리에서 돌기만
하면 되는 과제에서 로봇이 반경 0.5 m를 걸어 나갔다는 뜻이다. 그리고 **모든 카테고리가
52.3~52.8 cm로 사실상 동일하다** — 이건 성능 분포가 아니라 **상수 오프셋**이다.

> **판정: ✅ 가설은 맞았다 (속도 1.47 m/s p50, E2의 5배). ❌ 대가가 파괴적이다.**
> **근본 원인**: 목표가 절대 도달 불가능한 채로 앞서가면, 정책은 "목표 뒤에 일정 간격을
> 유지하며 계속 걷기"를 배운다. `stand`만 예외인 이유는 그 카테고리가 gait clock을
> 0으로 만들어 보행 자체를 끄기 때문이다. **`turn`은 gait clock이 살아 있어 걸어 나간다.**
> → 이것이 dwell(캐럿 정지)을 도입한 이유다. G1이 그 수리의 결과를 보여준다(§gbatch_results).

---

### 3-3. E2_robust — "외란·인지 노이즈 강건성의 비용은 얼마인가"

| 항목 | 내용 |
|---|---|
| **가설** | 실기 BT/비전은 목표를 흔든다. 관측만 흔들고 보상은 참값을 읽게 하면 "쫓기"가 아니라 "거르기"를 배운다 |
| **수정** | `_OFF_ROBUST`를 **통째로 해제** — 7개 레버 동시 |
| **측정** | ✅ `own_task` 유효, **3회 반복** |

**⚠️ 설계 결함: "한 번에 하나씩"을 위반했다.** `_OFF_ROBUST`(`make_v7_arms.py:66-74`)는
7개를 동시에 켠다:

```
disturbance.enabled           False → True    (충돌 40-150 N / 지지 3-15 N)
goal_bt_flicker.prob_per_step 0.0   → 0.004
goal_pos.range                [0,0] → [0, 0.04]  m   스텝별 지터
goal_heading.range            [0,0] → [0, 0.04]  rad
goal_pos_bias.range           [0,0] → [0, 0.05]  m   구간별 고정 편향
goal_heading_bias.range       [0,0] → [0, 0.05]  rad
goal_obs_hold_steps           [0,0] → [2, 3]        관측 지연
```

**결과:**

| 지표 | E2 own_task | 비교(E1 own) |
|---|---|---|
| waypoint 위치 median | **12.9 cm** | 52.1 cm |
| p90 | 34 cm | 57.9 cm |
| heading median | 3.7° | 3.3° |
| 낙상 | **2** | 94 |
| **최고속도 p50** | **0.28 m/s** | **1.47 m/s** |
| **평균속도 p50** | **0.11 m/s** | 0.45 m/s |
| along_err median | **−3.9 cm** (언더슈트) | −35.0 cm |
| \|cross_err\| median | 12.1 cm | — |
| 10 cm 안에 한 번도 못 들어간 구간 | 52% | 60% |

> **판정: ✅ 강건성은 진짜다 (낙상 2회, 위치 12.9 cm — E 배치 최고). ❌ 속도가 붕괴했다.**

#### 지시 16 — E2가 왜 느려졌나 (근본 원인)

같은 `common_task`에서 재보면 **E2 0.28 m/s vs E1 0.72 m/s — 2.6배**다. 과제가 같으므로
과제 탓이 아니다. 세 가지 증거가 하나를 가리킨다:

1. **오차가 종방향이 아니라 횡방향이다.** along −3.9 cm vs |cross| 12.1 cm.
   못 미치는 게 아니라 **옆으로 빗나간다.**
2. **오버슈트가 거의 없다** (도달후이탈 8%). 과감하게 가서 지나치는 게 아니라
   **조심스럽게 접근하다 멈춘다.**
3. **`goal_pos_bias`가 구간별로 고정된 편향**이다(σ=5 cm). 스텝별 지터(σ=4 cm)는
   시간 평균으로 씻기지만 **구간 고정 편향은 씻을 수 없다.** 게다가 보상은 참값을
   읽으므로 정책은 **자기가 보는 목표가 틀렸다는 것을 알 방법이 없다.**

**메커니즘**: 관측된 목표 위치의 분산이 σ² 라면, 빠르게 갈수록 잘못된 지점에 크게
빗나가는 대가가 커진다. 최적 정책은 **속도를 낮춰 판단을 미루는 것**이다.
`goal_reached`가 참값 기준 10 cm 안에서만 +1을 주므로, 편향된 관측 아래에서 그 보너스는
**복권**이 된다 — 기대값을 최대화하는 방법은 천천히 가서 헛발질 횟수를 줄이는 것뿐이다.

**같은 실수를 반복하지 않기 위한 처방** (H 배치 반영):
- ❌ **7개를 한꺼번에 켜지 말 것.** 최소한 `bias`(씻기지 않는 것)와 `jitter`(씻기는 것)를
  분리해야 한다. 이 둘은 정책에 **정반대의 압력**을 준다.
- ✅ **편향은 관측 가능하게 만들 것.** 실기에서도 로봇은 자기 위치 추정 오차를 안다
  (공분산). 편향을 주려면 그 **불확실성 자체를 관측에 넣거나**, 아니면 편향을
  **에피소드 내에서 변하게** 해서 시간 평균으로 씻기게 해야 한다.
- ✅ **속도를 명시적으로 지키는 항**을 함께 넣을 것. 강건성 항만 켜면 정책은 항상
  "덜 움직이기"로 도망친다. 이건 E2·G2에서 **두 번** 재현됐다.

---

### 3-4. V7_full — "전부 켜면"

| 지표 | 값 | vs 최고 |
|---|---|---|
| waypoint 위치 median | 40.6 cm | E2의 3.1배 |
| 낙상 | 38 | E2의 19배 |
| 최고속도 p50 (정제) | 1.29 m/s | E1의 88% |
| stand 위치 median | 5.5 cm / **이탈 27%** | E1 2.7 cm / 3% |

> **판정: ❌ 상호작용이 나쁘다.** path(E1)의 "안 멈춤"과 robust(E2)의 "느림"이
> 상쇄되지 않고 **둘 다 나온다**. E1보다 22% 정확하지만 E2보다 3.1배 부정확하고,
> E2보다 4.6배 빠르지만 E1보다 12% 느리다. **어느 축에서도 최고가 아니다.**

---

## 4. 배치 전체를 관통하는 결함 3개

### 4-1. ⛔ 속도 지표 손상 — 이 배치가 올리려던 바로 그 숫자

`segments.csv`의 `peak_speed_mps` 분포:

| run/scope | n | peak p99 | peak **max** | >3 m/s 구간 | 비율 |
|---|---|---|---|---|---|
| E1 own | 4591 | 130.06 | **203.24** | 810 | **17.6%** |
| E1 common | 4594 | 106.83 | 174.54 | 771 | 16.8% |
| E2 own | 4638 | 19.29 | 29.20 | 726 | 15.7% |
| E2 common | 4604 | 46.61 | 71.92 | 766 | 16.6% |
| V7 own | 4631 | 109.13 | 181.33 | 772 | 16.7% |
| V7 common | 4648 | 86.14 | 182.14 | 762 | 16.4% |

K1의 물리적 최대는 E0의 body-velocity 실측 **2.56 m/s**다. 200 m/s는 물리가 아니다.

**원인**: 속도를 `d(pose)/dt`(0.2 s 창)로 바꾼 뒤, **에피소드가 끝나 env가 스폰으로
리셋될 때의 텔레포트가 그대로 "속도"로 계산된다.** 오염 구간 수(726~810)가
`segments_censored_by_episode_end`(761~767)와 거의 일치하는 것이 증거다.

**오염 제외 후 정직한 속도:**

| run | peak p50 | peak p90 | peak max | mean p50 |
|---|---|---|---|---|
| E1 own | **1.47** | **2.03** | 3.00 | 0.45 |
| V7 own | 1.29 | 1.79 | 2.92 | 0.49 |
| E1 common | 1.06 | 1.93 | 2.81 | 0.33 |
| V7 common | 0.68 | 1.69 | 2.90 | 0.25 |
| E2 common | 0.45 | 0.86 | 2.99 | 0.20 |
| E2 own | **0.28** | **0.61** | 2.96 | 0.11 |

> 리포트 본문의 **"구간 최고속도 p90 / 최대"** 행과 **"명령속도 vs 실제속도"** 추종비
> 표는 **전부 이 오염을 포함한다.** 그 표를 근거로 한 판단은 전부 재검토가 필요하다.
> 이건 §gbatch_results에서 G 배치에도 그대로 적용된다(G1 "구간 최고속도 최대 141.59 m/s").

### 4-2. ⛔ 5 cm 게이트가 보상 구조상 도달 불가능

```
constellation_weight w = 0.2,  scale 3.5     → 3.5 · exp(−0.2 · d²)
goal_reached          radius 0.1 m, scale 1.0 → 1.0 (이진, 내부 기울기 없음)
```

| 행동 | 보상 변화 |
|---|---|
| 오차 10 cm → 5 cm (절반으로) | `3.5(e^{-0.002} − e^{-0.0005})` = **+0.005 /step** |
| 오차 10.1 cm → 9.9 cm (문턱 통과) | **+1.0 /step** |

**비율 200배.** 정책의 최적해는 "10 cm 공 안으로 겨우 들어가서 즉시 멈추기"다.
G1이 전 카테고리에서 **5.6~5.7 cm (p90 7.2~7.6 cm)**로 수렴한 것이 정확히 이 예측이다 —
10 cm 안에 노이즈 여유를 두고 앉되, 그 아래로 내려갈 이유가 없다.

> **5 cm median 게이트는 정책의 한계가 아니라 보상의 한계다.**
> H 배치 필수 수정: `goal_reached`에 **공 내부 기울기**를 넣을 것.

### 4-3. ⛔ 외란은 학습만 되고 **평가된 적이 없다**

`eval_goal_pose.py:356-364`가 `--keep_perturbations` 없이는
`randomization.disturbance.enabled = False`를 **강제**한다. 전 리포트 헤더가 "외란 OFF"다.
`--stress`의 선택지는 `jitter` 하나뿐이고, 그건 **목표를 흔들 뿐 힘을 가하지 않는다**.

> E2·V7·G2·G3가 충돌 외란(40~150 N)으로 **학습**했지만, 그 아래에서 어떻게 행동하는지
> **측정된 적이 없다.** (지시 9에 대한 정직한 답 — §hbatch.md에 상세)

---

## 5. 지시 21 — "E1, E0가 좋아 보이는데 수치는?"

**감각과 수치가 갈린다.** `own_task` 기준 (전부 waypoint 구간만, path 제외):

| arm | 위치 med | p90 | heading | 낙상 | 최고속도 p50 | 종합 |
|---|---|---|---|---|---|---|
| **E2_robust** | **12.9 cm** 🥇 | **34 cm** 🥇 | 3.7° | **2** 🥇 | 0.28 m/s ❌ | **정확도·안정성 1위, 속도 꼴찌** |
| E0_armB | ≈6.4 cm* | ≈12.2 cm* | 2.8° | 3* | — (미측정) | *오염 리포트에서 재구성, **직접 비교 불가** |
| V7_full | 40.6 cm | 58 cm | 4.1° | 38 | 1.29 m/s | 어느 축에서도 최고 아님 |
| E1_path | 52.1 cm ❌ | 58 cm | 3.3° | 94 ❌ | **1.47 m/s** 🥇 | **속도 1위, 정확도 꼴찌** |

**정직한 답:**

- **E1이 좋아 보이는 이유는 맞다** — 실제로 **가장 빠르다**(1.47 m/s, E2의 5.3배).
  영상에서 시원하게 걷는다. **하지만 waypoint 정확도는 52 cm로 배치 최악**이고
  **낙상 94회**다. `turn` 과제에서 도달후이탈 100%다.
- **E0가 좋아 보이는 이유도 맞다** — 재구성한 6.4 cm는 E1(52.1)·V7(40.6)보다 훨씬 좋다.
  **하지만 인용되던 2.7 cm는 사실이 아니고**, E0는 유효한 평가가 **한 번도 없다.**
- **수치상 1위는 E2다.** 위치 12.9 cm, 낙상 2회 — 두 축 모두 압도적 1위.
  **그런데 0.28 m/s라서 눈으로 보면 "안 걷는" 것처럼 보인다.** 그래서 감각에서 빠졌다.

> **이 배치는 "정확하지만 느린 정책(E2)"과 "빠르지만 부정확한 정책(E1)"을 만들었고,
> 둘을 합친 것(V7)은 양쪽 단점만 가져왔다.** 이게 E 배치의 한 줄 결론이다.

---

## 6. 가설별 최종 판정표

| # | 가설 | arm | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 팔 내림 + 대칭손실이 armB를 개선한다 | E0 | ⚠️ **검증 불가** | 유효 평가 0건. 재구성 6.4 cm는 armB 3.9 cm보다 나쁨 |
| 2 | 움직이는 목표가 속도를 만든다 | E1 | ✅ **성립** | 1.47 vs 0.28 m/s (5.3배) |
| 3 | path 학습이 waypoint를 망친다 | E1 | ✅ **성립, 심각** | `turn` 이탈 100%, 전 카테고리 52 cm 상수 오차 |
| 4 | 목표 노이즈가 필터링을 가르친다 | E2 | ✅ **부분 성립** | 낙상 2회, 위치 12.9 cm — 배치 1위 |
| 5 | 강건성의 대가는 감당 가능하다 | E2 | ❌ **기각** | 속도 2.6배 붕괴(동일 과제 기준) |
| 6 | 전부 켜면 통합된다 | V7 | ❌ **기각** | 양쪽 단점만 상속 |
| 7 | 대칭손실이 좌우 편향을 없앤다 | 전체 | ⚠️ **부분** | `feet_lat_offset` E1 −1.2 cm, `bias_Hip_Yaw` E1 +0.023 rad(1.3°) — 남아 있음 |

---

## 7. H 배치로 넘기는 것 (근거 있는 것만)

| 항목 | 근거 | 우선순위 |
|---|---|---|
| `goal_reached`에 공 내부 기울기 | §4-2, 200배 비율 | **최상** |
| 속도 지표 리셋 텔레포트 제거 | §4-1, 17% 오염 | **최상** |
| 외란 평가 모드(`--stress push`) 신설 | §4-3, 측정 0건 | **최상** |
| dwell 유지 (path의 "안 멈춤" 수리) | §3-2 | 높음 |
| robust 레버를 bias / jitter로 분리 | §3-3 | 높음 |
| 강건성 arm에 속도 유지 항 동반 | §3-3, E2·G2 2회 재현 | 높음 |
| E0 유효 재평가 (warm start 근거 복구) | §1-2 | 높음 |

