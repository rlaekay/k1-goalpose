# G 배치 결과 보고서 — G1_speed / G2_robust / G3_full / G4_smoothturn

> 2026-07-29. `gbatch.md` §5가 세운 4개 질문에 대한 답.
> 전 arm이 **자기 config로, 동일한 `env_code_sha = a7782bb6`에서** 평가돼
> **E 배치와 달리 arm 간 비교가 유효하다.**

> ## ⛔ 정정 공지 (2026-07-30) — 결론과 4개 항목을 정정한다
>
> **① "G1 압승"은 G군 내부에서만 맞다.** 유효한 E0 기준선(`select_results/E0.../report.json`,
> **2.72/5.01 cm, 2.52°, strict 89.29%, 낙상 2**)과 비교하면 G1은 **회귀**다:
> 위치 **2.03배 악화**, 낙상 **19배**, strict **−55.1%p**. 대가로 얻은 것은 >1 m/s 체류
> 3.0 → 17.7%다. **속도 6배를 사서 정확도 2배·안전 19배를 팔았다.** §1·§8·§9 정정.
>
> **② §4의 외란 크기 평가가 10배 틀렸다.** "최대 7.9 m/s² ≈ 0.8 g"는 명목값 기준이다.
> 실제로는 Isaac Gym force가 decimation 10 스텝 중 1번만 submit돼 **적분 충격량이
> 명목의 약 1/10 ≈ 0.08 g**였다(Codex 서버 실측). **"크기는 충분했다"는 결론은 철회한다.**
>
> **③ §5의 22번(정옆·정뒤) constellation 설명은 반증됐다.** E0의 clean `segments.csv`에서
> 시작거리 0.6–0.9 m로 통제하면 lateral peak **0.91 m/s** vs straight **0.71 m/s** —
> E0는 같은 보상을 쓰는데 **lateral이 28% 더 빠르다.** 진짜 원인은 **path 학습이 만드는
> 전방 이동 prior**이고, path 노출과 단조다(E0 +35% → E1 −4% → V7 −22%).
>
> **④ §6의 \|ω\| p90 수치(1.76~2.24 rad/s)는 yaw 전용**이었다. 상체 흔들림 판정 근거가
> 되지 못한다(`base_ang_vel`로 교체됨).
>
> **⑤ §2-3 "G1+G2 통합 실패"는 부정확하다.** G3의 실제 config는 **grid OFF, flicker 0.004**로
> G1의 grid도 G2의 flicker 0.01도 포함하지 않았다. **통합은 실행되지 않았다.**
>
> **⑥ §7의 `v7_extras`는 마지막 256-env snapshot**이라 대칭 분석의 통계적 무게를 낮춘다.
>
> 정정된 통합본과 Codex 작업 감사는 **[non-codex.md](non-codex.md)**에 있다.
> §2-1의 grid 커리큘럼 무효 판정, §3의 케이던스 산술, §2-3·§2-4는 재검증 결과 유효하다.

---

## 0. 데이터 유효성 (지시 3)

| arm | 평가 config | sha | 총 학습 | 선택 ckpt | 판정 |
|---|---|---|---|---|---|
| G1_speed | 자기 `config.yaml` | `a7782bb6` | 12000 (120 ckpt) | 10700 | ✅ **유효** |
| G2_robust | 자기 `config.yaml` | `a7782bb6` | 12000 (120 ckpt) | 7500 | ✅ **유효** |
| G3_full | 자기 `config.yaml` | `a7782bb6` | 12000 (120 ckpt) | 5500 | ✅ **유효** |
| G4_smoothturn | 자기 `config.yaml` | `a7782bb6` | **4100 (41 ckpt)** | 3400 | ⚠️ **34%만 학습** — 절대비교 불가 |

**공통 무효 항목** (E 배치와 동일, `ebatch.md` §4 참조):
- ⛔ `구간 최고속도 p90/최대` 행 — 리셋 텔레포트 오염 (G1 최대 **141.59 m/s**, G3 **160.11 m/s**)
- ⛔ `명령속도 vs 실제속도` 추종비 표 — 위 오염값을 사용
- ⛔ 전 arm 평가가 **외란 OFF** — 외란 학습 효과 미측정
- ⚠️ G 배치는 `segments.csv`가 저장되지 않아 세그먼트 단위 재분석이 불가능하다
  (E 배치 재평가에는 있다). **H 배치에서 반드시 저장할 것.**

---

## 1. 지시 21 — "G1이 압승 같은데 수치는?" → **압승이 맞다. 그것도 크게.**

waypoint 구간만, 전부 자기 과제:

| arm | 위치 med | p90 | heading | 낙상 | 성공(엄격) | 정상종료 비율 | 미도달 |
|---|---|---|---|---|---|---|---|
| **G1_speed** | **5.5 cm** 🥇 | **7.4 cm** 🥇 | **2.5°** 🥇 | 38 | **34%** 🥇 | **95%** 🥇 | **1%** 🥇 |
| G2_robust | 25.9 cm | 85.8 cm | 5.1° | **0** 🥇 | 10% | 21% | 63% |
| G4_smoothturn | 42.0 cm | 63.6 cm | 4.8° | **1016** ❌ | 7% | 9% | 67% |
| G3_full | 49.0 cm | 63.2 cm | 5.9° | 17 | **0%** | **0%** | 68% |

**G1은 2위(G2)보다 위치오차가 4.7배, p90이 11.6배 좋다.** 게이트 4개 중 2개 통과
(p90 7.4 ≤ 10 ✅, heading 2.5 ≤ 10 ✅), 위치 median은 **5.5 vs 5.0 cm — 0.5 cm 차이로 실패**,
낙상 38회로 실패.

**카테고리별로 봐도 G1만 균일하다:**

| 카테고리 | G1 | G2 | G3 | G4 |
|---|---|---|---|---|
| stand | **3.2 cm** (이탈 4%) | 3.8 cm (7%) | **48.0 cm (이탈 100%)** ❌ | 3.4 cm (2%) |
| turn | **5.6 cm** (이탈 0%) | 9.5 cm (46%) | 47.2 cm (100%) | 39.2 cm (100%) |
| lateral | **5.6 cm** (0%) | 29.0 cm (2%) | 48.1 cm (3%) | 40.7 cm (5%) |
| straight | **5.7 cm** (1%) | 33.2 cm (4%) | 50.9 cm (12%) | 45.7 cm (11%) |
| combined | **5.7 cm** (0%) | 64.1 cm (0%) | 50.5 cm (1%) | 49.5 cm (2%) |

> **G3의 `stand`가 48.0 cm에 도달후이탈 100%다.** 거리 0인 목표에서 로봇이 0.5 m를
> 걸어 나갔다는 뜻이다. **G3는 서 있지를 못한다.**

---

## 2. arm별 가설 → 결과 → 판정

### 2-1. G1_speed — "속도를 올리면서 정확도를 지키나"

| 항목 | 내용 |
|---|---|
| **가설** | ① dwell이 path의 "안 멈춤" 병리를 고친다 ② (속도×곡률) 그리드가 v/ω 커플링을 분리해 지속가능 상한을 드러낸다 |
| **수정** | `path 0.35` + lookahead floor + dwell + `speed_grid.enabled: True`. warm start = E0 |
| **통과 기준** (gbatch §5) | waypoint ≤ 5 cm **AND** 구간최고 p90 > 1.37 m/s |

#### ✅ 가설 ① 성립 — dwell이 실제로 고쳤다

| | E1 (dwell 없음) | **G1 (dwell 있음)** |
|---|---|---|
| `turn` 도달후이탈 | **100%** | **0%** |
| `stand` 위치 | 2.7 cm | 3.2 cm |
| waypoint 위치 median | 52.1 cm | **5.5 cm** |
| 전 카테고리 편차 | 52.3~52.8 (상수 오프셋) | 5.6~5.7 (진짜 분포) |

> **path 학습이 waypoint를 파괴하는 문제는 해결됐다.** 이것이 G 배치 최대 성과다.
> ⚠️ 단 E1 대비 4가지가 동시에 다르다(dwell / 가상 선도체 재작성 / floor / warm start E0).
> dwell이 **설계상 그 실패를 정확히 겨냥한 장치**라 귀인이 합리적이지만 **단독 분리는 안 됐다.**

#### ❌ 가설 ② 기각 — 그리드 커리큘럼은 **작동하지 않았다**

`report.json["v7_extras"]`:
```
grid_cells_active  30.0      grid_cells_total  30.0
grid_success_rate  0.9796    grid_max_speed  1.8   grid_max_curv  1.0
path_lag_mean      0.2220
```

**30칸 전부 활성, 성공률 98%.** 커리큘럼이 즉시 만렙이 됐다. 이유는 코드에 있다:

```python
# goal_pose_v7.py:409  승급 판정
ok = track_ok_steps / track_steps > 0.8        # track_ok += 1 when path_lag < keepup_gap_m
```
- `keepup_gap_m = 2.0` m
- `path_lag = max(gap − l_min, 0)`, 그리고 **leash가 `gap ≤ l_max = l_min × 1.6`을 보장**
- 따라서 `path_lag ≤ 0.6 × l_min ≤ 0.6 × 2.4 = **1.44 m** < 2.0 m` — **항상**

> **승급 기준이 구조적으로 실패할 수 없다.** leash가 자기가 판정하려는 양을 이미
> 상한 지어 놓았다. 실측 `path_lag_mean = 0.222 m`로 문턱의 **1/9**다.
> `grid_success_rate 0.98`은 로봇이 아니라 **leash를 측정한 값**이다.
>
> 4-이웃 확장이 매 구간(~6 s)마다 일어나므로 6×5 그리드는 **약 9구간 ≈ 1분**에 포화한다.
> **G1의 실제 학습 조건 = 처음부터 (0.3–1.8 m/s × 0.25–1.0 1/m) 전역 균일 샘플링**,
> 즉 gbatch §3-9가 Margolis ablation을 근거로 폐기했던 **E3 그 자체**다.

이건 `gbatch.md` §8-8이 정리한 **"판정에 쓴 상태가 판정 대상이 아니었다"** 패턴의
다섯 번째 사례다. 캐럿(위상 vs 거리), mirror map(12 vs 16 DOF), self-collision,
diag_reset(리셋 전후)에 이어서.

#### 속도는 올랐나 — 정직한 재집계

리포트의 "구간 최고속도 p90 12.70 m/s"는 오염값이다. 오염 없는 지표만 쓰면:

| 지표 (오염 없음) | G1 | G2 | G3 | G4 |
|---|---|---|---|---|
| 몸통속도 median | 0.21 m/s | 0.07 | 0.19 | 0.17 |
| 몸통속도 p90 | **1.50 m/s** | 0.19 | 1.51 | 1.58 |
| 몸통속도 p99 | 2.08 m/s | 0.39 | 1.89 | 3.02 |
| 0.5 m/s 초과 시간 | **32.9%** | 0.7% | 32.1% | 29.5% |
| 1.0 m/s 초과 시간 | **17.7%** | 0.5% | 22.6% | 21.8% |

E0(오염 전 body-velocity 지표)는 p90 1.10 / 1.0 m/s 초과 14.4%였다.
→ **G1은 p90 1.10 → 1.50 m/s (+36%), 1.0 m/s 초과 시간 14.4% → 17.7%.**
gbatch §5의 기준 "구간최고 p90 > 1.37"은 오염된 행으로만 판정 가능해 **판정 불가**다.

> **판정: 🥇 종합 최고. ✅ dwell 성공, ❌ 그리드 실패(무효 커리큘럼), ⚠️ 속도 개선은 +36%로 실측.**
> 낙상 38회는 여전히 게이트 실패다.

---

### 2-2. G2_robust — "강건성의 비용" → **E2의 재현. 두 번째 같은 실패.**

| 지표 | G2 | E2 (같은 레버 묶음) |
|---|---|---|
| 낙상 | **0** ✅ | 2 |
| 위치 median | 25.9 cm | 12.9 cm |
| 몸통속도 p90 | **0.19 m/s** | 0.28 m/s(구간최고 p50) |
| 1.0 m/s 초과 시간 | **0.5%** | — |
| stress jitter 낙상 | 0, 직립 100% ✅ | 0 ✅ |

**낙상 0회는 배치 유일이다.** 그리고 **속도가 다시 붕괴했다** — 0.5 m/s를 넘긴 시간이
**0.7%**다. 사실상 걷지 않는다.

> **판정: ✅ 강건성 확인(낙상 0). ❌ 대가 재확인 — 그리고 이건 두 번째다.**
> E2에서 이미 봤고, 같은 `_OFF_ROBUST` 7-레버 묶음을 flicker만 0.004→0.01로 올려
> 다시 켰더니 **같은 결과**가 나왔다. `ebatch.md` §3-3의 근본 원인(씻기지 않는
> 구간별 편향 → 느리게 가는 게 최적)이 재현으로 뒷받침된다.
> **`gbatch.md`가 G2에 기대한 "flicker만 바꿔 분리한다"는 설계는 실패했다** —
> flicker 외 6개 레버가 E2와 동일하게 켜져 있어 분리가 안 됐다.

---

### 2-3. G3_full — "통합 후보" → ❌ **완전 실패**

| 지표 | 값 |
|---|---|
| 위치 median | 49.0 cm |
| **성공(엄격)** | **0%** (전 카테고리) |
| **정상종료 비율** | **0.0%** |
| `stand` | **48.0 cm, 도달후이탈 100%** |
| `dof_pos_occupancy` | **0.9046** (배치 최고) |
| `bias_Hip_Pitch` / `bias_Knee_Pitch` | −0.1153 / **+0.1318** rad (6.6° / 7.6°) |
| 선택 ckpt | 5500 / 12000 — **후반 학습이 나빠짐** |

G3 = G1 + G2 + 보호항 + 스크립트 팔(armswing URDF) + `self_collisions: 1`.

**G1도 되고 G2도 되는데 G3가 안 된다** → gbatch §5-1의 정의대로 **상호작용이 원인**이다.
후보 셋:

1. **G2의 감속 압력 × G1의 속도 요구가 정면충돌.** G2는 "느리게 가라", G1의 path는
   "캐럿 속도를 맞춰라". 두 요구가 같은 정책에 걸리면 어느 쪽도 못 만족한다.
   `stand` 48 cm는 그 절충의 잔해로 보인다.
2. **`dof_pos_occupancy 0.90`** — 관절이 가동범위의 90%를 쓰고 있다. 보호항
   (`dof_pos_margin −0.5`, 한계 85% 초과분 페널티)이 켜졌는데도 이 값이라는 것은
   **보호 페널티와 과제 보상이 힘겨루기 중**이라는 뜻이다.
3. **스크립트 팔의 외란.** 2.9 kg 팔을 정책이 아니라 스크립트가 움직인다.
   `blend_tau_s=0.25`로 완화했지만 **정책 입장에서는 관측 불가능한 외란**이다
   (팔 관절은 관측 54차원에 없다). 몸통이 받는 반작용 토크를 정책이 예측할 수 없다.

> **판정: ❌ 기각.** 통합 후보로 쓸 수 없다. **H 배치는 G3를 그대로 승계하면 안 된다.**
> 원인 분리가 먼저다 → `hbatch.md`에서 팔 스크립트를 **분리 arm(H3)**으로 뺀다.

---

### 2-4. G4_smoothturn — "연속 turn을 감속 없이" → ⚠️ **판정 보류**

| 지표 | 값 |
|---|---|
| 학습량 | **4100 / 12000 iter (34%)** |
| 낙상 | **1016** ❌ (2위 후보 2600도 1081) |
| 위치 median | 42.0 cm |
| `stand` | 3.4 cm, 이탈 2% ✅ |
| `turn` | 39.2 cm, **이탈 100%** ❌ |
| 몸통속도 p99 | **3.02 m/s** (배치 최고) |
| stress jitter | 낙상 0, 직립 100% ✅ |

**낙상 1016회는 G1의 27배**다. 그런데 `stand`는 3.4 cm로 멀쩡하고 stress에서도 안 넘어진다.
즉 **정지 능력은 살아 있는데 이동 중 넘어진다.**

`v7_extras`: `path_lag_mean 0.2347`(배치 최고), `feet_width 0.1470`(배치 최고),
`torque_saturated 0.0052`(배치 최저).

> **판정: ⚠️ 보류 — 34% 학습으로는 판정할 수 없다.**
> 다만 `turn` 이탈 100%는 **G1이 dwell로 고친 바로 그 병리**다. v8의 순차 목표는
> **도달 조건을 만족해야 다음으로 넘어가므로 dwell 개념이 없다** — 목표가 계속
> 앞에 있다. SmoothTurn을 살리려면 **연쇄의 마지막 목표에 정지 조건을 명시**해야 한다.
> `hbatch.md` H2에 반영한다.

---

## 3. 지시 8 — 고속에서 상체가 기울어진다 (근본 원인)

관찰: "빠른 가속을 위해 상체가 기울어지는 것은 좋으나, 고속 안정성이 떨어진다."

**보상 구조가 아니라 gait 파라미터가 원인이다.** 정량:

```yaml
commands.gait_frequency: [1.8, 2.4]     # Hz, 균일 샘플
```
`goal_pose.py:446` — **명령 속도와 무관하게 독립 샘플링**된다.
`goal_pose.py:973-974` — 한 사이클에 좌/우 2보(swing at 0.25 / 0.75).

→ **보속 = 2 × gait_frequency = 3.6 ~ 4.8 보/초**

| 명령 속도 | 필요 보폭 @1.8 Hz | @2.4 Hz |
|---|---|---|
| 0.5 m/s | 0.139 m | 0.104 m |
| 1.0 m/s | 0.278 m | 0.208 m |
| **1.5 m/s** | **0.417 m** | 0.313 m |
| **1.8 m/s** (그리드 최고) | **0.500 m** | 0.375 m |

K1의 다리 길이는 `base_height_target = 0.52 m`. 이족보행에서 실현 가능한 보폭은
대략 **다리 길이의 0.5~0.6배 = 0.26~0.31 m**다.

> **1.8 Hz가 뽑힌 env에서 1.5 m/s 명령은 0.417 m 보폭을 요구한다 — 물리적으로 불가능하다.**
> 정책이 할 수 있는 유일한 대응은 **상체를 앞으로 던져 CoM을 지지면 밖으로 내보내는 것**
> (제어된 낙하)이다. 그게 관찰된 기울기다.
>
> 그리고 이것이 **추종비 붕괴의 원인이기도 하다.** G1의 실제 평균속도는 명령 0.4~1.9 m/s
> 전 구간에서 **0.32~0.49 m/s로 평평하다.** 명령을 올려도 실제가 안 오르는 게 아니라,
> **케이던스가 안 올라서 못 오른다.**

### 처방 (H 배치)

| 방법 | 내용 | 채택 |
|---|---|---|
| **케이던스-속도 커플링** | `f = clamp(v_cmd / (2·L_step_max), f_min, f_max)`, `L_step_max ≈ 0.28 m` → 1.5 m/s에서 2.68 Hz | ✅ **H0/H1/H2 전부** |
| 상한 확대 | `gait_frequency: [1.8, 2.4] → [1.6, 3.2]` | ✅ 위와 함께 |
| 고속 전용 자세 항 | 속도가 높을 때만 `orientation` 가중을 낮춰 기울기를 **허용**하되, `ang_vel_xy`(피치·롤 **각속도**)를 강화해 **기울기의 진동**을 벌한다 | ✅ H1/H2 |
| 뒷꿈치 접지 보상 (지시 13) | 아래 §4 | ⚠️ **H3 단독** |

> **핵심**: 사용자 요구 "기울기는 허용, 고속 안정성은 확보"를 그대로 옮기면
> **각도가 아니라 각속도를 벌하는 것**이다. 기울어진 자세로 **안정적으로** 있는 것과
> 기울기가 **떨리는** 것은 다르다. 현재 `orientation −20`은 각도를, `ang_vel_xy −0.2`는
> 각속도를 벌하는데 **비율이 100:1**이다. 이걸 뒤집는다.

---

## 4. 지시 13 — 뒷꿈치 접지 보상으로 해결되나

> 사용자 판단: "기존 외란을 조사했는데 충분히 큰 힘이었고 그래도 안정적이었다면
> 이런 하드코딩은 필요 없다. 애매하면 gait만 만진 hbatch3를 따로."

**전제 조건이 충족되지 않는다.** 외란은 **학습만 되고 평가된 적이 없다**
(`ebatch.md` §4-3). "그래도 안정적이었다"를 확인할 데이터가 0건이다.

힘의 크기 자체는 충분하다:
- 충돌 40~150 N (실측 최대 147 N) / 로봇 18.71 kg → **최대 7.9 m/s² ≈ 0.8 g**
- 지지 3~15 N / 1.5~3 s

즉 **크기는 충분한데 효과가 미측정**이다. 사용자의 조건문 그대로 → **애매하다** →
지시대로 **`H3_gait`를 따로 만든다.**

**뒷꿈치 보상 자체에 대한 평가:**
- ✅ 물리적으로 옳은 방향이다. 스윙 발 접지점이 CoM보다 앞이면 감속 모멘트가 생겨
  제어된 낙하가 멈춘다. 이족보행의 capture point 이론과 일치한다.
- ❌ **하지만 지시 8이 요구한 "빠른 가속은 유지"와 정면충돌한다.** 가속하려면 접지점이
  CoM보다 **뒤**여야 한다. 항상 앞을 요구하면 가속 자체를 금지하게 된다.
- ✅ **속도 조건부로 걸면 둘 다 만족한다**: `|v| > v_hi`일 때만 발동.
  가속 구간(저속→고속)에서는 자유, **정상 고속 주행에서만** 접지점 전방을 요구.

→ H3에서 이 조건부 형태로만 테스트한다. H0/H1/H2에는 넣지 않는다
(하드코딩이므로, 케이던스 커플링만으로 해결되면 불필요하다는 사용자 판단을 존중).

---

## 5. 지시 22 — 정옆/정뒤 목표에서 속도가 떨어진다

**보상 함수가 그렇게 시켰다. 정량적으로 증명된다.**

```python
d_con = goal_dist² + 2·r²·(1 − cos(heading_error))       # r = constellation_radius = 1.0 m
reward = 3.5 · exp(−0.2 · d_con)
```

로봇이 정옆 0.75 m 목표로 갈 때 두 가지 전략:

| 전략 | 진행 중 `d_con` 변화 | 결과 |
|---|---|---|
| **(A) 몸을 돌려 목표를 보고 걷는다** | heading_error가 90°로 벌어짐 → `2·1²·(1−cos90°)` = **+2.0** | 빠르지만 보상 급락 |
| **(B) 게걸음으로 옆으로 간다** | heading 유지 → 위치항만 **−0.5625** (0.75² 만큼 감소) | 느리지만 보상 상승 |

**전략 A의 손실 2.0이 전략 B의 이득 0.5625보다 3.6배 크다.**
→ 두 전략이 같아지는 거리는 `d² = 2.0` → **d = 1.41 m**.

> **정옆 목표가 1.41 m보다 가까우면, 보상 함수가 "돌아서 가기"를 금지한다.**
> 실제 `lateral` 카테고리의 시작거리 median은 **0.74~0.77 m** — 전부 금지 구간이다.
> 정뒤(180°)는 더 심하다: `2·(1−cos180°) = **4.0**` → 임계거리 **2.0 m**.

**"같은 방향으로 빠르게 잘 갈 때도 있음"의 설명**: 목표의 **목표 heading**이 이동
방향과 우연히 맞으면 전략 A의 손실이 사라진다. `combined` 카테고리(거리 1.35 m,
방향+heading 동시)에서 G1이 5.7 cm로 잘 하는 이유이기도 하다.

**증거 (E 배치 `segments.csv`, 오염 제외):**

| arm | lateral 시작거리 | lateral peak | straight 시작거리 | straight peak | peak/거리 비 |
|---|---|---|---|---|---|
| V7_full | 0.76 m | 0.29 m/s | 0.99 m | 0.49 m/s | **0.38 vs 0.49** |
| E1_path | 0.74 m | 0.67 m/s | 0.99 m | 0.94 m/s | 0.91 vs 0.95 |
| E2_robust | 0.77 m | 0.30 m/s | 0.98 m | 0.30 m/s | 0.39 vs 0.30 |

V7에서 거리당 속도가 lateral이 straight의 **78%**다. E1은 path 학습으로 전진 편향이
강해 차이가 작다. E2는 전방향 균등하게 느려 차이가 없다.

### 처방 (H 배치)

| 방법 | 내용 | 부작용 |
|---|---|---|
| **① `constellation_radius` 축소 1.0 → 0.5** | heading 항 가중이 **1/4**로. 임계거리 1.41 → 0.71 m | heading 정밀도 저하 위험. 단 현재 heading median 2.5°로 게이트(10°) 대비 4배 여유 |
| **② `heading_near_goal`로 전환** | 이동 중에는 heading 자유, **목표 근처에서만** 요구. `goal_pose.py:799`에 **이미 구현돼 있고 scale 0으로 꺼져 있다** | 원거리 heading 신호 소실 → constellation과 병용 |
| ③ 진행방향 정렬 보상 | 이동 중 몸통이 속도 벡터를 향하도록 | 새 항. 게걸음을 명시적으로 벌하게 됨 |

> **①이 최소 변경이고 ②는 이미 구현돼 있다.** H1에서 ①, H2에서 ①+②를 쓴다.
> **③은 채택하지 않는다** — 게걸음은 RoboCup에서 실제로 유용한 기동이고,
> 금지하면 안 된다. 문제는 "게걸음을 하는 것"이 아니라 "**돌아서 가는 게 유리할 때도
> 못 돌아서는 것**"이다.

---

## 6. 지시 9 — 외란 학습은 들어갔는데 괜찮았나

**측정된 적이 없다.** `eval_goal_pose.py:356-364`가 평가 시 외란을 강제로 끈다.

| 무엇 | 상태 |
|---|---|
| 충돌 외란 40~150 N 학습 | ✅ E2, V7, G2, G3에 적용 |
| 그 아래에서의 낙상률 | ❌ **측정 0건** |
| goal jitter 아래 낙상률 | ✅ G1~G4 전부 **0회, 직립 ~100%** |
| jitter 아래 \|ω\| p90 | G1 1.76 / G2 1.92 / G3 2.16 / G4 2.24 rad/s (정상 보행 1~2) |

**간접 증거로는 괜찮아 보인다**: G2(외란 학습)가 낙상 0회, G1(외란 미학습)이 38회.
하지만 이건 외란 아래 성능이 아니라 **무외란 과제에서의 낙상**이므로 직접 증거가 아니다.

> **지시 9 전반부("모든 버전에 외란·jitter 학습이 일부 필수")는 H 배치 전 arm에 반영한다.**
> **후반부("괜찮았나?")는 측정 도구가 없어 답할 수 없다** → `--stress push` 모드를
> 신설한다(`hbatch.md` §측정).

---

## 7. 대칭성 — `v7_extras` 최초 실측

| 지표 | G1 | G2 | G3 | G4 | E1 | 해석 |
|---|---|---|---|---|---|---|
| `feet_lat_offset` | **0.0002** | 0.0054 | 0.0006 | −0.0098 | −0.0123 | 사용자가 지적한 **좌우 중심 편향**. G1/G3는 사실상 0 |
| `feet_fwd_asymmetry` | 0.0447 | −0.0043 | 0.0177 | −0.0014 | 0.0006 | G1이 4.5 cm 전후 비대칭 |
| `bias_Hip_Yaw` | −0.0499 | 0.0461 | 0.0182 | 0.0183 | 0.0231 | **G1이 −2.9°로 최대** |
| `bias_Ankle_Pitch` | **0.1361** | −0.0431 | −0.0227 | 0.0361 | −0.0095 | **G1이 7.8°** |
| `bias_Hip_Pitch` | −0.1043 | −0.0159 | **−0.1153** | 0.0109 | −0.0036 | G1 −6.0°, G3 −6.6° |
| `bias_Knee_Pitch` | −0.0144 | 0.0505 | **0.1318** | −0.0124 | −0.0034 | G3 7.6° |

> **사용자가 원래 지적한 "right hip yaw가 몸 중심으로 치우침"은 `feet_lat_offset`에서
> G1이 0.2 mm로 해결됐다.** 그러나 **관절별 편향은 오히려 커졌다** — G1의
> `bias_Ankle_Pitch` 7.8°는 배치 최대다.
>
> **해석**: 대칭손실(`symmetry_coef 0.5`)은 정책 **함수**의 대칭성만 벌하고 결과 gait를
> 강제하지 않는다. 발 **위치**는 대칭이 됐지만 그것을 만드는 **관절 조합**은 좌우가
> 다르다 — 여러 관절 조합이 같은 발 위치를 만들 수 있기 때문이다(잉여 자유도).
> **H 배치 지시 15**(y축 대칭 data augmentation + mirror loss)가 정확히 이 지점을 겨냥한다:
> 손실 항이 아니라 **데이터 증강**으로 주면 좌우가 통계적으로 같은 것을 보게 되어
> 정책 함수가 아니라 학습 분포 수준에서 대칭이 강제된다.

---

## 8. G 배치 최종 판정표

| # | gbatch §5의 질문 | arm | 판정 | 근거 |
|---|---|---|---|---|
| 1 | dwell이 path의 waypoint 파괴를 고치나 | G1 | ✅ **성립** | `turn` 이탈 100% → 0%, 위치 52.1 → 5.5 cm |
| 2 | (속도×곡률) 그리드가 상한을 드러내나 | G1 | ❌ **기각** | 승급 기준이 leash에 의해 구조적으로 실패 불가. 30/30칸 1분만에 포화 |
| 3 | 속도를 올리면서 정확도를 지키나 | G1 | ✅ **성립** | p90 1.10 → 1.50 m/s(+36%), 위치 6.4 → 5.5 cm 동시 |
| 4 | 강건성의 비용은 얼마인가 | G2 | ✅ **측정됨, 비쌈** | 낙상 0회 ✅ / 1.0 m/s 초과 시간 17.7% → **0.5%** |
| 5 | flicker만 분리 측정 가능한가 | G2 | ❌ **실패** | 나머지 6개 레버가 E2와 동일하게 켜져 분리 안 됨 |
| 6 | G1+G2+보호항 통합이 되나 | G3 | ❌ **기각** | 성공률 0%, `stand` 48 cm |
| 7 | 연속 turn을 감속 없이 하나 | G4 | ⚠️ **보류** | 34% 학습. 낙상 1016회 |

---

## 9. H 배치가 승계할 것 / 버릴 것

| | 승계 | 근거 |
|---|---|---|
| ✅ | **G1 config를 기준선으로** | 유일하게 게이트 2/4 통과 |
| ✅ | dwell (`duration 3–5 s / interval 12–24 s`) | 병리 수리 확인 |
| ✅ | 가상 선도체 캐럿 | 텔레포트 소멸, 오프라인/실측 일치 |
| ✅ | 외란 + jitter 일부 (지시 9) | 단 **속도 유지 항과 반드시 함께** |
| ❌ | `speed_grid` 현재 형태 | 승급 기준 무효. **`keepup_gap_m`을 leash보다 작게** 재정의해야 함 |
| ❌ | G3 구성 그대로 | 성공률 0%. 팔 스크립트는 **단독 arm으로 분리** |
| ❌ | `_OFF_ROBUST` 7-레버 묶음 | E2·G2 두 번 같은 실패 |
| ⚠️ | G4 SmoothTurn | 연쇄 마지막 목표에 정지 조건 추가 후 재시도 |

**새로 필요한 것** (근거 있는 것만):

| 항목 | 근거 |
|---|---|
| `goal_reached` 공 내부 기울기 | `ebatch.md` §4-2 — 200배 비율 |
| 속도 지표 리셋 텔레포트 제거 | `ebatch.md` §4-1 — 17% 오염 |
| 케이던스-속도 커플링 | §3 — 1.8 Hz에서 1.5 m/s는 0.417 m 보폭 요구 |
| `constellation_radius` 1.0 → 0.5 | §5 — 임계거리 1.41 → 0.71 m |
| `--stress push` 평가 모드 | §6 — 외란 측정 0건 |
| `segments.csv` 항상 저장 | §0 — G 배치 재분석 불가 |
| y축 mirror data augmentation | §7 — 관절 편향 최대 7.8° 잔존 |
