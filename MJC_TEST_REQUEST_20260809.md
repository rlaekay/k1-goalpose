# MJC Tester 에게 — 실기 측방 불안정 재현 요청 (배포 세션, 2026-08-09)

배포 세션이다. 실기 보행이 **외부 지지 없이는 못 선다는 것이 정량으로 확정**됐고
(아래 §1), 원인 가설 넷을 세웠다(§2). **어느 것이 재현을 만드는지 MuJoCo A/B 로
갈라 달라.** 데이터는 전부 `realdata/` 에 커밋돼 있다.

## 1. 재현해야 하는 표적 (실기 실측, `realdata/2026-08-09_t_walk_i3b_run2.csv`)

`I3b_stance10 model_200`, `fixed 0.6,0,0`, 2.96 s, **사용자가 옆에서 잡아준 상태**.

| 지표 | 실측 | 비고 |
|---|---|---|
| 측방 capture 가 단일지지 한계(±4.4°) 밖 | **68 %** (단일지지 표본 기준) | capture = roll + gx·τ, τ=0.214 s |
| 측방 capture 가 양발 스탠스단(±15.2°) 밖 | **14 %**, \|max\| **32.3°** | **잡아주지 않으면 낙상** |
| 발목 roll \|dq\| p90 / max | **22.2 / 32.8 rad/s** (L) | 서기 대비 **33배**. URDF 속도한계 18 초과 |
| 발목 roll p2p | 0.94 rad | ROM(±0.40) 전체 + 오버슈트 |
| 힙롤 p2p / \|dq\| p90 | 0.19~0.26 rad / 1.27~1.54 | 서기 대비 6~7배, L/R 상관 −0.54 |
| L_Hip_Pitch 드리프트 | med −0.24 → **−0.43**, 마지막 −0.47 | 3 s 내내 단조. act0 도 동행 |
| 실효 보행 주파수 | **1.581 Hz** (지령 2.0) | **학습 대역 [1.8, 2.4] 밖** |
| 정책 tick | 41.1 Hz (설계 50) | |
| `pub_hz` | 183.7 (설계 500) → EMA fc **6.57 Hz** | |

⚠️ 읽기 전 `realdata/README.md` — 등간격 아님(CV 0.303), `t_s` 는 벽시계 아님.

## 2. 가설 넷 — 각각의 기전·정량 예측·판별 셀

### H1 — 보행 시계가 학습 대역 밖 (1.58 Hz < [1.8, 2.4])

기전: `Timer` 가 LowState 개수 × 0.002 라(§8-59) 시계 전체가 0.79 배로 돈다.
LIP 에서 단일지지 측방 발산은 e^{t/τ}: 반스텝 0.25 → 0.316 s 면 발산 배율
**3.21 → 4.38 (+36 %)**. 정책의 발 놓기 타이밍은 위상 기반이라 그대로인데 실측
진자 시간은 늘었다 — **스텝마다 측방 오차가 학습 때보다 36 % 더 자란 채 다음 발을 딛는다.**
예측: 시계만 0.79 배로 늦추면 측방 capture 위반과 발목 roll 폭주가 재현된다.

### H2 — 실효 구동 지연 ~36 ms (학습 모델 0~18 ms)

기전: tick/2 12.2 ms + EMA(fc 6.57 Hz) 위상지연 23.5 ms ≈ **35.6 ms**.
§8-40 스윕에서 지연 20 ms 는 2낙상/분, 40 ms 는 49낙상/분이었다 — **35.6 은 그 사이**다.
예측: 액션 지연 35 ms 를 넣으면 측방부터 무너진다(측방이 가장 빠른 축).

### H3 — armature 0 학습 → 실물 발목 관성에서 링잉

기전: sim 발목 관성 0 이라 빠른 roll 명령이 공짜였다. 실물(벤더 0.0565)에서는
그 명령이 진동 응답을 만든다. §8-52/§8-56 의 물리 어긋난 칸 발 교차(−17.1 cm)가
실기 재생 p1(−16.35 cm)과 일치했던 것과 같은 뿌리.
예측: 발목 armature 0.0565 만 넣어도 발목 roll \|dq\| 가 커진다.

### H4 — L_hip 단조 드리프트 = 외부 힘(손)에 대한 순응 (또는 영점 δ)

기전: 사용자가 잡고 있으면 정책은 tilt≈0 을 보면서 정체불명의 힘을 받는다 —
적분처럼 자세를 밀 수 있다. 또는 관절 영점 δ 의 비대칭.
예측: MuJoCo 에서 몸통에 지속 측방/전방 외력(10~30 N)을 걸면 hip pitch 가
단조 드리프트한다. 외력 없이 재현되면 δ 쪽(→ R7).

## 3. 요청 셀 (A/B, 한 셀 한 레버)

| 셀 | 조건 | 가설 |
|---|---|---|
| **C0** | 기준: 올바른 타이밍(50 Hz tick, 2.0 Hz, 필터 없음) | 대조군 — 걷어야 함 |
| **C1** | 시계 0.79 배 (tick 25.3 ms, gait 1.58 Hz) | H1 |
| **C2** | 액션 1차 지연 35 ms (또는 EMA fc 6.57 Hz @185 Hz) | H2 |
| **C3** | 발목 armature 0.0565 (나머지 0) | H3 |
| **C4** | C1+C2 동시 (= 실측 배포 조건) | H1×H2 |
| **C5** | C0 + 몸통 지속 외력 20 N 측방 | H4 |

도구는 그쪽이 이미 갖고 있다: `play_mujoco_goalpose.py`(배포 필터 지원, `7fba8f9`),
`tools/mj_sense_sweep.sh`, `tools/replay_real_in_mujoco.py`, MJC 발목 armature 강제(`117414c`).

## 4. 판정 사전등록

각 셀에서 **실기와 같은 지표**를 내라: 측방 capture 위반율(단일지지 ±4.4° / 양발 ±15.2°),
발목 roll \|dq\| p90, 힙롤 p2p, L_hip 드리프트, 낙상/분.

- **재현 성공** = 측방 capture 단일지지 위반 ≥ 50 % **그리고** 발목 roll \|dq\| p90 ≥ 10 rad/s
- C0 이 이 기준에 걸리면 → 정책 자체 문제, 타이밍 가설 전부 기각
- C1/C2/C3 중 하나만 걸리면 → 그 레버가 원인. **배포에서 그 레버만 고치면 된다**
- C4 만 걸리면 → 상호작용. 시계와 필터를 같이 고쳐야 한다
- C5 에서 L_hip 단조 드리프트가 나오면 → 실기 드리프트는 손 때문. 안 나오면 δ(→ R7)

⚠️ 낙상 수를 그대로 비교하지 마라 — 실기는 **잡아준 상태**라 낙상이 인위적으로 0 이다.
비교 축은 capture 위반율이다. 영상 규칙(2분) 적용— 판정 지는 셀은 반드시 렌더.

결과는 이 파일 아래 `## 결과 (MJC)` 절에 달아 달라.

---

## 결과 (MJC) — 2026-08-09, MJC 세션

**한 줄: 사전등록 기준을 넘은 셀이 없다. 그리고 그 기준으로는 원래 아무것도 못 가른다.**

자산 `K1_serial_realmass.xml`(19.666 kg), 정책 `I3b_stance10 model_200`, `--goal-hold`
120 s, seed 0. armature 는 셀 레버가 아니라 **바닥 물리**이므로 C3 를 뺀 전 셀에서
`vendor` 로 고정했다(고정 안 하면 학습 세션이 잰 1.5 s ↔ 3,740 s 스윙이 전 셀에 섞인다).
영상 전부 `logs/mujoco/mjc_request_C/`.

| 셀 | 낙상 | cap 단일지지 | cap 단일지지(직립) | **AnkR \|dq\| p90 L/R** | 발간격 min | L_hip δ |
|---|---:|---:|---:|---:|---:|---:|
| **C0_base** (대조) | 0 | **46.4 %** | 46.4 % | **4.41 / 4.22** | +6.5 cm | −0.006 |
| C1_clock079 | 0 | 79.4 % | 79.4 % | 4.19 / 4.57 | +0.4 cm | −0.012 |
| C2_actlag35 | 37 | 80.2 % | 78.5 % | 4.61 / 6.68 | −11.4 cm | −0.015 |
| C2b_ema185 | 0 | 70.2 % | 70.2 % | 2.71 / 2.79 | +7.5 cm | −0.010 |
| C3_ankarm | 0 | 54.6 % | 54.6 % | 4.00 / 3.41 | +7.4 cm | −0.019 |
| C4_clock_lag | 75 | 82.2 % | 79.8 % | 6.31 / 5.69 | −13.5 cm | +0.006 |
| C5_force20 (측방) | 29 | 68.9 % | 67.6 % | 7.30 / 5.03 | −7.4 cm | −0.004 |
| C5b_force20 (전방) | 40 | 71.5 % | 69.8 % | 7.10 / 6.53 | −8.5 cm | +0.011 |
| C6_armzero (괄호용) | 40 | 81.9 % | 81.0 % | **88.84 / 85.24** | −31.6 cm | −0.049 |
| **실기 표적** | (잡아줌) | **68 %** | — | **22.2** | −16.4 cm | **−0.19** |

### 1. ⛔ 재현 성공 셀 없음 — `AnkR |dq| p90 ≥ 10` 을 넘은 셀이 하나도 없다

최대가 C5 의 **7.30** 이고 표적은 **22.2**, 3 배 부족하다. H1·H2·H3·H4 전부 이 조건에서 탈락한다.

### 2. ⛔ capture 위반율은 판별 지표로 쓸 수 없다 — **대조군이 이미 걸린다**

**C0(완벽한 타이밍, 필터 없음, 낙상 0)이 46.4 %** 다. 실기 68 % 의 2/3 이고, 전 레버를
켠 C4 가 82.2 % 다. ⇒ 실기 값이 **정상 구간 한복판**에 있다. "±4.4° 위반 68 % ⇒ 외부
지지 없이는 못 선다"는 이 기준선 위에서 성립하지 않는다 — 120 초 무낙상으로 걷는
정책이 단일지지 표본의 46 % 에서 그 선을 넘는다.

⚠️ **그리고 §4 의 "낙상 수 비교 금지, 비교 축은 capture 위반율" 은 성립하지 않는다.**
낙상하는 셀은 넘어지는 동안 capture 를 자동으로 위반하는데 실기는 잡아준 상태라
낙상이 0 이다. 그대로 비교하면 낙상 수 비교를 이름만 바꿔서 하는 것이다. 위 표의
`직립`(tilt<15°) 열이 그 오염을 뺀 값이다 — 그래도 결론은 안 바뀐다.

⚠️ **분모 미정의**: 실기 로그에 접촉 센서가 없는데 표적이 "단일지지 표본 기준" 이다.
무엇으로 갈랐는지 문서에 없어 MuJoCo 쪽은 네 분모를 전부 냈다(json `capture` 참조).

### 3. ⭐ H2 의 두 구현이 갈린다 — "실효 지연 35 ms" 등가 서술이 틀렸다

`--act-lag-ms 35`(순수 전달지연) = **37 낙상**, `--deploy-filter --filter-hz 185`
(실제 배포 코드 경로, fc ~6 Hz) = **0 낙상**. EMA 는 지연만 만드는 것이 아니라 고주파를
깎아서 **오히려 안정화**한다. H2 를 순수 지연으로 모델링하면 **손해를 과대평가**한다.

### 4. ⭐ H4 기각 → δ (R7)

측방 20 N 도 전방 20 N 도 L_hip 단조 드리프트를 못 만든다(δ −0.004 / **+0.011**, 대조군
−0.006). 실기 **−0.19 rad** 는 10 배이고 전방 힘은 **부호가 반대**다.
⇒ §4 사전등록대로 **손 때문이 아니다. 영점 δ 쪽(R7)이다.**

### 5. ⭐⭐ 발목 roll 은 armature 가 단독 지배하고, **22.2 를 내는 값은 없다**

타이밍 레버를 어떻게 흔들어도 4~7 rad/s 인데 armature 하나가 20 배를 움직인다.
그래서 **실기 값으로 역산을 시도했고, 실패했다** — 응답이 단조가 아니라 **0 에서의 절벽**이다:

| 발목 armature | 0.0 | 0.0035 | 0.0071 | 0.0141 | 0.0283 | 0.0565 |
|---|---:|---:|---:|---:|---:|---:|
| AnkR \|dq\| p90 L | **88.84** | **3.02** | 3.21 | 3.69 | 4.12 | 4.41 |

벤더값의 1/16 만 넣어도 88.8 → 3.02 로 무너지고, 그 뒤로는 armature 가 늘수록 **증가**한다.
⇒ **22.2 를 내는 발목 armature 는 존재하지 않는다.**

⛔ **부수 확정: `armature = 0` 은 수치적으로 퇴화한 발목이다.** 그리고 그것이 **Isaac
학습 config 값**이다(`asset.armature: 0.0`). 정책은 발목이 88 rad/s 로 공짜로 도는
세계에서 배웠다. 발간격 min 도 그 칸이 **−31.6 cm** 로 전 셀 최악이다.

### 6. ⚠️ 실기 `AnkR |dq| p90 = 22.2` 자체가 미해결로 남는다 — **내가 못 닫았다**

시뮬이 못 내므로 실기 값을 봤다. 확정된 것과 아닌 것을 나눈다.

**확정:**
- `dq` 는 `q` 의 진짜 미분이다(보고 p90 23.3 대 q 차분 21.9). 조작된 열이 아니다.
- **발목 roll 2채널만 이상하다.** 한 틱 최대 이동이 다른 10 채널은 ≤ 0.263 rad 인데
  발목 roll 은 **0.721 / 0.747** 이고, `Δq > 0.2 rad` 인 틱이 다른 채널 0~2 회 대
  **51 회 / 24 회** 다.
- 물리 스톱(±0.40) 밖 표본이 **5 / 3 개**, 최대 초과 **8.6°**. 그만큼은 비물리다.

**확정 못 한 것 (내 가설 셋이 다 죽었다):**
- ⛔ *"평행기구 A/B 스큐"* → **기각.** 모터공간(p±r)으로 바꾸면 오히려 더 나쁘고
  (0.746/0.734), 한 표본 지연 보정도 안 먹는다.
- ⛔ *"벤더 속도한계 17.59 를 넘으니 비물리"* → **틀린 논증.** 정격 속도는 모터가
  **낼 수 있는** 값이지 **역구동으로 밀릴 때**의 한계가 아니다. 발목 roll 은 지면
  충격에 역구동되는 축이다.
- ⛔ *"스톱 위반이 도약의 원인"* → **아니다.** 큰 도약 51 회 중 스톱밖 표본에 인접한
  것은 **16 %** 뿐이다.

⇒ 남은 가능성은 **25 ms 표본에서 엘리어싱된 실제 역구동**이고, 나는 그것을 배제할
수단이 없다. **이 로그로는 못 닫는다.**

> **닫는 측정 (배포 세션에 요청)**: 발목 roll `q` 를 **LowState 율(≈380~500 Hz)로
> 3 초** 남겨 달라. 40 Hz 표본이 신호를 담는지가 그것 하나로 갈린다. 엘리어싱이면
> 고율 로그는 매끄럽고, 채널 결함이면 고율에서도 도약이 남는다.
> **그 전까지 `AnkR |dq|` 를 판정 축으로 쓰면 안 된다** — 어떤 시뮬 설정도 못 맞추고,
> 못 맞추는 이유가 정책인지 계측인지 아직 모른다.

### 6-b. ⭐⭐ 추가: 낙상 **방향**을 물리 스텝 해상도로 쟀다 — 그리고 전제가 틀렸다

앞선 표는 낙상 여부만 봤다. `--goal-hold` 영상 셋이 실기 붕괴와 비슷해 보인다는
지적이 있어 **방향**을 쟀다. ⛔ 먼저 계측 결함 하나를 고쳤다: `--dump-csv` 는 정책
tick(50 Hz)에만 쓰는데 낙상 판정은 물리 스텝(500 Hz)에서 돌고 **즉시 리셋**되어
**75 회 중 2 회만** 잡혔다. 링버퍼로 바꿔 **기록 = falls 를 전 셀에서 확인**했다
(37/37, 75/75, 29/29, 40/40, 40/40).

| 셀 | 낙상 | 측방 비율 | \|roll\|@20° | \|pitch\|@20° | 10°→낙상 | 낙상 전 발 교차 |
|---|---:|---:|---:|---:|---:|---:|
| C0_base / C1_clock079 | **0 / 0** | — | — | — | — | — |
| **C2_actlag35** | 37 | **81 %** | **19.1** | **5.9** | 0.67 s | **100 %** |
| C4_clock_lag | 75 | 51 % | 14.2 | 13.8 | 0.40 s | **99 %** |
| C5_force20 (측방) | 29 | 62 % | 17.0 | 10.6 | 0.38 s | 48 % |
| C5b_force20 (전방) | 40 | 50 % | 13.7 | 14.3 | 0.35 s | 52 % |
| C6_armzero | 40 | 57 % | 15.1 | 12.9 | 0.34 s | 57 % |
| **실기 붕괴 (tilt max 22.6°)** | — | **56 %** | **12.2** | **19.2** | — | (재생 p1 −16.4 cm) |

**① 방향은 셀을 잘 가른다.** C2(액션 지연)만 `|roll|/|pitch| = 3.2배`로 뚜렷한 측방이고
나머지는 전부 반반이다. **② 발 교차 선행도 가른다** — 타이밍 레버(C2·C4)는 낙상 전에
발이 **거의 항상** 교차하는데(100 %/99 %), 물리·외력 레버는 **절반**(48~57 %)이다.

**③ ⛔ 그런데 실기가 측방이 아니다.** 같은 tilt 수준(20~22°)에서 실기는
`|roll| 12.2 < |pitch| 19.2` 로 **전방 우세**이고, 상위 표본에서도 측방 우세가 56 % 로
반반이다. ⇒ *"발끼리 부딪혀 넘어졌다 ⇒ 측방"* 이라는 **전제가 자세 데이터로 지지되지
않는다.** 그리고 이 축에서 **C2 는 가장 잘 맞는 셀이 아니라 가장 어긋난 셀**이다(반대 방향).

**④ ⭐ 대신 두 실기 실행을 실제로 가르는 것이 나왔다 — 정적 자세 편향이다.**

| | roll median | pitch median | tilt median |
|---|---:|---:|---:|
| **붕괴** | **+3.47°** | **−1.86°** | 9.53° |
| **9걸음 성공** | −1.04° | +0.66° | 3.13° |

**두 축 모두 3 배**다. 이것이 §8-63/§8-66 의 **진입 자세** 서명이고(`hip+knee+ankle=0`
수정으로 tilt median 4.9~13.8° → 1.4°), **내 셀 어느 것도 이 편향을 갖고 있지 않다** —
전부 올바른 자세에서 출발한다. ⚠️ roll/pitch **부호 규약을 확인하지 않았으므로**
"앞으로" 인지 "뒤로" 인지는 말하지 않는다. 크기만 말한다.

> **⇒ H1~H4 가 재현을 못 만드는 이유의 유력 후보**: 실기 붕괴와 실기 성공을 가른 것이
> **정적 자세 편향**인데 네 가설 중 아무도 그것을 모델링하지 않는다.
> **다음 셀 제안**: 자세 편향(`pitch_chain` 오프셋 또는 상시 IMU 바이어스)을 단독
> 레버로 넣고 C0 에 얹는다. 그것이 측방/전방 서명과 발 교차를 동시에 만들면 재현이다.

### 7. 그래서 이 요청에서 살아남는 방향

- **armature 가 유일하게 실기 서명(발 교차 깊이)을 움직이는 축이다.** 학습 세션의
  −17.1 cm ↔ 실기 재생 −16.35 cm 일치를 MJC 에서도 재현했다(C6 −31.6 cm, C4 −13.5 cm,
  타이밍 맞는 칸 **양수**). **Isaac 의 `asset.armature: 0.0` 이 1순위 재학습 후보다.**
- 시계 팽창은 별도 판정이 붙어 있다 — `ibatch` 의 시간 팽창 2×2 절 참조. 요약:
  제어율이 같고 시계만 다른 두 칸이 **22 대 0 낙상**으로 갈린다(시계 쪽). 다만 실기는
  21 % 팽창에서 9걸음 무낙상을 갔고, 그 실행이 2.96 초뿐이라 **검정력이 없다**
  (MuJoCo 율이 맞아도 무낙상 확률 0.38). **30 초 연속 무낙상이면 p < 10⁻⁴ 로 갈린다.**
