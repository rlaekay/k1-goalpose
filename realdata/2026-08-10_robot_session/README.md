# 2026-08-10 실기 세션 — 되찾을 수 없는 측정

빌드: `deploy_goal_pose.py` sha256 `2f51828a92ac967a…` (발목 관측 게이트 + 시계 레버 포함).
정책률 50 Hz 실측, 케이던스 2.00 Hz(구 빌드 1.58 에서 고쳐짐).

| 파일 | 조건 | 유효 구간 | ⛔ 주의 |
|---|---|---|---|
| `ankroll_hirate_500hz.csv` | 읽기전용, 정지 30 s, **499.7 Hz** | 전체 | 관절이 안 움직여 "엘리어싱 대 채널결함" 판별 **불가** |
| `i3b_A_aborted.csv` | I3b, `fixed 0.6,0,0`, **토크박스 15** | **첫 ~1.5 s 만** | ⛔ 사용자가 ~2 s 에 PREP 로 전환. **그 뒤 28 s 는 로봇 정지 상태의 로그다** |
| `i3b_A2_boxoff.csv` | I3b, 같은 조건, **토크박스 OFF** | **첫 ~1.5 s 만** | ⛔ 같은 이유 |
| `nq_stand.csv` | **NQ_armvendor**, `fixed 0,0,0` 제자리 | 0.88 s 전체 | 사용자가 0.5 s 에 중단 |

## ⛔⛔ 이 로그를 읽는 사람에게 — 오늘 내가 한 실수

`i3b_A_aborted.csv` 의 전체 통계를 인용해 **"30초 완주"** 라고 보고했다. 틀렸다.
`[test-timeout] policy ran 30.00 s` 는 **벽시계**이고, 로봇은 PREP 로 넘어간 뒤에도
우리 프로세스가 계속 돌며 로그를 채운다. **95 %가 정지 구간이었다.**

그리고 `q` 가 언 것을 "토크 박스가 발목을 굶겼다" 로 읽었다. **12관절을 같이 봤으면
전부 얼어 있었다**(83~89 %) — 발목이 아니라 로봇이 멈춘 것이었다.

⇒ **규칙: 이 디렉터리의 로그는 "관절이 실제로 움직이는 구간"을 먼저 잘라내고 통계를 내라.**
판정 기준: 0.5 s 창에서 12관절 |Δq| 합 > 0.05 rad.
