# 철회·정정 대장

**이 파일을 읽지 않고 비교 주장을 하지 마라.** 여기 있는 주장을 다시 세우려면
왜 이번엔 다른지를 먼저 써야 한다.

---

## 왜 이 파일이 있는가

2026-08-07 하루에 주장을 **열세 번** 바꿨다. 무작위가 아니었다 — 전부 같은 모양이다:

> **비교 대상 두 쪽의 조건이 같은지 확인하기 전에 결론을 말했다.**

| 실제로 어긋난 조건 | 그래서 나온 틀린 주장 |
|---|---|
| 체크포인트 (best vs final) | "N0_ctrl이 세 축 모두 지배" |
| 표본 밀도 (2점 vs 8점) | "무릎이 아니라 절벽" |
| 키의 의미 (간격 vs 주기) | "ND_dwell duty 40%" |
| 파일 vs 심볼릭 링크 | "N1 최종 정책에서 출발" |
| 프로토콜 길이 (60s vs 120s) | 낙상 "1,177 대 10 = 118배" |
| 지표의 집계 범위 (마지막 1프레임) | "발 간격은 문제없다" |
| 과제 (도착 있음 vs 없음) | "속도 요구를 충족했다" |

**한 가지 규칙이 열세 개를 전부 막는다**:
비교 주장을 하기 전에 **양쪽의 조건을 나란히 찍어 보고 다른 항목이 없는지 확인한다.**
그 확인을 사람 기억에 맡기지 않으려고 `tools/claim_check.py` 를 만들었다.

---

## 대장

상태: **유효** / **정정**(방향은 맞고 숫자·귀속이 틀렸다) / **철회**(결론 자체가 틀렸다)

### C1. "우리 과제(waypoint)는 보행을 요구한 적이 없다" — **유효 (귀속 정정)**
- 근거: `required_speed_median` 0.119 m/s, `body_speed.median` 0.03, 순항 체류 0.59 %
- ⚠️ 그 표의 숫자는 `I0b_foot/model_200` 의 것이고 **배포 정책의 것이 아니다.**
  배포 정책 자신의 값은 `logs/stance10_eval/report.json` 에서 2.53 cm / 낙상 **2**다.
  §8-47이 "자기 자산으로만 채점돼서 낙상 4건"이라 쓴 그 4건은 다른 정책 것이다.
- 결론 자체는 여러 arm에서 재현된다(요구 속도 0.115~0.126).

### C2. "배포 정책을 실물 자산으로 채점하면 낙상률 0.368 / 0.604" — **유효 (단서 셋)**
- config diff로 **평가 동역학에서 다른 것이 `asset.file` 하나뿐**임을 확인했다.
- ⚠️ ① "실물 자산 arm **전부** 0.0005~0.005"는 **M배치 + N0만**이다.
  `N1_path`(0.0238) `N2_pathgrid`(0.0354) `N8_pathdelay`(0.0886)도 실물 자산이다.
- ⚠️ ② I3b는 **구간 분포가 4~5배 쉽다.** 넘어지면 목표 근처에서 리셋되므로 짧은
  구간이 늘어난다 — `required_speed_median` 0.0252 대 다른 arm 0.099~0.125.
  **그래서 I3b의 정확도 3.10 cm를 다른 arm과 나란히 순위 매기면 안 된다.**
- ⚠️ ③ 두 URDF의 `effort` 값이 28개 다르다. `torque_limits`가 그걸 싣고 토크를
  하드 클립하므로 **포화 한계가 학습 때와 다르다**(I3b 7.75 % 대 나머지 1.7~2.5 %).

### C3. "N0_ctrl이 정확도·속도·낙상 세 축 모두에서 path arm 전부를 지배한다" — **철회**
- 원인: **정확도 행만 체크포인트가 달랐다.** N0는 `best(2200)`, N1/N2는 `model_6000`.
- 같은 `model_6000` 으로 맞춘 뒤(2026-08-07 22:xx 측정):

  | | 정확도 | 보행 속도 | 보행 낙상률 |
  |---|---:|---:|---:|
  | N0_ctrl | **3.63 cm** | 1.10 | 0.00347 |
  | N1_path | 47.70 | **1.48** | **0.00066** |

- **최종: 지배는 정확도 한 축에서만 성립한다.** 속도와 보행 낙상은 path가 이긴다.

### C4. "path는 더 빠르면서 덜 넘어진다. 대가는 2.8 → 7.9 cm뿐" — **철회**
- 같은 체크포인트로 맞추면 **3.63 → 47.70 cm**다. v7 배치(6.3 → 37.9)보다 **나쁘다.**
- "dwell이 충돌을 줄였다"는 서사는 이 숫자 위에서 성립하지 않는다.

### C5. "무릎이 아니라 절벽이다" — **정정**
- 정밀 스캔(100 iteration 간격)에 **해상 가능한 중간점이 있다**: N1 it200 24.5 cm,
  it300 37.6 cm / N2 it200 15.7 cm, it300 35.0 cm. 불연속이 아니라 **급경사**다.
- "그 뒤 평평"도 틀렸다: N1 it900 39.9 → it6000 47.7 (**+20 %**),
  N2 it900 30.6 → it6000 42.2 (**+38 %**).
- **쓸 수 있는 체크포인트가 없다는 결론은 유효하다** (전부 게이트 5 cm의 3배 이상).

### C6. "속도와 정확도가 충돌한다(트레이드오프)" — **철회**
- N1 it100~500: 속도 1.15 → 1.22로 **평평한데** 정확도만 7.8 → 39.7로 무너진다.
  속도 이득(1.32~1.38)은 **it600 이후**에 온다.
- **교환이 아니라 순차적 소실이다.** 정확도는 속도가 오기 400 iteration 전에 사라진다.
- 그리고 `failure_modes`가 그 정체를 말한다 — N1 model_6000은 **62 %가 목표에 한 번도
  도착하지 않고 27 %는 도착했다 떠난다.** 정확도 열화가 아니라 **정지 행동의 소멸**이다.
- ⇒ 맞는 처방은 보상 튜닝이 아니라 **2단계 학습**이다.
- ⚠️ N2는 또 다르다: it300~900에서 **속도까지 0.99 → 0.36으로 무너진다**(1.0 m/s 초과 0 %).
  `speed_grid`가 정책을 정지 상태로 몰아넣는 구간이 있다.

### C7. "ND_dwell의 dwell duty를 18 % → 40 %로 올렸다" — **철회 후 수정**
- `interval_s`는 dwell **사이의 간격이 아니라 주기**다(`goal_pose_v7.py:317`의
  `path_dwell_next`가 dwell 중에도 감소한다). duty = duration / interval.
- 내가 잡은 `[4,8]`은 40 %가 아니라 **66.7 %**였고, max duration 5 > min interval 4라
  **dwell 중에 다음 dwell이 발화해 영구 dwell이 되는 env가 약 6 %** 생겼다.
- `[8,12]`로 고쳤다(duty 40.0 %, 겹침 없음). **arm이 돌기 전에 고쳤다.**

### C8. "N4_hist는 obs 폭이 달라 warm start가 불가능하다" — **철회**
- `tools/expand_checkpoint.py`가 첫 층만 넓히고 새 열을 0으로 채운다.
  float64 검증에서 actor 최대차 0.0, critic 2.8e-14 — **대수적 항등**이다.

### C9. "관측 arm들은 N1_path의 최종 정책에서 출발한다" — **철회**
- 코드는 `best.pth`를 썼고 그건 `--link_best`가 매번 다시 만드는 **심볼릭 링크**이며
  **`model_100`**(붕괴 전)을 가리킨다. `NA_histzero` `NZ_zeroiid` `NC_actfilter`
  세 arm이 이미 그 계보로 돌았다.
- 출발점이 **가변**이라 재현도 불가능했다. `WARM_IT`로 iteration 번호를 못 박았다.
- ⭐ 다만 그 사고가 **계보 B**를 만들었고, 그 대조군 `NE_ctrl100`을 세워 살렸다.

### C10. "47.70 cm는 오염된 스모크 채점값이다" — **철회**
- 오염됐다는 판단(어느 체크포인트인지 통제되지 않음)은 맞았지만, **값 자체는 진짜
  `N1_path/model_6000`의 것**이었다. 나중에 별도로 잰 값과 모든 유도 지표까지 동일하다.
- ⚠️ 같은 디렉터리의 `.walk` 쪽은 **`N2_pathgrid/model_200`**이다 — 한 행 안에서
  두 축이 서로 다른 정책이었다. 둘 다 `logs/eval_rounds/_contaminated/`로 격리했다.

### C11. "발 간격이 모든 arm에서 16~19 cm라 실기 문제는 sim에서 재현되지 않는다" — **철회**
- `v7_extras` 전체가 **롤아웃 마지막 한 프레임의 스냅샷**이다(arm당 표본 1).
  같은 필드에서 `bias_Hip_Roll`이 부호까지 뒤집힌다. **실기 대조에 쓰면 안 된다.**

### C12. "N2_pathgrid it5100(1.64 m/s)이 속도 요구를 처음 충족했다" — **정정**
- `N1_path` it900이 **1.38**로 먼저 G2(1.3)를 넘겼고 it1700이 1.44다.
  1.64는 **최초가 아니라 최댓값**이고 단일점·단일 seed다.
- ⚠️ 그리고 `forward_hold`의 `body_speed`는 사용자 요구와 **같은 양이 아니다.**
  이 프로브는 도착과 정지를 아예 없앤다. G1의 절반을 제거한 조건에서 잰 순항 속도다.

### C13. "GPU 유휴 감시가 작동한다" — **철회 후 수정**
- `pgrep -f`가 명령줄에 문자열이 든 **셸까지 세서** `nproc_gpu`가 0이 될 수 없었다.
  유휴가 **원리적으로 감지 불가**였고 autopilot 승격이 13시간 동안 0회였다.
- 실행파일 기준 계수로 고쳤고, **2026-08-07 22:00에 첫 승격이 실제로 일어났다**
  (`040-N7_critic`). 카드별 유휴 판정도 추가했다(한 장만 노는 상태가 안 잡혔다).

### C14. "낙상 1,177 대 10 = 50배" — **정정**
- 두 리포트의 `duration_s`가 **120 s 대 60 s**다. 원시 개수 비는 118배, 비율 비는 57.6배.
- `round_table.py`가 이제 원시 개수 대신 **낙상간격(초)**을 찍는다.

---

## 아직 근거불충분인 것 (주장하지 마라)

- **"path가 속도에서 확실히 이긴다"** — N0에는 파레토 스캔이 없다. N0 2점 대 N1 8점
  비교다. 확정된 것은 *model_6000 지점에서* 1.35배라는 것뿐이다.
- **"NA_histzero가 안 무너진 것은 이력+영점 덕이다"** — 대조군 `NE_ctrl100`이 아직
  돌고 있다. 붕괴가 레버 덕인지 **출발점(model_100) 덕인지** 못 가른다.
- **NA/N4/N5/N6의 결과 해석** — 대칭 손실이 확장 관측을 안 덮는 결함 위에서
  `NA_histzero`가 이미 완주했다. 그 결과는 "이력+영점"이 아니라
  "이력 + 깨진 대칭 손실"을 잰 것이다.
- **실기 필터(`pub_hz`) 서사 전체** — `pub_hz`가 **한 번도 측정된 적이 없다**(R6).
  MuJoCo의 "50 Hz면 66/68 낙상"은 강제한 조건이지 측정한 조건이 아니다.

---

## 2026-08-07 23:xx 라운드 — 7축 독립 감사 (`AUDIT_20260807_2300.md`)

### C15. "평행 발목 토크 변환 부재가 `base_walk` 반증을 설명하는 유일한 구조적 차이" — **철회**
- 두 스크립트 **모두** `cmd_type = SERIAL` 로 발행한다(`deploy/utils/command.py:19`).
  **직렬→평행 변환은 로봇이 한다.** SDK 헤더 `LowCmd.h:94` `enum CmdType`, 벤더 예제
  (같은 자세 SERIAL `(−0.1,0)` vs PARALLEL `(0.137,0.125)`), Booster Gym 논문의 배포 절
  (*"feedforward torque are set to zero"* = **지금 `goal_pose` 기본 경로**), 실기 발목
  추종오차 0.026 rad(변환 없으면 0.1–0.24) — 네 갈래가 같은 방향.
- "유일한 차이"도 거짓: 게인 100/2 대 **200/5**, ankle kd 1 대 **3**, 관절수 22 대 **23**,
  `parallel_mech_indexes` **T1 레이아웃** 등 10개 넘는다.
- ⇒ **`Base_Walk.yaml` 은 이 로봇의 config 가 아니다.** `FILTER_AUDIT` §4 의 반증 전체가
  존재하지 않는 대조에 기대고 있었다.

### C16. "발행 루프의 두 번째 `sleep(0.001)` 이 발행률 상한을 만든다" — **철회**
- 루프는 자유 실행이 아니다. `Timer` 가 **벽시계가 아니라 카운터**이고
  `tick_timer_if_sim()`(조건 없음)의 호출 지점이 `_low_state_handler` **한 곳뿐**이다.
  sleep 은 메시지 사이 대기일 뿐이다.

### C17. "`[joint-layout] … matches hardware` 로그가 SDK 관절공간 변환을 증명한다" — **철회**
- `deploy_goal_pose.py:684` 가 `sorted(observed)` 가 아니라 `sorted(configured)` 를 찍고,
  가드가 `if observed and …` 라 **`observed` 가 비어 있어도 같은 줄이 나온다.**
  결론(SDK 가 변환한다)은 C15 의 네 증거로 살지만 **이 근거는 아무것도 증명하지 않았다.**

### C18. "낙상하면 목표 근처에서 리셋돼 구간 분포가 4~5배 쉬워진다" — **철회** (C2 ② 정정)
- 목표는 로봇 로컬 프레임에서 뽑히므로 **거리 분포 불변**이고 구간 duration 도 같다
  (I3b 5.98 s vs 타 arm 5.88–6.00). **시도 기준** 유형 구성은 12개 arm 전부 config 와
  1.5 pp 이내 일치한다.
- 난이도 이동은 전적으로 **어떤 구간이 살아남는가** 때문이다 — I3b 완주율
  stand 99.1 % / turn 94.0 % / lateral 64.7 % / straight 53.4 % / **combined 37.9 %**.

### C19. "`N0_ctrl model_6000` 의 정확도는 측정된 적이 없다" — **해소**
- **3.63 cm** (120 s, 4,621구간, 낙상 5, `004-eval_N0final_acc`). C3 의 표를 갱신했다.

### C20. "관절 영점은 소거됐다(사용자 관측 + tilt 1.6°)" — **철회. 살아 있는 용의자로 복귀**

⭐ **2026-08-08 사용자가 캘리브레이션 절차를 알려줬고, 그것이 소거를 직접 무너뜨린다.**

> "zero cal 은 T1 처럼 핀을 꽂아서 하는 게 아니라 그냥 **눈대중으로 엉덩이 받쳐놓고
> 상체 세워서 중력의 힘으로만** 한다. 성공 여부는 그냥 **stand mode 에서 안 넘어지는 걸
> 기준으로** 한다."

이 절차가 δ(영점 오차)에 대해 말하는 것:

1. **판정 기준이 폐루프다.** "stand mode 에서 안 넘어짐" = **벤더 펌웨어의 균형 제어기가
   δ 를 흡수할 수 있는가**. 균형 제어기는 상수 자세 오프셋을 기울여 흡수하는 것이 본업이다.
   ⇒ **δ 는 그 대역 안에서 세션마다 자유롭게 움직이고 매번 합격한다.**
2. ⇒ **"완벽한 standing 자세가 나온다"는 영점이 맞다는 증거가 아니다.** 판정 기준을 통과했다는
   증거이고, 그 기준은 순환이다. 내가 소거의 근거로 쓴 tilt 1.6° 가 정확히 그 순환이다.
3. **합격선이 우리 정책보다 관대한 컨트롤러로 그어져 있다.** 벤더 쪽은 상태·적분항이 있고
   우리 정책은 `Linear(54→256→128→128→12)` 에 기억이 없다. **벤더가 통과시킨 δ 를 우리가
   감당한다는 보장이 없다.**
4. ⛔ 그리고 내가 같은 자리에서 쓴 *"중력으로 늘어지는 관절은 기계적 평형이라 재현된다"* 도
   **사용자가 정정했다** — 앉힌 엉덩이 각도, 힙·무릎·발목 모터의 **마찰과 backdrivability**
   때문에 항시 재현되지 않는다.

앞서 A4 가 든 세 반박도 그대로 유효하다:
- `ibatch.md:3316-3319` 가 *"이 로봇은 세 걸음 증상이 난 그 로봇이 아니다"* 라고 명시한다.
  영점은 개체별 값이라 이식성이 가장 낮다
- §8-46 제목 자체가 *"관절 영점 오차를 한 번도 시뮬레이션한 적이 없다"* 다(독립 추첨 결함)
- `logs/mujoco/sig/hiprbias*`: base 0 / **−5° 6낙상** / +5° 0 / ±3° 둘 다 0 (각 20 s, n=1).
  **부호 비대칭은 실재한다** (단 `ibatch:3893` 의 인용은 절 번호가 틀렸고 ±3° 결과를
  빠뜨렸다 — 그 자리에서 정정했다)

**상태: 소거 취소. 살아 있는 용의자.**

⛔ **2026-08-08 재정정 — 내가 여기 적었던 "닫는 방법 1번"이 성립하지 않는다.**
원문은 *"다음 캘리브레이션 때 관절별 보정량을 기록해 달라 — 사용자만 가능, 가장 싸다"*
였고 §8-30 도 같은 요청을 했다. **둘 다 틀렸다. 사용자가 지적했고 맞다:**

> "캘 보정량이 URDF 기준 zero 에서 얼마나 틀어졌는지 **모른다**. physics factory zero 를 모른다."

이 로봇의 캘리브레이션에는 **기준이 되는 물리적 자세가 없다.** 핀/지그 없이 엉덩이를
받치고 눈대중으로 세운 뒤 "지금이 0" 이라고 선언하는 절차라, **"얼마나 보정했다"는 양이
애초에 존재하지 않는다.** 공장 물리 영점이 어디인지 아무도 모르고 그 절차로는 알 수 없다.

⇒ **URDF 프레임 기준의 δ 는 캘리브레이션으로는 영원히 모른다. 재는 수밖에 없다.**
그래서 순서가 이렇게 바뀐다:

1. **배포단 영점 추정기(R7)** — δ 를 알 수 있는 **유일한** 경로다. 도구 완성, 로봇 미실행
2. **`joint_zero` DR** — 1 없이 이것만으로는 문헌 대비 17배 범위를 요구하게 된다
   (문헌의 에피소드 상수 오프셋 DR 은 ±0.57°~±2.3°, 우리 `max_deg` 는 10°)

⚠️ 그리고 **관절 마찰·백래시**는 δ 가 세션마다 달라지는 **원인**이지 학습에 넣을
별도 랜덤화 축이 아니다(2026-08-08 사용자 지적). 중력 캘리브레이션이 일률적이지
않다는 사실의 설명이지, 시뮬레이터에 추가할 모델이 아니다.
- ① `ibatch.md:3316-3319` 가 *"이 로봇은 세 걸음 증상이 난 그 로봇이 아니다"* 라고 명시한다.
  영점은 개체별 값이라 이식성이 가장 낮다.
- ② 그 standing 은 **제조사 균형 제어기가 도는 상태**다. 폐루프 균형기는 상수 오프셋을
  재자세로 흡수하므로 tilt 1.6° 는 "균형기가 동작한다"만 증명한다. 문제 구간(`b`~`r`)은
  **균형 주체가 없는 regime** 이다.
- ③ ⭐ 그리고 `sig/hiprbias-5`(영점 −5°) = **6 낙상**, `+5` = **0 낙상** 이 원본에 있다.
  아픈 것은 토크가 아니라 **영점 부호 비대칭**이다. (`ibatch:3893` 의 주장은 참인데
  출처가 §8-43 이 아니라 이 스윕이다 — 유령 인용의 실체.)

### C21. "`pub_hz ≡ LowState 도착률 = 499.2 Hz` 라 필터 서사는 죽었다" — **철회**
- 같은 라운드에서 세웠다가 같은 라운드에서 죽은 주장이다.
- §8-41 은 그 문서가 스스로 적었듯 *"LowState 구독만"* 한 **가벼운 프로세스**를 쟀다.
  실제 배포 프로세스에는 rclpy executor **3개**가 더 있다(`FallMonitor`·`ModeMonitor` 는
  `--goal-source fixed` 에서도 생성). `base_walk` 는 `rclpy` 를 **import 조차 안 한다.**
- 서버 실측(`switchinterval` 5 ms): 연속 GIL 점유 스레드 1개면 `sleep(0.001)` 루프 천장이
  **899 → 160 Hz**, 2개면 75, 4개면 23. **필요한 것은 19 ms 의 작업이 아니라 19 ms 의
  GIL 대기다. 50 Hz 는 도달 가능하다.**
- ⇒ `pub_hz = min(f_LowState, GIL 천장)`. **뒤 항은 한 번도 안 쟀다.**
  R6 은 닫히지 않았고, **정의가 바뀐다** — 재야 하는 것은 LowState 도착률이 아니라
  **실제 배포 프로세스가 돌 때의 `pub_hz`**(`--log-timing` 30초).
- 파생: `policy_goal_pose.py:171` 의 *"2 Hz is 2 Hz in wall clock"* 은 **거짓**.
  실제 보행 주파수 = `2.0 × f_LowState/500`.

### C22. "`gait_frequency` 2.0 에서 1.5 m/s 는 물리적으로 불가능하다" (X3) — **철회**
- 다리 길이가 `base_height_target 0.52` 가 아니라 URDF 실측 **0.4607 m**(hip 축→발바닥)이고,
  "보폭 ≤ 0.5–0.6 × 다리길이" 규칙은 **저장소에 출처가 없다.** Hip_Pitch ±2.21 rad 이라
  기구학이 구속이 아니다.
- `N2_pathgrid` 실측이 반증한다: `single_support_s` median **0.24 s** × 1.54 m/s
  = 보폭 **0.370 m**(p90 0.425), 이중지지 5 %, flight 1.4 %, 15,360 env·s 에 낙상 8.
- ⚠️ `cadence_coupling` 은 **구현돼 있고**(`goal_pose_v7.py:706-738`) 두 번 다 레버가 아닌
  이유로 죽었다(I1c 예산 200 iteration, L2 틀린 `--ref`). **공정 판정이 없다.**

### C23. "hip roll 만 토크 상한에 붙는다" — **정정 (원인 아님)**
- 4.8/2.7/7.4 는 원본에서 확인되지만 **낙상 0 인 500 Hz 런에도 2.7/4.8 이 있다.**
- `goal_hold` 프로토콜에서는 **Hip_Yaw 가 1위(8.9 %)** 이고 HipR 은 0 %대. "hip roll 만" 은
  `goal_reach` 하나만 본 결과다.
- ⛔ 지표 결함: `over_limit_pct` 는 URDF 기준인데 클램프는 MJCF 기준이라
  **`URDF==MJCF` 인 Ankle_Pitch 가 구조적으로 0 %** 다. 클램프를 풀면
  **AnkP 34–41 N·m / over 17–26 %** — 데이터셋 최대 포화가 지표에 안 보였다.

### C24. "dwell duty 가 결함이었다" — **정정**
- 카운터 직접 시뮬레이션: N1/N2 = **22.35 %** 로 의도 ≈20 % 와 일치한다. 결함이 아니었다.
- `a67763f` 의 "영구 dwell 6 %" 도 **틀렸다** — 실제 **0.00 %**, 6.4 % 는 발화 겹침 확률이다.
- ⭐ **진짜 결함은 다른 데 있다**: `pause_gait_during_dwell` 이 기본 `False` 이고
  **어떤 config 에도 없다.** dwell 중에도 시계가 돌아서, dwell 이 만드는 상태가
  정책이 100 % 실패하는 `turn`(시계 가동 + 변위 0)이지 한 번도 실패하지 않은
  `stand`(시계 정지 + 변위 0)가 아니다. **`ND_dwell`(duty 상향)은 방향이 반대다.**

---

## 아직 근거불충분인 것 (추가분)

- **"2단계 학습이 맞는 처방"** — 역방향(waypoint 능력이 100~200 iteration 에 파괴됨)만
  실측이다. 정방향은 미검증. 판별 측정 하나로 닫힌다:
  `N1 model_6000` 에서 `path: 0.0` 으로 400 iteration, 100마다 두 축.
- **"실기 발목 roll 이 URDF 한계 밖에서 돈다"의 해석** — 실측(−0.578 ~ +0.537 rad 대
  하드 스톱 ±0.345)은 확정이지만, (a) URDF 가 틀렸다 (b) `motor_state_serial` 이 URDF
  관절각과 다른 양이다 를 못 가른다. 무부하 램프 3분에 갈린다.
- **`NC_actfilter` 결과** — 평가 config 가 `action_filter_tau: [0,0]` 이라 **필터를 끄고
  채점 중**이다. arm 의 존재 이유가 측정되지 않는다.

### C25. "MuJoCo 는 발을 충돌 box(16.0 x 7.0 cm)로 쓴다" — **철회**
`HANDOFF_MUJOCO_FILTER_20260807.md` §8 이 *"발에 geom 이 둘 -- 시각 mesh(contype=0)와
충돌 box"* 라 적었다. **셋이다**, 그리고 어느 것이 닿는지도 반대다.
`K1_serial.xml`/`K1_serial_realmass.xml` 의 발 body:

| geom | contype | 역할 |
|---|---|---|
| `type=mesh` | **0** | 시각 전용 |
| `type=mesh` | (기본 **1**) | ⛔ **충돌 메시** — 기록에서 누락 |
| `type=box size=0.08 0.035 0.016 pos=0.014 0 -0.008` | (기본 1) | 충돌 박스 |

STL 실측(13,232 삼각형 직접 파싱): x[−68.5, +96.5] mm, y ±39.2 mm, **바닥 z −26.9 mm**.
box: x[−66, +94] mm, y ±35 mm, **바닥 z −24.0 mm**.
⇒ **box 가 메시 안에 완전히 묻혀 있다** — 모든 방향에서 메시가 더 크고 **2.9 mm 더 낮다**.
**MuJoCo 의 실제 접촉면은 메시(볼록껍질)이고 box 는 땅에 닿지 않는다.**

**함의**: Isaac 은 순수 box(16.0 x 7.0 cm), MuJoCo 는 메시(16.5 x 7.85 cm).
⇒ **두 시뮬레이터가 다른 발로 돌았다.** §8-39 의 "두 시뮬레이터 모두에서 안 넘어진다"는
같은 로봇 두 대가 아니라 발이 다른 두 대였다. 그리고 실물에 맞는 쪽은 **MuJoCo** 다.
학습의 `feet_edge_pos` 는 box(16.0 x 7.0)에 맞춰져 있다.

### C26. "순기구학 CoM 이 평행 발목 링크 때문에 직렬 체인으로 안 풀린다" — **철회**
`HANDOFF_TO_TRAINING.md:180`. **원인 귀속이 틀렸다.** 평행 링크는 발목 축 **아래**라
정강이 대비 발 위치를 바꾸지 않고, `motor_state_serial` 은 정의상 가상 직렬각이며,
발목 몇 도가 몸통 34도가 될 수 없다.
**진짜 원인은 FK 구현 버그다** — 부호뒤집기·항누락 64가지 전수에서 −34° 를 내는 조합이
**정확히 하나**(hip_pitch 부호 뒤집기 + ankle_pitch 누락, 합 +0.6 rad = 34.4°).
올바른 FK 로 같은 자세가 **+2.86°** 다. ⇒ **직렬 FK 경로는 막혀 있지 않다.**

### C27. "Hip_Pitch 원점은 자산마다 같다" — **정정 (아무도 주장하지 않았지만 아무도 확인도 안 했다)**
`Left_Hip_Pitch` joint origin z 전수:
- **−0.077** : `K1_locomotion_robot` · `K1_robot_boxfoot` · `k1/K1_locomotion` (실물 계열)
- **−0.062** : `K1_locomotion_armsdown` 외 6종 (**학습 계열**) 및 MJCF 둘
**15 mm 차이**이고 다리 전체 길이가 0.5406 대 0.5227 m 로 **17.9 mm** 갈린다.
N 배치는 `K1_robot_boxfoot`(−0.077)이라 안전하지만,
**배포된 `I3b_stance10` 은 `armsdown`(−0.062) 위에서 학습됐다.**

### C28. "`--hold-diag` 의 13.8° 는 정적 자세로 설명이 안 된다" — **철회 (사용자 실측이 갈랐다)**
2026-08-08. 내가 *"4.9° 는 맞는데 13.8° 는 다른 것이다(램프 중이거나, 발이 구르거나,
한계주기)"* 라고 썼다. **두 값은 다른 원인이 아니라 같은 원인의 두 조건이었다.**
사용자 실측: **4.9° = 발목을 손으로 바닥에 눌러 잡았을 때 / 13.8° = 손을 뗐을 때.**
⇒ 잡으면 발바닥이 평평하고 정적 자세가 그대로 설명한다(예측 4.5°, 0.4° 차이).
⇒ 놓으면 관절 추종은 0.6° 인데 몸통만 9° 더 간다 = **발바닥이 안 평평하다.**
**교훈**: "설명이 안 된다"고 쓰기 전에 **그 두 숫자가 같은 조건에서 나온 것인지**
물었어야 했다. 이 저장소가 2026-08-07 에 열세 번 한 실수와 같은 모양이다 --
내가 본 것 바로 옆의 조건이 어긋나 있었다.

### C29. "합 −0.05 자세는 무게중심이 지지다각형 밖으로 나간다" — **기각 (내 가설이 내 계산에 죽었다)**
C28 뒤에 내가 세운 대체 가설이다. `tools/static_balance_k1.py` 로 URDF 에서 직접 계산:

| 자세 | 합(rad) | CoM_x | 발끝 여유 | 발목토크(양발) |
|---|---:|---:|---:|---:|
| vendor_standing | 0.000 | 22.5 mm | **71.5 mm** | 4.13 N·m |
| prepare_fixed | 0.000 | 10.2 mm | **83.8 mm** | 1.88 N·m |
| rl_default | −0.050 | 41.0 mm | **53.0 mm** | 7.53 N·m |

발끝 경계는 +94 mm 다. **어느 자세도 근처에 못 간다.** 발목 토크도 정격 20 N·m 대비
여유롭다. ⇒ **정적 전도는 원인이 아니다.**

**대신 계산이 다른 것을 가리켰다.** 평형이 *있는 것*과 *안정한 것*은 다르다.
발바닥이 평평하면 몸통은 발목을 축으로 도는 역진자이고, 다리가 PD 로 잡혀 있으면
복원 강성은 `2·kp_ankle`, 중력 강성은 `m·g·h` 다(h = 발목 위 CoM 높이 = **0.4298 m**).

| 질량 | 게인 | 복원 − 중력 | 여유 | 흔들림 | 감쇠비 |
|---|---|---:|---:|---:|---:|
| URDF 18.714 | prepare 250 | 421.1 | ×6.34 | 1.76 Hz | 0.131 |
| URDF 18.714 | **정책 50** | **21.1** | **×1.27** | **0.39 Hz** | **0.117** |
| 실물 19.666 | **정책 50** | **17.1** | **×1.21** | **0.35 Hz** | **0.127** |

⚠️ **이것은 결론이 아니라 리드다.** 같은 모드가 시뮬에도 있으므로 이것만으로는
sim-real 격차를 설명하지 못한다. 다만 **실물 질량이 URDF 보다 5 % 무거워 여유가
×1.27 → ×1.21 로 깎이고**, 정책 게인에서 직립이 **0.35 Hz · 감쇠비 0.13** 의 거의
안 죽는 흔들림 모드를 갖는다는 것은 사실이다. 판정 측정은 MJC 세션에 걸어 뒀다.

### C30. "리드 B: 시뮬이 토크 포화를 안 해서 sim-real 격차다" — **정정. 토크는 맞고, 문제는 속도다**
2026-08-08. 내가 배포 감사에서 `common.torque_limit` 이 죽은 코드인 것을 보고
"학습/시뮬이 포화 안 하면 그것이 격차"라고 세션들에 보냈다. **학습 쪽 전제가 틀렸다.**

`envs/K1/goal_pose.py:866` 이 **하드 클램프**를 건다:
```python
dof_torques = torch.clip(dof_torques - friction, min=-self.torque_limits, max=self.torque_limits)
```
`torque_limits` 는 URDF `effort` 다(`:68`). ⇒ **학습은 토크를 실제로 포화시킨다.**

**대신 그 옆에서 진짜가 나왔다 — 속도에는 아무 제약이 없다.**

| | 하드 클램프 | 벌칙 스케일 |
|---|---|---|
| 토크 | **있다** (`:866`) | `torque_limits: -0.` (꺼짐) |
| **관절 속도** | **없다** | **`dof_vel_limits: -0.` (꺼짐)** |

`dof_vel_limits` 는 `_reward_dof_vel_limits` 한 곳에서만 쓰이고 그 스케일이 **0** 이다.
⇒ **학습에 관절 속도 제약이 전혀 없다.**

그런데 자산이 갈린다:

| 자산 | Hip_Pitch vel | Hip_Roll | Knee vel |
|---|---:|---|---:|
| `K1_locomotion*` (**학습 계열**) | **18** | effort 20 | **18** |
| `K1_locomotion_robot` · `K1_robot_boxfoot` (**실물 계열**) | **7.1** | effort **35** | **12.5** |

**실물 고관절 pitch 는 7.1 rad/s 인데 학습은 아무 상한 없이 배웠다.** 정책은 위치를
명령하고 속도는 PD 추종에서 나오므로, 학습된 보행이 7.1 을 넘는 고관절 속도를 요구하면
**실기는 그것을 물리적으로 못 낸다** — 스윙 다리가 제때 못 나가고 착지점이 어긋난다.
"몇 걸음 가다가 무너진다"와 모양이 맞는다.

⛔ **그리고 그 축은 평가에서도 한 번도 측정된 적이 없다.** 라운드 표의 `관절점유(%)` 는
`v7_extras.dof_pos_occupancy` = **위치** 점유다. 속도 지표는 리포트 어디에도 없다.
⇒ 제약이 없는 축이 측정도 안 되는 축이다.

⚠️ **미확인 (추측과 섞지 않는다)**:
① 7.1 rad/s 가 벤더 사양인지 누군가의 추정인지 — 실물 계열 자산 두 개가 일치할 뿐이다
② Isaac Gym 이 URDF velocity 를 PhysX 단에서 자체로 거는지 (코드는 안 건다)
③ **학습된 정책이 실제로 7.1 을 넘는가** — 이것이 판정 측정이고 아직 안 쟀다
자산 차이 자체는 무의미하다(아무도 그 값을 안 쓴다). 문제는 **실물에만 있는 상한**이다.

### C31. "모터 정격은 20 N·m 다" — **철회. 내가 만든 유령이다**
2026-08-08. 내가 배포 감사 요약에서 *"1.251 rad × kp 50 = 62.5 N·m 를 요구한다(정격 20)"*
라고 쓰고, 그 20 을 **전역 모터 정격**처럼 세 세션(Teacher/MJC/Deployer)에 보냈다.
학습 세션이 되물어서 다시 확인했다:

`deploy/configs/Goal_Pose_E0.yaml` `common.torque_limit` =
**45 / 30 / 30 / 45 / 20 / 20** (hipP / hipR / hipY / knee / ankP / ankR).

**균일 20 이 아니다.** 원래 계산은 kp 50 인 **발목**에 대한 것이라 발목 값 20 과 짝이
맞았는데, 내가 그것을 관절 전체의 사실로 일반화했다. 그리고 그 20 자체도 **config 값이지
검증된 벤더 사양이 아니다.**

⇒ ⛔ **URDF effort 를 20 으로 통일하지 마라.** 학습 세션이 그 커밋을 내겠다고 했고 막았다.

**같은 물리 관절에 값이 세 군데에서 다르다** (학습 세션 확인 + 내가 URDF 두 개 직접 파싱):

| 관절 | `boxfoot` (N 배치) | `armsdown` (배포 계보 I3b) | deploy config |
|---|---:|---:|---:|
| **Hip_Roll** | **35** | **20** | **30** |
| Ankle_Roll | 20 | 15 | 20 |

⚠️ **위험한 쪽이 직관과 반대일 수 있다.** `I3b`(20)를 실기에 올리면 정책이 **덜 쓰도록**
배운 것이라 보수적이다. **`N 배치`(35)가 오히려 실기에 없을 수도 있는 토크를 전제로
배운 쪽이다.** 실기 한계가 30 이면 N 배치가 위험하다. 그래서 이 축의 실험은
"I3b 가 왜 넘어졌나" 가 아니라 **"N 배치를 올려도 되는가"** 에 답한다.

⛔ **저장소 안에 실기 정격의 근거가 없다.** `Base_Walk.yaml`(60/25/30/60/24/15)은 T1
(23관절)이라 이 로봇 것이 아니다(C15). ⇒ **사용자/벤더 문서로만 닫힌다.**

### C32. "라운드 표의 토크포화 2.8~5.3 %" — **인용 철회**
`v7_extras.torque_saturated` 는 `_v7_extras` 호출 순간의 **한 프레임** 값이고
`eval_goal_pose.py:1968` 이 롤아웃 끝에 한 번 읽는다. **표본이 1이다.**
C11(발 간격)과 정확히 같은 결함인데, C11 을 내가 적어 놓고도 같은 열을 인용했다.
학습 세션이 관절별 롤아웃 누산기(`torque_by_joint`)를 넣었고 **속도 점유 누산기도
같이 들어갔다**(C30 대응). 앞으로는 그쪽을 인용한다.

⚠️ **C30 의 7.1 rad/s 도 같은 함정일 수 있다** — 출처가 실물 계열 URDF 두 개뿐이고
벤더 대조를 못 했다. C31 과 같은 실수를 반복하지 않도록 **"미확인"으로 유지한다.**


### C33. "armature 0.02 가 official Booster USD value 다" — **철회. K1 의 어느 관절에도 해당하지 않는다**
2026-08-08. `Get_Up.yaml:92` 의 주석을 근거로 0.02 를 '벤더 값'으로 취급하고
`NG_armature` arm 을 그 값으로 세웠다. 벤더 공식 저장소
[`BoosterRobotics/booster_train`](https://github.com/BoosterRobotics/booster_train)
를 직접 읽어 확인했다 — **K1 의 armature 는 관절별이고 0.028~0.096 이다:**

| 관절 | 모터 | armature |
|---|---|---:|
| Hip_Pitch | E6408 | 0.047813 |
| Hip_Roll | E4315 | 0.033955 |
| Hip_Yaw | E4310 | 0.028253 |
| Knee | E6416 | 0.095625 |
| **Ankle P/R** | E4310 ×평행2.0 | **0.056506** |

**0.02 는 그중 어느 것도 아니다.** 흥미롭게도 MuJoCo 의 발목 0.05 가 벤더 0.0565 에
가장 가깝다. 상세는 [VENDOR_ACTUATOR_SPEC_K1.md](VENDOR_ACTUATOR_SPEC_K1.md).

⚠️ 다만 **Booster 자신의 Isaac Gym config(`booster_gym/envs/T1.yaml`)도 `armature: 0.`** 이다.
관절별 값은 새 Isaac **Lab** 파이프라인에서 도입됐다. 그래서 "V7 의 0 이 벤더 관행과
같다"도 참이고 "벤더가 관절별 값을 안다"도 참이다.

### C34. "C30 의 판정 기준 — Hip_Pitch 가 7.1 rad/s 를 넘으면 확증" — **무효**
벤더 공식 `velocity_limit` 은 Hip_Pitch **14.66 rad/s** 다(`boxfoot` URDF 의 7.1 은 절반,
학습 계열의 18 은 과다 — **둘 다 틀렸다**). 측정된 Hip_Pitch peak 이 ~8 이므로
**고관절은 여유가 있다. 그 가설은 죽었다.**

⚠️ **남는 것은 발목 roll 하나다.** 벤더 한계 17.59, 측정 평균 7.5~12.8, peak 76~92,
한계 초과 시간 27~55 %. 다른 관절은 전부 1 % 미만이다.

### C35. "우리 URDF effort 가 실기보다 클 수 있어 N 배치가 위험하다" — **철회. 반대다**
C31 에서 나는 위험 방향이 "N 배치(Hip_Roll 35)가 실기에 없는 토크를 전제로 배운 쪽"
이라고 썼다. 벤더 값은 **Hip_Roll 76.0** 이다. 우리 URDF 는 전 관절에서 벤더의 **절반 이하**고
무릎은 40 대 **112 (2.8배)** 다.

⇒ **학습은 로봇이 낼 수 있는 토크의 절반 이하로 클램프된 채 돌았다.** 그 자체로 위험하진
않지만(덜 쓰는 쪽), 굶긴 상태에서 최적화된 보행은 실기에서 최적이 아니다.

⛔ **단 벤더 문서끼리 어긋난다**: 공개 페이지는 `Max Peak Torque 60 N·m` 인데 액추에이터
DB 는 무릎 112 다. **이걸 닫기 전에 effort 를 올리면 지금과 반대 방향의 같은 실수다.**


### C36. "게이트 5 cm 를 통과하는 체크포인트가 없다" — **철회. 8개가 통과한다**
2026-08-09, 3중 감사 수렴에서 서버 `report.json` **121개의 `gates` 전수 조회**로 확인.
120 s 기준 정확도 게이트 통과: **MA 2.69 / N0best 2.82 / I3b 3.10 / M3 3.30 / M7 3.54 /
N0 model_6000 3.63 / MB 4.28 / N8 4.39 cm** — **8개.**
(감사 A 는 1개로 셌고 B 는 8개로 셌다. 표결이 아니라 전수 조회로 갈랐다 — B 가 맞다.)

⛔ **이 문장은 `CLAUDE.md` 최상단과 C5 에 있었고 캠페인 전체의 헤드라인이었다.**
정확한 서술은 **"정확도·속도·낙상을 *동시에* 만족하는 정책이 없다"** 이다.
통과한 8개는 전부 `falls` 게이트에서 떨어지고, 대부분 느린 non-path arm 이다.
⇒ 문제는 "정확도를 못 낸다"가 아니라 **"두 축의 교집합이 비어 있다"** 이고,
그러면 처방이 달라진다(정확도 보상 탐색이 아니라 **2단계 학습/속도 유지**).

### C37. "발목 roll 이 속도 한계를 시간의 27~55 % 넘는다 = 실기 최강 리드" — **철회. 채점 물리의 산물이다**
`RETRACTIONS C30`·`C34`·`VENDOR_ACTUATOR_SPEC_K1.md §3` 및 코드 주석 2곳에 적혀 있다.
3중 감사 A 가 반증했고 수렴에서 재확인: **같은 정책 `NE_ctrl100` 을 채점 물리만 바꿔 재면
`over_share 0.5493 → 0.0000`, peak `80 → 18 rad/s`.** `NJ_armasset` 도 0.55 → 0.00.
⇒ **정책이 만든 것이 아니라 `armature: 0` 인 채점 환경이 만든 것이다.**
실기 로그의 발목 roll **위치** 초과(±0.55 대 URDF ±0.345)는 **별개 항목이고 여전히 유효**하다 —
두 문서가 그 둘을 뭉쳐 놨다. 위치 쪽은 **전원 없이 손으로 밀어 2분이면 갈린다.**

### C38. "`claim_check` 를 통과하면 나란히 비교해도 된다" — **정정. (2026-08-09 부분 해소)**

> ⚠️ **갱신**: 병렬 세션이 지문에 `asset`·`control` 을 추가했다
> (`eval_goal_pose.py:807-813`, `claim_check.py:66-67` — 직접 확인).
> ⛔ **그러나 기존 리포트 123개 중 그 지문을 담은 것은 0개다.** 도구가 `(기록없음)` 으로
> 찍고 "물리를 뺀 판정" 경고를 낸다. ⇒ **앞으로는 막히지만, 지금까지 쌓인 모든 라운드는
> 여전히 물리 대조가 불가능하다.** 옛 리포트로 armature 비교를 하지 마라.
`effective_eval_protocol` 은 `commands`·`noise`·`randomization` 만 담는다.
⇒ **같은 체크포인트를 채점 물리(armature)만 바꿔 잰 두 리포트** — 낙상 **28,955 대 0**,
정확도 127.3 대 62.09 cm — 에 대해 도구가 **`✅ 조건이 전부 같다. 나란히 비교해도 된다`**
를 출력한다(감사 B 가 서버에서 재현, 수렴에서 재확인).
⛔ `CLAUDE.md` 의 "읽는 법"대로 읽으면 **정확히 이 함정에 빠진다.**
armature 가 지금 조사의 1차 변수인데 가드가 그것을 못 따라왔다. **미수정 — 지문에
`asset`·`control` 을 추가해야 한다.**


### C39. "정확도 지표가 좋은 정책이 실기에서도 좋다" — **철회. 예측력이 음수다**
2026-08-09, 10회 고정질문 감사 중 회차 01 이 짚었고 내가 전수 조회로 확인했다.
waypoint 정확도 축(120 s), 오차 median 오름차순:

| 순위 | arm | 오차 | **낙상** |
|---:|---|---:|---:|
| 1 | MA_feetcross | 2.69 cm | 4 |
| 2 | N0_ctrl | 2.82 cm | 8 |
| **3** | **`I3b_stance10` ← 실기에서 세 걸음에 무너지는 그 정책** | **3.10 cm** | **2,488** |
| 6 | N0_ctrl model_6000 | 3.63 cm | 5 |
| — | `NH_zeroclock` (우리가 "최고 셀"이라 부른 것) | 9.33 cm | 1 |

⇒ **실기에서 실패한 바로 그 정책이 정확도 123개 중 3위다.** 원인은 생존 편향 —
낙상 2,488 회(다른 전부 2~441)라 **살아남은 구간만 오차 통계에 들어간다.**

⛔ **그리고 캠페인 헤드라인이 이 열만 보고 읽혔다**: C5 "5 cm 통과가 없다" → C36 "8개 통과".
**둘 다 낙상을 안 보고 센 것이다.** 낙상을 같이 보면 120 s 프로토콜에서 네 게이트를
전부 통과하는 체크포인트는 **0개**다(회차 01·03 일치).

**교훈**: 정확도는 **낙상과 같이 읽지 않으면 방향이 뒤집힌다.** 저장소가 `표본탈락(%)`
열을 만들어 두고도 순위는 오차로 매겨 왔다. 앞으로 정확도 단독 순위표를 만들지 마라.

### C40. "`Left_Hip_Pitch` origin z 15 mm 차이(C27)는 미확인이다" — **기각. 실재한다**
회차 03 이 *"두 URDF 사이에 없다 → 미확인"* 이라 했다. 직접 파싱해 확인:
`armsdown` **-0.062** / `boxfoot`·`robot` **-0.077** ⇒ **15 mm 차이 실재.**
회차 01 도 "엉덩이 +15 mm" 로 독립 확인했다. **C27 은 유효하다.**

### C41. ⛔⛔ "`real_walk.csv` 는 실기 로그다 / 실기 보행 시계가 1.51 Hz 로 관측됐다" — **철회. MuJoCo 출력이다**
2026-08-09. 10회 고정질문 감사 중 회차 **06** 이 *"파일이 `logs/mujoco/` 아래 있는데
이름이 real 이다"* 라는 이름 불일치를 미확인으로 남겼다. 그것을 따라가 확정했다.

**증거 넷 (전부 직접 확인):**
1. **배포 코드가 쓰는 타이밍 CSV 에는 `pub_hz` 열이 있다** (`deploy_goal_pose.py:497`).
   `real_walk.csv` 에는 **없다** — 63열이고 `pub_hz` 가 빠진 그 형식은
   `play_mujoco_goalpose.py:433-438` 이 쓰는 것과 정확히 같다.
2. 헤더가 **알려진 MuJoCo 산출물 `gh_hz500_lag0.csv` 와 바이트 단위로 동일**하다.
   형제 파일이 `gh_hz100/150/200/500_lag0/20.csv` = **루프 주파수 스윕**이다.
3. 같은 디렉터리의 `i3b_real_120s.*` 에는 **`.mp4` 렌더**가 딸려 있다. 로봇은 mp4 를 안 만든다.
   ⇒ 이 디렉터리의 `real` 은 "로봇에서 녹화" 가 아니라 **"실기 흉내(realistic)"** 라는 뜻이다.
4. 서버·로컬 전체에서 배포 타이밍 로그(`pub_hz` 포함)를 찾았다 — **0건.**

**따라서 철회되는 것:**
- *"실기 보행 시계가 1.51 Hz 로 돌았다(설계 2.0)"* → **MuJoCo 실행에서 나온 값이다.**
- *"제어 39.6 Hz / LowState ≈ 396 Hz 가 실기에서 측정됐다"* → **아니다.**
- 10회 감사 중 **6회(01·02·03·05·06·07)가 이것을 "실기 관측"으로 답했다**(Q5·Q10·Q12).
  **그 여섯 개의 Q5/Q10/Q12 답은 출처가 틀렸다.** 숫자 자체는 재현되지만 실기가 아니다.
- ⛔ **내가 사용자에게 두 번 "실기 실측" 이라고 말했고 "내가 독립 재현했다" 고 덧붙였다.**
  숫자는 재현했지만 **출처를 확인하지 않았다.** 파일 이름을 근거로 삼았다.

**남는 것 (약해진 형태로 유효)**: MuJoCo 에서 제어율이 39.6 Hz 로 떨어지면 고정 위상증분
때문에 보행이 1.26배 시간 팽창한다 — 이것은 **기전의 시뮬 재현**이고
`HANDOFF_MUJOCO_FILTER_20260807.md` 의 서사와 같은 계열이다. **로봇에서 확증되지 않았다.**

⛔ **그리고 이것이 C39/Q11 을 더 날카롭게 만든다**: 시뮬 지표와 실기가 연결된 적이 없는
정도가 아니라, **저장소에 실기 시계열 자체가 존재하지 않는다.** 지금 확정된 실기 사실은
사용자가 직접 관측해 말해 준 것들(tilt 4.9°/13.8°, 관절 판독, 캘리브레이션 절차)과
인계 문서의 서술뿐이다.

**⇒ `--log-timing 30초` 는 "확증/기각" 이 아니라 그 축의 *최초 측정*이다.**
