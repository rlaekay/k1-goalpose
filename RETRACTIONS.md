# 철회·정정 대장

**이 파일을 읽지 않고 비교 주장을 하지 마라.** 여기 있는 주장을 다시 세우려면
왜 이번엔 다른지를 먼저 써야 한다.

---

## 왜 이 파일이 있는가

2026-08-07 하루에 주장을 **열세 번** 바꿨다. 무작위가 아니었다 — 전부 같은 모양이다:

> **비교 대상 두 쪽의 조건이 같은지 확인하기 전에 결론을 말했다.**

| 실제로 어긋난 조건 | 그래서 나온 틀린 주장 |
|---|---|
| 체크포인트 (best vs final) | "N0_ctrl이 세 축 모두 지배" |
| 표본 밀도 (2점 vs 8점) | "무릎이 아니라 절벽" |
| 키의 의미 (간격 vs 주기) | "ND_dwell duty 40%" |
| 파일 vs 심볼릭 링크 | "N1 최종 정책에서 출발" |
| 프로토콜 길이 (60s vs 120s) | 낙상 "1,177 대 10 = 118배" |
| 지표의 집계 범위 (마지막 1프레임) | "발 간격은 문제없다" |
| 과제 (도착 있음 vs 없음) | "속도 요구를 충족했다" |

**한 가지 규칙이 열세 개를 전부 막는다**:
비교 주장을 하기 전에 **양쪽의 조건을 나란히 찍어 보고 다른 항목이 없는지 확인한다.**
그 확인을 사람 기억에 맡기지 않으려고 `tools/claim_check.py` 를 만들었다.

---

## 대장

상태: **유효** / **정정**(방향은 맞고 숫자·귀속이 틀렸다) / **철회**(결론 자체가 틀렸다)

### C1. "우리 과제(waypoint)는 보행을 요구한 적이 없다" — **유효 (귀속 정정)**
- 근거: `required_speed_median` 0.119 m/s, `body_speed.median` 0.03, 순항 체류 0.59 %
- ⚠️ 그 표의 숫자는 `I0b_foot/model_200` 의 것이고 **배포 정책의 것이 아니다.**
  배포 정책 자신의 값은 `logs/stance10_eval/report.json` 에서 2.53 cm / 낙상 **2**다.
  §8-47이 "자기 자산으로만 채점돼서 낙상 4건"이라 쓴 그 4건은 다른 정책 것이다.
- 결론 자체는 여러 arm에서 재현된다(요구 속도 0.115~0.126).

### C2. "배포 정책을 실물 자산으로 채점하면 낙상률 0.368 / 0.604" — **유효 (단서 셋)**
- config diff로 **평가 동역학에서 다른 것이 `asset.file` 하나뿐**임을 확인했다.
- ⚠️ ① "실물 자산 arm **전부** 0.0005~0.005"는 **M배치 + N0만**이다.
  `N1_path`(0.0238) `N2_pathgrid`(0.0354) `N8_pathdelay`(0.0886)도 실물 자산이다.
- ⚠️ ② I3b는 **구간 분포가 4~5배 쉽다.** 넘어지면 목표 근처에서 리셋되므로 짧은
  구간이 늘어난다 — `required_speed_median` 0.0252 대 다른 arm 0.099~0.125.
  **그래서 I3b의 정확도 3.10 cm를 다른 arm과 나란히 순위 매기면 안 된다.**
- ⚠️ ③ 두 URDF의 `effort` 값이 28개 다르다. `torque_limits`가 그걸 싣고 토크를
  하드 클립하므로 **포화 한계가 학습 때와 다르다**(I3b 7.75 % 대 나머지 1.7~2.5 %).

### C3. "N0_ctrl이 정확도·속도·낙상 세 축 모두에서 path arm 전부를 지배한다" — **철회**
- 원인: **정확도 행만 체크포인트가 달랐다.** N0는 `best(2200)`, N1/N2는 `model_6000`.
- 같은 `model_6000` 으로 맞춘 뒤(2026-08-07 22:xx 측정):

  | | 정확도 | 보행 속도 | 보행 낙상률 |
  |---|---:|---:|---:|
  | N0_ctrl | **3.63 cm** | 1.10 | 0.00347 |
  | N1_path | 47.70 | **1.48** | **0.00066** |

- **최종: 지배는 정확도 한 축에서만 성립한다.** 속도와 보행 낙상은 path가 이긴다.

### C4. "path는 더 빠르면서 덜 넘어진다. 대가는 2.8 → 7.9 cm뿐" — **철회**
- 같은 체크포인트로 맞추면 **3.63 → 47.70 cm**다. v7 배치(6.3 → 37.9)보다 **나쁘다.**
- "dwell이 충돌을 줄였다"는 서사는 이 숫자 위에서 성립하지 않는다.

### C5. "무릎이 아니라 절벽이다" — **정정**
- 정밀 스캔(100 iteration 간격)에 **해상 가능한 중간점이 있다**: N1 it200 24.5 cm,
  it300 37.6 cm / N2 it200 15.7 cm, it300 35.0 cm. 불연속이 아니라 **급경사**다.
- "그 뒤 평평"도 틀렸다: N1 it900 39.9 → it6000 47.7 (**+20 %**),
  N2 it900 30.6 → it6000 42.2 (**+38 %**).
- **쓸 수 있는 체크포인트가 없다는 결론은 유효하다** (전부 게이트 5 cm의 3배 이상).

### C6. "속도와 정확도가 충돌한다(트레이드오프)" — **철회**
- N1 it100~500: 속도 1.15 → 1.22로 **평평한데** 정확도만 7.8 → 39.7로 무너진다.
  속도 이득(1.32~1.38)은 **it600 이후**에 온다.
- **교환이 아니라 순차적 소실이다.** 정확도는 속도가 오기 400 iteration 전에 사라진다.
- 그리고 `failure_modes`가 그 정체를 말한다 — N1 model_6000은 **62 %가 목표에 한 번도
  도착하지 않고 27 %는 도착했다 떠난다.** 정확도 열화가 아니라 **정지 행동의 소멸**이다.
- ⇒ 맞는 처방은 보상 튜닝이 아니라 **2단계 학습**이다.
- ⚠️ N2는 또 다르다: it300~900에서 **속도까지 0.99 → 0.36으로 무너진다**(1.0 m/s 초과 0 %).
  `speed_grid`가 정책을 정지 상태로 몰아넣는 구간이 있다.

### C7. "ND_dwell의 dwell duty를 18 % → 40 %로 올렸다" — **철회 후 수정**
- `interval_s`는 dwell **사이의 간격이 아니라 주기**다(`goal_pose_v7.py:317`의
  `path_dwell_next`가 dwell 중에도 감소한다). duty = duration / interval.
- 내가 잡은 `[4,8]`은 40 %가 아니라 **66.7 %**였고, max duration 5 > min interval 4라
  **dwell 중에 다음 dwell이 발화해 영구 dwell이 되는 env가 약 6 %** 생겼다.
- `[8,12]`로 고쳤다(duty 40.0 %, 겹침 없음). **arm이 돌기 전에 고쳤다.**

### C8. "N4_hist는 obs 폭이 달라 warm start가 불가능하다" — **철회**
- `tools/expand_checkpoint.py`가 첫 층만 넓히고 새 열을 0으로 채운다.
  float64 검증에서 actor 최대차 0.0, critic 2.8e-14 — **대수적 항등**이다.

### C9. "관측 arm들은 N1_path의 최종 정책에서 출발한다" — **철회**
- 코드는 `best.pth`를 썼고 그건 `--link_best`가 매번 다시 만드는 **심볼릭 링크**이며
  **`model_100`**(붕괴 전)을 가리킨다. `NA_histzero` `NZ_zeroiid` `NC_actfilter`
  세 arm이 이미 그 계보로 돌았다.
- 출발점이 **가변**이라 재현도 불가능했다. `WARM_IT`로 iteration 번호를 못 박았다.
- ⭐ 다만 그 사고가 **계보 B**를 만들었고, 그 대조군 `NE_ctrl100`을 세워 살렸다.

### C10. "47.70 cm는 오염된 스모크 채점값이다" — **철회**
- 오염됐다는 판단(어느 체크포인트인지 통제되지 않음)은 맞았지만, **값 자체는 진짜
  `N1_path/model_6000`의 것**이었다. 나중에 별도로 잰 값과 모든 유도 지표까지 동일하다.
- ⚠️ 같은 디렉터리의 `.walk` 쪽은 **`N2_pathgrid/model_200`**이다 — 한 행 안에서
  두 축이 서로 다른 정책이었다. 둘 다 `logs/eval_rounds/_contaminated/`로 격리했다.

### C11. "발 간격이 모든 arm에서 16~19 cm라 실기 문제는 sim에서 재현되지 않는다" — **철회**
- `v7_extras` 전체가 **롤아웃 마지막 한 프레임의 스냅샷**이다(arm당 표본 1).
  같은 필드에서 `bias_Hip_Roll`이 부호까지 뒤집힌다. **실기 대조에 쓰면 안 된다.**

### C12. "N2_pathgrid it5100(1.64 m/s)이 속도 요구를 처음 충족했다" — **정정**
- `N1_path` it900이 **1.38**로 먼저 G2(1.3)를 넘겼고 it1700이 1.44다.
  1.64는 **최초가 아니라 최댓값**이고 단일점·단일 seed다.
- ⚠️ 그리고 `forward_hold`의 `body_speed`는 사용자 요구와 **같은 양이 아니다.**
  이 프로브는 도착과 정지를 아예 없앤다. G1의 절반을 제거한 조건에서 잰 순항 속도다.

### C13. "GPU 유휴 감시가 작동한다" — **철회 후 수정**
- `pgrep -f`가 명령줄에 문자열이 든 **셸까지 세서** `nproc_gpu`가 0이 될 수 없었다.
  유휴가 **원리적으로 감지 불가**였고 autopilot 승격이 13시간 동안 0회였다.
- 실행파일 기준 계수로 고쳤고, **2026-08-07 22:00에 첫 승격이 실제로 일어났다**
  (`040-N7_critic`). 카드별 유휴 판정도 추가했다(한 장만 노는 상태가 안 잡혔다).

### C14. "낙상 1,177 대 10 = 50배" — **정정**
- 두 리포트의 `duration_s`가 **120 s 대 60 s**다. 원시 개수 비는 118배, 비율 비는 57.6배.
- `round_table.py`가 이제 원시 개수 대신 **낙상간격(초)**을 찍는다.

---

## 아직 근거불충분인 것 (주장하지 마라)

- **"path가 속도에서 확실히 이긴다"** — N0에는 파레토 스캔이 없다. N0 2점 대 N1 8점
  비교다. 확정된 것은 *model_6000 지점에서* 1.35배라는 것뿐이다.
- **"NA_histzero가 안 무너진 것은 이력+영점 덕이다"** — 대조군 `NE_ctrl100`이 아직
  돌고 있다. 붕괴가 레버 덕인지 **출발점(model_100) 덕인지** 못 가른다.
- **NA/N4/N5/N6의 결과 해석** — 대칭 손실이 확장 관측을 안 덮는 결함 위에서
  `NA_histzero`가 이미 완주했다. 그 결과는 "이력+영점"이 아니라
  "이력 + 깨진 대칭 손실"을 잰 것이다.
- **실기 필터(`pub_hz`) 서사 전체** — `pub_hz`가 **한 번도 측정된 적이 없다**(R6).
  MuJoCo의 "50 Hz면 66/68 낙상"은 강제한 조건이지 측정한 조건이 아니다.
