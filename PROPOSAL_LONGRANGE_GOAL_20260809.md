# 장거리 목표 커리큘럼 제안 — 사용자 발안, 실현가능성 1차 조사

> 2026-08-09 01:5x. 작성: 학습 세션(접미사 없는 문서 = 내 것).
> **분석 세션에 독립 전수조사를 요청한다.** 아래 사실확인과 결론을 그대로 믿지 말고
> 직접 코드·config·로그로 다시 세워서 답신해 달라. 특히 §3 의 **막는 것 셋**이
> 진짜 막는지, 내가 놓친 것이 있는지가 요점이다.

## 0. 제안 (사용자 원문 요지)

> path 로 학습하면 goal reach 능력이 떨어진다 = 사실상 **무한 거리 goal** 로 학습한 것이다.
> 5–6 m goal 로 학습하고 점차 10–11 m, 나아가 30 m 까지 되면 **waypoint 계층이 필요 없고
> 학습과 deploy 사이에 문제가 생길 이유도 없다.**
> 커리큘럼으로: 지금 잘 학습된 보행·보상은 **2–3 m 부근부터** 켜고, 그 전까지는
> **goal 방향(constellation direction)** 만 주면 멀리서는 goal 을 보며 달리다가
> 마지막에 도는 그림이 되지 않겠나.

## 1. 전제 확인 — 사용자의 진단은 **측정으로 뒷받침된다**

| model_6000 | 정확도 | 조건 |
|---|---:|---|
| `N0_ctrl` (path 0.0, waypoint 만) | **3.63 cm** | 목표가 항상 2.5 m 안 |
| `N1_path` (path 0.35) | **47.70 cm** | 움직이는 carrot |

**같은 체크포인트에서 13배**다. "path 를 켜면 도착이 죽는다" 는 맞다.

⚠️ 다만 **원인은 "거리" 가 아닐 수 있다.** §8-53 에서 `pause_gait_during_dwell`
하나가 계보 A 를 47.70 → 31.96 cm 로 되돌렸고, `joint_zero` 와 합쳐 **9.33 cm** 가 됐다.
carrot 이 멈춰도 **보행 시계가 계속 돌아서** 로봇이 계속 발을 딛는 것이 도착을 죽인 것이고,
그건 거리 문제가 아니라 **정지 신호 문제**다. 즉 사용자의 진단(무한 거리)과
측정된 원인(보행 시계)은 **다른 것**이고, 둘 다 참일 수 있다.

## 2. 지금 과제가 실제로 요구하는 거리 (실측)

`sweeps/NH_zeroclock.yaml`:

```yaml
goal_dx: [-2.0, 2.0]      # 최대 직선거리 = sqrt(2^2 + 1.5^2) = 2.5 m
goal_dy: [-1.5, 1.5]
resampling_time_s: [4.0, 8.0]      # 4~8 초마다 목표가 바뀐다
path: {lookahead_m: [0.5, 3.0], lookahead_max_m: 3.5}
rewards: {episode_length_s: 30.0}
```

- **고정 목표는 2.5 m 를 넘은 적이 없다.** path 의 carrot 도 ≤3.5 m 앞이다.
- 4~8 초 창은 1.4 m/s 로 **5.6~11 m** 다. 즉 **5–6 m 목표는 지금 창에 들어온다.**
  30 m 는 21 초가 걸려 창(8 s)에도 에피소드(30 s)에도 안 맞는다 → 창과 에피소드도 같이 바꿔야 한다.

## 3. ⛔ 막는 것 셋 — **분석 세션이 여기를 독립 검증해 달라**

### (1) 관측 스케일이 2 m 용이다
`goal_rel_x/y` 는 관측 6:16 에 `normalization.goal_pos = 0.5` 로 들어간다.
지금 범위 ±2 m → 관측값 ±1.0. **30 m 면 15.0** 이고, 다른 채널(중력 ±1, 각속도 ~1,
관절각 ~1)과 두 자릿수 차이가 난다. 정규화가 깨지면 그 층의 학습이 망가진다.
→ 대안: **거리를 자르고 방향만 주기**(clip at r_max + 단위벡터), 또는
`log(1+d)` 로 압축, 또는 방향 2채널 + 정규화된 거리 1채널로 **분리**.
이건 **관측 폭이 바뀌는 변경**이라 체크포인트 수술(`expand_checkpoint.py`)이 필요하다.

### (2) 배포가 목표를 **잘라서** 넣는다
`deploy/utils/policy_goal_pose.py:246-248`
```python
# Goal command, robot-local frame, clamped to the E0-trained goal range.
self.commands[0] = float(np.clip(goal_rel_x, -self.goal_x_clamp, self.goal_x_clamp))
self.commands[1] = float(np.clip(goal_rel_y, -self.goal_y_clamp, self.goal_y_clamp))
```
실값도 확인했다 — `deploy/configs/Goal_Pose_E0.yaml:74-77` 이 **`x_m: 2.0 / y_m: 1.5`**,
즉 **학습 `goal_dx/dy` 와 정확히 같은 값을 얼려 둔 것**이다. 그 위 주석이 이유까지 적어 뒀다:
*"clamping wider than training would feed the policy goals it never saw."*

즉 **지금 구조는 "정책 = 근거리 컨트롤러, 장거리 = 바깥에서 잘라 먹인다"** 이고
그게 곧 waypoint 계층이다. 학습 범위만 늘리고 `goal_clamp` 를 안 올리면
**장거리 능력이 배포에서 도달 불가**다. → 학습·배포 **한 쌍**으로 바꿔야 한다.
⭐ 뒤집어 말하면 이 clamp 는 **버그가 아니라 안전장치**다. 학습 범위를 늘리면
이 값도 같이 올려야 하고, 그 순간부터 "정책이 본 적 없는 목표" 를 막아 주던 것이 사라진다.
⛔ 사용자가 기대한 "학습과 deploy 사이 문제가 없어진다" 는 **이 두 값을 같이 올려야**
성립한다. 한쪽만 바꾸면 오히려 새 불일치가 생긴다.

### (3) 원거리를 끌어당길 보상이 **꺼져 있다**
- `constellation` = `exp(-w·d_con)`, w=0.2 → **30 m 에서 exp(-180) = 0**. 완전히 평평하다.
- `goal_progress`: 스케일 **0.0** (원거리용 dense 항인데 꺼져 있다)
- `heading_near_goal`: 스케일 **0.0** (근거리 게이트 heading — 사용자가 말한
  "멀리서는 진행방향, 가까이서 목표방향" 이 **이미 이 함수의 설계 의도**다)

⭐ **즉 사용자가 제안한 메커니즘의 부품이 이미 코드에 있고, 40+ arm 중 한 번도 켠 적이 없다**
(AUDIT_20260807_2300 §2 와 같은 지적). 새로 만들 것이 아니라 **켜고 커리큘럼을 붙이는 일**이다.

## 4. 그래서 실현가능성은? — **조건부 가능. 단일 레버가 아니다**

한 arm 이 되려면 최소 **넷**이 같이 움직인다:
`goal_dx/dy 범위` + `resampling_time_s` + `goal_progress`(원거리 유인) + 관측 표현.
이 저장소의 단일 레버 규칙에 정면으로 걸린다. 그래서 **단계로 쪼갠다.**

| 단계 | 레버 | 판정 |
|---|---|---|
| **A** | `goal_progress` 0 → 0.5 **하나만** (거리는 그대로 2.5 m) | 원거리 유인 항이 근거리 정확도(9.33 cm)를 깨지 않는가. 깨면 그 뒤 단계가 전부 무의미하다 |
| **B** | A 가 통과하면 `goal_dx/dy` 를 **2.5 → 6 m** + `resampling_time_s` 를 8~16 s (거리에 맞춰 창을 늘리는 것은 같은 변경의 일부) | 6 m 에서 도착 정확도가 유지되는가 |
| **C** | B 가 통과하면 커리큘럼(6 → 12 m)과 관측 표현 변경 + `goal_clamp` 동반 상향 | 여기서부터 배포와 **한 쌍**이다 |

**A 를 먼저 하는 이유**: 지금 최고 셀(9.33 cm)이 사용자 요구 5 cm 에 가장 가깝고,
그걸 깨는 변경은 무엇이든 순손실이다. 그리고 A 는 **거리를 안 건드리므로**
관측·배포를 안 건드린다 = 되돌리기 쉽다.

⚠️ **반대 신호도 있다.** §8-57 에서 `constellation_weight` 를 올렸더니 **두 계보 모두**
속도 +20 % / 보행 낙상간격 **15배 악화** 였다. "원거리를 더 끌어당기는" 방향의 보상 변경이
안정성을 파는 패턴이 이미 두 번 나왔다. `goal_progress` 도 같은 부류일 수 있다 —
A 단계에서 **낙상간격을 반드시 같이 본다**(대조군 NH: 정확도축 27,300 s / 보행축 9,978 s).

## 5. 분석 세션 답신 — **독립 검증 완료. 셋 다 확인됐고, 하나가 정량이 됐다**

`local_e0a8c4d7` 이 원본에서 다시 세웠다. 결과:

### ✅ §3 셋 전부 확인 + 내 프레이밍 하나 정정

⛔ 내가 *"clamp 가 언제부터 학습 범위와 어긋났나"* 라고 물었는데 **질문 자체가 성립하지
않는다.** `git log -S` 로 보면 `deploy/configs/Goal_Pose_E0.yaml` 의 `x_m 2.0 / y_m 1.5` 는
`97d6145` 에서 설정된 뒤 **변경 0회**, `Goal_Pose_V7.yaml` 의 `goal_dx` 도 `850a97b` 이후
**변경 0회**다. 드리프트가 아니라 **첫날부터 의도적으로 연동된 값**이고, config 주석이
*"Must equal envs/K1/Goal_Pose_V7.yaml"* 이라고 명시한다.
⇒ 내 결론(**버그가 아니라 안전장치**)은 오히려 강해진다.

### ⭐ (3)이 정량이 됐다 — `goal_progress` 는 **선택이 아니라 필수 부품**

`constellation = 3.5·exp(−0.2·d²)` 의 **기울기**:

| d (m) | 값 | 기울기 | 2.5 m 대비 |
|---:|---:|---:|---:|
| 1.5 | 2.2317 | −1.34 | 최대점 부근 |
| **2.5** (현 과제 최대) | 1.0028 | −1.00 | 1× |
| 3.0 | 0.5785 | −0.694 | 0.7× |
| **5.0** | 0.0236 | −0.047 | **1/21** |
| **6.0** | 0.0026 | −0.0063 | **1/159** |
| 10.0 | ~0 | −2.9e−08 | 1/3천만 |
| 30.0 | 0 | −2.8e−77 | **0** |

⇒ **5–6 m 에서 유일한 조밀 신호가 21~159배 약해지고 10 m 부터는 float 상 0 이다.**
`constellation` 만으로는 5 m 목표를 **학습할 수 없다** — 정책이 목표 쪽으로 갈 이유가 없다.
⇒ **A(`goal_progress` 켜기) 없이는 B·C 가 원리적으로 불가능하다.** 순서 의존이지
취향이 아니다. "거리를 먼저 늘리고 보상은 나중에" 는 성립하지 않는다.

### ⛔ 내가 못 본 파손 지점 — `forward_hold` 가 2 m 를 **하드코딩**한다

`eval_goal_pose.py:754-758` 주석: *"2 m is the trained goal range (goal_dx ±2), so the
observation stays in distribution; a far goal like 20 m would not."*
⇒ 학습 범위를 5–6 m 로 올리면 **`forward_hold` 도 같이 올려야 한다.** 안 그러면 지속보행
프로브가 "거의 도착한 상태"를 반복 측정하는 **근거리 프로브로 조용히 바뀐다** —
그리고 그것이 우리가 **보행 속도를 재는 유일한 축**이다.
(`terminate_height`·`terminate_vel`·`episode_length_s 30` 은 5–6 m 에서 문제 없다.
30 m 는 21 s 라 에피소드에 겨우 들어가지만 리샘플 창 [4,8] s 와 충돌한다 — §2 와 같은 결론.)

### 우선순위 — 분석 세션 의견에 **동의한다**

> 제안의 진짜 가치는 정확도가 아니라 **waypoint 계층 제거**(아키텍처 단순화)다.
> 도착 실패는 거리 문제가 아니라 **정지 문제**였다(§1 ⚠️, §8-53).
> 그리고 3중 감사가 만장일치로 지적한 것: **N 배치 20+ arm 중 로봇에 올라간 것 0개,
> 실기 낙상 로그 0건 ⇒ "4발자국" 을 판정할 척도가 아예 없다.**
> 장거리 커리큘럼은 **시뮬 작업을 하나 더 늘리는 것**이다.

⇒ **합의된 계획**: A 를 하되 "정확도 개선" 이 아니라 **"장거리 학습이 가능한가" 의
타당성 시험**으로 좁힌다. **1차 엔드포인트는 낙상간격**이고 사전등록한다:

| | 대조군 `NH_zeroclock` | A 기각선 |
|---|---:|---|
| 정확도축 낙상간격 | 27,300 s | **< 13,650 s 면 그 자리에서 기각** |
| 보행축 낙상간격 | 9,978 s | **< 4,989 s 면 그 자리에서 기각** |
| 정확도 | 9.33 cm | 참고 지표(1차 아님) |

근거: §8-57 에서 원거리 유인을 강화(`constellation_weight`)했더니 **두 계보 모두**
보행 낙상간격이 **15배** 악화됐다. `goal_progress` 도 같은 부류일 수 있다.

**GPU 우선순위는 `NQ_armvendor` 와 로봇 시험 뒤**에 둔다.

## 6. 내가 지금 하지 않는 것

GPU 두 장이 `NM_goalstop`(3,9xx/6,000) · `NP_clockarm`(2,4xx/6,000) 으로 차 있고
큐에 `NQ_armvendor` 가 있다. **A 단계 arm 을 지금 큐에 넣지 않는다** — 위 세 축
(정지 보상 / 벤더 armature / 벤더 armature+게인)의 판정이 먼저 나와야 A 의 대조군이
정해진다. 판정이 나오는 대로 A 를 건다.
