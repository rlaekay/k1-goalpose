# 학습 쪽에서 배포 세션에 부탁하는 측정

작성: 2026-08-05, 학습 세션.
이 파일은 **요청 목록**이다. 배포 쪽 판단을 대체하지 않는다 — 로봇을 만지는 것은 그쪽이고,
아래는 "학습에서 못 닫는 것 중 로봇에서만 닫히는 것"만 골라 적은 것이다.
답을 적을 때는 각 항목 아래 `### 결과` 를 달아 주면 학습 쪽이 그대로 읽는다.

---

## R1. 다리 관절 토크 (⬇ 우선순위 내림 — MuJoCo가 대신 답했다)

> **2026-08-05 갱신**: MuJoCo 교차검증에서 토크 상한을 벤더값(45/30/30/45/20/20)과
> 학습값(30/20/20/40/20/15)으로 각각 180초 돌렸더니 과제오차 6.76 vs 6.70 cm,
> **낙상 둘 다 0**이었다. 정상 보행에서 정책이 토크 상한 근처에 가지 않는다.
> 그래서 이 측정은 이제 급하지 않다. **R3를 먼저 해 달라.**


**왜**: sim은 토크를 URDF `effort`로 **하드 클램프**한다
([envs/K1/goal_pose.py:592](htwk-gym/envs/K1/goal_pose.py:592)):

```
dof_torques = clip(kp·(target − q) − kd·q̇ − friction, ±effort)
```

그런데 `deploy_goal_pose.py:688 _fill_low_cmd`는 `q`/`kp`/`kd`만 보내고 `tau = 0`이다.
PD는 **온보드 모터 컨트롤러**가 돌고, config의 `common.torque_limit` 배열은 이 경로에서
**쓰이지 않는다**(`deploy_base_walk.py:186`과 `parameter_walk`만 읽는다).

즉 실기의 실제 토크 상한은 **모터 드라이버 내부 값이고 우리는 그것을 모른다.**
드라이버가 sim보다 관대하면, 정책이 학습 중 한 번도 겪지 않은 토크가 나간다.

**sim의 클램프 값** (이걸 넘는지가 질문이다):

| 관절 | 클램프 | kp | 포화 시작 각도오차 |
|---|---|---|---|
| Hip_Pitch | 30 N·m | 100 | 0.30 rad (17.2°) |
| Hip_Roll / Hip_Yaw | 20 | 100 | 0.20 rad (11.5°) |
| Knee_Pitch | 40 | 100 | 0.40 rad (22.9°) |
| Ankle_Pitch | 20 | 50 | 0.40 rad |
| Ankle_Roll | 15 | 50 | 0.30 rad |

**부탁**: 보행 중 `LowState`의 `motor_state[i].tau`를 다리 12관절(인덱스 10–21)에 대해
로깅하고, 관절별 **최댓값과 p99**를 알려 달라. 10 Hz면 충분하다.

- 넘지 않으면 → 이 항목은 닫힌다. sim의 클램프가 실기보다 빡빡하다는 뜻이고 안전한 방향이다.
- 넘으면 → 학습 쪽에서 토크 상한 자체를 랜덤화하거나 sim 클램프를 실측에 맞춘다.

---

## R2. 넘어지기까지의 세 걸음을, 버리지 말고 기록해 달라

⛔ **이 항목은 처음에 "2 m 목표로 연속 보행시켜 정상속도를 재 달라"로 썼다가 고쳤다.**
지금 로봇이 세 걸음도 못 간다는 것을 학습 쪽이 제대로 반영하지 않은 요청이었다.
연속 보행은 지금 성립하지 않는다.

**대신 중요한 것**: R1의 토크 질문은 **세 걸음이면 답이 나온다.** 지속 보행이 필요 없다.
포화가 일어난다면 그것은 무너지는 그 순간에 일어나지, 안정 순항 중에 일어나지 않는다.
오히려 **넘어지는 세 걸음이 가장 정보가 많은 구간**이다.

**부탁**: 지금 하던 대로(짧은 목표, 예: `./run_e0.sh fixed 0.6,0,0`) 돌리되,
넘어질 때까지의 전 구간에 대해 **동기화된 상태로**:

- 다리 12관절의 `motor_state[i].tau` (R1)
- 다리 12관절의 `motor_state[i].q` 와 정책이 보낸 `q_target` — 둘의 **차이**가
  PD가 실제로 얼마나 벌어진 채 돌고 있는지를 준다
- `policy_debug`의 `tilt_deg`
- 무엇이 먼저 왔는가: 발끼리 접촉 / 한쪽 기울어짐 / 상체 주저앉음 / 제자리 행진

가능하면 **영상 한 편**. 위 넷 중 무엇인지는 로그보다 영상이 빠르다.

**연속 보행(2 m 목표)은 나중이다.** 세 걸음 문제가 풀린 뒤에 의미가 있다.
참고로 그때 쓸 기준값은 이미 있다: sim `forward_hold`에서 정상 순항 **1.48 m/s**,
이동 중 몸통 높이 median **0.490 m**. 그리고 `fixed` 목표는 매 tick 로봇 **로컬
프레임**의 상수를 돌려주므로([deploy_goal_pose.py:92](htwk-gym/deploy/deploy_goal_pose.py:92),
[:1160](htwk-gym/deploy/deploy_goal_pose.py:1160)) 2 m를 주면 도달하지 못하고 계속 걷는다 --
sim의 그 프로브와 같은 조건이 된다.

---

## R3. (최우선) 지금 로봇에 실제로 올라가 있는 모델이 무엇인가

**왜**: `0cf9285`가 `policy_path`를 `goal_pose_i2a.pt` → `goal_pose_i3b.pt`로 바꿨는데,
그 커밋 메시지가 이렇게 적고 있다:

> Not yet on the robot: it was powered down for this step, so the copy and the
> joint-layout half of the cross-check are still outstanding.

즉 **config는 i3b를 가리키지만 로봇에 파일이 복사됐는지는 미확인**이다.
이게 중요한 이유: "세 걸음 가고 발끼리 부딪혀 넘어진다"는 증상은 **i2a의 증상**이고
(`0cf9285` 메시지가 그렇게 적고 있다), i3b는 정확히 그것을 고치려고 만든 것이다.
**i3b가 실제로는 안 올라간 상태에서 i2a로 테스트했다면, 스탠스 수정은 아직 한 번도
실기에서 시험되지 않은 것이다.**

**부탁**: 로봇 위 `models/` 의 파일 목록과 각각의 md5(또는 크기+수정시각).
서버 쪽 원본과 대조하면 어느 것이 올라가 있는지 확정된다.

---

## R5. (최우선) 관측 지연 — low_state_age_sec

**왜**: MuJoCo 감지 열화 스윕(§8-40)에서 **관측 지연만 여유가 0**이었다.
IMU 잡음·바이어스·자이로·`dof_vel`은 학습값의 10–17배까지 버티는데, 관측 지연은
**10 ms 무사 → 20 ms에서 2낙상/분 → 40 ms에서 49낙상/분**으로 무너진다.
그리고 학습은 관측 지연을 **전혀 모델링하지 않는다**(액션 쪽 0–18 ms만 있다).

**이미 측정 수단이 있다**: `deploy_goal_pose.py:1252`가 `policy_debug`에
`low_state_age_sec`를 10 Hz로 싣는다. 코드 수정이 필요 없다.

⚠️ **그런데 `fixed` 모드에서는 안 나온다.** `_publish_policy_debug`의 첫 줄이
`if self.goal_source_mode != "ros": return`이다. 지금까지의 시험이 전부 `fixed`였다.

**부탁**: `./run_e0.sh ros`로 한 번 돌리고(Brain이 목표를 쏘는 상태),
`/locomotion_test/policy_debug`의 **`low_state_age_sec`의 median과 p99**를 알려 달라.
걷는 동안 몇 십 초면 충분하다.

- 20 ms를 넘으면 → 원인을 잡았을 가능성이 높다. 학습에 관측 지연 랜덤화를 넣는다.
- 10 ms 아래면 → 이 가설도 죽는다. (단 이 값은 수신측 노후도라 **하한**이다.
  로봇 내부 센서→SDK 구간은 안 잡힌다.)

---

## R6. ⛔⛔ (최우선) 발행 루프의 실제 주파수 — `pub_hz`

**왜**: `deploy_goal_pose.py`의 발행 루프가 관절 목표에 필터를 건다:

```python
filtered = filtered * 0.8 + target * 0.2      # 발행 루프마다 1회
```

**계수 0.2는 500 Hz 발행을 가정한 값**이고 그때 시정수는 10 ms다. 그런데 그 루프는
파이썬이고 `time.sleep(0.001)`이다 -- 추론 루프는 설계 20 ms인데 **실측 25.3 ms**로
이미 밀린다. 루프가 느려지면 같은 계수가 훨씬 긴 시정수를 만들고, 필터가 **보행 자체를
깎는다**:

| 발행률 | 시정수 | 2 Hz 보행 감쇠 |
|---|---|---|
| 500 Hz (설계) | 10 ms | 0.99 |
| 100 Hz | 50 ms | 0.87 |
| **50 Hz** | **100 ms** | **0.66** |

**예측이 먼저였다**: 50 Hz면 0.66이라고 계산한 뒤 MuJoCo에서 0.64를 측정했고,
**실기 로그의 추종률(명령 대비 실제 도달)이 0.61**이다. MuJoCo에서 필터 발행률만
낮추면 낙상이 **0 → 60/분**이 된다.

**부탁 (로봇 안 움직여도 됨, 서 있기만)**:

```bash
cd ~/Workspace/deploy && ./run_e0.sh fixed 0,0,0 --log-timing /tmp/e0_pub.csv
```

30초면 충분하다. CSV의 **`pub_hz` 열**을 보면 된다.

- **50 Hz 근처** → 근본 원인 확정. 아래 수정을 켠다.
- **300 Hz 이상** → 이 가설은 죽는다.

### ⛔ 그리고 예측이 하나 더 있다 — 부하 의존

추론 루프와 발행 루프는 **GIL을 공유하는 파이썬 스레드**다. 정책 추론이 바쁠수록
발행 스레드가 굶는다. 그러므로:

**`pub_hz`는 걸을 때가 서 있을 때보다 낮아야 한다.**

맞다면 필터가 부하에 따라 나빠지고, 그것이 "몇 걸음 가다 무너진다"의 나선을 만든다 --
걷기 시작 → 부하 증가 → 발행률 하락 → 필터 시정수 증가 → 추종률 하락 → 정책이 더
크게 명령 → 부하 증가. **이 예측이 맞으면 원인이 하나로 닫힌다.**

**부탁**: 같은 로그 안에서 두 구간을 비교해 달라.
1. `./run_e0.sh fixed 0,0,0 --log-timing /tmp/e0_pub.csv` 로 30초 (서 있기)
2. 이어서 목표를 앞으로 주거나, 별도로 `fixed 1.0,0,0` 으로 짧게 (걷기)

CSV의 `walking` 열로 두 구간이 갈리므로, **`walking=0`일 때와 `walking=1`일 때의
`pub_hz` median을 각각** 보면 된다.

### 수정 (이미 들어가 있음, 기본 꺼짐)

```bash
./run_e0.sh fixed 1.0,0,0 --rate-fixed-filter --log-timing /tmp/e0_fix.csv
```

실측 `dt`로 필터 계수를 매번 다시 계산해 **시정수를 10 ms로 고정**한다. 30–500 Hz
어디서든 같다. MuJoCo에서 이 수정은 발행률 30 Hz에서도 낙상을 **70 → 2/분**으로,
추종률을 **0.49 → 0.85**로 되돌린다.

⚠️ 기본을 꺼둔 것은 **지금 동작이 하드웨어에서 시험된 것이고 수정본은 아니기**
때문이다. 같은 로그에서 A/B 해서 고르면 된다.

---

## R4. 영점 상태

**왜**: 정책은 `Linear(54→256→128→128→12)`이고 **순환도 프레임 스택도 없다**.
`dof_pos + bias`를 보지만 `bias`를 절대 못 본다 — 랜덤화로 **보정**하는 것은 구조적으로
불가능하고, 살 수 있는 것은 "어떤 offset에서도 안 넘어지는 보수적 자세"뿐이다.
그리고 지금 올라간 체크포인트는 그 랜덤화조차 **꺼진 채**(`joint_encoder_bias`,
`joint_target_offset` 둘 다 `[0, 0]`) 학습됐다.

**부탁**: 마지막 영점 보정이 언제였는지, 그리고 가능하면
**발을 바닥에 평평하게 놓은 자세에서 각 관절의 인코더 값**. 순기구학으로 기대되는 값과
비교하면 관절별 드리프트의 **크기와 부호**가 나온다. 그 숫자가 있으면 학습 쪽에서
랜덤화 폭을 추측이 아니라 실측으로 정할 수 있다.

---

## 학습 쪽이 지금 알고 있는 것 (참고)

- 배포 후보 `I3b_stance10 model_200`의 sim 성능: 과제오차 3.86 cm, heading 1.7°,
  3 seed × clean/force 27,672 구간에서 낙상 0.58/1000
- 배포 계약은 전부 일치 확인: `goal_dx ±2.0`/`goal_dy ±1.5` = `goal_clamp 2.0/1.5`,
  `gait_frequency 2.0 ∈ [1.8, 2.4]`, PD 게인 `{Hip 100, Knee 100, Ankle 50}` /
  `{2, 2, 1}`, obs 54 / act 12, 기본 자세 일치
- ⛔ **sim 낙상률과 실기 낙상은 아직 한 번도 대응된 적이 없다.** sim에서 0.06 %인데
  실기에서 세 걸음이면, 지금의 sim 낙상 지표는 실기를 예측하지 못한다는 뜻이다.
  R1–R4가 그 간극을 좁히려는 것이다.
