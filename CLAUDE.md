# k1-goalpose 작업 규칙

> ## 🔔 사용자에게 **먼저 말할 것** (미처리 항목)
>
> 사용자가 잊더라도 내가 먼저 꺼내야 하는 것들이다. 처리했으면 이 블록에서 지운다.
>
> - ~~N 배치 **1라운드** 결과 보고~~ — 2026-08-07 09:5x 보고 완료(ibatch §8-47).
> - ~~`N2_pathgrid` + `NA_histzero` 두 축 보고~~ — 2026-08-07 16:0x 보고 완료
>   (`HANDOFF_DEPLOY_ENTRY_20260807.md` §8, `logs/eval_rounds/n2na`).
> - ~~**파레토 스캔(거친/정밀) 결과 보고**~~ — 2026-08-07 18:00·22:xx 보고 완료.
>   ⛔ **그때 같이 말한 두 문장은 철회됐다** — [RETRACTIONS.md](RETRACTIONS.md) C5·C12.
>   ① *"무릎이 아니라 절벽"* → **급경사다.** 정밀 스캔에 해상 가능한 중간점이 있다
>   (N1 it200 24.5 / it300 37.6 cm). 그리고 "그 뒤 평평"도 틀렸다(it900→it6000 +20 %).
>   ② *"N2 it5100 이 속도 요구를 처음 넘겼다"* → **최초가 아니라 최댓값**이다
>   (N1 it900 이 1.38 로 먼저 넘겼다). 게다가 `forward_hold` 의 `body_speed` 는
>   **도착·정지를 제거한 조건**이라 사용자 요구 G2 와 같은 양이 아니다.
>   **유효하게 남는 결론은 하나: 게이트 5 cm 를 통과하는 체크포인트가 없다.**
> - 🔔 **백필 채점(`NC_actfilter` / `NZ_zeroiid`) 두 축 보고** — 사용자가 2026-08-07
>   22:1x 에 "백필 채점 끝나면 두 축 보고해"라고 요청했다. 백필 레인에서 학습과
>   병행 중이고 결과는 `logs/eval_rounds/n2na` 에 쌓인다
>   → `python tools/round_table.py logs/eval_rounds/n2na`.
>
>   ⚠️ **비교 규칙을 숫자 보기 전에 못 박아 둔다**(같은 실수를 열세 번 한 뒤에 만든 규칙):
>   1. 둘 다 **계보 B**(`N1_path/model_100` warm start)다. **`N1_path` 와 나란히 놓지
>      마라 — 출발점이 다르다**(RETRACTIONS C9). 올바른 대조군은 **`NE_ctrl100`** 이고
>      아직 학습 중이다. 그래서 이번 보고는 **절대값 + 두 arm 상호 비교까지**이고,
>      **"레버가 효과가 있다/없다"는 `NE_ctrl100` 이 나온 뒤에만 말한다.**
>   2. 표를 쓰기 전에 `python tools/claim_check.py <A> <B>` 를 돌려 ⛔ 가 없는지 본다.
>   3. 낙상은 원시 개수가 아니라 **낙상간격(초)** 으로 읽는다(C14).
>   4. 정확도 축에는 **생존 편향**이 있다 — 표본탈락(%) 을 같이 본다.
>   5. `v7_extras`(발 간격 등)는 **마지막 한 프레임 스냅샷**이라 인용하지 마라(C11).
>   6. ⛔ `NC_actfilter` 의 레버(`action_filter_tau`)는 **공통 평가 config 에 없다** —
>      채점은 필터 OFF 로 돈다. "필터를 학습시켰더니 좋아졌다"를 이 표로 말할 수 없다.
> - **`NA_histzero`** — 사용자가 직접 요청한 arm(이력 + 영점 오차 상호작용 셀).
>   ⛔ **결과 해석 보류.** 두 가지가 겹쳤다: warm start 가 `best.pth`(=N1 **model_100**,
>   가변 심볼릭 링크)였고, **대칭손실이 확장 관측(270)을 안 덮는 결함 위에서 완주**했다.
>   즉 잰 것이 "이력+영점"이 아니라 "이력 + 깨진 대칭손실"이다. 대조군 `NE_ctrl100` 대기.
> - ⛔ **관측 넓힌 arm 5개(NA·N4·N5·N6·N7)는 `export_model.py` 가 베이스 config 폭
>   (54/14)으로 `ActorCritic` 을 만들고 strict 로드해서 export 가 안 된다.**
>   `install_policy.sh:145` 의 스모크도 `(1,54)` 하드코딩이다. 배포 **런타임**은
>   (`policy_goal_pose.py:100-116`) 이미 지원하므로 끊긴 곳은 export 한 곳뿐이다. 미수정.
> - **⚠️ 이름 충돌 주의**: `N8_pathdelay`(Codex, 관측 지연)와
>   `NZ_zeroiid`(내 것, 관절 영점)는 **다른 arm**이다. 원래 둘 다 N8 이었고 내 쪽을
>   바꿨다. 사용자가 "N8"이라고만 하면 어느 쪽인지 되묻지 말고 문맥으로 판단하되,
>   명령을 걸기 전에 전체 이름으로 확인한다.
>
> ## 📌 세션 간 인계 — 먼저 읽어라
>
> 이 파일은 모든 세션 시작 시 자동으로 컨텍스트에 올라간다. 그래서 세션 사이의
> 전달은 여기에 포인터를 두는 방식으로 한다.
>
> - ⭐⭐ **[RETRACTIONS.md](RETRACTIONS.md) 를 먼저 읽어라.** 철회·정정 **24건**이 들어 있고,
>   아래 문서들 중 여러 개가 그 대상이다. **어느 문서를 읽든 이것과 대조하고 읽어라.**
> - ⭐⭐ **[AUDIT_20260807_2300.md](AUDIT_20260807_2300.md)** (2026-08-07 23:00) —
>   **7축 독립 감사. 현재 유효한 최신 종합이고 아래 문서 전부를 대체한다.**
>   용의자 14개 중 **5개가 죽었고, 죽은 것 중 3개는 같은 날 "가장 강한 리드"로 보고된 것**이다.
>   살아남은 것 중 최강 둘은 아무도 의심하지 않던 것이다:
>   ① **도착이 죽는 원인은 보행 시계다** — 시계를 얼린 목표(`stand`) 2.3 cm 대
>   같은 변위 0인데 시계가 도는 목표(`turn`) **47.4 cm**. 그리고 그것을 고치라고 있는
>   `pause_gait_during_dwell` 이 **기본 False 이고 어떤 config 에도 없다** →
>   **`ND_dwell`(duty 상향)은 방향이 반대다.**
>   ② **정확도 보상을 40+ arm 중 한 번도 안 건드렸다**(`goal_reached` 이진 계단,
>   `heading_near_goal` 스케일 0, `constellation_radius` 1.0 고정).
>   실기 쪽 최강 리드는 **발목 roll 이 URDF 하드 스톱(±0.345 rad) 밖에서 돈다**(실기 ±0.55).
> - ⚠️ **[STATUS_20260807_1800.md](STATUS_20260807_1800.md)** (2026-08-07 18:00) —
>   전 축 종합 분석. 조사 범위와 구조는 유효하지만 **결론 다섯 개가 그 뒤 철회됐다**
>   (RETRACTIONS C4·C5·C6·C11·C12). 특히 §1-1("절벽"), §3-A("트레이드오프"),
>   §2-4 발 간격 열은 **읽지 마라.** 그리고 §3-B1의 *"N0_ctrl model_6000 정확도가
>   측정된 적 없다"* 는 그 뒤 측정됐다 — **3.63 cm** (120 s, 4,621구간, 낙상 5).
>   **같은 체크포인트로 맞춘 최종 그림**:
>
>   | model_6000 | 정확도 | 보행 속도 | 보행 낙상률 |
>   |---|---:|---:|---:|
>   | `N0_ctrl` | **3.63 cm** | 1.10 | 0.00347 |
>   | `N1_path` | **47.70 cm** | **1.48** | **0.00066** |
>
>   즉 **정확도 13배를 팔아 속도 1.35배를 샀다.** v7 배치(6.3 → 37.9)보다 **나쁘다** —
>   dwell 이 충돌을 줄였다는 서사는 이 숫자 위에서 성립하지 않는다(C4).
> - **[HANDOFF_TO_TRAINING.md](HANDOFF_TO_TRAINING.md)** (2026-08-05) — 실기·MuJoCo
>   라운드 결과. **⛔ 지금 도는 M3/M4가 틀린 발 리비전 위에 있다**(로봇 실물은
>   19.666 kg / 발 0.4940 kg, 우리 학습은 18.714 / 0.38305). 죽은 가설 넷, 외력 모델의
>   물리적으로 불가능한 결함, 감지 열화 스윕의 랜덤화 근거가 같이 들어 있다.
>   **읽었으면 문서 맨 아래 ACK를 채워 커밋할 것.**
> - **[HANDOFF_NIGHT_20260806.md](HANDOFF_NIGHT_20260806.md)** (2026-08-06 야간) — 실기
>   증상의 signature를 재현 목표로 둔 자율 조사. **배포 필터가 루프 속도에 의존**해서
>   보행을 깎는다는 것이 가장 강한 후보(예측 0.66 → MuJoCo 0.64 → 실기 0.61). 수정
>   `--rate-fixed-filter`가 MuJoCo에서 증상을 없앤다. **다음 한 걸음은 로봇 30초
>   `pub_hz` 측정**(R6). 밤새 고친 방법론 오류 넷도 같이 있다.
> - **[FILTER_AUDIT-non-codex.md](FILTER_AUDIT-non-codex.md)** (2026-08-07) — Codex의
>   필터 가설(§8-45/§8-47) 감사. ②/③ 대조·질량 기각·떨림 기각은 옳다. **그러나
>   인과 전제가 코드와 다르다**: `sleep(0.001)`은 속도 제한이 아니라 데드라인 폴링이고
>   `common.dt: 0.002` → 루프는 **500 Hz를 겨냥**한다. "50 Hz로 돈다"는 가정이지 측정이
>   아니다. 그리고 `base_walk`는 **같은 필터·같은 루프·같은 dt로 걷는다** —
>   사용자의 반증이 아직 안 풀렸다(실마리: 우리 `gait_frequency` 2.0 vs base_walk 1.0).
>   **`pub_hz` 30초 실측이 유일한 판별 측정이다.**
> - **[HANDOFF_ROBOT_20260807.md](HANDOFF_ROBOT_20260807.md)** (2026-08-07 15:5x) —
>   **InitChannel GIL 데드락 해결.** 앞선 노트의 두 결론을 정정한다: "발행 중이면 매번
>   데드락"은 성공 로그가 반증하며 **경합**이고, "별도 스레드로 풀라"는 **GIL이 프로세스
>   전역이라 안 통한다**(SDK도 핸들러를 생성자로만 받는다). `faulthandler` 워치독으로
>   잘라내고 `run_e0.sh`가 재시도한다(커밋 `b71dc13`).
>   ⛔ **로봇에 배포 프로세스 두 개(33763, 42133)가 살아 있다 — 먼저 정리할 것.**
>   로봇 접속·의존성·경로 등 확정 사실이 §4에 모여 있다.
> - **[HANDOFF_DEPLOY_ENTRY_20260807.md](HANDOFF_DEPLOY_ENTRY_20260807.md)** (2026-08-07 저녁)
>   — **`b` 누르면 앞으로 쏠리던 것**의 원인과 수정. `--hold-diag` 실측이 갈랐다:
>   관절 추종은 0.6°인데 몸통 tilt 4.9–13.8°였다 = 서보가 아니라 **명령한 자세가
>   곧게 못 선다**. `b`~`r`에는 균형을 닫는 주체가 없어 `hip+knee+ankle=0`인 자세를
>   잡아야 하는데 RL 자세는 −0.05였다. 복구 경로 넷(get-up 대기·램프 속도·IS_READY
>   오판정·ModeMonitor)도 같이 들어갔다. **다음 한 걸음은 `--hold-diag 15`의 tilt
>   하나** — 2° 안쪽이면 맞은 것, 그대로면 영점(R4)이다.
> - **[HANDOFF_MUJOCO_FILTER_20260807.md](HANDOFF_MUJOCO_FILTER_20260807.md)** (2026-08-07 07:0x)
>   — **MuJoCo에서 실기 붕괴를 재현하고 수정으로 지웠다.** 배포 필터를 50 Hz로 돌리면
>   68구간 중 **66회 낙상**(실기 증상 그 자체)이고, 시정수를 고정하면 0이 된다.
>   수정본은 **50·150·200·500 Hz 전부 낙상 0**이라 **R6(`pub_hz` 실측)이 불필요해진다** —
>   500 Hz에서 켜나 안 켜나 같으므로 손해가 없다. 실물 질량(19.666 kg)도, 떨림도,
>   관측 지연 단독도 같이 기각됐다. ⛔ **서 있기로는 이 버그를 못 잡는다**(실측 확인:
>   50 Hz와 500 Hz가 구별 안 됨) — 저역통과는 상수를 그대로 통과시키기 때문이고,
>   그것이 "서 있기는 완벽한데 세 걸음에서 무너진다"와 정확히 맞는다.
>   상세는 `ibatch.md` §8-45/§8-47.
> - **[HANDOFF_TRAINING_20260807.md](HANDOFF_TRAINING_20260807.md)** (2026-08-07) —
>   **학습 쪽 세션 전체 정리.** 근본 원인(§8-44)부터 파레토 절벽까지, 만든 도구 14개,
>   arm 결과 두 축 표, **내가 낸 실수 9건과 수정**, 열려 있는 것. 학습 쪽을 이어서
>   할 사람은 이거 하나만 읽으면 된다.
>   ⛔ 지금 상태 한 줄: **배포 가능한 체크포인트가 아직 없다.**
>   ⛔⛔ 이 문서의 *"N0_ctrl 이 세 축 모두 지배"* 는 **철회됐다**(ibatch §8-49, §8-50).
>   같은 체크포인트(model_6000)로 맞추면 path 가 속도 1.35배, 보행 낙상 5~7배 낮다.
>   지배는 **정확도 한 축에서만** 성립하고, 그 정확도 비교조차 N0 는 best(2200),
>   N1 은 model_6000 이라 체크포인트가 달랐다.
>   ✅ **그 빠진 숫자는 이제 있다: `N0_ctrl model_6000` = 3.63 cm** (120 s, 4,621구간,
>   낙상 5). 맞춘 비교는 위 STATUS 항목의 표를 봐라.
> - **[DEPLOY_REQUESTS_FROM_TRAINING.md](DEPLOY_REQUESTS_FROM_TRAINING.md)** — 학습이
>   못 닫고 로봇에서만 닫히는 측정 요청. R1·R5는 이미 답이 나와 종결됐다.
> - **[ibatch.md §8-44](ibatch.md)** (2026-08-07) — ⛔ **근본 원인 확정: 우리 과제가
>   보행을 요구한 적이 없다.** 배포 정책과 M 배치 전부가 `path: 0.0` 위에서 돌았고,
>   그 과제의 요구 속도는 median 0.119 m/s / 몸통속도 median 0.03 m/s다. 평가가
>   학습과 같은 과제라 **지표가 보행을 잰 적이 없다**. N 배치(path 복원)가 돌고 있고,
>   평가에 `--goal_pattern forward_hold` 축이 새로 들어갔다.
>
> ### 서버 자율 운전 — 켜져 있는 것들
>
> GPU가 13.8시간 놀았던 사고(§8-44) 이후 감시를 코드로 옮겼다. 서버에서 상시 실행:
>
> - `tools/idle_watch.sh` → `queue/idle_state.json` (30초마다, 세 신호 교차확인)
> - `tools/autopilot.sh` → 유휴 30분이면 `queue/plan/gpu<N>/`에서 다음 작업 승격
> - `tools/gpu_queue.sh 0` / `1` → 카드당 워커 하나
>
> 상태는 **`ssh a6000 'bash /mnt/DATA/workspace/ws_eungkyu/k1-goalpose/htwk-gym/tools/round_status.sh'`**
> 한 번으로 읽는다. 셋 중 하나라도 죽어 있으면 다시 띄워라 —
> `setsid nohup bash tools/<name>.sh < /dev/null > queue/<name>.log 2>&1 &`.
> ⚠️ `pkill -f "<name>.sh"`는 **자기 ssh 세션을 죽인다**(명령줄에 그 문자열이 있다).
>
> 인계 문서를 새로 만들면 **여기에 한 줄 추가**한다. 그러지 않으면 다음 세션이 못 본다.

## ⛔ 비교 주장을 하기 전에 — 이 규칙이 먼저다

2026-08-07 하루에 주장을 **열세 번** 바꿨다. 무작위가 아니었다. 전부 같은 모양이다:

> **비교 대상 두 쪽의 조건이 같은지 확인하기 전에 결론을 말했다.**

체크포인트(best vs final), 프로토콜 길이(60 s vs 120 s), 표본 밀도(2점 vs 8점),
과제(도착 있음 vs 없음), 키의 의미(간격 vs 주기), 파일 vs 심볼릭 링크,
지표의 집계 범위(마지막 1프레임) — 매번 **내가 본 것 바로 옆**이 어긋나 있었다.

**그래서 규칙 두 개다.**

1. **두 숫자를 나란히 쓰기 전에 `claim_check.py` 를 돌린다.**
   ```bash
   cd htwk-gym && python tools/claim_check.py <report.json A> <report.json B>
   ```
   ⚠️ **경로는 `htwk-gym/tools/` 다.** 저장소 루트에서 `tools/claim_check.py` 를 치면
   없다(루트 `tools/` 는 배포 도구다). 2026-08-07 에 실제로 그걸로 한 번 헛짚었다.

   조건 12개를 나란히 찍고 어긋난 것에 ⛔ 를 붙인다. ⛔ 가 붙은 지표는 **비교 불가**다.
   조건을 맞춰 다시 재거나, 주장에 그 차이를 명시해라.

   **읽는 법 두 가지** (실측으로 확인, 2026-08-07 23:xx):
   - `run` 의 ⛔ 는 **arm 끼리 비교할 때 항상 뜬다.** 그게 비교의 목적이므로 무시해라.
     진짜로 봐야 하는 것은 `checkpoint · goal_pattern · duration_s · protocol_sha · env_code_sha` 다.
   - `env_code_sha` 는 **채점 시점의 env 코드**다. 학습 시점이 아니다. 두 리포트가
     다른 커밋에서 채점됐으면 그 커밋이 eval 경로를 건드렸는지 직접 확인해야 한다
     (예: `2076c49` 는 `history_steps=1`·`obs_delay=[0,0]` arm 에는 no-op 이지만,
     **"no-op 이다"를 확인하지 않고 넘어가면 그것이 바로 이 규칙이 막으려는 것이다**).
   - `표본탈락 (%)` 열이 **생존 편향의 크기**다. 낙상으로 끝난 구간은 정확도 표본에서
     빠진다. 이 값이 arm 마다 크게 다르면 정확도 순위 자체가 성립하지 않는다
     (I3b 36.8 % 대 N0/N1 0.11~0.15 %).

2. **[RETRACTIONS.md](RETRACTIONS.md) 를 읽는다.** 철회·정정된 주장 14건이 왜 틀렸고
   무엇이 최종인지 적혀 있다. **거기 있는 주장을 다시 세우려면 왜 이번엔 다른지를
   먼저 써야 한다.** `claim_check.py` 가 관련 항목을 자동으로 꺼내 준다.

기록을 남기는 것보다 **주장하는 순간에 읽히게 하는 것**이 어렵다. 그래서 도구가 한다.

---

## ⛔ 문제를 찾았으면 **끝장을 봐라** (2026-08-08 사용자 지시)

> **"현재 상태에 대한 혹은 무엇이든 문제를 찾으면 추가 investigation을 해서 꼭 결과를
> 내서 끝장을 보고 나에게 보고. 대충 '이런 문제 있어요' 하고 넘어가는 거 금지."**

발견을 보고 항목으로만 남기는 것은 **일을 안 한 것**이다. 문제를 하나 찾으면 그 자리에서:

1. **원인까지 내려간다.** "X가 이상하다"가 아니라 "X는 Y 때문이고 근거는 파일:행이다"
2. **판정 측정을 사전 고정한다.** 무슨 값이 나오면 확증이고 무슨 값이면 기각인지
3. **수정안을 낸다.** 코드든 config든 실험이든. "제안한다"로 끝내지 말고 **적극적으로 제안하고
   되도록 만들어 둔다** -- 근거가 없는 설정을 발견했으면 그것을 **고치는 것까지가 발견이다**
4. **못 닫으면 왜 못 닫는지와 누가 닫을 수 있는지를 적는다**

전례: `anti_mirror = 0.20` 의 유일한 근거 문장이 존재하지 않는 절을 인용하고 있는 것을
찾아 놓고 "유령 인용" 이라고 적고 넘어갔다. 그 값으로 `N9_zerostruct` 가 설계돼 있는데도.
전례: 실기 발목 roll 이 URDF 하드 스톱 밖에서 돈다는 것을 찾아 놓고 "무부하 3분이면
갈린다" 로 끝냈다. 그 3분을 설계해서 안전성까지 확인해 두는 것까지가 일이다.

## ⛔ 나에게 말할 때는 **교차검증하고 말해라** (2026-08-08 사용자 지시)

> **"같은 걸 두세 번씩 독립적으로 검색하라."**

한 번 grep 해서 나온 것으로 단정하지 마라. 같은 사실을 **서로 다른 경로**로 확인한다 --
코드 / config / 로그 / 실행 결과 / 문헌. 특히 **"없다"는 주장**은 검색어를 바꿔 최소 두 번
확인한다. 2026-08-07 에 `pub_hz` 를 "미측정" 이라고 세 문서가 말했는데 §8-41 이 이미
쟀었다.

---

## ⛔ 문제를 찾았으면 **끝장을 봐라** (2026-08-08 사용자 지시)

> **"현재 상태에 대한 혹은 무엇이든 문제를 찾으면 추가 investigation을 해서 꼭 결과를
> 내서 끝장을 보고 나에게 보고. 대충 '이런 문제 있어요' 하고 넘어가는 거 금지."**

발견을 보고 항목으로만 남기는 것은 **일을 안 한 것**이다. 문제를 하나 찾으면 그 자리에서:

1. **원인까지 내려간다.** "X가 이상하다"가 아니라 "X는 Y 때문이고 근거는 파일:행이다"
2. **판정 측정을 사전 고정한다.** 무슨 값이 나오면 확증이고 무슨 값이면 기각인지
3. **수정안을 낸다.** 코드든 config든 실험이든. "제안한다"로 끝내지 말고 **적극적으로 제안하고
   되도록 만들어 둔다** -- 근거가 없는 설정을 발견했으면 그것을 **고치는 것까지가 발견이다**
4. **못 닫으면 왜 못 닫는지와 누가 닫을 수 있는지를 적는다**

전례: `anti_mirror = 0.20` 의 유일한 근거 문장이 존재하지 않는 절을 인용하고 있는 것을
찾아 놓고 "유령 인용" 이라고 적고 넘어갔다. 그 값으로 `N9_zerostruct` 가 설계돼 있는데도.
전례: 실기 발목 roll 이 URDF 하드 스톱 밖에서 돈다는 것을 찾아 놓고 "무부하 3분이면
갈린다" 로 끝냈다. 그 3분을 설계해서 안전성까지 확인해 두는 것까지가 일이다.

## ⛔ 나에게 말할 때는 **교차검증하고 말해라** (2026-08-08 사용자 지시)

> **"같은 걸 두세 번씩 독립적으로 검색하라."**

한 번 grep 해서 나온 것으로 단정하지 마라. 같은 사실을 **서로 다른 경로**로 확인한다 --
코드 / config / 로그 / 실행 결과 / 문헌. 특히 **"없다"는 주장**은 검색어를 바꿔 최소 두 번
확인한다. 2026-08-07 에 `pub_hz` 를 "미측정" 이라고 세 문서가 말했는데 §8-41 이 이미
쟀었다.

---

## 답할 때 토큰을 아끼지 마라

아래 네 가지는 **비용이 아니라 전제**다. 여기서 아낀 토큰은 전부 GPU 시간으로 되돌아왔다.
이 저장소에서 날린 시간의 거의 전부가 "확인을 안 해서"였지 "생각을 덜 해서"가 아니다.

### 1. 전체 흐름 속에서 하려는 일을 파악한 뒤에 작업한다

단발 요청으로 받지 마라. 지금 무엇을 판정하려는 라운드인지, 그 판정이 어떤 앞선 결론에
기대고 있는지 먼저 세우고 움직인다.

> 전례: 스프린트 설계를 세 번 틀렸다. 매번 국소적으로는 말이 됐지만
> "배포는 목표를 2 m에서 자른다"는 상위 사실과 안 맞았다.
> 전례: L1을 `--ref 0.0253`으로 죽였다. 그 값은 **구 발 관성**에서 나온 기준이었다.
> 학습한 적 없는 과제로 정책을 채점한 것과 같은 부류의 오류다. L3도 같은 값에 죽었다.

### 2. 수정하는 파일은 전문을 읽는다

일부만 읽고 고치지 마라. 한 줄이라도 건드린 파일은 처음부터 끝까지 읽고,
의도가 100% 반영됐는지 직접 확인한다. diff만 보는 것은 확인이 아니다.

> 전례: `swing_apex`를 `done`에서 리셋하지 않았다. 스폰 기하를 스윙 높이로 기록할 뻔했다.
> 전례: `results["commands"]`는 존재하지 않는 키였다. 프로브 가드가 한 번도 발동하지 않아
> forward_hold에서 55.66 cm 도착 오차를 출력했다 — 도착이 정의되지 않는 프로브에서.

### 3. 관련 키워드로 현재 작업을 조사한다

고치려는 이름(변수·보상·플래그)을 저장소 전체에서 grep해서, 그것을 읽는 다른 경로가
있는지 본다. 병렬로 도는 Codex 쪽 커밋도 같이 본다.

> 전례: `--config`를 바꿔 한 가지를 바꾸려 했는데 **24개 키가 같이 바뀌었다**(발 기하,
> 경로 혼합, 노이즈 전부, 외란, 높이 목표, 보상 스케일). 35.6분을 버렸다.
> 그래서 규칙: **하나를 바꾸려고 `--config`를 갈아끼우지 않는다.**

### 4. 실행파일이 나오면 그 영향 아래 있는 것을 따진다

스크립트를 돌리기 전에 그 스크립트가 무엇을 읽고 무엇을 덮어쓰는지 확인한다.
기본값이 무엇인지, 인자를 빠뜨리면 무엇이 되는지까지 본다.

> 전례: 배치가 200 iteration인데 `--max_iterations 12000`으로 선택기를 돌렸다.
> 전례: `setsid` 서브셸이 conda PATH를 잃어 `python: command not found`가 났다.
> 이후로는 `bash -lc "source .../conda.sh && conda activate k1goalpose && ..."`로 감싼다.

---

## 이 저장소 고유의 규칙

- **평가/환경 변경**: 한 가지를 바꾸려고 `--config`를 통째로 바꾸지 않는다.
  선택기 iteration에 상한을 건다. 순위는 **task error**(`sqrt(d² + r²θ²)`)로 매긴다 —
  `strict`는 heading에 둔감하다.
- **코드 전달은 git으로**, 채팅으로 붙여넣지 않는다. 저장소에 쓰고 커밋하면 서버에서 pull한다.
- **`-codex` 접미사 문서는 Codex 것**이다. 접미사 없는 문서가 내 것이고,
  Codex 것에 대한 감사는 `*-non-codex.md`에 쓴다.
- **공유 파일을 고치기 전에 병렬 커밋을 확인**하고 코드 경로 전체를 읽는다.
- 결과는 `ibatch.md`에 판정 단위로 남긴다.
