# k1-goalpose 작업 규칙

## 답할 때 토큰을 아끼지 마라

아래 네 가지는 **비용이 아니라 전제**다. 여기서 아낀 토큰은 전부 GPU 시간으로 되돌아왔다.
이 저장소에서 날린 시간의 거의 전부가 "확인을 안 해서"였지 "생각을 덜 해서"가 아니다.

### 1. 전체 흐름 속에서 하려는 일을 파악한 뒤에 작업한다

단발 요청으로 받지 마라. 지금 무엇을 판정하려는 라운드인지, 그 판정이 어떤 앞선 결론에
기대고 있는지 먼저 세우고 움직인다.

> 전례: 스프린트 설계를 세 번 틀렸다. 매번 국소적으로는 말이 됐지만
> "배포는 목표를 2 m에서 자른다"는 상위 사실과 안 맞았다.
> 전례: L1을 `--ref 0.0253`으로 죽였다. 그 값은 **구 발 관성**에서 나온 기준이었다.
> 학습한 적 없는 과제로 정책을 채점한 것과 같은 부류의 오류다. L3도 같은 값에 죽었다.

### 2. 수정하는 파일은 전문을 읽는다

일부만 읽고 고치지 마라. 한 줄이라도 건드린 파일은 처음부터 끝까지 읽고,
의도가 100% 반영됐는지 직접 확인한다. diff만 보는 것은 확인이 아니다.

> 전례: `swing_apex`를 `done`에서 리셋하지 않았다. 스폰 기하를 스윙 높이로 기록할 뻔했다.
> 전례: `results["commands"]`는 존재하지 않는 키였다. 프로브 가드가 한 번도 발동하지 않아
> forward_hold에서 55.66 cm 도착 오차를 출력했다 — 도착이 정의되지 않는 프로브에서.

### 3. 관련 키워드로 현재 작업을 조사한다

고치려는 이름(변수·보상·플래그)을 저장소 전체에서 grep해서, 그것을 읽는 다른 경로가
있는지 본다. 병렬로 도는 Codex 쪽 커밋도 같이 본다.

> 전례: `--config`를 바꿔 한 가지를 바꾸려 했는데 **24개 키가 같이 바뀌었다**(발 기하,
> 경로 혼합, 노이즈 전부, 외란, 높이 목표, 보상 스케일). 35.6분을 버렸다.
> 그래서 규칙: **하나를 바꾸려고 `--config`를 갈아끼우지 않는다.**

### 4. 실행파일이 나오면 그 영향 아래 있는 것을 따진다

스크립트를 돌리기 전에 그 스크립트가 무엇을 읽고 무엇을 덮어쓰는지 확인한다.
기본값이 무엇인지, 인자를 빠뜨리면 무엇이 되는지까지 본다.

> 전례: 배치가 200 iteration인데 `--max_iterations 12000`으로 선택기를 돌렸다.
> 전례: `setsid` 서브셸이 conda PATH를 잃어 `python: command not found`가 났다.
> 이후로는 `bash -lc "source .../conda.sh && conda activate k1goalpose && ..."`로 감싼다.

---

## 이 저장소 고유의 규칙

- **평가/환경 변경**: 한 가지를 바꾸려고 `--config`를 통째로 바꾸지 않는다.
  선택기 iteration에 상한을 건다. 순위는 **task error**(`sqrt(d² + r²θ²)`)로 매긴다 —
  `strict`는 heading에 둔감하다.
- **코드 전달은 git으로**, 채팅으로 붙여넣지 않는다. 저장소에 쓰고 커밋하면 서버에서 pull한다.
- **`-codex` 접미사 문서는 Codex 것**이다. 접미사 없는 문서가 내 것이고,
  Codex 것에 대한 감사는 `*-non-codex.md`에 쓴다.
- **공유 파일을 고치기 전에 병렬 커밋을 확인**하고 코드 경로 전체를 읽는다.
- 결과는 `ibatch.md`에 판정 단위로 남긴다.
