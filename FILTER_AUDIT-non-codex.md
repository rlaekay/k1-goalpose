# 필터 가설 감사 (Codex §8-45 / §8-47에 대한 비-Codex 감사)

2026-08-07. `CLAUDE.md` 규칙에 따라 Codex 작업에 대한 감사는 이 파일에 쓴다.

대상: 커밋 `c699919`(§8-45), `309adbe`(§8-47), `44c5247`(렌더 수정), 그리고 그 사이의
발행률 스윕·2D 스윕·seed 스윕.

---

## 1. 옳고 잘 한 것

이건 이 저장소가 오래 못 하던 종류의 작업이다. 먼저 인정하고 시작한다.

- **②/③ 대조가 결정적이다.** 같은 자산·같은 정책·같은 프로토콜에서 **계수 하나**만
  바꿔 66/68 낙상 → 0/47이 됐다. 레버가 하나이고 되돌림까지 확인됐다.
- **질량·관성을 깨끗하게 기각했다.** §8-39가 `K1_serial.xml`(18.7142 kg = 학습과 같은
  틀린 값)로 돌아 두 개의 같은 로봇을 비교하고 있었다는 지적이 정확하고, 실물
  19.666 kg으로 바로잡아도 6.73 → 6.70 cm다. 이건 M 배치 전체의 전제를 검증해 준다.
- **떨림을 스펙트럼으로 죽였다.** "무릎 토크가 초당 23회 부호전환"이라는 무서운
  통계를 그대로 안 믿고 최대주파를 봤더니 보행 2 Hz 그 자체였고, Nyquist 대역
  에너지가 0.3~1.8 %뿐이었다. **요약통계를 의심하고 분포를 본** 올바른 절차다.
- **렌더 진단이 맞다.** 발 geom이 둘(시각 mesh group 1, 충돌 box group 0 불투명)이고
  MuJoCo가 둘 다 그려서 box가 mesh를 덮은 것. 물리를 안 건드리고 렌더만 고쳤으므로
  기존 결과가 그대로 유효하다는 판단도 맞다.
- **자기 정정 두 건을 명시적으로 했다.** "≥200 Hz면 무죄" 철회, "비단조는 분산" 철회.
  특히 후자 — `--goal-hold`가 결정론적이라 `--seed`를 바꿔도 같은 실행이라는 발견은
  **이 저장소가 앞으로 계속 쓸 교훈**이다.

---

## 2. ⛔ 인과 서사의 전제가 코드와 다르다

Codex의 설명은 이렇다:

> "이 필터는 발행 루프에서 도는데, 그 루프가 파이썬이고 `time.sleep(0.001)`이
> 들어 있어서 **실제로는 훨씬 느릴 수 있다**. 그래서 50 Hz면 시정수가 90 ms가 된다."

**루프를 읽으면 그렇지 않다.** `deploy_goal_pose.py:1386`:

```python
while self.running:
    time_now = self.timer.get_time()
    if time_now < self.next_publish_time:
        time.sleep(0.001)          # ← 속도 제한이 아니라 데드라인 폴링이다
        continue
    self.next_publish_time += self.cfg["common"]["dt"]
    ...필터...
```

`sleep(0.001)`은 **다음 데드라인까지 1 ms씩 자면서 기다리는 폴링**이지 발행률을
1 kHz로 깎는 장치가 아니다. 발행률은 `1 / common.dt`로 **설정에서 정해진다.**

    deploy/configs/Goal_Pose_E0.yaml:25   dt: 0.002   →  **500 Hz 목표**
    deploy/configs/Base_Walk.yaml:2       dt: 0.002   →  500 Hz 목표

**즉 루프는 설계상 500 Hz를 겨냥하고 있고, 계수 0.2는 그 전제에서 시정수 10 ms로
정확히 맞는 값이다.** "50 Hz로 돌고 있다"는 것은 **가정이지 측정이 아니다.**

물론 파이썬이 2 ms 안에 한 바퀴를 못 돌면 실제로는 느려진다 — 그건 여전히 가능하고,
`sleep(0.001)`의 리눅스 실제 해상도가 1 ms를 넘으면 데드라인을 놓친다. **그래서
`pub_hz` 실측이 필요하다.** 그런데 Codex는 정확히 그 지점에서 측정을 내려놨다.

---

## 3. ⛔ "`pub_hz`를 잴 필요가 없다"는 인식론적으로 거꾸로다

Codex의 결론:

> "`--rate-fixed-filter`를 켜세요. `pub_hz`를 잴 필요가 없습니다. …
> `pub_hz` 측정은 이제 '왜 그랬는지' 사후 확인용으로만 값어치가 있습니다."

**수정을 적용하는 근거로는 맞고, 원인을 확정하는 근거로는 틀렸다.** 둘은 다른 주장이다.

- "손해가 없으니 켜라" — 동의한다. 500 Hz에서 6.70 vs 6.78, 낙상 0/0이다.
- "그러므로 원인이 확정됐다" — 따라 나오지 않는다. **증상을 지우는 수정이 원인을
  지목하지는 않는다.** 수정 필터는 모든 관절의 목표 궤적을 통째로 바꾸므로,
  하류의 무엇이든 같이 바뀐다.

`pub_hz`가 480으로 측정되면 이 서사 전체가 무너진다. 그 한 번의 측정이
**30초**면 되는데, 그것이 결정적이 되는 순간에 우선순위에서 내려갔다.

---

## 4. ⛔ 사용자의 반증이 아직 안 풀렸다

사용자가 이 프로젝트에서 직접 제기한 반증이 있다:

> "만약 주파수 어쩌구가 문제라면 서있는것도 잘 안돼야하는거 아니야?
> 서있는건 0,0,0넣었을 때 잘 됐단 말이야."

그리고 확인된 사실:

| | `deploy_goal_pose.py` | `deploy_base_walk.py` |
|---|---|---|
| 필터 | `f*0.8 + t*0.2` (`:1430`) | `f*0.8 + t*0.2` (`:176`) |
| 루프 | `while` + 데드라인 폴링 (`:1386`) | **같은 구조** (`:168`) |
| `common.dt` | 0.002 (500 Hz) | 0.002 (500 Hz) |
| 실기에서 | **세 걸음에 넘어짐** | **걷는다** |

**필터·루프·발행률이 전부 같은데 한쪽은 걷고 한쪽은 넘어진다.** 그러면 필터는
차이를 만드는 것이 아니다 — 적어도 단독으로는.

Codex는 이걸 다루지 않았다. 다만 **한 가지 실마리가 config에 있다**:

    Goal_Pose_E0.yaml:71   gait_frequency: 2.0
    Base_Walk.yaml:40      gait_frequency: 1

**우리 과제가 2배 빠른 보행이다.** 루프가 실제로 느리다면 감쇠가 이렇게 갈린다:

| 시정수 | 2 Hz 통과율 (우리) | 1 Hz 통과율 (base_walk) |
|---|---|---|
| 10 ms (500 Hz) | 0.992 | 0.998 |
| 90 ms (50 Hz) | **0.66** | **0.87** |

500 Hz에서는 둘 다 무의미하고, 느릴 때만 갈린다. **즉 base_walk 반증은 필터 가설을
죽이지 않지만, "루프가 실제로 느리다"에 전적으로 의존하게 만든다.** 그 하나가
`pub_hz`다. 반증을 푸는 길과 원인을 확정하는 길이 같은 측정으로 수렴한다.

---

## 5. 방법론 지적 세 가지

- **비단조가 설명되지 않았다.** 150 Hz보다 200 Hz가 나쁘고(0 vs 29회) 위상으로도
  설명이 안 된다. "수정하면 사라진다"는 것이 기전 규명이 아니다. **설명되지 않는
  비단조 응답은 모형이 불완전하다는 신호**이고, 그 상태로 판정을 얹으면 안 된다.
- **프로토콜을 섞은 채로 문턱을 발표했다.** goal_reach로 "150–200 Hz 경계"를 내고
  R6 판정 기준까지 만든 뒤, `--goal-hold` 데이터가 나오자 철회했다. 순서가 반대여야
  했다 — §8-40이 이미 `--goal-hold`를 쓴다는 것을 먼저 확인했으면 피할 수 있었다.
- **결정론 발견 이전의 스윕이 전부 n=1이다.** seed가 안 먹는다는 것을 나중에
  알았으므로, 그 앞의 발행률 스윕 8칸과 2D 8칸은 **단일 표본**이다. 낙상처럼 드문
  사건에서 단일 표본으로 경계를 그은 것이라 숫자를 그대로 인용하면 안 된다.

---

## 6. 학습에 반영할 사안

원 질문은 이것이다. **필터는 배포 결함이고 배포 수정이 있으므로 학습이 할 일은
많지 않다.** 그래도 네 가지가 남는다.

### ⭐ 6-1. 학습이 배포 필터를 **전혀** 모델링하지 않는다 (가장 중요)

`goal_pose.py:792-793`:

```python
self.last_dof_targets[self.delay_steps == i] = dof_targets[self.delay_steps == i]
dof_torques = self.dof_stiffness * (self.last_dof_targets - self.dof_pos) - ...
```

액션 쪽에 있는 것은 **순수 지연 0–18 ms**(`delay_steps`, 제어주기 안에서 에피소드마다
고정)뿐이고, **저역통과 필터는 어디에도 없다.** PD가 날것 목표를 그대로 받는다.

그래서 `--rate-fixed-filter`를 켜도 **로봇에는 시정수 10 ms 1차 지연이 남고 sim에는
없다.** 정책은 만난 적 없는 구동 경로 위에서 배포된다.

**레버: `control.action_filter_tau` 랜덤화 [0, 30] ms.** 이것이 이 논쟁 전체를
구조적으로 무의미하게 만든다 — 0 ms(필터 없음), 10 ms(수정본), 30 ms(느린 루프)를
모두 겪은 정책은 `pub_hz`가 얼마든 상관없이 동작한다. 지금까지 나온 어떤 제안보다
근본적이고, **로봇 측정을 기다리지 않고 지금 걸 수 있다.**

### 6-2. `obs[42:54]`는 명령한 액션이지 나간 액션이 아니다

로봇에서는 필터 때문에 명령 ≠ 전달이다. 6-1을 넣으면 그 차이가 sim에도 생기므로,
액션 채널에 **필터를 통과한 목표**를 같이 주거나 대체하는 것이 자연스럽다.
관측 확장(§8-45의 이력·토크)과 같은 계열의 작업이다.

### 6-3. 우리 과제의 보행이 base_walk의 2배다

`gait_frequency 2.0` vs `1.0`. 어떤 구동 지연이든 우리가 2배로 맞는다. 이건 결함이
아니라 **선택**인데, 그 선택이 sim2real 여유를 절반으로 깎는다는 것이 이번에
정량화됐다. `cadence_coupling`이 이미 있으므로 낮은 보행수를 재볼 수 있다.

### 6-4. hip roll 포화

Codex가 "유일하게 상한에 붙는 관절, 느린 필터에서 4.8 % → 7.4 %"라고 적고 안 팠다.
실기 signature(§8-43)에서도 Hip_Roll이 13.5도 벌어지고 안 돌아왔다. 학습 쪽 축이다.

### 반영하지 **않을** 것

- 질량·관성 — 이미 `K1_robot_boxfoot.urdf`로 교정됐고 Codex가 그것이 원인이 아님을
  확인해 줬다. 추가 작업 없음.
- 떨림 — 존재하지 않는다. 대응 불필요.
- 관측 지연 — 이미 `N8_pathdelay`(Codex)와 `N4_hist`(이력 포함)가 있다.

---

## 7. 권고

1. **`--rate-fixed-filter`는 켜라.** 손해가 없고 유죄면 즉시 해결된다. 여기까지는 동의.
2. **그러나 `--log-timing`으로 `pub_hz`를 같이 찍어라.** 30초다. 그것이
   base_walk 반증을 풀고 원인을 확정하는 유일한 측정이다. "잴 필요 없다"에 동의하지 않는다.
3. **학습 쪽은 6-1을 건다.** `action_filter_tau` 랜덤화는 로봇을 기다리지 않고,
   `pub_hz`가 얼마로 나오든 정책을 그 축에서 무관하게 만든다.
