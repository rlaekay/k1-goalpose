# 야간 조사 결과 — 2026-08-05 23:32 ~ 08-06

작성: 학습 세션. 사용자가 자리를 비운 사이 자율 조사.
방법: **실기 증상의 signature를 재현 목표로 두고** Isaac/MuJoCo에서 검증.

---

## 0. 한 줄

**배포 필터가 발행 루프 속도에 의존해서 보행 명령을 깎는다** -- 이것이 가장 강한
원인 후보이고, 코드에서 확인되며, 수정이 준비돼 있고 MuJoCo에서 증상을 없앤다.
확인은 로봇에서 **30초, 안 움직이고** 된다(§1 마지막).

---

## ⛔ 0-1. 밤새 한 가장 큰 정정 — 원인과 결과를 섞고 있었다

실기 로그 2.4초 중 **뒤 1.4초가 이미 붕괴 구간**이다. `tilt`가 t=17.17초에 10도를
넘고 21–22도까지 가며, 발목 순간 파워가 −285 W까지 튀는 순간이 **전부 17.54–17.97초
0.43초에 몰려 있다.**

붕괴 **전**(34표본 0.94초) 대 **후** 대 MuJoCo:

| 지표 | 붕괴 전 | 붕괴 후 | MuJoCo |
|---|---|---|---|
| 발목 roll 속도 | **2.38** | 8.69 | **2.42** |
| 발목 파워 | **+0.39** | −8.14 | −0.55 |
| Hip_Roll 폭 | **6.91°** | 29.77 | **6.90** |
| 추종률 | **0.86** | 0.61 | 0.93 |

**"발목이 −5 W로 역구동된다", "Hip_Roll이 3배로 폭주한다", "발목 속도가 4.5배다"는
전부 원인이 아니라 결과였다.** 붕괴 전에는 sim과 같다.

**원인 창에서 sim과 다른 것은 셋뿐이다:**

| | 실기 붕괴 전 | MuJoCo | 비 |
|---|---|---|---|
| 고관절 pitch 속도 | 1.78 | 4.48 | **0.40배** |
| 발목 pitch 토크 | 6.22 | 10.48 | **0.59배** |
| 몸통 roll 폭 | 15.66° | 8.95 | 1.75배 |
| 추종률 | 0.86 | 0.93 | 0.92배 |

셋 다 **"덜 힘차게, 더 흔들리며 걷는다"**이고 필터 가설과 방향이 같다.

**붕괴 전 창으로 재정렬한 순위** (상위가 전부 필터):

| 조건 | 점수 |
|---|---|
| **필터 30 Hz** | **1.582** |
| 필터 40 Hz | 1.685 |
| 필터 40 + 지연 20 | 2.209 |
| 필터 50 Hz | 2.342 |
| 지연 20 ms 단독 | 2.476 |

지연이 맞추던 지표(다리 교차, Hip_Roll 표류)는 **전부 붕괴 후 구간의 것**이었다.
**원인 창에서는 필터가 지배적이고, 관측 지연은 붕괴를 증폭하는 쪽으로 보인다.**

---

## 1. ⛔ 가장 강한 것 — 배포 필터가 루프 속도에 의존한다

`deploy_goal_pose.py`의 발행 루프:

```python
filtered = filtered * 0.8 + target * 0.2      # 발행 루프마다 1회
```

**계수 0.2는 500 Hz 발행을 가정한 값**(시정수 10 ms)인데, 그 루프는 파이썬에
`time.sleep(0.001)`이다. 느려지면 **같은 계수가 훨씬 긴 시정수를 만들어 보행 자체를 깎는다.**

| 발행률 | 시정수 | 2 Hz 감쇠 |
|---|---|---|
| 500 Hz (설계) | 10 ms | 0.99 |
| 100 Hz | 50 ms | 0.87 |
| **50 Hz** | **100 ms** | **0.66** |

### 증거 — 예측이 먼저였다

계산으로 **0.66**을 낸 뒤, MuJoCo에서 **0.64**를 측정했고, 실기 로그의 추종률
(명령 대비 실제 도달)이 **0.61**이다.

MuJoCo에서 필터 발행률만 낮추면:

| | 추종률 | 낙상/분 |
|---|---|---|
| 필터 없음 | 0.93 | 0 |
| 100 Hz | 0.74 | 30 |
| **50 Hz** | **0.64** | **60** |
| 실기 | **0.61** | 낙상 |

그리고 실기 로그의 명령→관절 1차 동정에서 시정수 median이 **400 ms**다(MuJoCo 84.8).

### 수정 — 이미 들어가 있고 MuJoCo에서 증상을 없앤다

`--rate-fixed-filter`: 실측 `dt`로 계수를 매번 다시 계산해 시정수를 10 ms로 고정.

| 발행률 | 현재 | **수정** |
|---|---|---|
| 100 Hz | 0.74 / 30 낙상 | 0.89 / **0** |
| 50 Hz | 0.64 / 60 | 0.88 / **0** |
| 30 Hz | 0.49 / 70 | 0.85 / **2** |

**루프가 30 Hz까지 느려져도 수정하면 아무 일도 안 일어난다.**

### 다음 한 걸음 (로봇, 30초, 안 움직여도 됨)

```bash
cd ~/Workspace/deploy && ./run_e0.sh fixed 0,0,0 --log-timing /tmp/e0_pub.csv
```

CSV의 **`pub_hz`** 열. **50 Hz 근처면 원인 확정**, 300 Hz 이상이면 이 가설은 죽는다.

---

## 2. 관측 지연 20–28 ms

signature 탐색에서 **상위 10개가 전부 지연 기반**이다(최고 점수 2.76, 기본 12.4).
그리고 학습은 관측 지연을 **전혀 모델링하지 않는다** -- `delay_steps`는 **액션** 쪽
0–18 ms이고 관측 쪽은 0이다.

실기 `low_state_age`는 median 1 ms지만 그것은 **전송 구간만**이다. 센서→IMU 필터→SDK
발행 구간은 안 잡히고, 상보/칼만 필터의 실효 지연은 통상 10–40 ms다.

**수정**: `noise.obs_delay_steps` 를 env에 넣었다(링 버퍼, per-env, 리셋마다 재추첨).
Isaac에서 검증: 60 ms 지연 시 낙상 439 → **18,855**. 구현이 물리에 확실히 반영된다.
`MB_obsdelay` arm이 0–40 ms 랜덤화로 학습 중.

---

## 3. 각 축이 서로 다른 증상을 설명한다

| | 실기 | 필터 50 Hz | 지연 20 ms |
|---|---|---|---|
| 추종률 | 0.61 | **0.64** ✅ | 0.93 |
| 낙상/분 | 낙상 | **60** ✅ | 1.5 |
| 다리 교차 | 9.9 % | 4.9 % | **9.2 %** ✅ |
| 발목 파워 | **−5.0 W** | **−3.2** ✅ | +4.5 ⛔ |
| Hip_Roll 표류 | 13.5° | 4.3 | **6.3** |

### Out-of-sample 검증 (점수에 **안 넣은** 양)

| | 실기 | 기본 | 필터 | 지연 | 셋 조합 |
|---|---|---|---|---|---|
| 발 들기 % | 71 | 76 | 25 | **77** ✅ | 34 |
| 발목 roll 오차폭 ° | 110 | 27 | 65 | 89 | **110** ✅ |
| Hip_Roll 토크포화 % | 12.1 | 0.7 | **9.6** ✅ | 14.8 | 17.0 |

**적합에 쓰지 않은 세 양을 각 축이 나눠서 맞춘다** -- 과적합이 아니다.

### 그리고 붕괴 속도 (역시 적합에 안 쓴 양)

| 조건 | 첫 낙상까지 |
|---|---|
| 기본 | 없음 |
| 지연 20 ms 단독 | 35.2 s |
| 필터 50 Hz 단독 | 13.0 s |
| **필터 + 지연 + 발목감쇠** | **2.0 s** |
| 필터70 + 지연24 + 감쇠 | 3.1 s |
| **실기** | **1–3 s** |

**단독 축으로는 13–35초에 무너지는데 실기는 1–3초다. 셋을 합쳐야 맞는다.**
즉 원인이 하나가 아니라 **겹쳐 있을 가능성이 높다** -- 각각은 정책이 버티는
범위 안이지만 함께 오면 못 버틴다. 그리고 §4에서 적은 대로 발목 감쇠 레버는
부호가 틀렸으므로, 그 자리를 채우는 **진짜 세 번째 요인이 아직 미확인**이다.

---

## 4. ⚠️ 주의해서 읽어야 할 것

- **발목 감쇠 레버는 점수가 가장 좋지만 부호가 반대다.** 감쇠를 낮추면 `dq`는 커지지만
  모터가 **구동**한다(+17 W). 실기는 **역구동**(−5 W)이다. 점수는 크기만 보므로
  이런 경우를 못 거른다. **점수만 보고 채택하면 안 된다.**
- **증상이 체크포인트마다 반대다.** i2a는 "다리가 **모이면서**", stance10은
  "다리가 **벌어져**". `feet_offset_y −10`이 앞의 것을 고쳤지만 반대쪽으로 과했다.
  그래서 `feet_cross`를 **양방향 구간 제약**으로 만들었다(0.07 m 아래 / 0.26 m 위).
- **`pub_hz` 측정 전까지 필터 가설은 후보다.** 수정은 기본 꺼짐이고 실기 A/B가 필요하다.

---

## 5. 학습 중 (전부 실물 자산 `K1_robot_boxfoot.urdf` 위)

| arm | 무엇 | 상태 |
|---|---|---|
| `M4_scratch_phasefree` | 위상 자유, 처음부터 10000 | 진행 |
| `M9_anklegain` | 발목 이득 0.6–1.3배 | 진행 |
| `MB_obsdelay` | **관측 지연 0–40 ms** | 대기 |
| `M8_jointcal2` | 관절 영점 ±2° | 대기 |
| `MA_feetcross` | 발 간격 양방향 제약 | 대기 |
| `M3_scratch_phase` | 위상 기반, 처음부터 10000 | 대기 |

---

## 6. 밤새 고친 방법론 오류 (같은 실수를 반복하지 않기 위해)

1. **창 길이** -- 실기 2.4초를 sim 25–40초 전체와 비교했다. 폭은 창이 길수록 커지고
   표류는 상쇄된다. 창별 median으로 고치니 **순위가 통째로 뒤집혔다**(최고 7.16 → 2.76).
2. **주파수 분해능** -- 2.4초 창은 0.42 Hz 아래를 못 가르는데 "0.30 Hz 진동"을 지표로
   썼다. 표류하는 신호가 스캔 하한에 몰린 것이었다. 표류 지표로 교체했다.
3. **`dt` 추정** -- 첫 두 표본 간격을 dt로 썼다. 실기는 17–48 ms로 흔들려 주파수가
   1.45배 틀어졌다. median을 써야 한다.
4. **설정 주석 신뢰** -- `self_collisions: 0`을 "꺼짐"으로 읽었으나 주석이
   *"1 to disable, 0 to enable"*이다. 켜져 있었다.
