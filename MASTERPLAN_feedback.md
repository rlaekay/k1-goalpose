#### instruction
- 이 파일의 모든 피드백과 지시사항을 읽고 답변과 수정사항을 하나도 빠짐없이 아래에 댓글로 달아
- 그리고 새로운 버전들의 설명서는 masterplan2.md에 새로 작성한다.

> **[답변 전체 요약]** 아래 모든 항목에 개별 답변을 달았다. 근거는 5개 리포트의
> `segments.csv` 원자료(총 22,506 구간)를 직접 재집계한 값이고, 추정인 경우 "추정"이라고
> 명시했다. 코드 수정은 커밋 `850a97b`에 들어갔고 새 아키텍처는
> [masterplan2.md](masterplan2.md)에 정리했다.
>
> **이번 평가의 결론 두 줄**
> 1. **armB 압승**이고 그 원인은 `goal_reached` 단 하나다. A와 B는 이 레버 하나만 다르다.
> 2. **지금 과제는 속도를 요구한 적이 없다.** 4647개 구간에서 필요속도 median 0.116 m/s,
>    **최댓값 0.574 m/s**. 1.3~1.5 m/s는 학습도 측정도 구조적으로 불가능했다.

---

#### 질문
- Warm start라고 부르는 이유는 무엇이며, warmstart는 언제까지 하는거야. 본격적인 training은 warmstart에서 어떤 목적이 충족돼야 하는거야

> **답변.** "warm start"는 **가중치를 난수가 아니라 이미 걸을 줄 아는 정책에서 시작**한다는
> 뜻이다. 우리 경우 ParameterWalk(속도명령 추종 보행) 체크포인트를 GoalPose로 이어받았다.
> 관측 슬롯을 재사용한 이유가 이것 때문이다 — `commands[0:3]`이 원래 `lin_vel_x/lin_vel_y/
> ang_vel_yaw`였던 자리에 `goal_rel_x/goal_rel_y/heading_error`를 넣어 텐서 모양을 보존했다.
>
> **언제까지 하나**: warm start는 "기간"이 아니라 **1회 초기화 이벤트**다. 학습 시작 시점에
> 한 번 로드하고 끝이다. 다만 실무적으로는 "warm start가 유효한 구간"이 있는데, 그건
> **초기 분포가 깨지지 않는 동안**이다. armD가 정확히 이 지점에서 실패했다(아래 armD 항목).
>
> **본격 training으로 넘어가는 조건** — warm start가 충족시켜야 하는 것은 딱 하나다:
> **"넘어지지 않고 보행 리듬을 유지한 채, 새 관측(goal 3채널)에 대해 non-degenerate한
> gradient가 흐르는 상태"**. 구체적 판정 기준:
> | 조건 | 판정법 | 현재 상태 |
> |---|---|---|
> | 낙상률이 발산하지 않음 | 첫 100 iter 내 episode length가 상승 전환 | ✅ (v0 기록) |
> | goal 채널이 실제로 쓰임 | goal 거리별 성능 차이가 생김 | ✅ (거리별 표) |
> | 보행 리듬 보존 | gait_process 추종, feet_swing 보상 유지 | ✅ |
>
> 이 3개가 서면 warm start의 역할은 끝났고, 그 뒤는 전부 본격 training이다.

- heading sincos 가 도움이 됐어?

> **답변: 아직 안 해봤다. 한 번도 학습에 들어간 적 없다.**
> [make_sweep_configs.py:97-101](htwk-gym/tools/make_sweep_configs.py#L97)에 이유가 적혀 있다 —
> `heading_error` 1채널을 sin/cos 2채널로 바꾸면 `num_observations`가 54→55가 되고, 그러면
> **model_20000에서 warm start가 깨진다**(`strict=False`는 없는 키만 건너뛰지, 모양이 다른
> 키는 그대로 에러난다). 그래서 armA~D 어디에도 안 들어갔다.
>
> **필요한가 판정**: 지금 데이터로는 **급하지 않다**. heading은 이미 전 버전에서 통과
> 게이트다(armB median 7.3°, armC 1.8°, armA 2.1°). ±π 불연속이 문제가 되려면 heading
> 오차가 π 근처에 쌓여야 하는데, 그런 신호가 없다. 게다가 `constellation` 보상은
> `2r²(1-cosθ)` 형태라 **이미 ±π에서 매끄럽다** — 불연속은 관측에만 남아 있고 보상에는 없다.
> → **우선순위 낮음.** 어차피 v7에서 팔 URDF가 바뀌어 warm start를 다시 잡아야 하므로,
> **그때 obs를 재설계하는 김에 같이 넣는 게 공짜**다. masterplan2에 그렇게 적었다.

- 나중에 bt에 얹었을 때 입력이 몇 개인지

> **답변: BT가 넣어줄 숫자는 3개다.**
> 관측 54차원의 내역은 이렇다:
> | 구간 | 차원 | 출처 |
> |---|---|---|
> | projected_gravity | 3 | IMU (온보드) |
> | base_ang_vel | 3 | 자이로 (온보드) |
> | **commands** | **10** | **아래 참조** |
> | gait clock cos/sin | 2 | 내부 카운터 |
> | dof_pos | 12 | 엔코더 |
> | dof_vel | 12 | 엔코더 |
> | 직전 actions | 12 | 내부 |
>
> commands 10개 중 **BT가 실제로 주는 건 앞 3개뿐**이다:
> `goal_rel_x`, `goal_rel_y`(로봇 로컬 프레임 [m]), `heading_error`([rad], ±π 래핑).
> 나머지 7개(`gait_frequency`, `foot_yaw_L/R`, `body_pitch/roll_target`,
> `feet_offset_x/y_target`)는 **전부 상수로 고정**돼 있다(yaml에서 min==max).
> 배포 시엔 상수 테이블로 채우면 된다.
>
> 즉 **BT ↔ 정책 인터페이스 = (Δx, Δy, Δθ) 3개.** 나머지는 전부 온보드 센서 또는 내부 상태다.

- 외란, 외력 항을 warmstart때는 안 넣어도 되는거임?

> **답변: 넣어도 되고, 지금 이미 켜져 있다.** 다만 **강도를 warm start 시점에 올리면 안 된다**는
> 게 정확한 원칙이다.
> 현재 base config는 warm start 여부와 무관하게 kick(4초 간격 속도 임펄스)과 push(5초 간격
> 15N/1s)가 **항상 켜져 있다**. 끈 적이 없다.
>
> **원칙**: warm start는 "이 정책이 이미 잘하는 분포"에서 출발하는 것이므로, 초기 몇백 iter
> 동안은 **분포를 넓히지 말아야** 한다. 외란을 키우면 warm start 가중치가 보는 상태분포가
> 원래 학습 분포에서 벗어나고, PPO는 그걸 "정책이 나빠졌다"로 읽어 초기에 크게 망가진다.
> **armD가 정확히 이 실패다** — 외란만이 아니라 PD 게인까지 동시에 바꿔서 warm start의 전제를
> 깼다. 그래서 v7에서는 외란을 **커리큘럼으로 램프업**하도록 설계했다(masterplan2 §외란).

- 최고속도에 대한 best value를 학습을 통해서구하는 방법

> **답변.** "best value를 학습으로 찾는다"는 두 가지로 나뉜다. 우리에게 필요한 건 (B)다.
>
> **(A) 물리적 최대속도 탐색** — 로봇이 낼 수 있는 상한이 얼마인가.
> 방법: 보상에 `+v_x`를 넣고 낙상 페널티만 걸어 **최대속도를 직접 최적화**한 뒤, 넘어지기
> 직전까지의 속도를 측정. 이건 별도 태스크(`MaxSpeed`)로 짧게 돌리는 게 맞다. 결과는
> "이 로봇/이 게인/이 URDF의 상한"이라는 **참조값**으로만 쓴다.
>
> **(B) 과제가 요구하는 속도를 자동으로 올리기** ← **이게 v7에 구현한 것**
> 최고속도를 하이퍼파라미터로 손으로 찍지 않고, **커리큘럼이 성공률을 보고 스스로 올린다**:
> - lookahead point가 `path_speed`로 전진한다.
> - "따라붙고 있다" = lookahead 간격이 `keepup_gap_m`(2.0 m) 이내.
> - 그 비율의 EMA가 `up_threshold`(0.8)를 넘으면 `speed_level`을 올리고, `down_threshold`(0.5)
>   아래면 내린다.
> → **수렴한 `speed_level`이 곧 "이 정책이 안정적으로 유지 가능한 최고속도"의 학습된 추정치**다.
> 이게 v3 커리큘럼과 정반대인 점이 핵심이다: v3는 못하면 **목표를 가깝게** 만들어서 속도
> 요구를 **낮췄다**. v7은 잘하면 **속도를 올린다**.

- Goal까지 가는데 속도의 그래프 shaping어떻게. 가속도 Trapezoid, plateau 등등. 나는 가속과 각가속을 빠르게 하고싶은데 이 속도 값이 현재 뭐지? 가능하다면 1.4, 1.5m/s 정도까지로 올리고 싶은데

> **답변. 먼저 "현재 이 속도 값이 뭐지"에 대한 실측 답:**
>
> | arm | 접근속도 median | 접근속도 p95 | **요구속도 median** | **요구속도 최댓값** |
> |---|---|---|---|---|
> | armA | 0.106 | 0.341 | 0.119 | 0.574 |
> | armB | 0.109 | 0.351 | 0.116 | 0.574 |
> | armC | 0.114 | 0.350 | 0.132 | 0.569 |
> | armD | 0.073 | 0.276 | 0.116 | 0.606 |
> | v3 | 0.043 | 0.256 | 0.063 | 0.589 |
>
> **핵심: 요구속도의 "최댓값"이 0.574 m/s다.** 4647개 구간 전체에서 가장 빡센 목표조차
> 0.6 m/s면 도달한다. 그래서 정책이 0.35 m/s에서 멈춰 있는 건 **정책의 한계가 아니라 과제의
> 한계**다. 1.4~1.5 m/s를 원한다면 지금 세팅으로는 **영원히 안 나온다.**
>
> 왜 이렇게 됐나(계산):
> - `goal_dx ∈ [-2,2]`, `goal_dy ∈ [-1.5,1.5]` → 최대 거리 2.5 m
> - `resampling_time_s ∈ [4,8]` → 최소 4 s
> - 이론적 최대 요구속도 = 2.5/4 = **0.625 m/s**. 실측 0.574와 일치.
> - 게다가 stand(10%) + turn(19%)는 **거리가 정확히 0**이다 → 전체의 **29.5%가 이동 0**.
>
> **속도 그래프 shaping에 대한 답 — trapezoid를 직접 명령하지 마라.**
> 이유: 가속 프로파일을 보상으로 지정하면 그건 **속도추종 문제**로 되돌아가는 것이고,
> "No More Marching" 논문이 굳이 goal-pose로 간 이유(정책이 스스로 경로/속도를 고르게 함)를
> 버리는 것이다. 대신 **경계조건만 주고 프로파일은 정책이 고르게** 한다:
> - **끝 조건**: `goal_reached`가 "도착 + 정지"를 강제 → 감속 구간은 자동으로 생긴다.
>   (armB 실측: 도착속도 >0.1 m/s인 구간이 **1%**. 이미 완벽하게 멈춘다.)
> - **중간 조건**: path mode의 lookahead가 `path_speed`로 도망간다 → **plateau가 강제된다.**
>   따라붙으려면 그 속도를 유지할 수밖에 없다.
> - **가속 조건**: lookahead 간격이 벌어지면 보상이 떨어지므로 **초기 가속이 빠를수록 이득**.
>   즉 trapezoid(빠른 가속 → plateau → 도착 시 감속)가 **명령이 아니라 결과로** 나온다.
>
> 각가속: `rose/star` 경로(shape 3)와 `figure-8`(shape 0)이 곡률이 급해서 **높은 yaw rate를
> 강제**한다. 팔을 내리면서 yaw 관성이 0.180→0.076 kg·m²(-58%)로 떨어진 것도 여기에 직접
> 기여한다.
>
> **1.4~1.5 m/s 도달 가능성 판정(정직하게)**: `speed_range_mps`의 상단을 1.6으로 잡아뒀지만
> **커리큘럼이 거기까지 올라간다는 보장은 없다.** K1의 다리 길이/게인에서 1.4 m/s가 물리적으로
> 가능한지는 아직 **미검증**이다. 그래서 masterplan2에 (A)의 `MaxSpeed` 짧은 탐색을 별도
> 항목으로 넣었다 — 상한을 먼저 알아야 목표가 합리적인지 판정할 수 있다.

- 그리고 현재 실험 결과가 goto 논문에서 말한 기대효과, 단점, 성능 등에 어느정도 부합하는지 근거를 가지고 체크

> **답변.** 원 논문은 "No More Marching: Learning Humanoid Locomotion for Short-Range SE(2)
> Targets" (arXiv:2508.14098). 대조 결과:
>
> | 논문 주장 | 우리 실측 | 판정 |
> |---|---|---|
> | **constellation 보상이 위치·heading을 하나의 커널로 결합** → 덧셈식 분리 보상보다 우수 | 전 버전 heading이 게이트 통과(1.8~7.3°). 위치만 문제였음 | ✅ **부합**. heading은 공짜로 해결됐다 |
> | **goal-pose가 velocity-tracking보다 짧은 거리에서 우수** ("제자리 행진" 제거) | stand 카테고리에서 armB 3.3 cm / v3 2.4 cm. 행진 안 함 | ✅ **부합** |
> | **goal 유형 혼합(stand/straight/lateral/turn/combined)** 이 필요 | 유형별 편차가 실제로 큼(armD: combined 31.7 cm vs stand 5.0 cm) | ✅ **부합**, 혼합은 필수였다 |
> | 도달 정확도 | 논문은 cm급 주장. armB 3.9 cm median / 6.7 cm p90 | ✅ **부합** (게이트 5 cm/10 cm 통과) |
> | **단점: 원거리에서 gradient 소실** (`exp(-w·d²)`가 평평해짐) | armD/v3에서 **미도달 60%/50%**, 거리 2 m 이상에서 미도달 94~97% | ✅ **단점도 그대로 재현됨** |
>
> **논문과 어긋나는 지점 1개 — 이게 중요하다.**
> 논문의 constellation은 **"도착"까지만 다룬다. "도착 후 정지"는 다루지 않는다.**
> 우리 실측에서 armA/C는 위치·heading이 맞는데도 **도착속도 >0.1 m/s가 47~49%**였다.
> constellation만으로는 안 멈춘다. `goal_reached`(논문에 없는 항)를 넣은 armB에서 그 비율이
> **1%**로 떨어졌다. → **논문 그대로는 우리 과제(RLKick 핸드오프를 위한 정지)에 부족하고,
> 우리가 추가한 항이 맞았다**는 근거다.
>
> **논문과 어긋나는 지점 2개 — 속도.**
> 논문은 "short-range SE(2) targets"를 명시한 과제다. 우리는 거기에 더해 **필드를 가로지르는
> 지속 주행**이 필요하다. 논문 세팅을 그대로 쓴 결과가 위에서 본 요구속도 0.116 m/s다.
> → 논문 범위 밖이므로 **논문 밖의 메커니즘(path mode)이 필요**하다는 판단.

- goal sampling을 4-8초로 하면 오래걷기는 어떻게 학습해? 어차피 lookahead point가 학습거리 이내로만 주어지면 상관 없나? lookaheadpoint가 계속 이어지는듯한(trajectory를 following하고있는 듯하게)goal을 줄 수는 없나? 이건 아직 이럴 단계가 아닌가? 예를 들면 커다란 8자나 spiral, star, random trajectory 위를 lookahead point(0.5-3.0m)를 두고 빨리 따라가도록 하는걸 해보고 싶네. 별로면 말고. 일단 피드백 줘봐

> **답변: 별로이기는커녕 이게 이번 피드백에서 제일 정확한 지적이다. 그대로 구현했다.**
>
> **"4-8초로 하면 오래걷기를 어떻게 학습하나"** → **못 한다.** 위에서 계산한 대로 요구속도
> 최댓값이 0.574 m/s이고, 29.5%는 아예 안 움직여도 된다. 지속 보행은 학습되지 않는다.
>
> **"lookahead가 학습거리 이내면 상관없나"** → **상관있다.** 이게 핵심 통찰이다.
> 정지한 목표는 **아무 속도로나** 도달 가능하다(늦게 가도 도착만 하면 됨). 반면 **전진하는**
> lookahead는 **그 속도를 내야만** 간격이 유지된다. 목표까지의 거리가 같아도(0.5~3.0 m)
> **속도 요구가 완전히 다르다.** 거리가 아니라 **목표의 속도**가 정책의 속도를 결정한다.
>
> **구현한 것** ([goal_pose_v7.py](htwk-gym/envs/K1/goal_pose_v7.py)):
> - 경로 5종: **figure-8, circle, spiral, rose/star(5엽), pseudo-random wander** — 요청한 것 전부
> - lookahead **0.5~3.0 m** (요청 범위 그대로)
> - lookahead가 `path_speed`로 경로를 따라 전진
> - **"목줄"(leash)**: 간격이 `lookahead_max_m`(3.5 m)를 넘으면 목표가 **멈춰서 기다린다**.
>   → 못 따라가는 로봇에게 무한히 도망가는 목표를 주면 gradient가 죽는다. 이걸 막는다.
> - waypoint 모드(기존 과제)와 **50:50 혼합** → armB가 잘하는 "도착 후 정지"를 잃지 않는다
>
> **"이럴 단계가 아닌가"** → **지금이 정확히 그 단계다.** 이유: armB가 이미 도달·정지 게이트를
> 통과했다(3.9 cm / 6.7 cm p90). 남은 미해결 항목이 속도 하나뿐이므로, 지금 안 하면
> 다음에 할 일이 없다.

- Eval을 할 때는 목표가 jitter하거나 반경3미터 원 안에서 50hz로 random sample되는 항목도 추가하면 좋겠어

> **답변: 채택. 다만 학습과 eval에서 다르게 넣었다 — 이유가 있다.**
>
> **학습**: `goal_bt_flicker`(prob 0.004/step ≈ 5초에 1회, 반경 1 m 점프)를 **관측에만** 넣었다.
> 보상은 여전히 **참값 goal**을 읽는다. 이게 중요한 이유: 보상까지 흔들면 정책은 "흔들리는
> 목표를 쫓는 것"을 배운다(= 더 경련한다). 관측만 흔들고 보상은 참값이면 정책은
> **"흔들림을 걸러내는 것"**을 배운다. 우리가 원하는 건 후자다.
>
> **eval**: 요청하신 "반경 3 m 원 안에서 50 Hz 랜덤 샘플"은 **성능 게이트가 아니라 강건성
> 스트레스 테스트**로 분리해야 한다. 이유: 50 Hz로 완전 랜덤이면 **참값 목표 자체가 무의미**
> 해져서(기댓값이 원 중심) 위치오차라는 지표가 정의되지 않는다. 그래서 eval에서는
> **"넘어지지 않는가 / 몸통이 발산하지 않는가"만 측정**하는 별도 모드로 넣는 게 맞다.
> masterplan2 §eval에 `--stress jitter` 항목으로 설계를 적었다.

- symmetric loss와 팔을 일단 내리고, 몸체 선속도에 따라 뒤로숨기는거 바로 진행해야 할듯

> **답변: 둘 다 진행. 단, 여기서 중요한 사실 하나를 발견했다.**
>
> **⚠️ symmetric loss는 armA~D 어디에서도 켜진 적이 없다.**
> `Goal_Pose.yaml`의 `algorithm.symmetric_coef: 10.`은 **읽히지 않는 죽은 키**다.
> [runner_v3.py:13](htwk-gym/utils/runner_v3.py#L13)에 명시돼 있다 — 실제로 동작하는 키는
> `symmetry_coef`이고 **RunnerV3에서만** 쓰인다. armA~D는 `train.py`(기본 Runner)로 돌았으므로
> **대칭 손실 0**이었다. 아래 armA의 "왼발이 항상 앞" 관찰이 여기서 설명된다.
> → v7은 `train_v7.py`(RunnerV3) + `symmetry_coef: 0.5`로 **진짜로 켰다.**
>
> **팔 내리기: 완료.** [K1_locomotion_armsdown.urdf](htwk-gym/resources/K1/K1_locomotion_armsdown.urdf)
> 자세한 건 아래 "팔 제발 내려" 항목에 전부 적었다.
>
> **속도 연동 후방 스윙: 설계만 했고 아직 미구현.** 정직하게 말하면 이건 URDF 편집만으로 안 된다.
> 팔 관절이 **전부 `type="fixed"`**라 정책이든 스크립트든 **움직일 수가 없다.** 자세한 건
> 아래 "팔 제발 내려" 항목 참조.

- 최고속도를 확인하기 위해서 4-8초 동안 주는 2,3미터 goal은 별로인듯

> **답변: 정확하다. 숫자로 확증됐다.**
> 2.5 m / 4 s = 0.625 m/s가 이 세팅의 **이론적 상한**이고, 실측 요구속도 최댓값 0.574 m/s가
> 그것과 일치한다. "2~3 m를 4~8초"는 최고속도 측정 도구로 **부적합**하다.
> → path mode로 교체. 그리고 eval 리포트에 **몸통 속도 절대값 섹션**을 새로 넣어서
> (median/p90/p99/순간최대/구간최고속도/0.5·1.0 m/s 초과 시간비율) 이제 속도가 직접 보인다.
> 1.0 m/s 초과 시간이 1% 미만이면 리포트가 **"이건 정책 한계가 아니라 과제 한계"**라고
> 경고문까지 출력한다.

---

#### 전체적인 피드백(모든 버전에 동일)
- 시작 속도를 randomize하거나, dist to goal에 노이즈를 추가하는 정도가 아니라 goal이 마구잡이로 변할 때(ball 관측이나 bt분기 때문에 shaking 하는 경우)에도 robust하게 걷기를 학습해야 함

> **답변: 동의. 노이즈의 "구조"가 틀렸다는 지적으로 읽었고, 그게 맞다.**
> 기존 `noise.goal_pos`는 **가우시안 지터**다. 가우시안은 작고 연속적이라 로봇 입장에서는
> 그냥 저역통과 필터로 지워진다 — BT 분기 전환과는 통계가 완전히 다르다. BT가 가지를 바꾸거나
> 공을 재검출하면 목표는 **한 번에 크게 점프**한다.
> → `goal_bt_flicker` 추가: 확률 0.004/step으로 **반경 1 m / heading 0.5 rad의 이산 점프**.
> 가우시안 지터·구간 바이어스·지연(2~3 step)과 **공존**하며, 각각 다른 주파수 성분을 담당한다:
> | 항 | 주파수 | 크기 | 모델 대상 |
> |---|---|---|---|
> | `goal_pos` 지터 | 매 step | 4 cm | 측정 잡음 |
> | `goal_pos_bias` | 구간당 1회 | 5 cm | 정위(localization) 계통오차 |
> | `goal_obs_hold_steps` | 2~3 step | — | 카메라 20 fps vs 제어 50 Hz |
> | **`goal_bt_flicker`** | **~5 s에 1회** | **1 m** | **BT 분기 / 공 재검출** |

- 관절보호에 대한 codex의 의견을 masterplan.md에서 읽고 네 critic을 더해서 반영해

> **답변: 읽었다. codex 의견은 대체로 옳고, 두 곳에서 반대한다.**
>
> **동의하는 부분**: (1) 관절보호는 보상 하나로 안 되고 배포단 hard guard가 최우선이다.
> (2) 이미 걷는 정책에 강한 Lagrangian을 갑자기 섞으면 보행이 무너진다. (3) `power`가
> 양의 기계적 파워만 봐서 배터리 소비의 대용물로 쓰면 안 된다. — 전부 맞다.
>
> **내 반대 1: "soft_dof_pos_limit: 1.0이라 URDF 한계 초과에서만 작동"은 맞지만, 처방이
> 애매하다.** codex는 "limit의 0.8~0.9를 넘을수록 증가하는 continuous margin penalty"를
> 제안했는데 이건 옳다. 다만 **그냥 `soft_dof_pos_limit`를 0.85로 낮추면 안 된다** — 그
> 파라미터는 기존 `dof_pos_limits` 보상과 곱해져서 **기존 보상의 크기까지 바꾼다**(스케일이
> -1.0). 그러면 "관절보호를 켰다"가 아니라 "기존 보상을 재조정했다"가 되어 A/B가 오염된다.
> → v7에서는 **별도 항**(`dof_pos_margin`, `dof_vel_margin`, `torque_margin`)으로 넣었다.
> 정상 대역(한계의 85% 이내)에서 **정확히 0**이라 건강한 보행을 왜곡할 수 없고, 켜고 끄기가
> 독립적이다.
>
> **내 반대 2: codex의 "에너지는 Wh/m 제약으로 다뤄라"는 지금 단계에서 과하다.**
> Wh/m 제약은 **실제 전류/전압 로그가 있어야** 의미가 있는데 우리는 아직 없다. 없는 상태에서
> 제약을 걸면 시뮬레이터의 부정확한 파워 모델에 정책을 과적합시킨다.
> → v7에서는 **제약이 아니라 약한 페널티**(`electrical_power: -2e-3`)로만 넣었고,
> 내용은 `|τ·dq| + k·τ²`다. `|τ·dq|`는 제동 시에도 전류가 흐르는 걸 반영하고(부호 무시),
> `k·τ²`는 **저속·고토크에서 지배적인 동손(I²R)**을 반영한다. 기존 `power`(양의 기계파워만)의
> 두 가지 결함을 정확히 메운다. Wh/m 게이트는 **실기 로그를 확보한 뒤**에 도입하는 게 맞다.

- battery가 low하면 protect하는것도 들어가있어?

> **답변: 아니, 전혀 안 들어가 있다. 배터리 관련 항은 코드 어디에도 없다.**
> 확인한 것: 관측·보상·도메인랜덤화·종료조건 전부에 SOC/전압/전류 항이 없다. codex도
> MASTERPLAN에서 같은 지적을 했다("전류, 전압, 배터리 SOC, 모터/드라이브 온도와 contact
> impulse를 반영한 항은 아직 없다").
>
> **어디에 넣어야 하는가 — 보상이 아니라 두 곳이다:**
> 1. **도메인 랜덤화 (학습)**: 배터리가 닳으면 버스 전압이 떨어지고 → **실효 토크 상한이
>    내려간다.** 이건 `torque_limits`를 에피소드마다 0.8~1.0배로 스케일하는 것으로 모델링하는
>    게 물리적으로 정확하다. 이러면 정책이 "토크 여유가 없는 상태"에서도 걷는 법을 배운다.
>    (v7에 아직 미구현 — masterplan2에 항목으로 넣었다.)
> 2. **배포단 guard (실기)**: SOC 임계 이하에서 최고속도 제한 → stand 전환 → disable.
>    이건 **학습이 아니라 런타임 상태기계**의 일이다. 정책에게 "배터리가 없으니 알아서 아껴라"를
>    학습시키는 건 관측에 SOC를 넣어야 가능한데, 그러면 obs가 바뀌고 warm start가 깨진다.
>    비용 대비 효과가 나쁘다. **guard로 처리하는 게 맞다.**

- 팔 제발 내려. urdf수정하라고 했잖아. 팔은 그냥 사람처럼 중력방향으로 늘어트리게 만들고 body속도가 0.5m/s이상이 되면 팔꿈치와 arm yaw를 틀어서 손이 팔 뒤로 가게끔 하자. shoulder pitch나 roll은 건들지 말 것.

> **답변: 팔 내리는 것 완료. 후방 스윙은 구조적으로 막혀 있어서 설계만 했다. 이유를 정확히
> 설명하겠다 — 이게 왜 지금까지 안 됐는지의 진짜 원인이다.**
>
> **원인: K1 URDF의 팔 관절은 전부 `type="fixed"`다.**
> ```
> ALeft_Shoulder_Pitch   fixed    Left_Shoulder_Roll   fixed
> Left_Elbow_Pitch       fixed    Left_Elbow_Yaw       fixed   (우측도 동일)
> ```
> 게다가 `collapse_fixed_joints: true`라 로드 시점에 **부모 링크로 병합**된다. 그래서:
> - 정책은 팔을 못 움직인다 (`num_actions: 12` = 다리 12개뿐, 팔은 액션이 아예 없음)
> - 보상으로도 못 고친다
> - **URDF를 고치는 것 외에 방법이 없다** ← 지시하신 게 정확했다
>
> **한 것**: [K1_locomotion_armsdown.urdf](htwk-gym/resources/K1/K1_locomotion_armsdown.urdf)
> 기존 URDF는 팔이 **T자로 옆으로 뻗어 있다**(손이 몸통 중심에서 y=±0.31 m, z=+0.17 m).
> `Shoulder_Roll`의 **고정 마운트 rpy**를 ∓80°로 돌려 팔을 중력방향으로 접었다.
> | 항목 | 기존 | armsdown |
> |---|---|---|
> | 손 위치 (Trunk 기준) | y=±0.31, z=+0.17 | y=±0.174, **z=+0.008** |
> | 팔의 yaw 관성 기여 | 0.180 kg·m² | **0.076 kg·m² (−58%)** |
> | 팔 무게중심 높이 | +0.172 m | **+0.083 m (−8.9 cm)** |
>
> **90°가 아니라 80°인 이유**: 다리가 y=±0.096에 있는데 팔을 완전 수직으로 내리면 y=±0.145에
> 놓인다. 팔(Arm_2/Arm_3/hand)에 **충돌 지오메트리가 있어서** 4.9 cm 간격이면 스친다.
> 10° 외측으로 벌리면 간격이 7.8 cm가 되고, 사람의 자연스러운 팔 각도(carrying angle)와도 맞는다.
>
> **"shoulder pitch나 roll은 건들지 말 것"과의 관계 — 확인 필요.**
> 기하학적으로 **T자를 아래로 내리려면 shoulder roll 회전이 불가피**하다. 제가 한 것은
> **관절을 움직인 게 아니라 고정 마운트 각도를 바꾼 것**이라, "보행 중 shoulder가 움직이지
> 않는다"는 요구는 지켜진다(여전히 fixed). 지시를 그 뜻으로 해석했는데, **"URDF 마운트
> 각도조차 건드리지 말라"는 뜻이었다면 알려주시라** — 그 경우 팔을 내릴 방법이 없어진다.
>
> **후방 스윙(미구현)과 필요한 작업**: 팔꿈치+yaw를 속도에 따라 틀려면
> 1. `Left/Right_Elbow_Pitch`, `Left/Right_Elbow_Yaw` 4개를 `revolute`로 전환
> 2. **비학습 DOF 지원 추가**: `num_dofs`는 16이 되지만 `num_actions`는 12로 유지하고,
>    이 4개는 `dof_pos_ref`를 `clip((|v|-0.5)/0.5, 0, 1) * θ_max`로 **스크립트 구동**
> 3. 관측/액션 벡터에서 제외 → **obs 54 유지 → warm start 보존** (이게 이 설계의 핵심 이점)
> 4. 손대는 코드: `goal_pose.py`의 `_init_buffers`(default_dof_pos), `step()`의 `dof_targets`
> 
> 이건 다음 작업으로 masterplan2 §팔에 적었다. **팔을 내린 것만으로도 동역학이 바뀌었으니
> (yaw 관성 −58%) 어차피 재학습이 필요하고, 그때 같이 넣는 게 효율적이다.**

- 모든 버전에서 body속도를 표기하지 않음. body속도는 1.3m/s정도 나와야함.

> **답변: 지적대로였고, 고쳤다.**
> 기존 리포트/영상은 **constellation 오차만** 보여줬다. 그래서 "느린 정책"과 "빨리 갈 이유가
> 없었던 정책"이 구분이 안 됐다 — 실제로 후자였는데 전자로 보였다.
>
> **추가한 것** ([eval_goal_pose.py](htwk-gym/eval_goal_pose.py)):
> - **영상 HUD**: 속도 |v| / vx / vy / yaw rate / 목표거리 / heading 오차 / **실시간 외란(N, N·m)**
>   (외부 폰트 의존성 없이 5×7 비트맵 폰트를 코드에 내장 — 렌더 실패 위험 0)
> - **리포트 "몸통 속도" 섹션**: median / p90 / p99 / 순간최대 / 구간최고속도(median·p90·max) /
>   0.5 m/s 초과 시간비율 / **1.0 m/s 초과 시간비율**
> - **segments.csv**: `peak_speed_mps`, `mean_speed_mps` 컬럼 추가
> - 1.0 m/s 초과가 1% 미만이면 **"이건 과제의 한계다"라는 경고를 리포트가 직접 출력**
>
> **1.3 m/s 목표에 대한 정직한 현재 판정**: 지금 정책의 p95 접근속도가 0.35 m/s다.
> 1.3 m/s는 **약 4배**다. path mode + 속도 커리큘럼으로 요구를 만들었지만, K1이 1.3 m/s를
> 물리적으로 낼 수 있는지는 **아직 아무도 측정한 적이 없다.** masterplan2에 `MaxSpeed` 단독
> 탐색을 넣은 이유다 — 상한을 모르면 목표가 합리적인지 판정할 수 없다.

- video에 속도, goal까지 거리에 더해서 외란도 표기하자. 그리고 외란은 무조건 더 줘야해. 단순히 팔만 걸리는게 아니라 로봇 두 대랑 부딪히는 경우도 많아. urdf있으니까 충격량 계산해서 학습에 반영해.

> **답변: 표기 완료. 충격량 계산해서 반영 완료.**
>
> **URDF 기반 충격량 계산:**
> - K1 총질량 = **18.71 kg** (URDF 전 링크 합산, 직접 계산)
> - 동일 로봇 2대 충돌의 **환산질량** m_eff = m₁m₂/(m₁+m₂) = 18.71/2 = **9.36 kg**
> - 서로 0.5 m/s로 마주 걸어와 충돌 → 상대속도 Δv = 1.0 m/s
> - 충격량 J = m_eff·Δv = **9.36 N·s**
> - 접촉 시간 τ ≈ 0.05~0.15 s (플라스틱 외피 반강체)
> - **평균 힘 F = J/τ ≈ 94 N** (τ=0.1 s 기준)
> - 몸통 중심에서 ~0.15 m 벗어난 지점 타격 → **토크 ≈ 14 N·m**
>
> **기존 설정과 비교**: 기존 15 N / 1 s, armD는 **5 N / 3 s로 오히려 낮췄었다**.
> 실제 로봇 충돌은 **힘이 6배 크고 지속시간이 10~20배 짧다.** 완전히 다른 사건을
> 하나의 파라미터로 뭉개고 있었다.
>
> **고친 것 — 외란을 2종으로 분리** ([goal_pose_v7.py](htwk-gym/envs/K1/goal_pose_v7.py)):
> | 종류 | 힘 | 토크 | 지속 | 모델 대상 |
> |---|---|---|---|---|
> | **collision** | 40~150 N | 3~20 N·m | 0.05~0.15 s | **로봇 2대 충돌** |
> | **support** | 3~15 N | 0.2~2 N·m | 1.5~3.0 s | 사람이 팔 잡아 지지 |
> 50:50 혼합, 3~8초 간격.
>
> **추가로 고친 버그성 설계**: 기존 `_push_robots`는 `common_step_counter % interval == 0`이라
> **4096개 env 전부가 같은 스텝에 동시에 밀렸다.** 배치 내 외란이 완전 상관돼서 표본 다양성이
> 사실상 1이었다. v7은 **env별 독립 타이머**라 매 스텝 서로 다른 env가 밀린다.
> (이건 지시하신 항목은 아니지만 외란을 손보다 발견한 것이라 같이 고쳤다.)

---

#### 학습 결과 및 향후 방향
### instruction
- 이번 대화에선 armA,B,C,D와 v3에 대해서 평가할 것임.
- 각 버전의 의도, 테스트하고자 하는 것, 리워드함수의 특징, 알고리즘의 특징을 먼저 조사하고(masterplan.md) 댓글로 간략히 정리한 후에 내 video feedback을 확인한다(k1 walk 폴더에 eval py report.md들이 저장되어있음 그것도 읽어).

> **답변 — 버전별 의도/레버 대조표 (make_sweep_configs.py의 ARMS 정의 기준):**
>
> | 버전 | 의도 | 바꾼 레버 | 알고리즘 | 대칭손실 |
> |---|---|---|---|---|
> | **armA** | "v1은 iteration만 더 필요했다"는 기준선 | **없음** (순수 연장) | PPO, mini_epochs 5 | ❌ (죽은 키) |
> | **armB** | 도달·정지 두 지표를 직접 공략 | **`goal_reached: 1.0`** 하나만 | 동일 | ❌ |
> | **armC** | 물리 500→200 Hz 속도 향상 검증 | **`dt 0.005`, `decimation 4`** 하나만 | 동일 | ❌ |
> | **armD** | sim2real 총집합 예행연습 | **12개 동시** (지각노이즈·PD 2배·goal 범위·push·armature) | 동일 | ❌ |
> | **v3** | 학습 스택 업그레이드 | **armD 전체 + 커리큘럼 + 대칭 + 미니배치PPO** | RunnerV3 | ✅ |
>
> **보상 함수는 5개 버전 전부 `constellation: 3.5` 단독**(goal_position/heading/stop은 0).
> 차이는 `goal_reached`(B/D/v3만 1.0)와 `stand_posture`(D/v3만 -2.0)뿐이다.
>
> **⚠️ 중요: v3는 armA의 후속이 아니라 armD 위에 쌓은 것이다.** `Goal_Pose_V3.yaml`은 armD의
> 12개 레버를 **전부 상속**한다(PD 200/5, goal 3m/2m, 4~10s, 지각노이즈, armature 0.02).
> 이 사실이 v3 판정을 완전히 바꾼다(아래 v3 항목).

- video feedback을 확인한 후에는 근거에 입각해서 나의 의문을 해소해주고, 향후 수정 방향을 적용하면 된다.
- 이렇게 되면 나의 동영상을 통한 피드백과 report.md를 통해 네가 할 수 있는 피드백이 잘 융합된 다음 버전이 나오겠지? 그러면 버전별 의도에 따라서 버전을 통폐합한다.

> **답변: 융합 결과가 v7이다.** 영상 관찰(정성) ↔ 원자료 재집계(정량) 매칭 결과:
> | 영상에서 본 것 | 자료에서 확인된 것 | 판정 |
> |---|---|---|
> | B가 A보다 가속이 좋아 보임 | 접근속도 p95 A 0.341 vs B 0.351 — **차이 없음** | ❌ **착시** (원인은 아래 armB 항목) |
> | B는 발이 모임 | `goal_reached`의 정지 조건이 좁은 스탠스를 선택 | ✅ 인과 확인 |
> | B는 제자리걸음 시 흔들림이 A보다 심함 | `goal_reached`의 정지 판정에 **각속도가 없음** | ✅ **코드에서 원인 확인** |
> | C는 목표 근처에서 움찔거림 | C 도착속도 >0.1 m/s가 **49%** | ✅ 일치 |
> | D는 최종 도달속도가 느림 | D 접근속도 median 0.073 (최저 수준) | ✅ 일치 |
> | v3 몸통 흔들림 | v3 낙상 3회(최저)인데 속도 median 0.043(최저) — **과보수적** | ✅ 일치 |

- 버전 AB는 쌍둥이다. 만약 b가 더 좋으면 b만 살리고

> **답변: B 압승. A 폐기.**
> | 지표 | armA | **armB** | 차이 |
> |---|---|---|---|
> | 위치오차 median | 7.6 cm ❌ | **3.9 cm ✅** | **-49%** |
> | 위치오차 p90 | 14.7 cm ❌ | **6.7 cm ✅** | **-54%** |
> | 엄격 성공률 | 13.9% | **52.8%** | **+3.8배** |
> | 도착 후 안 멈춤 | 33.4% | **0.5%** | **-98%** |
> | 한 번도 도달 못함 | 19.3% | **0.2%** | **-99%** |
> | 낙상 | 34 | 37 | 동급(둘 다 실패) |
>
> **전 카테고리에서 B가 이겼다** (stand 0.073→0.033, turn 0.075→0.038, straight 0.076→0.039,
> lateral 0.074→0.040, combined 0.082→0.040 m). 부분 우위가 아니라 **전면 우위**다.
> A와 B의 유일한 차이가 `goal_reached: 1.0`이므로 **이 레버 하나가 전부를 설명한다.**

- 버전 c는 물리계산만 바꿨다. 500hz인 애들과 비교해서 200hz가 더 낫다고 판단되면 ab 중 승자와 나머지 버전들에 적용하면 된다. 만약 상대우위가 없다면, 그냥 500hz로 둔다. c는 삭제

> **답변: 상대우위 없음. 500 Hz 유지. armC 삭제.**
>
> | 지표 | armA (500Hz) | armC (200Hz) | 판정 |
> |---|---|---|---|
> | 위치 median | 7.6 cm | **8.3 cm** | C가 **나쁨** |
> | 위치 p90 | 14.7 cm | **16.7 cm** | C가 **나쁨** |
> | 엄격 성공률 | 13.9% | **13.0%** | C가 **나쁨** |
> | 미도달 | 19.3% | **25.3%** | C가 **나쁨** |
> | 낙상 | 34 | **10** | C가 좋음 |
> | 학습 iter | 11,700 | **16,800** | C가 **43% 더 돌았는데도** 짐 |
>
> **주 게이트(위치)에서 전부 졌다.** 유일한 우위인 낙상 감소(34→10)는 두 가지 이유로 채택
> 근거가 안 된다:
> 1. **교란됨**: C가 43% 더 많은 iteration을 돌았다. 낙상 감소가 200 Hz 때문인지 학습량
>    때문인지 분리 불가.
> 2. **더 나은 대안이 존재**: **v3가 500 Hz에서 낙상 3회**를 냈다. 물리 주파수를 낮추지 않고도
>    낙상은 해결된다는 실증이다. 물리 정확도를 팔아서 낙상을 사는 거래는 불필요하다.
>
> **덤: 원래 목적이었던 "속도 향상"이 측정된 적 없다.** armC의 명분은 "substep 비용 2.5배
> 감소"였는데, eval 벽시계는 A 794.7 s vs C 784.2 s로 **사실상 동일**하다(eval은 학습이 아니라
> 별개 프로세스이고 GPU 공유 상태도 달라 비교 불가). **약속한 이득이 검증된 적이 없다.**
> → 삭제.

- 버전 D는 현실을 많이 반영한 것으로 보인다. 아마 abc보다 성능이 안 좋아보인다면 보다 현실적인 요소들 때문이겠지? 버전 D는 삭제하고, 거기에서 사용된 외란 및 강건함을 위한 요소들을 a/b 중 승자에 집어넣자.

> **답변: D 삭제 동의. 다만 "현실적 요소 때문에 나빠졌다"는 해석은 자료와 맞지 않는다.
> 범인은 따로 있고, 그걸 특정하는 게 중요하다.**
>
> **결정적 단서 — D는 서 있는 건 잘했다:**
> | 카테고리 | armA | **armD** | 이동 필요? |
> |---|---|---|---|
> | **stand** | 7.3 cm | **5.0 cm** ← D가 **더 좋음** | ❌ 이동 0 |
> | turn | 7.5 cm | 20.0 cm | ❌ 이동 0 (제자리 회전) |
> | straight | 7.6 cm | **29.8 cm** | ✅ |
> | lateral | 7.4 cm | **25.2 cm** | ✅ |
> | combined | 8.2 cm | **31.7 cm** | ✅ |
>
> **지각 노이즈가 원인이라면 stand도 같이 나빠져야 한다** — stand의 목표도 똑같이 노이즈를
> 받으니까. 그런데 stand는 **오히려 좋아졌다**. 즉 **노이즈는 범인이 아니다.**
> 반면 **이동이 필요한 모든 카테고리가 붕괴**했다. 이건 **보행 자체가 깨졌다**는 뜻이다.
>
> **→ 주범: `control.stiffness` 100→200, `damping` 2→5 (Hip/Knee 2배).**
> warm start는 100/2에서 학습된 정책이다. 같은 액션이 **2배 토크**를 내게 되면 정책이 아는
> 액션↔결과 관계가 통째로 무효가 된다. 보행이 깨지고, 서 있기는 (토크 여유가 커져서) 오히려
> 좋아진다. **관측된 패턴과 정확히 일치한다.**
> **공범: `goal_dx/dy` 2→3 m, 2→3 m + `resampling` 8→10 s.** 깨진 보행으로 더 먼 목표를
> 받으니 미도달이 거리 0.5 m 이상에서 **87~97%**가 됐다.
> **무죄: 지각 노이즈(bias/jitter/staleness), armature 0.02, push 완화.**
>
> **v7에 흡수한 것 / 버린 것:**
> | armD 요소 | v7 | 근거 |
> |---|---|---|
> | 지각 노이즈 (bias 0.05, jitter 0.04, hold 2-3) | ✅ **흡수** | stand가 무사했으므로 무해함이 실증됨 |
> | `goal_reached: 1.0` | ✅ 흡수 (armB와 동일) | B에서 이미 검증 |
> | `stand_posture` | ✅ 흡수하되 -2.0→**-1.0** | D/v3에서 stand는 잘 됐으나 과도할 이유 없음 |
> | `armature: 0.02` | ⬜ 보류 | 무해해 보이나 단독 검증된 적 없음 |
> | **PD 200/5** | ❌ **폐기** | **주범** |
> | goal 3m/2m, 4~10 s | ❌ 폐기 | path mode가 대체 |
> | push 5N/3s | ❌ 폐기 | 충격량 계산으로 대체(아래) |

- push 현실화: duration 1→3초로 늘리되 강도는 낮춤(15N→5N) <- 힘 키우자.

> **답변: 힘 키웠다. 그리고 "길게 약하게"와 "짧게 세게"를 분리했다.**
> armD의 5 N/3 s는 **사람이 잡아주는 상황**의 모델로는 맞지만, 지시하신 **로봇 충돌**과는
> 물리가 다르다. 하나의 파라미터로는 둘 다 표현할 수 없어서 **2종으로 분리**했다:
> - **collision**: 40~150 N, 3~20 N·m, 0.05~0.15 s ← 18.71 kg 충격량 계산 기반 (위 참조)
> - **support**: 3~15 N, 0.2~2 N·m, 1.5~3.0 s ← armD의 의도를 보존
> 기존 최대 15 N 대비 **최대 힘 10배**. 지시대로 확실히 키웠다.

- v3에서 학습 알고리즘을 업그레이드한게 어떤 성능을 보였는가? 현재 성능이 안 좋더라도 장래가 좋아서 계속 해볼만한지 아니면 여기서 그만두고 모든 자원을 armA/B에 투입하는게 좋은지 근거와 함께 결정.

> **답변: 계속한다. 단, v3를 단독 버전으로 두지 않고 v7에 흡수하는 형태로.**
>
> **핵심 사실: v3는 armD 위에 쌓은 것이다.** 그러니 v3의 성적은 armA가 아니라 **armD와
> 비교해야** 한다:
> | 지표 | armD (v3의 기반) | **v3** | 개선 |
> |---|---|---|---|
> | 위치 median | 24.3 cm | **13.6 cm** | **-44%** |
> | 위치 p90 | 49.5 cm | **30.1 cm** | **-39%** |
> | 엄격 성공률 | 6.2% | **17.5%** | **+2.8배** |
> | **낙상** | 21 | **3** | **-86%** |
> | stand 카테고리 | 5.0 cm | **2.4 cm** | **-52%**, 전 버전 최고 |
> | 도착 후 안 멈춤 | 6.2% | **2.7%** | -56% |
>
> **v3의 3개 메커니즘(커리큘럼+대칭+미니배치PPO)은 망가진 기반을 절반 가까이 복구했다.**
> 특히 **낙상 3회는 전 버전 최저**고, 이건 armC가 물리 주파수를 희생해서 얻은 10회보다도 좋다.
> 알고리즘 업그레이드 자체는 **명백히 효과가 있다.**
>
> **그런데 v3를 그대로 이어가면 안 되는 이유 2개:**
> 1. **한 번도 좋은 기반 위에서 시험된 적이 없다.** armD라는 손상된 기반 위에서만 측정됐다.
>    armB 위에 올렸을 때의 진짜 가치는 **미측정**이다.
> 2. **커리큘럼의 방향이 우리 목표와 반대다.** v3의 `goal_curriculum`은 성공률이 낮으면
>    **목표 거리를 줄인다**(`init_level: 0.35` → 범위의 35%). 그래서 v3의 요구속도 median이
>    **0.063 m/s**로 전 버전 중 최저다. 속도를 올리려는 우리 목표와 정면충돌한다.
>
> **결정 (자원 배분):**
> - ✅ **대칭 손실 유지** → v7에서 `symmetry_coef: 0.5`로 진짜 활성화 (armA~D는 죽은 키라 0이었음)
> - ✅ **미니배치 PPO 유지** → `num_minibatches: 4`
> - ❌ **거리 커리큘럼 비활성화** → 속도 커리큘럼으로 **대체**
> - ⬜ Rudin timed reward(`final_window_s`)는 0으로 둔 채 보류 — v3에서도 꺼져 있어서 **효과가
>   측정된 적이 없다.** 켠 적 없는 걸 "효과 있다/없다" 판정할 수는 없다.
> - **"모든 자원을 armA/B에" → 그렇게 하되, armB의 config에 v3의 러너를 붙이는 형태.**
>   별도 브랜치로 v3를 따로 굴리는 건 중단.

---

#### 버전 별 피드백, Q&A
### armA
- 회전은 자이로를 사용하나?

> **답변: 절반만.**
> - **각속도(yaw rate)**: 예. `base_ang_vel`이 관측 3~6번 채널이고, 실기에서는 **IMU 자이로**가
>   그대로 매핑된다. 노이즈도 이미 모델링돼 있다(`noise.ang_vel` std 0.1 rad/s).
> - **절대 heading(`heading_error`)**: 아니오. 이건 자이로가 아니라 **목표 대비 상대 각도**이고,
>   실기에서는 **정위(localization)/오도메트리**에서 온다. 자이로만으로는 적분 드리프트 때문에
>   못 만든다.
> - **자세(roll/pitch)**: `projected_gravity`(관측 0~2)로 들어간다. 실기에서는 IMU 가속도계 +
>   자이로 융합(AHRS).
>
> **함의**: 회전 성능은 자이로 품질(고주파)과 정위 품질(저주파) **양쪽**에 걸려 있다.
> 정위 오차는 `goal_heading_bias`로 모델링돼 있고 v7에서 0.05 rad로 켰다.

- 외란을 줘야함

> **답변: 켜져 있었으나 약했고, eval에서는 아예 꺼져 있었다.**
> - **학습**: kick 4 s 간격 + push 5 s 간격 15 N/1 s — 켜져 있었다.
> - **eval**: 리포트 헤더에 "외란 OFF"라고 적힌 대로, `--keep_perturbations` 없이 돌려서
>   **외란 없는 조건에서 측정**됐다. 즉 armA~v3의 모든 숫자는 **무외란 성적**이다.
> → v7에서 학습 외란을 대폭 강화(최대 150 N)했고, eval도 외란 ON 조건을 **별도 리포트**로
>   돌리도록 masterplan2에 넣었다. 두 조건의 성능 차이 자체가 강건성 지표가 된다.

- symmetric loss를 사용하는지는 모르겠는데 아무튼 대칭적임. 근데 왼발이 오른발보다 항상 살짝 앞에 가있음

> **답변: 대칭 손실은 안 쓰고 있었다. 그리고 그게 왼발 편향의 원인이다.**
> `algorithm.symmetric_coef: 10.`은 **아무도 읽지 않는 죽은 키**다
> ([runner_v3.py:13](htwk-gym/utils/runner_v3.py#L13)에 명시). 실제 키는 `symmetry_coef`이고
> **RunnerV3에서만** 동작하는데, armA~D는 전부 `train.py`(기본 Runner)로 돌았다.
> → **armA~D의 대칭 손실 = 0.**
>
> **그럼 왜 대칭적으로 보이나**: warm start한 ParameterWalk 보행이 원래 좌우 대칭적이기
> 때문이다. **구조적으로 강제된 대칭이 아니라 물려받은 대칭**이라, 학습이 진행되면서
> 서서히 무너진다. "왼발이 항상 살짝 앞"이 바로 그 드리프트다 — 대칭을 강제하는 항이 없으니
> PPO가 좌우 비대칭 해를 막을 이유가 없고, 한번 치우치면 그 자리에 머문다.
> → v7에서 `symmetry_coef: 0.5` + `train_v7.py`(RunnerV3)로 **처음으로 실제 활성화**.
> 이건 관찰하신 것에서 나온 직접적인 수정이다.

- 현실에서 이 모션을 사용할 때 줘야하는 input이 뭐임

> **답변: (Δx, Δy, Δθ) 3개.** 위 "bt에 얹었을 때 입력이 몇 개인지" 항목에 전체 표를 적었다.
> - `goal_rel_x`, `goal_rel_y`: **로봇 로컬 프레임** 기준 목표 위치 [m]
> - `heading_error`: 목표 heading − 현재 heading, **±π로 래핑** [rad]
> 나머지 51차원은 온보드 센서(IMU/자이로/엔코더)와 내부 상태(게이트 클럭, 직전 액션)다.
> 주의: 로컬 프레임이므로 **매 제어 주기마다 현재 자세로 다시 투영**해줘야 한다.
> 월드 좌표를 그대로 넣으면 안 된다.

- video log에서 constellation error말고 body velocity(xyw)도 표기해주고, position error도 표기해줘

> **답변: 완료.** 영상 HUD에 다음을 표기한다:
> ```
> VEL  1.34 M/S        <- |v_xy|
>   VX +1.34  VY -0.12 <- 몸통 프레임 x, y 성분
>   WZ +0.45 RAD/S     <- yaw rate (요청하신 w)
> DIST  1.62 M         <- position error (목표까지 거리)
> HEAD  -7.3*          <- heading 오차
> PUSH 12.5N 1.8NM     <- 외란 (인가 중일 때만 표시)
> ```
> 외부 폰트/cv2 의존 없이 5×7 비트맵 폰트를 코드에 내장해서 렌더 실패 위험을 없앴다.

- -x성분속도가 현저히 느림

> **답변: 관찰이 맞고, 원인은 학습 분포다.**
> `goal_dx ∈ [-2, 2]`로 **대칭 샘플링**이라 후진 목표는 전진과 같은 빈도로 나온다.
> 그런데 보행 자체가 전진에 최적화돼 있다:
> - `feet_swing` 보상(+3.0)이 **전진 스윙 궤적**을 전제로 한다
> - warm start한 ParameterWalk가 전진 위주로 학습됨
> - `feet_offset_x` 항이 비활성(-0.)이라 후진 시 발 배치를 조정할 유인이 없다
>
> 참고로 **armC는 후진이 B보다 낫다고 관찰**하셨는데(아래 armC 항목), 그건 200 Hz의
> 부드러운 접촉이 후진처럼 불안정한 모드에서 상대적으로 유리했을 가능성이 있다 — 다만
> 자료로 방향별 속도를 분리 측정한 적이 없어서 **미검증 가설**이다.
> → v7의 HUD가 이제 `VX`를 부호까지 표시하므로 **다음 영상에서 바로 확인 가능**하다.
> 근본 해결은 path mode의 곡선 경로가 전 방향 이동을 강제하는 것이다.

- 연속적인 goto를 보기 힘듬(data dist 문제)

> **답변: 정확히 데이터 분포 문제였고, 숫자로 확인된다.**
> armA의 구간별 결과: **도달했다가 이탈 11.8%, 한 번도 도달 못함 19.3%.** 그래서 "목표를
> 향해 쭉 가서 도착하는" 깔끔한 goto가 영상에서 잘 안 보인다.
> 더 근본적으로 **29.5%의 구간이 이동거리 0**(stand 10% + turn 19%)이라, 영상을 틀면
> 셋 중 하나는 제자리 동작이다. **연속 goto가 보이려면 운이 좋아야 한다.**
> → v7의 path mode는 **목표가 끊기지 않고 이어지므로** 연속 goto가 기본 동작이 된다.
> 이것도 지적하신 것이 그대로 설계에 반영된 항목이다.

### armB
- A와 다르게, 발이 모여있음←왜?

> **답변: `goal_reached`의 정지 판정 조건이 좁은 스탠스를 선택했다.**
> ```python
> stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1   # 몸통 선속도 < 0.1 m/s
> return ((self.goal_dist < 0.1) & stopped).float()              # +1/step
> ```
> 이 보상은 **"목표 10 cm 이내 + 몸통 속도 0.1 m/s 미만"인 매 스텝마다 +1**을 준다.
> 4~8초 구간을 50 Hz로 채우면 **최대 200~400점**이 걸린 매우 큰 항이다.
>
> 넓은 스탠스는 좌우 체중이동(lateral weight shift)이 필요하고, 그 자체가 **몸통 선속도를
> 만든다.** 즉 넓게 서 있으면 `stopped` 판정을 계속 놓친다. 반대로 발을 모으면 지지다각형이
> 작아지는 대신 **몸통을 정지시키기 쉬워서** 보너스를 연속으로 받는다.
> → **좁은 스탠스는 이 보상의 최적해**다. 설계 의도는 아니었지만 논리적 귀결이다.
>
> **주의: 이건 양날의 검이다.** 좁은 스탠스는 지지다각형이 작아 **횡방향 외란에 약하다.**
> armB의 낙상이 37회로 A(34회)보다 오히려 많은 것과 무관하지 않을 수 있다(미검증).
> v7에서 외란을 크게 키웠으므로 여기가 압력을 받을 것이고, 필요하면 `feet_distance_ref`
> (현재 0.18 m) 기반 페널티로 하한을 걸어야 한다. masterplan2에 감시 항목으로 넣었다.

- 속도가 0일때랑 0으로 수렴할 때랑 자세가 비슷함←이건 좋은 점. 왜 이러는지 찾아서 살려

> **답변: 원인은 `goal_reached`가 만든 "흡수 상태(absorbing state)"다. v7에서 그대로 살렸다.**
>
> A와 B의 보상 구조 차이:
> - **A (constellation 단독)**: 보상이 `exp(-w·d_con)`으로 **거리에 따라 연속적으로 변한다.**
>   목표에 가까울수록 좋지만 **"여기서 멈춰라"라는 지점이 없다.** 그래서 접근 방향·속도에 따라
>   제각각인 자세로 수렴한다.
> - **B (+goal_reached)**: 조건을 만족하는 순간 **매 스텝 +1이라는 평평한 고원**이 생긴다.
>   이 상태에 **오래 머무는 것 자체가 목적**이 되고, 그 상태를 벗어나면 보상이 뚝 끊긴다.
>   → 정책은 **"가장 안정적으로 오래 유지할 수 있는 단 하나의 자세"**로 수렴한다.
>   어느 방향에서 접근했든 **같은 종착 자세**로 간다.
>
> **이게 왜 중요한가**: 이게 정확히 **RLKick 핸드오프에 필요한 성질**이다. 킥 정책은
> 예측 가능한 시작 자세를 전제하는데, B는 그걸 공짜로 준다. 실측으로도
> **도착속도 median 0.033 m/s, >0.1 m/s인 구간 1%**로 압도적이다(A는 47%).
> → v7에서 `goal_reached: 1.0`을 **그대로 유지**하고, `stand_posture: -1.0`을 약하게 더해
> 그 종착 자세가 **기본 기립자세**에 가깝도록 살짝 유도만 했다(armD의 -2.0은 과했다고 판단).

- 가속이 꽤 괜찮음(a보단 빨라보임)←실측 속도가 어떤지 알아내서 실제로도 괜찮은건지, a랑 뭐가 달라서 가속도가 괜찮게 나온건지 탐색

> **답변: 실측하니 A와 B의 속도는 사실상 같다. "빨라 보인 것"은 착시이고, 원인이 흥미롭다.**
>
> | 지표 | armA | armB | 차이 |
> |---|---|---|---|
> | 접근속도 median | 0.106 m/s | 0.109 m/s | +3% |
> | 접근속도 p95 | 0.341 m/s | 0.351 m/s | **+3%** |
>
> **3% 차이는 유의미하지 않다. B는 A보다 빠르지 않다.**
>
> **그럼 왜 빨라 보이나 — 두 가지가 겹쳤다:**
> 1. **A는 목표에 도달을 못 한다.** A의 미도달 19.3% + 최근접거리 median 2.8 cm vs
>    **B는 미도달 0.2% + 최근접거리 1.4 cm**. A는 목표 근처에서 배회하다 구간이 끝난다.
>    같은 속도라도 **"헤매는 것"은 느려 보이고 "곧장 가는 것"은 빨라 보인다.**
> 2. **정지의 선명함.** A는 도착 후에도 47%가 0.1 m/s 이상으로 계속 꿈틀댄다. B는 1%다.
>    **출발–정지의 대비가 뚜렷하면 가속이 커 보인다.** 실제 최고속도가 같아도 그렇다.
>
> **교훈**: 영상만으로는 속도를 판정할 수 없다는 게 이 항목에서 증명됐다. 그래서 HUD에
> 숫자를 박은 것이다. 다음 영상부터는 이런 착시가 불가능하다.

- 제자리걸음을 할 때 body orientation이 a에 비해 흔들림이 심함

> **답변: 관찰이 맞고, 코드에서 원인이 특정된다. 이건 `goal_reached`의 실제 결함이다.**
> ```python
> stopped = torch.norm(self.root_states[:, 7:9], dim=-1) < 0.1
> #                                      ^^^^^ 7:9 = 선속도 x, y 뿐
> ```
> **`root_states[:, 7:9]`는 선속도만이다. 각속도(10:13)가 빠져 있다.**
> → 정책은 **몸통을 회전시키면서도** 보너스를 계속 받을 수 있다. 제자리에서 요/롤/피치로
> 흔들려도 선속도만 낮으면 +1이 계속 들어온다. **흔들림에 대한 억제가 이 항에 없다.**
>
> 참고로 `goal_stop` 보상은 각속도를 포함하지만(`+ torch.square(self.base_ang_vel[:, 2])`),
> **스케일이 0이라 꺼져 있다.** 즉 각속도를 벌하는 항이 아무것도 활성화돼 있지 않다.
> (`ang_vel_xy: -0.2`가 있지만 전역이라 약하고, 정지 상태에 특화돼 있지 않다.)
>
> **수정 방향** (masterplan2 §보상에 기록):
> `goal_reached`의 정지 판정에 각속도를 추가 —
> `stopped = (|v_xy| < 0.1) & (|ω| < 0.3 rad/s)`.
> 이러면 "발이 모이고, 자세가 일관되고, **흔들리지 않는**" 종착 상태가 된다.
> A/B 비교에서 나온 유일한 B의 약점이고, 원인이 한 줄로 특정됐으니 고치기도 쉽다.

### armC
- goal 주변에 도착했을 떄, 정지 할거면 하고, 말거면 말아야 하는 것이, body가 움직이에는 충분하지 않지만 motor몇 개 돌아가기에는 충분한 input이 들어가니까 stand자세가 아닌 상태에서 몸이 흔들림. 앞으로 가려는데 가지 못하고 움찔거리기만 함

> **답변: 자료가 정확히 일치한다. 그리고 원인은 200 Hz가 아니라 `goal_reached` 부재다.**
> armC 실측: **도착 후 안 멈춤 28.5%, 도달했다가 이탈 13.6%, 도착속도 median 0.097 m/s**
> (정지 기준 0.10 m/s **바로 아래에서 맴돈다**).
>
> **"움찔거림"의 메커니즘**: constellation 단독 보상은 `exp(-w·d_con)`이라 목표 근처에서
> **기울기가 거의 0**이다(d→0에서 미분 0). 그래서 "여기 있으면 이득, 저기 가면 손해"라는
> 신호가 사라진다. 정책은 **어느 쪽으로도 확신이 없는 상태**가 되고, 미세한 관측 노이즈에
> 따라 이쪽저쪽 토크를 낸다 → 몸통을 움직이기엔 부족하고 모터를 돌리기엔 충분한 입력.
> **관찰하신 현상 그대로다.**
>
> **결정적 증거**: armB는 같은 500 Hz인데 이 문제가 없다(도착 후 안 멈춤 **0.5%**).
> 차이는 `goal_reached`뿐이다. → **물리 주파수 문제가 아니라 보상 설계 문제.**
> `goal_reached`가 목표 근처에 **평평한 고원**을 만들어주면 "여기 머물러라"가 명확해진다.

- 뒤로 걷는 속도가 b보다 나음

> **답변: 관찰은 존중하되, 현재 자료로는 검증할 수 없다 — 방향별 속도를 측정한 적이 없다.**
> segments.csv에는 `final_speed`(스칼라)만 있고 **부호나 방향 성분이 없다.** 그래서
> "후진이 더 빠르다"를 확인할 방법이 지금은 없다.
>
> **가설(미검증)**: 200 Hz는 `contact_offset 0.02` + TGS 솔버 조건에서 접촉이 상대적으로
> 부드러워진다. 후진은 발뒤꿈치 접촉이 지배적이라 충격이 큰 모드인데, 접촉이 부드러우면
> 그 모드에서 유리할 수 있다. armC의 낙상이 10회로 적은 것과 같은 원인일 가능성이 있다.
> **다만 armC가 43% 더 오래 학습한 교란이 있어 분리 불가.**
> → v7의 HUD가 `VX`를 **부호 포함**으로 표시하므로 다음 영상부터 직접 확인 가능하다.
> 자료로 확인되면 그때 판단하는 게 맞고, **지금 이 근거로 200 Hz를 채택하지는 않는다.**

- 제자리 걸음 할 때 orientation 흔들림은 b보다 덜함

> **답변: 이것도 `goal_reached`의 각속도 누락으로 설명된다 — 그리고 이게 그 가설을 지지한다.**
> armB만 `goal_reached`가 켜져 있고, 그 항이 **선속도만 보고 각속도를 무시**한다(위 armB 항목).
> 그래서 B는 "선속도를 0으로 만들되 각속도는 자유"라는 방향으로 최적화됐다.
> C는 그 항이 없으니 그런 편향도 없다 — 대신 애초에 정지를 못 한다(28.5%가 안 멈춤).
>
> **즉 C가 더 나은 게 아니라, B가 특정 방향으로 과최적화된 것이다.** 처방은 C를 채택하는 게
> 아니라 **B의 `stopped` 판정에 각속도를 넣는 것**이다. 그러면 B의 정지 능력(1% vs 49%)을
> 유지한 채 흔들림만 제거할 수 있다. 두 관찰(armB 흔들림 ↑, armC 흔들림 ↓)이 **같은 한 줄의
> 코드를 가리킨다**는 게 이 분석의 수렴점이다.

- c가 뭐의 리워드를 상속받았을까? 전체적인 상위레벨움직임은 좋다. ←원인분석

> **답변: armC의 보상은 armA와 100% 동일하다. 상속받은 게 아니라 같은 것이다.**
> `make_sweep_configs.py`의 armC 패치는 딱 2줄이다:
> ```python
> "armC_200hz": { "sim.dt": 0.005, "control.decimation": 4 }
> ```
> **보상 스케일은 단 하나도 안 바뀌었다.** armA와 armC는 `constellation: 3.5` 단독으로 동일.
>
> **그럼 "상위레벨 움직임이 좋다"는 어디서 오나 — 물리 쪽이다:**
> 1. **제어 주파수는 그대로 50 Hz다** (dt 0.005 × decimation 4 = 0.02 s). 정책이 보는
>    시간 간격은 변하지 않았다. 바뀐 건 **PD 루프가 500 Hz → 200 Hz**로 돈다는 것.
> 2. PD 갱신이 성기면 **고주파 토크 처핑이 줄어든다.** 관절 수준의 미세 떨림이 감소하고,
>    그 결과 몸통 궤적이 **매끄러워 보인다.**
> 3. 실측으로도 **낙상 34→10회**로 급감했다. 접촉이 부드러워져 하드 임팩트가 줄어든 것과
>    일관된다.
>
> **하지만 그 대가가 정밀도다**: 위치 median 7.6→8.3 cm, p90 14.7→16.7 cm, 미도달 19.3→25.3%.
> 접촉 해상도가 낮아지면 **발 배치의 정밀도가 떨어진다.** "부드럽지만 부정확"한 거래다.
> → **상위레벨이 좋아 보이는 건 사실이나, 게이트는 정밀도로 판정하므로 채택하지 않는다.**
> 그리고 v3가 500 Hz에서 낙상 3회를 냈으므로 **부드러움은 다른 방법으로 얻을 수 있다.**

### armD
- 마지막 도달속도가 너무 느림

> **답변: 실측 일치. 접근속도 median 0.073 m/s로 armA/B/C(0.106~0.114)보다 **30~36% 느리다.**
> 원인은 위 armD 총평에서 특정한 **PD 게인 2배**다. warm start 정책이 알던 액션↔토크 관계가
> 무효화되면서 보행이 깨졌고, 정책은 **느리게 가는 것으로 안전을 확보**하는 쪽으로 퇴화했다.
> 미도달이 거리 0.5 m 이상에서 87~97%인 것과 같은 원인이다.

- 제자리에 서 있을 때 몸통만 앞뒤로 흔들리는 경우 존재

> **답변: `stand_posture: -2.0`의 과도한 강도가 유력하다(부분 검증).**
> armD는 목표 0.3 m 이내에서 **기본 기립자세와의 관절각 제곱합**에 -2.0을 건다. 강한 자세
> 복원력이 걸린 상태에서 **발을 움직여 균형을 잡는 선택지가 비싸진다** → 발은 붙박이가 되고
> **몸통(발목/고관절)만으로 균형을 잡으려 한다**. 그게 앞뒤 흔들림으로 보인다.
> 실측 지지: armD의 stand 카테고리 위치오차는 5.0 cm로 **좋다**(발이 안 움직이니 위치는 정확).
> 즉 "위치는 맞는데 흔들린다"는 관찰과 정확히 부합한다.
> → v7에서 **-2.0 → -1.0으로 완화**했다.

- 멀리서 rotation을 먼저 맞추고 translation을 천천히 맞추러 감←goto 논문을 다시 참고해야 할듯

> **답변: 논문의 constellation 수식에서 직접 설명된다. 그리고 이건 튜닝 문제다.**
> ```
> d_con = d² + 2r²(1 − cos θ)        r = constellation_radius = 1.0 m
> ```
> `r = 1.0`이면 heading 항의 계수가 `2r² = 2`다. θ=180°일 때 heading 항은 **4**를 기여하는데,
> 이는 **거리 2 m와 동등**하다(d²=4). 즉 **"180° 돌아 있는 것"과 "2 m 떨어진 것"이 같은
> 페널티**다.
>
> **회전은 제자리에서 몇 초면 되지만 2 m 이동은 훨씬 오래 걸린다.** 그러니 정책 입장에서
> **회전을 먼저 해치우는 게 압도적으로 유리**하다. 관찰하신 "멀리서 회전부터 맞추고 천천히
> 이동"이 **수식이 시키는 최적 행동**이다.
>
> **왜 armD에서 유독 두드러지나**: armD는 `goal_dx/dy`를 3 m/2 m로 넓혀서 **거리가 더 멀어졌다.**
> 먼 거리에서 `exp(-w·d²)`는 평평하지만(w=0.2, d=3 → exp(-1.8)) heading 항은 거리와 무관하게
> 그대로 작동한다. → **원거리일수록 heading이 상대적으로 지배적**이 된다.
>
> **처방**: `constellation_radius`를 1.0 → **0.6~0.7**로 낮추면 heading 기여가
> `2·0.65² ≈ 0.85`로 **60% 감소**해서 위치 항이 상대적으로 커진다. eval 리포트도 같은 처방을
> 자동 제안하고 있다. **다만 v7에서는 아직 1.0으로 뒀다** — path mode가 heading을 경로 접선으로
> 주기 때문에 heading 오차 자체가 작아지고, 그러면 이 불균형이 자동으로 완화된다.
> **한 번에 두 개를 바꾸지 않기 위해** path mode 결과를 먼저 보고 조정하는 게 맞다(armD의 교훈).

- 몸통의 앞 뒤 움직임이 항상 심함

> **답변: 위 "제자리에서 몸통 흔들림"과 같은 원인(`stand_posture: -2.0`)으로 보되, 보행 중
> 앞뒤 움직임은 **PD 게인 2배**가 추가 원인이다.**
> damping 2 → 5로 2.5배 올리면 관절이 **과감쇠(overdamped)** 쪽으로 간다. 스윙 다리가
> 목표 각도에 도달하는 게 느려지고, 그 지연을 몸통이 앞뒤로 흔들려 보상하게 된다.
> warm start 정책은 damping 2를 전제로 타이밍이 맞춰져 있었으므로 그 타이밍이 전부 어긋난다.
> → v7에서 **PD를 원래대로(100/2) 되돌렸다.** armD의 12개 레버 중 유일하게 "주범"으로
> 지목한 항목이다.

### V3
- 몸통 흔들림이 좀 심함

> **답변: armD에서 상속받은 `stand_posture: -2.0` + PD 200/5가 그대로다.**
> v3는 armD의 12개 레버를 전부 물려받았으므로, armD의 흔들림 원인 2개(`stand_posture` 과강도,
> PD 2배)를 **그대로 안고 있다.** v3 자신의 3개 메커니즘은 이 문제를 건드리지 않는다.
> → v7에서는 PD 복원 + `stand_posture` 완화로 **두 원인 모두 제거**했다.
> 참고로 v3가 낙상 3회로 최저인 것과 흔들림이 있는 것은 모순이 아니다 —
> **넘어지지 않으려고 보수적으로 움직인 결과**가 느린 속도(median 0.043 m/s, 최저)와
> 몸통 흔들림으로 같이 나타난 것으로 보인다.

- video에 max body vel을 관찰할 수 잇는 iter가 없음. training때도 비슷한 분포란 이야기. maxvel을 10초정도 뽑아냈을 때 안정성을 추후에 따로 training해도 괜찮은거임?

> **답변: 진단이 정확하다. 그리고 "따로 training"에 대한 답은 조건부 예스다.**
>
> **진단 확인**: v3의 요구속도 median은 **0.063 m/s**로 전 버전 최저다. 이유는 v3의
> `goal_curriculum`이 `init_level: 0.35`에서 시작해 **목표 범위를 35%로 줄여놓았기** 때문이다.
> 즉 **학습 분포 자체가 더 느렸다.** "training때도 비슷한 분포"라는 추론이 맞다.
>
> **"maxvel 10초 뽑아내고 안정성을 나중에 따로 학습해도 되나"** — 두 갈래로 답한다:
>
> **❌ 순차 학습(먼저 빨리, 나중에 안정)은 위험하다.**
> 최고속도만 보상하는 정책은 **넘어지기 직전의 궤적**으로 수렴한다(발을 거의 안 들고 질질
> 끄는 등). 그 상태에서 안정성 항을 나중에 추가하면 **파국적 망각**이 일어나거나, 속도를
> 대부분 반납해야 한다. armD가 "warm start의 전제를 깨면 무슨 일이 나는지"를 이미 보여줬다.
>
> **✅ 대신 이렇게 한다 (v7 설계):**
> 1. **속도와 안정성을 처음부터 같이 최적화한다.** path mode는 속도를 요구하면서도
>    `survival`/낙상 페널티/자세 항이 전부 살아 있다. **커리큘럼이 속도를 서서히 올리므로**
>    정책은 매 단계에서 "그 속도에서 안정적인 해"를 찾는다.
> 2. **최고속도 상한 탐색은 별도 단기 실험으로 분리한다.** 이건 배포용 정책을 만드는 게
>    아니라 **"K1이 물리적으로 얼마까지 가능한가"라는 참조값**을 얻는 게 목적이다.
>    짧게 돌리고 버린다. 그 값이 있어야 1.3~1.5 m/s 목표가 합리적인지 판정할 수 있다.
> 3. **10초 지속 측정은 eval에 넣었다.** 새 리포트의 "구간 최고속도" + "1.0 m/s 초과 시간
>    비율"이 정확히 "지속 가능한 최고속도"를 본다. 순간 최댓값과 지속 속도를 분리해서 본다.
>
> 요약: **속도 학습은 따로 하지 말고, 속도 측정은 따로 하자.**
