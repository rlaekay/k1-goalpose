# non-codex.md — Codex 작업 감사와 통합본

> 2026-07-30. Codex가 산출한 `ebatch-codex.md` / `gbatch_results-codex.md` /
> `hbatch-codex.md` / `H0–H3-codex.yaml` / `K1_locomotion_hbatch-codex.urdf` /
> H 하네스 코드를 **전수 교차검증**하고, 내 기존 `ebatch.md` / `gbatch_results.md`의
> 오류까지 함께 정정한 통합본.
>
> 방식: Codex의 모든 정량 주장을 리포지토리 실물 파일로 재계산해 확인/반증했다.
> 확인된 것은 출처를 명시하고, 반증된 것은 반증 데이터를 제시한다.

---

## 0. 먼저 — 내 오류 두 건 정정

지적받기 전에 스스로 밝힌다. **내 `ebatch.md`의 가장 큰 주장 두 개가 틀렸다.**

### 오류 ①: "E0의 2.7 cm는 존재하지 않는 숫자다" → **틀렸다. Codex가 맞다.**

내가 `ebatch.md` §0-1, §2에서 단언한 내용이다. 원인은 명백한 절차 실수다:
`cd K1_walk/v7` 상태에서 `ls K1_walk/`를 실행해 실패한 것을 **"디렉터리가 없다"로
결론냈다.** 실물은 존재한다.

```
K1_walk/select_results/E0_armB_armsdown/report.json
  checkpoint : model_6200.pth          ← ARMSDOWN_CKPT의 실제 근거
  config     : .../2026-07-26-19-36-15_E0_armB_armsdown/config.yaml   ← 자기 config, 오염 없음
  date       : 2026-07-27 20:04:39
  구간       : 4633개 전부 waypoint (path 0개)
  pos        : 2.72 / 5.01 cm     heading 2.52°     falls 2     strict 89.29%
```

게이트 판정: 위치 median ✅ / p90 ✅ / heading ✅ / 낙상 ❌(2회). **3/4 통과.**
실패모드 분해: 정상종료 **99.3%**, 미도달 **0.0%**, 도달후이탈 0.1%.
카테고리 균일: stand 2.9 / straight 2.8 / turn 2.7 / lateral 2.7 / combined 2.6 cm.

> **E0@6200은 이 프로젝트 최고 정책이고, `gbatch.md`의 주장은 정확했다.**
> 내 `ebatch.md` §0-1·§2·§5의 E0 관련 서술은 **폐기**한다.

### 오류 ②: "5 cm 게이트는 보상 구조상 도달 불가능하다" → **과잉 주장이었다.**

계산 자체는 맞다. `constellation_weight 0.2`, `scale 3.5`, `goal_reach_radius 0.1`에서
오차 10→5 cm의 보상 이득은 `3.5(e^{-0.002}-e^{-0.0005}) = 0.005/step`이고 10 cm 문턱
통과는 `1.0/step`이다. **비율 200배**는 사실이다.

**그러나 결론이 틀렸다.** E0가 **같은 보상으로 2.72 cm**를 냈다. 따라서 200배 비율은
"5 cm에 도달할 수 없다"는 뜻이 아니라 **"공 안에서 어디에 앉을지는 다른 압력이
결정한다"**는 뜻이다. G1의 5.5 cm는 보상의 한계가 아니라 **path 학습이 만든 압력**이다.

> 권고는 유지하되 등급을 내린다: 공 내부 기울기는 **필수 수정이 아니라 강건화 조치**다.
> (Codex는 이 항목을 아예 건드리지 않았다 — §5-C에서 다룬다.)

---

## 1. Codex 작업 정량 평가

### 1-1. 평가 축 정의

| 축 | 0 | 3 | 5 |
|---|---|---|---|
| **사실성** | 파일과 불일치 | 대체로 일치, 일부 미확인 | 전 수치 재계산 일치 |
| **근본원인** | 증상만 기술 | 원인 후보 나열 | 메커니즘 확정 + 정량 |
| **반증가능성** | 가설을 반증할 설계 없음 | 부분 측정 | 실패 조건이 사전 정의 |
| **교란통제** | 5+ 레버 동시 | 2–3 레버 | 1 레버 격리 |
| **실행검증** | 미실행 | 정적 검사만 | GPU 실측 통과 |

### 1-2. 항목별 점수 (사용자 지시 번호 기준)

| # | 항목 | 사실성 | 근본원인 | 반증가능성 | 교란통제 | 실행검증 | 종합 | 비고 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 1 | masterplan 흐름 파악 | 5 | — | — | — | — | **A** | 정확 |
| 2 | 버전별 수정의도 대비 결과 | **5** | 4 | — | — | — | **A** | G3/G4 config-의도 불일치 발견 (내가 놓침) |
| 3 | 데이터 유효성 분류 | **5** | 5 | — | — | 5 | **A+** | 106행 CSV. E0 기준선 복원 |
| 4 | ebatch 보고 | 5 | 4 | — | — | — | **A** | E2 언더슈트 91.1% 정량화 |
| 5 | gbatch 보고 | 5 | 4 | — | — | — | **A** | 낙상 path/waypoint 15.6배 분해 |
| 6 | H0 = current bests | 4 | — | **1** | **1** | 3 | **C** | §5-A: G1 기준선 선택, 8레버 묶음 |
| 7 | H1/H2 보수적 실험 | 4 | — | 3 | 2 | 3 | **B−** | H2는 4레버 묶음 (본인도 인정) |
| 8 | 고속 상체 기울기 | 4 | **2** | 4 | 3 | 2 | **C+** | §5-D: 케이던스 산술 누락 |
| 9 | 외란·jitter 의무화 + "괜찮았나" | **5** | **5** | 5 | 4 | 4 | **A+** | substep 1/10 버그 발견 — 최고 성과 |
| 10 | train+eval 단일 하네스 | 5 | 5 | 5 | — | **5** | **A+** | 5단계 게이트, atomic marker |
| 11 | 모델/하네스 적절 활용 | — | — | — | — | 5 | **A** | 서버 실행까지 완주 |
| 12 | hbatch.md 작성 | 5 | — | — | — | — | **A** | 336줄, 전 항목 응답 |
| 13 | heel 접지 보상 | 5 | 4 | **5** | **5** | 2 | **A** | H3 단일레버 격리 — 설계 모범 |
| 14 | joint DR 점검·강화 | 5 | 4 | 4 | 3 | 3 | **B+** | encoder bias/target offset 추가 |
| 15 | y축 mirror 증강 + loss | 5 | 4 | 4 | **2** | 3 | **B** | mirror와 joint DR을 H1에 함께 |
| 16 | E2 감속 원인 | **5** | 4 | 4 | — | — | **A−** | 요구속도 동일 입증이 결정적 |
| 17 | 시뮬 시점 영상 + 외력 화살표 | 5 | 5 | **5** | — | 4 | **A** | 신규 로거 없이 재투영 — 정답 |
| 18 | sim2real 서치 | 4 | 4 | — | — | — | **B+** | 출처 8개. 단 §5-F |
| 19 | URDF 팔 겹침 | **5** | 4 | 3 | — | 3 | **B−** | §5-E: 겹침은 고쳤으나 대가 미보고 |
| 20 | 모든 질문 응답 | 5 | — | — | — | — | **A** | 누락 없음 |
| 21 | G1 압승 / E1·E0 인상 검증 | **5** | 5 | — | — | — | **A+** | 두 축 분리가 정확 |
| 22 | 정옆·정뒤 감속 | **2** | **1** | 4 | — | 2 | **D** | §5-B: E0 데이터가 설명을 반증 |

**종합: A/A+ 9건, B계열 6건, C계열 3건, D 1건.**
하네스 엔지니어링과 데이터 유효성 판정은 내 작업보다 확실히 낫다.
물리 메커니즘 진단(8·22)과 실험 설계의 교란통제(6·7·15)가 약하다.

---

## 2. Codex가 맞고 내가 놓친 것 — 크레딧

전부 재확인했다.

| 발견 | 검증 | 영향 |
|---|---|---|
| **E0@6200 유효 기준선 존재** | `select_results/E0.../report.json` 실물 확인 | 내 오류 ① |
| **외력이 decimation 10 중 1번만 submit** → 실제 충격량 명목의 **1/10**, eval은 10배 과대보고 | 코드 구조상 타당. Codex가 substep 재submit + submit 횟수 == `steps×decimation` 검사 추가 | **내 `gbatch_results.md` §4의 "최대 7.9 m/s² ≈ 0.8 g"는 10배 오류. 실제 ≈0.08 g** |
| **G3 실제 config에 grid OFF, flicker 0.004** | 문서 의도와 불일치 | 내 "G1+G2 통합 실패" 판정이 부정확 — **통합은 실행되지 않았다** |
| **G4 config에 강외란+protection 동시 ON** | 순수 SmoothTurn ablation 아님 | 내 G4 해석 보정 필요 |
| **`path_lag = max(gap−lookahead, 0)`는 단측(one-sided)** | 캐럿 추월(=floor 붕괴)이 `lag=0` = 완벽으로 기록 | 내 지적(leash가 상한)보다 **더 치명적인 절반**. 두 이유가 독립적으로 성립 |
| **checkpoint에 `grid_active/trials/success` 미저장** | `30/30`은 eval 종료 snapshot | H가 매 프로세스 1칸에서 재시작 |
| **G1@10700 저장 Adam LR = 1.70859e-4** = H 선언 `5e-6`의 **34.17배**, loader가 복원 → H1/H2 첫 iteration NaN | 서버 실측 | 내가 접근 불가했던 실행 버그 |
| **`time_out_buf` / `extras["time_outs"]` 갱신 시점 불일치** | 낙상과 goal-resample 동시 발생 시 timeout이 낙상을 가림 | PPO bootstrap 오염 |
| **mirrored critic의 COM-y가 raw uniform latent** → `u→−u`가 아니라 `u→1−u` | 물리적으로 맞음 | 미러 구현 정확도 |
| **64-env 1-iteration smoke의 사각지대** | 마지막 optimizer step 뒤 forward 없음 → 그 step의 NaN 미검출 | 게이트 설계 |
| **기존 \|ω\|가 yaw만 측정** | `cur_omega`를 `base_ang_vel`로 교체 | **내 `gbatch_results.md` §6의 \|ω\| p90 1.76~2.24는 yaw 전용 — 상체 흔들림을 못 봤다** |
| **`armsdown` 손이 Trunk 충돌 메시 내부** | 메시 실측 **관통 17.1 mm** (§6-3) | 사용자 관찰의 정확한 정체 |

**결론: 위 12건은 전부 유효하고, 그중 4건은 내 문서를 직접 정정한다.**

---

## 3. 내 기존 문서 정정 목록

| 문서 | 위치 | 정정 |
|---|---|---|
| `ebatch.md` | §0-1, §2, §5 | E0 "2.7 cm 없음" **전면 폐기**. E0 = 2.72/5.01 cm, 89.29%, 낙상 2, 게이트 3/4 통과 |
| `ebatch.md` | §4-2 | "5 cm 게이트 도달 불가" → "공 내부 기울기 부재. E0는 도달했으므로 강건화 항목" |
| `ebatch.md` | §7 | `goal_reached` 기울기 우선순위 **최상 → 중** |
| `gbatch_results.md` | §1, §8, §9 | "G1 압승" → **"G군 내부 1위. E0 대비는 회귀"** (위치 2.03배, 낙상 19배, strict −55.1%p) |
| `gbatch_results.md` | §4 | "외란 최대 0.8 g로 충분" → **substep 버그로 실제 ≈1/10 ≈ 0.08 g. 충분했다고 볼 수 없다** |
| `gbatch_results.md` | §5 | 22번 constellation 설명 **폐기** → §5-B의 path 전방 prior |
| `gbatch_results.md` | §6 | \|ω\| p90 수치는 **yaw 전용**이라 상체 흔들림 판정 근거 아님 |
| `gbatch_results.md` | §2-3 | "G1+G2 통합 실패" → **"통합은 실행되지 않았다"**(G3 grid OFF) |
| `gbatch_results.md` | §7 | `v7_extras`는 **마지막 256-env snapshot** — 대칭 분석의 통계적 무게 하향 |

---

## 4. 정정된 통합 성능표 (권위 있는 것만)

전부 자기 config, waypoint 구간, path 게이트 제외.

| 버전 | 증거 | ckpt | 위치 med/p90 | heading | strict | 낙상 | 속도 | 게이트 |
|---|---|---:|---:|---:|---:|---:|---|---|
| **E0_armB** | 유효(SHA 미기록) | 6200 | **2.72 / 5.01 cm** | **2.52°** | **89.29%** | **2** | body p90 0.58, >1m/s **3.0%** | **3/4** 🥇 |
| G1_speed | 유효 `a7782bb6` | 10700 | 5.52 / 7.42 cm | 2.54° | 34.22% | 38 | d(pose)/dt p90 **1.50**, >1m/s **17.7%** | 2/4 |
| E2_robust | 유효 `a7782bb6` | 11400 | 12.95 / 33.78 cm | 3.71° | 15.29% | 2 | body p90 **0.32**, >0.5m/s 2.84% | 1/4 |
| G2_robust | 유효 `a7782bb6` | 7500 | 25.93 / 85.78 cm | 5.12° | 10.27% | **0** | p90 **0.19**, >1m/s **0.50%** | 2/4 |
| V7_full | drift 영향 | 7500 | 40.38 / 57.79 cm | 4.05° | 4.19% | 38 | p90 1.18 | 1/4 |
| G4_smoothturn | 34% 학습 | 3400 | 42.04 / 63.61 cm | 4.82° | 7.29% | **1016** | p90 1.58 | 1/4 |
| E1_path | drift 영향 | 11400 | 52.10 / 57.94 cm | 3.30° | 8.25% | 94 | 구형 p90 1.31 | 1/4 |
| G3_full | 유효, 다중레버 | 5500 | 49.00 / 63.20 cm | 5.88° | **0%** | 17 | p90 1.51 | 1/4 |

**이 프로젝트가 지금까지 만든 것의 정직한 요약:**

> path 학습의 실제 교환비 (E0 → G1, 둘 다 E0 계보):
> **위치 2.72 → 5.52 cm (2.03배), 낙상 2 → 38 (19배), strict 89.29 → 34.22% (−55.1%p)**
> 대가로 **>1 m/s 체류 3.0 → 17.7% (5.9배)**.
>
> 즉 **속도를 6배 사서 정확도를 2배, 안전을 19배 팔았다.** 이것이 H 배치가 마주한 실제 문제다.

---

## 5. Codex가 틀렸거나 놓친 것

### 5-A. ⛔ H0가 "통합 기준선"이 아니다 — 8레버 묶음이다

Codex는 E2/G2를 "7 레버를 한꺼번에 켰다"고 정확히 비판했다. 그런데 H0는 G1 대비:

1. 새 팔 URDF (동역학 변경, §6-3에서 +32.4% I_zz)
2. low-dose 충돌/지지 외란 (신규)
3. goal jitter (신규)
4. goal segment bias (신규)
5. goal hold 2–3 step (신규)
6. rare flicker (신규)
7. encoder bias ±0.015 rad (신규)
8. motor target offset ±0.010 rad (신규)
9. Adam LR 재시작 (G1의 1.71e-4 → 5e-6, 34배 감소)
10. path floor/dwell/curriculum **의미 자체** 변경

**10개다.** H0가 G1의 속도·정확도를 보존하지 못하면 원인을 특정할 수 없고,
Codex의 처방("dose를 더 낮춘다")은 10개 중 4개(2–6번)만 건드린다.

> **요구 델타**: H0를 **레버 0개 짜리 순수 대조군**으로 재정의하거나, 최소한
> `H0a = G1 + 새 URDF + LR 재시작 + path 의미 수정`(측정 필수, DR 전부 OFF)와
> `H0b = H0a + low-dose 외란/jitter/DR`로 쪼갠다. **GPU 1장이면 순차로 가능하다.**
> 이게 없으면 "E2 실수를 반복하지 않는다"는 문서의 약속이 지켜지지 않는다.

### 5-B. ⛔ 22번(정옆·정뒤) 진단이 데이터로 반증된다

Codex의 설명(`hbatch-codex.md` 최종답, `gbatch_results-codex.md` §옆/뒤):

> "constellation이 위치+heading을 묶어, 진행방향으로 몸을 돌리면 `dtheta=0` heading
> 보상을 잃으므로 느린 side/back-step이 과제 정의상 합리적인 해다."

**E0의 clean `segments.csv`(오염 0건)가 이를 반증한다.** E0는 **같은 constellation
보상**을 쓰고 path를 학습하지 않았다. 시작거리 0.6–0.9 m로 통제하면:

| 카테고리 | n | 시작거리 | peak p50 | mean p50 |
|---|---|---|---|---|
| **lateral** | 162 | 0.74 m | **0.91 m/s** | 0.15 |
| straight | 122 | 0.76 m | **0.71 m/s** | 0.15 |
| combined | 153 | 0.76 m | 0.85 m/s | 0.15 |

**E0에서 lateral이 straight보다 28% 빠르다.** 감속이 없다. 보상이 원인이면
E0에서도 나타나야 한다. 나타나지 않는다.

**path 노출과의 관계는 단조다** (peak/시작거리 비, 오염 제외):

| arm | path 비중 | lateral | straight | lateral 우위 |
|---|---|---|---|---|
| **E0** | **0%** | **1.23** | 0.91 | **+35%** |
| E1 | 46% | 0.91 | 0.95 | −4% |
| V7 | 46% + robust | **0.38** | 0.49 | **−22%** |

> **진짜 메커니즘**: path 모드의 캐럿은 **항상 선도체의 heading 방향으로 전진**한다.
> 정책은 강한 **전방 이동 prior**를 학습하고, 목표가 옆·뒤에 생기면 그 prior와 싸운다.
> "같은 방향인데도 가끔 빠른 것"은 현재 yaw·momentum·gait phase가 우연히 맞을 때다
> (이 부분은 Codex도 맞게 지적했다).
>
> **치명적 함의**: Codex의 H 설계는 **네 arm 전부 `path: 0.35`로 고정**했다
> (`H0-codex.yaml:199`). 따라서 **가설을 반증할 축이 없다.** 원인이 path dose라면
> H0–H3 전부에서 lateral 감속이 재현되고, `--goal_pattern lateral` 측정은 그 사실을
> 기록하되 원인을 지목하지 못한다.
>
> **요구 델타**: 최소 한 arm에서 `path`를 축으로 만든다. `path: 0.15`(또는 학습 중
> ramp)로 두고 lateral 감속이 완화되는지 본다. 비용은 arm 하나다.

### 5-C. ⛔ 정확도 레버를 하나도 건드리지 않았다

H0–H3 네 config 전수 확인:

```
goal_reach_radius   : 0.1   (4/4 동일, 이진 보상, 공 내부 기울기 없음)
goal_reached        : 1.0   (4/4 동일)
constellation_radius: 1.0   (4/4 동일)
constellation_weight: 0.2   (4/4 동일)
heading_near_goal   : 0.0   (4/4 꺼짐 — 이미 구현돼 있는데 미사용)
gait_frequency      : [1.8, 2.4]  (4/4 동일, 속도와 비커플)
```

**H 배치 전체가 정확도 회복을 "dwell과 checkpoint selection"에만 의존한다.**
그런데 G1도 이미 dwell을 켰고 selection도 했는데 5.52 cm였다. 즉 **이미 실패한 두
장치에 다시 기대는 계획**이다. 채택 기준도 "G1의 5.52 cm보다 악화 금지"로,
**E0의 2.72 cm는 "회복 목표"라는 이름의 비-게이트**로 남겼다.

> **요구 델타**: H1 또는 H2에 정확도 레버 **하나**를 넣는다. 후보 우선순위:
> ① `goal_reached`를 `exp(-(d/0.05)²)`로 등급화 (공 내부 기울기 생성)
> ② `heading_near_goal` 활성화 + `constellation_radius` 0.5 (이동 중 heading 자유)
> ①이 최소 변경이고 §0-② 계산이 그 크기를 준다.

### 5-D. ⛔ 고속 기울기: 케이던스 산술이 빠졌다 — H2 실패를 예측한다

Codex는 "로그에 pitch가 없어 확정 불가"라 하고 steady-cruise gate로 우회했다.
**로그 없이 확정 가능한 부분이 있다.**

```
commands.gait_frequency: [1.8, 2.4] Hz   ← 명령속도와 독립 균일 샘플 (goal_pose.py:446)
좌/우 swing at gait_process 0.25 / 0.75  ← 1 사이클 = 2보 (goal_pose.py:973-974)
→ 보속 = 2 × f = 3.6 ~ 4.8 보/초
```

| 명령속도 | 필요 보폭 @1.8 Hz | @2.4 Hz | K1 실현가능(다리 0.52 m × 0.5~0.6) |
|---|---|---|---|
| 1.0 m/s | 0.278 m | 0.208 m | 0.26–0.31 m ✓ |
| **1.5 m/s** | **0.417 m** | 0.313 m | ✗ **불가능** |
| **1.8 m/s** (그리드 상한) | **0.500 m** | 0.375 m | ✗ **불가능** |

> **1.8 Hz가 뽑힌 env에서 1.5 m/s 명령은 0.417 m 보폭을 요구한다.** 정책이 할 수 있는
> 유일한 대응은 상체를 던져 CoM을 지지면 밖으로 내보내는 것 — **제어된 낙하**다.
> 즉 관찰된 lean은 **학습된 선택이 아니라 강제된 결과일 수 있다.**
>
> 이것은 G1의 추종비 붕괴도 설명한다: 명령 0.4→1.9 m/s 전 구간에서 실제 평균속도가
> **0.32~0.49 m/s로 평평하다.** 명령을 올려도 안 오르는 게 아니라 **케이던스가 안 올라서
> 못 오른다.**
>
> **예측(반증 가능)**: H2의 순항 pitch/roll 페널티는 물리와 싸운다. 정책이 취할 수 있는
> 유일한 순응 방법은 **감속**이고, 그건 **Codex가 막으려는 E2형 collapse 그 자체**다.
> H2가 "순항 안정성은 개선, 속도는 −5% 이내"를 동시에 만족하지 못하면 이 예측이 맞다.
>
> **요구 델타**: H2에 케이던스-속도 커플링을 함께 넣는다.
> `f_cmd = clip(v_cmd / (2·L_max), 1.6, 3.2)`, `L_max ≈ 0.28 m` → 1.5 m/s에서 2.68 Hz.
> `gait_frequency` 상한도 2.4 → 3.2로 올린다. **이건 보상이 아니라 명령 분포 수정이라
> 관측 54차원을 건드리지 않는다.** 넣지 않으면 H2의 성패를 해석할 수 없다.

### 5-E. ⚠ URDF: 겹침은 옳게 고쳤으나 대가를 보고하지 않았다

§6-3에서 메시 실측으로 전면 재검증했다. 요약: Codex의 진단은 맞고 처방은 **차선**이다.

### 5-F. ⚠ sim2real 조사에 이 프로젝트 최대 위험이 빠졌다

Codex의 10개 항목은 타당하고 출처도 실재한다. 그런데 **관측 벡터 자체에 대한 판정이
없다.** 이 프로젝트의 관측은:

```
projected_gravity(3) + base_ang_vel(3) + commands(10) + gait clock(2)
+ dof_pos(12) + dof_vel(12) + prev_actions(12) = 54
```

**base linear velocity가 없다.** 이것은 sim2real에서 **매우 좋은 신호**다 — 실기에서
몸통 선속도는 직접 관측 불가(적분 drift)이고, 그걸 관측에 넣은 정책은 전이가 깨진다.
이 프로젝트는 우연히 올바른 선택을 했고, **그 사실이 문서화되지 않아 앞으로 누군가
"속도를 관측에 넣자"고 제안할 위험**이 있다.

반면 실제 위험은 `commands(10)` 중 **슬롯 4–9가 `[0,0]`으로 고정**돼 있다는 것이다
(`Goal_Pose_V7.yaml:239-244`). 실기 배포 시 이 6채널에 무엇을 넣을지가 정의돼 있지 않고,
v8은 이 슬롯을 SmoothTurn lookahead로 재사용한다. **v7 정책과 v8 정책은 같은 54차원을
쓰지만 의미가 다르다** — 배포 코드가 잘못된 쪽을 채우면 조용히 실패한다.

> **요구 델타**: `hbatch.md`에 관측 계약(슬롯별 의미·단위·정규화·v7/v8 차이)을 표로 고정하고,
> 실기 dump와 element-wise 비교하는 절차를 체크리스트에 넣는다.

---

## 6. 새로 확정한 것

### 6-1. 22번 근본원인: path 학습의 전방 prior (§5-B, 데이터 제시 완료)

### 6-2. 8번 근본원인 후보: 케이던스-보폭 불가능성 (§5-D, 산술 제시 완료)

### 6-3. 19번 이중검사 — 실제 메시로 전면 재검증

사용자가 요구한 **이중검사**를 수행했다. **1차 시도는 내 근사가 틀려 실패했고, 그
실패를 이중검사가 잡았다.**

**1차(실패)**: 손 링크를 COM + 반경 35 mm 구로 근사 → 잘못된 권고안 도출.
**발견된 오류**: `left_hand_link`의 실제 메시(`Left_Arm_4.STL`, 삼각형 2350개)는
로컬 y로 **−0.030 ~ +0.228 m**, 즉 **0.258 m 길이의 팔뚝+손 통합 링크**다.
COM 기준 최대 반경 **0.1436 m** — 내 가정의 **4.1배**.

**2차(유효)**: 메시 정점 190개를 FK로 변환해 Trunk 충돌 프리미티브
(박스 `0.12×0.18×0.2 @ (0,0,0.1)`, 골반 실린더 `r0.05 l0.1 @ (0,0,−0.06)` 축=y)와
직접 관통 검사. 결과:

| 자세 (좌: roll/pitch/yaw) | Trunk 관통 | 손끝 최전방 x | ego 반폭 | 양팔 I_zz | vs armsdown |
|---|---|---|---|---|---|
| **armsdown 기존** (−90.0/+119.2/−90.5) | **17.1 mm** ❌ | −0.121 m | 0.156 m | 0.07624 | — |
| **hbatch-codex** (−77.3/0/0) | 0 ✅ | −0.027 m | **0.250 m** | **0.10090** | **+32.4%** |
| T포즈 원본 (0/0/0) | 0 ✅ | −0.001 m | 0.396 m | 0.20028 | +162.7% |
| **권고안** (−93.4/+124.9/−51.4) | **0 ✅** | **−0.113 m** | **0.145 m** | **0.07345** | **−3.7%** |

**판정:**
- ✅ **Codex의 진단은 정확하다** — armsdown은 관통 17.1 mm. 사용자가 본 "팔 겹침"의 정체.
  이것이 `gbatch.md` §8-5④의 PhysX 444,180 N과 같은 원인이다.
- ❌ **Codex의 처방은 차선이다** — 참조 각도(팔꿈치 0)는 겹침을 없애는 대신
  **ego 반폭 0.156 → 0.250 m (+60%)**, **yaw 관성 +32.4%**를 되돌린다.
  `hbatch-codex.md` §URDF는 "기존 htwk dynamics/mass/collision을 보존"이라고만 쓰고
  **이 대가를 보고하지 않았다.**
- ✅ **제3안이 존재한다**: 겹침 0, 손끝이 힙 뒤 11.3 cm, ego 반폭 **0.145 m**(armsdown보다도
  좁다), I_zz **armsdown보다 3.7% 낮다**. 즉 **겹침만 제거하고 관성 이득은 전부 유지**한다.

**전 관절 한계 검증** (`k1/K1_locomotion.urdf` 선언값):

| 관절 | 한계 | 권고값 | 여유 |
|---|---|---|---|
| Left_Shoulder_Roll | [−99.7°, +90.0°] | **−93.4°** | 6.3° ✓ |
| Left_Elbow_Pitch | ±130.1° | **+124.9°** | 5.2° ✓ |
| Left_Elbow_Yaw | [−139.8°, 0°] | **−51.4°** | 88.4° ✓ |

**붙일 값** (좌우 미러 = roll·yaw 부호반전, pitch 유지 — 미러 검증 통과, 좌우 I_zz 차
2.2e−5 kg·m²는 URDF 자체의 질량 비대칭 0.456 vs 0.457 kg에서 오는 고유값):

```xml
<!-- K1_locomotion_hbatch.urdf -->
Left_Shoulder_Roll   rpy="-1.63000 0 0"      Right_Shoulder_Roll  rpy="1.63000 0 0"
Left_Elbow_Pitch     rpy="0 2.18000 0"       Right_Elbow_Pitch    rpy="0 2.18000 0"
Left_Elbow_Yaw       rpy="0 0 -0.89700"      Right_Elbow_Yaw      rpy="0 0 0.89700"
```

> ## ⛔ 정정 (2026-08-03) — 위 §6-3의 권고 각도는 무효다
>
> 아래 "주의"가 경고한 바로 그 일이 실제로 일어났다. **전 정점 7050개로 재검증하니
> 위 권고안(−93.4°/+124.9°/−51.4°)은 7.9 mm 관통이 남아 있었다.** 190점 부분표본이
> 놓쳤다. 위 표의 "권고안" 행을 쓰지 말 것.
>
> 전 정점 + Trunk 프리미티브 3 mm 팽창으로 다시 푼 값이 최종이며
> `htwk-gym/resources/K1/K1_locomotion_hbatch.urdf`에 반영했다:
>
> | | 관통 | 손끝 x | ego 반폭 | 양팔 I_zz | G1 대비 |
> |---|---|---|---|---|---|
> | armsdown (G1이 학습) | 25.32 mm | −0.121 | 0.160 | 0.07624 | — |
> | hbatch-codex | 0.00 mm | −0.028 | 0.251 | 0.10090 | **+32.3%** |
> | **hbatch (신규)** `roll ∓1.72 / pitch +1.40 / yaw ∓0.21833` rad | **0.00 mm** | −0.029 | **0.155** | **0.06984** | **−8.4%** |
>
> **그리고 더 중요한 정정**: armsdown의 관통은 **물리적으로 존재하지 않는다.**
> 팔 관절이 전부 `type="fixed"`이고 `asset.collapse_fixed_joints: true`라
> 팔이 Trunk와 한 강체로 병합돼 PhysX가 그 관통을 평가하지 않는다.
> (`gbatch.md` §8-5④의 444,180 N은 팔꿈치가 `revolute`인 **armswing**이었다.)
> 즉 사용자가 본 겹침은 **렌더링 전용 결함**이고, Codex의 asset 교체는
> **물리적 이득 0에 동역학 +32.3%를 지불**했다. 이것이 H 배치 공통 실패의
> 유력 용의자이며 `hbatch.md`의 A 스크린이 이를 시험한다.
>
> 아래 원문은 방법론 기록으로 남긴다.

> **주의**: 190점 부분표본 검사다. 채택 전 전 정점(7050개)으로 재검증하고,
> Isaac Gym에서 `self_collisions: 0`으로 첫 스텝 접촉력이 0인지 확인해야 한다
> (`gbatch.md` §8-5④가 정확히 그 검사다). Codex의 smoke가 이미 8개 joint를 `1e-6`
> 오차로 비교하므로, 기대값만 위 표로 바꾸면 그 게이트를 그대로 재사용할 수 있다.

---

## 7. H 배치에 요구하는 델타 (우선순위)

| # | 델타 | 근거 | 비용 |
|---|---|---|---|
| **1** | **H0를 쪼갠다**: `H0a`(URDF+LR+path의미만) → `H0b`(+DR/외란) | §5-A, 10레버 묶음 | arm 1개 추가 또는 순차 |
| **2** | **케이던스-속도 커플링**을 H2에 추가, `gait_frequency` 상한 2.4→3.2 | §5-D, 1.5 m/s는 0.417 m 보폭 요구 | config만 |
| **3** | **정확도 레버 1개** 투입 (`goal_reached` 등급화 우선) | §5-C, E0 2.72 vs G1 5.52 | config + 보상 함수 1개 |
| **4** | **팔 rpy를 §6-3 권고안으로** | I_zz −32%, ego 반폭 −42% (codex 대비) | URDF 3줄 |
| **5** | **`path` dose를 한 arm에서 축으로** (0.35 → 0.15) | §5-B, 22번 반증 축 부재 | arm 1개 |
| **6** | **관측 계약 표 고정** (슬롯 4–9, v7 vs v8) | §5-F, 조용한 배포 실패 위험 | 문서 |
| 7 | E0 채택선을 게이트로 승격 (`E0의 2.72/5.01` 대비 열세 명시) | §4, 회귀를 회귀라 부르기 | 비교기 1줄 |

**유지할 것 (Codex 설계 중 그대로 좋은 것):**

- 공통 held-out 외란 profile + effective test-config SHA 비교 거부 — **내 설계보다 낫다**
- 5단계 smoke(STATIC/PATH_MECHANICS/TRAIN_UPDATE/DISTURBANCE/VIDEO) + atomic marker
- `model_0` 및 초기 checkpoint를 selection 후보에 강제 포함 (tail 60% 편향 제거)
- H3의 단일레버 격리 + 속도 조건부 heel target (`clip(0.08·vx, 0.02, 0.12)`)
- 신규 RGB 로거 없이 카메라 행렬로 3D 재투영 (17번의 정답)
- signed `gap−lookahead` 양측 판정 + dwell recovery 2.0 s 강제 종료
- `(falls)/(completed+falls)×1000` — survivor bias 제거

---

## 8. 한 줄

> **Codex의 하네스·유효성 판정은 채택한다(A+ 다수). 물리 진단 두 건(고속 lean의 케이던스
> 원인, 옆·뒤 감속의 path prior 원인)은 반증했고, H 설계의 교란통제 세 건(H0 10레버,
> 정확도 레버 0개, path dose 축 부재)은 E2/G2에서 두 번 반복한 실수의 세 번째 반복
> 위험이다. 그리고 이 프로젝트 최고 정책은 여전히 E0@6200(2.72 cm / 89.29% / 낙상 2)이며,
> G1은 속도 6배를 사서 정확도 2배·안전 19배를 판 거래다 — H 배치의 채택선은 G1이 아니라
> E0여야 한다.**
