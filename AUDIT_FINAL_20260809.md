# AUDIT_FINAL_20260809 — 감사 13건 수렴 최종 보고서

- **작성**: 2026-08-09 10:0x KST
- **입력**: 고정질문 12문항 × **독립 10회**(`AUDIT10_01`~`10`) + 데이터 감사 3건
  (`DATA_AUDIT_SPECTRAL` / `DATA_AUDIT_METHOD` / `DATA_AUDIT_PROVENANCE`)
- **규칙**: 10/10 일치만 "확정". 갈린 것은 **표결하지 않고 코드·데이터로 갈랐다**.
  못 가른 것은 "갈렸다"로 남겼다.
- **본 보고서에서 내가 직접 재확인한 것(원본 8건)**: Q1 전수 게이트 스캔(서버
  `logs/**/report.json` 559개), `eval_goal_pose.py:778-813` 지문 코드,
  리포트의 `effective_eval_protocol.asset` 보유 수, `I2a_dr/model_150` 리포트 2건,
  `git log -- eval_goal_pose.py`, `logs/` 디렉터리 분포.
- ⚠️ **각 결론에 "무엇이 이것을 뒤집는가"를 한 줄로 붙였다.** 이 캠페인은 철회 50건을 쌓았다.

---

## 1. 요약 — 만장일치 결론만

1. **`AUDIT_MODE_20260808.md` 의 "⛔ 미수정 3건"은 전부 닫혀 있다** (10/10, 커밋 `54e0885`).
2. **평가 프로토콜 지문이 채점 물리를 못 본다** — 559개 리포트 중 `asset` 지문 보유 **1개**.
   같은 체크포인트를 다른 물리로 잰 두 리포트가 **같은 `protocol_sha` 를 받는다**.
3. **발목 roll 속도한계 초과(over_share 0.27~0.55)는 채점 물리(armature=0) 산물이다**
   (9/10 + 반대 1건은 근거가 약함). 같은 체크포인트에서 armature 만 바꾸면 0.55 → 0.000.
4. **정확도 지표는 생존 편향에 인과적으로 반응한다** — 한 정책을 65 % 확률로 넘어뜨리면
   정확도가 **30 % 좋아진다**(`NJ_armasset` 38.75 → 27.26 cm, 다른 조건 전부 동일).
5. **`_OFF_PROTECT` 가 관절 보호 보상 6개를 전부 끈 채 I1 배치부터 현재까지 상속됐다** (10/10).
6. **배포 정책 `I3b_stance10` 의 학습 자산이 실물과 어긋난다** (10/10).
7. **실기 제어 루프가 39.6 Hz 로 돈다**(설계 50 Hz), **보행 케이던스 1.51 Hz**(명령 2.0) (9/10).
8. **시뮬 정확도 개선(47.70 → 9.33 cm)이 실기 성공과 연결된 근거는 0건이다** (10/10).
   그 지표에서 **3.10 cm** 인 정책(`I3b_stance10`)이 로봇에서 실패한다.
9. **`5 cm 게이트를 넘는 체크포인트가 없다`는 명제는 틀렸다** — 넘는 것이 많다(§3).
   진짜 문제는 **그 게이트가 실기와 무관한 축**이라는 것이다.
10. **다음 한 걸음은 GPU 가 아니라 로봇 30초다** (8/10).

---

## 2. 12문항 일치표

`n/10` 은 **답 줄에 그 명제를 명시한 감사 수**다(침묵은 불일치로 세지 않고 별도 표기).

| Q | 일치 | 확정 답 | 근거 |
|---|---|---|---|
| **Q1** 게이트 통과 개수 | **갈림** → §3 에서 내가 확정 | **범위·단위를 정해야 답이 하나가 된다.** `logs/eval_rounds/**`(124건) 기준 정확도게이트(`pos_median ≤ 5 cm`) 통과 = **리포트 19건 / 고유 체크포인트(sha256) 9개**; **120 s 로 좁히면 리포트 12건 / 고유 sha 8개**. **4게이트 전부**(+`falls == 0`) 통과 = **3건, 전부 20 s `select`** → **120 s 에서는 0개**. `logs/` **전체**(534건, 게이트 보유) 로 넓히면 정확도게이트 274건 / 120 s 114건, **4게이트 전부 + 120 s 는 고유 체크포인트 1개** = `I2a_dr/model_150`. | 내 전수 스캔(§3) |
| **Q2** 미수정 3건 | **10/10** | **3건 전부 닫혀 있다.** 커밋 `54e0885`(2026-08-08 03:02, 감사 커밋 `d2a17b2` 의 7분 뒤). `b`~`r` 낙상 → `deploy_goal_pose.py:1613-1620` + `_start_policy_control()` 분리(`:1536`, 호출자 `:1511`/`:1634`), 발행 스레드 사망 → `:1822-1836`, `_verify_joint_layout` → `:668-690`. | 10회 전원이 행 번호로 답함 |
| **Q3** 지문에 asset/control | **10/10**(실효) | **코드에는 2026-08-09 커밋 `06104bc` 로 들어갔다**(`eval_goal_pose.py:792-813`, `asset` 10키 + `control` 5키). **그러나 서버 리포트 559개 중 `asset` 지문 보유는 1개뿐**(`logs/eval_rounds/video/NJ_armasset_walk_ownphysics`). ⇒ **현존 데이터 전부에 대해 도구는 채점 물리 차이를 못 잡고, 같은 `protocol_sha` 를 찍는다.** | 내가 코드·리포트 양쪽 직접 확인 |
| **Q4** `_OFF_PROTECT` | **10/10** | **키 6개**를 끈다(`tools/make_v7_arms.py:82-89`): `rewards.scales.{dof_pos_margin −0.5, dof_vel_margin −0.02, torque_margin −0.002, electrical_power −2e-3, stand_posture −1.0}` + `rewards.stop_ang_speed_threshold 0.3` → 전부 0. **베이스 config 에서 6개 전부 0 이 아니므로 6개 다 학습을 바꾼다.** `_I1_BASE`(`:273`)부터 `_I2`→`_I3`/`_I3_STANCE`→`_M`→`_N` 까지 **한 번도 꺼진 적 없이** 상속. | 10/10, 파일:행 일치 |
| **Q5** 실기 실효 보행 주파수 | **9/10** | **1.51 Hz**(명령 2.0 Hz → **1.32배 시간 팽창**). `tick_dt_s` **median 0.02524 s = 39.6 Hz**(mean 0.0261 = 38.3 Hz), 설계 50 Hz(`sim.dt 0.002 × control.decimation 10`). ⚠️ **LowState 율은 갈렸다** — §3 참조. | `real_walk.csv` 91행(2.38 s) |
| **Q6** 발목 roll 속도초과 | **9/10** | **채점 물리(armature=0)가 만든 것이다.** 같은 체크포인트(`NE_ctrl100/model_6000`) · 같은 프로토콜(`forward_hold` 120 s)에서 `asset.armature` 만 바꾸면 `AnkleR over_share` **0.5493 → 0.0000**, peak 80~90 → 18 rad/s, 낙상 **28,955 → 0**. | 다수가 독립적으로 재실행 |
| **Q7** 발목 pitch 토크 포화 | **10/10 방향, 크기·순위는 갈림** | **살아 있다. 발목 pitch 는 전 arm · 전 물리에서 최다 포화 관절이다**(`forward_hold` 120 s `sat_share` **3.7~24.6 %**, 웨이포인트 120 s 는 0.3~19.8 %). **armature 를 켜도 사라지지 않는다** — 이 저장소에서 물리를 바꿔도 안 죽는 유일한 관절 리드. ⛔ **다만 "포화가 낙상을 만든다"는 성립하지 않는다** — 낙상 0 인 arm 에서도 포화가 높다. | 6/10 "살아있고 1위", 4/10 "살아있으나 1위 아님 / 크기 2~4배 감소" |
| **Q8** `NH_zeroclock` 9.33 cm | **10/10 숫자 유효 / 10/10 귀속 실패** | **숫자는 유효**(`model_6000`, waypoint, **120 s**, 4,610구간, 낙상 1, 표본탈락 0.02 %, psha `6400ca58`, 채점 armature **0.0**). ⛔ **"레버 둘의 합산"은 틀렸다** — 대조군 `NF_dwellclock` 에도 `pause_gait_during_dwell` 이 이미 켜져 있어 NF→NH 는 **레버 하나(`joint_zero`)** 짜리다. ⛔ **"낙상간격 2배"는 1건 대 2건**(p=1.00, CI 폭 1,100배). ✅ **"정확도 3.4배"는 살아 있다**(부트스트랩 CI 비중첩). | `DATA_AUDIT_METHOD` §3-3 |
| **Q9** `I3b_stance10` 자산 불일치 | **10/10** | **어긋난다.** 학습 자산 = `resources/K1/K1_locomotion_armsdown.urdf`(`config.yaml:49`), armature 0.0. 항목: 총질량 **18.714 vs 19.666 kg (−4.8 %)** · 발 링크 **0.383 vs 0.494 kg (−22 %)** · **발 관성 ixx·iyy 가 정확히 10배**(0.00202 / 0.00741 대 0.000202 / 0.000741, 같은 질량·같은 izz ⇒ 리비전 차이가 아니라 **오타**) · Hip_Pitch z **+15 mm** · Hip_Pitch 속도한계 **2.5배 과대**(18.0 대 7.1) · Knee 1.44배 · 발목 roll effort −25 %. | 10/10, 자산 직접 파싱 |
| **Q10** 실기 붕괴 최유력 | **8/10 이 "시계/루프 속도"를 1위** | §6 참조 (1위 시계, 2위 자산, 3위 발목 pitch 토크). 2/10 이탈: 02(정책 자체가 지속보행 불가를 1위), 09(토크–속도 곡선 부재를 1위). | |
| **Q11** 시뮬↔실기 연결 | **10/10** | **연결 근거가 하나도 없다.** ① 로봇에 올라간 정책은 `I3b_stance10` 하나뿐(`deploy/configs/Goal_Pose_E0.yaml:65`), N 배치는 **export 조차 안 됐다**. ② 실기 시계열은 2개뿐이고 둘 다 I3b 계보. ③ **반대 증거**: 실기에서 실패한 `I3b_stance10` 의 정확도가 **3.10 cm** 로 현재 "최고 셀" 9.33 cm 보다 **3배 좋다**(같은 120 s). ④ 같은 `NH_zeroclock` 의 `forward_hold` 오차는 **60.92 cm**. | 10/10 |
| **Q12** 다음 1위 / 그만둘 것 | **8/10** | **1위 = 로봇에서 `--log-timing` 30~60 초 재측정 + 배포 시계를 벽시계로 분리.** 비용 **GPU 0 / 코드 ~10행 / 로봇 30분**. **그만둘 것 = waypoint 정확도(cm)로 체크포인트를 고르는 것**(9/10) + **armature 0.0 채점 텔레메트리를 리드로 쓰는 것**(6/10). | |

---

## 3. 갈린 것과 판정 근거

### 3-1. ⭐⭐ Q1 — 게이트 통과 개수. **갈린 이유는 답이 다른 게 아니라 세는 단위가 달랐다**

10회가 낸 값: **8 / 15 / 19 / 44 / 197**. 전부 **같은 데이터**를 셌다.
내가 서버에서 전수 조회해 **다섯 값이 전부 재현되는 것을 확인했다**(`FINAL_q1_scan.py`).

**세는 축이 넷이다.** 이걸 명시하지 않으면 답이 하나가 될 수 없다:

| 축 | 선택지 |
|---|---|
| **범위** | `logs/eval_rounds/**` (124건) / `logs/**` **전체** (559건, 게이트 보유 534건) |
| **단위** | 리포트 / 체크포인트 **경로** / 체크포인트 **내용(sha256)** |
| **프로토콜 길이** | 20 s `select` / 60 s / **120 s** |
| **게이트 수** | 정확도 1개(`pos_median ≤ 0.05`) / 정확도 3개(+`pos_p90 ≤ 0.10`, `heading_median ≤ 10°`) / **4개**(+`falls == 0`) |

**전수 조회 결과 (2026-08-09 실측, 서버 `htwk-gym/logs`)**

| 범위 | 게이트 | 리포트 | 고유 sha256 |
|---|---|---:|---:|
| `logs/eval_rounds/**` | `pos_median ≤ 5 cm` (전 길이) | **19** | **9** |
| `logs/eval_rounds/**` | `pos_median ≤ 5 cm`, **120 s** | **12** | **8** ⭐ |
| `logs/eval_rounds/**` | **4게이트 전부** | **3** | 3 — **전부 20 s `select`** |
| `logs/eval_rounds/**` | **4게이트 전부, 120 s** | **0** | **0** |
| `logs/**` 전체 | `pos_median ≤ 5 cm` (전 길이) | **274** | **189** |
| `logs/**` 전체 | `pos_median ≤ 5 cm`, **120 s** | **114** | **37** |
| `logs/**` 전체 | **4게이트 전부** | **16** | **11** |
| `logs/**` 전체 | **4게이트 전부, 120 s** | **5** | **1** ⭐⭐ |

**⇒ 캠페인 헤드라인으로 쓸 하나의 숫자**

> **`logs/` 전체를 전수 조회하면, 120 s 프로토콜에서 4게이트(정확도 5 cm · p90 10 cm ·
> heading 10° · 낙상 0)를 전부 통과하는 체크포인트는 정확히 `1`개다 —
> `2026-08-04-01-03-51_I2a_dr/nn/model_150.pth` (sha256 `3fb58445…`).**
> 두 개의 독립 실행에서 재현된다: `logs/cruise_i2a/seed2` **2.53 cm / 낙상 0 / 4,643구간**,
> `logs/force_ab/…/I2a_dr/force_seed1` **2.62 cm / 낙상 0 / 4,630구간**. 표본탈락 0 %.

⛔ **그러나 이것을 "쓸 수 있는 정책" 으로 읽으면 안 된다.** 같은 리포트가
`segments_path = 0`, `goal_pattern = None` 이다 — **웨이포인트 과제이고 보행 구간을
한 번도 채점하지 않았다.** 즉 게이트 자체가 §6 의 실기 실패와 무관한 축이다.

**따라서 CLAUDE.md 헤드라인의 두 문장이 둘 다 정정 대상이다:**
- ⛔ *"5 cm 게이트를 통과하는 체크포인트가 없다"* → **틀렸다.** 최소 189개가 통과한다.
- ⛔ *"정확도 게이트를 통과하는 체크포인트는 8개 있다(전수 조회)"* → **범위가 좁았다.**
  8은 `logs/eval_rounds/**` **한정 · 120 s · 고유 sha** 라는 4중 조건의 값이다.
  그 조건을 안 쓰면 8은 재현되지 않는다.

**왜 10회가 갈렸나 (계수 오류가 아니다)**
- **8** = eval_rounds · 120 s · 고유 sha ← 6/10 이 이 값을 냈다(축을 명시한 쪽)
- **15** = eval_rounds · 고유 **경로** (같은 sha 를 `best.pth` 와 `model_XXXX.pth` 두 경로로 셈)
- **19** = eval_rounds · **리포트** 수 ← 8/10 이 이 값을 냈다
- **44 / 197** = `logs/` **전체** 범위 · 경로 단위 (AUDIT10_10). 내 sha 기준값 37 / 189 와
  다른 것은 **경로 대 내용**의 차이다. `sha256` 이 없는 리포트가 **36건** 있어
  (`input_provenance` 결측, `DATA_AUDIT_PROVENANCE` §6-9) 고유 개수는 **하한**이다.

> **이 결론을 뒤집는 측정**: `input_provenance.checkpoint.sha256` 이 없는 36건을 복원해
> 다시 세면 고유 개수가 늘어난다(리포트 수 19/274/16 은 안 바뀐다).

### 3-2. Q5 — LowState 율이 **500 Hz 냐 378 Hz 냐**로 갈렸다 (4 대 2)

**표결로 안 갈랐다. 두 숫자가 서로 다른 양이기 때문이다.**
- **500 Hz** ← `low_state_age_s` median **1.024 ms**. 비동기 소비자가 보는 샘플 나이의
  기댓값은 주기의 절반이므로 주기 ≈ 2 ms = **메시지 도착률 ~500 Hz**.
- **378 Hz** ← 정책 tick 37.8 Hz × `decimation` 10. 이건 **소비/전진율**이다.
- ⭐ **그리고 배포 프로세스의 실제 발행률은 따로 측정돼 있다: `pub_hz` **185.1 Hz**
  (`server-dump/t_stand.csv`, 6,330행 / 184 s). 설계 500 Hz 의 **37 %**.**
- 옛 `499.2 Hz` 는 **모드 전환 프로브**의 산출물이지 배포 프로세스가 아니다
  (`measurements/mode_matrix_20260804.json` 의 `low_hz` 499.06~499.30 — 출처 확정).

⇒ **셋 다 맞고 셋 다 다른 양이다. 나란히 쓰면 안 된다.**

> **뒤집는 측정**: 배포 프로세스에서 LowState **도착 카운터**를 30 초 직접 세라.
> ~500 이면 도착은 정상이고 병목은 파이썬 루프, ~185 이면 발행 경로 자체가 병목이다.

### 3-3. Q5 부속 — `real_walk.csv` 가 실기 로그인가 (9 대 1)

AUDIT10_09 만 *"MuJoCo 출력이다"* 라고 답했다. **데이터로 기각된다.**
`DATA_AUDIT_SPECTRAL` §1-1 과 `DATA_AUDIT_PROVENANCE` §2-1 이 **서로 다른 4개 판별자**로
배포 산출물임을 확정했다: `low_state_age_s` 고유값 91/91·sd 0.0022(시뮬은 상수),
`tick_dt_s` 고유값 91/91, `tick_dt` ↔ `t_s` 차분 자기정합(편차 max 0.93 ms),
그리고 열 수 63(=`pub_hz` 추가 커밋 `5e4b306` **18분 전** 생성).

### 3-4. Q7 — 발목 pitch 포화가 "1위 리드" 인가 (6 대 4)

**갈린 채로 남긴다.** 방향(존재·물리 불변)은 10/10 이지만, 크기가 채점 물리에 의존한다:
`NE_ctrl100/model_6000`, `forward_hold` 120 s, `Left_Ankle_Pitch sat_share` =
armature 0.0 → **0.145**, 0.02 → **0.062**(2.4배 감소), 관절별 → 그 사이.
한 감사(07)는 armature 를 켜면 **Hip_Yaw** 가 1위가 된다고 답했다.
⇒ **"발목 pitch 가 최다 포화 관절이다"는 확정, "가장 강한 리드다"는 미확정.**

> **뒤집는 측정**: 낙상한 구간과 안 한 구간에서 `sat_share` 를 분리 집계하라.
> 차이가 없으면 이 리드는 낙상의 원인이 아니다(현재 낙상 0 arm 에서도 높다는 정황이 있다).

### 3-5. Q10 1위 — 시계 팽창(8) 대 정책 자체(1) 대 토크–속도 곡선(1)

**갈린 채로 남기되 우선순위는 §6 에서 정했다.** 판정 근거는 "몇 명이 그랬나"가 아니라
**실기에서 관측된 것이냐**다. 시계 팽창은 실기 로그 2개에서 직접 측정된 유일한 계통
오차이고, 나머지 둘은 시뮬·자산 쪽 추론이다.

---

## 4. 디버깅 항목별 활동·결과

상태는 **살아있음 / 죽음 / 미확인 / 종결**. 숫자에는 조건을 붙였다.

### 4-A. 학습 축

| # | 가설 | 무엇을 했나 | 결과 (조건 포함) | 상태 |
|---|---|---|---|---|
| L1 | **관절 영점 오차(`joint_zero` iid)를 학습에 넣으면 정확도가 오른다** | `NF_dwellclock` → `NH_zeroclock` 단일 레버 | **31.96 → 9.33 cm** (model_6000, waypoint, 120 s, 채점 armature 0.0). 부트스트랩 CI 비중첩([31.41,32.42] 대 [9.04,9.67]), 재현 노이즈 ~2 % 보다 훨씬 큼 | **살아있음** (단 지표 자체의 실기 타당성은 §4-B 참조) |
| L2 | `pause_gait_during_dwell` 이 도착 정확도를 살린다 | NF↔NH 비교로 귀속 시도 | ⛔ **귀속 불가.** 대조군 `NF` 에도 이미 켜져 있다 ⇒ NF→NH 는 **레버 하나**짜리 비교였다. "레버 둘의 합산" 서술은 근거 없음 | **미확인** |
| L3 | 낙상간격이 2배로 좋아졌다 (27,300 s 대 13,690 s) | 정확 Poisson 비 검정 | ⛔ **죽음.** 실체는 **낙상 1건 대 2건**, 비 0.502, 정확 95 % CI **[0.0085, 9.6]**, **p = 1.00** | **죽음** |
| L4 | `action_filter_tau` 학습이 도움이 된다 (`NC_actfilter`) | 백필 채점 | ⛔ 레버가 **공통 평가 config 에 없다** → **필터 OFF 로 채점**됐다. 이 데이터로 판정 불가 | **미확인** |
| L5 | 관측 이력 + 지연 (`NA_histzero`, `N4`, `N8_pathdelay`) | arm 완주 | ⛔ **대칭 손실이 확장 관측(270)을 안 덮는 결함 위에서 완주.** 잰 것이 "이력+영점"이 아니다 | **미확인** |
| L6 | **`armature` 가 안정성의 1차 변수다** | 같은 체크포인트를 채점 물리만 바꿔 재측정 | ⭐ **확정, 그리고 예상보다 크다.** `NE_ctrl100/model_6000`, `forward_hold` 120 s: armature 0.0 → 낙상 **28,955** / `AnkleR over_share` **0.55**; armature 0.02 → 낙상 **0** / over_share **0.0000** | **살아있음 (최상위)** |
| L7 | `path` 복원이 보행을 살린다 (N1/N2/N3) | model_6000 로 맞춘 대조 | **정확도 13배를 팔아 속도 1.35배를 샀다** — `N0_ctrl` 3.63 cm / 1.10 m/s 대 `N1_path` 47.70 cm / 1.48 m/s (둘 다 model_6000, 120 s) | **살아있음 (트레이드오프 확정)** |
| L8 | 대칭 손실 / `anti_mirror 0.20` 이 도움이 된다 | `N0_ctrl` zerostress 프로브 4셀 | `z0_none` **3.63 cm**(낙상 5) 대 `z3_anti_mirror` **4.14**(17) 대 `z3_iid` **4.19**(16) 대 `z3_mirror` **4.35**(10). 전부 같은 sha `2ef3364d`, 120 s. **anti_mirror 가 최선이지만 차이가 노이즈 바닥 근처** | **미확인** |
| L9 | **`_OFF_PROTECT` 가 관절 보호를 다 껐다** | 코드 전수 + 상속 추적 | 6개 키가 `_I1_BASE`(`make_v7_arms.py:273`)부터 **N 배치 전부까지 한 번도 안 켜졌다**. `dof_vel_margin`·`torque_margin` 이 없는 상태에서 §4-B 의 발목 포화·속도초과가 학습됐다 | **살아있음 (미검증 결함)** |
| L10 | **정확도 보상 자체를 한 번도 안 건드렸다** | 40+ arm config 전수 | `goal_reached` 이진 계단 · `heading_near_goal` 스케일 0 · `constellation_radius` 1.0 고정. 40+ arm 이 전부 **보상은 그대로 두고 관측·물리만 바꿨다** | **살아있음 (미시도)** |
| L11 | `constellation_weight` (`NK_clockconw`) | N3 대조 | `N3_pathcross walk` 낙상 22 대 `NK_clockconw walk` 44, 비 0.50, CI **[0.285, 0.90] — 유의**. 저장소에서 **유의한 낙상 쌍 2개 중 하나** | **살아있음** |
| L12 | 체크포인트 선택이 공정한가 | `select` 대 `winner_video` 28쌍 대조 | ⛔ **`.select` 의 낙상 수가 계통적으로 낙관 편향**(부호 23:4, 부호검정 **p = 0.0000**, 최대 불일치 **1 대 37**). `best.pth` 를 고르는 숫자가 이것이다 | **살아있음 (신규 결함)** |

### 4-B. 실기 축

| # | 가설 | 무엇을 했나 | 결과 (조건 포함) | 상태 |
|---|---|---|---|---|
| R1 | **배포 시계가 벽시계가 아니라 LowState 카운터다 → 궤적 시간 팽창** | `real_walk.csv` 91행(2.38 s) 언랩 + `deploy/utils/timer.py:15-19` 코드 대조 | ⭐ **실기에서 직접 관측.** `gait_process` 실효 **1.51 Hz**(명령 2.0) · `tick_dt_s` median **25.24 ms**(설계 20) · **91틱 중 75틱(82 %)이 예산 초과** · max 48.4 ms. `Timer` 는 LowState 콜백에서만 증가하는 카운터 | **살아있음 (1위)** |
| R2 | 배포 발행률이 설계보다 낮다 | `t_stand.csv` 6,330행 `pub_hz` | **185.1 Hz** (설계 500). 정책 **38.4 Hz**(설계 50) | **살아있음 (신규, 미해석)** |
| R3 | 배포 필터가 루프 속도에 의존해 보행을 깎는다 | MuJoCo 재현 + `--rate-fixed-filter` | 50 Hz 로 돌리면 68구간 중 **66회 낙상**(실기 증상), 시정수 고정하면 **0**. 수정본은 50·150·200·500 Hz 전부 낙상 0 | **살아있음, 로봇 미검증** |
| R4 | `b`~`r` 자세가 곧게 못 선다 | `--hold-diag` 실측 + `hip+knee+ankle=0` 수정 | 관절 추종 0.6° 인데 몸통 tilt **4.9~13.8°** → 수정 후 tilt median **1.4°** | **종결** |
| R5 | 모드 전환 3건 미수정 | 코드 재감사(10/10) | **커밋 `54e0885` 로 이미 닫혀 있었다.** 문서가 22시간 방치돼 배포 세션이 **없는 결함 때문에 로봇 시험을 미뤘다** | **종결 (문서가 원인)** |
| R6 | InitChannel GIL 데드락 | `faulthandler` 워치독 + `run_e0.sh` 재시도 | 경합이지 결정론적 데드락이 아님. 커밋 `b71dc13` | **종결** |
| R7 | **발목 roll 이 URDF 하드 스톱(±0.345) 밖에서 돈다** | `q5`/`q11` 초과 표본 + 매끄러움 검사 | ⛔ **보류.** 초과는 `q5` **4/91행**, `q11` **5/91행**, **전부 고립 단일 스파이크**(연속 아님). 그 채널은 12채널 중 **유일하게 매끄러움 검사 탈락**(r1(q) **+0.13/+0.39** 대 나머지 +0.88~0.97, MuJoCo +0.86/+0.95). **진짜 과주행과 계측 결함이 같은 모양을 만든다** | **미확인 (보류)** |
| R8 | 발목 roll 지배 주파수 11~12 Hz | 부트스트랩 + 백색잡음 널 + Lomb-Scargle | ⛔ **죽음.** 피크 파워 0.192 < 널 p95 **0.202**, Bonferroni 불통과, 파워 80 %가 나이퀴스트 위. 샘플링이 등간격이 아니다(**CV 0.303**) | **죽음** |
| R9 | 발목 roll 이 속도 에너지 최대 | 이동블록 부트스트랩 (B=4000) | ✅ **살아남음. 단 배수는 축소** — "2.6~4.7배"는 **가장 조용한** 관절로 나눈 값. 정직한 값 **2.09× [1.19,3.13] / 2.64× [1.63,3.85]**. ⭐ **그리고 이 우세는 실기 고유다** — 시뮬 6/6 파일에서 발목 roll 은 오히려 가장 조용한 축(0.33~0.54배) | **살아있음** |
| R10 | 진동이 명령에 있나 응답에 있나 | 명령/응답 대역 파워 분리 | ⭐ **응답/계측 쪽이다.** 명령 대역파워 0.058/0.025 대 응답 0.140. `act5` 는 12채널 중 **유일하게 시간구조 없음**(p=0.39) | **살아있음 (신규)** |
| R11 | 실기 토크가 sim effort 한계를 넘는다 | `real_walk.csv` `tau*` | `Left_Hip_Pitch` **37.82 / 30** · `Right_Ankle_Pitch` **23.64 / 20** · `Left_Ankle_Pitch` 21.94 / 20. ⚠️ **단 sim `tau` 는 명령 토크(클립 후), 실기 `tau` 는 펌웨어 추정 실토크 — 같은 양이 아니다** | **살아있음 (약화)** |
| R12 | `I3b_stance10` 학습 자산이 실물과 어긋난다 | URDF 3자 대조 | §2 Q9 표. **발 관성 10배는 오타로 보이고 관성 텐서가 삼각부등식을 2.64배 위반**한다 ⇒ 물리적으로 불가능한 자산 | **살아있음 (어긋남 확실 / 인과 미확인)** |
| R13 | N 배치를 로봇에 올릴 수 있나 | export 경로 확인 | ⛔ **못 올린다.** 관측 넓힌 arm 5개는 `export_model.py` 가 베이스 폭(54/14)으로 `ActorCritic` 을 만들고 strict 로드해 실패. `install_policy.sh:145` 스모크도 `(1,54)` 하드코딩. **배포 런타임은 이미 지원한다** — 끊긴 곳은 export 한 곳 | **살아있음 (미수정)** |

---

## 5. 데이터·측정 인프라 결함 — 무엇이 믿을 수 있고 무엇이 아닌가

### 5-1. ⛔ 믿으면 안 되는 것

| 대상 | 왜 | 크기 |
|---|---|---|
| **`pos_err_m.median` 을 arm 순위로 쓰는 것** | **생존 편향에 인과적으로 반응한다.** 낙상한 구간은 정확도 표본에서 **빠진다** | 한 정책 안 비교(교락 0): `NJ_armasset/model_6000` 을 **자기 물리**로 재면 **38.75 cm / 낙상 2 / 탈락 0.04 %**, **틀린 물리(armature 0.0)** 로 재면 **27.26 cm / 낙상 6,145 / 탈락 65.1 %**. **65 % 확률로 넘어뜨렸더니 정확도가 30 % 좋아졌다.** `NG_armature` 도 40.96 → 35.91 로 같은 방향 |
| **`armature 0.0` 로 채점한 `dof_vel_by_joint`** | 물리가 아니다 | 같은 체크포인트 `over_share` **0.55 → 0.0000**, peak 80~90 → 18 rad/s |
| **`falls` 원시 개수와 낙상간격(MTBF) 단일값** | 노이즈 바닥이 크고 실행 경로에 계통 편향 | 동일 체크포인트 28쌍 재측정: **11/28 이 공통 율과 양립 불가**, 부호 23:4(p=0.0000), 최대 **1 대 37** |
| **`.select` 리포트의 낙상 수** | 재사용 env 안에서 롤아웃 → 일관되게 낙관적. **그런데 `best.pth` 를 그 숫자로 고른다** | 위와 같음 |
| **`v7_extras` 의 스냅샷 계열(발 간격 등)** | 마지막 한 프레임 | — |
| **`claim_check.py` 의 체크포인트 라벨** | `_resolve_ckpt`(`:98-107`)가 **지금 이 순간의 심볼릭 링크**를 따라간다. 리포트 안에 불변 `input_provenance.checkpoint.sha256` 이 있는데 안 읽는다 | 리포트 **15/124** 에 `input_provenance` 자체가 없다 |
| **프로토콜 지문 `effective_eval_protocol_sha`** | 559개 중 `asset` 보유 **1개**. 채점 물리가 달라도 **같은 sha** 를 찍는다 | 낙상 488 대 70 인 두 리포트가 sha 동일(`c1c91bf8`) |
| **헤드라인 pooled median** | 표본의 **44.6 %(stand+turn)가 병진 이동 0 m** 인 구간 — "제자리에 있었나"를 잰다 | 카테고리별 낙상률 0.88 %(stand) ~ 62.1 %(combined), **70배** |
| **`gait_freq`·주파수 스펙트럼 주장** | 실기 샘플링이 등간격이 아니다 (**CV 0.303**), 비정상(EARLY 85 Hz → LATE 11.9 Hz) | §4-B R8 |
| **`real_walk.csv` 의 발목 roll `q`/`dq` 절대값** | 그 채널만 매끄러움 검사 탈락. **40샘플**에서 나온 통계 | §4-B R7 |

### 5-2. ✅ 믿을 수 있는 것

- **`pos_err_m.median` 의 재현성** — 동일 체크포인트 28쌍 상대 불일치 **median 1.09 %**,
  seed 복제 1.95 %. ⇒ **정확도 축의 노이즈 바닥은 상대 2 % 근처.** 9.33 대 31.96 은 진짜 차이다.
- **`eval_goal_pose.py` 의 지표 계산 자체** — 필요한 양은 거의 다 계산되고 CI 까지 찍는다
  (`report.md` 에 *"낙상 안전성 — survivor bias 보정"* 절이 있다, `:3371-3394`).
  ⛔ **깨지는 곳은 계산이 아니라 마지막 한 걸음이다** — 그 보정이 **낙상률에만** 적용되고
  바로 위 게이트 표의 **정확도에는 적용되지 않았다.** 그리고 `round_table`·`claim_check`·
  `ibatch` 의 교차 arm 표에는 **CI 가 하나도 없다.**
- **관절 인덱스 매핑** — 가정이 아니라 **하드웨어 산출물로 확정**됐다
  (`measurements/robot_layout_20260804.json` + PD 항등식 3,961행 회귀 + 부호 비대칭 + 하드스톱 접촉).
  Isaac 12 / MuJoCo 22 / SDK serial 22 **3자 일치**. ⚠️ 미확인 하나: 슬롯 1 vs 2(Hip_Roll vs Hip_Yaw).
- **실기 시계열의 출처 판별** — 판별자 4개 독립 일치.

### 5-3. ⛔ 인프라 결함 (신규, 전부 미수정)

1. **실기 시계열이 `logs/mujoco/` 에 산다.** 같은 디렉터리에 이름에 `real` 이 들어간 **시뮬**
   파일이 있다(`i3b_real_120s.csv`). **오판정 2회가 전부 여기서 나왔다.**
2. **실기 산출물 4개가 전부 단일 사본이고 버전관리 밖이다.**
   `real_walk.csv`(서버만) · `t_stand.csv`(맥만) · `measurements/*.json`(저장소 **밖**).
   **이 저장소에서 가장 비싼 데이터가 백업 0이다.** 총 3.4 MB.
3. **`check_policy_contract.py:227-240` 의 하드웨어 대조가 사실상 꺼져 있다** — `--robot-layout`
   인자는 구현돼 있는데 그 덤프가 저장소 밖이라 어떤 스크립트도 경로를 모른다.
4. **`tools/accept_eval.py`(N seed 재현성 게이트)가 어디에서도 호출되지 않는다** — 만들어 놓고
   `force_ab` 로 대체한 뒤 복귀하지 않았다. `hbatch-codex.md:614` 의 더 엄격한 승격 규칙도 미사용.
5. **`segments.csv` 에 `env_id`·`t_start_s` 가 없다** → env 내 상관을 직접 추정 못 한다
   (design effect 3.1 은 간접 추정).
6. **`Goal_Pose_E0.yaml:21-22` 의 주석이 23관절 인덱스를 말한다** — 같은 파일이 `joint_cnt: 22`
   다. **그 오해가 첫 하드웨어 기동을 죽인 원인**이었다(`deploy/utils/command.py:5-14`).

---

## 6. 결론과 방향

### 6-1. 실기가 세 걸음에서 무너지는 원인 — 최유력 1~3위

⚠️ **`[실기 관측]` 과 `[시뮬 산물]` 을 반드시 구분해서 읽어라.**

#### 🥇 1위 — 배포 시간축이 벽시계가 아니다 `[실기 관측]`

- **크기**: `tick_dt_s` median **25.24 ms**(설계 20) → 제어 **39.6 Hz**(설계 50).
  `gait_process` 는 **틱당 정확히 0.040** 씩 전진하므로 벽시계 케이던스가
  **1.51 Hz** 로 늘어난다(명령 2.0). **91틱 중 75틱(82 %)이 예산 초과.**
- **기전**: `deploy/utils/timer.py:15-19` 의 `Timer` 가 **LowState 콜백에서만 증가하는
  카운터**다. 루프가 느려지면 시계도 같이 느려진다.
- **왜 1위인가**: (a) 실기에서 **직접 측정된 유일한 계통 오차**다. (b) 같은 결함이
  **저역통과 필터**도 깎는다 — MuJoCo 에서 50 Hz 로 돌리면 68구간 중 **66회 낙상**,
  시정수를 고정하면 **0**. 실기 증상 그 자체를 재현했다. (c) **GPU 0 시간에 닫힌다.**
- ⛔ **서 있기로는 이 버그를 못 잡는다**(실측 확인) — 저역통과는 상수를 그대로
  통과시킨다. 그것이 *"서 있기는 완벽한데 세 걸음에서 무너진다"* 와 정확히 맞는다.

> **뒤집는 측정**: 로봇 30 초 `--log-timing`. `tick_dt_s` median 이 **20±2 ms** 로 오고
> 실효 gait 가 **2.0±0.05 Hz** 면 이 가설은 **기각**(첫 로그의 25 ms 는 로깅 오버헤드였다).
> **≥ 23 ms** 면 확증.

#### 🥈 2위 — 배포 계보의 학습 자산이 물리적으로 불가능하다 `[자산 확실 / 실기 인과 미확인]`

- `I3b_stance10` 학습 자산 `K1_locomotion_armsdown.urdf`: 총질량 **−4.8 %**,
  발 링크 **−22 %**, **발 관성 ixx·iyy 가 정확히 10배**(같은 질량·같은 izz ⇒ **오타**,
  관성 텐서가 **삼각부등식을 2.64배 위반**), Hip_Pitch **+15 mm**,
  **Hip_Pitch 속도한계 2.5배 과대**(18.0 대 벤더 7.1).
- **의미**: 속도한계가 2.5배 과대면 정책은 **실기가 낼 수 없는 스윙 속도**를 전제로 배운다.
  발 관성 10배면 발목이 **3.16배 느린 세계**에서 배운다.
- ⚠️ **이건 자산 파일에서 확인한 것이고, 실기 붕괴와의 인과는 측정된 적이 없다.**

> **뒤집는 측정**: `K1_robot_boxfoot.urdf`(8개 항목 전부 벤더 일치)로 같은 레시피를
> 재학습해 로봇에서 비교. 차이가 없으면 2위는 죽는다. 비용 = GPU 1 arm.

#### 🥉 3위 — 발목 pitch 토크 포화 + 보호 보상 전무 `[양쪽 관측, 인과 미확인]`

- **시뮬**: `sat_share` 가 `forward_hold` 120 s 에서 발목 pitch **3.7~24.6 %** 로 전 관절 1위.
  **armature 를 어떻게 바꿔도 안 사라진다** — 물리 불변인 유일한 관절 리드.
- **실기**: `Right_Ankle_Pitch` |τ| **23.64 N·m** > URDF effort 20, `Left_Hip_Pitch` **37.82** > 30.
  ⚠️ **단 sim `tau` 는 명령 토크(클립 후), 실기 `tau` 는 펌웨어 추정 실토크 — 같은 양이 아니다.**
- **구조적 배경**: `_OFF_PROTECT` 가 `torque_margin`·`dof_vel_margin`·`dof_pos_margin` 을
  **I1 배치부터 지금까지 계속 0 으로** 두었다. **정책은 한계를 피할 이유를 배운 적이 없다.**

> **뒤집는 측정**: 낙상 구간 대 비낙상 구간의 `sat_share` 분리 집계. 차이가 없으면 죽는다.

#### ⛔ 후보에서 **빼야 하는** 것 (전부 `[시뮬 산물]` 또는 `[계측 결함 의심]`)

| 제외 대상 | 이유 |
|---|---|
| **발목 roll 속도한계 초과 0.27~0.55** | armature 0.0 채점 산물. 물리만 바꾸면 **9,000배 사라진다** |
| **발목 roll 11~12 Hz 진동** | 백색잡음 널을 못 넘는다. 샘플링이 등간격이 아니다 |
| **"발목 roll 이 하드 스톱 밖에서 돈다"** | 초과 9행 전부 고립 스파이크, **12채널 중 유일하게 결함 있는 채널**에서만 |
| **`peak 713 rad/s` 류의 텔레메트리** | 물리가 아니다 |

### 6-2. ⭐ 이 감사에서 가장 큰 결론 — **우리가 최적화한 축이 실기와 무관하다**

10/10 이 독립적으로 같은 곳에 도달했다:

- 로봇에 올라간 적 있는 정책은 **`I3b_stance10` 하나뿐**. N 배치는 **export 조차 안 된다**(R13).
- 그 실패한 정책의 정확도가 **3.10 cm** 로, 지금 "최고 셀"이라 부르는 `NH_zeroclock`
  **9.33 cm** 보다 **3배 좋다**(같은 120 s).
- 같은 `NH_zeroclock` 의 `forward_hold` 위치오차는 **60.92 cm** — 정확도 축과 보행 축이 **반대**다.
- 게이트를 통과하는 리포트 전부가 `segments_path = 0` — **보행 구간을 채점한 적이 없다.**
- 그 과제의 요구 속도 median 은 **0.12 m/s** 수준이다. 실기는 **1.5 m/s 로 세 걸음**에서 무너진다.

> **⇒ 40+ arm 이 "실기 성능과의 상관이 한 번도 측정된 적 없는 대리지표"를 최적화했다.**
> 그리고 그 지표는 §5-1 에서 **생존 편향에 인과적으로 반대 방향**임이 증명됐다.

> **뒤집는 측정**: 정확도가 크게 다른 두 체크포인트를 **같은 로봇에서** 돌려 상관을 재라.
> 최소 2점이면 방향이 나온다. 이것이 이 프로젝트에서 한 번도 안 한 측정이다.

### 6-3. 다음 걸음 (비용 순)

| 순위 | 할 일 | 비용 | 사전등록 판정 |
|---|---|---|---|
| **1** | **로봇 30~60 초 `--log-timing` 재측정** (현재 배포 빌드 그대로) | 로봇 30분 · **GPU 0** | `tick_dt_s` median ≥ 23 ms → 시계 팽창 **확증**; 20±2 ms → **기각**. 동시에 `pub_hz`(185 대 500), 발목 roll `q` 매끄러움(r1), 하드스톱 초과 체류율 **셋을 같이 닫는다** |
| **2** | **`Timer` 를 `time.monotonic()` 기반으로 분리** + MuJoCo 회귀 | 코드 ~10행 · 1시간 | MuJoCo 는 500 Hz 라 **no-op 이어야 한다** — 그 no-op 자체가 검증. 실기에서 gait 2.0±0.05 Hz 면 확증 |
| **3** | **1차 엔드포인트를 시도(attempt) 단위로 바꾼다** — `P(strict 성공 \| 시도)` | 코드 몇 줄 · **새 GPU 0** (기존 리포트 51개 재집계) | 이미 계산해 뒀다: `I3b_stance10` 이 **3위 → 11위**로 내려가고 `NJ/NG` 의 "틀린 물리" 행이 "자기 물리" 행 아래로 간다 = **순위가 물리를 따라간다** |

**같이 하면 공짜인 것 3개**
- `claim_check._resolve_ckpt` 가 `input_provenance.checkpoint.sha256` 을 우선 읽게 한다(한 함수).
- `real_walk.csv` 를 `logs/robot/` 로 옮기고 CSV 첫 줄에 출처 주석을 쓰게 한다(오판정 2회의 원인).
- 실기 산출물 4개를 `.gitignore` 예외로 추적한다(3.4 MB, 백업 0인 유일본).

### 6-4. ⛔ 지금 당장 그만둘 것

1. **waypoint 정확도(cm)로 체크포인트를 고르는 것** (9/10). 그 지표에서 3.10 cm 인 정책이
   로봇에서 실패한다. 지표가 목표와 무관하다는 것이 **데이터로** 나왔다.
2. **`armature 0.0` 로 채점하고 그 텔레메트리를 리드로 쓰는 것** (6/10). 벤더 값은
   힙 0.0283~0.0478 / 무릎 **0.0956** / 발목 0.0565 다. 0.0 은 어느 자산도 말하지 않는다.
3. **레버를 하나씩 더 붙인 N 배치 arm 을 새로 뽑는 것.** 큐에 있는 축이 전부 **시뮬 내부 축**이다.
   **실기 로그 한 개가 40개 arm 보다 많은 것을 말해 줬다.**
4. **낙상 한 자리 수 차이를 "개선"이라 부르는 것.** 유의한 쌍은 저장소 전체에서 2개뿐이다
   (`N3 vs NK` 22:44, `NJ vs NE` 자기물리 0:70).

---

## 7. 정정이 필요한 문서 — 파일·주장 단위

⚠️ CLAUDE.md 규칙: **결론이 바뀌면 문서 수정 + 담당 세션 전달 + 코드 주석 수정**을 그 자리에서.
아래는 **본 감사 결과 정정 대상**이다. (본 보고서는 저장소 쓰기를 이 파일 하나로 제한받았으므로
**수정은 하지 않았다** — 목록만 낸다.)

| # | 파일 | 정정할 주장 | 무엇으로 바꿔야 하나 | 전달 대상 |
|---|---|---|---|---|
| 1 | **`CLAUDE.md`** 최상단 ⭐ | *"5 cm 게이트는 아직 못 넘었다"* / *"통과하는 체크포인트는 8개(전수 조회)"* | **범위·단위를 붙여야 성립한다.** §3-1 표로 교체. `logs/` 전체 · 120 s · 4게이트 = **1개**(`I2a_dr/model_150`) | Teacher |
| 2 | **`CLAUDE.md`** 최상단 ⭐ | *"두 레버(`joint_zero` + `pause_gait_during_dwell`)의 합산이 성립"* | **레버는 하나다.** `pause_gait_during_dwell` 은 대조군 `NF` 에도 켜져 있다 | Teacher |
| 3 | **`CLAUDE.md`** 최상단 ⭐ | *"낙상간격 27,300 s"* 를 성과로 인용 | **낙상 1건 대 2건, p = 1.00.** 인용을 빼거나 "판정 불가"로 | Teacher |
| 4 | **`CLAUDE.md`** armature 절 | *"MuJoCo `K1_serial.xml` 은 발목·**어깨·팔꿈치** 0.05"* | **각각 pitch 축만이다.** `Shoulder_Roll`·`Elbow_Yaw` 는 속성 없음(=0). 그 서술에서 유도한 `make_v7_arms.py:1125-1133` 의 부분문자열 매칭이 **자산이 말하지 않는 값을 넣는다**(현재 실해 0 — 다리 12관절 자산이라 안 걸린다. armswing 자산으로 옮기면 조용히 틀어진다) | Teacher |
| 5 | **`CLAUDE.md`** 최상단 | *"발목 roll 이 URDF 하드 스톱 밖에서 돈다"*(실기 최강 리드) | **보류로 강등.** 초과 9행 전부 고립 스파이크 + 그 채널만 매끄러움 검사 탈락 | Deployer · Tester |
| 6 | **`AUDIT_20260807_2300.md`** | 실기 최강 리드 = 발목 roll 하드스톱 초과 | 위와 같음. 그리고 *"정확도 보상을 한 번도 안 건드렸다"* 는 **유효**(10/10 확인) | Deployer |
| 7 | **`RETRACTIONS.md` C43** | *"저장소에 실기 시계열은 `real_walk.csv` 하나뿐"* | **2개다.** `server-dump/t_stand.csv` 6,330행 / 184 s (2026-08-09 생성). ⚠️ **낡은 항목에 무효 표시 필요** | 전원 |
| 8 | **`RETRACTIONS.md`** | 항목 50건이 쌓였는데 **낡은 항목의 무효 표시가 없다**(C5·C30·C34 에 이어 C43) | 항목마다 "현재 유효 / 후속 항목이 무효화" 상태열 추가 | 전원 |
| 9 | **`ibatch.md` §8-60** | 발목 통계(부호반전 51 %, \|dq\| 26.5 rad/s) | **40샘플에서 나온 값**이다. `t_stand.csv`(3,961행)로 다시 잴 수 있다 — 다만 서기 로그라 보행 붕괴는 못 잰다 | Tester |
| 10 | **`ibatch.md` §8-53** | `NH_zeroclock` 4셀 판정 | #2·#3 과 같은 정정. 순항체류 *"그대로다"* 는 **120 s 대 60 s** 비교이고 **≈5.5 SD** 차이다 | Teacher |
| 11 | **`OPINION_TRAINING_20260808.md`** | `anti_mirror 0.20` 의 근거 | 근거 문장이 존재하지 않는 절을 인용한다(유령 인용). zerostress 실측은 `z3_anti_mirror` **4.14 cm** 대 `z0_none` **3.63 cm** — **근거가 없을 뿐 아니라 방향도 반대일 수 있다** | Teacher |
| 12 | **`deploy/configs/Goal_Pose_E0.yaml:21-22,43`** | 주석이 **23관절 인덱스**를 말한다 | 같은 파일이 `joint_cnt: 22`. **그 오해가 첫 하드웨어 기동을 죽였다**(`deploy/utils/command.py:5-14`). **주석 수정 필수** (동작 영향 없음) | Deployer |
| 13 | **`tools/claim_check.py:98-107`** | `_resolve_ckpt` 가 현재 심볼릭 링크를 따라간다 | 리포트 안의 불변 `input_provenance.checkpoint.sha256` 우선. 없으면 `?` 표시 | Teacher |
| 14 | **`eval_goal_pose.py:3371-3394` 주변** | survivor bias 보정이 **낙상률에만** 적용됨 | 같은 페이지 정확도 표에도 적용하거나, **적용 안 됐다고 표에 명시** | Teacher |
| 15 | **`DEPLOY_REQUESTS_FROM_TRAINING.md`** | R6(`pub_hz` 실측) 상태 | **닫혔다.** `t_stand.csv` 가 **185.1 Hz**(설계 500)를 준다. 옛 `499.2 Hz` 는 **모드 전환 프로브**의 값이고 배포 프로세스가 아니다(`measurements/mode_matrix_20260804.json`) | Deployer |

### 7-1. ⚠️ 데이터 감사가 고정질문 10회의 답을 무효화한 지점 (근거의 질로 판정)

| 무효화된 답 | 어느 감사가 | 왜 그쪽이 이기나 |
|---|---|---|
| "발목 roll 지배 주파수 11~12 Hz" (`ibatch` §8-60 계열) | SPECTRAL | **널 분포를 만들어 검정**했다. 피크 0.192 < 널 p95 0.202. 원 분석은 등간격 FFT 를 가정했고 실제 CV 는 0.303 |
| "정확도 지표로 arm 을 세운다" (10회 중 다수가 표를 그렇게 읽음) | METHOD | **한 정책 안에서 교락 없이** 인과를 보였다(38.75↔27.26 cm, 다른 조건 전부 동일) |
| "실기 시계열은 1개" (여러 문서) | PROVENANCE | **전수 스캔 + 4개 독립 판별자**. 2개다 |
| "낙상간격 2배" (CLAUDE.md ⭐) | METHOD | **정확 Poisson CI**. 1건 대 2건, p = 1.00 |
| "`§8-41` 의 499.2 Hz" | PROVENANCE | **산출 파일을 특정**했다. 모드 전환 프로브의 값 |

---

## 8. 재현

```bash
# Q1 전수 게이트 스캔 (본 보고서 §3-1 의 모든 숫자)
scp scratchpad/FINAL_q1_scan.py a6000:/tmp/
ssh a6000 "bash -lc 'source /mnt/DATA/workspace/ws_eungkyu/miniconda3/etc/profile.d/conda.sh \
  && conda activate k1goalpose && python /tmp/FINAL_q1_scan.py'"
```

- 입력 감사 13건: `scratchpad/AUDIT10_01..10.md`, `DATA_AUDIT_{SPECTRAL,METHOD,PROVENANCE}.md`
- 본 감사에서 **서버 큐·GPU·저장소를 수정하지 않았다.** 저장소 쓰기는 이 파일 하나.

