# 디버깅 프로세스 3중 독립 감사 — 수렴 보고서

작성 2026-08-09 01:3x KST · 브랜치 `ekay-fix` @ `ebf2f35` · 서버 `a6000` 읽기 전용 조회
입력: 독립 감사 3건(A = 항목별 근거 재확인 / B = `report.json` 121개 전수 역추적 /
C = 반증 시도). **세 감사는 서로의 결과를 못 본 상태에서 같은 대상을 팠다.**

**이 문서의 규칙**: 3/3 합의만 결론으로 적는다. 갈린 것은 **표결하지 않고** 코드·config·
데이터·`git show` 로 직접 갈랐다. 못 가른 것은 "갈렸다"로 남겼다.
**세 감사가 인용한 숫자는 내가 원본에서 다시 뽑았다** — 17건을 재확인했고
그중 **2건이 틀렸다**(§3-1, §3-2). 확인 못 한 것은 "미확인"으로 적었다.
서버 큐·GPU 는 건드리지 않았다(조회 시점 GPU0 100 % / GPU1 86 %, 유휴 0초).

---

## 1. 요약 — 지금 어디에 서 있는가 (3/3 합의만)

**한 문장**: 시뮬 정확도 축은 진짜로 5배 전진했고(47.70 → 9.33 cm), 실기 축은
**2026-08-07 이후 한 걸음도 안 갔다**. 그리고 최우선 목표는 실기 쪽에 있다.

| | 3/3 합의 결론 | 내 재확인 |
|---|---|---|
| **S1** | **과제가 보행을 요구한 적이 없었다**(`path: 0.0`)는 근본 진단은 옳았다. `forward_hold` 축 신설은 옳은 대응 | ✅ N0 `required_speed_median` 0.1214 m/s |
| **S2** | **path 를 켜면 걷고 도착을 잃는다.** 같은 `model_6000` 에서 N0 3.63 cm / 1.10 m/s ↔ N1 47.70 cm / 1.48 m/s | ✅ 리포트 직접 조회 |
| **S3** | **`NH_zeroclock` 9.33 cm 가 현재 최고 셀이고 조건 축에서 유효하다.** 세 감사 중 둘이 이것을 죽이려 했고 실패했다 | ✅ `per_category` · 지문 재확인 |
| **S4** | **`constellation_weight` 는 죽었다.** 두 계보(NI·NK)에서 "정확도를 사고 안정성을 판다"가 재현. 사전등록대로 기각 | ✅ NK 11.77 cm(NH 9.33보다 나쁨) |
| **S5** | **`ND_dwell`(duty 상향)은 방향이 반대다. `N3_pathcross` 는 무효다** | ✅ |
| **S6** | **armature 는 시뮬 안정성의 1차 변수다.** 같은 체크포인트가 채점 물리만 바꿔 낙상 0 ↔ 28,955 | ✅ §3-2·§3-4 |
| **S7** | **`AUDIT_MODE` 의 "미수정 3건"은 `54e0885` 로 전부 닫혔다.** 문서 둘이 22시간째 틀린 상태 | ✅ 코드 3곳 직접 확인 |
| **S8** | **"5 cm 게이트를 통과하는 체크포인트가 없다"(CLAUDE.md·C5)는 거짓이다** | ✅ 개수는 갈렸다 → §3-1 |
| **S9** | **실기 축이 멈춰 있다.** N 배치 20+ arm 중 로봇에 올라간 것 **0개**. R2·R3·R5·R6·R7 에 `### 결과` **0건**. 마지막 로봇 접촉 2026-08-07 | ✅ |
| **S10** | **도구는 실제로 작동했다.** `claim_check` 도입(08-07 22:19) 이후 "비교 조건이 달랐다"형 철회는 **0건**. 2026-08-07 의 "하루 열세 번"은 재발하지 않았다 | ✅ C15~C35 전수 확인 |

⛔ **그리고 3/3 이 합의한 가장 큰 것**: 시뮬 지표와 실기 실패가 **한 번도 대응된 적이 없다.**
`DEPLOY_REQUESTS_FROM_TRAINING.md` 가 2026-08-05 에 스스로 적어 둔 문장이고 오늘까지 유효하다.

---

## 2. 디버깅 항목별 활동과 결과

숫자에는 조건을 병기했다: **체크포인트 / 프로토콜 / 길이 / 자산·채점물리**.
합의도 열은 A·B·C 세 감사의 일치도다. 근거는 전부 **내가 원본에서 확인한 것**이다.

### 2-A. 학습(시뮬) 축

| # | 가설 | 무엇을 했나 | 결과 (조건 병기) | 지금 상태 | 근거 | 합의 |
|---|---|---|---|---|---|---|
| T1 | 과제가 보행을 요구한 적 없다 | 리포트 `feasibility.required_speed_median` | N0 **0.1214 m/s** (waypoint, 120 s) | **살아있음/사실** | `logs/eval_rounds/**/report.json` | 3/3 |
| T2 | path 가 도착을 파괴한다 | `model_6000` 맞춤 비교 | N0 3.63 cm·`ok` 97.3 % ↔ N1 47.70 cm·`never_arrived` 61.9 % (120 s, 공통 프로토콜, boxfoot) | **살아있음/사실** | 〃 | 3/3 |
| T3 | 도착 소멸의 원인은 **보행 시계**(stand↔turn 자연실험) | `NF_dwellclock`(레버 하나) | ⛔ **자기 사전등록 기준으로 기각**. turn 32.42 cm (기준 2~4 cm의 8~16배) | ⛔ **부분기각** | §3-3 | 2/3 (C 미판정) |
| T4 | `joint_zero`(iid)가 정확도·안정성을 산다 | NZ ↔ 대조군 NE | NE 40.92 → NZ 25.61 cm, 낙상 7 → 1 (m6000, 120 s) | **살아있음** | 리포트 | 3/3 |
| T5 | 두 레버 합산 성립 | `NH` = NF + 영점 | **9.33 cm** / stand 2.70 / 보행 1.42 m/s / 보행낙상 3 (m6000, 120 s, armature 0) | **살아있음 / 최고 셀** | 리포트 | 3/3 |
| T6 | `constellation_weight` 가 정확도를 산다 | NI(계보 B), NK(계보 A) | NZ 25.61→NI 9.61 (이득) / NH 9.33→NK **11.77** (손해). 보행 낙상간격 NH 9,978→NK 698 s | **죽음(최고 셀에서 이득 0)** | 리포트 | 3/3 |
| T7 | armature 가 안정성 1차 변수 | 교차 물리 채점 2×2+ | 아래 §3-4 표 | **살아있음, 단 비대칭** | 리포트 | 3/3 |
| T8 | `feet_cross` 벌칙 | N3_pathcross | 44.58 ↔ N1 47.70 = null | **죽음(무효)** | 리포트 | 3/3 |
| T9 | `ND_dwell` duty 상향 | 900/6000 중단 | — | **죽음(방향 반대)** | `queue/` | 3/3 |
| T10 | 정확도 보상을 한 번도 안 건드렸다 | config 확인 | `goal_reached: 1.0` 이진, `heading_near_goal: 0.0` | **살아있음** | `sweeps/NH_zeroclock.yaml:606-607` | 3/3 |
| T11 | **보호 벌칙 6개가 08-03부터 상속으로 꺼져 있다** | `_OFF_PROTECT` 추적 | `dof_vel_margin` −0.02→**0**, `torque_margin` −0.002→**0**, `stand_posture` −1.0→**0**, `stop_ang_speed_threshold` 0.3→**0** | ⭐ **살아있음 / 미착수** | `tools/make_v7_arms.py:82-89` → `sweeps/NH_zeroclock.yaml:608-612,646` | **1/3 (A만)** |
| T12 | 남은 병목은 횡방향(≈`feet_distance_ref` 0.18) | NH 값 직접 조회 | ⛔ **NH cross_abs 4.19 cm**. 전체 오차와 같이 준다 | ⛔ **죽음** | §3-5 | 1/3 (A만) |
| T13 | 2단계 학습(path→waypoint) | — | **미실행 (0.25 h짜리, 25시간째)** | **미확인** | 큐·arm 정의 부재 | 3/3 |
| T14 | `zerostress`(영점 강건성 직접 측정) | 16런 rc0 완료 | **저장소 md 전체에서 인용 0건** | ⛔ **채점됨·미보고** | §3-6 | 2/3 (A·B) |

### 2-B. 실기(sim2real) 축

| # | 가설 | 무엇을 했나 | 결과 (조건 병기) | 지금 상태 | 근거 | 합의 |
|---|---|---|---|---|---|---|
| R1 | 평행 발목 토크 변환 부재 | SDK·config 확인 | `utils/command.py:19 cmd_type = LowCmdType.SERIAL` | **죽음** | 코드 | 3/3 |
| R2 | `base_walk` 가 같은 코드로 걷는다(반증) | config diff | `Base_Walk.yaml` = 23관절 T1 레이아웃, 이 로봇은 22관절 | **죽음** | 코드 | 3/3 |
| R3 | 두 번째 `sleep(0.001)` 이 발행률 상한 | `Timer` 전문 | `tick_timer_if_sim` 호출점 1곳 | **죽음** | 코드 | 3/3 |
| R4 | 질량·관성·발관성·떨림·전송지연 | MuJoCo 스윕 | — | **죽음** | 문서(원본 미검증) | 3/3 |
| R5 | **`pub_hz` 가 GIL 에 굶는다** | — | **미측정**. 단 §3-7 이 상한을 깎았다 | ⚠️ **살아있음·약해짐** | §3-7 | 3/3(살아있음) |
| R6 | **실기 발목 roll 이 URDF 하드 스톱 밖(±0.55 vs ±0.345)** | 실기 로그 분석 | 8/91 표본 초과, 전부 0.65 s 창 안, **단 1건은 tilt 1.29°(로그 최소)** | ⚠️ **갈렸다 → §3-2** | `real_walk.csv` | **A 살아있음 / C 죽음** |
| R7 | 관절 영점 δ | 캘 절차가 폐루프 | 소거 불가. `zerostress` 는 답 못 함 | **보류(살아있음)** | §3-6 | 3/3 |
| R8 | 모드 전환 결함 9건 | 3중 감사 → `54e0885` | **9/9 수정 완료** | **닫힘** | §3-8 | 2/3 (A·B) |
| R9 | RL hold 자세가 개루프로 못 선다 | MuJoCo hold_diag + 실기 | 실기 tilt 4.9°/13.8° 재현 | **살아있음 / 수정 미적용** | 문서(원본 미검증) | 1/3 (A만) |
| R10 | **실기 토크가 sim 클램프를 넘는다** | `tau0..11` 직접 분석 | HipP **37.82**(클램프 30), HipR **34.04**(배포계보 20), AnkP **23.64**(20) | ⭐ **살아있음 / 저장소에 사흘 방치** | §3-9 | 1/3 (C만) |
| R11 | **실기 보행 시계가 1.51 Hz 로 돌았다**(설계 2.0) | 위상 언랩 적분 | **1.5116 Hz**, 카운터모델 RMSE 0.00424 ↔ 벽시계 0.01990 | ⭐ **살아있음 / 신규** | §3-9 | 1/3 (C만) |
| R12 | **발목 pitch 가 토크 한계에 붙어 있다** | `torque_by_joint` 누산 | I3b 19.82/20.11 %, NH 17.6/14.7 % (`forward_hold` 120 s) | ⚠️ **살아있음, 단 조건 필요** | §3-4 | 1/3 (B만) |
| R13 | **배포 정책이 틀린 자산으로 학습됐다** | URDF 3종 직접 파싱 | 6축 전부 다르다(질량·발·허벅지·effort·속도한계·ROM) | ⭐ **살아있음** | §3-10 | 2/3 (B·C) |

---

## 3. 갈린 것과 그 판정

**표결하지 않았다.** 각 항목마다 무엇으로 갈랐는지 적는다.

### 3-1. ⛔ 5 cm 게이트 통과 체크포인트 — A 1개, B 8개 → **답: 8개**

리포트 121개의 `gates` 딕셔너리를 전수 조회했다(`pos_median ≤ 0.05` · `pos_p90 ≤ 0.10` ·
`heading_median ≤ 10°`). **세 게이트 통과 = 19개 리포트 행, 중복 제거하면 120 s 기준 8개 체크포인트다.**

| 체크포인트 | pos med | p90 | heading | 낙상 | 조건 |
|---|---:|---:|---:|---:|---|
| `MA_feetcross` best | **2.69** | 5.55 | 1.84 | 4 | 120 s, 공통 |
| `N0_ctrl` best | **2.82** | 5.24 | 1.92 | 8 | 〃 |
| `I3b_stance10` best(75) | **3.10** | 5.85 | 2.77 | **2,488** ⚠ | 〃 |
| `M3_scratch_phase` best | **3.30** | 6.08 | 2.21 | 16 | 〃 |
| `M7_robotasset` best | **3.54** | 6.50 | 1.86 | 10 | 〃 |
| `N0_ctrl` **model_6000** | **3.63** | 6.37 | 3.27 | 5 | 〃 |
| `MB_obsdelay` best | **4.28** | 8.91 | 8.57 | 8 | 〃 |
| `N8_pathdelay` best(100) | **4.39** | 7.88 | 2.95 | **441** ⚠ | 〃 |

**판정 근거**: `gates` 필드 직접 조회. **A 가 과소계수했다**(N0_ctrl 하나만 셌다).
**세 감사 다 옳게 지적한 것**: CLAUDE.md 상단과 `RETRACTIONS C5` 의
*"유효하게 남는 결론은 하나: 게이트 5 cm 를 통과하는 체크포인트가 없다"* 는 **거짓이다.**
그 문장은 파레토 스캔이 훑은 `N1`/`N2` 안에서만 참인데 한정 없이 적혀 있다.

⚠️ **다만 8개를 "쓸 수 있다"로 읽으면 안 된다.** 전부 `falls`(한계 0)를 못 넘고,
두 개는 낙상이 2,488·441이며, 나머지는 전부 **path 를 안 쓴 느린 arm**(0.87~1.10 m/s)이다.
**정확한 서술**: *"정확도 3게이트를 통과하는 체크포인트는 8개다. 정확도·속도·낙상을
동시에 만족하는 것이 0개다."*

### 3-2. ⛔ 발목 roll — A "채점 물리 탓", C "낙상의 결과" → **둘은 서로 다른 양을 판정했다**

**이것이 이번 감사에서 가장 중요한 판정이다. 두 감사는 사실 충돌하지 않았다.
저장소 문서가 서로 다른 두 양을 "발목 roll" 하나로 뭉쳐 놓았고, 각 감사가 다른 쪽을 잡았다.**

**(a) A 의 주장 = 시뮬 발목 roll *속도* 초과 → ✅ 참이다. 내가 재현했다.**

같은 정책 `NE_ctrl100` / 같은 `model_6000` / 같은 `forward_hold` 120 s, **채점 물리만 바꿈**:

| 채점 물리 | AnkleR mean | peak | **over_share** | 낙상 |
|---|---:|---:|---:|---:|
| `armature 0` (`dofvel` 라운드) | 12.29 | **80.0** | **0.5493** | 38 |
| `armature 0.02` (`armature02`) | 1.37 | 27.0 | **0.0001** | 488 |
| **자산 분포**(발목 0.05, `armature_asset`) | **1.60** | **17.8** | **0.0000** | 70 |

**정책은 한 글자도 안 바뀌었다. over_share 0.55 → 0.00 이다.**
⇒ 76~92 rad/s 는 **반사관성 0 인 관절이 접촉 충격에 자유롭게 역구동되는 수치 현상**이다.
⛔ `RETRACTIONS C30`·`C34`, `VENDOR_ACTUATOR_SPEC_K1.md §3`, `envs/K1/goal_pose.py` 및
`goal_pose_v7.py` 의 주석이 이것을 **실기 리드**로 적고 있다 — **정정 필요**.

**(b) C 의 주장 = 실기 발목 roll *위치* 초과가 "교란의 결과" → ⚠️ 절반만 참, 인과는 미확정.**

`real_walk.csv`(91행, 2.379 s, walking=1)를 내가 직접 파싱했다. `|q| > 0.345 rad`:

| t (s) | 관절 | q (rad) | tilt (°) |
|---:|---|---:|---:|
| 17.168 | R AnkR | −0.3875 | 10.58 |
| 17.344 | R AnkR | **+0.4564** | **1.29** ⬅ **로그 전체의 tilt 최솟값** |
| 17.372 | R AnkR | +0.4039 | 8.85 (중앙값 9.53 미만) |
| 17.590 | R AnkR | +0.5375 | 22.60 (최대) |
| 17.672 | L·R | −0.5779 / +0.3673 | 19.44 |
| 17.730 | L AnkR | +0.4904 | 11.43 |
| 17.805 | L AnkR | +0.3873 | 12.36 |
| 17.816 | L AnkR | +0.3734 | 14.11 |

- ✅ **C 의 시간 군집 사실은 맞다** — 8개 표본 전부 0.65 초 창(17.168~17.816) 안이다.
- ⛔ **C 의 인과 서술은 데이터가 지지하지 않는다.** C 는 *"전부 tilt 가 10.6°→22.6° 로
  튀는 창 안"* 이라 썼지만 **t=17.344 의 tilt 는 1.29° 로 로그 전체의 최솟값**이고
  C 는 이 표본을 언급하지 않았다. corr(tilt, max|AnkR|) = **0.32**.
- ⇒ **n=1, 2.4 초, 표본 8개, 상관 0.32 로는 원인과 결과를 가를 수 없다.**
  A(살아있음)도 C(죽음)도 이 로그만으로는 성립하지 않는다. **갈린 채로 남긴다.**
- ⭐ **그러나 이 질문은 동역학 없이 정적으로 갈린다** — 전원 없이 발목을 손으로 스톱까지
  밀고 인코더를 읽으면 된다(2분). 실기가 기계적으로 ±0.55 에 닿으면 **URDF 가 틀린 것**이고
  인과 논쟁 자체가 무의미해진다. **A 도 C 도 이 지름길을 못 봤다.**

**(c) ⭐ 그리고 아무도 계산하지 않은 기전이 따로 있다 — 벤더는 토크-속도 곡선을 쓰고 우리는 안 쓴다.**

벤더 저장소 `booster_train` 원본을 직접 읽었다
(`source/booster_train/booster_train/assets/robots/actuator.py`, `booster.py`).
K1 발목 = `BoosterK1AnkleParaWrapperCfg(base=BoosterJointE4310, effort_ratio (1,1),
velocity_ratio (1,1), armature_ratio (2,2))`:

```
tau_max = 38.3 N·m,  v_knee = 7.85 rad/s,  v_max = 17.59 rad/s,  armature = 2×0.0282528 = 0.0565
max_effort = clamp( tau_max · (v_max − |v|) / (v_max − v_knee),  0,  tau_max )     # actuator.py:114-131
```

**즉 벤더 모델에서 발목 토크는 7.85 rad/s 위에서 선형으로 줄고 17.59 rad/s 에서 0 이 된다.
우리 sim 은 속도와 무관한 상수 클램프 20 N·m 다.** 교차점은 **12.5 rad/s** — 그 위에서
벤더가 우리보다 **약하다**.

실기 91 표본에 벤더 포락선을 씌워 계산했다:

| 관절 | 실기 max\|dq\| | 벤더 cap < 우리 클램프인 표본 |
|---|---:|---:|
| L Ankle_Roll | **22.18 rad/s** | **9.9 %** (최소 cap **0.0 N·m**) |
| R Ankle_Roll | **26.49 rad/s** | **18.7 %** (최소 cap **0.0 N·m**) |
| 나머지 10개 관절 | — | 0.0~1.1 % |

⇒ **실기 발목 roll 은 벤더 velocity_limit(17.59)을 넘는 구간이 있고, 그 구간에서
벤더 자신의 액추에이터 모델은 토크 0 을 준다. 우리 sim 은 20 N·m 를 계속 준다.**
그리고 **최고 셀 `NH_zeroclock` 은 `forward_hold` 채점에서 발목 roll 이 18.1 rad/s 를
44.1 % 의 시간 동안 넘는다**(armature 0 물리) — 벤더 모델이면 그 시간 내내 토크 0 인 구간이다.

**⇒ 발목 roll 리드는 살아 있다. 그러나 기전은 셋 중 어느 것도 아니다:**
- ❌ *"정책이 실기가 못 내는 속도를 요구한다"*(C30/C34/VENDOR §3) — A 가 옳게 죽였다
- ❌ *"낙상의 결과일 뿐"*(C) — 데이터가 지지하지 않는다
- ✅ **"armature 0 이 발목을 비물리적으로 돌리고, 토크-속도 곡선이 없어서 그 속도에서도
  sim 이 full 토크를 준다"** — 두 결함이 곱해진다

### 3-3. ⛔ "도착이 죽는 원인은 보행 시계" — B 만 사전등록으로 기각 → **B 가 옳다**

`tools/make_v7_arms.py:943-950` 이 NF 를 세우면서 **숫자 보기 전에** 판정을 적었다:

> *"1차: `per_category` 의 **turn 범주 위치오차**. N1 은 it6000 에서 47.4 cm 였다.
> 그것이 **stand 수준(2~4 cm)으로 내려오면** M3(시계 결합)가 원인이었다는 확증이다.
> ⚠️ **도착이 안 돌아오면 M3 는 기각**이고 남는 것은 M1·M2다."*

`per_category` 를 내가 직접 뽑았다 (전부 `model_6000`, 공통 프로토콜 120 s, 위치오차 cm):

| arm | 전체 | **stand** | straight | lateral | **turn** | combined |
|---|---:|---:|---:|---:|---:|---:|
| `N0_ctrl` | 3.63 | 1.57 | 3.85 | 3.91 | 3.78 | 3.78 |
| `N1_path` | 47.70 | **2.25** | 47.86 | 49.00 | **47.55** | 49.18 |
| `NF_dwellclock` | 31.96 | **5.33** | 34.22 | 32.78 | **32.42** | 34.36 |
| `NH_zeroclock` | 9.33 | 2.70 | 10.46 | 10.03 | 10.12 | 10.87 |

**판정 셋, 전부 이 표 하나에서 나온다:**

1. ⛔ **사전등록 기준 미달.** turn 32.42 cm 는 기준(2~4 cm)의 **8~16배**다.
   **사전등록대로면 M3 는 기각**이고 남는 것은 M1(관측 동일성)·M2(신호 비대칭)다.
2. ⛔ **A 의 지적도 옳다**: `turn`(47.55)은 `straight`(47.86)·`lateral`(49.00)과 **구별되지 않는다.**
   turn 이 특별한 게 아니라 **stand 만 특별하다.** `AUDIT_20260807_2300 §2-1` 의
   stand↔turn dichotomy 는 **네 범주 중 둘만 보여준 것**이다. 실제 현상은
   **"시계가 도는 모든 범주에서 ~48 cm 의 고정 크기 종단 표류"** 다.
3. ⛔ **아무도 안 적은 대가**: `NF` 의 `stand` 가 N1 대비 **2.25 → 5.33 cm 로 2.4배 악화**했다.
   레버가 겨냥한 바로 그 상태(시계 정지 + 변위 0)에서 나빠졌다. `NH` 도 2.70 으로 더 나쁘다.

⚠️ **그럼에도 레버는 쓸모 있다**(47.70 → 31.96 cm). **살아 있는 서술은 이것뿐이다:
"시계를 얼리면 오차가 33 % 준다. 나머지 67 % 는 다른 것이다."**
`pause_gait_during_dwell` 을 **인과 확증으로 쓰는 것을 그만둬야 한다.**

**구조적 원인도 확인했다**: `tools/round_table.py` 에 `per_category` 가 **0회** 등장한다.
표준 보고 도구가 그 지표를 안 찍으므로 사전등록 기준이 **계산될 자리가 없었다.**

**부수 판정 (B 만 발견, 내가 확인)**: `AUDIT_20260807_2300.md:110` 의
*"저장소 전체에서 이 키는 코드 두 줄이 전부다. 어떤 yaml 에도, 어떤 sweep 에도 없다"* 는
**틀렸다.** `htwk-gym/archive/sweeps/hbatch/H{0,1,2,3}-codex.yaml:262` 에
`pause_gait_during_dwell: true` 가 있고, 그 sweep 의 `task: K1/Goal_Pose_HBatch` 는
`archive/envs/K1/goal_pose_hbatch.py:160 class GoalPoseHBatch(GoalPoseV7)` 이라
**그 키를 읽는 코드 경로 위에 있다.**
⚠️ **미확인**: H 학습 프로세스가 키 도입 커밋(`4659d01`, 07-30 03:49) 이후에 시작됐는지는
프로세스 시작시각 기록이 없어 못 닫았다. 그러나 **"어떤 sweep 에도 없다"는 문장은 거짓이다.**

### 3-4. ⛔ 발목 pitch 토크 포화 — B 만 1순위로 올림 → **숫자는 정확하다. 조건 하나와 반대 신호 하나가 빠졌다**

`v7_extras.torque_by_joint`(롤아웃 전체 누산, `torque_steps_accumulated: 6000`)를 직접 뽑았다.
**B 의 다섯 숫자가 전부 정확히 맞았다:**

| arm / 체크포인트 | L AnkleP `sat_share` | R AnkleP | 조건 |
|---|---:|---:|---|
| `I3b_stance10` best(75) | **0.1982** | **0.2011** | `forward_hold` 120 s, boxfoot 재채점 |
| `N0_ctrl` m6000 | 0.1699 | 0.0802 | 〃 |
| `NE_ctrl100` m6000 | 0.1453 | 0.0589 | 〃 |
| `NF_dwellclock` m6000 | 0.1425 | 0.0678 | 〃 |
| `N1_path` m6000 | 0.1201 | 0.0487 | 〃 |
| `NH_zeroclock` m6000 | 0.1757 | 0.1469 | 〃 |

다른 관절은 무릎 0.02~2.4 %, hip roll 0.004~1.3 %. **발목 pitch 만 두 자릿수다.**
우리 URDF `Ankle_Pitch effort = 20` (boxfoot·armsdown 둘 다, 직접 파싱).
벤더 = `BoosterJointE4310 effort_limit 38.3` (벤더 저장소 원본 직접 확인). **B 의 대조는 옳다.**

**그러나 B 가 안 적은 것 둘 — 둘 다 결론을 약하게 만든다:**

1. ⚠️ **표는 전부 `forward_hold`(지속 보행) 채점이다.** 같은 arm 을 **정확도 프로토콜**로
   재면 훨씬 낮다: `N0_ctrl` **0.0287/0.0275**, `N1_path` 0.0147/0.0123,
   `NH_zeroclock` 0.0688/0.0955. **"20 % 의 시간"은 지속 보행 조건에서만 참이다.**
2. ⛔ **반대 신호**: `NJ_armasset` 을 **자기 물리**에서 채점하면 발목 pitch 포화가
   **0.1050/0.1799 인데 낙상 0, 몸통속도 1.40 m/s** — 데이터셋 최고의 보행 셀이다.
   **높은 발목 pitch 포화가 그 자체로 병리는 아니다.**

⭐ **그리고 B 가 못 본 연결 — 이게 더 중요하다**: 포화가 높은 진짜 이유가 A 의 발견에 있다.
**`rewards.scales.torque_margin` 이 모든 arm 에서 0 이다**(§3-11). 베이스 config 는 −0.002 다.
**토크 한계에 붙어 있는 것에 대한 벌칙이 아예 등록조차 안 돼 있다.**
⇒ 리드는 **살아 있고 arm 하나 값어치가 있다.** 그러나 *"이것이 세 걸음 붕괴의 기전이다"* 는
아직 성립하지 않는다.

### 3-5. ⛔ "남은 병목은 횡방향" — A 만 기각 → **A 가 옳다**

`§8-53·§8-54·§8-57` 이 세 번 반복한 *"횡방향 |오차| 0.17 m ≈ `feet_distance_ref` 0.18 m
라 스탠스 폭이 바닥을 만든다"* 를 검사했다. `approach_error_m` 직접 조회
(전부 `model_6000`, 공통 프로토콜 120 s):

| arm | 전체 | **cross_abs** | along_med | along p10..p90 | overshoot |
|---|---:|---:|---:|---:|---:|
| `NZ_zeroiid` | 25.61 | **16.94** | −2.08 | −25.8 .. +25.3 | 0.460 |
| `NI_conw` | 9.61 | 5.85 | −0.03 | −10.8 .. +10.8 | 0.498 |
| **`NH_zeroclock`** | 9.33 | **4.19** | −1.08 | **−13.8 .. +11.6** | 0.426 |
| `N0_ctrl` | 3.63 | 2.14 | −0.44 | −4.5 .. +3.8 | 0.449 |

⇒ **횡방향 잔차는 0.18 m 근처에 고정돼 있지 않다. 전체 오차와 같이 준다**(16.94→4.19).
그 0.17 m 은 **`NZ` 한 점의 값**이었고 NH 로 이월되지 않는다. **그만둘 것.**

⭐ **대신 NH 의 진짜 잔차 구조가 보인다**: `along` median −1.08 cm 인데 p10..p90 이
**−13.8 ~ +11.6 cm**, overshoot 42.6 % 로 대칭이다. **편향이 아니라 접근축 위의
정지 정밀도(분산) 문제다.** ⇒ `NM_goalstop`(도는 중)이 겨냥한 양이 맞다. **방향은 옳고 근거 문장만 틀렸다.**

### 3-6. `zerostress` — A·B 둘 다 발견, 결론이 미묘하게 다름 → **둘 다 옳고 합치면 더 강하다**

16런 rc0 완료. **`grep -r zerostress --include='*.md'` = 0건.** 저장소 어느 문서에도 숫자가 없다.
전부 `model_6000` / 120 s / 공통 config (위치오차 cm / strict % / 낙상):

| arm | 학습시 `joint_zero` | 프로브 0° | 3° iid | 3° mirror | 3° anti_mirror |
|---|---|---|---|---|---|
| `N0_ctrl` | off | 3.63 / **71.8** / 5 | 4.19 / 59.4 / **16** | 4.35 / 57.9 / 10 | 4.14 / 61.2 / **17** |
| `N1_path` | off | 47.70 / 10.1 / 7 | 47.83 / 9.3 / 8 | 47.88 / 9.0 / 4 | 47.60 / 9.2 / 9 |
| `NE_ctrl100` | off | 40.92 / 7.2 / 7 | 40.97 / 7.2 / 7 | 41.10 / 7.4 / 12 | 40.94 / 7.4 / 10 |
| `NZ_zeroiid` | **on (iid, max 10°)** | 25.61 / 8.6 / 1 | 25.56 / 8.2 / 2 | 25.75 / 6.9 / 3 | 25.57 / 7.8 / 1 |

**합쳐서 읽으면:**
1. 3° 영점 오차의 **위치오차 대가는 모든 arm 에서 ≤ 0.7 cm** 다.
2. 눈에 보이는 손상은 `N0_ctrl` 하나 — strict 71.8 → 57.9~61.2 %, 낙상 5 → 10~17.
   **그런데 그 arm 에는 레버가 없다.**
3. 레버를 켠 `NZ` 와 대조군 `NE` 사이에 **분리가 없다.** 둘 다 25~41 cm 라 3° 가 만들 여지가 없다.
4. ⛔ **시험 강도(3°)가 학습 강도(커리큘럼 상한 10°)보다 낮다** — 학습 분포 **안쪽**을 찔렀다.
5. ⇒ **이 실험은 자기가 답하려던 질문에 원리적으로 답할 수 없는 설계였다.**
   해상도가 있는 arm(N0 3.63, 지금은 NH 9.33)에 레버가 없거나 안 걸었기 때문이다.

⚠️ **그럼에도 `NH` 의 정확도 3.4배 이득이 `joint_zero` 에 귀속돼 있다.**
그 이득이 "영점 강건성" 때문이라는 증거는 **없다.** 지금 증거로는 **정규화 효과**로 읽는 것이 안전하다.

### 3-7. `pub_hz` / 필터 — C 만 정량 상한을 걸었다 → **C 가 옳다, 리드가 약해진다**

`real_walk.csv` 에서 내가 직접 재확인:
- Δ`gait_process` = **0.03996 ± 0.00424**, 최댓값 **0.052**
- `run()` 이 `next_inference_time += 0.02` 로 위상을 잃지 않고 따라잡으므로,
  최댓값 0.052 는 정책 스레드의 **최악 GIL 굶주림 ≤ 6 ms** 를 뜻한다.
- `pub_hz` 가 50 Hz 가 되려면 발행 스레드가 **주기당 19 ms** 를 굶어야 한다 — 3배 이상.
- 그리고 `pub_hz ≤ f_LowState ≈ 378 Hz` 이므로 필터 τ ≈ **11.9 ms**(설계 10).
  MuJoCo 가 붕괴를 재현한 조건은 τ ≈ 89.6 ms(50 Hz) 로 **7.5배 밖**이다.

⇒ **필터·GIL 리드는 살아 있으나 "유력"의 지위를 잃는다.** 30초면 여전히 닫힌다.

### 3-8. `AUDIT_MODE` 미수정 3건 — A·B 둘 다 "닫혔다" → **참이다. 코드에서 확인**

`54e0885`(2026-08-08 03:02, `deploy_goal_pose.py` +122/−31) 가 셋 다 닫았다:

| 항목 | 현재 코드 | 위치 |
|---|---|---|
| 1-③ `b`~`r` 대기 중 낙상 | 대기 루프가 `in_recovery()` → `_step_recovery()` 를 돈다. `_policy_authorized` 게이트 신설 | `deploy_goal_pose.py:1614-1615`, `:1633` |
| 1-④ 발행 스레드 사망 | `_publish_cmd` 가 `BaseException` 까지 잡아 `_publish_error` 기록 | `:1831-1832`, 확인점 `:899` |
| 1-⑥ `_verify_joint_layout` | `_joint_layout_checked=True` 를 **통과했을 때만**, 오류는 `_layout_error` 로 전달 | `:725`, `:729`, 확인점 `:894` |

⛔ **그런데 `AUDIT_MODE_20260808.md:22-25,46,55,65` 와 `CLAUDE.md:110` 이 그대로 "⛔ 미수정"이다.**
CLAUDE.md 는 그 뒤 여러 번 수정됐다. **세션마다 자동 로드되는 문서가 다음 사람에게
이미 닫힌 안전 결함을 최우선으로 지시하고 있다** — 배포 세션이 이걸 읽으면 로봇 시험을 미룬다.

### 3-9. C 의 신규 둘 (NEW-1 시계 / NEW-2 토크) — **둘 다 내가 재현했다. 참이다**

**NEW-1 — 실기 보행 시계 1.51 Hz** (`real_walk.csv`, 91행, 2.379 s, walking=1, `gait_freq` 명령 2.0):

```
위상 언랩 적분 = 3.5960 사이클 / 2.379 s → 1.5116 Hz          (설계 2.0)
Δgp 회귀 RMSE:  벽시계 모델 0.01990  vs  카운터 모델(0.04) 0.00424   ← 4.7배 더 잘 맞는다
tick_dt_s med = 0.02524 s → 제어 39.6 Hz (설계 50), decimation 10 ⇒ LowState ≈ 396 Hz (설계 500)
```
⇒ **정책 궤적 전체가 실기에서 약 1.32배 시간 팽창한 채 실행됐다. 중력은 1배다.**
`deploy/utils/timer.py` 의 `get_time()` 이 **LowState 카운터 × 0.002** 이고 벽시계가 아니다.
정지·서 있기에는 영향이 없고 동적 보행에서만 누적된다 —
*"서 있기는 완벽한데 세 걸음에서 무너진다"* 와 **형태가 정확히 맞는다.**
⚠️ `policy_goal_pose.py:200` 의 docstring *"2 Hz is 2 Hz in wall clock"* 은 거짓이다.

**NEW-2 — 실기 토크가 sim 클램프를 넘는다** (관절 순서는 `Goal_Pose_E0.yaml:49-53,68`
`leg_dof_start: 10` + `[-0.2,0,0,0.4,-0.25,0]×2` 로 확정):

| 관절 | 실기 max\|τ\| (L/R) | boxfoot(N배치) | armsdown(**배포 계보**) | 벤더 |
|---|---:|---:|---:|---:|
| Hip_Pitch | **37.82** / 27.38 | 30 ⛔ | 30 ⛔ | 68.0 |
| Hip_Roll | 34.04 / 33.90 | 35 | **20** ⛔⛔ | 76.0 |
| Hip_Yaw | 15.97 / **21.15** | 20 ⛔ | 20 ⛔ | 38.3 |
| Knee | 30.47 / 28.86 | 40 | 40 | 112.0 |
| Ankle_Pitch | 21.94 / **23.64** | 20 ⛔ | 20 ⛔ | 38.3 |
| Ankle_Roll | 18.91 / 11.58 | 20 | **15** ⛔ | 38.3 |

⇒ **R1 의 답("실기 토크가 sim 클램프를 넘는가")은 2026-08-05 부터 저장소 안에 있었다.
답은 "넘는다", 6개 관절 중 4개에서.** 배포 계보의 Hip_Roll 은 클램프 20 인데 실기가 34.0 — **1.7배**.
`DEPLOY_REQUESTS` 는 R1 을 *"MuJoCo 가 대신 답했다"* 며 우선순위를 내려 뒀다.

### 3-10. 배포 자산 불일치 — B·C 둘 다 → **참이다. URDF 3종을 직접 파싱했다**

| | `armsdown` (I3b = **배포된 정책**) | `boxfoot` (N 배치 = 실물) |
|---|---:|---:|
| 총질량 | **18.7142 kg** | **19.6660 kg** |
| 발 질량 | 0.38305 / 0.38637 | **0.494 / 0.494** |
| `Hip_Roll` effort | **20** | 35 |
| `Ankle_Roll` effort | **15** | 20 |
| 관절 속도 한계 | 전부 **18** | 7.1 / 12.9 / 18.1 / 12.5 |
| `Hip_Pitch` ROM | [−2.21, 2.21] | **[−3.0, 2.21]** |

**조각은 문서에 흩어져 있지만(C27 origin, C31 effort, C30 velocity, HANDOFF 질량)
"배포된 정책은 이 여섯 축 전부에서 틀린 로봇으로 배웠다"는 한 문장으로 모인 적이 없다.**
(`Hip_Pitch` ROM 차이는 B 가 처음 확인했고 내가 재확인했다.)

### 3-11. ⭐ 모든 arm 이 보호 벌칙을 끈 채로 학습했다 — A 만 발견 → **참이다. 실행 config 에서 확인**

`tools/make_v7_arms.py:82-89` 의 `_OFF_PROTECT` 가 `_I1_BASE`(2026-08-03) → `_M_BASE`
→ `_N_BASE` 로 **상속**돼 I·M·N 배치 전부에 걸려 있다.
**실제로 학습에 쓰인 `sweeps/NH_zeroclock.yaml`(현재 최고 셀)을 서버에서 직접 읽었다:**

| 키 | `Goal_Pose_V7.yaml` (근거로 인용되는 파일) | **실제 arm** |
|---|---:|---:|
| `rewards.scales.dof_vel_margin` | −0.02 | **0.0** (:610) |
| `rewards.scales.dof_pos_margin` | −0.5 | **0.0** (:609) |
| `rewards.scales.torque_margin` | −0.002 | **0.0** (:611) |
| `rewards.scales.electrical_power` | −2e-3 | **0.0** (:612) |
| `rewards.scales.stand_posture` | −1.0 | **0.0** (:608) |
| `rewards.stop_ang_speed_threshold` | 0.3 | **0.0** (:646) |
| `rewards.scales.heading_near_goal` | — | **0.0** (:607) |
| `asset.armature` / `armature_by_joint` | — | **0.0** / **{}** (:91-92) |

**귀결 셋, 전부 지금 도는 arm 에 직접 걸린다:**

1. ⭐ **"학습에 관절 속도·토크 제약이 없다"의 해결책이 이미 구현돼 있고 6일째 꺼져 있다.**
   `_reward_dof_vel_margin`(`goal_pose_v7.py:1128-1130`)과 `_reward_torque_margin`(`:1132-1134`)은
   85 % 문턱 위 이차벌칙이고 **켜기만 하면 되는 레버다. 켠 arm 0개.**
   그리고 이것이 §3-4(발목 pitch 20 % 포화)의 가장 그럴듯한 이유다.
2. ⭐ **`stop_ang_speed_threshold: 0` 은 `goal_reached` 에서 몸통 정착 요구를 제거한다**
   (`goal_pose_v7.py:1100-1102`: `thr <= 0.0` 이면 base 를 그대로 반환).
   베이스 config 는 그 값을 0.3 으로 둔 이유를 주석에 적어 뒀다 —
   *"정책이 제자리에서 흔들면서 보너스를 계속 받을 수 있다."*
   ⇒ **`NM_goalstop` 이 지금 새로 사려는 것을 베이스 config 가 이미 갖고 있었고
   `_OFF_PROTECT` 가 껐다.** NH 의 병목이 정확히 `not_stopped` **19.6 %** 다.
3. ⛔ **`RETRACTIONS C30` 의 근거가 틀렸다**(결론은 맞다). *"`dof_vel_limits` 는
   `_reward_dof_vel_limits` 한 곳에서만 쓰이는데 그 스케일이 0"* → `dof_vel_limits` 는
   `_reward_dof_vel_margin` 에서도 쓰이고 그 베이스 스케일은 **−0.02(0 이 아니다)**.
   제약이 없는 진짜 이유는 `_OFF_PROTECT` 다. 같은 틀린 근거가 **코드 주석 두 곳에 복사**돼 있다.

⇒ **감사·문서·코드주석이 6일 동안 실행되지 않는 파일(`Goal_Pose_V7.yaml`)을 근거로 인용해 왔다.**

### 3-12. ⛔⛔ `claim_check` 가 자산과 게인을 지문에 안 넣는다 — B 만 발견 → **재현했다. 심각도 최상**

`eval_goal_pose.py:769-783` 의 `effective_protocol` 구성 전수:
`task, sim_dt, control_decimation, commands, noise, randomization, stress, goal_pattern,
record_video, force_visualization_probe, joint_dr_probe, joint_zero_probe`.
**빠진 것**: `asset.*`(URDF 파일, `armature`, `armature_by_joint`, `feet_edge_pos`),
`control.stiffness/damping`, `terrain`, `init_state.default_joint_angles`.

**서버에서 직접 돌려 재현했다 — 같은 체크포인트, 채점 물리만 다른 두 리포트:**

```
$ python tools/claim_check.py \
    logs/eval_rounds/lineageB/2026-08-08-18-18-59_NJ_armasset_final.walk/report.json \
    logs/eval_rounds/armature_asset/2026-08-08-18-18-59_NJ_armasset_final.walk/report.json

   ckpt_iter      6000            6000
   goal_pattern   forward_hold    forward_hold
   duration_s     120             120
   protocol_sha   c1c91bf8        c1c91bf8
   env_code_sha   07a1addf        07a1addf
   pos_err med    127.3 cm        62.09 cm
   falls (원시)     28955           0
   body_speed med 0.95            1.4

✅ 조건이 전부 같다. 이 리포트들은 나란히 비교해도 된다.
```

⛔ **낙상 28,955건 대 0건인 두 실행에 도구가 ✅ 를 찍는다.**
갈린 것은 `config` 파일명뿐인데 그 열은 **IDENTITY(달라도 정상)** 로 분류돼 있고,
`CLAUDE.md` 의 읽는 법이 *"진짜로 봐야 하는 것은 checkpoint · goal_pattern · duration_s ·
protocol_sha · env_code_sha 다"* 라고 못 박고 있다. **문서가 시킨 대로 읽으면 정확히 이 함정에 빠진다.**

**이번엔 사람이 알고 돌려서 사고가 안 났다. 그러나 이것은 이 저장소가 열세 번 겪은
바로 그 오류의 자동 생성기이고, 지금 라운드의 핵심 변수가 정확히 `asset.armature` 다.**

---

## 4. 아무도 안 본 곳 (세 감사 합집합, 중복 제거, 심각도 순)

| # | 안 본 것 | 왜 중요한가 | 비용 | 출처 |
|---|---|---|---|---|
| **U1** | ⭐ **N 배치 최고 셀을 로봇에 올린다** | **이것만이 최우선 목표(4발자국)를 판정한다.** 45시간 동안 0회 | 로봇 1세션 | B·C |
| **U2** | ⭐ **로봇 위 모델의 정체(R3)** — `ls -l` + `md5sum` | *"세 걸음 만에 무너진다"* 의 **주어가 확정되지 않았다**. i2a 인지 i3b 인지 모른다. 나흘째 | **2분** | B |
| **U3** | ⭐ **발목 roll 을 손으로 스톱까지 밀고 인코더 읽기** (DAMPING, 전원 불필요) | §3-2 의 인과 논쟁을 **동역학 없이** 끝낸다. 자산 오류 대 관측 정의 차이 | **2분** | B |
| **U4** | ⭐ **`--log-timing` 30초: `tick_dt_s` · `pub_hz` · `f_LowState` 동시** | §3-9(시계)와 §3-7(필터)을 한 번에 가른다 | **30초** | A·C |
| **U5** | ⭐ **`torque_margin`·`dof_vel_margin` 을 켠 arm** | 구현·근거·주석까지 다 있는데 08-03부터 상속으로 off. §3-4 를 정면 조준 | arm 1개 | A |
| **U6** | ⭐ **`stop_ang_speed_threshold: 0.3` 복원** | NH 병목 `not_stopped` 19.6 % 를 정면 조준. 베이스 config 가 이유를 주석에 적어 뒀다 | NM 과 합치면 0개 추가 | A |
| **U7** | ⭐ **속도-무관 클램프 → 벤더 토크-속도 곡선** | §3-2(c). 우리 sim 에 **T-N 곡선이 아예 없다**. 발목 roll 이 고속인데 그 지점에서 벤더는 토크 0 | arm 1개 + env 수정 | C(제기), 나(정량) |
| **U8** | ⭐ **2단계 학습**(`N1 model_6000` → `path: 0.0` 400 it) | 유일하게 "속도를 가진 정책에 정확도를 되돌리는" 방향. **0.25 h 인데 25시간째 미실행** | **0.25 h** | A·B |
| **U9** | **구동 지연 2~8 스텝** | 벤더가 **모든 관절 기본으로 켜는 축**(`booster.py:47-88`, legs·feet·arms 전부). 우리 최고 셀은 `obs_delay_steps [0,0]`, 그나마 그건 **관측** 지연이지 **구동** 지연이 아니다. 저장소에 구동 지연 모델이 **없다** | arm 1개, 3.6 h | B·C |
| **U10** | **`NH` 를 다른 물리에서 재채점** | 최고 셀·배포 후보 1순위가 **`armature 0` 물리에서만 존재**한다. §3-4 표의 민감도는 300~3600배 | **10분 GPU** | B |
| **U11** | ⛔ **`install_policy.sh:282` 로봇측 스모크의 `torch.zeros(1, 54)`** | `d51820d` 가 export 와 **서버측**(2/5, 폭을 모듈에서 읽음)은 고쳤는데 **로봇측은 안 고쳤다.** 폭이 54 아닌 arm 은 **설치 마지막 단계에서** 실패한다. 137행 로그 문구도 "(1,54)" | **5분** | C |
| **U12** | **미보고 라운드 4건**: `zerostress`(16런) · `N9_zerostruct` · `NB_zerocritic` · `NA_v2` | 채점 끝났는데 저장소에 숫자 0건. `round_status.sh` 는 "채점됨"으로 초록불을 준다 | 보고만 | A |
| **U13** | **`_SELECTION_IGNORED_GATES = ("falls",)`** | 선택기가 낙상을 순위에서 **명시적으로 제거**한다. N 배치에서 재확인 안 됨 | `select_best_checkpoint.py:116` | C |
| **U14** | **`audit_env.py` / `audit_urdf.py` / `probe_ankle_mapping.py`** | 한 번도 안 돌았다. 그 사이 신규 축이 6개 더 들어왔다(`armature_by_joint`, `target_fall_rate` 포함) | 분 단위 | A·B |
| **U15** | **`real_walk.csv` 의 나머지 40+ 컬럼** | 63 컬럼인데 문서가 인용한 것은 약 10개. §3-9 두 개가 여기서 나왔다 | **0** | C |
| **U16** | **`only_positive_rewards: true` 의 포화** | 총합이 음수면 0 으로 클립 = 큰 벌칙 구간에서 기울기 소멸. **U5 를 켜기 전에 확인해야 한다** | 계산 | A |
| **U17** | **지면 컴플라이언스** | `terrain.type: plane`. RoboCup 인조잔디의 침하·전단이 모델에 없다 | 미확인 | B |

---

## 5. 방법론 — 무엇이 반복되고 무엇이 고쳐졌나

### 5-1. 도구가 실제로 막은 것 (인정할 것)

- ⭐ **`claim_check.py` 는 자기 담당 구역에서 작동했다.** 도입(2026-08-07 22:19) 전후로
  철회의 성격이 갈린다: C1–C14 는 **거의 전부 "비교 조건이 달랐다"**, C15–C35 는
  **하나도 그 모양이 아니다**(전부 배포 코드·물리 자산·벤더 사양).
  **철회 건수가 안 줄어든 것은 실패가 아니라 조사 전선이 옮겨간 결과다.**
- ✅ **`RETRACTIONS.md`(35건)가 작동한다.** 세 감사 중 누구도 *"철회된 주장이 다시 세워진
  사례"* 를 못 찾았다. 나도 못 찾았다.
- ✅ **사전등록이 정착했다.** `make_v7_arms.py` 는 arm 마다 판정 기준을 숫자 보기 전에 적는다.
  `NK`(§8-57)는 그 기준대로 **자기가 기대한 방향과 반대로 기각**됐다. 드문 규율이다.
- ✅ **큐 사전검사**: `queue/gpu0/150-NQ_armvendor.sh` 가 학습 전에
  `grep -q 'Knee_Pitch: 0.095625'` 등 4개를 검사하고 실패하면 exit.
  "레버가 실제로 들어갔는지"를 사람 기억에 안 맡긴다.
- ✅ **음성 대조**: `armature_by_joint: {}` 로 no-op 실측 확인, 회귀 테스트 14검사에
  **11/14 실패하는 음성 대조**로 검출력 확인.
- ✅ **3중 감사에서 2대1을 표결로 안 정하고 `git show` 로 가른 것**은 모범이다.
- ✅ **서버 자율 운전**: 조회 시점 GPU 2장 94~100 %, 워커 4, 감시자 28초 전 갱신, 유휴 0초.
  §8-44 의 13.8시간 유휴는 코드로 막혔다.

### 5-2. 도구가 못 막은 것 — **감시 범위가 조사 전선을 못 따라온다**

| 함정 | 증거 | 왜 도구가 못 막았나 |
|---|---|---|
| **F1. 자산·게인이 지문에 없다** | §3-12: 낙상 28,955 ↔ 0 에 ✅ | `effective_protocol` 에 `asset`·`control` 이 없다. **그리고 지금 라운드의 핵심 변수가 `asset.armature` 다** |
| **F2. 학습 config 를 아무도 안 본다** | §3-11: 보호 벌칙 6개가 6일째 off | `claim_check` 는 `config` 를 IDENTITY 로 분류해 **파일명만** 찍는다. "레버가 정확히 하나인가"를 검증하는 것은 사람 주석뿐 |
| **F3. 계보(warm start)가 지문에 없다** | C9 가 만든 규칙인데 도구에 항목이 없다 | §8-54 가 *"두 수는 계보가 달라 직접 겨루면 안 된다"* 를 **손으로** 적어야 했다 |
| **F4. 사전등록은 쓰이는데 채점이 안 된다** | §3-3: NF 의 1차 기준이 **계산된 적이 없다** | `round_table.py` 가 `per_category` 를 **0회** 찍는다. 사후 자유도를 없애려던 장치가 사후에 무시되면 자유도가 그대로 돌아온다 |
| **F5. 근거를 실행 config 가 아니라 base yaml 에서 읽는다** | §3-11 | *"코드와 config 를 봤다"* 가 *"실행된 config 를 봤다"* 를 보장하지 않는다 |
| **F6. 정정이 원본까지 안 간다** | §3-8(수정됐는데 문서가 미수정), §3-2(a)(무효화됐는데 VENDOR·주석 그대로) | `RETRACTIONS` 는 "추가만 되는 대장"이라 **항목 자체가 낡는다.** 상태 열(유효/무효화됨)이 없다 |
| **F7. n=1 극값이 새 옷을 입고 재발한다** | C11(`feet_width` 마지막 프레임) → C32(`torque_saturated` 한 프레임) → `feet_sep_min_m`(롤아웃 전체 최솟값) | 누산기를 넣으면서 min/max 도 같이 넣었고 **판정에는 분율이 아니라 min 을 썼다.** 세 번째다 |
| **F8. "수정했다"의 검증이 커밋 메시지에 의존한다** | §5-3 | 정반대 방향의 같은 병 |

### 5-3. ⛔ 반복 확인: 커밋 메시지와 코드가 따로 논다 (양방향)

- **한 방향** (§3-8): 문서가 "미수정"이라는데 **고쳐져 있다**.
- **반대 방향**: `de1cc82` 가 약속한 세 수정 중 **둘이 코드에 없다.** 내가 확인:
  - `git show --stat de1cc82` → `Goal_Pose_V7.yaml`(+5), `eval_goal_pose.py`(+34).
    **`envs/K1/goal_pose.py` 를 한 줄도 안 건드렸다.**
  - `grep -r "zero_level_mean\|zero_deg_mean"` → **저장소 전체 0건** (약속 ②)
  - `goal_pose.py:565` 는 여전히 `d = torch.where(fell[env_ids], -step, step)` — **대칭 승강** (약속 ①)
  - `grep -r target_fall_rate` → **`Goal_Pose_V7.yaml:555` 한 곳. 읽는 코드 0개.** 죽은 config 다
  ⇒ **커밋이 스스로 *"반증 불가능한 주장이 된다"* 고 적은 문제가 그대로 남아 있다.**
  `NH` 가 몇 도에서 학습했는지는 여전히 아티팩트로 복원 불가능하다.
  ⚠️ **역설적 이득**: 그 덕에 `NZ` 와 `NH` 의 영점 레버는 같은 세기라 비교 가능성은 살았다.

⛔ **그리고 저장소가 가진 방어를 우회하는 구멍**: *"`set_dotted` 가 베이스에 없는 경로를
거부해서 오타가 죽은 config 를 못 만든다"* 는 방어가 있는데, **베이스 자체에 죽은 키를
넣으면 그 방어를 통과한다**(`target_fall_rate` 가 실례다).

### 5-4. 내가 이번에 저지른 같은 실수 (자기 표본)

**나도 이 저장소의 대표 실수를 한 번 했다.** 낙상간격을 검산하면서
`duration_s`(=120 s)를 썼는데 `claim_check._mtbf` 는 `feasibility.segment_duration_s.median`
(≈0.94~0.98 s)을 쓴다. 그래서 I3b 의 낙상간격을 **198.8 s** 로 얻고 5분간 들고 있었다
(도구의 값은 1.2~2 s). **원인은 "내가 본 것 바로 옆"이 어긋난 것** — 정확히 이 저장소가
열세 번 겪었다고 스스로 진단한 모양이다. 코드(`tools/claim_check.py:142-148`)를 읽고 나서야 갈렸다.
⇒ **이것 자체가 F5 의 증거다.** 그리고 도구의 `_mtbf` 는 직접값(`aggregate_env_simulated_time_s / falls`)
대비 **수 % 이내**로 타당하다는 것도 같이 확인했다(NH walk: 9,978 대 10,240 s).

### 5-5. ⛔ 가장 큰 방법론 문제 — 목표가 둘인데 지표가 하나다 (3/3 합의)

| 목표 | 지표 | 현재 |
|---|---|---|
| **G-가) 실기에서 4발자국 이상 걷기** (최우선) | 실기 낙상간격 | ⛔ **측정 자체가 없다.** 실기 낙상이 기록된 로그 **0건** |
| G-나) 목표자세 5 cm 게이트 | sim 정확도 med | 9.33 cm (최고 셀) |

**마지막 22~45시간의 커밋이 전부 G-나 에 들어갔다. G-가 는 2026-08-07 이후 한 걸음도 안 갔다.**
그리고 두 목표는 **서로 다른 레버를 요구한다** — `constellation_weight` 는 정확도를 사고
**보행 낙상간격을 15배 팔았다**(§3-1 의 NK).

⛔ **더 나쁜 것**: 실기 보행 증거의 **전부**가 `logs/mujoco/real_walk.csv` **한 파일,
91 표본, 2.379 초** 이고, **그 로그에는 낙상이 없다**(tilt max 22.60°, 낙상 문턱
`Goal_Pose_E0.yaml:169` = 45°). 즉 **"세 걸음 뒤 붕괴"를 기록한 파일이 저장소에 없다.**
그리고 증언 둘("다리가 모이며 발끼리 부딪혀" / "덜덜 떨며 다리가 벌어져")은 **서로 반대
증상이고 서로 다른 체크포인트다** — "세 걸음 붕괴"는 **하나의 증상이 아니라 최소 두 개**일 수 있다.

⇒ **지금 최적화하는 목적함수(`pos_err_m.median` in Isaac, armature 0, 필터 없음,
구동지연 없음, 발목 20 N·m, T-N 곡선 없음)가 실기 실패와 상관이 있다는 증거가 0 이다.**
이건 개별 판정의 오류가 아니라 **루프가 안 닫혔다**는 구조적 문제다.

---

## 6. 전체 결론과 방향

### 6-1. 프로세스는 옳았는가

**절반은 그렇다.** 2026-08-07 의 "하루 열세 번"에 대한 대응(`claim_check`·`RETRACTIONS`·
사전등록·대조군·pre-flight 게이트·3중 감사)은 **작동했고 재발을 막았다.**
08-08 이후 새 철회는 성격이 달라졌다 — 조건 불일치가 아니라 **인용 출처와 해석**의 오류다.
**그건 성숙의 징후다.**

**나머지 절반은 아니다.** 구조적으로 셋이 남아 있다:
1. **감시 범위가 조사 전선을 못 따라온다**(F1·F2) — 도구는 "어제의 실수"를 막는다
2. **읽지 않은 결과와 계산되지 않은 사전등록 기준**(F4) — 도구가 "돌았다"까지만 보증한다
3. **최우선 목표(로봇 4발자국)와 노력 배분의 어긋남**(§5-5) — GPU 는 100 % 로 도는데
   병목은 로봇에 있다. §8-44 의 "13.8시간 유휴"와 형태는 다르지만 같은 종류의 실패다 —
   그때는 자원이 놀았고, 지금은 자원이 **병목이 아닌 곳에서 100 % 로 돈다.**

### 6-2. 지금 가장 유력한 것

| 순위 | 리드 | 왜 | 판별 측정 |
|---|---|---|---|
| **1** | ⭐ **자산·armature·T-N 곡선 = 실기 물리 불일치 묶음** | 같은 정책이 채점 물리만으로 낙상 0 ↔ 28,955. 배포 정책은 6축이 틀린 URDF 로 배웠다. 벤더는 발목 armature 0.0565·T-N 곡선·구동지연을 기본으로 켜는데 우리는 **셋 다 없다** | `NP_clockarm`·`NQ_armvendor` 두 물리 채점(도는 중) + U7·U9 |
| **2** | ⭐ **실기 시간 팽창 (1.51 Hz / 39.6 Hz)** | 정책 궤적이 1.32배 늘어난 채 실행됐고 중력은 1배다. **정지에는 무해하고 동적 보행에서만 누적된다** — 증상 형태와 정확히 맞는다 | **로봇 30초** `--log-timing` (U4) |
| **3** | ⭐ **꺼져 있는 자체 레버 둘**(`torque_margin`·`stop_ang_speed_threshold`) | 구현·근거·주석까지 다 있는데 08-03부터 상속으로 off. NH 병목 `not_stopped` 19.6 % 와 발목 20 % 포화를 **동시에** 조준 | arm 1개 (U5·U6) |
| **4** | **실기 토크가 sim 클램프를 넘는다** | 6관절 중 4개. 배포 계보 Hip_Roll 1.7배. **답이 사흘째 저장소 안에 있었다** | 이미 있다 — 해석만 하면 된다 |
| **5** | **`b`~`r` hold 자세 (합 −0.05)** | MuJoCo 가 실기 4.9°·13.8° 둘 다 재현. 기전이 기하라 확실하다. **수정 미적용** | **로봇 15초** `--hold-diag 15`, tilt < 2° |
| **6** | **실기 발목 roll 위치가 하드 스톱 밖** | §3-2(b). **인과는 미확정이지만 정적으로 갈린다** | **로봇 2분, 전원 불필요** (U3) |
| **7** | **`pub_hz` / GIL** | §3-7 이 상한을 6 ms 로 깎았다. 약해졌지만 30초면 닫힌다 | U4 와 동시 |

### 6-3. 다음 걸음 (비용 오름차순, 판정 기준 사전 고정)

| # | 걸음 | 비용 | 확증 / 기각 |
|---|---|---|---|
| 1 | 로봇 `ls -l models/` + `md5sum` (**R3**) | **2분** | 디버깅 대상이 i2a 인지 i3b 인지. **나흘째 미해결** |
| 2 | 발목 roll 을 손으로 스톱까지 밀고 인코더 읽기 (전원 불필요) | **2분** | ±0.55 에 닿으면 **URDF 가 틀린 것**(자산 버그). ±0.345 에서 멈추면 **관측 정의 차이** |
| 3 | `--log-timing` 30초 → `tick_dt_s` · `pub_hz` · `f_LowState` | **30초** | `tick_dt_s` med > 23 ms → **시간 팽창 확증**. 20 ms 근처 → `real_walk.csv` 가 특수 조건 |
| 4 | `--hold-diag 15` → tilt | **15초** | 2° 안쪽이면 hold 자세 수정이 맞은 것 |
| 5 | `install_policy.sh:282` 폭 하드코딩 제거 | **5분** | 넓은 관측 arm 의 마지막 출구 차단 |
| 6 | **`NH` 를 `armature_by_joint` 물리에서 재채점** | **10분 GPU** | 최고 셀이 물리 변화에 얼마나 취약한가. 스크립트 이미 있다(`028-eval_NJ_dual`) |
| 7 | **2단계 학습**(`N1 m6000` → `path: 0.0` 400 it, 100마다 두 축) | **0.25 h** | 유일하게 "속도를 가진 정책에 정확도를 되돌리는" 방향. 실패해도 15분 |
| 8 | arm: `torque_margin` + `stop_ang_speed_threshold` 복원 (NH 기반, 레버 2개) | 3.6 h | 발목 `sat_share` 8~20 % → 1 % 미만 + `not_stopped` 19.6 % 감소면 확증 |
| 9 | arm: 구동 지연 `[2, 8]` 스텝 (NH 기반, 레버 1개) | 3.6 h | 벤더 기본값이고 우리에게 **없는** 축 |
| 10 | env: 벤더 T-N 곡선(`knee_point_velocity`) 도입 | 반나절 | 우리 sim 에 **아예 없는** 물리 |
| 11 | **N 배치 최고 셀을 로봇에 올린다** | 로봇 1세션 | ⛔ **이것이 유일하게 최우선 목표를 판정한다** |
| 12 | `effective_protocol` 에 `asset`·`control` 추가 + `round_table` 에 `per_category` | 1.5시간 | §3-12·§3-3 의 재발을 **구조적으로** 막는다 |

⛔ **11번이 없으면 1~10번은 전부 sim 점수다.**

### 6-4. 그만둘 것

- ⛔ **`constellation_weight`** — 두 계보에서 사전등록대로 기각. 최고 셀에서 이득 0, 대가만. **닫혔다.**
- ⛔ **`ND_dwell` duty 상향** — 방향 반대.
- ⛔ **"횡방향이 남은 병목"** — NH 에서 4.19 cm. `NZ` 한 점의 우연이었다(§3-5).
- ⛔ **`pause_gait_during_dwell` 을 "원인 확증"으로 취급하는 것** — 레버로는 쓸모 있다(47.7→32.0).
  **인과 주장으로는 자기 사전등록 기준에서 떨어졌다**(§3-3).
- ⛔ **"발목 roll 속도 초과 = 실기 리드"** — 채점 물리(armature 0)의 산물이다(§3-2a).
  **단, 위치 축과 T-N 곡선 축은 별개이고 살아 있다.**
- ⛔ **`feet_sep_min_m`(전체 롤아웃 최솟값)으로 교차를 판정하는 것** — n=1 극값.
  분율(`neg_share` 0.005~0.037 %)로 보면 세 칸 다 교차 없음이다.
- ⛔ **`armature` 값을 더 탐색하는 것** — `NQ_armvendor` 하나로 충분하다.
  §3-4 의 비대칭 때문에 **더 많은 armature arm 은 실기 값을 모르는 채 취약성만 산다.**
- ⛔ **시뮬 정확도를 더 깎는 것을 최우선으로 두는 것** — 정확도 3게이트를 통과하는
  체크포인트가 이미 8개다. **막힌 것은 정확도가 아니라 실기 루프다.**

---

## 7. 정정이 필요한 기존 문서 목록

| # | 파일 | 어느 주장이 틀렸나 | 무엇으로 바꿔야 하나 | 근거 |
|---|---|---|---|---|
| 1 | `CLAUDE.md` 상단 · `RETRACTIONS.md` **C5** | *"유효하게 남는 결론은 하나: 게이트 5 cm 를 통과하는 체크포인트가 없다"* | **8개가 정확도 3게이트를 통과한다.** 없는 것은 "정확도+속도+낙상 동시 만족" | §3-1 |
| 2 | `CLAUDE.md:110` · `AUDIT_MODE_20260808.md:22-25,46,55,65` | *"3건이 ⛔ 미수정이다"* | **`54e0885` 가 셋 다 닫았다.** 배포 세션이 없는 결함 때문에 로봇 시험을 미룬다 | §3-8 |
| 3 | `RETRACTIONS.md` **C30·C34** · `VENDOR_ACTUATOR_SPEC_K1.md §3` · `envs/K1/goal_pose.py` 주석 · `envs/K1/goal_pose_v7.py` 주석 | *"발목 roll 만 남는다 — 한계 초과 시간 27~55 %"* 를 **실기 리드**로 서술 | **채점 물리(armature 0)의 산물이다.** 같은 정책이 자산 armature 에서 over_share 0.000 | §3-2(a) |
| 4 | `RETRACTIONS.md` **C30** | *"`dof_vel_limits` 는 `_reward_dof_vel_limits` 한 곳에서만 쓰이는데 스케일이 0"* (결론은 맞고 **근거가 틀렸다**) | `_reward_dof_vel_margin` 에서도 쓰이고 베이스 스케일은 **−0.02**. 진짜 이유는 `_OFF_PROTECT` | §3-11 |
| 5 | `AUDIT_20260807_2300.md §2-1` (헤드라인 🔴1) | *"도착이 죽는 원인은 보행 시계다 — stand 2.3 대 turn 47.4"* | **turn 은 straight·lateral 과 구별되지 않는다.** 그리고 `NF` 는 **자기 사전등록 기준에서 떨어졌다**(turn 32.42, 기준 2~4) | §3-3 |
| 6 | `AUDIT_20260807_2300.md:110` (§2-4) | *"저장소 전체에서 이 키는 코드 두 줄이 전부다. 어떤 yaml 에도, 어떤 sweep 에도 없다"* | `archive/sweeps/hbatch/H{0..3}-codex.yaml:262` 에 **`true` 로 있다**. 그 task 는 `GoalPoseHBatch(GoalPoseV7)` 라 코드 경로 위다 | §3-3 |
| 7 | `ibatch.md §8-53·§8-54·§8-57` | *"남은 용의자는 횡방향 — 0.17 m ≈ `feet_distance_ref` 0.18"* | **`NZ` 한 점의 값이다. `NH` 는 4.19 cm.** 실제 잔차 구조는 접근축 위 **분산**(p10..p90 −13.8~+11.6) | §3-5 |
| 8 | `CLAUDE.md:91-93` | *"`export_model.py` 가 … export 가 안 된다. `install_policy.sh:145` 스모크도 하드코딩. **미수정**"* | `d51820d` 가 export 와 **서버측**을 고쳤다. **남은 것은 `install_policy.sh:282` 로봇측 하나**(줄 번호도 다르다) | U11 |
| 9 | `CLAUDE.md` 인계 블록 | *"[RETRACTIONS.md] 철회·정정 **24건**"* | **실제 35건**(C1~C35) | 직접 계수 |
| 10 | `DEPLOY_REQUESTS_FROM_TRAINING.md` **R1** | *"⬇ 우선순위 내림 — MuJoCo 가 대신 답했다"* | **답이 2026-08-05 부터 `real_walk.csv` 안에 있었다. 답은 "넘는다", 6관절 중 4개** | §3-9 |
| 11 | `deploy/utils/policy_goal_pose.py:200` docstring | *"2 Hz is 2 Hz in wall clock"* | **거짓이다.** 실기 실측 **1.5116 Hz**. `time_now` 는 LowState 카운터 × 0.002 | §3-9 |
| 12 | `deploy/configs/Goal_Pose_E0.yaml` 주석 | *"23-joint order … indices 11..22 = legs"* | `joint_cnt: 22`, `leg_dof_start: 10`, 배열도 10..21. **코드는 맞고 주석만 틀렸다** — C15(T1 레이아웃 혼동)가 나온 자리다 | 직접 확인 |
| 13 | `envs/K1/Goal_Pose_V7.yaml:555` | `target_fall_rate: 0.05` | **읽는 코드가 0개인 죽은 키**인데 베이스에 있어 모든 arm 에 상속된다. `de1cc82` 가 약속한 커리큘럼 수정이 안 들어왔다 | §5-3 |
| 14 | `ibatch.md §8-56` | *"자산 물리 짝비교는 ⛔ 하나도 없다"* / `feet_sep_min_m` 으로 교차 판정 | 짝비교 서술은 **`NE@자산` ↔ `NJ@자산` 한 쌍에만** 참이다. 그리고 min 은 **n=1 극값**이라 판정에 못 쓴다(분율은 0.005~0.037 %) | A §3·§2-G |

⚠️ **정정 방식 제안**: `RETRACTIONS.md` 에 **상태 열**(유효 / 무효화됨 / 근거교체)을 추가한다.
지금은 "추가만 되는 대장"이라 **항목 자체가 낡고**(F6), 이번 감사에서 C30·C34·C5 세 건이
그 상태다.

---

## 부록 A — 내가 원본에서 직접 재확인한 값 (조건 병기)

| # | 값 | 조건 | 출처 | 판정 |
|---|---|---|---|---|
| 1 | 정확도 3게이트 통과 = **19 리포트 / 8 체크포인트** | 120 s 기준, `gates` 필드 | `logs/eval_rounds/**/report.json` 121개 | **B 맞음, A 과소** |
| 2 | `NE_ctrl100` AnkleR `over_share` **0.5493 → 0.0000** | 동일 m6000·`forward_hold` 120 s, 채점 물리만 변경 | 〃 | **A 맞음** |
| 3 | I3b AnkleP `sat_share` **0.1982 / 0.2011** | `forward_hold` 120 s, boxfoot 재채점, model_75 | `torqueaudit` | **B 맞음** |
| 4 | `per_category` 4행 (N0/N1/NF/NH) | m6000, 공통 프로토콜 120 s | 〃 | **A·B 둘 다 정확** |
| 5 | `zerostress` 16셀 전수 | m6000, 120 s, ±3° | `zerostress` | **A·B 둘 다 정확** |
| 6 | URDF: 19.6660 vs 18.7142 kg, 발 0.494 vs 0.383, HipP ROM −3.0 vs −2.21 | boxfoot ↔ armsdown, XML 파싱 | `resources/K1/*.urdf` | **B 맞음** |
| 7 | `claim_check` 가 낙상 28,955 ↔ 0 에 **✅** | 동일 m6000·`forward_hold`·동일 두 sha | 서버 실행 | **B 맞음, 심각** |
| 8 | `de1cc82` 가 `goal_pose.py` 미변경, `zero_level_mean` **0건**, `target_fall_rate` 리더 **0개** | git + grep | 〃 | **B 맞음** |
| 9 | `54e0885` 가 3건 전부 코드에서 닫음 | `:1614-1615`, `:1831-1832`, `:725/729` | `deploy_goal_pose.py` | **A·B 맞음** |
| 10 | `sweeps/NH_zeroclock.yaml` 의 보호 벌칙 6개 = **0.0** | 실행된 학습 config | 서버 | **A 맞음** |
| 11 | 실기 시계 **1.5116 Hz**, 카운터 RMSE 0.00424 ↔ 벽시계 0.01990 | 91표본 / 2.379 s / n=1 | `real_walk.csv` | **C 맞음** |
| 12 | `tick_dt_s` med **0.02524 s**, tilt max **22.60°**(문턱 45°) | 〃 | 〃 | **C 맞음** |
| 13 | 발목 roll 위치 초과 **8/91**, **단 1건은 tilt 1.29°(최소)** | 〃, 한계 ±0.345 rad | 〃 | **C 부분 과장** |
| 14 | 실기 max\|τ\| HipP **37.82** / HipR **34.04** / AnkP **23.64** | 〃 | 〃 | **C 맞음** |
| 15 | 실기 발목 roll 속도 **22.18 / 26.49 rad/s**, 18.1 초과 6.6 % / 7.7 % | 〃 | 〃 | **신규(나)** |
| 16 | 벤더 K1 발목 = E4310 ratio(1,1): **38.3 N·m / v_knee 7.85 / v_max 17.59 / armature 0.0565** | 벤더 저장소 원본 | `booster_train/.../actuator.py`, `booster.py` | **B·C 맞음** |
| 17 | 벤더 T-N 포락선이 우리 클램프보다 낮은 실기 표본: AnkR **9.9 % / 18.7 %**, 최소 cap **0.0 N·m** | 91표본에 벤더 공식 적용 | 계산 | **신규(나)** |
| 18 | `round_table.py` 에 `per_category` **0회** · `make_eval_cfg.py` 에 `randomization`/`joint_zero`/`action_filter`/`armature` **각 0건**(`obs_delay` 만 1건) | grep | 〃 | **B 맞음**(내 보정: obs_delay 는 예외) |
| 19 | `zerostress` 가 저장소 md 전체에서 **0건** | grep | 〃 | **A 맞음** |
| 20 | RETRACTIONS **35건** (CLAUDE.md 는 24건이라고 적음) | 계수 | `RETRACTIONS.md` | **C 맞음** |

**확인 못 함(미확인으로 남긴다)**: H 배치 학습 프로세스 시작 시각(따라서
`pause_gait_during_dwell` 이 H 학습에서 실제로 살아 있었는지) · MuJoCo 필터 스윕 원본 ·
로봇 위 파일 목록 · 지면 컴플라이언스의 실제 영향 · 벤더 공개문서의 60 N·m 주장
(벤더 **DB** 는 무릎 E6416 = 112.0 으로 확인됨) · `R9`(hold 자세)의 MuJoCo 원본.
