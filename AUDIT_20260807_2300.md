# 전면 재검증 — 2026-08-07 23:00 (7축 독립 감사)

사용자 지시: *"방금의 방법론 및 의심 후보에 대해서 리스트업 하고 각 항목별로 에이전트를 붙여서
제대로 조사해서 문서화 해. 그리고 이 문제 리스트들이 애초에 맞는지도 검사해."*

⚠️ **이 문서는 앞선 두 문서를 대체한다** — `STATUS_20260807_1800.md`(내 것)와
그것을 감사한 병렬 세션의 결과. 둘 다 결론이 여러 개 뒤집혔다. 철회는
[RETRACTIONS.md](RETRACTIONS.md)에 번호로 관리한다.

---

## 0. 한 장 요약 — 무엇이 죽었나

**용의자 14개 중 5개가 죽었다. 죽은 것 중 3개는 이 세션이 몇 시간 전에 "가장 강한 리드"라고
보고한 것이다.** 그리고 **살아남은 것 중 가장 강한 둘은 아무도 의심하지 않던 것**이다.

| | 축 | 결론 |
|---|---|---|
| 🔴 **1** | **학습** | **도착이 죽는 원인은 보행 시계다.** 시계를 얼린 목표(`stand`)는 2.3 cm, 같은 변위 0인데 시계가 도는 목표(`turn`)는 **47.4 cm**. 로봇이 딛고 선 목표에서 47 cm를 걸어 나간 뒤 멈춘다 |
| 🔴 **2** | **학습** | **그 결손을 고치라고 있는 dwell 이 정확히 반대로 걸려 있다.** `pause_gait_during_dwell` 이 기본 `False` 이고 **어떤 config 에도 없다** → dwell 이 만드는 상태가 정책이 100 % 실패하는 바로 그 상태다 |
| 🔴 **3** | **학습** | **정확도 보상을 한 번도 안 건드렸다.** 40+ arm 중 `goal_reached` 등급화·`heading_near_goal`·`constellation_radius` 를 바꾼 arm **0개** |
| 🔴 **4** | **실기** | **`pub_hz` 는 이미 측정됐다 — 499.2 Hz.** 필터 서사 전체가 하드웨어에 없는 조건 위에 있었다 |
| 🔴 **5** | **실기** | **실기 발목 roll 이 URDF 하드 스톱 밖에서 돈다** (±0.55 대 ±0.345 rad). sim 이 금지한 영역이다 |
| ⚠️ **6** | **측정** | 생존 편향은 참이지만 **`I3b_stance10` 에만 크다**(결측 36.8 %). 선택기 승자는 안 바뀐다 |
| ⚠️ **7** | **코드** | 감사 수정 7건 중 **1건이 자기가 진단한 결함을 안 고쳤다**. `NA_histzero` 는 무효 — 재실행 |

---

## 1. 방법

Opus 에이전트 7개를 **읽기 전용**으로 독립 실행했다. 공통 규칙:

- 요약표·인계문서 숫자 인용 금지 → **원본(`report.json`·코드·로그·논문)에서 재도출**
- 숫자마다 **체크포인트 / 프로토콜 / 구간수 / 실행길이** 병기
- 확인 못 한 것은 "미확인" 명시, 추측과 사실 분리
- A4 는 **확인이 아니라 반증이 임무**임을 명시

| 에이전트 | 축 | 상세 보고서 |
|---|---|---|
| A1 | 생존 편향의 전파 범위 | `scratchpad/A1_survivorship.md` |
| A2 | 평행 발목 구동 경로 | `scratchpad/A2_parallel_ankle.md` |
| A3 | 도착 행동의 소멸 | `scratchpad/A3_arrival_collapse.md` |
| A4 | **리스트 자체를 공격**(적대적) | `scratchpad/A4_adversarial.md` |
| A5 | 감사 수정 7건 검증 | `scratchpad/A5_fix_verification.md` |
| A6 | 기록 고고학(미착지 처방) | `scratchpad/A6_record_archaeology.md` |
| A7 | 나머지 실기 후보 4개 | `scratchpad/A7_deploy_suspects.md` |

**교차 검증이 실제로 작동했다** — A4 와 A2 가 평행 발목을 독립적으로 기각했고,
A3 와 A6 가 정확도 레버 부재를 독립적으로 짚었다.

---

## 2. 🔴 학습 축 — 근본 원인이 특정됐다

### 2-1. 자연실험: 보행 시계

`goal_categories` 의 `stand` 와 `turn` 은 **둘 다 목표가 로봇의 현재 위치**다(dx=dy=0).
환경상 차이는 **`gait_frequency` 하나뿐** — stand 만 0.0 으로 시계를 얼린다
([`goal_pose.py:684`](htwk-gym/envs/K1/goal_pose.py:684)).

| | **stand** (시계 정지) | **turn** (시계 가동) |
|---|---|---|
| `N1_path` it100 → it6000 | **1.6 → 2.3 cm** | **8.3 → 47.4 cm** (`arrived_then_left` **100 %**) |
| `N0_ctrl` it6000 | 1.6 cm | 3.8 cm |

> **잃은 것은 목표 인지도 정지 능력도 아니다.
> "보행 시계가 도는 동안 제자리를 지키는 능력" 하나다.**

뒷받침 셋:
- **오차가 시작거리와 무관하다** — 목표가 25 cm 안이어도 45.5 cm, 2 m 밖이어도 49.4 cm.
  추종 실패가 아니라 **고정 크기 종단 표류**다
- `not_stopped: 0.02 %` — 멈추긴 한다. 엉뚱한 데서
- `N0` 대 `N1` 의 config diff 는 **`path: 0.0 → 0.35` 두 줄뿐**(동일 warm start, 동일 6000
  iteration, 동일 보상) → `model_6000` 대 `model_6000` 에서 **3.63 → 47.70 cm (13.1배)**,
  `never_arrived` 0.09 % → **62.0 %**

**기각된 대안 설명**: `feet_swing`(3.0)이 `goal_reached`(1.0)를 압도한다는 보상 산술 가설 —
세 arm 모두 swing 이 0.85~0.94 로 불변이고 `N0` 는 같은 보상으로 도착률 53 % 를 유지한다.

### 2-2. 인과 순서 — "트레이드오프"가 아니다

반감점 선형보간:

| arm | 정확도 열화 50 % | 속도 이득 50 % | 간격 |
|---|---:|---:|---:|
| `N1_path` | **it225** | it595 | **370 iteration** |
| `N2_pathgrid` | **it248** | it2572 | **2324 iteration** |

그리고 `N2` 는 붕괴 구간에서 **속도까지 1.05 → 0.29 m/s 로 같이 떨어진다**(>1.0 m/s 체류 0 %).
**두 arm 모두에서 교환이 아니라 순차적 소실이다.**

### 2-3. 왜 27 % 가 73 % 를 이기는가

**코드로 확정**: `history_steps: 1`, RNN 없음, 관측 54채널에 **모드 표시 채널 없음**,
`gait_frequency` 도 두 모드가 같은 분포. path carrot 간격 median 1.30 m ↔ waypoint 시작거리의
55.8 % 가 0.5 m 초과.
⇒ **겹치는 대역에서 정답이 정반대인데 기억 없는 정책은 하나만 고를 수 있다.**

`NA_histzero` 가 이 층을 이미 시험했다(path 0.35 유지 + `history_steps: 5`):
학습 신호로는 똑같이 붕괴하는데 **평가 실패 모드가 갈린다** — `closest_approach` 2.6 cm,
`never_arrived` 7.2 %, 그러나 **`not_stopped` 46.3 %**.
**NA 는 접근을 살리고 정지를 잃었다. N1 은 둘 다 잃었다.**
⚠️ 단일 레버 대조가 아니고(warm start·obs_delay·dof_tau 도 다르다), **§5-2 때문에 무효다.**

### 2-4. ⛔ dwell 이 반대로 걸려 있다

```
envs/K1/goal_pose_v7.py:338   if bool(p.get("pause_gait_during_dwell", False)):
envs/K1/goal_pose_v7.py:687   if bool(self.path_cfg.get("pause_gait_during_dwell", False)):
```

**저장소 전체에서 이 키는 코드 두 줄이 전부다.** 어떤 yaml 에도, 어떤 sweep 에도 없다.
⇒ 항상 `False` ⇒ **dwell 중에도 시계가 돈다.**

dwell 의 목적은 *"path 모드가 waypoint 와 같은 arrive-and-stop 상태로 끝나게 하는 것"*
(베이스 config 주석)인데, 그것이 만드는 상태는 **정책이 100 % 실패하는 `turn` 상태**이지
**한 번도 실패한 적 없는 `stand` 상태**가 아니다.

**결과 둘:**
- **`ND_dwell`(duty 22.4 % → 39.9 %)은 결손 상태를 더 만든다. 방향이 반대다.**
  지금 `queue/gpu0/030-ND_dwell.sh` 로 대기 중이다
- **`pause_gait_during_dwell: true` 가 값싼 판별 시험이다** — 레버 하나, 코드 수정 0

(dwell duty 자체는 결함이 아니었다. 카운터 직접 시뮬레이션: `N1`/`N2` = **22.35 %**로
의도 ≈20 % 와 일치. `a67763f` 후 `ND` = 39.91 %, 겹침 0 %. 커밋의 "영구 dwell 6 %" 는
**틀렸다** — 실제 0.00 %, 6.4 % 는 발화 겹침 확률이다.)

### 2-5. ⛔ 정확도 보상을 한 번도 안 건드렸다

진단이 *"정지 행동의 소멸"* 인데, **정지를 만드는 보상은 이 저장소에서 한 번도 바뀐 적이 없다.**

| 레버 | 코드 | 현재 값 | 상태 |
|---|---|---|---|
| `goal_reached` | `:1338` **하드 이진 계단** `(dist<0.1) & stopped` | `1.0` | 등급화 미착지 |
| `heading_near_goal` | `:1344` **구현돼 있다** | **`0.`** (V7/V8/V3/H0–H3 전부) | 미착지 |
| `constellation_radius` | `:1326` | **`1.0`** | 0.5–0.7 처방, **40+ arm 중 0개** |
| `goal_position`/`goal_heading`/`goal_stop` | 구현됨 | 전부 **`0.`** | — |
| `goal_reach_radius` | | **`0.1`** ↔ 게이트 **0.05** | 공 내부 기울기 미착지 |
| path dose | `_PATH_ON` 고정 | **10개 arm 전부 0.35** | 0.15 축 미착지 |
| `arrival_reward_mode` | `goal_pose_v7.py` 안에만 | `legacy` | 켜도 no-op(러닝 중 `goal_reached` 가 구조적으로 0) |

살아 있는 목표 보상은 **`constellation 3.5` + `goal_reached 1.0`** 둘뿐이다. 그리고
**10 cm 안에서 기울기가 0이다** — 10→5 cm 가 `+0.005/step`, 10 cm 문턱 통과가
`+1.0/step` = **200배**(`ebatch:298-301`). `MASTERPLAN` 시절부터 **X5** 로 기록된 충돌이다.

### 2-6. 기전 3층과 처방 대응

| 층 | 내용 | 처방 | 무엇을 건드리나 |
|---|---|---|---|
| **M1** | 관측 동일성(모드 구분 불가) | `history_steps: 5` | 직접 — NA 가 부분 시험, **재실행 필요** |
| **M2** | 신호 비대칭(X5, 200배 계단) | `goal_reached` 등급화 | 직접 — **3~6 cm 바닥은 풀지만 47 cm 는 못 푼다** |
| **M3** | 시계 결합 | **`pause_gait_during_dwell: true`** | **직접, 역방향** — 결손 영역 밖으로 옮긴다 |
| — | — | 2단계 학습(path→waypoint) | M1·M3 **우회**. 시간상수 비대칭(225 vs 595/2572)이 근거 |
| — | — | `ND_dwell` duty 상향 | ✗ **결손을 더 만든다** |
| — | — | path dose ↓ | 완화만. 27→85 % 초선형이라 예측 불가 |

⚠️ **2단계 학습은 미검증이다.** 역방향(waypoint→path 파괴)만 실측됐다. 판별 측정 하나:
**`N1 model_6000`에서 `path: 0.0`으로 400 iteration, 100마다 두 축 측정.**

---

## 3. 🔴 실기 축 — 가장 큰 서사가 닫혔다

### 3-1. `pub_hz` 는 이미 측정됐다 (499.2 Hz)

```python
# deploy/utils/timer.py  (전문)
class Timer:
    def __init__(self, config=None): self.counter = 0
    def tick_timer_if_sim(self):  self.counter += 1     # ← 이름과 달리 조건이 없다
    def get_time(self): return self.counter * self.config.time_step   # 벽시계가 아니다
```

`tick_timer_if_sim()` 의 호출 지점은 전 저장소에 **딱 하나** —
[`deploy_goal_pose.py:720`](htwk-gym/deploy/deploy_goal_pose.py:720), `_low_state_handler` 안이다.
발행 루프([`:1615`](htwk-gym/deploy/deploy_goal_pose.py:1615))가 그 `get_time()` 으로 데드라인을 잰다.

```
get_time() 은 LowState 1건당 정확히 +0.002
next_publish_time 은 발행 1회당 정확히 +0.002
⇒ LowState 1건당 발행 정확히 1회  ⇒  pub_hz ≡ LowState 도착률
```

**§8-41 이 관련 값을 이미 쟀다**(2026-08-05, 로봇 직접 접속):
LowState 도착 간격 median **2.00 ms** / p90 2.08 / p99 2.59 — **499.2 Hz, 14,074 표본 / 28.2 초.**
그 조건에서 τ = −0.002 / ln(0.8) = **8.96 ms** = 설계 의도 10 ms.

### ⛔ 정정 (A7) — 이 소거는 **너무 강했다.** R6 은 아직 안 닫혔다

내가 위에서 *"pub_hz ≡ 499.2 Hz, 필터 서사 사망"* 이라고 썼다. **틀렸다.** 이유 둘:

**① §8-41 은 다른 프로세스에서 쟀다.** 그 문서가 스스로 *"관절을 잡지 않았다 — LowState
구독만 했다"* 고 적는다. 즉 **가벼운 프로세스의 `f_LowState`** 이지 **실제 배포
프로세스의 `pub_hz`** 가 아니다.

**② GIL 문턱이 실제로 존재한다.** A7 이 서버에서 직접 재현했다
(`sys.setswitchinterval` 기본 5 ms):

| 연속 CPU 점유 파이썬 스레드 | `sleep(0.001)` 루프 천장 |
|---:|---:|
| 0 | **899 Hz** |
| 1 | **160 Hz** |
| 2 | **75 Hz** |
| 4 | **23 Hz** |

**필요한 것은 19 ms 의 *작업* 이 아니라 19 ms 의 *GIL 대기* 다. 50 Hz 는 도달 가능하다.**
감사(`FILTER_AUDIT` §2)와 내가 "파이썬이 2 ms 에 한 바퀴 못 돌 리 없다"로 기각한 논거가
**기전을 잘못 짚었다.**

⇒ 올바른 서술: **`pub_hz = min(f_LowState, GIL 천장)`.** §8-41 은 앞의 항만 쟀다.

### ⭐ 그리고 이것이 `base_walk` 반증에 새 답을 준다

루프·필터·두 sleep·counter `Timer` 까지 동일한데:

| | `deploy_base_walk.py` | `deploy_goal_pose.py` |
|---|---|---|
| `rclpy` | **import 조차 안 한다** | import + executor |
| 파이썬 스레드 | 발행 + 추론 | 발행 + 추론 + **rclpy executor 3개** (`FallMonitor`·`ModeMonitor` 는 `--goal-source fixed` 에서도 생성) |

**같은 코드가 아니라 더 무거운 프로세스다.** 사용자의 반증이 처음으로 기전 있는 답을 얻었다.
⚠️ 단 A7 은 deploy 코드에 **연속** 점유 스레드는 안 보인다고 했고, 간헐 점유는 무해하다
(3스레드 25 % duty → 568 Hz). **그래서 여전히 측정 문제다.**

### 그래서 R6 의 정의가 바뀐다

**"LowState 도착률" 이 아니라 "실제 배포 프로세스가 돌 때의 `pub_hz`" 를 재야 한다.**
`_pub_dt` 는 `time.monotonic()` 벽시계로 재므로 `--log-timing` 이 정확히 그 값을 찍는다.

```bash
cd ~/Workspace/deploy && python3 -u deploy_goal_pose.py --config Goal_Pose_E0.yaml \
  --goal-source fixed --goal "0,0,0" --log-timing /tmp/e0_pub.csv    # 30초, 서 있기
```

- **≥ 300 Hz** → 필터 가설 사망(이번엔 진짜로)
- **≤ 100 Hz** → 확정. 그리고 GIL 이 기전이므로 **수정은 필터가 아니라 executor 분리다**

⚠️ 그리고 그 값이 **실제 `gait_frequency` 도 정한다** — `policy_goal_pose.py:171` docstring
*"2 Hz is 2 Hz in wall clock"* 은 **거짓**이다. 시계가 counter 기반이므로
실제 보행 주파수 = `2.0 × f_LowState/500`.

### 그 밖에 A7 이 확인한 것

- **증거 기반이 거의 전부 n=1**: `goal_hold` seed 반복(`gh_*_s1/_s2`)이 **바이트 동일**.
  헤드라인 "66/68 낙상" 도 n=1. **비단조**도 남는다(lag20: 100 Hz=22, 150 Hz=**0**, 200 Hz=25)
- MuJoCo 검증 한계: `step = 1/filter_hz` 가 2 ms 격자로 양자화돼 **110/130/160 Hz 가 같은 실험**
  (해시 동일). 지터·catch-up·정지를 **하나도 안 봤다**
- 실측 상수: `sleep(0.001)` = **1.062 ms**, 무경합 천장 899 Hz, 추론 forward **0.034 ms**(무죄)
- `--rate-fixed-filter` 의 진짜 위험은 catch-up 이 아니라 **정지 쪽** — 50 ms 멈추면
  `alpha = 0.993` = 계단 명령. 필터 없는 것보다 거칠다
- `--filter-tau-ms` 기본 **10.0** 인데 계수 0.2 의 실제 tau 는 **8.96 ms** → A/B 가 no-op 이 아니다

**살아남는 것**: sim 에는 필터가 **아예 없고** 로봇에는 최소 τ≈9 ms 가 있다. `NC_actfilter` 유효.
⛔ 단 **상한 30 ms 는 `pub_hz ≥ 150 Hz` 만 덮는다.** 50 Hz 면 τ=89.6 ms 로 범위 밖 3배 —
*"pub_hz 가 얼마든 무관해진다"* 는 **성립하지 않는다.**

### 3-2. 평행 발목 — 기각

두 스크립트 **모두** `cmd_type = SERIAL` 로 발행한다
([`deploy/utils/command.py:19`](htwk-gym/deploy/utils/command.py:19) → `init_Cmd_T1`).
**직렬→평행 변환은 로봇이 한다.**

| 증거 | 내용 |
|---|---|
| SDK 헤더 | `LowCmd.h:94` `enum CmdType { PARALLEL, SERIAL }`. 그 관절의 SDK 이름은 `kCrankUpLeft/kCrankDownLeft` — "Ankle" 이 SDK 에 없다 |
| 벤더 예제 | 같은 물리 자세가 SERIAL `(−0.1, 0.0)` vs PARALLEL `(0.137, 0.125)` — 격차 0.237 rad |
| **Booster Gym 논문** | 배포 절이 *"joint velocity targets and feedforward torque are **set to zero**"* — **논문 표준이 지금 `goal_pose` 기본 경로다.** 추가 층을 얹은 쪽이 `base_walk` |
| 실기 `hold-diag` | 발목 추종오차 최대 **0.026 rad**. 변환이 없었다면 0.1–0.24 rad — **9–10배 작다** |

`--parallel-torque` 가 꺼진 이유("하중에 접히는 발목")의 1차 근거는 **커밋 메시지 하나**이고
**교란돼 있다** — 당시 코드가 호출자의 prepare kp **450** 대신 `common.stiffness` **50** 을 썼다.
처짐 0.88° → 7.9°, 9배. **게인 버그지 토크 경로의 성질이 아니다.**

⛔ **미확인**: 변환 코드가 클라이언트 `.a` 에 없다(17 MB 심볼 훑음 — DDS 접근자뿐).
**SERIAL 의 PD 가 관절공간인지 크랭크공간인지는 소스로 판정 불가.**

### 3-3. ⭐ 대신 나온 최강 리드 — 발목 roll 이 sim 한계 밖에서 돈다

`logs/mujoco/real_walk.csv`(§8-42 의 그 실기 로그) 직접 분석:

| | 값 |
|---|---|
| **실기 발목 roll 실측 범위** | **−0.578 ~ +0.537 rad** |
| **벤더 URDF / MJCF 하드 스톱** | **±0.345 rad** |
| 초과 표본 / 최대 초과 | 4–5 % / **0.233 rad (13.4°)** |
| 다른 관절 | hip roll 초과 0.015–0.033 rad 뿐 |

초과가 `t = 17.2–17.8 s`, tilt 22.6° 인 **붕괴 국면에 몰려 있다.**

해석 둘 중 하나:
- **(a) URDF 한계가 틀렸다** → sim 이 과구속. 넓히고 재학습
- **(b) `motor_state_serial` 이 URDF 관절각과 다른 양이다** → 펌웨어의 "가상 직렬 관절"
  정의가 우리 URDF 와 다르다. 그러면 **`obs[18:30]` 의 발목 두 채널이 학습과 다른 것을 싣는다**

**판별: 무부하 3분.** 발목 roll 을 ±25°(0.436 rad)까지 20초 램프.
0.436 까지 추종 + τ<8 N·m → **(a)**. 0.345 부근 포화 → **(b)**.

### 3-4. `base_walk` 반증은 두 겹으로 죽었다

| | `Base_Walk.yaml` | `Goal_Pose_E0.yaml` |
|---|---|---|
| stiffness 항목 수 | **23** | **22** |
| `parallel_mech_indexes` | **`[15,16,21,22]`** ← **T1 레이아웃** | `[14,15,20,21]` |
| 다리 Hip/Knee kp / kd | **200 / 5** | **100 / 2** |
| **ankle kd** | **3** (ζ 0.93) | **1** (ζ 0.31) |
| `gait_frequency` | 1.0 | 2.0 |
| 필터·루프·dt·두 번째 sleep | **동일** | 동일 |

⇒ **`Base_Walk.yaml` 은 이 로봇의 config 가 아니다.** *"base_walk 는 같은 필터·같은 루프로
걷는다"* 의 그 base_walk 가 **이 코드일 수 없다** — 벤더 내장 보행 모드다.
`FILTER_AUDIT-non-codex.md` §4 전체가 **존재하지 않는 대조**에 기대고 있었다.

### 3-5. ⛔ 안전 — 미해결 경로

**복구 재진입에서 발행 스레드가 진입 램프를 stale 자세로 덮어써 최대 96° 사각파가 모터로 나간다.**
§8-49 가 기록한 "펌웨어 관절 보호(빨간불) 4회" 의 잔여 경로다.
`--parallel-torque` A/B 시 `kp=0` 잔류로 같은 경로가 재현된다 →
**A/B 는 `--log-timing` 필수, 낙상 복구가 발동한 실행은 무효 처리.**

### 3-6. 진단 로그가 증거가 아니었다

[`deploy_goal_pose.py:684`](htwk-gym/deploy/deploy_goal_pose.py:684) 가 `sorted(observed)` 가 아니라
`sorted(configured)` 를 찍는다. 가드가 `if observed and configured and ...` 라
**`observed` 가 비어 있어도 `[joint-layout] … matches hardware` 가 똑같이 찍힌다.**

---

## 4. ⚠️ 측정 신뢰성 — 생존 편향

`eval_goal_pose.py:1750`:

```python
completed = changed & ~done      # done = 낙상 | 에피소드 타임아웃
```

낙상 시 진행 중이던 구간이 **통째로 삭제된다**(`seg["pos_err"]` append 안 됨, segments.csv 에도 없음).
`attempts = n + falls` 이므로 **결측률 = `fall_rate_per_attempt` 그 자체**다.
이 이중 의미가 코드·문서 어디에도 없다.

| arm (동일 프로토콜 `sweeps/N0_ctrl.yaml`, 120 s, 256 env) | 결측률 | raw med | **penalized med** |
|---|---:|---:|---:|
| **`I3b_stance10` best(75)** ← 배포 계보 | **36.77 %** | 3.10 | **4.74** → 순위 **3위 → 8위** |
| `N8_pathdelay` best(100) | 8.86 % | 4.39 | 4.66 |
| `N2_pathgrid` best(100) | 3.54 % | 5.74 | 5.89 |
| 나머지 9개 | ≤0.35 % | — | 변화 ≤0.02 cm |

**난이도 이동의 기전 정정**: *"낙상하면 목표 근처에서 리셋돼 구간이 쉬워진다"* 는 **틀렸다**.
목표는 로봇 로컬 프레임에서 뽑히므로 거리 분포가 불변이고 구간 duration 도 같다(5.98 vs 5.88–6.00 s).
이동은 전적으로 **어떤 구간이 살아남는가** 때문이다 — I3b 완주율:
stand 99.1 % / turn 94.0 % / lateral 64.7 % / straight 53.4 % / **combined 37.9 %**.
**시도 기준** 유형 구성은 12개 arm 전부 config 와 1.5 pp 이내 일치한다(결정적 검증).

**선택기는 안 바뀐다** — "모든 `best.pth` 가 오염" 은 **기각**. 12개 selection.json 을 보정
재계산해 승자가 바뀌는 것은 `I3b`(75→100, 둘 다 파탄) 하나뿐이다. 다만
`select_best_checkpoint.py:116` 의 `_SELECTION_IGNORED_GATES = ("falls",)` 가 낙상을 순위에서
**명시적으로 제거**하고, 그 근거 주석의 전제("2300구간에 1회 = 노이즈")는 깨져 있다
(N8 승자 74/710 = 9.4 %, Poisson 95 % [57,91]).

**두 번째 편향(새로 발견)**: `speed_valid`(`:1174`) 가 리셋 직후 정지 구간을 제외한다.
I3b 는 전체 스텝의 **17.12 %** 제외 → `1.12 m/s` 가 실은 **≈0.98**. 보행 축 순위도 3위→6위권.

**문헌 대조**: 낙상 시 에피소드 종료 + 전체 rollout 분모 실패 계상이 표준. 생존 프레임
조건부 오차 자체는 관행이나 **항상 success rate 와 병기하고 그것으로 정렬한다.**
우리가 벗어난 지점 셋: ① 조건부 오차 하나로 순위 ② 선택기가 낙상 제거
③ `fall_rate_per_attempt` 이 곧 결측률이라는 사실이 어디에도 없음.

---

## 5. ⚠️ 감사 수정 7건 검증

| # | 수정 | 판정 |
|---|---|---|
| 1 | 대칭손실 맵 확장 (`2076c49`) | **통과** — 7개 폭에서 involution·전단사·부호·비혼합 **96/96** |
| 2 | **이력/지연 리셋** (`2076c49`) | ⛔ **부분 실패** — 아래 |
| 3 | ND_dwell duty (`a67763f`) | **통과**(수치) / 근거 문장 둘은 틀림 |
| 4 | warm start 고정 (`0c2a91a`) | 큐 **통과**(`best.pth` 0건) / `stage_obs_arms.sh` **부분 실패** |
| 5 | `eval_round.sh` PIPESTATUS | **부분** — 1단계만. **3·4단계(보행 축)는 검사 자체가 없다** |
| 6 | 카드별 유휴 / 워커 v2 / 유출 정리 | **통과** (21:45 실제 승격 발화, 서버 고아 0) |
| 7 | 기존 arm(54/h1) 회귀 | **통과** — 맵 비트 동일, prime 완전 no-op, RNG 불변 |

### 5-1. ⛔ 같은 커밋이 진단한 결함을 그 커밋이 안 고쳤다

[`goal_pose.py:746`](htwk-gym/envs/K1/goal_pose.py:746) 의 `self._obs_prime_ids = env_ids` 가
여전히 `_resample_goals` 안에 있고 `env_ids` 는 **재추첨 집합**이다.
`_reset_idx` 가 `episode_length_buf` 와 `cmd_resample_time` 을 둘 다 0 으로 놓으므로
리셋 env 는 그 **진부분집합**이다.

이벤트 스케줄 재현: **prime 의 81.9 % 가 목표 경계**(에피소드당 4.52회 = 걷는 중 약 6.6초마다
100 ms 이력이 한 프레임 복제로 평탄화). **커밋 주석이 "결함 (1)" 로 적어 놓은 숫자 그대로다.**
고친 것은 값(직전 프레임 → 새 프레임)뿐이다.

### 5-2. `NA_histzero` 는 무효 — 재실행

옛 맵에서 270폭의 **82.6 % 가 항등+부호 +1** 인데 `symmetry_coef 0.5` 로 6000 iteration 완주했다.
게다가 이력이 6.6초마다 평탄화됐다. **잰 것이 "이력+영점" 이 아니라
"이력 + 깨진 대칭손실 + 평탄화된 이력" 이다.**

| arm | 재실행? |
|---|---|
| **`NA_histzero`** | **무효, 재실행** |
| `N4_hist` | rc1 로 죽음. `queue/held/`. **§5-1 수정 전에 풀면 같은 값을 잰다** |
| `N5_tau` / `N6_foot` | 이력 안 켬 → §5-1 무관. 바로 풀어도 됨 |
| `N8_pathdelay` | 영향 ≤40 ms. 낮은 우선순위 |
| `NZ`/`NC`/`NE`/`N7`/`ND`/`N0`/`N1`/`N2` | **재실행 불필요** (맵 비트 동일, prime 완전 no-op) |

### 5-3. 운영 잠재 결함

`gpu_worker.sh` big 레인과 구 `gpu_queue.sh` 가 **같은 `queue/gpu$N`** 을 읽고 flock 이
v1 을 배제하지 못한다. 지금은 v1×2 가 big, v2×2 가 `LANE=small` 이라 안 겹치지만,
**문서화된 기본 호출 `tools/gpu_worker.sh 0` 이면 즉시 겹친다.**
그리고 `idle_watch.sh`·`round_status.sh` 는 `gpu_queue.sh` 만 세고,
`autopilot.sh` 정체 복구도 `gpu_queue.sh` 만 재기동한다.
`gpu_worker.sh` 주석의 *"autopilot 이 `.owner` 를 `kill -0` 로 확인한다"* 는 **거짓** —
autopilot 은 `*.running` 을 조건 없이 되돌린다(오탐 시 같은 학습 중복 실행).

---

## 6. 기록 — 미착지 처방과 재발

### 6-1. 선언됐지만 코드에 없는 것 (전부 코드에서 직접 확인)

§2-5 의 정확도 레버 6개 외에:

| 처방 | 코드 확인 |
|---|---|
| 외력 결함 ①수직성분 ②작용점 ③토크-힘 상관 | `goal_pose_v7.py:990-994` — **z 성분 대입하는 줄 자체가 없음**, 작용점 `base_indice` 상수, 토크가 힘과 **독립 난수**(`τ=r×F` 아님) |
| 배터리 / `torque_limits` 랜덤화 | `randomization:` 에 키 자체가 없음. 사용자 직접 질문 항목 |
| 팔 동적 후방 스윙 | 코드는 살아 있음(`:834-912`). 켠 arm 은 `G3_full` **하나뿐**(07-28 실패), 이후 전부 off |
| `arrival_hold` | 저장소 grep **0건** |
| MaxSpeed 상한 단독 탐색 | arm 정의만, sweeps yaml 도 run 도 없음 |
| 사용자 영상 피드백 | **4건 미답변**(mp2:837, mp2:844, gbatch:477, gbatch:478) |

### 6-2. ⛔ 반대로 — 문서가 "미반영"이라 적었는데 실제로는 들어가 있는 것

**다음 세션이 또 고치려 들 위험이 있다.**

- **케이던스-속도 커플링**: `ibatch.md:317` 이 "미이행"이라 적었지만 **구현돼 있다**
  (`goal_pose_v7.py:706-738`, `I1c`/`I1d`/`L2_cadence` 세 arm 이 켬).
  `C-3 #2` 가 "미반영"이라 불린 진짜 원인은 **`L2_cadence` 가 796 iteration 에서 죽고
  한 번도 채점되지 않은 것**이다
- `ND_dwell` duty, `006-pareto_fine_*` — 둘 다 이미 처리됨(CLAUDE.md 갱신함)

### 6-3. 재발 항목과 초발

| 항목 | 초발 | 재발 |
|---|---|---|
| **평행 발목** | `ibatch.md:3231` (08-05 §8-39) | **3회, 3회 다 "정량화 안 됨"으로 종료.** 이번에 기각 |
| **생존 편향** | `hbatch-codex.md:296` (07-30, 낙상 분모) | 낙상 축만 고쳐졌고 **정확도 축은 그때 안 고쳤다** |
| **시간순 최신 run 선택** | `ibatch.md:4013` (08-07) | `eval_round.sh` 만 고침. **`export_model.py:38,44,50` 에 살아 있음** |
| **`v7_extras` 무효** | `gbatch.md:74` (07-28, 배관 없음) | RETRACTIONS C11 이 **같은 필드를 두 번째로 무효화** |
| **검증 도구가 먼저 틀린다** | — | `STATUS:588` 이 "세 번째" 라 기록. 이번 §5-1 로 **네 번째** |

### 6-4. 한 번도 안 돈 진단 도구

| 도구 | 열려 있는 질문 |
|---|---|
| `probe_joint_zero.py` | **R4.** `NZ`/`N9`/`NA`/`M8` 네 arm 의 폭이 전부 추측 |
| `probe_ankle_mapping.py` | §3-3 의 (a)/(b) 를 가르는 수단 |
| `probe_com_from_ankle.py` | FK CoM 무효 판정 뒤 문서가 "이걸로만 닫힌다"고 명시한 경로 |
| `audit_env.py` | **`cadence_coupling`/`joint_zero`/`action_filter_tau`/`obs_delay_steps` 네 신규 축이 이 도구 이후 들어왔고 아무도 안 돌렸다** |
| `audit_urdf.py` | URDF **9종 중 2종이 틀린 것으로 판명된** 저장소인데 현재 자산은 수동 검증만 |

### 6-5. 미채점 run (⚠️ `round_status.sh` 목록은 오탐 4 + 누락 5)

`M8_jointcal2`(±2° 영점 유일 데이터) · `M9_anklegain`(실기 최상위 증상의 유일한 학습측 arm) ·
**`L2_cadence`(케이던스 커플링의 유일한 장기 실행, 796 it 에서 사망)** ·
`L4_settle` · `M4_scratch_phasefree`

### 6-6. ⛔ 유령 인용

`ibatch.md:3893-3894` 가 *"Hip_Roll 에 −5° 는 실기 증상을 재현했고 +5° 는 낙상 0 — 근거는 §8-43"*
이라 쓴다. **§8-43 전문에 그 실험이 없다.** 저장소 전체 grep 에서 이 주장의 출현은 그 두 줄
자신뿐이고, §8-42 레버표는 오히려 *"관절 영점 … 교차 재현 실패"* 라 적는다.
그 문장이 `joint_zero.modes.anti_mirror = 0.20` 의 **유일한 근거**이자
지금 plan 에 있는 `N9_zerostruct` 의 설계 근거다.

---

## 7. 살아남은 용의자와 각각의 판별 측정

| 용의자 | 상태 | 판별 측정 | 비용 |
|---|---|---|---|
| **보행 시계 결합 (M3)** | 🔴 기전 특정됨 | `pause_gait_during_dwell: true` 1개 arm | 3.6 h GPU |
| **정확도 신호 비대칭 (M2)** | 🔴 미시험 | `goal_reached` 등급화 1개 arm | 3.6 h GPU |
| **관측 동일성 (M1)** | 🔴 부분 시험(무효) | `NA_histzero` 재실행 | 3.6 h GPU |
| 2단계 학습 | 미검증 | `N1 model_6000` 에서 `path: 0.0` 400 it | **0.25 h** |
| **발목 roll 한계 (URDF ±0.345 vs 실기 ±0.55)** | 🔴 최강 실기 리드 | 무부하 발목 roll ±25° 램프 20초 | **3분, 로봇** |
| **`pub_hz` (GIL 기전으로 부활)** | 🔴 **미측정** — §8-41 은 다른 프로세스 | **실제 배포 프로세스에서 `--log-timing` 30초** | **30초, 로봇** |
| PD 게인 100/2, **ankle kd 1 vs 3 (ζ 0.31 vs 0.93)** | 살아 있음, 두 문서가 "미검증" 기록 | 실기 A/B | 로봇 |
| `b`~`r` 자세 합 | **(C) 시험 불가** — t=0.05 s 에 이미 10.3°(램프 전), 수정이 합+무릎굴곡 동시 변경 | 재설계 필요 | — |
| 복구 96° 사각파 | 🔴 **안전** | 코드 수정 | — |
| **`NC_actfilter` 를 필터 끄고 채점 중** | 🔴 arm 의 존재 이유가 안 재진다 | 고정 `action_filter_tau` 축 평가 | GPU 소 |

**죽은 것**: 평행 발목 변환 · `base_walk` 반증 · 두 번째 `sleep` · **`gait_frequency` X3
"물리적 불가능"** · **hip roll 포화(원인 아님)** · Hip_Roll 폭주 · 질량/관성 · 토크 상한 ·
떨림 · 발 관성 · 관측 지연(전송)
**보류**: 관절 영점(§8) · 필터 루프 속도(기전 교체, 미측정)

### 7-1. A7 이 추가로 죽인 것 둘

**`gait_frequency` X3 "1.5 m/s 는 물리적으로 불가능" — 기각.**
산술은 맞지만 상한이 틀렸다. URDF 직접 계산: 다리 길이는 `base_height_target 0.52` 가 아니라
**0.4607 m**(hip 축→발바닥). 0.5–0.6× 규칙 자체가 저장소에 출처가 없고, Hip_Pitch 가동범위
±2.21 rad 이라 기구학이 구속이 아니다.
**`N2` 가 실측으로 반증한다**: `single_support_s` median **0.24 s** × `body_speed` **1.54 m/s**
= 보폭 **0.370 m**(p90 0.425). 이중지지 5 %, flight 1.4 % — 가벼운 조깅.
15,360 env·s 에 낙상 8.
⚠️ `cadence_coupling` 은 **두 번 다 레버가 아닌 이유로 죽었다**(I1c=200 iteration 예산,
L2=틀린 `--ref 0.0253`). **공정 판정이 없다.**

**hip roll 포화 — 기각 (원인이 아니라 결과).**
- 4.8 / 2.7 / 7.4 는 원본에서 확인된다. **그러나 낙상 0 인 500 Hz 런에도 2.7/4.8 이 있다**
- `goal_hold` 에서는 **Hip_Yaw 가 1위(8.9 %)**, HipR 은 0 %대. "hip roll 만" 은 `goal_reach`
  하나만 본 결과다
- ⛔ **지표 자체에 구멍**: `over_limit_pct` 는 URDF 기준인데 클램프는 MJCF 기준이라
  **`URDF==MJCF` 인 Ankle_Pitch 는 구조적으로 0 %** 로 나온다. 클램프를 풀면
  **AnkP 34–41 N·m / over 17–26 %** — **데이터셋 최대 포화가 지표에 안 보였다**
- Isaac 은 답할 수 없다: `goal_pose_v7.py:781-782` 가 **관절축을 접는다**(max/mean)

**⭐ 그리고 §6-6 의 유령 인용에 실체가 있을 수 있다**: A7 이 `sig/hiprbias-5`(영점 −5°) =
**6 낙상**, `+5` = **0 낙상** 을 원본에서 찾았다. `ibatch:3893` 의 주장은 **참인데 §8-43 이
아니라 이 스윕이 출처다.** 그리고 함의가 다르다 — *"아픈 건 토크가 아니라 **영점 부호
비대칭**"* 이고, 이것은 **§8 의 관절 영점 보류를 강화한다.**

---

## 8. ⚠️ A4 가 반박한 나의 소거 — 관절 영점

내가 사용자 관측 + 로그 한 줄로 소거했다. A4 가 **1위 오분류**로 판정했다:

1. **다른 로봇이다** — `ibatch.md:3316-3319` 가 *"이 로봇은 세 걸음 증상이 난 그 로봇이 아니다"*
   라고 명시한다. 영점은 개체별 값이라 가장 이식성이 낮다
2. **균형 제어기가 켜져 있었다** — 폐루프 균형기는 상수 오프셋을 재자세로 흡수한다.
   tilt 1.6° 는 "균형기가 동작한다" 를 증명할 뿐이고, 문제 구간(`b`~`r`)은 **균형 주체가 없는
   regime** 이다
3. **§8-46 제목 자체가 "관절 영점 오차를 한 번도 시뮬레이션한 적이 없다"** — 두 파라미터가
   독립 추첨이라 학습한 것은 다른 고장이었다

**⇒ 소거를 "보류" 로 내린다.** 다만 §7 의 우선순위는 안 바뀐다 — 위 셋 중 어느 것도
"영점이 지금 문제다" 를 세우지는 않는다. `probe_joint_zero.py` 를 돌려 닫는다.

---

## 9. 이번 라운드에 죽은 나의 주장 (RETRACTIONS 추가분 → C15–C20)

| # | 주장 | 왜 틀렸나 |
|---|---|---|
| C15 | *"평행 발목 변환 부재가 base_walk 반증을 설명하는 유일한 구조적 차이"* | 둘 다 `cmd_type=SERIAL`, 로봇이 변환. 그리고 게인 2배·ankle kd 3배·T1 인덱스 등 차이가 10개 넘는다 |
| C16 | *"발행 루프에 두 번째 `sleep` 이 있어 발행률 상한이 생긴다"* | 루프는 자유 실행이 아니라 LowState 카운터에 묶여 있다. sleep 은 메시지 사이 대기일 뿐 |
| C17 | *"`[joint-layout] matches hardware` 로그가 SDK 관절공간 변환을 증명한다"* | `:684` 가 `observed` 가 아니라 `configured` 를 찍는다. 빈 집합에서도 같은 줄이 나온다 |
| C18 | *"낙상하면 목표 근처에서 리셋돼 구간이 쉬워진다"* | 목표는 로컬 프레임 추첨이라 거리 분포 불변. 이동은 완주율 차이(combined 37.9 %) 때문 |
| C19 | *"`N0_ctrl model_6000` 정확도가 측정된 적 없다"* | 그 뒤 측정됨 — **3.63 cm** (120 s, 4,621구간, 낙상 5) |
| C20 | *"관절 영점은 소거됐다"* | §8. **보류**로 내린다 |
| C21 | ⛔ *"`pub_hz ≡ 499.2 Hz` 라 필터 서사는 죽었다"* — **이 문서가 몇 시간 전에 쓴 것** | §8-41 은 **구독 전용 프로세스**의 `f_LowState` 를 쟀다. 실제 배포 프로세스에는 rclpy executor 3개가 더 있고, GIL 연속 점유 1스레드면 천장이 **899→160 Hz** 다(서버 실측). `pub_hz = min(f_LowState, GIL 천장)` 이고 **뒤 항은 한 번도 안 쟀다** |
| C22 | *"`gait_frequency` 2.0 은 물리적으로 불가능한 보폭을 요구한다"*(X3) | 다리 길이가 0.52 가 아니라 **0.4607 m**, 0.5–0.6× 규칙은 출처 없음. `N2` 실측 보폭 **0.370 m** 로 반증 |
| C23 | *"hip roll 만 토크 포화한다"* | `goal_hold` 에서는 **Hip_Yaw 가 1위**. 낙상 0 런에도 같은 값. 그리고 지표가 URDF 기준이라 **AnkP 는 구조적으로 0 %** — 클램프 풀면 AnkP 가 최대(over 17–26 %) |

---

## 10. 방법론 — 왜 같은 실수가 반복되나

이번 라운드에서 죽은 주장 6개와 앞선 14개를 나란히 놓으면 **모양이 셋뿐**이다:

1. **조건을 확인하기 전에 결론을 말했다** — 14건 중 대부분. `claim_check.py` 가 이걸 막는다
2. **한 소스의 한 줄을 증거로 썼다** — C17(로그 한 줄), C20(관측 1회), 유령 인용(§6-6),
   `v7_extras`(n=1). **표본 1은 증거가 아니다**
3. **이미 있는 측정을 못 찾았다** — `pub_hz`(§8-41 이 이틀 전에 쟀다),
   케이던스 커플링(구현돼 있었다), `L2_cadence`(돌았는데 미채점)

**3번이 가장 비싸다.** R6 하나에 세 문서의 헤드라인이 걸려 있었고, 답은 저장소 안에 있었다.

**그래서 규칙 하나를 추가한다**:
> **"이 값은 측정된 적이 없다" 를 쓰기 전에, 그 양의 이름으로 `ibatch.md` 전체와
> `logs/**/report.json` 을 grep 한다.** 없으면 그때 "미측정" 이라고 쓴다.
