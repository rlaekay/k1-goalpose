# sim2real 트래커 — 목표까지 무엇이 남았나

작성 2026-08-09 16:4x. **이 문서 하나가 "왜 로봇이 못 걷나"의 현재 지도다.**
판정이 바뀌면 여기를 고친다. 세부 논쟁은 `ibatch.md`, 철회는 `RETRACTIONS.md`.

## 0. 목표와 현재 거리

> **목표: 로봇이 아무도 안 잡은 채로 4발자국 이상 걷는다.**

| | 상태 |
|---|---|
| 달성 | **없음** |
| 최고 기록 | 2.96 s / 시계 4.7주기 — ⛔ **사용자가 잡고 있었다** |
| 무지지 연속 보행 | **한 번도 측정된 적 없다** |
| 배포 스택 | 오늘 대부분 고쳐졌고 **아직 그 위에서 걸어본 적이 없다** |

⭐ **핵심 사실 하나**: 지난 며칠 우리가 고친 것은 거의 전부 **배포 루프의 결함**이지
sim2real 격차 자체가 아니다. 격차 본체(물리 자산)는 **손도 안 댔다**(§2-B).

---

## 1. 변수 트래킹

### A. 배포 루프 — 격차를 **재게** 만드는 것 (거의 닫힘)

| 변수 | 설계 | 실측(수정 전) | 상태 | 4발자국을 막았나 |
|---|---|---|---|---|
| 정책률 | 50 Hz | 38.4 → **50.4** | ✅ 수정 | **막았다** (학습 대비 −23 %) |
| 관측 신선도 | 매 tick | **50 중 18~22만 새 표본**, 최장 218 ms 개루프 | ✅ 수정 | **막았다** (2 Hz 반주기 개루프) |
| gait 시계 | 벽시계 2.0 Hz | LowState 카운터 → **1.51~1.58 Hz**(학습대역 [1.8,2.4] **밖**) | ✅ 수정(fresh 빌드) | **막았다 — 유일하게 살아남은 인과축** |
| `b`~`r` 자세 | 개루프 직립 | tilt **4.9~13.8°** (RL 자세 합 −0.05) | ✅ 수정 → **1.4°** | **막았다** |
| 복구 재진입 게인 | 학습값 | 첫 낙상 뒤 영구 **5배** | ✅ 수정 | 막았다(2회차 이후) |
| 발행 EMA | tau 10 ms | 율 의존 → 실효 fc 6.57 Hz | ✅ 율 고정 | ⚠️ **tau 값 미결**(10 대 24.2) |
| 발목 roll 관측 | 실측값 | **보행 틱 44 %가 물리적으로 불가능한 표본** | ✅ 게이트 | ⚠️ 원인 아님(§8-70), 곡선만 이동 |
| InitChannel | — | GIL 데드락 | ✅ 워치독 | 시험 자체를 막았다 |

⇒ **A 는 사실상 닫혔다. 남은 것은 tau A/B 하나.**

### B. 물리 자산 — **진짜 sim2real 격차. 여기가 비어 있다**

⚠️ **자산이 둘이다. 섞어 읽지 마라**(분석 세션 1차자료 감사): 배포 계보 I3b =
`K1_locomotion_armsdown.urdf`, N 배치 = `K1_robot_boxfoot.urdf`.
⭐ **"인과 증거" 열이 이 표의 핵심이다** — 자산 차이는 확실하지만 **그것이 실기 붕괴의
원인이라는 측정은 거의 없다.** 이 열이 없으면 "차이가 크다"가 "원인이다"로 조용히
승격된다(오늘 armature 에서 실제로 그럴 뻔했다).

| 변수 | 학습 sim | 실물/벤더 | 올바른 값 arm | **인과 증거** |
|---|---:|---:|---|---|
| armature 힙 | **0** (양 자산) | 0.0283~0.0478 | `NQ` 채점 중 | ⚠️ MJC 최대 레버였으나 **본인 철회**(채점물리 교락) |
| armature 무릎 | **0** | **0.0956** | `NQ` 채점 중 | ⚠️ 위와 같음 |
| armature 발목 | 0 (I3b) / 0.05 (NP) | 0.0565 | `NQ` 채점 중 | ⛔ **NP(발목만)는 실기 서기에서 최악**(각속도 4.90배) |
| ⛔ **effort 6관절 전부** | 벤더의 **1/1.9~1/2.8**, **두 URDF 다** | Hip_P 68 · Hip_R 76 · Knee **112** · 나머지 38.3 | ⛔ **없음** | ⛔ **미측정.** 단 `goal_pose.py:115`→`:944` 가 **하드 클램프** ⇒ 정책이 **굶긴 토크로 최적화됨** |
| 총 질량 | **18.714**(armsdown) | 19.666 | ✅ **boxfoot 이 이미 19.666** | ⛔ 미측정 |
| 발 질량 | **0.383**(armsdown) | 0.494 | ✅ **boxfoot 이 이미 0.494** | ⛔ 미측정 |
| ⛔ **Hip_Pitch ROM** | boxfoot **[−3.0, 2.21]** 대 armsdown **[−2.21, 2.21]** | — | — | ⛔ **미문서화·미측정.** 두 계보가 **다른 가동범위**에서 학습 |
| Hip_Pitch 속도한계 | boxfoot **7.1**(0.48×) / armsdown 18(1.23×) | **14.66** | ⛔ 없음 | ⛔ 미측정 |
| 발목 roll ROM | ±0.345 | **±0.40**(손 실측) | ⛔ 없음 | ⚠️ 실기 스톱 밖 표본은 §8-69 로 **계측 결함**으로 판명 |
| Hip_Pitch z | +15 mm (armsdown) | 실기 | boxfoot ✅ | ⛔ 미측정 |
| **정책 게인 kp/kd** | Hip/Knee 100, Ankle 50 / 2·2·1 | **동일** | ✅ **격차 없음** | — |
| 게인 DR | ±5 % | — | 좁다 | ⛔ 미측정 |
| 관절 영점 | **0°** (I3b) | 반복 측정 **L−R chain 2.31°** | NH/NP/NQ 가 커리큘럼 | ⛔ 미측정(R7 레인) |

⛔ **"인과 증거" 열이 사실상 전부 비어 있다. 이것이 B 블록의 진짜 상태다** —
자산 사실은 확실하지만 **어느 것도 실기 붕괴와 인과로 연결된 적이 없다.**

⇒ ⛔⛔ **거의 모든 물리 파라미터가 틀렸고, 지금 로봇에 올라가 있는 정책(I3b)은
그 중 무엇도 교정된 적 없는 자산 위에서 학습됐다.**

✅ **게인은 예외다 — 확인했다**(`Goal_Pose_V7.yaml:148-149` 대 `Goal_Pose_E0.yaml` 다리,
둘 다 Hip/Knee 100 · Ankle 50 · kd 2/2/1). ⛔ **벤더의 `kp = armature·(2π·4Hz)²` 를
학습에 적용하지 마라** — 그건 벤더 컨트롤러의 튜닝 레시피이지 우리 로봇 게인의 서술이
아니고, 적용하면 학습이 Hip 30.2/Knee 60.4/Ankle 35.7 로 가는데 **로봇은 계속 100/50 을
받으므로 없던 격차를 만든다**(Teacher 지적, 내 독립 확인). ⇒ **격차는 armature 하나다.**

⭐ **그런데 게인이 같아도 그 게인이 작용하는 플랜트가 다르다**: 학습은 armature 0 이라
로터 반사관성이 **아예 없고**, 실물은 0.0283~0.0956 이다. **같은 kp 가 다른 폐루프
응답**을 낸다 — MJC 가 `armature=0` 칸을 *"수치적으로 퇴화"* 라고 부른 것이 이것이고,
armature 가 1순위인 이유다.

**우리 게인 × 벤더 armature 의 폐루프 특성**(Teacher 계산, 실물 튜닝 참고용):
Hip_P **7.28 Hz**/ζ0.46 · Hip_R 8.64/0.54 · Hip_Y 9.47/0.59 · Knee **5.15**/0.32 ·
Ankle **4.73**/0.30. ⇒ **발목은 벤더 의도(4 Hz)에 거의 맞고 힙이 ~2배 뻣뻣하다.**
전 관절 저감쇠(ζ 0.30~0.59). ⚠️ 이건 sim 일치 문제가 아니라 **실물 튜닝 후보**다.

**`NQ_armvendor`**(= NP + `armature_by_joint` 만, 레버 하나): **학습 6000 완주,
5시드 채점 중**(2026-08-09). B 블록의 첫 진짜 수정이고 오늘 안에 결과가 나온다.

### C. 과제 정합 — 우리가 시킨 적 없는 것을 요구하고 있다

| 변수 | 상태 |
|---|---|
| **배포 정책의 학습 과제** | ⛔⛔ `I3b_stance10` 은 `{waypoint: 1.0, **path: 0.0**}`. 분석 세션이 실제 샘플링 범위(`Goal_Pose_V7.yaml:189-212`)로 **20만 표본 몬테카를로**를 돌렸다: 목표거리 median 0.698 m · **요구속도 median 0.116 m/s** · 0.3 초과 11 % · **1.0 m/s 초과 0.00 %**(저장소 실측 0.126/12 % 와 일치 = MC 검증됨) |
| ⭐⭐ **그래서 학습된 보폭** | 학습 gait 대역 **[1.8, 2.4] Hz 강제** × 요구속도 0.116 m/s ⇒ **보폭 2.4~3.2 cm.** 실제 보행(1.42 m/s @2 Hz)의 **35.5 cm 대비 12배 차이** ⇒ **I3b 는 "덜 걷도록" 배운 게 아니라 "빠른 시계 × 3 cm 발 끌기"가 최적해인 과제에서 최적화됐다.** ⚠️ 실기 증상과 정확히 일치한다 — **시계는 9.4주기인데 눈으로 센 발자국은 4**(§8-67) |
| 보행 학습 arm | `NH`/`NP` 는 path 켜고 1.42~1.48 m/s. ⛔ **그러나 실기 서기에서 I3b 대비 몸 각속도 2.34~4.90배** |
| gait 대역 | 학습 [1.8, 2.4] Hz. 구 빌드 실측 1.51~1.58 = **대역 밖** → fresh 빌드에서 해소 |
| `feet_cross` 보상 | ⛔ config 에서 `-0.` (꺼짐). 켠 arm `N3_pathcross` 완주, 채점 대기 |
| 평가 과제 | ⛔ 오래 waypoint 정확도만 쟀다. `success_per_attempt` + forward_hold 로 전환됨 |

⇒ ⭐ **여기에 가장 아픈 모순이 있다**: 실기에서 가장 조용한 정책은 **걷도록 학습된 적이
없고**, 걷도록 학습된 정책들은 **실기에서 더 요동친다.**

### D. 붕괴 기전 — **모른다. 후보 4개를 지웠을 뿐이다**

| 후보 | 판정 |
|---|---|
| 발 간격 / 발 교차 기하 | ⛔ 기각 — 낙상 ±2 s 빼면 오염 칸이 오히려 **더 넓다** |
| 발끼리 충돌이 매개 | ⛔ 기각 — 오염 팔 낙상의 44~57 %만 충돌 선행 |
| 오염 관측 → 전신 오명령 | ⛔ 기각 — 두 팔 명령 서명 동일 |
| capture point 위반 | ⛔ 판별력 없음 — 무낙상 대조군이 이미 46 % |
| 발목 roll 진동 | ⛔ 계측 산물이었다 |
| **제어주기/케이던스** | ✅ **유일하게 살아남은 인과축.** MuJoCo 용량-반응 확인 |

⇒ **인과축이 하나뿐이고, fresh 빌드가 이미 그것을 고쳤다.** 그래서 다음 실기 시험이
"남은 것이 있는가"에 대해 **진짜로 결정적**이다.

---

## 2. 지난 하루 triage — 목적에 부합했나

**부합(=4발자국에 직결)**
- fresh-state 수정(218 ms 개루프 제거) · 정책률 50 Hz 복원 — **Codex**
- `b`~`r` 자세 수정(tilt 13.8→1.4°) — 이것이 없었으면 시도 자체가 없었다
- 30초 시험 설계 + `--gait-clock-scale` 레버 — **유일하게 남은 가설을 죽일 수 있는 실험**
- Codex 감사 교차검증 — 빌드를 믿을 수 있는지가 모든 시험의 전제였다

**부분 부합**
- §8-69 발목 게이트 — 실재하는 결함을 지웠지만 **붕괴 원인은 아니다**(곡선만 이동)

**⛔ 부합하지 않음 — 여기서 시간을 썼다**
- p 값 3연속 정정(1.5e-11 → 0.0045 → 0.05): **배포 판단을 1 mm 도 안 바꿨다**
- capture 위반율 · `p1` 매개 · "두 독립 경로" 왕복: 순지식은 **후보 4개 소거**뿐
- 같은 정정을 세 세션이 각자 다시 유도한 왕복

⇒ **교훈**: MJC 의 추가 매개 분석은 **실기 30초 데이터가 나오기 전까지 한계효용이
0 에 가깝다.** 시뮬 안에서 더 쪼개도 실물이 어디에 앉는지를 모른다.
⇒ **그리고 B 블록(물리 자산)에 오늘 투입된 시간은 0 이다. 거기가 본체다.**

---

## 3. 다음 실험 — 정보/비용 순

| # | 실험 | 비용 | 무엇이 닫히나 |
|---|---|---|---|
| **1** | **로봇 30초 A 실행** (현 HEAD 재설치, 게이트 ON, `fixed 0.6,0,0`) | 로봇 10분 | **수정 스택 전체의 생존 판정.** 완주하면 4발자국 달성 + A~D 대부분 종결. 실패하면 남은 원인이 B/C 에 있다는 것이 확정 |
| **2** | 같은 세션 `read_joint_live` 30초 | 로봇 1분 | 발목 채널 결함 확정 + 게이트 오탐률 + 콜백 dt 분포 |
| **3** | 조건부 B 실행 `scale = 1.512/A케이던스` | 로봇 5분 | 시계 가설 확증/기각 (A 완주가 전제) |
| **4** | tau 10 대 24.2 ms A/B (제자리 1초) | 로봇 5분 | 발행 필터 잔여 레버 |
| **5** | ✅ **`NQ_armvendor`**(벤더 armature **만**) — 학습 완주, **5시드 채점 중** | GPU | **B 블록의 첫 진짜 수정. 오늘 결과.** armature 하나로 얼마나 움직이는지가 "자산 교정에 계보를 끊을 값이 있는가"의 직접 증거 |
| **6** | `N3_pathcross` 채점(feet_cross 켠 쌍) | GPU 0 | C 블록: 교차 보상이 실제로 듣는가 |
| **7a** | 토크한계·발목 ROM 교정 | GPU | **한계값이라 대부분 안 걸린다 ⇒ 계보 안 끊고 다음 arm 부터** |
| **7b** | **질량 18.714→19.666 · 발관성 0.383→0.494** | GPU | ⚠️ **상시 걸리는 플랜트 변경 ⇒ warm start 가 무의미해질 수 있다. 여기가 진짜 분기점** — `NQ` 결과 보고 결정 |

⛔ **1~3 은 한 세션에 다 들어간다.** 그 전에 다른 시뮬 분석을 더 쌓지 마라.

**1번 사전등록**(케이던스 = 총위상/총시간, ⛔ median 금지):
- 30초 완주 ⇒ **목표 달성.** 그 자리에서 케이던스·게이트 카운터·dt 분포를 기록하고 B 로.
- 조기 낙상 ⇒ 케이던스가 **≥1.60 Hz** 였는지 먼저 본다. 그랬다면 **시계는 무죄**이고
  원인은 B(물리) 또는 C(과제 정합)다 ⇒ 5·6·7 로 간다.
- ⚠️ 손 완전히 뗌 + 영상 + 접촉 시점을 기록 항목에 넣는다(§8-68 이 이것 때문에 흔들렸다).

---

## 4. 결정이 필요한 분기 — **이건 사용자 판단이 필요하다**

> **로봇에 올릴 정책을 `I3b`(조용하지만 보행 미학습) 로 계속 갈 것인가,
> 아니면 물리 교정 후 재학습한 보행 arm 으로 갈아탈 것인가.**

- **I3b 유지**: 1번 실험이 값싸고, 완주하면 목표 즉시 달성. 단 요구속도 0.119 m/s 과제로
  학습돼 **연속 보행이 분포 가장자리**다.
- **재학습**: B 블록이 본체이므로 근본적. 단 GPU 시간이 들고 `NH`/`NP` 전례처럼
  **sim 개선이 실기로 안 옮겨질 위험**이 실측돼 있다(서기 각속도 2.34~4.90배).

⭐⭐ **분기가 데이터로 기울었다(분석 세션 MC)**: `path: 0.0` 은 "속도 요구가 낮다"가
아니라 **"1.0 m/s 를 요구하는 구간이 20만 표본 중 0개"** 다. 보간·외삽의 문제가 아니라
**분포에 없는 영역**이고, 학습된 보폭이 **3 cm** 다. ⇒ **4발자국을 제대로 걷는 것은
재학습 없이는 어렵다.**

**그래도 순서는 바꾸지 않는다:**
1. **A 실행(30초, 게이트 ON)을 먼저.** I3b 가 3 cm 보폭 정책이어도 **안 넘어지고 30초를
   가는지**는 별개 사실이고, **D 블록의 마지막 열린 축**이다. 그리고 그 답이 없으면
   재학습한 정책이 실패했을 때 원인이 정책인지 스택인지 못 가른다.
2. **B**: 재학습 없이 **자산만 벤더값으로 바꿔 I3b 채점**(MJC). ⛔ **한 번에 한 레버** —
   토크 한계만 먼저(effort 가 6관절 전부 1.9~2.8배 부족하고 하드 클램프이므로).
   **게인은 건드리지 않는다.**
3. **C 는 새 arm 이 필요하다.** `path` 켠 N 배치는 이미 있지만 전부 `boxfoot` 이고
   **관측 넓힌 arm 은 export 가 막혀 있다**(CLAUDE.md 최상단) — **그게 진짜 병목이다.**

⇒ **어느 쪽이든 GPU 는 로봇과 병렬이다.** `NQ` 채점은 이미 돌고 있고, 그 결과가
"자산 교정에 계보를 끊을 값이 있는가"를 정한다.
