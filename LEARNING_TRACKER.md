# 학습 트래커 — **무엇을 학습시켜야 로봇이 걷는가**

> 배포 쪽 지도는 [SIM2REAL_TRACKER.md](SIM2REAL_TRACKER.md) 다(배포 루프·실기 측정).
> **이 문서는 학습 쪽 지도다.** 판정이 바뀌면 여기부터 고친다.
> ⛔ 작은 오류를 찾아 고치는 것은 이 표의 칸을 채울 때만 의미가 있다.

---

## 0. 한 줄

⛔⛔ **우리는 올바른 물리에서 학습한 적도, 올바른 물리에서 순위를 매긴 적도 없다.**
2026-08-09 에 둘 다 증명됐고, **그 효과가 지금까지 시험한 어떤 보상 레버보다 크다.**

| | 크기 | 근거 |
|---|---|---|
| 채점 물리를 바꾸면 | 낙상 **1.6 ↔ 22,227** (같은 체크포인트) | §8-72 |
| 학습 물리를 바꾸면 | 낙상 **571.6 → 15.8 → 1.6** (armature 0 → 발목만 → 전관절) | §8-72 |
| 지금까지 최고 보상 레버 | 도착률 **+13.8 %p** | C52(귀속은 C55 로 미확인) |

---

## 1. 변수 블록 — 학습 쪽

### 블록 A. 플랜트(물리 자산) — ⛔ **여기가 1순위인데 투입 시간이 거의 0이었다**

| 변수 | 학습값 | 실물/벤더 | 격차 | **올바른 값으로 학습한 arm** |
|---|---:|---:|---|---|
| armature 힙 | **0** | 0.0283~0.0478 | ∞ | ⭐ **`NQ_armvendor`**(2026-08-09) |
| armature 무릎 | **0** | **0.0956** | ∞ | ⭐ `NQ_armvendor` |
| armature 발목 | **0** | 0.0565 | ∞ | `NP`(0.05, 추측값) · ⭐ `NQ`(벤더) |
| 총 질량 | 18.714 kg | **19.666 kg** | −4.8 % | ⛔ **없음** |
| 발 질량 | 0.38305 kg | **0.4940 kg** | **−22.5 %** | ⛔ **없음** |
| 발목 roll ROM | ±0.345 | **±0.40**(손 실측) | −14 % | ⛔ **없음** |
| Hip_Pitch 원점 z | −0.062(armsdown) | **−0.077** | 15 mm | N 배치는 안전(boxfoot), **배포 계보 `I3b` 는 틀린 자산** |
| 토크 한계 **힙 P/Y** | 30 / 20 | **68 / 38.3** | 1.9~2.3배 | ⏳ **`NS_effort`** 학습 완주·채점 중. ⛔ **벤더도 부족하다** — 직진 재측정 낙상 URDF 66.0 → 벤더 **4.2** → 벤더×1.5 **0.0** ⇒ **정책이 실물 모터보다 더 원한다.** `NS` 의 목표는 제약 제거가 아니라 **한계 안에서 도는 보행 학습** |
| 토크 한계 **무릎·힙R** | 40 / 35 | 112 / 76 | — | ✅ **고칠 것 없음** — `sat_share` **0.001~0.06 %** 로 **안 물린다**(요구 5~8 N·m) |
| 토크 한계 **발목** | 20 | 38.3 | — | ⛔ **올리면 안 된다** — MJC 안전창 **[15,20]**, 22→4.8·25→41.4·38.3→49.2 낙상 |

⇒ **A 블록의 다음 한 걸음**: 질량·발관성·ROM 을 고치고 **벤더 armature 로** 학습.
⚠️ 질량/발관성은 **상시 걸리는 플랜트 변경**이라 warm start 가 의미를 잃는다 = **계보를 끊는 지점**.
토크 한계·ROM 은 한계값이라 계보를 안 끊고 다음 arm 부터 켜도 된다.

### 블록 B. 과제 정의 — **"걸으라고 요구한 적이 있는가"**

| 사실 | 근거 |
|---|---|
| `path: 0.0` 과제의 요구 속도 median **0.119 m/s** ⇒ **보행을 잰 적이 없다** | §8-44 |
| 배포된 `I3b` 가 그 과제 산물 ⇒ 학습된 보폭 **2.4~3.2 cm**(실제 보행의 1/12) | 배포 세션 몬테카를로 |
| `path` 를 켜면 도착률 **71.7 % → 10.1 %**(7배) | §8-65, **sha 일치 확인됨** |
| 40+ arm 이 그 대가의 **1/3도 못 되찾았다** | §8-65 |

⇒ **B 의 미해결**: 도착과 보행이 한 정책 안에서 양립하는 과제를 아직 못 만들었다.
사용자 제안(장거리 목표 커리큘럼: 5~6 m → 10~11 m → 30 m)이 이 칸을 겨냥한다 —
**waypoint 층이 사라지면 학습/배포 불일치도 같이 사라진다.**

### 블록 C. 보상 — **껐거나 틀린 것들**

| 항목 | 상태 |
|---|---|
| `_OFF_PROTECT` 6키 | E 배치부터 **전 계보에서 0**(배포 `I3b` 포함). `NR_protect` 가 처음 되켬 |
| `feet_cross` | 3일간 0 **이었고, 식 자체가 부호맹**이었다 → 2026-08-09 수정 |
| 정확도 보상 | 40+ arm 중 **한 번도 안 건드림**(`goal_reached` 이진 계단) |
| `dof_vel_limits` | 스케일 0 ⇒ **관절 속도에 학습 제약이 전혀 없다** |

### 블록 D. 채점 — **순위를 만드는 축**

| 항목 | 상태 |
|---|---|
| 채점 물리 | ⛔ 공통 프로토콜이 `armature 0` ⇒ **전 arm 순위가 그 위에서 매겨졌다** |
| 정확도 cm | 생존 편향으로 **인과가 반대** → `success_per_attempt` 로 교체(C48) |
| 노이즈 바닥 | 축마다 9배 다름 → `stat_compare --noise-table` |
| 학습 코드 일치 | 5쌍 중 **2쌍 교락**(C55) → `claim_check train_env_sha` |

---

## 2. 오늘(2026-08-09) 나온 것 — **목적에 부합하는가**

### ✅ 부합 — 학습 방향을 실제로 바꾼다

1. **채점 물리가 순위를 지배한다**(§8-72). ⇒ 우리가 "최고"라 부른 것이 전부 재검토 대상.
2. **학습 armature 의 용량-반응**: 0 → 발목만 → 전관절 = 낙상 **571.6 → 15.8 → 1.6**.
   ⇒ **A 블록이 1순위라는 직접 증거.**
3. **`052`**: waypoint 지표가 학습 진행과 반상관. 봉우리는 지표 인공물이고
   **6000 iteration 이 사는 것은 보행과 생존**이다(속도 +24.9 %, 낙상 1/479).
4. **`feet_cross` 부호 결함** 수정 — 실기 서명(p1 −16.35 cm)이 무벌칙 구간이었다.

### ⚠️ 정확도는 지켰지만 **방향은 안 정한 것** (= 디테일)

노이즈 바닥 축별 분리 · 라벨 `WHICH_ARM.txt` · 철회 사슬 표시 · 속도 양자화 · 인덱스 22↔12.
**전부 필요했지만 "무엇을 학습할지"를 하나도 안 정했다.** 이 문서가 없어서 그렇게 됐다.

---

## 3. 다음 실험 — 우선순위와 사전등록

| # | 실험 | 비용 | 무엇을 정하나 | 사전등록 |
|---|---|---|---|---|
| **1** | **`168` 축소판**: `N0`↔`N1` · `NE`↔`NZ` 를 벤더에서(**두 축**) | GPU만, **학습 0** | ⭐ *"`path` 대가 7배"* 가 **물리 산물인지**. 그 문장이 40+ arm 의 방향을 정했고 `armature 0` 에서 측정됐다 | 벤더에서 7배가 **크게 줄면** ⇒ "보행↔도착 양립 불가" 전제가 깨지고 **C 블록이 쉬워진다.** 그대로면 진짜 교환 |
| **1b** | ⭐⭐ **`NQ_s2`**(`NQ` 를 **시드만** 바꿔 재학습) — `NS` 와 **동시에** | 학습 1회 | ⭐ **학습 시드 바닥 — 캠페인 최초.** 이 값 없이는 `NS` 도 또 하나의 미확인 귀속이 된다 | 바닥이 레버보다 **크게 나올 가능성을 미리 인정**한다. 그러면 과거 *"레버 하나로 X배"* 상당수가 **판정 불가**로 간다 — 그것이 사려는 정보다 |
| **2** | `167` **NQ held-out**(×0.5·×1.5) | 18런 | `I3b` → `NQ` 배포 교체 | 세 점 전부 승 ⇒ 승격 / ×1.0 만 승 ⇒ 물리 과적합, 교체 금지 |
| **3** | ⭐ **`NT_cross`** = `NQ` + **고친 `feet_cross`(−20)** | 학습 1회, 레버 1개 | **배포 후보의 측정된 약점** — `NQ` 는 실기 케이던스에서 스탠스 0.122(`I3b` 0.158), 발간격 min **−0.031 = 교차** | 명령 `p1` **증가** + 음수체류율 **감소** + 시도 분모 낙상률 **불변 이상**. ⚠️ **`NQ_s2` 바닥을 넘는지로** 판정 |
| **3b** | **`NS_effort`**(축소판) = `NQ` + 힙 **P/Y** effort 벤더 | 학습 1회, 레버 1개 | 학습 중 **구속되는** 토크 제약이 정책을 빚는가 | ⚠️ MJC 의 `NQ` 스윕(×0.5~×2.0 낙상 0)은 **이미 학습된 정책**에 상한을 바꿔 낀 것이라 이 질문에 답하지 않는다 |
| **3b** | **A 블록 잔여**: 질량 19.666 · 발 0.4940 · ROM ±0.40 | 학습 1회 + **계보 분기** | 상시 걸리는 플랜트 오차 | 질량·발관성은 warm start 를 무의미하게 만들 수 있다 |
| **4** | `feet_cross` 쌍 재학습(`N1`·`N3` **둘 다**, 같은 커밋) | 학습 2회 | 저장소 최초의 **코드까지 맞춘** 레버 쌍 | 명령 음수체류율 + 시도 분모 낙상률(독립 종점) |
| **5** | 장거리 목표 커리큘럼 | 설계 필요 | **B 블록** — waypoint 층 제거 | 미설계 |

### ⛔ 그만둘 것

* **`armature 0` 위에서 새 보상 레버를 뽑는 것.** 40+ arm 이 그렇게 나왔고 `path` 대가의
  1/3도 못 되찾았다. 플랜트가 틀린 상태에서 보상을 조율하는 것은 **틀린 로봇을 잘 걷게 하는 것**이다.
* **정확도 cm 로 체크포인트·arm 을 고르는 것**(C48).
* **공통 채점 순위를 인용하는 것** — 벤더 물리 재채점 전까지.
* ⛔⛔ **`armature 0` 에서 잰 레버 결론을 인용하는 것**(§8-73 ③). **그리고 과거를 다시 재지
  마라 — `NQ` 위에서 다시 물어라.** 전수 재측정은 또 사후 장부다(분석 세션 합의).

### ⭐ 전방 규칙 (2026-08-10, 재측정을 대체한다)

> **앞으로 모든 레버는 `NQ + 레버 하나` 로, 같은 커밋에서, `NQ_s2` 바닥에 대고 잰다.**
> 그리고 **레버를 붙이기 전에 `NQ` 에서 그 레버가 겨냥하는 결함이 실제로 측정되는지 먼저 본다**
> (조작 확인). `NT_cross` 를 그렇게 걸러냈다 — `NQ` 의 `<0.07` 체류가 **0.0086 %** 라 발화하지 않는다.

⚠️ **지금 이 기준을 통과하는 레버는 `NS_effort` 하나뿐이다**(힙 P/Y 포화 4.5~7.4 % = `NQ` 에서
직접 측정된 구속). `167` 이 `NQ` 를 세 물리 점 전부 1위로 냈으므로 **다른 약점이 측정되기
전까지 새 레버를 만들 근거가 없다.**

---

## 4. 열려 있는 큰 질문

1. **armature 0 으로 학습한 정책이 벤더 물리에서 회복 가능한가?**
   지금 데이터는 "아니오" 쪽이다(`NH` 6.0 → 571.6). 그러면 **라이브러리의 가치가 제한적**이고
   재학습이 답이다. 실험 1 이 이 질문에 답한다.
2. **`NQ` 의 우위가 강건성인가 물리 과적합인가** — 실험 2.
3. **도착과 보행이 한 정책에서 양립하는가** — B 블록. 아직 아무 arm 도 못 했다.
4. ~~**학습 시드 산포를 한 번도 안 쟀다.**~~ → **실험 1b 로 산다**(`NQ_s2`, `NS` 와 동시).
   ⭐ 분석 세션 지적: *"`NS` 를 곧 돌리는데 **결과를 읽을 자가 없다**"* — `NS` 가 `NQ` 보다
   30 % 낮아도 레버 때문인지 시드 때문인지 구별할 방법이 지금 0 이다. C55 와 같은 자리다.
   **그 바닥은 한 번만 사면 앞으로 모든 arm 에 재사용된다.**
