# 현재 상황 종합 분석 — 2026-08-07 18:00 KST

작성 근거: 저장소 문서 전수 + 서버 실측(ssh) + **1차 자료(`report.json`) 직접 읽기** + 코드 정독.
요약표를 인용하지 않고 원본 JSON에서 다시 뽑았다. 검증 절차는 §7에 적어 뒀다.

⚠️ 이 문서는 **하나의 서사로 수렴시키지 않는다.** 축이 다섯 개이고 서로 독립이다.
억지로 합치면 지난 배치들이 반복한 오류("한 원인으로 다 설명된다")를 그대로 재현한다.

---

## 0. 한 장 요약

| 축 | 한 줄 | 상태 |
|---|---|---|
| **A. 과제** | 사용자의 두 요구(≤5 cm 도착 / 1.3–1.5 m/s)가 **현재 정책 공간에서 양립하지 않는다.** 파레토 곡선 가운데가 비었다 | 측정 완료, 미해결 |
| **B. 측정** | **판정의 근거 자체에 구멍이 여섯 개** 있다. 그중 둘은 이미 나온 결론을 뒤집는다 | 🔴 신규 발견 |
| **C. 실기 필터** | 감사 결론 유지. 새 사실 하나(두 번째 `sleep`) 추가. **MuJoCo와 Isaac이 배포 정책에 대해 반대로 말한다** | 미해결 |
| **D. 배포 파이프라인** | **관측 넓힌 arm 5개는 export 자체가 불가능하다.** 아직 아무도 안 짚었다 | 🔴 신규 발견 |
| **E. 운영** | GPU 2장 정상 가동. 자율운전 잔여 프로세스 5개(무해). 큐 순서가 가장 급한 답을 3시간 뒤로 미룬다 | 경미 |

**가장 크게 바꿔야 할 판단 하나**:
`HANDOFF_TRAINING_20260807.md` §1-4의 *"`N0_ctrl`이 정확도·속도·낙상 세 축 모두에서
path arm 전부를 지배한다"* 는 **데이터와 반대다.** 같은 문서 §1-3 표가 이미 반증하고 있다.
→ §3-B1.

---

## 1. 🔔 선제 보고 의무 항목 (CLAUDE.md)

### 1-1. 파레토 스캔 — **완료됐고, 이것이 결과다**

`queue/done/005-pareto_N1_path.rc0`, `005-pareto_N2_pathgrid.rc0` (둘 다 rc0 = 정상).
각 체크포인트를 **두 축 60 s씩** 재서 뽑았다.

**N1_path** (`logs/pareto/2026-08-07-03-45-31_N1_path`)

| iter | 오차med(cm) | strict% | 보행속도 | >1.0 m/s | 보행낙상률 | |
|---:|---:|---:|---:|---:|---:|---|
| 100 | **7.84** | 26.1 | 1.15 | 88.3 | 0.0363 | ✗ 낙상초과 |
| 900 | 39.89 | 9.1 | 1.38 | 97.7 | 0.0011 | ★ |
| 1700 | 41.12 | 9.3 | 1.44 | 98.3 | 0.0018 | ★ |
| 2600 | 43.38 | 9.5 | 1.38 | 98.0 | 0.0007 | |
| 3400 | 42.87 | 8.1 | 1.40 | 98.3 | 0.0007 | |
| 4300 | 45.17 | 8.6 | 1.40 | 98.1 | 0.0004 | |
| 5100 | 44.57 | 8.1 | 1.41 | 98.1 | 0.0005 | |
| 6000 | 47.69 | 10.2 | **1.48** | 98.3 | 0.0007 | ★ |

**N2_pathgrid** (`logs/pareto/2026-08-07-07-19-10_N2_pathgrid`)

| iter | 오차med(cm) | strict% | 보행속도 | >1.0 m/s | 보행낙상률 | |
|---:|---:|---:|---:|---:|---:|---|
| 100 | **5.80** | 38.8 | 1.05 | 67.0 | 0.0414 | ✗ 낙상초과 |
| 900 | 30.62 | 7.7 | 0.36 | 0.0 | 0.0000 | ★ |
| 1700 | 32.34 | 9.6 | 0.88 | 18.4 | 0.0000 | ★ |
| 2600 | 37.02 | 8.0 | 1.36 | 95.3 | 0.0029 | |
| 3400 | 34.14 | 8.9 | 1.34 | 93.9 | 0.0020 | |
| 4300 | 33.25 | 7.7 | **1.50** | 98.3 | 0.0011 | ★ |
| 5100 | 43.99 | 6.8 | **1.64** | 97.9 | 0.0018 | ★ |
| 6000 | 42.21 | 8.0 | 1.54 | 97.6 | 0.0005 | ★ |

**판정**

1. **무릎이 아니라 절벽이다.** 두 arm 다 iteration 100 → 900 사이에 정확도가
   5.8–7.8 cm에서 30–40 cm로 무너지고, 그 뒤 6000까지 거의 평평하다.
2. **쓸 수 있는 ★가 없다.** 프론트에 남은 것이 전부 30–48 cm다. 사용자 게이트는
   median ≤ 5 cm이므로 **어떤 목표 반경으로도 "도착"이 아니다.**
3. **다만 속도 요구는 처음으로 충족됐다.** `N2_pathgrid` iter 5100이 **1.64 m/s**,
   iter 6000이 1.54 m/s에 보행낙상률 0.0005다. 사용자 요구 G2(1.3 m/s)/G2b(1.4–1.5 m/s)를
   **넘긴 첫 체크포인트들**이다. 이 저장소가 1년 가까이 "미측정/미달"로만 적어 온 항목이다.
4. **파레토 스캔이 `N1` 대 `N2`의 순위도 뒤집는다.** `best.pth` 두 개만 보면
   N2(5.74) > N1(7.93)인데, 곡선 전체로 보면 N2가 속도에서도 앞선다(1.64 대 1.48).

⛔ **아직 안 나온 것**: `006-pareto_fine_*`(iteration 100–900 정밀 스캔). 절벽의 모양을
가르는 유일한 측정인데 **큐에서 대기 중**이고, 앞의 학습 두 개 때문에 ~20:30 KST에야
시작한다(§2-1, §5).

### 1-2. N2 / NA 두 축 — 이미 `HANDOFF_DEPLOY_ENTRY_20260807.md` §8에 보고됨

원본 JSON으로 재확인했고 값이 일치한다. §2-4 표에 통합해 뒀다.

---

## 2. 현재 상황 (실측)

### 2-1. 서버 `a6000` — 2026-08-07 17:56 KST 실측

```
GPU 0: RTX A6000  49,140 MiB 중 3,914 사용  util 57–64 %
GPU 1: RTX A6000  49,140 MiB 중 3,979 사용  util 46–53 %
```

| | 무엇 | 진행 | 예상 완료 |
|---|---|---|---|
| 학습 (PID 2394852) | `NC_actfilter` | **1,893 / 6,000** | ~20:30 KST |
| 학습 (PID 2395318) | `NZ_zeroiid` | **1,862 / 6,000** | ~20:30 KST |

실측 학습 속도 **2.18 s/iteration** (16:47 시작 → 17:56에 ~1,880).

**큐** (워커는 카드당 1개, 사전순으로 집는다)

| | 대기 1 | 대기 2 |
|---|---|---|
| gpu0 | `006-pareto_fine_N1_path.sh` | `820-ND_dwell.sh` |
| gpu1 | `006-pareto_fine_N2_pathgrid.sh` | `800-N4_hist.sh` |

**plan/** (autopilot 대체재): gpu0 `N5_tau · N6_foot · N9_zerostruct · NB_zerocritic · N3_pathcross`,
gpu1 `N7_critic · N1_long`

**최근 실패 두 건** (인계 문서 작성 이후에 생긴 것):

| 작업 | rc | 원인 |
|---|---|---|
| `800-N4_hist` | **rc1** | `FileNotFoundError: .../2026-08-07-16-45-46_N4_hist/summaries/events.out.tfevents...` — ibatch §8-48이 기록한 `rm -rf` 사고로 **돌던 run 디렉터리가 지워졌다.** 재큐됨 |
| `830-NZ_zeroiid` | **rc1** | 같은 사고. 재큐돼서 지금 돌고 있다 |

**감시자**: `idle_watch.sh` 1개, `autopilot.sh` **6개**(정상 1 + 잔여 5 — §3-E), 워커 2개.
`idle_state.json`은 29초 전 갱신, `stalled: false`.

### 2-2. 로봇 — **지금 접속 불가**

```
$ ssh booster@192.168.10.102
ssh: connect to host 192.168.10.102 port 22: Network is unreachable
```

`HANDOFF_ROBOT_20260807.md` §4가 적은 대로, Mac에서 로봇 이더넷과 서버 경로가 충돌한다.
따라서 아래 두 가지는 **이 분석에서 확인하지 못했다**:

- 로봇에 남아 있던 배포 프로세스 두 개(PID 33763 / 42133)가 정리됐는지 — **미확인**
- `--hold-diag 15`의 tilt 실측 — **미실행** (배포 진입 자세 수정의 유일한 판정)

### 2-3. 저장소

- 브랜치 `ekay-fix`, `origin/main`보다 2 커밋 앞. **작업 트리 깨끗**. 서버도 같은 커밋(`ee4a01a`).
- ⚠️ `htwk-gym/sweeps/`가 **git 추적 밖**이다(`.gitignore:225`). 공통 평가 프로토콜
  `sweeps/N0_ctrl.yaml`도 여기 있다. `make_v7_arms.py`로 재생성되지만, **지금까지의 모든
  판정이 기댄 평가 프로토콜이 저장소에 없다.**

### 2-4. 지금까지 나온 전부 — 두 축 (원본 `report.json`)

**축 1 — 정확도** (공통 waypoint 프로토콜 `sweeps/N0_ctrl.yaml`, 120 s, 256 env, ~4,600 구간)
게이트: median ≤ 5 cm / p90 ≤ 10 cm / heading ≤ 10° / **낙상 0**

| arm | 체크포인트 | 오차med | 낙상 | 낙상률 | strict% |
|---|---|---:|---:|---:|---:|
| **MA_feetcross** | best(1225) | **2.69 cm** | 4 | 0.00087 | 83.6 |
| **N0_ctrl** | best(2200) | **2.82** | 8 | 0.00173 | **86.4** |
| I3b_stance10 ← **배포된 정책** | best(75) | 3.10 | **2,488** | **0.36767** | 70.5 |
| M3_scratch_phase | best(8175) | 3.30 | 16 | 0.00345 | 79.3 |
| M7_robotasset | best(1225) | 3.54 | 10 | 0.00216 | 73.8 |
| MB_obsdelay | best(1500) | 4.28 | 8 | 0.00174 | 37.3 |
| N8_pathdelay | best(100) | 4.39 | 441 | 0.08857 | 52.3 |
| NA_histzero | best(3300) | 5.47 | 12 | 0.00259 | 20.3 |
| N2_pathgrid | best(100) | 5.74 | 168 | 0.03536 | 39.7 |
| N1_path | best(100) | 7.93 | 112 | 0.02378 | 25.5 |
| N1_path | **model_6000** | **47.70** | 7 | 0.00151 | 10.1 |
| N0_ctrl | **model_6000** | **⛔ 측정된 적 없음** | — | — | — |

**축 2 — 지속 보행** (`--goal_pattern forward_hold`, 요구속도 med 2.00–2.08)

| arm | 체크포인트 | 길이 | 속도med | >1.0 m/s | 낙상 | 낙상률 |
|---|---|---:|---:|---:|---:|---:|
| **N2_pathgrid** | model_6000 | 60 s | **1.54** | 97.6 % | 8 | **0.00052** |
| **N1_path** | model_6000 | 60 s | **1.48** | 98.3 % | 10 | **0.00066** |
| NA_histzero | model_6000 | 60 s | 1.32 | 97.6 % | 61 | 0.00398 |
| N8_pathdelay | model_6000 | 60 s | 1.28 | 94.2 % | 65 | 0.00424 |
| **N0_ctrl** | model_6000 | 60 s | **1.10** | 78.9 % | 53 | **0.00347** |
| N0_ctrl | best(2200) | 120 s | 0.98 | 42.3 % | 116 | 0.00379 |
| M3_scratch_phase | model_10000 | 60 s | 0.99 | 48.0 % | 17 | 0.00111 |
| MA_feetcross | model_1500 | 60 s | 0.98 | 44.9 % | 35 | 0.00229 |
| M7_robotasset | model_1500 | 60 s | 0.87 | 7.4 % | 17 | 0.00111 |
| MB_obsdelay | best(1500) | 120 s | 0.67 | **0.0 %** | 16 | 0.00052 |
| I3b_stance10 ← 배포 | model_200 | 60 s | 1.14 | 62.1 % | **11,769** | **0.55966** |

**속도 숫자의 자체 정합성 확인**: `forward_hold`는 ~1 s마다 목표를 2 m 앞에 다시 둔다.
따라서 `2 − pos_err_median ≈ 1초간 이동거리 ≈ 속도`여야 한다.
N2_final: 2 − 0.516 = **1.48** (측정 1.54) ✅ / N0_final: 2 − 0.929 = **1.07** (측정 1.10) ✅ /
**I3b: 2 − 1.153 = 0.85인데 측정 1.14** ❌ — 앞으로 고꾸라지는 것이 속도로 잡히고 있다는
증거이고, "속도만 보면 안 된다"의 반례가 같은 표 안에 있다는 §8-47의 경고가 정량으로 확인된다.

---

## 3. 문제 분석

### A. 과제 축 — 두 요구가 양립하지 않는다

사용자 요구는 `docs-research/user_requirements.md`에 원문까지 추출돼 있다:

- **G1** `최종 위치 오차 median ≤ 5 cm / p90 ≤ 10 cm`, `heading ≤ 10°`, `넘어짐률 0%` (22회 반복 요구)
- **G2** `body속도는 1.3m/s정도 나와야함` / **G2b** `가능하다면 1.4, 1.5m/s 정도까지`

지금 데이터가 말하는 것:

| | G1 위치 | G1 heading | G1 낙상 | G2 속도 |
|---|---|---|---|---|
| N0_ctrl best | **2.82 cm ✅** | 1.9° ✅ | 8회 ❌ | 0.98 m/s ❌ |
| MA_feetcross best | **2.69 cm ✅** | 1.8° ✅ | 4회 ❌ | 0.98 m/s ❌ |
| N2_pathgrid 5100 | 43.99 cm ❌ | — | ✅ 수준 | **1.64 m/s ✅** |
| N2_pathgrid 6000 | 42.21 cm ❌ | — | ✅ 수준 | **1.54 m/s ✅** |

**두 요구를 동시에 만족한 체크포인트는 하나도 없고, 곡선 가운데도 비어 있다.**

이것은 `user_requirements.md`가 **X1**로 기록해 둔 구조적 충돌의 현재 모습이다:

> X1: 1.3~1.5 m/s (N6/N7) ↔ 과제 구조상 최대 요구속도 0.574 m/s

§8-44가 그 충돌을 `path` 모드로 풀려 했고, **속도 쪽은 실제로 풀렸다**(1.64 m/s).
대신 정확도가 무너졌다. 즉 **문제가 해결된 게 아니라 이동했다.**

**왜 무너지는가 — 가설(미검증)**: `path` 모드의 carrot은 dwell 중이 아니면 구조적으로
잡을 수 없다(코드가 floor `gap ≥ lookahead`로 강제한다, `goal_pose_v7.py:349`).
정책이 "따라붙되 도달하지 않는다"로 수렴하고, 그것이 waypoint의 "도착해서 선다"와 정면 충돌한다.
dwell이 그 충돌을 없애려고 있는 장치인데 duty가 낮다.
⛔ **이 가설은 아직 직접 측정된 적이 없다.** 재는 방법은 §4-A2에 적었다.

베이스 config 주석이 2026-07-27에 이미 같은 일을 기록했다:
*"waypoint-only error was 6.3 cm for the non-path arm vs 37.9/38.7 cm for the two
path-trained ones."* → **dwell은 6.3→37.9을 2.8→7.9로 줄였을 뿐 없애지 못했다.**

---

### B. 측정 축 — 판정의 근거에 구멍이 여섯 개

이 절이 이 문서에서 가장 중요하다. 아래 여섯 개 중 **B1과 B2는 이미 내려진 결론을 바꾼다.**

#### 🔴 B1. `N0_ctrl model_6000`의 정확도가 측정된 적이 없다 — 그리고 "지배한다"는 틀렸다

`tools/eval_round.sh`의 4단계는 이렇다:

```
1/3 선택기          → best.pth 결정
2/3 정확도 (120 s)  → best.pth
3/3 지속 보행(120s) → best.pth
4/4 지속 보행(60 s) → 마지막 체크포인트     ← 보행만. 정확도는 없다
```

**마지막 체크포인트의 정확도를 재는 단계가 아예 없다.** 실제로 서버에
`*_final.accuracy` 디렉터리는 `N1_path` 하나뿐이고, 그건 나중에 별도 작업
(`026-eval_N1final_acc`)으로 따로 돌린 것이다.

그 결과 `HANDOFF_TRAINING_20260807.md` §1-3의 헤드라인 표는 이렇게 돼 있다:

| | N0_ctrl | N1_path | 실제로 어느 체크포인트인가 |
|---|---|---|---|
| 보행 속도 | 1.10 | 1.48 | 둘 다 **model_6000** |
| 보행 낙상률 | 0.0035 | 0.0007 | 둘 다 **model_6000** |
| 정확도 | **2.82** | 47.70 | N0는 **best(2200)**, N1은 **model_6000** ⛔ |

표 머리글은 *"둘 다 `model_6000` 기준"*이라고 적혀 있는데, **정확도 행만 체크포인트가 다르다.**
이것이 정확히 §8-47이 *"한 축으로 뽑아 다른 축으로 채점하는 것이 이 저장소가 반복한 오류"*라고
쓴 그 오류다.

그리고 §1-4의 결론:

> **그리고 `N0_ctrl`이 정확도·속도·낙상 세 축 모두에서 그 전부를 지배한다.**

**이것은 같은 문서 §1-3 표가 이미 반증한다.** 원본 JSON으로 다시 확인하면:

| | 최고 속도 | 최저 보행낙상률 |
|---|---|---|
| N0_ctrl (전 체크포인트) | **1.10** m/s | **0.00347** |
| N1_path model_6000 | 1.48 | 0.00066 |
| N2_pathgrid model_6000 | 1.54 | 0.00052 |
| N1_path 파레토 ★ 최저 | 1.38 | 0.0011 |

**N0는 어떤 체크포인트에서도 N1/N2의 속도를 못 따라가고(1.10 < 1.38), 보행 낙상은 5–7배 많다.**
지배는 **정확도 한 축에서만** 성립한다.

**이 정정이 왜 중요한가**: "N0가 세 축 모두 지배 → path는 막다른 길"이라는 서사가
`ND_dwell`·2단계 학습 같은 다음 수의 근거였다. 실제 그림은 **N0와 path arm이 파레토
프론트의 양 끝에 있고 가운데가 비어 있다**이며, 그러면 다음 수의 우선순위가 달라진다(§4-A).

**⛔ 그리고 아직 모르는 것**: `N0_ctrl model_6000`의 정확도. 이 숫자 하나가
"N0는 6000까지 정확도를 유지하는가"를 결정한다. **평가 1회, 약 3분이면 나온다.**

#### 🔴 B2. warm start 계보가 둘로 갈렸다 — 문서와 코드가 다르다

`tools/stage_obs_arms.sh`가 만드는 큐 스크립트의 **주석**(33·37행):

> *"warm start 는 N1 의 **최종** 정책에 관측 수술을 해서 쓴다. …
> 그래서 이 arm 은 N1 과 **완전히 같은 정책에서 출발**하고, 'N1 대 N4_hist' 이 정확히
> '관측을 더하니 좋아지는가' 가 된다."*

같은 스크립트의 **코드**(43행):

```bash
CK="$D/nn/best.pth"
```

서버 실측:

```
$ ls -la .../2026-08-07-03-45-31_N1_path/nn/best.pth
best.pth -> .../nn/model_100.pth
```

**`best.pth`는 `model_100`이다.** 학습 로그가 확정해 준다:

```
queue/logs/030-NA_histzero.log : warm start 원본: .../N1_path/nn/best.pth
queue/logs/830-NZ_zeroiid.log  : warm start 원본: .../N1_path/nn/best.pth
queue/logs/810-NC_actfilter.log: warm start:      .../N1_path/nn/best.pth
queue/logs/800-N4_hist.log     : warm start 원본: .../N1_path/nn/best.pth
queue/logs/030-N8_pathdelay.log: Loading model from .../I3b_stance10/nn/model_200.pth
```

즉 N 배치에 **서로 다른 계보 두 개**가 섞여 있다:

| 계보 | arm |
|---|---|
| 배포 정책 `I3b_stance10 model_200` | `N0_ctrl` `N1_path` `N2_pathgrid` `N8_pathdelay` `N3_pathcross`(예정) |
| **`N1_path model_100`** (붕괴 **전** 체크포인트) | `NA_histzero`(완료) `NZ_zeroiid`(진행) `NC_actfilter`(진행) `N4_hist` `N5_tau` `N6_foot` `N7_critic` `N9_zerostruct` `NB_zerocritic` `ND_dwell` |

**결과 세 가지**:

1. **"N1 대 NA/NZ/NC/N4"는 한 레버 비교가 아니다.** 출발점도 다르고 배포 정책 기준
   누적 학습량도 다르다(6000 대 100+6000).
2. **그러나 두 번째 그룹끼리는 서로 비교 가능하다** — 전부 같은 warm start다.
   **그 그룹의 대조군(= `N1_path model_100` + plain N1 6000 iteration)이 없다.**
   이게 지금 가장 싸게 메울 수 있는 구멍이다.
3. `NA_histzero`가 유일하게 붕괴하지 않은 것(best가 iteration **3300**, 5.47 cm)에 대해
   인계 문서는 *"이력+영점이 학습 궤적 전체를 안정화"*라고 해석했다. **그 해석이 여전히
   유력하다** — NA는 붕괴 전 지점에서 출발해 **6000 iteration을 더 돌고도 안 무너졌고**,
   N1은 같은 지점에서 800 iteration 만에 무너졌다. 다만 **비교의 기준선이 문서가 말하는
   것과 다르다**는 것은 고쳐 적어야 한다.

**추가 위험 — warm start 원본이 가변이다.** `best.pth`는 `select_best_checkpoint.py --link_best`가
매번 다시 만드는 심볼릭 링크다(`:679-683`). 즉 **평가 라운드를 한 번 더 돌리면 앞으로
큐에서 나갈 arm 들의 출발점이 조용히 바뀐다.**

#### 🔴 B3. `ND_dwell`의 레버 크기가 의도의 1.7배다 (아직 안 돌았다 — 지금 고칠 수 있다)

`make_v7_arms.py`의 설계 주석:

```
현재  interval [12,24] duration [3,5] -> duty 18.2 %
ND    interval [ 4, 8] duration [3,5] -> duty 40.0 %   (2.2배)
```

18.2 % = 4/(18+4), 40.0 % = 4/(6+4). 즉 **`duty = duration/(interval+duration)`**,
다시 말해 *"dwell이 끝난 뒤에 interval이 다시 시작된다"*고 가정한 산식이다.

코드는 그렇지 않다(`goal_pose_v7.py:316-327`):

```python
self.path_dwell_left = (self.path_dwell_left - 1).clamp(min=0)
self.path_dwell_next =  self.path_dwell_next - 1
fire = (self.path_dwell_next <= 0) & ids
if fire.any():
    self.path_dwell_left[fire] = duration 추첨     # 둘이
    self.path_dwell_next[fire] = interval 추첨     # 동시에 시작한다
```

**주기 = `interval`이고 dwell이 그 안에 들어 있다.** 따라서 `duty = duration/interval`:

| | 설계 주석 | **실제 코드** |
|---|---|---|
| 현재(N1/N2) | 18.2 % | **22.2 %** (4/18) |
| `ND_dwell` | 40.0 % | **66.7 %** (4/6) |

그리고 `fire` 조건에 `& ~dwelling`이 없다. `duration > interval`이 뽑히면
(duration~U[3,5], interval~U[4,8] → **약 6.25 %**) 그 env는 dwell 중에 타이머가 다시
발화해 **에피소드가 끝날 때까지 영구히 dwell**한다. 에피소드당 발화가 3–5회이므로
**env의 약 20 %가 한 번쯤 영구 dwell에 빠진다.**

**결과**: `ND_dwell`은 "dwell을 40 %로 올리면 두 축이 양립하는가"를 묻는 arm인데,
실제로는 **~67 % 이상 + 일부 env는 사실상 waypoint 전용**이 된다. 그러면 이 arm은
십중팔구 "정확도는 돌아오고 속도를 잃었다" = `N0_ctrl` 재현으로 끝나고,
**중간 지점이 있는지는 여전히 모르게 된다.** 3.6시간짜리 학습이다.

#### B4. 게이트가 정보를 주지 않는다

공통 프로토콜의 게이트(`sweeps/N0_ctrl.yaml:672-676`):

```yaml
gates:
  pos_median_m: 0.05      # 사용자 G1
  pos_p90_m: 0.1          # 사용자 G1
  heading_median_deg: 10  # 사용자 G1
  max_falls: 0            # 사용자 G1 "넘어짐률 0%"
```

`max_falls: 0`은 **4,600 구간 전체에서 낙상 0회**를 요구한다.
→ **15개 arm 전부 `fail`.** 표의 "게이트" 열이 전부 같은 값이라 아무것도 구분하지 않는다.

⚠️ 사용자 요구를 낮추자는 말이 아니다. **판정 도구로서 기능하지 않는다**는 말이다.
MA_feetcross의 0.087 %(4/4601)와 배포 정책의 36.8 %(2488/4279)가 같은 `fail`로 찍힌다.
그 둘의 차이가 **423배**다.

#### B5. `forward_hold`가 이력 arm을 구조적으로 불리하게 잰다

관측 이력·지연 버퍼는 `_resample_goals()` 끝에서 지워진다(`goal_pose.py:719-741`).
그런데 `_resample_goals()`는 **에피소드 리셋뿐 아니라 평범한 목표 재추첨마다** 돈다
(`:612` `episode_length_buf == cmd_resample_time`).

| | 목표 재추첨 주기 | 제어스텝 | 이력(k=5) 퇴화 구간 |
|---|---|---|---|
| 학습 | 4–8 s (평균 300 스텝) | 0.02 s | **1.3 %** |
| 정확도 프로토콜 | 4–8 s | 0.02 s | 1.3 % |
| **`forward_hold`** | **0.8–1.2 s (평균 50 스텝)** | 0.02 s | **8 %** |

`forward_hold`는 이력을 **학습보다 6배 자주 지운다.** `history_steps: 1`인 arm
(N0/N1/N2/N8)은 `_apply_obs_history`가 즉시 return하므로 **영향이 0**이다.

즉 **이력 arm(NA_histzero, N4_hist)만 보행 프로토콜에서 추가 페널티를 받는다.**
NA의 보행 낙상 61회(대 N1 10회)를 "이력이 보행에 도움이 안 된다"로 읽기 전에
이 교란을 통제해야 한다. ⚠️ 크기는 정량화하지 않았다 — 가설이지 판정이 아니다.

부수 문제: 이 wipe는 **실기에 존재하지 않는다.** 로봇의 관측 버퍼는 목표가 바뀌어도
안 지워진다. 정책이 "이력이 퇴화했다 = 목표가 방금 점프했다"를 단서로 쓸 수 있고,
그건 배포에서 없는 신호다.

#### B6. `900-N1_long.sh`에 이미 고친 버그가 남아 있다

```bash
D=$(ls -1td logs/K1/K1/Goal_Pose_V7/*_N1_path 2>/dev/null | head -1)
```

**시간순 최신 선택** — §8-47이 *"시간순 최신은 '내가 원하는 run'의 안전한 대용이 아니다"*라고
기록하고 `tools/pick_run.sh`를 만든 바로 그 버그다. plan/의 다른 스크립트는 전부 `pick_run.sh`를
쓰는데 이것 하나만 안 고쳐졌다. N1_path 이름으로 스모크가 하나 생기면 그 run을 집는다.

(같은 계열: `export_model.py:38-52`의 fallback도 "logs 아래 mtime 최신 `.pth`"다.
`eval_goal_pose`의 같은 fallback이 `N0_ctrl` 채점을 오염시켰던 그 구조다.)

---

### C. 실기 필터 축

#### C-1. 감사 결론은 유지된다 — 그리고 새 사실 하나

`FILTER_AUDIT-non-codex.md` §2는 *"`sleep(0.001)`은 속도 제한이 아니라 데드라인 폴링"*이라고
적었다. **맞다. 그런데 그 루프에는 `sleep`이 두 개다:**

```python
# deploy_goal_pose.py:1613-1685
while self.running:
    if time_now < self.next_publish_time:
        time.sleep(0.001);  continue      # ← 감사가 인용한 것 (데드라인 폴링)
    ...필터...
    self._send_cmd(self.low_cmd)
    time.sleep(0.001)                     # ← :1685. 발행마다 무조건. 감사가 안 봤다
```

`:1685`는 **모든 발행 뒤에 무조건** 1 ms를 잔다. 이건 폴링이 아니라 **하드 상한**이고,
발행률의 이론 최대를 ~1000 Hz로, 실질적으로는 그보다 낮게 묶는다.

**그래도 결론은 안 바뀐다**: 50 Hz(발행당 20 ms)가 되려면 발행 스레드가 20 ms 중 19 ms를
GIL에 굶어야 한다. 추론은 `Linear(54→256→128→128→12)` 한 번이고 torch는 matmul 중 GIL을
놓는다. 실측된 유일한 열화는 *"추론 루프 설계 20 ms 대 실측 25.3 ms"*(+27 %)인데,
같은 비율이면 발행은 ~390 Hz다. **`pub_hz`가 50 근처일 근거는 여전히 없다.**

그리고 `deploy_base_walk.py`는 필터(`:176`)·루프 구조(`:168`)·**두 번째 sleep(`:195`)까지
전부 같다.** 사용자의 반증(*"base_walk는 걷는다"*)은 **더 강해졌다.**

→ **`pub_hz` 30초 실측(R6)이 여전히 유일한 판별 측정이다.** 감사의 §3 권고 유지.

#### C-2. ⛔ MuJoCo와 Isaac이 배포 정책에 대해 정반대로 말한다 — 아무도 안 짚었다

같은 정책(`I3b_stance10 model_200`), 같은 실물 질량(19.666 kg), 같은 120 s:

| 출처 | 결과 |
|---|---|
| `HANDOFF_MUJOCO_FILTER` §3 (MuJoCo, `K1_serial_realmass.xml`) | **낙상 0** / 47구간 47도착 / 6.70 cm |
| `ibatch` §8-47 (Isaac, `K1_robot_boxfoot.urdf`) | **낙상 2,488 / 4,279 구간 (36.8 %)** |

두 문서가 나란히 저장소에 있고 서로를 언급하지 않는다.

**차이를 찾았다 — 프로토콜이다.** 공통 Isaac 평가는 외란이 켜져 있다:

```yaml
# sweeps/N0_ctrl.yaml:420
disturbance:
  enabled: true
  interval_s: [8.0, 14.0]
  collision: {force_n: [40, 100], torque_nm: [3, 20]}
```

MuJoCo 기준선은 외란·DR·노이즈가 전부 0이었다.

**따라서 두 결과는 모순이 아니고, 합치면 이렇게 읽힌다**:

> 배포 정책은 실물 질량 위에서 **깨끗한 조건이면 걷는다.** 그러나 같은 질량 위에서
> **외란·DR·노이즈가 붙으면 실물 자산으로 학습한 arm 대비 200배 부서진다**
> (0.368 대 0.0017).

⚠️ 단, §8-47의 *"평가 동역학에서 다른 것은 **자산 하나뿐**"*이라는 주장은 **아직 검증되지
않았다.** 그 비교의 대조군(= 배포 정책을 **공통 프로토콜 + 옛 자산**으로 채점)이 없다.
평가 1회로 자산 대 프로토콜을 가를 수 있다(§4-C1).

#### C-3. `--rate-fixed-filter`에 MuJoCo가 못 본 구간이 있다

수정본(`deploy_goal_pose.py:1652`):

```python
alpha = 1.0 - math.exp(-max(_dt, 1e-6) / self._filter_tau_s)
```

`_dt`는 **실측** 발행 간격이다. MuJoCo 검증(`play_mujoco_goalpose.py:493-502`)은
`step = 1.0/filter_hz`라는 **완벽히 규칙적인** 값을 썼다.

즉 **스케줄링 튐(jitter/stall)에서의 거동은 검증된 적이 없다.**
루프가 한 번 100 ms 멈추면 `alpha = 1 − exp(−10) ≈ 1.0` → 그 순간 필터가 사라지고
**모터에 계단 명령이 나간다.** 기존 고정계수 0.2는 그 순간에도 20 %만 통과시켰다.
게다가 `next_publish_time += dt`가 catch-up 누산기라 stall 직후에는 밀린 만큼 몰아서 발행한다.

**권고는 안 바뀐다(켜라).** 다만 **첫 실기 실행에서 이 한 줄을 같이 넣는 것이 싸다**:

```python
alpha = min(alpha, 1.0 - math.exp(-3.0 * self.cfg["common"]["dt"] / self._filter_tau_s))
```

(= 정상 주기 3배 이상의 튐은 3배로 취급. `_dt`를 클램프해도 같다.)

#### C-4. 그래서 필터 축의 현재 위치

| 후보 | 상태 |
|---|---|
| 질량·관성 오차 | **기각** (6.73 → 6.70 cm, 낙상 0 → 0) |
| 토크 상한 불일치 | **기각** (§8-39②) |
| 떨림/채터링 | **기각** (Nyquist 대역 0.3–1.8 %) |
| 배포 필터 *존재* 자체 | **기각** (§8-39③) |
| 관측 지연 단독 | **기각** (수정 필터 켜면 20 ms 지연에도 200 Hz 낙상 0) |
| **필터가 루프 속도에 의존** | **유력하지만 전제(`pub_hz`가 낮다)가 미측정** |
| **학습에 필터가 아예 없다** | ✅ **확정** — `goal_pose.py`의 PD가 날것 목표를 받는다. `--rate-fixed-filter`를 켜도 로봇에는 τ=10 ms가 남고 sim에는 0이다. `NC_actfilter`가 지금 이걸 메우는 중 |
| **b~r 구간 자세가 개루프로 못 선다** | ✅ 원인 확정(tilt 4.9–13.8° / 관절 추종 0.6°), 수정 투입, **검증 미실행** |
| 200 Hz 비단조 이상 | **원인 미상**. 수정 필터가 지우므로 실기 권고엔 무영향 |

---

### D. 🔴 배포 파이프라인 — 관측 넓힌 arm은 export가 불가능하다

아직 어느 문서에도 없다. `export_model.py:32`:

```python
cfg_file = os.path.join("envs", "{}.yaml".format(args.task))   # ← 항상 베이스 config
...
model = ActorCritic(cfg["env"]["num_actions"],
                    cfg["env"]["num_observations"],       # = 54  (베이스)
                    cfg["env"]["num_privileged_obs"])     # = 14  (베이스)
model.load_state_dict(model_dict["model"])                # ← strict=True
```

arm 별 `sweeps/*.yaml`을 **안 읽는다.** 그러므로:

| arm | 학습 폭 | export 결과 |
|---|---|---|
| N0/N1/N2/N8/NZ/NC/ND | 54 / 14 | ✅ 정상 |
| **NA_histzero** (사용자 직접 요청) | **270** / 14 | ⛔ `size mismatch` |
| **N4_hist** | **270** / 14 | ⛔ |
| **N5_tau** | **66** / 14 | ⛔ |
| **N6_foot** | **56** / 14 | ⛔ |
| **N7_critic** | 54 / **29** | ⛔ |

`tools/install_policy.sh:145-147`도 스모크가 `(1,54)`로 **하드코딩**돼 있다.

역설적인 것은 **배포 런타임은 이미 지원한다**는 점이다 —
`deploy/utils/policy_goal_pose.py:100-116`이 `history_steps`·`extra_foot_offset`·
`extra_dof_tau`로 폭을 계산하고 대조한다(§8-45가 맞춰 놓았다).
**끊긴 곳은 export 한 곳뿐이다.** 고치는 비용은 작다(§4-D).

⚠️ 이건 지금 **12개 N arm 중 5개**(NA·N4·N5·N6·N7)의 출구를 막고 있고,
그중 하나는 **사용자가 직접 요청한 arm**이다.

---

### E. 운영 축

**E-1. `autopilot.sh`가 6개 돌고 있다** (정상 1 + 잔여 5)

```
2405616  cwd=/mnt/DATA/.../htwk-gym            ← 정상
2406305  cwd=/tmp/tmp.bx5KW3Qbzt (deleted)     ┐
2406337  cwd=/tmp/tmp.bx5KW3Qbzt (deleted)     │ tools/test_autopilot.sh 잔여
2406390  cwd=/tmp/tmp.bx5KW3Qbzt (deleted)     │ (17:2x 자체 시험)
2406434  cwd=/tmp/tmp.bx5KW3Qbzt (deleted)     │
2406477  cwd=/tmp/tmp.bx5KW3Qbzt (deleted)     ┘
```

원인: `test_autopilot.sh`가 `trap 'rm -rf "$TMP"' EXIT` + `kill $(cat "$TMP/.pid")`로
**PID를 하나만** 정리하는데, 시험이 autopilot을 여러 번 띄운다.

**영향 평가 — 지금은 무해하다.** 그들의 `$ROOT`가 지워진 디렉터리라 `idle_state.json`을
못 읽고, 그러면 `idle`·`stall`이 항상 0이라 **승격도 복구도 안 한다.** 120초 sleep만 돈다.
`idle_watch.sh`/`round_status.sh`는 autopilot을 세지 않으므로 계수 오염도 없다
(둘 다 `train_v7.py` 계열 python과 `tools/gpu_queue.sh` bash만 센다).

⚠️ 그래도 정리하는 게 맞다. 그리고 **자체 시험 도구가 또 먼저 틀렸다** —
인계 문서가 *"검증 도구 자체가 먼저 틀린 경우가 두 번"*이라고 적었는데 이게 세 번째다.

정리 명령(안전 — PID 지정, `pkill -f` 아님):

```bash
ssh a6000 'kill 2406305 2406337 2406390 2406434 2406477'
```

**E-2. 큐 순서가 가장 급한 답을 3시간 뒤로 미룬다**

`pareto_fine`은 **읽기 전용 평가**다(256 env, ~4 GB, 8체크포인트 × 2축 × 60 s ≈ 30분).
그런데 워커가 카드당 하나라 6000-iteration 학습 뒤에 줄을 선다.

| 지금 | ~20:30 | ~21:15 | ~00:50 |
|---|---|---|---|
| NC / NZ 학습 | `pareto_fine` ×2 시작 | pareto_fine 결과 | ND_dwell / N4_hist 완료 |

카드는 **49 GB 중 3.9 GB**만 쓰고 있다. 평가를 지금 병렬로 돌리면 절벽의 모양을
**3시간 일찍** 알 수 있고, 그 답이 `ND_dwell`을 돌릴지 말지를 결정한다(§3-B3와 겹친다).

**E-3. 미채점 run 21개**가 그대로다(M1/M2/M5/M8/M9, L1/L4, I3_rough 등).
`M9_anklegain`·`M8_jointcal2`처럼 **한 번도 두 축으로 안 재본 arm**이 섞여 있다.

---

## 4. 해결방안 분석

수렴시키지 않는다. **축별로 따로 적고, 각각 "무엇을 판정하는가 / 비용 / 위험"을 붙인다.**

### A. 과제 축 — 두 축을 양립시키는 길 네 가지

| # | 방법 | 무엇을 판정하나 | 비용 | 위험 |
|---|---|---|---|---|
| **A1** | **`pareto_fine`** (큐에 있음) — iteration 100–900을 8점으로 | 절벽인가 무릎인가. 무릎이면 **오늘 배포 후보가 나온다** | 30분 ×2 | 없음(읽기 전용) |
| **A2** | **`ND_dwell`을 duty를 고쳐서** 돌린다 | "dwell을 올리면 양립하는가" | 3.6 h | **지금 설정은 의도의 1.7배**(§3-B3). 고치지 않으면 3.6시간이 N0 재현으로 끝난다 |
| **A3** | **2단계 학습** — path로 걷기를 익히고 waypoint로 finetune | 순차 학습이 충돌을 푸는가 | 3.6 h + 1–2 h | 레버가 아니라 절차라 비쌈. 그런데 **A1이 절벽을 확인하면 이게 남는 최선**이다 |
| **A4** | **carrot을 잡을 수 있게 만든다** — `catchup_ratio`를 로봇 속도에 묶거나 floor를 속도 의존으로 | 가설(§3-A) 자체를 직접 친다 | 코드 + 3.6 h | 미설계. 그러나 **가설을 직접 재는 유일한 안** |

**A2 수정안 두 가지** (하나만 고르면 된다):

```yaml
# 안 1 — config만. 코드의 실제 산식(duty = duration/interval)에 맞춘다
ND    interval_s: [8.0, 12.0]   duration_s: [3.0, 5.0]   →  duty 40 %
```

```python
# 안 2 — 코드를 설계 의도에 맞춘다 (goal_pose_v7.py:325)
#   interval 타이머가 dwell이 끝난 뒤부터 돌게 한다
self.path_dwell_next[fire] = (dwell_left[fire] + interval 추첨)
#   그러면 기존 [12,24]가 18.2 %, ND의 [4,8]이 40.0 % — 주석의 산식 그대로가 된다
```

⚠️ 안 2는 **N1/N2가 이미 학습한 조건도 22.2 % → 18.2 %로 바꾼다.** 지금까지의 arm과
비교하려면 안 1이 안전하다. 안 2를 고르면 그 사실을 arm 정의에 적어야 한다.

**A5 (선행 조건, 거의 공짜)**: **`N0_ctrl model_6000`의 정확도를 잰다.** 평가 1회.
이게 없으면 §3-B1의 구멍이 계속 남고, 파레토 프론트에 N0를 못 올린다.

### B. 측정 축 — 구멍 메우기

| # | 무엇 | 비용 | 우선도 |
|---|---|---|---|
| **B1'** | `N0_ctrl model_6000` 정확도 1회 + `eval_round.sh`에 **5/5 단계(마지막 체크포인트 정확도)** 추가 | 평가 3분 + 코드 5줄 | 🔴 최상 |
| **B2'** | 큐 스크립트의 `best.pth` → **명시적 체크포인트**로 고정. 최소한 스크립트 주석을 사실에 맞게 고친다. 그리고 **누락된 대조군**(N1@100 + plain N1 6000)을 plan/에 넣는다 | 주석 즉시 / 대조군 3.6 h | 🔴 최상 |
| **B3'** | `ND_dwell` duty 수정 (§4-A2) | 즉시 | 🔴 (돌기 전에) |
| **B4'** | 게이트에 **비율 게이트**를 병기 (`fall_rate_per_attempt`와 그 부트스트랩 CI). 사용자 게이트 `max_falls: 0`은 **그대로 두고** 옆에 한 열 더 | 코드 10줄 | 🟠 |
| **B5'** | 이력 wipe를 **에피소드 리셋에서만** 하도록 분리 (목표 재추첨에서는 유지). 또는 최소한 `forward_hold` 결과를 읽을 때 이 교란을 명시 | 코드 5줄 | 🟠 (N4_hist 돌기 전에) |
| **B6'** | `900-N1_long.sh`의 `ls -1td` → `pick_run.sh`. `export_model.py`의 mtime fallback도 같이 | 2줄 | 🟡 |

### C. 실기 축

| # | 무엇 | 비용 | 판정 |
|---|---|---|---|
| **C1** | **배포 정책을 공통 프로토콜 + 옛 자산으로 1회 채점** | 평가 3분 | §3-C2의 "자산 하나뿐인가"를 가른다. 지금 그 주장은 대조군이 없다 |
| **C2** | **`--hold-diag 15`의 tilt** (로봇) | 15초 | 2° 안쪽 = 진입 자세 수정 성공 / 4.9–13.8° 그대로 = 관절 영점(R4) |
| **C3** | **`--rate-fixed-filter --log-timing`로 30초** (로봇) | 30초 | `pub_hz` 하나로 필터 서사 전체와 base_walk 반증이 동시에 닫힌다 |
| **C4** | C3 전에 alpha 클램프 한 줄(§3-C3) | 1줄 | stall에서 계단 명령이 나가는 미검증 구간을 막는다 |
| **C5** | 로봇 잔여 프로세스 두 개(33763/42133) 정리 | — | ⛔ **C2/C3보다 먼저.** 발행자 둘이 동시에 살아 있다 |

⛔ **C2·C3·C5는 로봇이 필요하고 지금 로봇은 네트워크가 안 닿는다.**
Mac 시스템 설정 → 네트워크 → 서비스 순서에서 **Wi-Fi를 이더넷 위로** 올리는 것이
영구 해법이라고 `HANDOFF_ROBOT` §4가 기록해 뒀다.

### D. 배포 파이프라인

```python
# export_model.py — arm의 sweep config를 받을 수 있게
parser.add_argument("--config", type=str, default=None)
cfg_file = args.config or os.path.join("envs", "{}.yaml".format(args.task))
```

그리고 `install_policy.sh:145`의 `(1,54)`를 config의 `num_observations`에서 읽게 바꾼다.
**둘 다 합쳐 10줄 미만.** 이게 없으면 NA/N4/N5/N6/N7은 아무리 좋아도 로봇에 못 올린다.

⚠️ 그리고 배포 config(`Goal_Pose_E0.yaml`)에 `policy.history_steps` 등 `observation`
블록을 옮겨 적어야 한다 — 런타임이 그 키로 폭을 계산한다.

### E. 운영

- `kill 2406305 2406337 2406390 2406434 2406477` (§3-E1)
- `test_autopilot.sh`가 띄운 **모든** PID를 기록·정리하도록 수정
- `round_status.sh`에 "autopilot 개수 / cwd" 한 줄 추가 — 지금은 감시자가 몇 개인지 안 보인다
- `pareto_fine` 두 개를 **지금 병렬 실행**할지 결정 (§3-E2)

---

## 5. 앞으로 계획

### 5-1. 지금 (GPU 추가 소모 0 또는 극소, 오늘 안에 답이 나옴)

| 순 | 무엇 | 얻는 것 |
|---|---|---|
| 1 | `ND_dwell` duty 수정 (§4-A2) | **3.6시간이 낭비되는 것을 막는다.** 큐에 있으므로 지금이 마지막 기회 |
| 2 | `N0_ctrl model_6000` 정확도 1회 | §3-B1을 닫는다. 파레토에 N0를 올린다 |
| 3 | 배포 정책 × 옛 자산 1회 | §3-C2를 닫는다 |
| 4 | `pareto_fine` ×2 (지금 병렬 또는 순서대로) | **절벽인가 무릎인가** — 다음 수 전체가 여기 달렸다 |
| 5 | 잔여 autopilot 5개 정리 | 운영 위생 |
| 6 | `export_model.py` + `install_policy.sh` 폭 수정 | 관측 arm 5개의 출구를 연다 |
| 7 | 큐 스크립트 주석 정정 + `N1_long`의 `ls -1td` 교체 | 다음 세션이 잘못 읽지 않게 |

### 5-2. 오늘 밤 (GPU)

| 조건 | 다음 |
|---|---|
| pareto_fine에 **무릎이 있으면** | 그 체크포인트가 배포 후보다. 즉시 두 축 정식 채점(120 s) → export → 로봇 |
| pareto_fine이 **절벽이면** | `ND_dwell`(수정본) **또는** 2단계 학습. 둘 중 하나만 — 카드 두 장에 `ND_dwell` + **누락 대조군**(N1@100 + plain 6000)을 같이 태우면 §3-B2도 같이 닫힌다 |
| `NC_actfilter` / `NZ_zeroiid` 완료(~20:30) | 두 축 채점. **NC는 `pub_hz` 논쟁과 무관하게 값어치가 있는 유일한 학습 레버다** |

### 5-3. 로봇이 돌아오면 (순서 고정)

```
1. 잔여 프로세스 두 개 정리 (33763은 로봇 상태 확인 후 — cleanup이 DAMPING을 건다)
2. --hold-diag 15        →  tilt 하나로 진입 자세 판정
3. --rate-fixed-filter --log-timing 로 30초 서 있기  →  pub_hz
4. (3이 통과하면) 짧은 목표로 걷기 A/B
```

⚠️ 다른 터미널을 미리 열어 둘 것. 정지는 `touch /tmp/e0_abort`.

### 5-4. 그 다음 분기

- **양립 지점을 찾으면** → export → 로봇 → 미션 1–5
- **못 찾으면** → 사용자에게 **트레이드오프를 명시적으로 물어야 한다.**
  지금 선택지는 두 개다:
  - `MA_feetcross` / `N0_ctrl` 계열: **2.7–2.8 cm 도착, 0.98–1.10 m/s** (G1 위치 ✅, G2 속도 ❌)
  - `N2_pathgrid` 계열: **1.54–1.64 m/s, 42–44 cm 도착** (G2 ✅, G1 ❌)

  RoboCup 관점에서 어느 쪽이 쓸모 있는지는 **내가 정할 문제가 아니다.**
  참고: 실기 미션 도달 임계는 `goal_reached_xy_m: 0.10` / `theta 6°`(missions.md)이므로,
  42 cm는 어떤 미션도 통과 못 한다. 반대로 0.98 m/s는 느릴 뿐 미션을 통과한다.
  → **지금 배포 가능한 유일한 계열은 정확도 쪽이고, 속도 요구는 그 다음 라운드다.**
  이건 §1의 파레토 결과가 사실상 이미 말하고 있는 것이다.

---

## 6. 열려 있는 것 / 내가 확인하지 못한 것

| # | 항목 | 왜 못 했나 |
|---|---|---|
| 1 | 로봇 잔여 프로세스, `hold-diag` tilt, `pub_hz` | 로봇 네트워크 불가 |
| 2 | `N0_ctrl model_6000` 정확도 | 측정된 적이 없다 |
| 3 | `NA_histzero model_6000` 정확도 | 같은 이유(4/4단계가 보행만 잰다) |
| 4 | §3-B5(이력 wipe)가 NA의 보행 낙상 61회를 얼마나 설명하는가 | 정량화 안 함 — **가설이다** |
| 5 | §3-A의 "carrot을 못 잡아서 무너진다" | 직접 측정된 적 없음 |
| 6 | 200 Hz 비단조 | 원인 미상 (수정 필터가 지움) |
| 7 | `M4_scratch_phasefree` NaN(iteration 6,171) | 미조사 |
| 8 | 미채점 run 21개 | 미실행 |
| 9 | `HANDOFF_TO_TRAINING.md` ACK | 미작성 |
| 10 | `E1_path` 평가 | N 배치로 대체됐고 그 사실이 사용자에게 전달 안 됨 |

---

## 7. 재검토 절차 — 무엇을 어떻게 확인했나

요약표를 믿지 않고 **매번 1차 자료로 돌아가** 확인했다. 통과/발견 기록:

| # | 무엇을 | 어떻게 | 결과 |
|---|---|---|---|
| 1 | 문서 전수 | 인계 6종 + `ibatch` §8-44~8-49 + `user_requirements.md` 정독 | 서사 파악 |
| 2 | 서버 상태 | `round_status.sh` + `ps` + `nvidia-smi` 교차 | **N4_hist rc1 / NZ rc1** 발견 (문서에 없음) |
| 3 | 파레토 | 서버에서 `pareto_table.py` 직접 실행 | 인계 문서 값과 일치 |
| 4 | 파레토 프론트 | `pareto_table.py` 알고리즘을 **손으로 재계산** | 8/8 일치. 자기지배 버그 없음 |
| 5 | 두 축 표 | 요약 대신 **모든 `report.json`을 파싱** | **`N0_ctrl` 오염 행이 `N1_final`과 바이트 동일** 확인. `N0@6000 정확도 부재` 발견 |
| 6 | 속도 숫자 | `2 − pos_err ≈ 속도`로 **독립 산술 검증** | N0/N2 ✅, **I3b만 불일치** = 낙상이 속도로 잡힘 |
| 7 | 지배 주장 | 낙상률·속도를 원본에서 재대조 | **§1-4가 §1-3과 모순** 확정 |
| 8 | warm start | **학습 로그의 `Loading model from`** 확인 | 계보 2개 확정. 문서와 코드 불일치 |
| 9 | dwell 산식 | `goal_pose_v7.py:316-327` 정독 + 확률 계산 | duty **22.2 % / 66.7 %** (주석은 18.2/40.0) |
| 10 | 필터 | `deploy_goal_pose.py:1613-1685` **전문** + `deploy_base_walk.py:167-195` 대조 | **두 번째 `sleep`** 발견. base_walk 반증 강화 |
| 11 | 관측 버퍼 | `_reset_idx` → `_resample_goals` 호출 경로 추적 | wipe가 **목표 재추첨마다** 발생 확인 |
| 12 | 자산 | 서버에서 **URDF 질량 직접 합산** | 19.6660 / 18.7142 kg — 문서와 일치 |
| 13 | MuJoCo↔Isaac 모순 | 공통 config의 `disturbance` 블록 확인 | 프로토콜 차이로 **화해 가능**. 단 대조군 없음 |
| 14 | 게이트 | `sweeps/N0_ctrl.yaml:672` + `eval_goal_pose.py:2319` | 사용자 G1 그대로. 전 arm `fail` |
| 15 | export 경로 | `export_model.py` + `install_policy.sh` + `policy_goal_pose.py` 3개 대조 | **런타임은 되는데 export가 막혀 있다** |
| 16 | 프로세스 | `/proc/<pid>/cwd` 확인 | 잔여 5개가 **삭제된 tmp** → 무해 확정 |

**내가 이 문서에서 틀렸을 수 있는 곳** (검증 강도가 낮은 순서):
§3-B5(이력 wipe 영향 — 정량화 안 함) > §3-A 가설(carrot) > §3-B3의 "env 20 %가 영구 dwell"
(해석적 계산이고 시뮬레이션으로 확인 안 함) > 나머지는 1차 자료 직접 확인.
