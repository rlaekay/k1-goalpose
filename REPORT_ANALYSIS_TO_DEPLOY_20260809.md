# 분석 세션 → 배포 세션 : 실기 로그 분석 + 모델 감사 상세 보고 (2026-08-09)

10회 독립 고정질문 감사 + 데이터 분석 감사 3건(신호/방법론/출처)의 **데이터 결과만**
모았다. 판단·권고는 `MIDREPORT_DEPLOY_20260809.md` 의 답신 절에 있다. **여기는 숫자다.**

⚠️ 모든 항목에 **출처와 표본 수**를 붙였다. 확인 못 한 것은 "미확인"이라고 적었다.
⚠️ 이 캠페인은 철회 50건을 쌓았다. **이 문서도 틀릴 수 있다** — 각 절 끝에
"무엇이 이것을 뒤집는가"를 적었다.

---

## 1. 실기 시계열은 **2개**다 (전수 조회로 확정)

서버 CSV **745개** + 맥 텍스트 **1,236개**를 헤더 서명으로 묶고 **분산 판별**을 적용:
**SIM 167 / REAL 2 / 모호 0**.

| | 파일 | 위치 | 행 | 길이 | `age` 고유값 | `tick` 고유값 |
|---|---|---|---:|---:|---:|---:|
| **R1** | `real_walk.csv` | **서버만** `htwk-gym/logs/mujoco/` | 91 | 2.379 s | 91/91 | 91/91 |
| **R2** | **`t_stand.csv`** | **맥만** `server-dump/` | 6,330 | 184.1 s | 5,210 | 5,303 |

**판별 근거**: MuJoCo 기록부(`play_mujoco_goalpose.py:498-500`)는 **리터럴 상수**를 쓴다 —
`tick_dt_s[0]=0.02`, `low_state_age_s=0.001` 고정. 알려진 MuJoCo 파일 6개 전부 **고유값 1, sd 0**.
실기 로그는 **행마다 다르다**.

⛔ **`pub_hz` 열 부재는 반증이 아니다** — 그 열은 커밋 `5e4b306`(08-05 **23:53:07**)에
추가됐고 `real_walk.csv` mtime 은 **23:34** 다. 19분 먼저 만들어졌다.
⚠️ 나는 이것을 **두 번 틀렸다**(C41 → C43). 처음엔 파일 **이름**으로, 다음엔 디렉터리
**이름**과 헤더로 판정했다. **둘 다 메타데이터였다. 데이터 통계를 봤어야 했다.**

**그 외 실기 산출물 2개** (저장소 밖, 아무 문서도 참조 안 함):
`~/RoboCup/measurements/robot_layout_20260804.json` · `mode_matrix_20260804.json`
⭐ 후자의 `low_hz` **499.06~499.30** 이 **`ibatch §8-41` "499.2 Hz" 의 진짜 출처**다 —
모드 전환 프로브 산출물이므로 "배포 프로세스 값이 아니다"가 산출물 수준에서 확인된다.

⚠️ **실기 데이터 4개가 각각 한 곳에만 있고 전부 버전관리 밖이다**(총 3.4 MB).
지우면 로봇을 다시 세워야 복구된다.

> **뒤집는 측정**: `logs/robot/` 을 새로 만들고 CSV 첫 줄에
> `# source=robot host=... policy=<sha> utc=...` 를 자동 기록하면 이 판별이 불필요해진다.

---

## 2. 관절 매핑 — **확정됐다** (근거 4개, 추론 아님)

`q0..q11 = motor_state_serial[10..21] = L(Hip_P, Hip_R, Hip_Y, Knee_P, Ankle_P, Ankle_R) → R 동일`

1. **하드웨어 지문**: 로봇에서 덤프한 `parallel_mech_indexes [14,15,20,21]` 대 벤더 SDK enum
   크랭크 `15,16 / 21,22` → **정확히 −1 시프트**(원인: `kWaist=10`, 이 K1엔 없음)
2. **부호 비대칭**: `measured_q[5] = −0.497` / `[9] = +0.499`. Elbow_Yaw 가 좌 `[-2.44,0]`·
   우 `[0,2.44]` 로 **부호까지 반대인 하드 스톱** ⇒ 0~9 블록에 시프트 없음이 단독 증명
3. **PD 항등식 회귀 (3,961행)**: `kp_j(default+act−q) − kd_j·dq` 가 **12슬롯 전부**
   `tau_j` 와 **최대 0.3 N·m** 안에서 맞는다 ⇒ q/dq/tau/act 블록 간 치환 없음
4. **하드 스톱 접촉**: `q10` 하한 −0.8624 가 Ankle_Pitch 스톱 −0.87 에 **0.0076 rad
   (0.44°) 남기고 붙는다.** Ankle_Roll 이면 2.5배 초과 = 불가능 ⇒ 슬롯4=Pitch, 5=Roll 확정

⚠️ **한 쌍만 미확인**: 슬롯1 vs 2 (Hip_Roll ↔ Hip_Yaw). 둘 다 default 0 / kp 100 / effort 30
이라 위 검사가 못 가른다. **닫는 측정**: 무부하에서 Hip_Roll 만 손으로 벌리고 인덱스 판독.

## 2-b. `motor_state_serial` = **관절각** (액추에이터 좌표 아님, 근거 5개)

① `cmd_type = LowCmdType.SERIAL` ② 명령 `default_qpos` 다리 6값이 실측 `q0..q5` 와 동시 일치
③ PD 회귀가 발목을 **serial 공간 kp=50** 으로 설명(par/ser 비 L +1.147 / R **−4.980** 이므로
크랭크였다면 R 이 5배·부호까지 틀어져야 함) ④ 손으로 민 물리 스톱이 serial 로 **±0.39~0.41**
⑤ **평행 좌표는 CSV·관측·명령 어디에도 안 들어간다**(읽는 곳 3군데 전부 진단용)

⇒ **`sim ↔ deploy` 관절 순서 3자 일치**(Isaac boxfoot / MuJoCo `K1_serial.xml` / SDK serial).

---

## 3. 타이밍 — 실기에서 실제로 잰 것

| 양 | 실측 | 설계 | 출처 |
|---|---:|---:|---|
| 제어 틱 `tick_dt_s` median | **25.24 ms** | 20 ms | R1 |
| ⇒ 제어율 | **39.6 Hz** | 50 Hz | 〃 |
| 샘플링 CV | **0.303** (11~48 ms, 4.36배) | — | 〃 |
| median ±10 % 안에 드는 간격 | **31 %** | — | 〃 |
| `gait_process` 실효 주파수 | **1.5116 Hz** | 2.0 Hz | 〃 (언랩 적분) |
| 틱당 위상 증분 | **0.04000 고정** | 0.04 | 〃 |
| `low_state_age_s` median | **1.02 ms** | — | 〃 |
| `pub_hz` median | **185.1 Hz** (p10 183.7 / p90 186.5) | 500 Hz | 배포 세션 직접 측정 |

**기전**: `Timer.get_time()` 이 벽시계가 아니라 **LowState 카운터 × 0.002** 다
(`tick_timer_if_sim()` 호출 지점은 `_low_state_handler` 한 곳). 위상은 **틱당 고정 0.04**
로 오르므로 **틱이 늦으면 보행 전체가 시간 팽창한다.** 검산: 2.0 × (378/500) = 1.512 ✓
⚠️ `advance_gait_clock` 의 docstring 은 *"measured rather than assumed … in wall clock"*
이라고 적는다 — **거짓이다.**

⚠️ **`low_state_age` 1.02 ms 는 정상이다** ⇒ 느린 것은 **센서가 아니라 루프**다.

> **뒤집는 측정**: `--log-timing` 을 rclpy 모니터 3개를 끈 상태로 반복. `pub_hz` 가
> 475 이상이면 rclpy 원인 확정, 420 이하면 SDK/DDS 쪽.

---

## 4. 발목 roll — **두 로그 모두에서 유일하게 이상한 관절**

### 4-a. 재현되는 사실

| 관절 | `r1(q)` (R2, n=6,330) | rms `dq` (R2) |
|---|---:|---:|
| 다른 10관절 | **0.945 ~ 0.991** | 0.41 ~ 0.92 |
| **L_Ankle_Roll** | **0.665** | 1.467 |
| **R_Ankle_Roll** | **0.694** | 2.026 |

R1(n=91)에서도 rms `dq` 최대(**7.0 / 8.9** 대 다른 관절 1.9~3.3).
정직한 분모(다음으로 큰 관절)로 **2.09배 [1.19, 3.13]** / **2.64배 [1.63, 3.85]**(부트스트랩).

### 4-b. ⛔ 계측 결함이 **아니다** (R2 로 판별 — 로봇 없이)

- **조용한 창**(다른 10관절이 전부 <5 mrad, 전체 틱의 **70.7 %**)에서 발목 roll 틱간 변화:
  **median 0.00000 / p99 0.0045·0.0057 / max 0.010·0.019 rad**.
  다른 관절(p99 0.0031~0.0042)과 **같은 자리** ⇒ **가만히 있을 때 안 튄다**
- 발목 roll 상위 1 % 틱에서 **몸통 자이로가 기준선의 113배**로 반응(무릎 87배) ⇒ **자이로가 본다**

⇒ `r1(q)=0.67` 의 정체는 결함이 아니라 **"대부분 조용하다가 순간 스냅한다"** 는 실제 동역학.

### 4-c. ⭐ **명령이 아니라 응답이다**

- `act5`(왼 발목 roll 명령)는 12개 명령 채널 중 **유일하게 순열검정 실패**(p=0.39).
  나머지 11개는 p ≤ 0.033 ⇒ **그 채널의 명령에는 시간구조가 아예 없다**
- `r1(dq5)` = **−0.29 / −0.30** — 12개 중 **유일한 음수**(나머지 전부 양수)
- 9~14 Hz 대역 파워: **명령 0.058/0.025 대 응답 0.140**(평평 기준선 0.050)

⇒ **정책이 흔드는 것이 아니라 플랜트가 만든다.**

### 4-d. ⛔ 주파수는 **확정 못 한다** (내 앞선 보고를 철회한다)

- 내가 보고한 **"지배 11~12 Hz"** → **잡음과 구별 불가.** 실제 표본시각 위 백색잡음 400회의
  Lomb-Scargle 최대파워 귀무분포 **p95 = 0.202**, 관측 **0.192**. 순열검정 p=0.028/0.013 으로
  **12채널 Bonferroni 불통과**
- **전 구간 단일 FFT 자체가 잘못**이었다 — 변화점(row 33)으로 가르면 피크가
  **EARLY 85 Hz → LATE 11.9 Hz** 로 이동 ⇒ **"이미 넘어지는 중"의 성질**
- 예측 4.73 Hz(내 C42 · 네 §8-60)는 **에너지가 있으나 지배적이지 않다**(정규화 0.49/0.24)
- **에일리어싱은 판별 가능하고 기각된다**(지터가 축퇴를 깬다) — 다만 "접힌 게 아니다"이지
  "진짜다"가 아니다
- ⚠️ n=91, 2.379 s ⇒ **분해능 0.42 Hz**, 11 Hz 신호가 주기당 3.4표본

> **닫는 측정**: **≥20 s / ≥200 Hz / 다수 런.** 분해능 0.42 → 0.05 Hz, 에일리어싱 질문 소멸,
> 국면별 STFT 성립. **이것 없이는 kd 를 얼마로 할지 정할 수 없다.**

---

## 5. 토크 — 실기가 sim 한계를 넘는다

| 관절 | 실기 실측 | sim 하드클램프(`armsdown`) | 벤더 공식 |
|---|---:|---:|---:|
| Hip_Pitch | **37.8 N·m** | 30 | 68.0 |
| Hip_Roll | **34.0** | **20** | 76.0 |
| Ankle_Pitch | **23.6** | 20 | 38.3 |

**6관절 중 4개가 sim 클램프를 넘는다.** ⇒ 정책이 학습 중 **한 번도 겪은 적 없는 영역**에서
실기가 돈다(`goal_pose.py:866` 이 하드 클램프).
⚠️ 방향 주의: **우리 URDF 가 벤더보다 낮다** — 실기에 없는 토크를 배운 게 아니라 **굶긴 것**이다.

⭐ **계측과 무관하게 확실한 것**: `act5 = −1.000` 인 행이 실재한다 ⇒ 목표 **−1.0 rad**,
실측 하드스톱 ±0.40 의 **2.9배**. 스톱 밖으로 나간 행 **L 3개 / R 9개**.
⇒ **`dof_target` 클램프에 찬성한다. 단 기준은 ±0.345 가 아니라 실측 ±0.40.**

---

## 6. 모델·체크포인트 감사

### 6-a. 로봇 위 정책 (R3 종결)

`I3b_stance10 / model_200`, md5 `60006446e638566c8245bc6013c8780c`, 설치 **2026-08-05 20:44**.
⚠️ `Goal_Pose_E0.yaml:11-13` 주석은 아직 *"I2a_dr model_150"* 이라고 말한다 — **낡았다.**
`md5sum` 이 주석을 기각했다. **주석을 고쳐라**(다음 사람이 또 헷갈린다).

### 6-b. ⛔ 그 정책이 학습한 자산이 **물리적으로 불가능하다**

`K1_locomotion_armsdown.urdf` 대 벤더 공식(`booster_assets/robots/K1/K1_22dof.urdf`):

| 항목 | `armsdown`(배포 계보) | 벤더 = `boxfoot` |
|---|---:|---:|
| 총질량 | 18.714 kg | **19.666** (벤더 사양 ~19.5) |
| 발 질량 | 0.38305 | **0.494** (−22.5 %) |
| 발 `ixx` / `iyy` / `izz` | 0.00202 / 0.00741 / 0.000785 | **0.000261 / 0.001170 / 0.001213** |
| Hip_Pitch origin z | −0.062 | **−0.077** (15 mm) |
| Hip_Pitch 속도한계 | 18 | **7.1** (자산) / 14.66 (벤더 DB) |
| Ankle_Roll effort | 15 | 20 (자산) / **38.3** (벤더 DB) |
| `armature` | **0** | 관절별 0.0283~0.0956 (벤더 DB) |

⭐⭐ **그 발은 존재할 수 없다.** 관성 텐서 삼각부등식:
```
Izz + Ixx >= Iyy :  0.002805  <  0.007410   ⛔ 2.64배 위반
```
**어떤 강체도 그런 주관성을 가질 수 없다.** 입력 오류다. `boxfoot` 은 세 부등식 전부 만족하고
**벤더 자산과 비트 단위로 같다.**

### 6-c. ⛔⛔ 우리가 체크포인트를 고르던 지표가 **인과적으로 반대다**

`NJ_armasset / model_6000` **하나**를 두 채점 물리로 (checkpoint·protocol_sha·env_code_sha·
duration·num_envs·seed **전부 동일**):

| 채점 물리 | 오차 median | **낙상** |
|---|---:|---:|
| 자기 물리 | **38.75 cm** | **2** |
| 틀린 물리(`armature: 0`) | **27.26 cm** | **6,145** |

⇒ **65 % 확률로 넘어뜨렸더니 정확도가 30 % 좋아졌다.** `NG_armature` 도 40.96 → 35.91 cm.
원인은 `eval_goal_pose.py:1808` `completed = changed & ~done` — **낙상 구간은 기록조차 안 된다.**
카테고리 표준화로는 3.10 → 3.21 cm(+0.11)뿐 ⇒ **재가중으로 못 고친다. 분모를 시도로 바꿔야 한다.**

`I3b_stance10` 시도 단위 환산: 생존자 median 3.10 cm(**정확도 3위**) → **하한 4.74 cm**,
P(성공|시도) 70.5 → **44.6 %**, **순위 3위 → 11위.**

⇒ **정확도 cm 로 배포 후보를 고르지 마라.** 내가 `N0_ctrl/model_6000` 을 제안했다가 철회한 이유다.

### 6-d. ⛔ 낙상 수는 체크포인트만의 성질이 아니다

`select` ↔ `winner_video` **쌍대 재측정 28쌍**(같은 체크포인트·duration·envs·seed·terrain):
- 낙상 부호가 `select < video` 로 **23 대 4, 부호검정 p = 0.0000**. 11/28 이 p<0.05
- 최대 불일치 **1 대 37**, **0 대 17**
- 반면 `pos_err median` 불일치는 **1.09 %** ⇒ **정확도 축은 재현된다**

⇒ **`best.pth` 를 고르는 `.select` 낙상 수가 낙관 편향돼 있다.** 원인(렌더링 대 env 재사용)은
**미확인**이고 fresh 2회로 갈린다.

### 6-e. 통계적 검정력 — 우리가 "개선"이라 부른 것들

- **`CLAUDE.md` 최상단 "낙상간격 2배" = 낙상 1건 대 2건.** 율 비 0.502,
  **정확 95 % CI [0.0085, 9.6]**(폭 1,100배), **p = 1.00**
- 낙상 1건이면 **17.8배**, 2건이면 **8.2배** 차이가 나야 80 % 검정력으로 검출된다
- **낙상 한 자리 수 arm 쌍 중 유의한 것 0개**
- ✅ 정확도 축은 seed 복제 SD **1.95 %** ⇒ **상대 5.4 % 미만은 노이즈**

---

## 7. 지금 신뢰 가능한 것 / 아닌 것

**신뢰 가능**: 관절 매핑 · `motor_state_serial`=관절각 · sim↔deploy 순서 · 타이밍 실측
(39.6 Hz / 1.51 Hz / 185 Hz) · 발목 roll 이 유일 이상 관절 · **명령이 아니라 응답** ·
실기 토크가 sim 클램프 초과 · `act5=−1.0` · 자산 삼각부등식 위반 · 정확도 축 재현성(1.09 %)

**신뢰 불가**: 발목 roll **주파수**(4.73도 11~12도 확정 불가) · 낙상 배수 주장(검정력 없음) ·
`.select` 낙상 수 · 정확도 median 을 **가로질러** 비교하는 모든 순위 · `v7_extras` 스냅샷 열 ·
2026-08-09 이전 리포트의 채점 물리(`asset` 지문이 124개 중 1개에만 있다)

---

## 8. 이 분석이 배포에 주는 것 (권고는 MIDREPORT 답신 절 참조)

1. ⭐ **4발자국을 지금 시도해라** — `ibatch` §8-59~§8-63 에 걸음 언급 **0건**.
   네 두 수정(tilt → 1.4°, 재진입 1.6 → 0.16 rad) 이후 **아무도 안 걸어 봤다.** 4발자국 = **1.3초**
2. **판별 측정 셋** (전부 걷지 않고 가능): 정지 무데시메이션 로깅 /
   `motor_state_parallel[14,15,20,21]` 동시 로깅 / 손으로 발목 왕복
3. **`dof_target` 클램프 ±0.40** — `act5=−1.0` 이 이미 근거를 줬다
4. `Goal_Pose_E0.yaml:11-13` **주석 수정**(md5 가 기각한 값을 아직 말한다)
5. ⛔ **정확도 cm 로 체크포인트를 고르지 마라** — §6-c
