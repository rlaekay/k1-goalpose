# 학습 세션 앞 인계 — 2026-08-05 실기·MuJoCo 라운드

작성: 배포/검증 세션. 이 문서는 **학습 쪽이 지금 돌리는 것을 바꿔야 하는 내용**을 담는다.
읽었으면 맨 아래 **확인(ACK)** 을 채워서 커밋해 달라. 그걸로 전달 여부를 확인한다.

---

## 0. 지금 당장 — ⛔ 도는 학습 두 개가 틀린 발 위에 있다

`M3_scratch_phase`(GPU 0)와 `M4_scratch_phasefree`(GPU 1)가 각 10000 iteration,
합쳐 약 11시간 예정으로 돌고 있다. 둘 다 `asset.file`이
`K1_locomotion_armsdown_footfix2.urdf`인데 **footfix2는 틀린 리비전이다.**

**근거 (오늘 로봇에 직접 붙어 확인)**:
로봇의 `~/Workspace/test_deploy/tasks/scratch_curriculum/assets/K1_locomotion.urdf`

| | 로봇(실물) | 우리 학습 | footfix (내 원본) | footfix2 (내 "수정") |
|---|---|---|---|---|
| 총 질량 | **19.666 kg** | 18.714 | — | 18.714 |
| 발 질량 | **0.4940** | 0.38305 | **0.4940** ✅ | 0.38305 ⛔ |
| 발 ixx/iyy/izz | **0.000261 / 0.001170 / 0.001213** | 0.002020 / 0.007410 / 0.000785 ⛔ | **동일** ✅ | 0.000202/0.000741/0.000785 ⛔ |

**즉 `footfix.urdf`(원본)가 실물이고 `footfix2`가 틀렸다.** 내가 어제 footfix를
"다른 리비전이라 폐기"했는데, 그 "다른 리비전"이 실물이었다. footfix2는 우리 저장소의
낡은 MJCF에서 만든 것이라 같은 낡은 계보다.

**제안**: M3/M4를 죽이고 `footfix.urdf`로 재시작. 더 나은 것은 **자산 전체 교체**다 --
차이가 발뿐이 아니다.

### 자산 전수 대조 (로봇 − 학습)

- **링크 22개 전부 로봇 쪽이 무겁다.** 총 **+0.952 kg (+5.1%)**
- 로봇 값은 `0.3 / 0.7 / 0.5 / 1.65 / 6.5 / 0.494` 같은 **깔끔한 수** = 실측·사양.
  우리 값은 `0.2973 / 0.6139 / 1.5591 / 6.3921 / 0.3831` = **CAD 원본.**
  불가능한 발 관성(삼각부등식 2.64배 위반)도 CAD export 버그로 설명된다.
- `Left/Right_Hip_Pitch` 원점 **z −15 mm** (로봇 엉덩이가 더 낮다)
- **Trunk CoM은 완전히 동일** (−0.0043, −0.0007, 0.0657)
- 전신 CoM x 차이는 **+0.66 mm** — 발 반길이 80 mm 대비 무의미

**MuJoCo는 우리 학습 자산을 로드조차 거부한다**:
`ValueError: inertia must satisfy A + B >= C` (`left_foot_link`).
Isaac Gym은 물리적으로 불가능한 강체를 조용히 받아준다.

---

## 1. 오늘 죽은 가설 넷 — 다시 파지 말 것

| 가설 | 반증 방법 | 수치 |
|---|---|---|
| **토크 상한 불일치** | MuJoCo A/B | 벤더(45/30/30/45/20/20) vs 학습(30/20/20/40/20/15): 과제오차 **6.76 vs 6.70**, 낙상 **둘 다 0** |
| **배포측 저역통과 필터** | MuJoCo | `deploy_goal_pose.py:1273`의 τ 8.9 ms 필터를 넣어도 **6.73 / 낙상 0** |
| **발 관성이 실기 실패의 원인** | 사용자 논리 + MuJoCo | MuJoCo는 **정상 발**로 돌았고 정책이 걸었다. 발이 원인이면 거기서 터졌어야 한다 |
| **관측 지연** | 실기 실측 | 무너짐 25–30 ms vs 실측 **median 0.99 ms / p99 2.12 ms** — 25배 여유 |

⚠️ 발 관성은 **실재하는 결함이고 고쳐야 하지만**(§0), **실기 세 걸음의 원인은 아니다.**
두 문장을 섞지 말 것.

---

## 2. MuJoCo 교차검증 — 핵심 결과

**정책은 강체 동역학 문제가 없다.** 다른 엔진·다른 접촉모델·정상 발·자유로운 팔에서:

| 조건 | 구간 | 낙상 | 과제오차 |
|---|---|---|---|
| Isaac Gym (기준, stance10@200) | 4,647 | 2 | **3.86 cm** |
| MuJoCo 벤더 토크 | 72 | **0** | 6.76 |
| MuJoCo 학습 토크 | 73 | **0** | 6.70 |
| MuJoCo + 배포 필터 | 69 | **0** | 6.73 |
| MuJoCo 연속보행 60 s ×2 | — | **0** | — |

⛔ **아직 결론 안 난 것**: 왜 과제오차가 **75% 나쁜가**(6.76 vs 3.86).
후보 셋이 분리 안 됐다 — 발 관성(MuJoCo가 정상값), 팔 자유도(MuJoCo는 22관절 PD /
Isaac은 팔 고정), 엔진·접촉모델. **낙상은 0인데 정밀도만 나쁘다** = 균형은 이전되고
정밀도는 안 이전된다. **실기에서 3.86 cm를 기대하면 안 된다.**

실행: `python play_mujoco_goalpose.py --duration 180 --policy <pt>`

---

## 3. 감지 열화 스윕 — 랜덤화 폭을 정하는 근거

정책이 읽는 값만 열화시키고 물리·낙상 판정은 참값. 연속보행 60초, 낙상/분:

| 열화 | 학습에 있는 값 | 무너지는 지점 | 여유 |
|---|---|---|---|
| IMU 기울기 잡음 | std 0.01 (≈0.57°) | 5° 무사 → **10°: 6/분** | **17배** |
| IMU 기울기 **바이어스** | **없음** | **10°까지 전부 0** | 큼 |
| 자이로 잡음 | std 0.1 rad/s | 0.5 무사 → **1.0: 11/분** | **10배** |
| `dof_vel` 잡음 | std 0.1 rad/s | 0.5 무사 → **1.0: 2/분, 3.0: 22/분** | **10배** |
| 관측 지연 | **0 ms** | 10 무사 → **20: 2/분, 30: 34/분, 40: 49/분** | 실측 1 ms라 무관 |

**함의 둘**:
- **영점 드리프트·IMU 정렬 오차는 원인이 아니다.** 바이어스 10°까지 낙상 0인데,
  학습에 바이어스 항이 아예 없는데도 그렇다. → **jointcal 2×2 잔여 셀은 취소.**
- 나머지 채널도 10–17배 여유다. **감지 쪽 랜덤화를 넓히는 것은 우선순위가 낮다.**

---

## 4. ⛔ 외력 모델에 물리적으로 불가능한 결함이 있다 — 아직 안 고쳐졌다

[`envs/K1/goal_pose_v7.py:990`](htwk-gym/envs/K1/goal_pose_v7.py:990):

```python
ang = torch_rand_float(-np.pi, np.pi, (k, 1))
pushing_forces[fire, base_indice, 0] = fmag * cos(ang)
pushing_forces[fire, base_indice, 1] = fmag * sin(ang)
tang = torch_rand_float(-1.0, 1.0, (k, 3), ...)      # 힘과 무관한 난수
pushing_torques[fire, base_indice, :] = tang/|tang| * tmag
```

| 결함 | 내용 | 고치는 법 |
|---|---|---|
| ① 수직 성분 | `[:, 2]`에 아무것도 안 씀 → **모든 밀침이 순수 수평** | 3D 방향으로 |
| ② 작용점 | **항상 몸통(`base_indice`)** | 어깨·팔·발 링크로 확장 |
| ③ **토크 상관** | ⛔ **토크가 힘과 독립 난수.** 실제 편심 충돌은 `τ = r × F`라 토크⊥힘 | 작용점 `r`을 뽑고 `τ = r × F`로 계산 |

**③이 가장 나쁘다 — 물리적으로 존재할 수 없는 외란을 학습시키고 있다.**

그리고 `force_ab`(12셀, 27,672구간)에서 held-out 외력의 판별력은 **p = 0.15**로 없었다.
**"정책이 강인하다"가 아니라 "이 외란 모델이 애초에 못 흔든다"일 수 있다.**
`M2_forcewide`는 **크기만** 40→200 N으로 넓혔고 ①②③은 안 고쳤다 —
그래서 M2가 좋아도 "크기 문제였다"까지만 말한다.

---

## 5. 학습 방법론 — 오늘 확정된 것

| | 값 | 근거 |
|---|---|---|
| 학습 속도 | **2.04 s/iteration** (4096 envs, A6000) | 12,000 구간 median |
| **봉우리** | **iteration 1250** (정식 프로토콜). 500–2000이 고원 | L1_long 전 체크포인트 |
| 250 iteration | **4.4 cm — 나쁘다.** 더 짧게 가는 건 답이 아니다 | 같은 실행 |
| 12000 iteration | 0.0396 / 낙상 **63** (500에서 0.0278 / 1) | L4 완주 |
| 외력 A/B | **p = 0.15, 판별력 없음** | force_ab 12셀 |
| phase-free | **5.1 cm**, heading 2.3° (위상기반 3.7 / 1.4) | L3 it 500, 정식 프로토콜 |
| 판정 단축 | 20 s 단계가 120 s와 **1위 일치 (2/2)** → `--fast` | L1peak, L3peak |

⚠️ **`--fast`의 해상도 한계**: 같은 체크포인트의 과제오차가 20 s↔120 s에서 0.2–0.3 cm
움직인다. **약 0.4 cm 이상 차이만 가릴 수 있다.** arm 사이 비교엔 충분하고,
한 arm 안 체크포인트 고르기엔 부족하다(그런데 그 구간은 어느 걸 골라도 같다).

⚠️ **`--tail_frac` 기본 0.6의 도움말이 "early checkpoints are never best"라고 단언하는데
거짓이다.** L1_long의 봉우리는 1250이었고 마지막 2542가 아니었다. 도움말은 고쳤다.

⚠️ **warm-start 미세조정과 from-scratch를 같은 말로 쓰지 말 것.** 우리 봉우리 1250은
"이미 걷는 정책을 42분 다듬은" 값이지 "15분에 걷게 됐다"가 아니다. Booster 공식
T1.yaml은 4096 envs / **10000 iteration을 처음부터** 돈다.

---

## 6. 남들의 레시피 — 우리는 이미 Booster 것을 쓰고 있다

[Booster 공식 T1.yaml](https://github.com/BoosterRobotics/booster_gym)의 `randomization`이
우리 base config와 **값까지 동일**하다: `dof_stiffness/damping [0.95,1.05]`,
`dof_friction [0,2]`, `friction [0.1,2]`, `compliance [0.5,1.5]`, `restitution [0.1,0.9]`,
`base_com [-0.1,0.1]`, `base_mass [0.8,1.2]`, `other_com/mass` 전부 같다.
우리가 한 것은 `base_com`을 ±0.025로 좁히고(K1 충돌 상자에 맞춰)
`dof_stiffness`를 ±15%로 넓힌 것뿐이다.

**갈리는 곳 하나 — PD 게인**: Booster T1은 Hip/Knee **200 / damping 5**,
우리 K1은 **100 / 2**. 정규화 감쇠 `kd/√kp`로 T1 0.354 vs 우리 0.200(**43% 덜 감쇠**).
다만 htwk-gym의 K1 태스크 8개가 전부 100/2라 이건 K1 값이지 실수는 아니다.
실기 링잉을 의심하면 여기가 한 칸 올려볼 자리다.

---

## 7. 내가 오늘 틀린 것 — 물려받지 말 것

1. **"sim은 정지 자세를 지탱해 본 적이 없다"** — **거짓.** `goal_categories.stand: 0.1`로
   목표의 10%가 정지 범주고, `_reward_stand_posture`(scale −1.0, 반경 0.3 m)가 있으며
   stand 목표는 클럭을 얼린다. 커밋 `ee88e5a`가 이미 자기교정했다.
2. **"기본 자세가 정적으로 불안정하다"를 큰 결론으로 올린 것** — 관측은 `b`와 `r`
   **사이**에서 났다. 거기서는 정책이 아예 안 돈다(prepare 게인으로 자세를 붙들 뿐,
   균형 제어 없음). **정책이 그 자세를 잡을 수 있는지는 아직 아무도 안 봤다.**
3. **"17분이면 된다"** — from-scratch 논문과 warm-start 미세조정을 겹쳐 놓았다.
4. **"낙상 2배"** — 0.12% vs 0.06%를 그렇게 부른 것은 오도다. **sim 낙상률은 실기를
   예측하지 못한다**(sim 0.06%인데 실기는 세 걸음).
5. **`footfix2`** — §0. 실물이 아닌 리비전으로 "고쳤다".
6. **순기구학 CoM 계산** — 평행 발목 링크 때문에 직렬 체인으로 안 풀린다.
   몸통 pitch −34°, 발이 몸 앞 23 cm라는 값이 나왔다. **그 계산에 기반한 CoM 주장은
   전부 무효.** CoM은 실측(`tools/probe_com_from_ankle.py`)으로만 닫힌다.

---

## 8. 열린 항목

| 항목 | 상태 |
|---|---|
| M1_footfix / M2_forcewide / M5_footfix2 평가 | `--fast`로 **실행 중** (`logs/mfast/`) |
| MuJoCo 정확도 격차 6.76 vs 3.86의 원인 분리 | 미착수 |
| 외력 ①②③ 수정 | 미착수 |
| 평행 발목 링크 | 어느 sim 자산에도 없음. 정량화 안 됨 |
| `base_height_target` 0.55가 도달 불가(최대 0.5227), 실기 0.58이 나았음 | 미해결 |
| I3_rough 지형 | 미완 |
| jointcal 2×2 | **취소** (§3) |

---

## 확인 (ACK)

읽었으면 아래를 채우고 커밋해 달라. 비어 있으면 전달 안 된 것으로 본다.

```
읽은 세션:
읽은 날짜:
§0(M3/M4가 틀린 발)에 대한 조치:
    [ ] 죽이고 footfix로 재시작
    [ ] 자산 전체를 로봇 리비전으로 교체 후 재시작
    [ ] 그대로 둔다 (이유:                                   )
동의하지 않는 항목이 있으면 번호와 이유:
```
